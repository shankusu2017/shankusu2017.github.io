<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>愿星光伴你左右</title>
  
  
  <link href="http://www.shankusu.me/atom.xml" rel="self"/>
  
  <link href="http://www.shankusu.me/"/>
  <updated>2021-09-03T09:44:25.373Z</updated>
  <id>http://www.shankusu.me/</id>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>分布式系统的Raft算法</title>
    <link href="http://www.shankusu.me/%E5%88%86%E5%B8%83%E5%BC%8F/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F%E7%9A%84Raft%E7%AE%97%E6%B3%95/"/>
    <id>http://www.shankusu.me/%E5%88%86%E5%B8%83%E5%BC%8F/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F%E7%9A%84Raft%E7%AE%97%E6%B3%95/</id>
    <published>2021-09-03T09:38:11.000Z</published>
    <updated>2021-09-03T09:44:25.373Z</updated>
    
    <content type="html"><![CDATA[<p>　　过去, Paxos一直是分布式协议的标准，但是Paxos难于理解，更难以实现，Google的分布式锁系统Chubby作为Paxos实现曾经遭遇到很多坑。</p><p>　　来自Stanford的新的分布式协议研究称为Raft，它是一个为真实世界应用建立的协议，主要注重协议的落地性和可理解性。</p><p>　　在了解Raft之前，我们先了解Consensus一致性这个概念，它是指多个服务器在状态达成一致，但是在一个分布式系统中，因为各种意外可能，有的服务器可能会崩溃或变得不可靠，它就不能和其他服务器达成一致状态。这样就需要一种Consensus协议，一致性协议是为了确保容错性，也就是即使系统中有一两个服务器当机，也不会影响其处理过程。</p><p>　　为了以容错方式达成一致，我们不可能要求所有服务器100%都达成一致状态，只要超过半数的大多数服务器达成一致就可以了，假设有N台服务器，N/2 +1 就超过半数，代表大多数了。</p><p>　　Paxos和Raft都是为了实现Consensus一致性这个目标，这个过程如同选举一样，参选者需要说服大多数选民(服务器)投票给他，一旦选定后就跟随其操作。Paxos和Raft的区别在于选举的具体过程不同。</p><p>　　在Raft中，任何时候一个服务器可以扮演下面角色之一：</p><ol><li>Leader: 处理所有客户端交互，日志复制等，一般一次只有一个Leader.</li><li>Follower: 类似选民，完全被动</li><li>Candidate候选人: 类似Proposer律师，可以被选为一个新的领导人。</li></ol><p>Raft阶段分为两个，首先是选举过程，然后在选举出来的领导人带领进行正常操作，比如日志复制等。下面用图示展示这个过程：</p><ol><li>任何一个服务器都可以成为一个候选者Candidate，它向其他服务器Follower发出要求选举自己的请求：</li></ol><p><img src="raft1.png" alt="img"></p><ol start="2"><li>其他服务器同意了，发出OK。</li></ol><p><img src="raft2.png" alt="img"></p><p>注意如果在这个过程中，有一个Follower当机，没有收到请求选举的要求，因此候选者可以自己选自己，只要达到N/2 + 1 的大多数票，候选人还是可以成为Leader的。</p><ol start="3"><li>这样这个候选者就成为了Leader领导人，它可以向选民也就是Follower们发出指令，比如进行日志复制。</li></ol><p><img src="raft3.png" alt="img"></p><ol start="4"><li>以后通过心跳进行日志复制的通知</li></ol><p><img src="raft4.png" alt="img"></p><ol start="5"><li>如果一旦这个Leader当机崩溃了，那么Follower中有一个成为候选者，发出邀票选举。</li></ol><p><img src="raft5.png" alt="img"></p><ol start="6"><li>Follower同意后，其成为Leader，继续承担日志复制等指导工作：</li></ol><p><img src="raft6.png" alt="img"></p><p>值得注意的是，整个选举过程是有一个时间限制的，如下图：</p><p><img src="raft7.png" alt="img"></p><p>　　Splite Vote是因为如果同时有两个候选人向大家邀票，这时通过类似加时赛来解决，两个候选者在一段timeout比如300ms互相不服气的等待以后，因为双方得到的票数是一样的，一半对一半，那么在300ms以后，再由这两个候选者发出邀票，这时同时的概率大大降低，那么首先发出邀票的的候选者得到了大多数同意，成为领导者Leader，而另外一个候选者后来发出邀票时，那些Follower选民已经投票给第一个候选者，不能再投票给它，它就成为落选者了，最后这个落选者也成为普通Follower一员了。</p><h2 id="日志复制"><a href="#日志复制" class="headerlink" title="日志复制"></a>日志复制</h2><p>　　下面以日志复制为例子说明Raft算法，假设Leader领导人已经选出，这时客户端发出增加一个日志的要求，比如日志是”sally”：</p><p><img src="raft8.png" alt="img"></p><ol start="2"><li>Leader要求Followe遵从他的指令，都将这个新的日志内容追加到他们各自日志中：</li></ol><p><img src="raft9.png" alt="img"></p><p>3.大多数follower服务器将日志写入磁盘文件后，确认追加成功，发出Commited Ok:</p><p><img src="raft10.png" alt="img"></p><ol start="4"><li>在下一个心跳heartbeat中，Leader会通知所有Follwer更新commited 项目。</li></ol><p>对于每个新的日志记录，重复上述过程。</p><p>如果在这一过程中，发生了网络分区或者网络通信故障，使得Leader不能访问大多数Follwers了，那么Leader只能正常更新它能访问的那些Follower服务器，而大多数的服务器Follower因为没有了Leader，他们重新选举一个候选者作为Leader，然后这个Leader作为代表于外界打交道，如果外界要求其添加新的日志，这个新的Leader就按上述步骤通知大多数Followers，如果这时网络故障修复了，那么原先的Leader就变成Follower，在失联阶段这个老Leader的任何更新都不能算commit，都回滚，接受新的Leader的新的更新。</p><p>总结：目前几乎所有语言都已经有支持Raft算法的库包，具体可参考：<a href="http://raftconsensus.github.io/" rel="external nofollow noopener noreferrer" target="_blank">raftconsensus.github.io</a></p><p><a href="http://thesecretlivesofdata.com/raft/" rel="external nofollow noopener noreferrer" target="_blank">英文动画演示Raft</a></p><h3 id="CAP定理"><a href="#CAP定理" class="headerlink" title="CAP定理"></a><a href="https://www.jdon.com/37625" rel="external nofollow noopener noreferrer" target="_blank">CAP定理</a></h3><h3 id="分布式Paxos算法"><a href="#分布式Paxos算法" class="headerlink" title="分布式Paxos算法"></a><a href="https://www.jdon.com/artichect/paxos.html" rel="external nofollow noopener noreferrer" target="_blank">分布式Paxos算法</a></h3><h3 id="ZooKeeper在服务发现中应用"><a href="#ZooKeeper在服务发现中应用" class="headerlink" title="ZooKeeper在服务发现中应用"></a><a href="http://www.jdon.com/artichect/zookeeper.html" rel="external nofollow noopener noreferrer" target="_blank">ZooKeeper在服务发现中应用</a></h3><h3 id="分布式事务"><a href="#分布式事务" class="headerlink" title="分布式事务"></a><a href="https://www.jdon.com/transaction.html" rel="external nofollow noopener noreferrer" target="_blank">分布式事务</a></h3>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;　　过去, Paxos一直是分布式协议的标准，但是Paxos难于理解，更难以实现，Google的分布式锁系统Chubby作为Paxos实现曾经遭遇到很多坑。&lt;/p&gt;
&lt;p&gt;　　来自Stanford的新的分布式协议研究称为Raft，它是一个为真实世界应用建立的协议，主要注重协</summary>
      
    
    
    
    <category term="redis" scheme="http://www.shankusu.me/categories/redis/"/>
    
    <category term="分布式" scheme="http://www.shankusu.me/categories/分布式/"/>
    
    
    <category term="分布式" scheme="http://www.shankusu.me/tags/分布式/"/>
    
    <category term="raft" scheme="http://www.shankusu.me/tags/raft/"/>
    
  </entry>
  
  <entry>
    <title>股票投资建议</title>
    <link href="http://www.shankusu.me/%E9%87%91%E8%9E%8D/%E5%BB%BA%E8%AE%AE/"/>
    <id>http://www.shankusu.me/%E9%87%91%E8%9E%8D/%E5%BB%BA%E8%AE%AE/</id>
    <published>2021-09-03T04:47:36.000Z</published>
    <updated>2021-09-03T05:07:10.645Z</updated>
    
    <content type="html"><![CDATA[<h3 id="资金分配"><a href="#资金分配" class="headerlink" title="资金分配"></a>资金分配</h3><p>短期，中期，长期  1：1：1</p><h4 id="投资参考数据"><a href="#投资参考数据" class="headerlink" title="投资参考数据"></a>投资参考数据</h4><ul><li>长投，分析企业</li><li>中投，看板</li><li>短投，看K线</li><li>不要用短投的思维，去看长投，也不要用长投思维去看短投。</li></ul><h4 id="企业护城河"><a href="#企业护城河" class="headerlink" title="企业护城河"></a>企业护城河</h4><p>&nbsp;&nbsp;&nbsp;&nbsp;多维度分析一个企业，应该成本优势、无形资产、转换成本、网络效应、有效规模。这五种来看。组成了公司发展的护城河</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h3 id=&quot;资金分配&quot;&gt;&lt;a href=&quot;#资金分配&quot; class=&quot;headerlink&quot; title=&quot;资金分配&quot;&gt;&lt;/a&gt;资金分配&lt;/h3&gt;&lt;p&gt;短期，中期，长期  1：1：1&lt;/p&gt;
&lt;h4 id=&quot;投资参考数据&quot;&gt;&lt;a href=&quot;#投资参考数据&quot; class=&quot;h</summary>
      
    
    
    
    <category term="金融" scheme="http://www.shankusu.me/categories/金融/"/>
    
    <category term="股票" scheme="http://www.shankusu.me/categories/金融/股票/"/>
    
    
  </entry>
  
  <entry>
    <title>More Redis internals: Tracing a GET &amp; SET</title>
    <link href="http://www.shankusu.me/redis/more-redis-internals-racing-get-set/"/>
    <id>http://www.shankusu.me/redis/more-redis-internals-racing-get-set/</id>
    <published>2021-09-01T17:24:09.000Z</published>
    <updated>2021-09-01T17:28:07.005Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://www.pauladamsmith.com/blog/2011/03/redis_get_set.html" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p><p>MARCH 10, 2011 </p><p><img src="https://www.pauladamsmith.com/images/redis.png" alt="img"></p><p>In my <a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html" rel="external nofollow noopener noreferrer" target="_blank">previous article</a>, I took a superficial look at how Redis starts up and prepares itself to process commands. In this article, I’ll follow a <code>GET</code> and a <code>SET</code> command as they move from client through the server and back. The <code>GET</code> will be for a key that doesn’t exist, and the <code>SET</code> will set that key. Then I’ll look quickly at a subsequent <code>GET</code> and how it differs.</p><p>As before, I’m exploring Redis with the source code open in my editor and indexed with a <code>tags</code> file, and the Redis server running under <code>gdb</code> in another terminal.</p><p>Caveat: this article was written against the codebase of Redis 2.2.1. With respect to my previous article, for a list of what has changed in Redis since I wrote it, see this <a href="http://news.ycombinator.com/item?id=2301804" rel="external nofollow noopener noreferrer" target="_blank">comment on HN</a>.</p><p><strong>Edit:</strong> I made two minor changes based on feedback—Redis keys are not Redis objects, they are <code>sds</code> strings; and you don’t have to hack the Makefile to compile without optimizations.</p><h1 id="GET"><a href="#GET" class="headerlink" title="GET"></a>GET</h1><p>Let’s execute the command <code>get users:1234</code> on Redis.</p><h2 id="Preparing"><a href="#Preparing" class="headerlink" title="Preparing"></a>Preparing</h2><p>If you inspect certain variables under <code>gdb</code>, you might not get what you want. Instead you see a message like “.” This is because the compiler has optimized the machine code in such a fashion that the portion of memory you wanted to look at that was never used, at least for the variable under inspection. To make stepping through the code and inspecting a little easier, we make sure that Redis is not compiled with optimizations. You can do this by either building Redis with the following invocation:</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">make noopt</span></span><br></pre></td></tr></table></figure><p>or by setting an environment variable:</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">OPTIMIZATION</span>= make</span><br></pre></td></tr></table></figure><h2 id="Sending-the-command-from-the-client"><a href="#Sending-the-command-from-the-client" class="headerlink" title="Sending the command from the client"></a>Sending the command from the client</h2><p>If we look at the <code>repl</code> loop of redis-cli, we see that it uses the <code>linenoise</code> library to split the arguments of the command. It dispatches on the first argument. The loop checks for client commands that aren’t processed as a command by the Redis server, like <code>exit</code>/<code>quit</code>, <code>clear</code> (to clear the screen), and <code>connect</code> (which is a way to connect to a specified Redis server on host port, instead of the default host of 127.0.0.1 and port of 6379.</p><p>Any other argument is considered the name of a command to send to the server. The remaining arguments are the arugments for that command.</p><p>We’re trying to get the on-the-wire representation of the <code>get users:1234</code> command. redis-cli uses a <code>redisContext</code> struct to encapsulate the state of the connection to the server, as well as the output buffer where the command in the form of the Redis protocol is written for sending to the server. We know from reading the source of hiredis (the Redis C client library that the redis-cli program is built on) that the <code>obuf</code> field is where the raw command is stored:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta"># hiredis.h:107</span></span><br><span class="line"><span class="comment">/* Context for a connection to Redis */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">redisContext</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> fd;</span><br><span class="line">    <span class="keyword">int</span> flags;</span><br><span class="line">    <span class="keyword">char</span> *obuf; <span class="comment">/* Write buffer */</span></span><br><span class="line">    <span class="keyword">int</span> err; <span class="comment">/* Error flags, 0 when there is no error */</span></span><br><span class="line">    <span class="keyword">char</span> *errstr; <span class="comment">/* String representation of error when applicable */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Function set for reply buildup and reply reader */</span></span><br><span class="line">    redisReplyObjectFunctions *fn;</span><br><span class="line">    <span class="keyword">void</span> *reader;</span><br><span class="line">&#125; redisContext;</span><br></pre></td></tr></table></figure><p>If we set a breakpoint on <code>cliReadReply</code>, we can inspect the output buffer and see exactly how the command looks as a bytestring bound for the server.</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">client $ gdb src/redis-cli</span><br><span class="line">(gdb) b cliReadReply</span><br><span class="line">(gdb) run</span><br><span class="line">Starting program: /home/paul/src/redis-2.2.0-RC2/src/redis-cli </span><br><span class="line">Reading symbols <span class="keyword">for</span> shared libraries +. done</span><br><span class="line">redis&gt; <span class="builtin-name">get</span> users:1234</span><br><span class="line"></span><br><span class="line">Breakpoint 1, cliReadReply (<span class="attribute">output_raw_strings</span>=0) at redis-cli.c:421</span><br><span class="line">421         <span class="keyword">if</span> (redisGetReply(context,&amp;_reply) != REDIS_OK) &#123;</span><br><span class="line">(gdb) p context-&gt;obuf </span><br><span class="line"><span class="variable">$1</span> = 0x100102428 <span class="string">"*2\r\n<span class="variable">$3</span>\r\nget\r\n<span class="variable">$10</span>\r\nusers:1234\r\n"</span></span><br></pre></td></tr></table></figure><p>We see that the Redis command <code>get users:1234</code> as entered in our client is translated to <code>*2\r\n$3\r\nget\r\n$10\r\nusers:1234\r\n</code>. Any Redis client is going to convert our command expressed in its respective syntax to the same on-the-wire format. So in Python:</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;</span>&gt; redis_client.get(<span class="string">'users:1234'</span>)</span><br></pre></td></tr></table></figure><p>will send the same <code>*2\r\n$3\r\nget\r\n$10\r\nusers:1234\r\n</code> bytestring to the server.</p><p>Let’s print that bytestring to screen and render those `\r\n’s as line feeds so we can see an expanded view and get a better look at the protocol.</p><figure class="highlight gams"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">*2</span></span><br><span class="line"><span class="meta"><span class="meta-keyword">$3</span></span></span><br><span class="line">get</span><br><span class="line"><span class="meta"><span class="meta-keyword">$10</span></span></span><br><span class="line">users:<span class="number">1234</span></span><br></pre></td></tr></table></figure><p>The first bit is <code>*2</code>, which tells us that the arity of the command, including the command name, is 2. That is, the server should expect two arguments to follow.</p><p>The next bit is <code>$3</code>, which means that the length of the first argument in bytes is 3. The argument itself follows, our command name, <code>get</code>.</p><p>The next bit after that is <code>$10</code>, so the length in bytes of the second argument is 10. Our one and only argument to the command is next, <code>users:1234</code>, the key we are trying to <code>get</code>.</p><h2 id="Receiving-the-command-on-the-server"><a href="#Receiving-the-command-on-the-server" class="headerlink" title="Receiving the command on the server"></a>Receiving the command on the server</h2><p>If you remember from the last article, <code>readQueryFromClient</code> is a good place to set a breakpoint in your debugger on the server side for inspecting an inbound client command.</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">server $ gdb src/redis-server</span><br><span class="line">(gdb) b readQueryFromClient</span><br><span class="line">Breakpoint 1 at 0x100011445: file networking.c, line 882.</span><br><span class="line">(gdb) <span class="builtin-name">run</span> redis.conf</span><br><span class="line">Starting program: /home/paul/src/redis-2.2.0-RC2/src/redis-server redis.conf</span><br><span class="line">Reading symbols <span class="keyword">for</span> shared libraries +. done</span><br><span class="line">[63700] 01 Mar 11:04:40 *<span class="built_in"> Server </span>started, Redis version 2.2.1</span><br><span class="line">[63700] 01 Mar 11:04:40 * The<span class="built_in"> server </span>is now ready <span class="keyword">to</span> accept connections on<span class="built_in"> port </span>6379</span><br></pre></td></tr></table></figure><p>Now back in the terminal with the client running in the debugger, continue to send the command to the server, which will stop at the breakpoint we set.</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># client</span></span><br><span class="line">(gdb) c</span><br><span class="line">Continuing.</span><br><span class="line"></span><br><span class="line"><span class="comment"># server</span></span><br><span class="line">Breakpoint 1, readQueryFromClient (<span class="attribute">el</span>=0x100200000, <span class="attribute">fd</span>=5, <span class="attribute">privdata</span>=0x100804e00, <span class="attribute">mask</span>=1) at networking.c:801</span><br><span class="line">801     void readQueryFromClient(aeEventLoop <span class="number">*e</span>l, int fd, void *privdata, int mask) &#123;</span><br></pre></td></tr></table></figure><p>Let’s step to the following line:</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># src/networking.c:808</span></span><br><span class="line"><span class="attr">nread</span> = read(fd, buf, REDIS_IOBUF_LEN)<span class="comment">;</span></span><br></pre></td></tr></table></figure><p>If we step to here in our debugger, we see that the server read 30 bytes. If you count the number of bytes in our Redis protocol-encoded command, <code>*2\r\n$3\r\nget\r\n$10\r\nusers:1234\r\n</code>, you’ll see it’s 30. Just for good measure, let’s look at the 30 bytes beginning at the memory location pointed to by <code>buf</code>:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">(gdb)</span> <span class="string">p</span> <span class="string">nread</span></span><br><span class="line"><span class="number">30</span></span><br><span class="line"><span class="string">(gdb)</span> <span class="string">x/30cb</span> <span class="string">buf</span></span><br><span class="line"><span class="attr">0x7fff5fbfeaf0:</span> <span class="number">42</span> <span class="string">'*'</span>  <span class="number">50</span> <span class="string">'2'</span>  <span class="number">13</span> <span class="string">'\r'</span> <span class="number">10</span> <span class="string">'\n'</span> <span class="number">36</span> <span class="string">'$'</span>  <span class="number">51</span> <span class="string">'3'</span>  <span class="number">13</span> <span class="string">'\r'</span> <span class="number">10</span> <span class="string">'\n'</span></span><br><span class="line"><span class="attr">0x7fff5fbfeaf8:</span> <span class="number">71</span> <span class="string">'G'</span>  <span class="number">69</span> <span class="string">'E'</span>  <span class="number">84</span> <span class="string">'T'</span>  <span class="number">13</span> <span class="string">'\r'</span> <span class="number">10</span> <span class="string">'\n'</span> <span class="number">36</span> <span class="string">'$'</span>  <span class="number">49</span> <span class="string">'1'</span>  <span class="number">48</span> <span class="string">'0'</span></span><br><span class="line"><span class="attr">0x7fff5fbfeb00:</span> <span class="number">13</span> <span class="string">'\r'</span> <span class="number">10</span> <span class="string">'\n'</span> <span class="number">117</span> <span class="string">'u'</span> <span class="number">115</span> <span class="string">'s'</span> <span class="number">101</span> <span class="string">'e'</span> <span class="number">114</span> <span class="string">'r'</span> <span class="number">115</span> <span class="string">'s'</span> <span class="number">58</span> <span class="string">':'</span></span><br><span class="line"><span class="attr">0x7fff5fbfeb08:</span> <span class="number">49</span> <span class="string">'1'</span>  <span class="number">50</span> <span class="string">'2'</span>  <span class="number">51</span> <span class="string">'3'</span>  <span class="number">52</span> <span class="string">'4'</span>  <span class="number">13</span> <span class="string">'\r'</span> <span class="number">10</span> <span class="string">'\n'</span></span><br></pre></td></tr></table></figure><p>And we can see our whole command bytestring is there, byte by byte.</p><p>The server has now read in the entirety of our command in one step. (Because we had a relatively short command, one that fits inside a kernel buffer, and we are the only client on a loopback network device, this is the case, but it need not be. Since Redis is event-driven, this function, <code>readQueryFromClient</code>, is called whenever there are bytes from buffers to be read. If our command was particularly long, or there was a lot of network contention, the command may take more than one I/O event before it is fully read. For this reason, Redis builds up a buffer per client and appends bytes to it on each call to this function. It only proceeds with processing the command when it has been fully read. But we don’t need to consider this in our simple example, so we will proceed.)</p><p>We’re going to elide the processing of the input buffer. This is the point where the server takes the Redis protocol-encoded bytestring of our request and unpacks it into arguments on the client struct object. If you are interested in the details of that parsing, examine the function <code>processMultibulkBuffer</code> in <code>networking.c</code>. All we are interested in at this point is that the <code>argc</code> member of the client object is the number of command arguments (counting the command name itself) and <code>argv</code> is a pointer to the list of arguments.</p><p>The bit of code we care at this point is <code>processCommand</code>. The first thing the server does is look up the command in its command table (see “Setting up command table” in the previous article, but note that this lookup is now O(1), see the HN thread linked above). Assuming the command is found (which our <code>get</code> will be), the server will double-check that the arity of the command as defined in the command table matches the number of arguments received from the client (<code>c-&gt;argc</code>).</p><figure class="highlight xl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"># redis.c:<span class="number">998</span></span><br><span class="line"><span class="function"><span class="title">cmd</span> = lookupCommand(c-&gt;</span><span class="function"><span class="title">argv</span>[0]-&gt;</span>ptr);</span><br><span class="line"><span class="keyword">if</span> (!cmd) &#123;</span><br><span class="line">    addReplyErrorFormat(c,<span class="string">"unknown command '%s'"</span>,</span><br><span class="line">        (<span class="function"><span class="title">char</span>*)c-&gt;</span><span class="function"><span class="title">argv</span>[0]-&gt;</span>ptr);</span><br><span class="line">    return REDIS_OK;</span><br><span class="line">&#125; <span class="function"><span class="title">else</span> <span class="keyword">if</span> ((cmd-&gt;</span><span class="function"><span class="title">arity</span> &gt; 0 &amp;&amp; cmd-&gt;</span><span class="function"><span class="title">arity</span> != c-&gt;</span>argc) ||</span><br><span class="line">           (<span class="function"><span class="title">c</span>-&gt;</span><span class="function"><span class="title">argc</span> &lt; -cmd-&gt;</span>arity)) &#123;</span><br><span class="line">    addReplyErrorFormat(c,<span class="string">"wrong number of arguments for '%s' command"</span>,</span><br><span class="line">        <span class="function"><span class="title">cmd</span>-&gt;</span><span class="keyword">name</span>);</span><br><span class="line">    return REDIS_OK;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Skip down to the end of <code>processCommand</code>. Because our humble <code>get</code> is not a “multi” command like <code>mget</code>, <code>mset</code>, etc., it doesn’t require queue-like processing of the underlying multiple commands, so we go right to <code>call</code>, which is where our command is dispatched.</p><figure class="highlight xl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"># redis.c:<span class="number">953</span></span><br><span class="line">void call(redisClient *c, struct redisCommand *cmd) &#123;</span><br><span class="line">    long long dirty;</span><br><span class="line"></span><br><span class="line">    dirty = server.dirty;</span><br><span class="line">    <span class="function"><span class="title">cmd</span>-&gt;</span>proc(c);</span><br><span class="line">    dirty = server.dirty-dirty;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (server.appendonly &amp;&amp; dirty)</span><br><span class="line">        <span class="function"><span class="title">feedAppendOnlyFile</span>(cmd,c-&gt;</span><span class="function"><span class="title">db</span>-&gt;</span><span class="function"><span class="title">id</span>,c-&gt;</span><span class="function"><span class="title">argv</span>,c-&gt;</span>argc);</span><br><span class="line">    <span class="function"><span class="title">if</span> ((dirty || cmd-&gt;</span>flags &amp; REDIS_CMD_FORCE_REPLICATION) &amp;&amp;</span><br><span class="line">        listLength(server.slaves))</span><br><span class="line">        <span class="function"><span class="title">replicationFeedSlaves</span>(server.slaves,c-&gt;</span><span class="function"><span class="title">db</span>-&gt;</span><span class="function"><span class="title">id</span>,c-&gt;</span><span class="function"><span class="title">argv</span>,c-&gt;</span>argc);</span><br><span class="line">    <span class="keyword">if</span> (listLength(server.monitors))</span><br><span class="line">        <span class="function"><span class="title">replicationFeedMonitors</span>(server.monitors,c-&gt;</span><span class="function"><span class="title">db</span>-&gt;</span><span class="function"><span class="title">id</span>,c-&gt;</span><span class="function"><span class="title">argv</span>,c-&gt;</span>argc);</span><br><span class="line">    server.stat_numcommands++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Let’s focus on line 952, <code>cmd-&gt;proc(c);</code>. This is Redis’s dynamic dispatching of command function calling. Redis makes this clean and simple by encapsulating commands and giving all the actual underlying command functions the same function signature, taking our client object, which carries the payload of our command’s arguments. So we’re interested in looking into the details of the Redis command object and the actual function that will handle our <code>get</code>.</p><figure class="highlight tcl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># redis.h:504</span></span><br><span class="line">struct redisCommand &#123;</span><br><span class="line">    char *name;</span><br><span class="line">    redisCommandProc *<span class="keyword">proc</span>;</span><br><span class="line"><span class="title">    int</span> arity;</span><br><span class="line"><span class="title">    int</span> flags;</span><br><span class="line">    /*<span class="title"> Use</span> a<span class="title"> function</span> to<span class="title"> determine</span> which<span class="title"> keys</span> need<span class="title"> to</span> be<span class="title"> loaded</span></span><br><span class="line"><span class="title"></span>     *<span class="title"> in</span> the<span class="title"> background</span> prior<span class="title"> to</span> executing<span class="title"> this</span> command.<span class="title"> Takes</span> precedence</span><br><span class="line">     *<span class="title"> over</span> vm_firstkey<span class="title"> and</span> others,<span class="title"> ignored</span> when<span class="title"> NULL</span> */</span><br><span class="line"><span class="title">    redisVmPreloadProc</span> *vm_preload_proc;</span><br><span class="line">    /*<span class="title"> What</span> keys<span class="title"> should</span> be<span class="title"> loaded</span> in<span class="title"> background</span> when<span class="title"> calling</span> this<span class="title"> command?</span> */</span><br><span class="line"><span class="title">    int</span> vm_firstkey; /*<span class="title"> The</span> first<span class="title"> argument</span> that's<span class="title"> a</span> key (0 =<span class="title"> no</span> keys) */</span><br><span class="line"><span class="title">    int</span> vm_lastkey;  /*<span class="title"> THe</span> last<span class="title"> argument</span> that's<span class="title"> a</span> key */</span><br><span class="line"><span class="title">    int</span> vm_keystep;  /*<span class="title"> The</span> step<span class="title"> between</span> first<span class="title"> and</span> last<span class="title"> key</span> */</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>If we pop up to the top of <code>redis.c</code>, we see the definition of the Redis command table, and our <code>get</code> is the first entry.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta"># redis.c:71</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">redisCommand</span> <span class="title">readonlyCommandTable</span>[] = &#123;</span></span><br><span class="line">    &#123;<span class="string">"get"</span>,getCommand,<span class="number">2</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>&#125;,</span><br></pre></td></tr></table></figure><p><code>getCommand</code> is the function that does the actual work for our command. It’s a thin wrapper for <code>getGenericCommand</code>.</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"># t_string.<span class="built_in">c</span>:<span class="number">62</span></span><br><span class="line">int getGenericCommand(redisClient *<span class="built_in">c</span>) &#123;</span><br><span class="line">    robj *o;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((o = lookupKeyReadOrReply(<span class="built_in">c</span>,<span class="built_in">c</span>-&gt;argv[<span class="number">1</span>],shared.nullbulk)) == <span class="type">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="type">REDIS_OK</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (o-&gt;type != <span class="type">REDIS_STRING</span>) &#123;</span><br><span class="line">        addReply(<span class="built_in">c</span>,shared.wrongtypeerr);</span><br><span class="line">        <span class="keyword">return</span> <span class="type">REDIS_ERR</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        addReplyBulk(<span class="built_in">c</span>,o);</span><br><span class="line">        <span class="keyword">return</span> <span class="type">REDIS_OK</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void getCommand(redisClient *<span class="built_in">c</span>) &#123;</span><br><span class="line">    getGenericCommand(<span class="built_in">c</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The arguments to <code>lookupKeyReadOrReply</code> are the client object, the key <code>users:1234</code> we’re trying to look up, and an object, <code>shared.nullbulk</code> that will be the default reply to the client if the key is not found.</p><figure class="highlight gauss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta"># db.c:58</span></span><br><span class="line">robj *<span class="built_in">lookupKeyReadOrReply</span>(redisClient *c, robj *<span class="built_in">key</span>, robj *reply) &#123;</span><br><span class="line">    robj *o = <span class="built_in">lookupKeyRead</span>(c-&gt;db, <span class="built_in">key</span>);</span><br><span class="line">    <span class="keyword">if</span> (!o) <span class="built_in">addReply</span>(c,reply);</span><br><span class="line">    <span class="keyword">return</span> o;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>lookupKeyRead</code> is a thin wrapper for <code>lookupKey</code> that handles removing keys that have been set to expire.</p><p>Now we get to the heart of the <code>get</code> command – looking up the key in the database.</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"># db.c:<span class="number">9</span></span><br><span class="line">robj *lookupKey(redisDb *db, robj *key) &#123;</span><br><span class="line">    dictEntry *de = dictFind(db-&gt;dict,key-&gt;ptr);</span><br><span class="line">    <span class="keyword">if</span> (de) &#123;</span><br><span class="line">        robj *<span class="keyword">val</span> = dictGetEntryVal(de);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* Update the access time for the aging algorithm.</span></span><br><span class="line"><span class="comment">         * Don't do it if we have a saving child, as this will trigger</span></span><br><span class="line"><span class="comment">         * a copy on write madness. */</span></span><br><span class="line">        <span class="keyword">if</span> (server.bgsavechildpid == -<span class="number">1</span> &amp;&amp; server.bgrewritechildpid == -<span class="number">1</span>)</span><br><span class="line">            <span class="keyword">val</span>-&gt;lru = server.lruclock;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (server.vm_enabled) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="keyword">val</span>-&gt;storage == REDIS_VM_MEMORY ||</span><br><span class="line">                <span class="keyword">val</span>-&gt;storage == REDIS_VM_SWAPPING)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">/* If we were swapping the object out, cancel the operation */</span></span><br><span class="line">                <span class="keyword">if</span> (<span class="keyword">val</span>-&gt;storage == REDIS_VM_SWAPPING)</span><br><span class="line">                    vmCancelThreadedIOJob(<span class="keyword">val</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                int notify = (<span class="keyword">val</span>-&gt;storage == REDIS_VM_LOADING);</span><br><span class="line"></span><br><span class="line">                <span class="comment">/* Our value was swapped on disk. Bring it at home. */</span></span><br><span class="line">                redisAssert(<span class="keyword">val</span>-&gt;type == REDIS_VMPOINTER);</span><br><span class="line">                <span class="keyword">val</span> = vmLoadObject(<span class="keyword">val</span>);</span><br><span class="line">                dictGetEntryVal(de) = <span class="keyword">val</span>;</span><br><span class="line"></span><br><span class="line">                <span class="comment">/* Clients blocked by the VM subsystem may be waiting for</span></span><br><span class="line"><span class="comment">                 * this key... */</span></span><br><span class="line">                <span class="keyword">if</span> (notify) handleClientsBlockedOnSwappedKey(db,key);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        server.stat_keyspace_hits++;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">val</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        server.stat_keyspace_misses++;</span><br><span class="line">        <span class="keyword">return</span> NULL;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Redis uses its own hash table implementation to store keys and their values in memory. Inside the <code>db</code> object, the field <code>dict</code> is a pointer to the hash value for the current Redis database (remember there can be up to 16 separate databases in a single Redis server instance, indexed by number).</p><p>First, Redis calls <code>dictFind</code> with the database’s hash table and a pointer to the key’s bytestring. <code>dictFind</code> looks up the hash of the key in the table, using a standard algorithm that should be familiar to anyone who’s implemented a hash table (check out <code>dict.c</code> starting at line 391 if you’re interested, the table is an array with linked lists for colliding hashes).</p><p>If the key is found in the table, <code>dictFind</code> returns a pointer to the entry in the table. Otherwise, it returns <code>NULL</code>. Back in <code>lookupKey</code>, if the entry is not null, Redis retrieves the value (i.e., the Redis object our key references) from the hash table via <code>dictGetEntryVal</code> and takes care of a bit of bookkeeping for expiry and VM, if the key was found, and stats in either case (hits and misses). If the entry was <code>NULL</code>, then <code>lookupKey</code> also returns <code>NULL</code>; we’ll see how this is handled by Redis for a reply to the client when the key is not found, which is the case for us at this stage.</p><p>With the value of <code>lookupKey</code>, we’ll go back up the stack to our callers. Back to <code>lookupKeyReadOrReply</code>, we look at line 60:</p><figure class="highlight gcode"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr"># t_string.c:60</span></span><br><span class="line">        <span class="keyword">if</span> <span class="comment">(!o)</span> addReply<span class="comment">(c,reply)</span>;</span><br></pre></td></tr></table></figure><p>Since we got <code>NULL</code> from <code>lookupKey</code> this time, we call <code>addReply</code>. The value of <code>reply</code> here comes from the call in <code>getGenericCommand</code>, and it is <code>shared.nullbulk</code>. This field in the global struct object <code>shared</code> is initialize thusly:</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># redis.c:712</span></span><br><span class="line"><span class="attr">shared.nullbulk</span> = createObject(REDIS_STRING,sdsnew(<span class="string">"$-1\r\n"</span>))<span class="comment">;</span></span><br></pre></td></tr></table></figure><p>We can see that it is a Redis string object who’s on-the-wire value is <code>$-1\r\n</code>, meaning a length of -1, Redis’s way of indicating null to a client, according to the protocol.</p><p><code>addReply</code> builds the reply to the client. It does this by first setting up a write event on the main event loop listener with <code>_installWriteEvent</code>. This makes sure that the reply is written out to the client connection when there are bytes present in the buffer. Next, Redis adds the reply to the client’s buffer. If the reply object were an non-string value like an integer, or a list, or a set, Redis would first decode it to a bytestring that can be serialized to, for example, on a socket. Redis string objects are encoded “raw,” or as-is. The <code>nullbulk</code> object is technically a string object, so no decoding is necessary in our case. In any case, the reply bytestring is copied to the client’s reply buffer with <code>_addReplyToBuffer</code>, which for all intents and purposes completes the execution of our <code>get</code> command on the server.</p><p>The client will read the on-the-wire reply of <code>$-1\r\n</code> and know that it is a string reply of length -1, and therefore is the null (or “nil,” in the context of <code>redis-cli</code>) object, and to convert that into the appropriate object for the language of the client. Back to our <code>redis-cli</code> client patiently waiting for a reply from our breakpointed server, which we continue from, that looks like:</p><figure class="highlight lisp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="name">nil</span>)</span><br><span class="line">redis&gt;</span><br></pre></td></tr></table></figure><h1 id="SET"><a href="#SET" class="headerlink" title="SET"></a>SET</h1><p>The <code>set</code> command proceeds much the same way as the <code>get</code>, up to the point of command dispatching on the server.</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># client</span></span><br><span class="line">redis&gt; <span class="builtin-name">set</span> users:1234 <span class="string">"Paul Smith"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># server</span></span><br><span class="line">Breakpoint 1, readQueryFromClient (<span class="attribute">el</span>=0x100400000, <span class="attribute">fd</span>=6, <span class="attribute">privdata</span>=0x100805e00, <span class="attribute">mask</span>=1) at networking.c:801</span><br><span class="line">801     void readQueryFromClient(aeEventLoop <span class="number">*e</span>l, int fd, void *privdata, int mask) &#123;</span><br><span class="line">(gdb) n</span><br><span class="line">802         redisClient <span class="number">*c</span> = (redisClient*) privdata;</span><br><span class="line">(gdb) n</span><br><span class="line">808         nread = read(fd, buf, REDIS_IOBUF_LEN);</span><br><span class="line">(gdb) n</span><br><span class="line">809         <span class="keyword">if</span> (nread == -1) &#123;</span><br><span class="line">(gdb) <span class="builtin-name">print</span> nread</span><br><span class="line"><span class="variable">$1</span> = 47</span><br><span class="line">(gdb) x/47cb buf</span><br><span class="line">0x7fff5fbfeba0: 42 <span class="string">'*'</span>  51 <span class="string">'3'</span>  13 <span class="string">'\r'</span> 10 <span class="string">'\n'</span> 36 <span class="string">'$'</span>  51 <span class="string">'3'</span>  13 <span class="string">'\r'</span> 10 <span class="string">'\n'</span></span><br><span class="line">0x7fff5fbfeba8: 115 <span class="string">'s'</span> 101 <span class="string">'e'</span> 116 <span class="string">'t'</span> 13 <span class="string">'\r'</span> 10 <span class="string">'\n'</span> 36 <span class="string">'$'</span>  49 <span class="string">'1'</span>  48 <span class="string">'0'</span></span><br><span class="line">0x7fff5fbfebb0: 13 <span class="string">'\r'</span> 10 <span class="string">'\n'</span> 117 <span class="string">'u'</span> 115 <span class="string">'s'</span> 101 <span class="string">'e'</span> 114 <span class="string">'r'</span> 115 <span class="string">'s'</span> 58 <span class="string">':'</span></span><br><span class="line">0x7fff5fbfebb8: 49 <span class="string">'1'</span>  50 <span class="string">'2'</span>  51 <span class="string">'3'</span>  52 <span class="string">'4'</span>  13 <span class="string">'\r'</span> 10 <span class="string">'\n'</span> 36 <span class="string">'$'</span>  49 <span class="string">'1'</span></span><br><span class="line">0x7fff5fbfebc0: 48 <span class="string">'0'</span>  13 <span class="string">'\r'</span> 10 <span class="string">'\n'</span> 80 <span class="string">'P'</span>  97 <span class="string">'a'</span>  117 <span class="string">'u'</span> 108 <span class="string">'l'</span> 32 <span class="string">' '</span></span><br><span class="line">0x7fff5fbfebc8: 83 <span class="string">'S'</span>  109 <span class="string">'m'</span> 105 <span class="string">'i'</span> 116 <span class="string">'t'</span> 104 <span class="string">'h'</span> 13 <span class="string">'\r'</span> 10 <span class="string">'\n'</span></span><br><span class="line">(gdb) <span class="builtin-name">print</span> (char *)buf</span><br><span class="line"><span class="variable">$2</span> = 0x7fff5fbfeba0 <span class="string">"*3\r\n<span class="variable">$3</span>\r\nset\r\n<span class="variable">$10</span>\r\nusers:1234\r\n<span class="variable">$10</span>\r\nPaul Smith\r\n"</span></span><br></pre></td></tr></table></figure><p>This time, our protocol-encoded bytestring is 47 bytes long, owing to the extra argument “Paul Smith” and the length tag it requires. Also notice the leading <code>*3</code> indicates there are three arguments: <code>set</code>, <code>users:1234</code>, <code>Paul Smith</code>.</p><p>Let’s skip ahead now to the point in <code>call</code> in <code>redis.c</code>, after the command has been looked-up in the command table and the server is about ready to call the underlying <code>proc</code> function with the client object argument. The <code>set</code> command, in the form of a <code>redisCommand</code> struct, looks like this:</p><figure class="highlight 1c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta"># redis.c:73</span></span><br><span class="line">&#123;<span class="string">"set"</span>,setCommand,<span class="number">3</span>,REDIS_CMD_DENYOOM,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br></pre></td></tr></table></figure><p>Notice that the arity of the command is 3, which includes the leading command name, plus key and value, and matches what we expect from the client. The <code>set</code> command has a flag that <code>get</code> did not: the constant <code>REDIS_CMD_DENYOOM</code> means that, in out-of-memory situations where Redis can’t allocate any more memory, the execution of the command should be denied. (The absence of this flag means that Redis can continue to serve client “read” requests like <code>get</code> even when the server can no longer write any new data.)</p><p>I set a breakpoint on <code>setCommand</code> and let the server continue running until that point:</p><figure class="highlight llvm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"># server</span><br><span class="line">(gdb) b setCommand</span><br><span class="line">Breakpoint <span class="number">2</span> at <span class="number">0x10001a6e2</span>: file t_string.<span class="keyword">c</span>, line <span class="number">48</span>.</span><br><span class="line">(gdb) <span class="keyword">c</span></span><br><span class="line">Continuing.</span><br><span class="line">Breakpoint <span class="number">2</span>, setCommand (<span class="keyword">c</span>=<span class="number">0x100805e00</span>) at t_string.<span class="keyword">c</span>:<span class="number">48</span></span><br><span class="line"><span class="number">48</span>          <span class="keyword">c</span>-&gt;argv[<span class="number">2</span>] = tryObjectEncoding(<span class="keyword">c</span>-&gt;argv[<span class="number">2</span>])<span class="comment">;</span></span><br></pre></td></tr></table></figure><p>Incidentally, you can inspect the values of the client’s command arguments at any time, with a simple <code>gdb</code> invocation. The arguments are of type <code>robj</code>, which has a field <code>ptr</code> that is a pointer to the actual value in memory. Since in our <code>set</code> case these are strings, we can inspect them by typecasting to <code>char *</code> like so:</p><figure class="highlight xl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">(<span class="function"><span class="title">gdb</span>) p (char *)c-&gt;</span><span class="function"><span class="title">argv</span>[0]-&gt;</span>ptr</span><br><span class="line">$<span class="number">10</span> = <span class="number">0</span>x10032ae78 <span class="string">"set"</span></span><br><span class="line">(<span class="function"><span class="title">gdb</span>) p (char *)c-&gt;</span><span class="function"><span class="title">argv</span>[1]-&gt;</span>ptr</span><br><span class="line">$<span class="number">11</span> = <span class="number">0</span>x10032b068 <span class="string">"users:1234"</span></span><br><span class="line">(<span class="function"><span class="title">gdb</span>) p (char *)c-&gt;</span><span class="function"><span class="title">argv</span>[2]-&gt;</span>ptr</span><br><span class="line">$<span class="number">12</span> = <span class="number">0</span>x10032b098 <span class="string">"Paul Smith"</span></span><br></pre></td></tr></table></figure><p>The first thing the server does in <code>setCommand</code> is encode the value being set with <code>tryObjectEncoding</code>. It will try to create an efficient encoding if the bytestring can be interpreted as an integer, for example. This can save space especially in the case where many numbers are being stored. Additionally, Redis will try to reuse shared integers as values instead of allocating resources for new ones – see the previous article for more on the creation and use of shared integers.</p><p>Once the value being set has been encoded, <code>setGenericCommand</code> is called (<code>set</code> shares <code>setGenericCommand</code> with the <code>setnx</code> and <code>setex</code> commands). From here, <code>dbAdd</code> is called, with the client, key, and value as arguments. <code>dbAdd</code> will only add the value to the database’s hash table if the key does not already exist. In our case, since the key <code>users:1234</code> does not exist, the value of <code>dictFind</code> is null, and the function proceeds to add the value with <code>dictAdd</code>.</p><p><code>dictAdd</code> takes the dictionary hash table, key, and value as arguments. It uses <code>_dictKeyIndex</code> to find the index of a free slot in the hash table for our new entry. See the implementation in <code>dict.c</code> and <code>dict.h</code> for the details of key hashing, and the structure of the dictionary and its component hash tables (each Redis dictionary contains two hash tables in order to provide incremental rehashing as the dictionary grows). <code>dictAdd</code> allocates memory for the new entry and stores it in the new index.</p><p>The server returns back up the stack from <code>dictAdd</code> and <code>dbAdd</code> to <code>setGenericCommand</code>, where it increments the reference count on our new value. Redis uses reference counting in order to free memory used by values that have been deleted or have expired. It then “touches” the key so that if any clients are <code>watch</code>ing the key, the next <code>exec</code> command will fail. It also increments the server’s <code>dirty</code> flag, which it uses to determine when to write out the dump file to disk. Finally, it writes out the reply to the client, which is a shared object, <code>shared.ok</code>. This is special Redis string object in the protocol that consists of the bytestring “+OK\r\n”. Clients will typically convert this into the equivalent “true” value for their language.</p><h1 id="GET-redux"><a href="#GET-redux" class="headerlink" title="GET redux"></a>GET redux</h1><p>Our key is now set, so we can try the <code>get users:1234</code> command again and see how it differs for a found key.</p><figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"># <span class="keyword">db</span>.c:9</span><br><span class="line">robj *lookupKey(redisDb *<span class="keyword">db</span>, robj *key) &#123;</span><br><span class="line">    dictEntry *<span class="keyword">de</span> = dictFind(<span class="keyword">db</span>-&gt;dict,key-&gt;ptr);</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">de</span>) &#123;</span><br><span class="line">        robj *val = dictGetEntryVal(<span class="keyword">de</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// ... skipping the lru &amp; vm parts ... </span></span><br><span class="line"></span><br><span class="line">        server.stat_keyspace_hits++;</span><br><span class="line">        <span class="keyword">return</span> val;</span><br></pre></td></tr></table></figure><p>The point where a <code>get</code> on an existing key and a <code>get</code> on a non-existent key differ is line 11, where the <code>de</code> entry in the database’s dictionary is found. <code>dictGetEntryVal</code> is a simple macro for accessing the field in the <code>de</code> struct that carries the value associated with the key. Redis updates its statistics to indicate a key hit and returns the value object.</p><p>Again, as with the key miss from above (remember the null value is a Redis object, too), the value is decoded into the Redis bytestring protocol. This is the response to the client, and we have concluded our <code>GET</code>/<code>SET</code>/<code>GET</code> dance.</p><h4 id="Recent-posts"><a href="#Recent-posts" class="headerlink" title="Recent posts"></a>Recent posts</h4><ul><li><a href="https://www.pauladamsmith.com/blog/2011/01/bluecollarleague.html" rel="external nofollow noopener noreferrer" target="_blank">The All-Blue-Collar League</a></li><li><a href="https://www.pauladamsmith.com/blog/2011/04/floppy.html" rel="external nofollow noopener noreferrer" target="_blank">A partial list of metaphors, visual and otherwise, anti-skeuomorphism zealots have to tackle to reach utopia</a></li></ul><h4 id="About"><a href="#About" class="headerlink" title="About"></a>About</h4><p>Paul Smith is a software engineer. <a href="https://www.pauladamsmith.com/about.html" rel="external nofollow noopener noreferrer" target="_blank">More …</a></p><ul><li><strong><a href="tel:13124924668" rel="external nofollow noopener noreferrer" target="_blank">+1-312-492-4668</a></strong></li><li><strong><a href="mailto:paulsmith@gmail.com" rel="external nofollow noopener noreferrer" target="_blank">paulsmith@gmail.com</a></strong></li></ul><p>ⓒ Paul Smith</p><h4 id="Elsewhere"><a href="#Elsewhere" class="headerlink" title="Elsewhere"></a>Elsewhere</h4><ul><li><a href="https://twitter.com/paulsmith" rel="external nofollow noopener noreferrer" target="_blank">@paulsmith</a></li><li><a href="http://www.flickr.com/photos/psmith" rel="external nofollow noopener noreferrer" target="_blank">Flickr</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://www.pauladamsmith.com/blog/2011/03/redis_get_set.html&quot; rel=&quot;external nofollow noopener noreferrer&quot; target=&quot;_blank&quot;&gt;原文链接&lt;</summary>
      
    
    
    
    <category term="redis" scheme="http://www.shankusu.me/categories/redis/"/>
    
    
    <category term="redis" scheme="http://www.shankusu.me/tags/redis/"/>
    
  </entry>
  
  <entry>
    <title>under the hood</title>
    <link href="http://www.shankusu.me/redis/redis-under-the-hood/"/>
    <id>http://www.shankusu.me/redis/redis-under-the-hood/</id>
    <published>2021-09-01T11:37:20.000Z</published>
    <updated>2021-09-01T12:00:38.176Z</updated>
    
    <content type="html"><![CDATA[<p>by <a href="https://www.pauladamsmith.com/" rel="external nofollow noopener noreferrer" target="_blank">Paul Smith</a> (<a href="http://twitter.com/paulsmith" rel="external nofollow noopener noreferrer" target="_blank">@paulsmith</a>)</p><p><a href="http://code.google.com/p/redis/" rel="external nofollow noopener noreferrer" target="_blank"><img src="https://www.pauladamsmith.com/articles/redis_under_the_hood/redis.png" alt="Redis logo"></a></p><p>How does the <a href="http://code.google.com/p/redis/" rel="external nofollow noopener noreferrer" target="_blank">Redis</a> server work?</p><p>I was curious to learn more about Redis’s internals, so I’ve been familiarizing myself with the source, largely by reading and jumping around in Emacs. After I had peeled back enough of the onion’s layers, I realized I was trying to keep track of too many details in my head, and it wasn’t clear how it all hung together. I decided to write out in narrative form how an instance of the Redis server starts up and initializes itself, and how it handles the request/response cycle with a client, as a way of explaining it to myself, hopefully in a clear fashion. Luckily, Redis has a nice, clean code base that is easy to read and follow along. Armed with a <a href="http://ctags.sourceforge.net/" rel="external nofollow noopener noreferrer" target="_blank">TAGS</a> file, my <code>$EDITOR</code>, and GDB, I set out to see how it all works under the hood. (Incidentally, I was working with the Redis code base as of <a href="http://github.com/antirez/redis/tree/b4f2e412d087bae0a523fe6ea40fcad30fe74b5b" rel="external nofollow noopener noreferrer" target="_blank">commit b4f2e41</a>. Of course, internals such as I outline below are subject to change. However, the broad architecture of the server is unlikely to change very much, and I tried to keep that in mind as I went along.)</p><p>This article examines server startup and takes a high-level view of the request/response processing cycle. In a <a href="https://www.pauladamsmith.com/blog/2011/03/redis_get_set.html" rel="external nofollow noopener noreferrer" target="_blank">subsequent article</a>, I’ll dive in to greater detail and trace a simple <code>SET</code>/<code>GET</code> command pair as they make their way through Redis.</p><ul><li>Redis: under the hood<ul><li>Startup<ul><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#beginning-global-server-state-initialization" rel="external nofollow noopener noreferrer" target="_blank">Beginning global server state initialization</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#setting-up-command-table" rel="external nofollow noopener noreferrer" target="_blank">Setting up command table</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#loading-config-file" rel="external nofollow noopener noreferrer" target="_blank">Loading config file</a></li><li><code>initServer()</code><ul><li>Shared objects<ul><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#shared-integers" rel="external nofollow noopener noreferrer" target="_blank">Shared integers</a></li></ul></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#event-loop" rel="external nofollow noopener noreferrer" target="_blank">Event loop</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#databases" rel="external nofollow noopener noreferrer" target="_blank">Databases</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#tcp-socket" rel="external nofollow noopener noreferrer" target="_blank">TCP socket</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#server-cron" rel="external nofollow noopener noreferrer" target="_blank">Server cron</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#registering-connection-handler-with-event-loop" rel="external nofollow noopener noreferrer" target="_blank">Registering connection handler with event loop</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#opening-the-aof" rel="external nofollow noopener noreferrer" target="_blank">Opening the AOF</a></li></ul></li><li>Back up to <code>main()</code><ul><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#restoring-data" rel="external nofollow noopener noreferrer" target="_blank">Restoring data</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#event-loop-setup" rel="external nofollow noopener noreferrer" target="_blank">Event loop setup</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#entering-the-event-loop" rel="external nofollow noopener noreferrer" target="_blank">Entering the event loop</a></li></ul></li></ul></li><li>Processing a request &amp; returning a response<ul><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#handling-a-new-connection" rel="external nofollow noopener noreferrer" target="_blank">Handling a new connection</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#reading-a-command-from-a-client" rel="external nofollow noopener noreferrer" target="_blank">Reading a command from a client</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#executing-the-command-and-responding" rel="external nofollow noopener noreferrer" target="_blank">Executing the command and responding</a></li></ul></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#summary" rel="external nofollow noopener noreferrer" target="_blank">Summary</a></li><li><a href="https://www.pauladamsmith.com/articles/redis-under-the-hood.html#next-time-tracing-a-set-and-get" rel="external nofollow noopener noreferrer" target="_blank">Next time — tracing a <code>SET</code> and <code>GET</code></a></li></ul></li></ul><h2 id="Startup"><a href="#Startup" class="headerlink" title="Startup"></a>Startup</h2><p>Let’s begin with the <code>main()</code> function in <code>redis.c</code>.</p><p><a href="https://www.pauladamsmith.com/articles/redis_under_the_hood/startup.png" rel="external nofollow noopener noreferrer" target="_blank"><img src="https://www.pauladamsmith.com/articles/redis_under_the_hood/startup_th.png" alt="Startup   diagram"></a></p><h3 id="Beginning-global-server-state-initialization"><a href="#Beginning-global-server-state-initialization" class="headerlink" title="Beginning global server state initialization"></a>Beginning global server state initialization</h3><p>First, <code>initServerConfig()</code> is called. This partially initializes a variable <code>server</code>, which has the type <code>struct redisServer</code>, that serves as the global server state.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// redis.h:338</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">redisServer</span> &#123;</span></span><br><span class="line">    <span class="keyword">pthread_t</span> mainthread;</span><br><span class="line">    <span class="keyword">int</span> port;</span><br><span class="line">    <span class="keyword">int</span> fd;</span><br><span class="line">    redisDb *db;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// redis.c:69</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">redisServer</span> <span class="title">server</span>;</span> <span class="comment">/* server global state */</span></span><br></pre></td></tr></table></figure><p>There are a huge number of members in this struct, but they generally fall into the following categories:</p><ul><li>general server state</li><li>statistics</li><li>configuration from config file</li><li>replication</li><li>sort parameters</li><li>virtual memory config, state, I/O threads, &amp; stats</li><li>zip structure</li><li>event loop helpers</li><li>pub/sub</li></ul><p>For example, this struct includes members that map to options in the configuration file (usually named <code>redis.conf</code>) such as the port the server listens on and how verbose logging should be, pointers to linked lists of connected clients and slave Redis servers as well as the Redis database(s) itself, and counters for statistics like the number of commands processed since startup.</p><p><code>initServerConfig()</code> provides default values for the members that can be configured by the user via the <code>redis.conf</code> config file.</p><h3 id="Setting-up-command-table"><a href="#Setting-up-command-table" class="headerlink" title="Setting up command table"></a>Setting up command table</h3><p>The next thing <code>main()</code> does is sort the table of Redis commands. These are defined in a global variable <code>readonlyCommandTable</code> which is an array of <code>struct redisCommand</code>s.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// redis.c:70</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">redisCommand</span> *<span class="title">commandTable</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">redisCommand</span> <span class="title">readonlyCommandTable</span>[] = &#123;</span></span><br><span class="line">    &#123;<span class="string">"get"</span>,getCommand,<span class="number">2</span>,REDIS_CMD_INLINE,<span class="literal">NULL</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>&#125;,</span><br><span class="line">    &#123;<span class="string">"set"</span>,setCommand,<span class="number">3</span>,REDIS_CMD_BULK|REDIS_CMD_DENYOOM,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">"setnx"</span>,setnxCommand,<span class="number">3</span>,REDIS_CMD_BULK|REDIS_CMD_DENYOOM,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">"setex"</span>,setexCommand,<span class="number">4</span>,REDIS_CMD_BULK|REDIS_CMD_DENYOOM,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">"append"</span>,appendCommand,<span class="number">3</span>,REDIS_CMD_BULK|REDIS_CMD_DENYOOM,<span class="literal">NULL</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>&#125;,</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// redis.h:458</span></span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="keyword">void</span> <span class="title">redisCommandProc</span><span class="params">(redisClient *c)</span></span>;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">redisCommand</span> &#123;</span></span><br><span class="line">    <span class="keyword">char</span> *name;</span><br><span class="line">    redisCommandProc *proc;</span><br><span class="line">    <span class="keyword">int</span> arity;</span><br><span class="line">    <span class="keyword">int</span> flags;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>A <code>redisCommand</code> struct keeps track of its name — the mnemonic, i.e., “get” — a pointer to the actual C function that performs the command, the command’s arity, command flags such as whether it returns a bulk response, and a number of VM-specific members.)</p><p>The read-only table is ordered in source code so that commands are grouped by category, such as string commands, list commands, set commands, etc., to make it easier for a programmer to scan the table for similar commands. The sorted table of commands is pointed to by the global variable <code>commandTable</code>, and is used to lookup Redis commands with a standard binary search (<code>lookupCommand()</code>, which returns a pointer to a <code>redisCommand</code>).</p><h3 id="Loading-config-file"><a href="#Loading-config-file" class="headerlink" title="Loading config file"></a>Loading config file</h3><p><code>main()</code> moves on to processing command-line options given by the user starting up the redis-server executable. Currently, there is only a single argument that Redis takes — aside from the usual version <code>-v</code> and help <code>-h</code> flags — which is a path to a config file. If the path is given, Redis loads the config file and overrides any defaults already set by <code>initServerConfig()</code> by calling <code>loadServerConfig()</code>. This function is fairly straightforward, looping over each line in the config file and converting values that match directive names to appropriate types for the matching member in the <code>server</code> struct. At this point, Redis will daemonize and detach from the controlling terminal if it has been configured to do so.</p><h3 id="initServer"><a href="#initServer" class="headerlink" title="initServer()"></a><code>initServer()</code></h3><p><code>initServer()</code> finishes the job of initializing the <code>server</code> struct that was begun by <code>initServerConfig()</code>. First, it sets up signal handling (<code>SIGHUP</code> and <code>SIGPIPE</code> signals are ignored — there is an opportunity to improve Redis by adding the ability to reload its config file when it receives a <code>SIGHUP</code>, in the fashion of other daemons), including printing a stacktrace if the server receives a <code>SIGSEGV</code> (and other related signals), see <code>segvHandler()</code>.</p><p>A number of doubly-linked lists (see <code>adlist.h</code>) are created to keep track of clients, slaves, monitors (a client that has sent the <code>MONITOR</code> command), and an object free list.</p><h4 id="Shared-objects"><a href="#Shared-objects" class="headerlink" title="Shared objects"></a>Shared objects</h4><p>One interesting thing Redis does is create a number of shared objects, which are accessible via the global <code>shared</code> struct. Common Redis objects that are required by many different commands, response strings and error messages, for example, can be shared without having to allocate them each time, saving memory, with the tradeoff of a bit more initialization effort at startup time.</p><figure class="highlight ceylon"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// redis.c:662</span></span><br><span class="line"><span class="keyword">shared</span>.crlf = createObject(REDIS<span class="number">_</span>STRING,sdsnew(<span class="string">"\r\n"</span>));</span><br><span class="line"><span class="keyword">shared</span>.ok = createObject(REDIS<span class="number">_</span>STRING,sdsnew(<span class="string">"+OK\r\n"</span>));</span><br><span class="line"><span class="keyword">shared</span>.err = createObject(REDIS<span class="number">_</span>STRING,sdsnew(<span class="string">"-ERR\r\n"</span>));</span><br><span class="line"><span class="keyword">shared</span>.emptybulk = createObject(REDIS<span class="number">_</span>STRING,sdsnew(<span class="string">"$0\r\n\r\n"</span>));</span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></table></figure><h5 id="Shared-integers"><a href="#Shared-integers" class="headerlink" title="Shared integers"></a>Shared integers</h5><p>The greatest impact in terms of memory savings with shared objects comes from a large pool of shared integers.</p><figure class="highlight d"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// redis.c:705</span></span><br><span class="line"><span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; REDIS_SHARED_INTEGERS; j++) &#123;</span><br><span class="line">    <span class="keyword">shared</span>.integers[j] = createObject(REDIS_STRING,(<span class="keyword">void</span>*)(<span class="built_in">long</span>)j);</span><br><span class="line">    <span class="keyword">shared</span>.integers[j]-&gt;encoding = REDIS_ENCODING_INT;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>createSharedObjects()</code> creates an array of the first 10,000 non-negative integers as Redis objects (strings with an integer encoding). Various Redis objects like strings, sets, and lists often contain many small integers (for IDs or counters), and they can reuse the same objects already allocated in memory, for a large potential savings. One could imagine the constant that defines the number of shared integers to be created, <code>REDIS_SHARED_INTEGERS</code>, being exposed to configuration to give users the opportunity, based on knowledge of their applications and needs, to increase the number of shared integers for greater memory savings. The tradeoff is that Redis statically allocates slightly more memory at startup time, but this would likely be a small amount compared to the overall typical database sizes and potential savings.</p><h4 id="Event-loop"><a href="#Event-loop" class="headerlink" title="Event loop"></a>Event loop</h4><p><code>initServer()</code> continues by creating the core event loop, calling <code>aeCreateEventLoop()</code> (see <code>ae.c</code>) and assigning the result to the <code>el</code> member of <code>server</code>.</p><p>One key aspect of Redis’s implementation is the use of locally-provided wrappers to simplify and hide the complexity of common tasks, without adding dependecies at build-time. For example, <code>zmalloc.h</code> defines a number of wrappers for the family of <code>*alloc()</code> functions, which keep track of how much memory Redis has allocated. <code>sds.h</code> defines an API to a dynamic strings library (basically, a string that keeps track of its length and whether its memory can be freed.</p><p><code>ae.h</code> provides a platform-independent wrapper for setting up I/O event notification loops, which uses <code>epoll</code> on Linux, <code>kqueue</code> on BSD, and falls back to <code>select</code> if the respective first choice is not available.) Redis’s event loop polls for new connections and I/O events (reading requests from and writing responses to a socket), being triggered when a new event arrives. This is what makes Redis so responsive, it can serve thousands of clients simultaneously without blocking while individual requests are processed and responded to.</p><h4 id="Databases"><a href="#Databases" class="headerlink" title="Databases"></a>Databases</h4><p><code>initServer()</code> also initializes a number of <code>redisDb</code> objects, which are structs that encapsulate the details of a particular Redis database, including tracking expiring keys, keys that are blocking (either from a <code>B{L,R}POP</code> command or from I/O), and keys that are being watched for check-and-set. (By default there are 16 separate databases, which can be thought of as namespaces within a Redis server.)</p><h4 id="TCP-socket"><a href="#TCP-socket" class="headerlink" title="TCP socket"></a>TCP socket</h4><p><code>initServer()</code> is where the socket that Redis listens for connections (by default, bound to port 6379) is set up. Another Redis-local wrapper, <code>anet.h</code>, defines <code>anetTcpServer()</code> and a number of other functions that simplify the usual complexity of setting up a new socket, binding, and listening to a port.</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="regexp">//</span> redis.c:<span class="number">791</span></span><br><span class="line">server.fd = anetTcpServer(server.neterr, server.port, server.bindaddr);</span><br><span class="line"><span class="keyword">if</span> (server.fd == -<span class="number">1</span>) &#123;</span><br><span class="line">    redisLog(REDIS_WARNING, <span class="string">"Opening TCP port: %s"</span>, server.neterr);</span><br><span class="line">    <span class="keyword">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="Server-cron"><a href="#Server-cron" class="headerlink" title="Server cron"></a>Server cron</h4><p><code>initServer()</code> further allocates various dicts and lists for databases and for pub/sub, resets stats and various flags, and notes the UNIX timestamp of the server start time. It registers <code>serverCron()</code> with the event loop as a time event, executing that function once every 100 milliseconds. (This is a bit tricky, because initially, <code>serverCron()</code> is set to run in 1 millisecond by <code>initServer()</code>, in order to have the cron cycle start right away with the server startup, but then the return value of <code>serverCron()</code>, which is 100, is plugged in to the calculation of the next time the time event process should be handled.)</p><p><code>serverCron()</code> performs a number of periodic tasks for Redis, including verbose logging of database size (# of keys and memory used) and connected clients, resizing hash tables, closing idle/timed-out client connections, performing any post-background save or AOF rewrite cleanup, kicking off a background save if the save conditions as configured have been met (so many keys changed in so many seconds), calculating LRU information and dealing with expired keys (Redis only expires a few timed-out keys per cron cycle, using a adaptive, statistical method to avoid tying up the server, but will get more aggressive if expiring keys can help avoid out-of-memory situations), swapping out values to disk if virtual memory is enabled, and syncing with a master if this server is a slave.</p><h4 id="Registering-connection-handler-with-event-loop"><a href="#Registering-connection-handler-with-event-loop" class="headerlink" title="Registering connection handler with event loop"></a>Registering connection handler with event loop</h4><p>Crucially, <code>initServer()</code> hooks up the event loop with the server’s TCP socket by registering the socket’s descriptor, registering the <code>acceptHandler()</code> function to be called when a new connection is accepted. (More on this below in the “Processing a request” section.)</p><figure class="highlight axapta"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// redis.c:821</span></span><br><span class="line"><span class="keyword">if</span> (aeCreateFileEvent(<span class="keyword">server</span>.el, <span class="keyword">server</span>.fd, AE_READABLE,</span><br><span class="line">    acceptHandler, NULL) == AE_ERR) oom(<span class="string">"creating file event"</span>);</span><br></pre></td></tr></table></figure><h4 id="Opening-the-AOF"><a href="#Opening-the-AOF" class="headerlink" title="Opening the AOF"></a>Opening the AOF</h4><p><code>initServer()</code> creates or opens the append-only file (AOF), it the server was configured to use it.</p><figure class="highlight axapta"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// redis.c:824</span></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">server</span>.appendonly) &#123;</span><br><span class="line">    <span class="keyword">server</span>.appendfd = open(<span class="keyword">server</span>.appendfilename,O_WRONLY|O_APPEND|O_CREAT,<span class="number">0644</span>);</span><br></pre></td></tr></table></figure><p>Finally, <code>initServer()</code> initializes Redis’s virtual memory system, again, if the server had been configured for it.</p><h3 id="Back-up-to-main"><a href="#Back-up-to-main" class="headerlink" title="Back up to main()"></a>Back up to <code>main()</code></h3><p>If the server was configured to daemonize, Redis will now try to write out a pid file (the path of which is configurable, but defaults to <code>/var/run/redis.pid</code>).</p><p>At this point, the server has started up, and Redis will log this fact to its log file. However, there’s still a bit more to do in <code>main()</code> before Redis is fully ready.</p><h4 id="Restoring-data"><a href="#Restoring-data" class="headerlink" title="Restoring data"></a>Restoring data</h4><p>If there is an AOF or a database dump file (eg., <code>dump.rdb</code>), it will be loaded, restoring data to the server from a previous session. (If both exist, the AOF takes priority.)</p><figure class="highlight gauss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// redis.c:1452</span></span><br><span class="line"><span class="keyword">if</span> (server.appendonly) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">loadAppendOnlyFile</span>(server.appendfilename) == REDIS_OK)</span><br><span class="line">        <span class="built_in">redisLog</span>(REDIS_NOTICE,<span class="string">"DB loaded from append only file: %ld seconds"</span>,<span class="built_in">time</span>(<span class="built_in">NULL</span>)-start);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">rdbLoad</span>(server.dbfilename) == REDIS_OK)</span><br><span class="line">        <span class="built_in">redisLog</span>(REDIS_NOTICE,<span class="string">"DB loaded from disk: %ld seconds"</span>,<span class="built_in">time</span>(<span class="built_in">NULL</span>)-start);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The server is now ready to start accepting requests.</p><h4 id="Event-loop-setup"><a href="#Event-loop-setup" class="headerlink" title="Event loop setup"></a>Event loop setup</h4><p>To finish up, Redis registers a function to be called each time it enters the event loop, <code>beforeSleep()</code> (since the process essentially goes to sleep while it waits to be notified of events). <code>beforeSleep()</code> does two things: it deals with serving clients that have requested keys that were swapped to disk if the virtual memory system was enabled, and it flushes the AOF to disk. The writing of the AOF is handled by <code>flushAppendOnlyFile()</code>. The function encapsulates some tricky logic about flushing the buffer that holds pending AOF writes (the frequency of which is configurable by the user).</p><h4 id="Entering-the-event-loop"><a href="#Entering-the-event-loop" class="headerlink" title="Entering the event loop"></a>Entering the event loop</h4><p>Redis now enters the main event loop by calling <code>aeMain()</code>, with the argument <code>server.el</code> (remember, this member contains a pointer to an <code>aeEventLoop</code>). If there are any time (i.e., server cron) or file events to process each time through the loop, their respective handler functions will be called. <code>aeProcessEvents()</code> encapsulates this logic — time events are handled by custom logic, whereas file events are handled by the underlying <code>epoll</code> or <code>kqueue</code> or <code>select</code> I/O event notification system.</p><p>Because of Redis’s need to respond to time events as well as file or I/O events, it implements a custom event/polling loop, <code>aeMain()</code>. By checking to see if any time events need processing, and utilizing file event notification, the event loop can efficiently sleep until there is work to be done, and not tie up the CPU in a tight <code>while</code> loop.</p><h2 id="Processing-a-request-amp-returning-a-response"><a href="#Processing-a-request-amp-returning-a-response" class="headerlink" title="Processing a request &amp; returning a response"></a>Processing a request &amp; returning a response</h2><p><a href="https://www.pauladamsmith.com/articles/redis_under_the_hood/request-response.png" rel="external nofollow noopener noreferrer" target="_blank"><img src="https://www.pauladamsmith.com/articles/redis_under_the_hood/request-response_th.png" alt="Request/response   diagram"></a></p><p>We are now inside Redis’s main event polling loop, listening on a port and waiting for clients to connect. It’s time to look at how Redis processes a command request.</p><h3 id="Handling-a-new-connection"><a href="#Handling-a-new-connection" class="headerlink" title="Handling a new connection"></a>Handling a new connection</h3><p>Back under <code>initServer()</code>, Redis registered <code>acceptHandler()</code> to be called when there was an I/O event associated with the file descriptor of the socket the server is listening to (i.e., the socket has data waiting to be read or written). <code>acceptHandler()</code> creates a client object — pointer to a <code>redisClient</code>, a struct defined in <code>redis.h</code> — to represent a new client connection.</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// networking.c:347</span></span><br><span class="line">cfd = anetAccept(server.neterr, fd, cip, &amp;cport);</span><br><span class="line"><span class="keyword">if</span> (cfd == AE_ERR) &#123;</span><br><span class="line">    redisLog(REDIS_VERBOSE,<span class="string">"Accepting client connection: %s"</span>, server.neterr);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line">redisLog(REDIS_VERBOSE,<span class="string">"Accepted %s:%d"</span>, cip, cport);</span><br><span class="line"><span class="keyword">if</span> ((c = createClient(cfd)) == <span class="literal">NULL</span>) &#123;</span><br><span class="line">    redisLog(REDIS_WARNING,<span class="string">"Error allocating resoures for the client"</span>);</span><br><span class="line">    <span class="built_in">close</span>(cfd); <span class="comment">/* May be already closed, just ingore errors */</span></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>createClient()</code> is called to allocate and initialize the client object. It selects database 0 by default (since there has to be at least one Redis db per server), and associates the client file descriptor generated by <a href="http://linux.die.net/man/2/accept" rel="external nofollow noopener noreferrer" target="_blank"><code>accept(2)</code></a> in <code>acceptHandler()</code> with the client object. Other flags and members are initialized, and finally the client is appended to the global list of clients being tracked by <code>server.clients</code>. The key thing Redis does in <code>createClient()</code> is registering a handler with the event loop, the function <code>readQueryFromClient()</code>, for when there is data from the client connection to be read.</p><figure class="highlight gauss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// networking.c:20</span></span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">aeCreateFileEvent</span>(server.el,fd,AE_READABLE, readQueryFromClient, c) == AE_ERR)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">close</span>(fd);</span><br><span class="line">    <span class="built_in">zfree</span>(c);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Reading-a-command-from-a-client"><a href="#Reading-a-command-from-a-client" class="headerlink" title="Reading a command from a client"></a>Reading a command from a client</h3><p><code>readQueryFromClient()</code> is called by the main event loop when the client makes a command request. (If you are debugging with GDB, this is a good function to set as a breakpoint.) It reads it as much as it can of the command — up to 1024 bytes — to a temporary buffer, then appends it to a client-specific query buffer. This allows Redis to process commands where the payload (command name plus arguments) is larger than 1024 bytes, or because of I/O reasons have been split up into multiple read events. It then calls <code>processInputBuffer()</code>, passing the client object as an argument.</p><figure class="highlight xl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// networking.c:754</span></span><br><span class="line">void readQueryFromClient(aeEventLoop *el, int fd, void *privdata, int mask) &#123;</span><br><span class="line">    redisClient *c = (redisClient*) privdata;</span><br><span class="line">    char buf[REDIS_IOBUF_LEN];</span><br><span class="line">    int nread;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">    nread = read(fd, buf, REDIS_IOBUF_LEN);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">if</span> (nread) &#123;</span><br><span class="line">        <span class="function"><span class="title">size_t</span> oldlen = sdslen(c-&gt;</span>querybuf);</span><br><span class="line">        <span class="function"><span class="title">c</span>-&gt;</span><span class="function"><span class="title">querybuf</span> = sdscatlen(c-&gt;</span>querybuf, buf, nread);</span><br><span class="line">        <span class="function"><span class="title">c</span>-&gt;</span>lastinteraction = <span class="built_in">time</span>(NULL);</span><br><span class="line">        <span class="comment">/* Scan this new piece of the query for the newline. We do this</span></span><br><span class="line"><span class="comment">         * here in order to make sure we perform this scan just one time</span></span><br><span class="line"><span class="comment">         * per piece of buffer, leading to an O(N) scan instead of O(N*N) */</span></span><br><span class="line">        <span class="function"><span class="title">if</span> (c-&gt;</span><span class="function"><span class="title">bulklen</span> == -1 &amp;&amp; c-&gt;</span>newline == NULL)</span><br><span class="line">            <span class="function"><span class="title">c</span>-&gt;</span><span class="function"><span class="title">newline</span> = strchr(c-&gt;</span>querybuf+oldlen,<span class="string">'\n'</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        return;</span><br><span class="line">    &#125;</span><br><span class="line">    Processinputbuffer(c);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>processInputBuffer()</code> parses the raw query from the client into arguments for execution of a Redis command. It first has to contend with the possibility that the client is blocked in a <code>B{L,R}POP</code> command, and bails early if that is the case. The function then parses the raw query buffer into arguments, creating Redis string objects of each and storing them in an array on the client object. The query is in the form of the <a href="http://code.google.com/p/redis/wiki/ProtocolSpecification" rel="external nofollow noopener noreferrer" target="_blank">Redis protocol</a>. <code>processInputBuffer()</code> is really a protocol parser, calling back to <code>processCommand()</code> to fully parse the query. Somewhat confusingly, the source code comments describe parsing the “multi bulk command type,” an alternative protocol originally meant for commands like <code>MSET</code>, but it actually is now the main Redis protocol for all commands. It is binary-safe and easy to parse and debug. (Note: this code has been refactored for the upcoming 2.2 release and is a bit easier to follow.) Now it’s time to actually execute the command the client has sent, by calling <code>processCommand()</code> on the client object.</p><p><code>processCommand()</code> takes the arguments of a command from a client and executes it. Before it gets to the actual execution of the command, it performs a number of checks — if any check fails, it appends a error message to the client object’s reply list and returns to the caller, <code>processInputBuffer()</code>. After handling the <code>QUIT</code> command as a special case (in order to shut down the client safely), <code>processCommand()</code> looks up the command name in the <code>commandTable</code> that was previously set up during Redis’s start up cycle. If it’s an unknown command, or the client got the arity of the command wrong, it’s an error. While it’s not commonly used, Redis can be configured to require a password to authenticate a client before it will accept commands, and this is the stage where Redis will check if the client is authenticate, and will set an error if not. If Redis is configured to use up to a maximum amount of memory, it will at this point try to free up memory if it can (be freeing objects from the free list and removing expired keys), otherwise if the server is over the limit, it won’t process commands with the <code>REDIS_CMD_DENYOOM</code> flag set (mainly writes like <code>SET</code>, <code>INCR</code>, <code>RPUSH</code>, <code>ZADD</code>, etc.), again, an error. One final check Redis makes is that a client can only issue <code>SUBSCRIBE</code> or <code>UNSUBSCRIBE</code> commands while there are outstanding channels subscribed to, otherwise, it’s an error. If all the checks have been passed, the command will be executed, by calling <code>call()</code> with the client object and the command object as arguments.</p><h3 id="Executing-the-command-and-responding"><a href="#Executing-the-command-and-responding" class="headerlink" title="Executing the command and responding"></a>Executing the command and responding</h3><p><code>call()</code>, gets a pointer to function of type <code>struct redisCommandProc</code>, from the <code>proc</code> member of the <code>command</code> object, which takes a single argument, that of a client object. The Redis command procedure is called.</p><figure class="highlight axapta"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// redis.c:864</span></span><br><span class="line"><span class="keyword">void</span> call(redisClient *c, struct redisCommand *cmd) &#123;</span><br><span class="line">    <span class="keyword">long</span> <span class="keyword">long</span> dirty;</span><br><span class="line"></span><br><span class="line">    dirty = <span class="keyword">server</span>.dirty;</span><br><span class="line">    cmd-&gt;proc(c);</span><br><span class="line">    dirty = <span class="keyword">server</span>.dirty-dirty;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></table></figure><p>Each Redis command is responsible for setting the reply for the client. This is possible because the signature of a Redis command procedure is of a single argument, which is the client object. Likewise, each command procedure is responsible for encoding, or deserializing, arguments from the command, and for decoding, or serializing, Redis objects in memory for response to the client.</p><p>Write commands, like <code>SET</code> and <code>ZADD</code>, make the server “dirty,” in other words, the server is marked as having pages in memory that have changed. This is important for the automatic save process, which keeps track of how many keys have changed in a certain period, or the writing to the AOF. The function calls <code>feedAppendOnlyFile()</code> if use of the AOF has been enabled, which writes out the command buffer from the client to the AOF, so the command can be replayed. (It translates commands that set a relative key expiration to an absolute expiration, but otherwise it basically copies the command as it came in from the client, see <code>catAppendOnlyGenericCommand()</code>.) If any slaves are connected, <code>call()</code> will send the command to each of them to be executed locally, see <code>replicationFeedSlaves()</code>. Likewise, if any clients are connected and have issued the <code>MONITOR</code> command, Redis will send a representation of the command, prefixed with a timestamp, see <code>replicationFeedMonitors()</code>.</p><figure class="highlight xl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// redis.c:871 (call() cont.'d)</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">if</span> (server.appendonly &amp;&amp; dirty)</span><br><span class="line">        <span class="function"><span class="title">feedAppendOnlyFile</span>(cmd,c-&gt;</span><span class="function"><span class="title">db</span>-&gt;</span><span class="function"><span class="title">id</span>,c-&gt;</span><span class="function"><span class="title">argv</span>,c-&gt;</span>argc);</span><br><span class="line">    <span class="function"><span class="title">if</span> ((dirty || cmd-&gt;</span>flags &amp; REDIS_CMD_FORCE_REPLICATION) &amp;&amp;</span><br><span class="line">        listLength(server.slaves))</span><br><span class="line">        <span class="function"><span class="title">replicationFeedSlaves</span>(server.slaves,c-&gt;</span><span class="function"><span class="title">db</span>-&gt;</span><span class="function"><span class="title">id</span>,c-&gt;</span><span class="function"><span class="title">argv</span>,c-&gt;</span>argc);</span><br><span class="line">    <span class="keyword">if</span> (listLength(server.monitors))</span><br><span class="line">        <span class="function"><span class="title">replicationFeedMonitors</span>(server.monitors,c-&gt;</span><span class="function"><span class="title">db</span>-&gt;</span><span class="function"><span class="title">id</span>,c-&gt;</span><span class="function"><span class="title">argv</span>,c-&gt;</span>argc);</span><br><span class="line">    server.stat_numcommands++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Control returns to the caller, <code>processCommand()</code>, which resets the client object for subsequent commands.</p><p>As mentioned, each Redis command procedure is itself responsible for setting the response to be sent to the client. After <code>readQueryFromClient()</code> exits and Redis returns the to event loop in <code>aeMain()</code>, <code>aeProcessEvents()</code> will pick up the waiting response in the write buffer and will copy it to the socket the client is connected on.</p><p>And that’s it! The response has been sent, and both client and server are back to a state where they can respectively emit and process more Redis commands.</p><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>Redis starts up by initializing a global server state variable, and reading in an optional configuration file to override any defaults. It sets up a global command table that connects command names with the actual function that implements the command. It creates an event loop using the best available underlying system library for event/readiness notification, and registers a handler function for when there is a new client socket connection to accept. It also registers a periodic (i.e., time-based) event handler for dealing with <code>cron</code>-like tasks like key expiry that need to be addressed outside the regular client-handling path. Once a client has connected, a function is registered on the event loop for being notified when the client has data to be read (i.e., a query for a command). The client’s query is parsed and a command handler is called to execute the command and write the response back to the client (the writing of data to the client is also handled by the event-notification loop). The client object is reset and server is ready to process more queries.</p><h2 id="Next-time-—-tracing-a-SET-and-GET"><a href="#Next-time-—-tracing-a-SET-and-GET" class="headerlink" title="Next time — tracing a SET and GET"></a>Next time — tracing a <code>SET</code> and <code>GET</code></h2><p>I’ll follow-up this article with one that takes a close look at the processing of two commands, <code>SET</code> and <code>GET</code>, by stepping through the implementation of each command’s procedure and examining the data structures Redis uses to store and index data. <strong>March 15, 2011</strong>: <a href="https://www.pauladamsmith.com/blog/2011/03/redis_get_set.html" rel="external nofollow noopener noreferrer" target="_blank">here it is</a>.</p><p>—<a href="https://www.pauladamsmith.com/" rel="external nofollow noopener noreferrer" target="_blank">Paul Smith</a> (<a href="http://twitter.com/paulsmith" rel="external nofollow noopener noreferrer" target="_blank">follow me on the Twitter</a>)</p><p><em>Thanks to pietern on <code>#redis</code> for feedback on a draft of this article</em></p><p>October 18, 2010</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;by &lt;a href=&quot;https://www.pauladamsmith.com/&quot; rel=&quot;external nofollow noopener noreferrer&quot; target=&quot;_blank&quot;&gt;Paul Smith&lt;/a&gt; (&lt;a href=&quot;http://t</summary>
      
    
    
    
    <category term="redis" scheme="http://www.shankusu.me/categories/redis/"/>
    
    
  </entry>
  
  <entry>
    <title>预期与计划表</title>
    <link href="http://www.shankusu.me/%E9%87%91%E8%9E%8D/%E8%AE%A1%E5%88%92%E8%A1%A8/"/>
    <id>http://www.shankusu.me/%E9%87%91%E8%9E%8D/%E8%AE%A1%E5%88%92%E8%A1%A8/</id>
    <published>2021-09-01T05:04:59.000Z</published>
    <updated>2021-09-03T04:44:07.664Z</updated>
    
    <content type="html"><![CDATA[<p>板块 锂电池<br>9.1 机构抱团出货，目前价位过高</p><p>SH601728 中国电信<br>9.1 刚上市不久，筹码非常分散，需等待庄入场 </p><p>SZ2511 中顺洁柔<br>9.1 专家在出货 等到 15块附近再买<br>9.3 可购入(听说)，等到明年初卖出</p><p>SZ600004 白云机场<br>9.1 庄家在洗盘 等待</p><p>HK0700 腾讯控股<br>9.1 年底会涨到520</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;板块 锂电池&lt;br&gt;9.1 机构抱团出货，目前价位过高&lt;/p&gt;
&lt;p&gt;SH601728 中国电信&lt;br&gt;9.1 刚上市不久，筹码非常分散，需等待庄入场 &lt;/p&gt;
&lt;p&gt;SZ2511 中顺洁柔&lt;br&gt;9.1 专家在出货 等到 15块附近再买&lt;br&gt;9.3 可购入(听说)，等到</summary>
      
    
    
    
    <category term="金融" scheme="http://www.shankusu.me/categories/金融/"/>
    
    <category term="股票" scheme="http://www.shankusu.me/categories/金融/股票/"/>
    
    
  </entry>
  
  <entry>
    <title>深入理解Linux IO模型(二)</title>
    <link href="http://www.shankusu.me/os/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3LinuxIO%E6%A8%A1%E5%9E%8B%E4%BA%8C/"/>
    <id>http://www.shankusu.me/os/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3LinuxIO%E6%A8%A1%E5%9E%8B%E4%BA%8C/</id>
    <published>2021-08-26T10:09:43.000Z</published>
    <updated>2021-08-26T10:15:04.887Z</updated>
    
    <content type="html"><![CDATA[<h1 id="深入理解Linux-IO模型-二"><a href="#深入理解Linux-IO模型-二" class="headerlink" title="深入理解Linux IO模型(二)"></a>深入理解Linux IO模型(二)</h1><p>原创 Pearl <a href="javascript:void(0" target="_blank" rel="noopener">Pearl的仲夏夜之梦</a>;) <em>6月13日</em></p><p>为何使用epollselect的缺陷epoll的提出epoll的原理数据结构实现原理高效原因epoll的API创建epoll控制epoll等待epoll使用epoll API触发方式水平触发(Level Triggered, LT)边缘触发(Edge Triggered, ET)两者对比Reference</p><p>接着前文《深入理解Linux IO模型(一)》，本文深入分析IO多路复用模型中的epoll。</p><blockquote><ol><li>本文为了描述方便，统一用读操作讲述，写操作同理。</li><li>本文为了撰写方便，统一将I/O写成了IO。</li><li>欢迎指正文中的错误。</li></ol></blockquote><h3 id="为何使用epoll"><a href="#为何使用epoll" class="headerlink" title="为何使用epoll"></a>为何使用epoll</h3><p>Nginx在几十万并发连接下，是如何做到高效利用服务器资源的？答案是epoll。</p><p>设想一个场景：有100万用户同时与一个网络应用进程保持着TCP连接，而每一时刻只有几十或几百个TCP连接是活跃的（网络应用进程接收到TCP报文），那么在这个时刻，进程只需要处理这100万个连接中的这些活跃的连接即可。那么，内核和应用进程如何协同，才能高效地处理这种情况呢？</p><h4 id="select的缺陷"><a href="#select的缺陷" class="headerlink" title="select的缺陷"></a>select的缺陷</h4><p>应用进程是否在每次调用系统调用，询问内核有事件发生的TCP连接时，把这100万个连接告诉内核，由内核找出其中有事件发生的TCP连接呢？<br>这正是select的做法。</p><p>select有明显的缺陷，因为这100万个TCP连接中，大部分是没有事件发生的。如果应用程序每次收集事件时，都把这100万个socket fd传给内核。有以下问题：</p><ol><li>导致用户空间到内核空间的大量复制。</li><li>内核需要遍历所有的socket fd，判断哪些有事件到来。</li><li>应用进程仍需要遍历所有的socket fd，来判断哪些socket fd可读。</li></ol><p>所以select限制了同时监听的socket fd数量，最多只能同时处理1024个并发连接。</p><h4 id="epoll的提出"><a href="#epoll的提出" class="headerlink" title="epoll的提出"></a>epoll的提出</h4><p>epoll在内核中申请了一片内存空间。</p><p>epoll的做法是，把应用程序的一个select调用分成三部分：</p><ol><li>调用epoll_create创建一个epoll对象</li><li>调用epoll_ctl向epoll对象添加这100万个socket fd</li><li>调用epoll_wait等待收集发生事件的连接</li></ol><p>这样，只要在应用进程启动时，建立1个epoll对象(下文用epfd表示)，并在TCP连接到来和断开的时候，对epfd添加和删除事件就可以了（也可以修改）。<br>应用进程调用epoll_wait时，不需要向内核空间传递这100万个连接，内核也不需要遍历全部的连接。所以epoll_wait很高效。</p><h3 id="epoll的原理"><a href="#epoll的原理" class="headerlink" title="epoll的原理"></a>epoll的原理</h3><p>当应用进程调用epoll_create时，内核会在内核空间创建一个独立的eventpoll结构体对象，用于维护使用epoll_ctl向其添加的事件。</p><h4 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h4><p>epoll的数据结构如图所示。eventpoll有两个核心的数据结构：</p><ol><li>红黑树(rbr)：维护通过epoll_ctl添加的事件。</li><li>就绪链表(rdllist)：保存就绪的事件，当事件发生时，由内核的中断处理程序插入该就绪链表。</li></ol><p><img src="1.png" alt="图片"></p><blockquote><p>说明: 这张图来自于《深入理解Nginx》，网上很多博客用了这张图，但是这张图关于就绪链表的描述有个小错误。”红黑树中每个节点都是基于epitem结构中的rdllink成员”应该改成”就绪链表中每个节点都是基于epitem结构中的rdllink成员”。</p></blockquote><figure class="highlight gauss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"> <span class="number">1</span><span class="comment">// 这里只列出了成员rbr、rdllist，它们和epoll的使用密切相关</span></span><br><span class="line"> <span class="number">2</span><span class="keyword">struct</span> eventpoll &#123;</span><br><span class="line"> <span class="number">3</span>  ...</span><br><span class="line"> <span class="number">4</span>  <span class="comment">// 红黑树的根节点，这棵树中存储着所有添加到epoll中的事件，即这个epoll监控的事件</span></span><br><span class="line"> <span class="number">5</span>  <span class="keyword">struct</span> <span class="type">rb_root</span> rbr;</span><br><span class="line"> <span class="number">6</span></span><br><span class="line"> <span class="number">7</span>  <span class="comment">// 双向链表rdllist保存着要通过epoll_wait返回给应用程序的满足条件的事件</span></span><br><span class="line"> <span class="number">8</span>  <span class="keyword">struct</span> <span class="type">list_head</span> rdllist;</span><br><span class="line"> <span class="number">9</span>  ...</span><br><span class="line"><span class="number">10</span>&#125;;</span><br><span class="line"> <span class="number">1</span><span class="keyword">struct</span> epitem &#123;</span><br><span class="line"> <span class="number">2</span>  ...</span><br><span class="line"> <span class="number">3</span>  <span class="comment">// 红黑树节点</span></span><br><span class="line"> <span class="number">4</span>  <span class="keyword">struct</span> <span class="type">rb_node</span> rbn;</span><br><span class="line"> <span class="number">5</span>  <span class="comment">// 双向链表节点</span></span><br><span class="line"> <span class="number">6</span>  <span class="keyword">struct</span> <span class="type">list_head</span> rdllink;</span><br><span class="line"> <span class="number">7</span>  <span class="comment">// 事件句柄等信息</span></span><br><span class="line"> <span class="number">8</span>  <span class="keyword">struct</span> <span class="type">epoll_filefd</span> ffd;</span><br><span class="line"> <span class="number">9</span>  <span class="comment">// 指向其所属的eventpoll对象</span></span><br><span class="line"><span class="number">10</span>  <span class="keyword">struct</span> <span class="type">eventpoll</span> *ep;</span><br><span class="line"><span class="number">11</span>  <span class="comment">// 期待的事件类型</span></span><br><span class="line"><span class="number">12</span>  <span class="keyword">struct</span> <span class="type">epoll_event</span> event;</span><br><span class="line"><span class="number">13</span>  ...</span><br><span class="line"><span class="number">14</span>&#125;</span><br></pre></td></tr></table></figure><h4 id="实现原理"><a href="#实现原理" class="headerlink" title="实现原理"></a>实现原理</h4><p>在epoll中，为每个事件都建立一个epitem结构体对象。<br>这些事件都会添加到rbr红黑树中（重复添加的事件可以通过红黑树高效地识别出来）。从红黑树中查找事件非常快。</p><p>所有添加到epoll对象中的事件都会与设备驱动程序（如网卡驱动程序）建立回调关系，当相应的事件发生时，会调用回调函数（中断处理程序）。这个回调函数在内核叫<code>ep_poll_callback</code>，回调函数会把就绪的事件写入rdllist双向链表中。</p><p>当应用程序调用epoll_wait检查是否有事件发生的连接时，内核只是检查eventpoll对象的rdllist双向链表是否有epitem元素而已。如果rdllist链表不为空，内核把这里的事件复制到用户空间，同时返回对应的事件数量。</p><h4 id="高效原因"><a href="#高效原因" class="headerlink" title="高效原因"></a>高效原因</h4><p>最后总结分析下epoll之所以可以处理百万级别的并发连接，而且效率很高的原因。</p><ol><li>应用程序在调用系统调用epoll_create创建epoll时，内核为它开辟了一片内存空间。把listen fd存在里面，以及当每次来客户端请求，三次握手后建立的client fd都会存在内核态的这个内存空间。这样不同于select，避免了用户空间和内核空间之间重复传递fd的过程。</li><li>应用程序要知道哪些事件可读了，不同于在select中应用程序需要主动遍历所有fd，内核只是将就绪列表中的事件复制到用户空间的event数组中（应用程序提前申请好的内存），这样应用程序只需要遍历这些就绪的事件。</li><li>就绪事件是怎么放入就绪列表的？答案是epoll利用了事件驱动。当数据包进入网卡，网卡将数据包通过DMA方式写入内存。网卡raise硬件中断IRQ，通知CPU有数据包到来了。CPU查询中断向量表，得到中断服务程序的指针，这个中断服务程序会调用网卡驱动程序。这里的中断服务程序是事先注册的，所以也可以理解CPU根据中断号回调中断处理程序，从内存读取数据，得到事件的fd，写入就绪列表rdllist中。</li></ol><blockquote><p>这里用到了上一篇《深入理解Linux IO模型(一)》写到的硬中断，不熟悉的可以看一下。</p></blockquote><p>从程序的本质上看，程序是否有更好的并发，是看少浪费了什么。<br>CPU、内存、硬盘、网络带宽的利用率决定程序是不是能应对更复杂的场景。<br>当遇到高并发问题，怀疑程序在服务器上运转不良好时，一定要回过头看硬件有没有被充分利用，有没有浪费硬件资源。<br>CPU在执行什么事情的指令，决定了它的利用率。<br>select需要CPU在内核模式下（CPU的一种执行模式）主动去遍历所有描述符，这样CPU浪费在了遍历上。而epoll靠的是硬件中断，利用中断把就绪fd写入就绪列表。更充分发挥了硬件，不浪费CPU。这是epoll高效的最大原因。</p><h3 id="epoll的API"><a href="#epoll的API" class="headerlink" title="epoll的API"></a>epoll的API</h3><p>epoll提供给应用程序的系统调用API有三个。</p><ul><li>创建epoll：epoll_create系统调用</li><li>控制epoll：epoll_ctl系统调用</li><li>等待epoll：epoll_wait系统调用</li></ul><h4 id="创建epoll"><a href="#创建epoll" class="headerlink" title="创建epoll"></a>创建epoll</h4><p>创建epoll，指的是在内核空间创建一颗红黑树（平衡二叉树）的根节点root。它返回一个fd（下文用epfd指代），用来标识这个epoll对象。这个root根节点与epfd相对应。如图所示。</p><p><img src="2.png" alt="图片"></p><p>API如下：</p><figure class="highlight basic"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span>/** </span><br><span class="line"><span class="symbol">2 </span>* @param size 告诉epoll要处理的大致事件数量，而不是能处理的事件最大数量。</span><br><span class="line"><span class="symbol">3 </span>* </span><br><span class="line"><span class="symbol">4 </span>* @returns 返回一个epoll句柄（即一个文件描述符） </span><br><span class="line"><span class="symbol">5 </span>*/</span><br><span class="line"><span class="number">6</span><span class="keyword">int</span> epoll_create(<span class="keyword">int</span> size);</span><br></pre></td></tr></table></figure><p>调用方法：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span>int epfd = epoll_create(<span class="number">1000</span>);</span><br></pre></td></tr></table></figure><h4 id="控制epoll"><a href="#控制epoll" class="headerlink" title="控制epoll"></a>控制epoll</h4><p>控制epoll，指的是以下三种操作:</p><ul><li>注册新的事件到epoll</li><li>修改已经注册的事件</li><li>删除一个注册到epoll的事件</li></ul><p>添加某事件的时候，事件被插到红黑树的某个节点，并且与相应的设备驱动程序建立回调关系。当事件发生后，内核中断处理程序调用这个回调函数，将事件添加到就绪链表。</p><p>API如下:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"> <span class="number">1</span><span class="string">/**</span></span><br><span class="line"> <span class="number">2</span><span class="string">*</span> <span class="string">@param</span> <span class="string">epfd</span> <span class="string">用epoll_create所创建的epoll实例</span></span><br><span class="line"> <span class="number">3</span><span class="string">*</span> <span class="string">@param</span> <span class="string">op</span> <span class="string">表示对epoll监控描述符控制的动作</span></span><br><span class="line"> <span class="number">4</span><span class="string">*</span></span><br><span class="line"> <span class="number">5</span><span class="string">*</span> <span class="string">EPOLL_CTL_ADD(添加新的事件到epoll中)</span></span><br><span class="line"> <span class="number">6</span><span class="string">*</span> <span class="string">EPOLL_CTL_MOD(修改已经注册到epoll中的事件)</span></span><br><span class="line"> <span class="number">7</span><span class="string">*</span> <span class="string">EPOLL_CTL_DEL(删除epoll中的事件)</span></span><br><span class="line"> <span class="number">8</span><span class="string">*</span></span><br><span class="line"> <span class="number">9</span><span class="string">*</span> <span class="string">@param</span> <span class="string">fd</span> <span class="string">待监测的连接fd</span></span><br><span class="line"><span class="number">10</span><span class="string">*</span> <span class="string">@param</span> <span class="string">event</span> <span class="string">告诉内核需要监听的事件(包括类型)，指向epoll_event的指针</span></span><br><span class="line"><span class="number">11</span><span class="string">*</span></span><br><span class="line"><span class="number">12</span><span class="string">*</span> <span class="string">@returns</span> <span class="string">成功返回0，失败返回-1,</span> <span class="string">errno查看错误信息</span></span><br><span class="line"><span class="number">13</span><span class="string">*/</span></span><br><span class="line"><span class="string">14int</span> <span class="string">epoll_ctl(int</span> <span class="string">epfd,</span> <span class="string">int</span> <span class="string">op,</span> <span class="string">int</span> <span class="string">fd,</span></span><br><span class="line"><span class="string">15struct</span> <span class="string">epoll_event</span> <span class="string">*event);</span></span><br><span class="line"><span class="number">16</span></span><br><span class="line"><span class="string">17struct</span> <span class="string">epoll_event</span> <span class="string">&#123;</span></span><br><span class="line"><span class="number">18</span>  <span class="string">__uint32_t</span> <span class="string">events;</span> <span class="string">//</span> <span class="string">epoll</span> <span class="string">事件</span></span><br><span class="line"><span class="number">19</span>  <span class="string">epoll_data_t</span> <span class="string">data;</span> <span class="string">//</span> <span class="string">用户传递的数据</span></span><br><span class="line"><span class="number">20</span><span class="string">&#125;</span></span><br><span class="line"><span class="number">21</span></span><br><span class="line"><span class="string">22typedef</span> <span class="string">union</span> <span class="string">epoll_data</span> <span class="string">&#123;</span></span><br><span class="line"><span class="number">23</span>  <span class="string">void</span> <span class="string">*ptr;</span></span><br><span class="line"><span class="number">24</span>  <span class="string">int</span> <span class="string">fd;</span> <span class="string">//</span> <span class="string">监听的事件fd</span></span><br><span class="line"><span class="number">25</span>  <span class="string">uint32_t</span> <span class="string">u32;</span></span><br><span class="line"><span class="number">26</span>  <span class="string">uint64_t</span> <span class="string">u64;</span></span><br><span class="line"><span class="number">27</span><span class="string">&#125;</span> <span class="string">epoll_data_t;</span></span><br></pre></td></tr></table></figure><p>关于epoll_event，具体介绍下。上文介绍过epoll为每个事件创建epitem对象，在结构体epitem中有一个成员epoll_event。</p><p>epoll_event.events的取值包括：<br>EPOLLIN：表示对应的连接上有数据可以读出（TCP连接的远端主动关闭连接，也相当于可读事件，因为要处理发过来的FIN包）<br>EPOLLOUT：表示对应的连接上可以写入数据发送<br>EPOLLRDHUP：表示TCP连接的远端关闭或半关闭连接<br>EPOLLPR：表示对应的链接上有紧急数据需要读<br>EPOLLERR：表示对应的连接发生错误<br>EPOLLHUP：表示对应的连接被挂起<br>EPOLLET：表示将处罚方式设置为边缘触发（ET），系统默认为水平触发（LT）<br>EPOLLONESHOT：表示对这个事件只处理一次，下次需要处理时需要重新加入epoll</p><p>调用方法:</p><figure class="highlight haxe"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span>struct epoll_event <span class="keyword">new</span><span class="type">_event</span>;</span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">3</span><span class="keyword">new</span><span class="type">_event</span>.events = EPOLLIN | EPOLLOUT;</span><br><span class="line"><span class="number">4</span><span class="keyword">new</span><span class="type">_event</span>.data.fd = <span class="number">5</span>;</span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">6</span>epoll_ctl(epfd, EPOLL_CTL_ADD, <span class="number">5</span>, &amp;<span class="keyword">new</span><span class="type">_event</span>);</span><br></pre></td></tr></table></figure><p>在用户空间创建一个IO事件，绑定到某个fd上，然后把该事件的fd添加到内核中的epoll红黑树中。当fd可读或可写时，触发的是epoll_event。如图所示。</p><p><img src="3.png" alt="图片"></p><h4 id="等待epoll"><a href="#等待epoll" class="headerlink" title="等待epoll"></a>等待epoll</h4><p>收集在epoll监控的事件中已经发生的事件，内核会检查就绪链表中是否有存在添加过的事件，如果没有任何事件发生，最多等待timeout毫秒后返回（在timeout设置&gt;0的情况下）。返回值表示当前发生的事件个数。</p><p>API如下:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"> <span class="number">1</span><span class="string">/**</span></span><br><span class="line"> <span class="number">2</span><span class="string">*</span></span><br><span class="line"> <span class="number">3</span><span class="string">*</span> <span class="string">@param</span> <span class="string">epfd</span> <span class="string">用epoll_create所创建的epoll实例</span></span><br><span class="line"> <span class="number">4</span><span class="string">*</span> <span class="string">@param</span> <span class="string">event</span> <span class="string">从内核得到的就绪的事件集合</span></span><br><span class="line"> <span class="number">5</span><span class="string">*</span> <span class="string">@param</span> <span class="string">maxevents</span> <span class="string">本次可以返回的最大事件数目，通常与预分配的event数组大小相等</span></span><br><span class="line"> <span class="number">6</span><span class="string">*</span> <span class="string">注意:</span> <span class="string">值不能大于创建epoll_create()时的size</span></span><br><span class="line"> <span class="number">7</span><span class="string">*</span> <span class="string">@param</span> <span class="string">timeout</span> <span class="string">等待IO事件发生的超时时间</span></span><br><span class="line"> <span class="number">8</span><span class="string">*</span> <span class="number">-1</span><span class="string">:</span> <span class="string">永久阻塞</span></span><br><span class="line"> <span class="number">9</span><span class="string">*</span> <span class="attr">0:</span> <span class="string">如果就绪链表rdllist为空，立即返回，不会等待，即非阻塞</span></span><br><span class="line"><span class="number">10</span><span class="string">*</span> <span class="string">&gt;0:</span> <span class="string">指定最多等待的时间，单位毫秒</span></span><br><span class="line"><span class="number">11</span><span class="string">*</span></span><br><span class="line"><span class="number">12</span><span class="string">*</span> <span class="string">@returns</span> <span class="string">成功:</span> <span class="string">有多少文件描述符就绪,时间到时返回0</span></span><br><span class="line"><span class="number">13</span><span class="string">*</span> <span class="string">失败:</span> <span class="number">-1</span><span class="string">,</span> <span class="string">errno</span> <span class="string">查看错误</span></span><br><span class="line"><span class="number">14</span><span class="string">*/</span></span><br><span class="line"><span class="string">15int</span> <span class="string">epoll_wait(int</span> <span class="string">epfd,</span> <span class="string">struct</span> <span class="string">epoll_event</span> <span class="string">*event,</span></span><br><span class="line"><span class="number">16</span>               <span class="string">int</span> <span class="string">maxevents,</span> <span class="string">int</span> <span class="string">timeout);</span></span><br></pre></td></tr></table></figure><blockquote><p>注意: epoll_event不能是空指针，内核只是负责把内核空间中就绪链表的数据复制到用户空间的event数组中，不会去帮忙分配内存，所以用户空间需要自己提前分配内存。</p></blockquote><p>调用方法:</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span>struct epoll_event my_event[<span class="number">1000</span>];</span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">3</span>int event_cnt = epoll_wait(epfd, my_event, <span class="number">1000</span>, <span class="number">-1</span>);</span><br></pre></td></tr></table></figure><p>如图所示。应用程序调用epoll_wait后，进入阻塞状态。当内核检测到new_event或event1绑定的fd可读了，内核把就绪的event事件拷贝到用户空间的my_event数组。应用程序只需要遍历my_event，取出对应的事件和fd，知道是可读了，然后堵塞调用recv(fd)读数据。</p><p><img src="4.png" alt="图片"></p><h3 id="使用epoll-API"><a href="#使用epoll-API" class="headerlink" title="使用epoll API"></a>使用epoll API</h3><p>一个简单的使用epoll API的编程架构如下。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"> <span class="number">1</span><span class="comment">// 创建epoll fd，最多可接收1000个事件</span></span><br><span class="line"> <span class="number">2</span>int epfd = epoll_crete(<span class="number">1000</span>);</span><br><span class="line"> <span class="number">3</span></span><br><span class="line"> <span class="number">4</span><span class="comment">// 将listen_fd添加进epoll中</span></span><br><span class="line"> <span class="number">5</span>epoll_ctl(epfd, EPOLL_CTL_ADD, listen_fd, &amp;listen_event);</span><br><span class="line"> <span class="number">6</span></span><br><span class="line"> <span class="number">7</span>while (<span class="number">1</span>) &#123;</span><br><span class="line"> <span class="number">8</span>  <span class="comment">// 阻塞等待epoll中的事件fd触发</span></span><br><span class="line"> <span class="number">9</span>  int active_cnt = epoll_wait(epfd, events, <span class="number">1000</span>, <span class="number">-1</span>);</span><br><span class="line"><span class="number">10</span></span><br><span class="line"><span class="number">11</span>  for (i = <span class="number">0</span> ; i &lt; active_cnt; i++) &#123;</span><br><span class="line"><span class="number">12</span>    if (evnets[i].data.fd == listen_fd) &#123;</span><br><span class="line"><span class="number">13</span>      <span class="comment">// 表示新的客户端连接请求到来</span></span><br><span class="line"><span class="number">14</span>      <span class="comment">// 调用accept进行三次握手，创建client_fd</span></span><br><span class="line"><span class="number">15</span>      <span class="comment">// 并将client_fd加进epoll中</span></span><br><span class="line"><span class="number">16</span>    &#125;</span><br><span class="line"><span class="number">17</span>    else if (events[i].events &amp; EPOLLIN) &#123;</span><br><span class="line"><span class="number">18</span>      <span class="comment">// 表示不是新的客户端连接，可读客户端发来的数据</span></span><br><span class="line"><span class="number">19</span>      <span class="comment">// client_fd就绪可读，对此fd进行读操作</span></span><br><span class="line"><span class="number">20</span>    &#125;</span><br><span class="line"><span class="number">21</span>    else if (events[i].events &amp; EPOLLOUT) &#123;</span><br><span class="line"><span class="number">22</span>      <span class="comment">// 表示不是新的客户端连接，可把数据回写客户端</span></span><br><span class="line"><span class="number">23</span>      <span class="comment">// client_fd就绪可写，对此fd进行写操作</span></span><br><span class="line"><span class="number">24</span>    &#125;</span><br><span class="line"><span class="number">25</span>  &#125;</span><br><span class="line"><span class="number">26</span>&#125;</span><br></pre></td></tr></table></figure><p>如图所示，是一个服务器使用epoll的常规流程。</p><p><img src="5.png" alt="图片"></p><h3 id="触发方式"><a href="#触发方式" class="headerlink" title="触发方式"></a>触发方式</h3><p>epoll有两种工作模式：水平触发和边缘触发。默认情况下，epoll采用水平触发模式。</p><h4 id="水平触发-Level-Triggered-LT"><a href="#水平触发-Level-Triggered-LT" class="headerlink" title="水平触发(Level Triggered, LT)"></a>水平触发(Level Triggered, LT)</h4><p>如果应用程序阻塞在epoll_wait，当内核有事件发生的时候，内核把已经触发的事件队列复制到用户空间。如果应用程序本次没有完成读操作，下一次epoll_wait会再次返回该事件。<br>即只要一个事件对应的套接字缓冲区还有数据，就总能从epoll_wait中获取这个事件。</p><p>优点：事件不会丢掉，除非应用程序处理完毕。保证事件的完整性。<br>缺点：如果应用程序不处理这个事件，就导致内核每次都把该事件从内核空间拷贝到用户空间，系统调用消耗性能。</p><h4 id="边缘触发-Edge-Triggered-ET"><a href="#边缘触发-Edge-Triggered-ET" class="headerlink" title="边缘触发(Edge Triggered, ET)"></a>边缘触发(Edge Triggered, ET)</h4><p>如果应用程序阻塞在epoll_wait，当内核有事件发生的时候，内核把已经触发的事件队列复制到用户空间。如果应用程序本次没有完成读操作，下一次epoll_wait不再会返回该事件。<br>即如果这次没有把这个事件对应的套接字缓冲区处理完，在这个套接字没有新的事件再次到来时，无法再次从epoll_wait调用中获取到这个事件。</p><p>优点：内核不会重复把该事件从内核空间拷贝到用户空间。保证性能。<br>缺点：如果应用程序没有处理完毕，事件会被丢掉。导致事件不完整。</p><h4 id="两者对比"><a href="#两者对比" class="headerlink" title="两者对比"></a>两者对比</h4><p>在水平触发模式下，开发基于epoll的应用要简单一些，不太容易出错。而在边缘触发模式下，当事件发生时，如果没有彻底地将缓冲区数据处理完，则会导致缓冲区中的用户请求得不到响应。</p><h3 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h3><p>深入理解Nginx（第2版）<a href="https://book.douban.com/subject/26745255/" rel="external nofollow noopener noreferrer" target="_blank">https://book.douban.com/subject/26745255/</a><br><a href="https://mp.weixin.qq.com/s/kWDKpgmcOQFjoBAK3LyPTg" rel="external nofollow noopener noreferrer" target="_blank">https://mp.weixin.qq.com/s/kWDKpgmcOQFjoBAK3LyPTg</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;深入理解Linux-IO模型-二&quot;&gt;&lt;a href=&quot;#深入理解Linux-IO模型-二&quot; class=&quot;headerlink&quot; title=&quot;深入理解Linux IO模型(二)&quot;&gt;&lt;/a&gt;深入理解Linux IO模型(二)&lt;/h1&gt;&lt;p&gt;原创 Pearl &lt;a </summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
    <category term="linux" scheme="http://www.shankusu.me/tags/linux/"/>
    
    <category term="epoll" scheme="http://www.shankusu.me/tags/epoll/"/>
    
    <category term="select" scheme="http://www.shankusu.me/tags/select/"/>
    
  </entry>
  
  <entry>
    <title>深入理解Linux IO模型(一)</title>
    <link href="http://www.shankusu.me/os/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3LinuxIO%E6%A8%A1%E5%9E%8B%E4%B8%80/"/>
    <id>http://www.shankusu.me/os/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3LinuxIO%E6%A8%A1%E5%9E%8B%E4%B8%80/</id>
    <published>2021-08-26T09:50:32.000Z</published>
    <updated>2021-08-26T17:10:52.168Z</updated>
    
    <content type="html"><![CDATA[<h3 id="深入理解Linux-IO模型-一"><a href="#深入理解Linux-IO模型-一" class="headerlink" title="深入理解Linux IO模型(一)"></a>深入理解Linux IO模型(一)</h3><p>原创 Pearl <a href="javascript:void(0" target="_blank" rel="noopener">Pearl的仲夏夜之梦</a>;) <em>6月12日</em></p><p>Linux IO模型是后端工程师的必备技能。从以往的面试中看，部分后端开发人员对它的理解停留在调API的层面，我自己也理解欠缺。最近系统学习了一下，整理了此文。本文参考了一些文章，放在本文最后，大家可以直接去看这些文章，值得阅读。</p><blockquote><ol><li>本文为了描述方便，统一用读操作讲述，写操作同理。</li><li>本文为了撰写方便，统一将I/O写成了IO。</li><li>欢迎指正文中的错误。</li></ol></blockquote><h3 id="操作系统预备知识"><a href="#操作系统预备知识" class="headerlink" title="操作系统预备知识"></a>操作系统预备知识</h3><h4 id="UNIX体系结构"><a href="#UNIX体系结构" class="headerlink" title="UNIX体系结构"></a>UNIX体系结构</h4><p>UNIX操作系统的体系结构如图所示。</p><p><img src="1.png" alt="图片"></p><p>内核(kernel)：控制计算机硬件资源，提供程序运行环境。<br>系统调用(system call)：内核的函数接口。<br>公共函数库：构建在系统调用之上的函数接口。<br>shell：特殊的应用程序，为运行其他应用程序提供了一个接口。<br>应用程序：用户编写的程序。可使用公共函数库，也可直接调用系统调用。</p><h4 id="系统调用"><a href="#系统调用" class="headerlink" title="系统调用"></a>系统调用</h4><p>进一步介绍下系统调用(syscall)。<br>内核用于控制硬件资源，例如从磁盘上读写文件，需要控制硬盘这个硬件设备做IO操作。应用代码通过调用内核暴露出来的系统调用接口来使内核进行IO操作。<br>如图所示，库函数调用系统调用接口，应用程序可以调用系统调用和库函数。</p><p><img src="2.png" alt="图片"></p><p>例如用户常用的<code>printf</code>函数，可以调用它输出内容到显示器上，但是控制显示器的输出是内核。系统调用提供的是<code>write</code>函数，<code>printf</code>是库函数，它封装了<code>write</code> 这个系统调用接口。</p><h4 id="用户空间和内核空间"><a href="#用户空间和内核空间" class="headerlink" title="用户空间和内核空间"></a>用户空间和内核空间</h4><p>对于32位CPU（表示CPU的寄存器长度为32位），指令集长度32位，数据总线宽度32位，地址总线宽度32位（因为受到寄存器长度的限制，再大也是浪费，无法把指令或数据从内存装载到寄存器或把寄存器的值写入内存）。因为地址总线宽度32位，所以最大寻址范围2^32，即对应 2^32*8bit=4GB 内存寻址空间。虽然内存的最大寻址容量只有4GB，但是每个进程的虚拟存储空间却都为4GB。</p><blockquote><p>虚拟存储空间是什么？</p><ol><li>MMU(内存管理单元)通过段页式存储管理，负责物理地址和逻辑地址(虚拟地址)的转化。逻辑空间可以理解为内存空间和磁盘空间之间的抽象，为了解决容量问题。</li><li>程序的局部性原理。CPU访问内存时，无论是存取指令还是数据，所访问的存储单元都趋于聚集在一个较小的连续区域中。程序运行时，无需全部装入内存，如果访问页不在内存，发出缺页中断，发起页面置换（页面置换有常用的几种算法，FIFO、LFU、LRU）。</li></ol><p>操作系统怎么划分的虚拟存储空间？</p><ol><li>程序在磁盘中，加载进内存后，才能变成进程运行起来。内存的第一个进程是kernel。</li><li>kernel会注册一个GDT(Global Descriptor Table)，把4GB虚拟内存划分成用户空间和内核空间。最高的1GB，从虚拟地址 0xC0000000 到 0xFFFFFFFF），供内核使用，作为内核空间；较低的3GB，从虚拟地址0x00000000到0xBFFFFFFF），供各个应用进程使用，作为用户空间。</li></ol></blockquote><p>内核独立于普通的应用程序，可以访问受保护的内存空间，也有访问底层硬件设备的所有权限。为了保证内核的安全，用户进程不能直接操作内核。操作系统将4GB的虚拟存储空间划分为两部分，用户空间和内核空间。</p><blockquote><p>CPU如何区分指令来自内核空间还是用户空间的？<br>指令存储在内存中，通过数据总线加载到CPU的指令寄存器，CPU解码执行。实际上CPU本身并不能区分是谁发出的指令，而是通过特权等级来区分。以X86架构来说，CPU指令集的特权等级分为Ring0~3，内核空间对应指令集Ring0，具有最高权限，可以访问所有资源；用户空间对应指令集Ring3，不能直接访问硬件设备。</p><p>如果用户空间存在特权指令，CPU如何区分这个指令来自用户进程从而禁止执行？<br>CPU有两种执行模式，用户模式和内核模式。用户模式受到限制，某些指令不能被执行，某些寄存器不能被访问，IO设备也不能被访问。内核模式则没有这些限制，可以执行所有的机器指令，可以读写所有的内存位置。<br>这个问题我理解可能不到位，抛砖引玉。</p></blockquote><h4 id="用户态和内核态"><a href="#用户态和内核态" class="headerlink" title="用户态和内核态"></a>用户态和内核态</h4><p>进程运行时会有用户态和内核态的区别。<br>如图所示，程序执行时，如果执行的是用户空间的应用代码，这些代码运行在用户态；当调用了系统调用后，内核空间的内核代码就会执行，内核中的这些代码运行在内核态。</p><p><img src="3.png" alt="图片"></p><h4 id="进程阻塞"><a href="#进程阻塞" class="headerlink" title="进程阻塞"></a>进程阻塞</h4><p>进程有五个状态，创建、就绪、执行、阻塞和终止。<br>就绪状态：当进程被分配到除CPU以外所有必要的资源（包括PCB、栈空间、堆空间等）后。只要获得CPU的使用权，就可以立即执行。<br>执行状态：进程获得CPU，在执行的状态。单CPU同一时刻只能有一个进程在执行状态。<br>阻塞状态：因为某种原因如IO未就绪，进程放弃CPU的使用权，进入阻塞状态。</p><p>进程模型之间的切换关系如图所示。</p><p><img src="4.png" alt="图片"></p><p>进程切换(进程调度)是指操作系统通过某种进程调度算法决定哪个就绪进程可以获得CPU的使用权。进一步说，内核以一定策略挂起当前正在利用CPU运行的进程，并保存进程的上下文运行信息，然后分配CPU给某个就绪状态的另一个进程执行。</p><p>可以看出，进程阻塞是进程的主动行为，只有处于获得CPU在执行状态的进程，才可能转变成阻塞状态。当进程进入阻塞状态，不占用CPU资源。所以，在执行IO请求后进入阻塞状态的进程，是不占用CPU资源的。</p><blockquote><p>PCB(进程控制块)指的是什么？<br>PCB是进程常驻在内存中的通用数据结构，记录进程运行的全部信息，被用于操作系统调用时读取。记录包括进程的标识符、状态、优先级、程序计数器、内存指针、CPU的上下文数据、被进程占用的IO的状态信息、记账信息等。</p></blockquote><h4 id="中断"><a href="#中断" class="headerlink" title="中断"></a>中断</h4><p>理解中断，对理解IO模型很重要。</p><h5 id="任务事件"><a href="#任务事件" class="headerlink" title="任务事件"></a>任务事件</h5><p>操作系统是事件驱动的，只有在有中断、陷阱或系统调用时才执行。如图所示。</p><p><img src="5.png" alt="图片"></p><blockquote><p>为什么操作系统要采用事件驱动设计呢？</p><ol><li>操作系统不能信任用户进程</li></ol><ul><li>用户进程可能是错误的或恶意的</li><li><p>用户进程崩溃不应影响操作系统</p><p>2.操作系统需要保证对所有用户进程公平性</p></li><li><p>一个进程不能霸占CPU时间</p></li><li>采用定时中断的方式</li></ul></blockquote><p>事件触发简化流程如图所示。</p><p><img src="6.png" alt="图片"></p><p>事件有中断和异常两类。中断由硬件或者程序触发，以引起操作系统的注意。异常由于非法操作而导致的。本文不讲异常，只讲中断。<br>中断又有硬件中断和软件中断之分：硬件中断由外部硬件设备触发；软件中断由应用进程触发。</p><p>每个中断都有一个编号，称为中断向量(interrupt vector)，用于在中断描述符表IDT(也称为中断向量表)中进行索引，从而获得中断服务程序的指针，即中断处理程序的入口点。</p><blockquote><p>为什么「中断向量」不叫「中断指针」？我猜可能是历史原因，知道的读者可以留言告诉我。</p></blockquote><h5 id="硬件中断"><a href="#硬件中断" class="headerlink" title="硬件中断"></a>硬件中断</h5><p>与CPU相连接的外部设备，如键盘、鼠标、网卡等，偶尔需要CPU提供服务，但是CPU无法预测它们何时发生。<br>如果在数据采集系统中，可以采用CPU定期轮询设备的方式，以确定它们是否需要提供服务。否则，轮询会浪费CPU资源。<br>所以引入了硬件中断的方式，每个连接的外部设备都可以向CPU发出信号，表示它们需要CPU提供服务。一般来说，CPU有2个引脚，INT用于中断，NMI用于不可屏蔽的关键的信号。</p><p>如图所示，是8259可编程中断控制器。可支持转发8个中断。当设备通过中断请求(IRQ)引发中断，CPU确认并查询8259以确定哪个设备产生中断。8259可以为每个IRQ线分配优先权，可以级联以支持更多的中断。</p><p><img src="7.png" alt="图片"></p><p>当硬件中断发生的时候，发生了什么？流程如图所示。</p><p><img src="8.png" alt="图片"></p><p>第1步，CPU完成当前指令后，立即响应硬件中断，先获取到中断向量。<br>第2步，切换到内核堆栈不是必须的，因为只有从用户模式转到内核模式，才需要进行堆栈切换。有可能中断来临时，CPU正在内核模式执行内核的指令。<br>第3步，保存程序状态，是为了保证当前正常执行的程序在中断服务完成后能恢复。<br>第4步到第7步，是中断处理程序执行流程。</p><blockquote><p>典型的中断处理程序过程是:</p><ol><li>保存CPU上下文</li><li>处理中断(如与IO设备通信)</li><li>调用内核调度程序</li><li>恢复CPU上下文并返回</li></ol></blockquote><p>如图所示，中断处理是有延时的，最小值受限于中断控制器，它的最大值受到操作系统的限制，如当内核和中断处理程序需要操作同一个全局变量，需要保证内核执行的是原子操作，中断处理程序需要等待原子操作完成，才能得到处理。<br><img src="9.png" alt="图片"></p><h5 id="软件中断"><a href="#软件中断" class="headerlink" title="软件中断"></a>软件中断</h5><p>下面讲软件中断，它是为何产生？<br>为了让CPU能尽快响应其它硬件中断，中断处理程序需要小型化，可以只是设置flag或放入工作队列，让非关键性的代码推迟执行。于是提出了Top and Bottom Half Technique。<br>Top half：做最小的工作并从中断处理程序中返回。如保存寄存器、取消对其他中断的屏蔽、恢复寄存器并返回到以前的上下文。<br>Bottom half ：对Top half剩下的工作延迟处理。</p><p>硬件中断和软件中断的直观对比如图所示。硬件中断由一个设备（如PIC）向CPU的一个引脚发出信号产生；软件中断由正在执行的某条指令产生。</p><p><img src="10.png" alt="图片"></p><p>软件中断通过请求系统调用产生，如图所示。</p><p><img src="11.png" alt="图片"></p><p>一个write system call的例子，如图所示。</p><p><img src="12.png" alt="图片"></p><p>软件中断产生、处理和返回的流程，如图所示。</p><p><img src="13.png" alt="图片"></p><p>操作系统是如何区分系统调用的？答案是利用 System call number。直观上看，如图所示。</p><p><img src="14.png" alt="图片"></p><h3 id="数据包的接收过程"><a href="#数据包的接收过程" class="headerlink" title="数据包的接收过程"></a>数据包的接收过程</h3><h4 id="网卡-gt-内存"><a href="#网卡-gt-内存" class="headerlink" title="网卡-&gt;内存"></a>网卡-&gt;内存</h4><p>数据包如何进入内存，并被内核的网络模块开始处理的？流程如图所示。</p><p><img src="15.png" alt="图片"></p><ol><li>数据包进入网卡（如果目的地址不是该网卡，且该网卡没有开启混杂模式，该包会被网卡丢弃）。</li><li>网卡将数据包通过DMA方式写入指定的内存地址（该地址由网卡驱动分配并初始化）。</li><li>网卡raise硬件中断IRQ，通知CPU，告诉有数据包到来。</li><li>CPU查询中断向量表，得到中断服务程序的指针，这个中断服务程序会调用网卡驱动程序中的相应函数。</li><li>网卡驱动先禁用网卡的硬件中断，表示驱动程序已经知道内存中有网络数据，如果网卡下次再接收到数据包，直接DMA方式写内存就可以，不需要raise硬件中断通知CPU（这样避免CPU不停地被中断）。</li><li>网卡驱动程序raise软件中断，内核启动软件中断服务。目的是将硬件中断服务程序中耗时久的部分放到软中断函数慢慢处理。</li></ol><h4 id="内存-gt-内核网络模块-gt-内核网络协议栈"><a href="#内存-gt-内核网络模块-gt-内核网络协议栈" class="headerlink" title="内存-&gt;内核网络模块-&gt;内核网络协议栈"></a>内存-&gt;内核网络模块-&gt;内核网络协议栈</h4><p>内核在软件中断服务中接收链路层帧，并逐层递交上层协议栈处理，处理流程如下。</p><ol><li>内核中有专门的进程负责接收网卡驱动raise的软中断，然后该进程调用对应的软中断处理函数，读取之前网卡写到内存中的数据包。</li><li>网卡驱动程序知道如何处理内存中的数据包格式，它将数据包转换成内核网络模块能识别的格式。</li><li>内核网络模块将数据包合并(这样可以减小调用协议栈的次数)，并将数据包放入CPU对应的接收队列(softnet_data.input_pkt_queue)中等待处理。</li><li>CPU在软中断上下文中处理队列中的网络数据。</li><li>调用协议栈相应的函数，把数据包交给协议栈处理。</li><li>协议栈的处理过程（IP层-&gt;TCP/UDP层）不展开描述了。</li><li>用户空间的应用层通过调用socket接口接收数据。比如调用recvfrom函数阻塞等待数据到来，当socket fd收到通知后，recvfrom函数被唤醒，然后读取数据；或通过select/epoll等IO多路复用方式监听多个socket fd，只要其中有fd收到通知，进程主动调用recvfrom函数去读取数据。本文后面会展开描述。</li></ol><blockquote><p>CPU的接收队列input_pkt_queue是什么？<br>网络设备模块在初始化时，为每个CPU初始化结构体softnet_data，用于处理网络数据。input_pkt_queue是该结构体的一个成员变量，作为接收队列，在对其操作的时候，关闭当前CPU的中断。<br>如果接收队列input_pkt_queue不为空，将接收队列拼接到处理队列process_queue上。接收队列input_pkt_queue清空，继续处理添加到处理队列process_queue的数据包，并且在处理前就打开当前的CPU中断。</p></blockquote><h3 id="关于IO的认知"><a href="#关于IO的认知" class="headerlink" title="关于IO的认知"></a>关于IO的认知</h3><h4 id="IO是什么"><a href="#IO是什么" class="headerlink" title="IO是什么"></a>IO是什么</h4><p>IO是指Input/Output，即输入和输出。<br>IO从广义上说，是数据流动的过程。<br>IO有内存IO、网络IO和磁盘IO等。</p><p>从计算机架构上讲，CPU和内存与其他外部设备之间的数据转移过程就是IO。<br>本文从用户进程的角度理解IO。用户进程要完成IO读写，需要对内核发起IO调用，内核执行IO任务，返回IO结果，即完成一次IO。内核为每个IO设备维护一个内核缓冲区。</p><h4 id="不带缓冲的IO和带缓冲的IO"><a href="#不带缓冲的IO和带缓冲的IO" class="headerlink" title="不带缓冲的IO和带缓冲的IO"></a>不带缓冲的IO和带缓冲的IO</h4><p>IO分为不带缓冲的IO和带缓冲的IO（标准IO）。</p><p>不带缓冲的IO：读和写都调用内核中的系统调用read和write，写入内核缓冲区。</p><p>带缓冲的IO：目的是减少调用系统调用read和write的次数。方法是在用户空间建立流缓冲区。例如用户多次调用fwrite将数据写入流缓冲区，等流缓冲区满的时候只调用一次系统调用write，写入内核缓冲区。标准IO库实现的就是对IO流的缓存管理。</p><p>需要注意，不管是哪种IO，内存和磁盘之间，总是会有内核缓冲区的，这是IO设备的缓冲区。</p><p>总结一下数据流向路径:<br>不带缓冲的IO: 数据—内核缓存区—磁盘<br>带缓冲的IO: 数据—流缓存区—内核缓存区—磁盘</p><p>不管是哪种IO，因为用户进程是运行在用户空间的，不能直接操作内核缓冲区的数据。所以数据在传输过程中，总是需要从内核缓冲区复制到用户进程空间（对于带缓冲的IO，就是需要在内核缓冲区到用户缓冲区之间复制）。这个复制的过程对CPU和内存的开销是比较大的。</p><h4 id="文件描述符"><a href="#文件描述符" class="headerlink" title="文件描述符"></a>文件描述符</h4><p>文件描述符(fd)在形式上是一个非负整数。<br>内核用以标记一个特定进程正在访问的文件。<br>当内核打开一个现有的文件或创建一个新的文件时，内核返回一个文件描述符，用于后续的IO操作。</p><h4 id="流"><a href="#流" class="headerlink" title="流"></a>流</h4><p>流是可以进行读写操作的内核对象。<br>比如文件、管道、套接字。<br>Linux一切皆文件，一切都是流。用户进程都是对这些流进行读写操作，实现数据交换。<br>用户进程用文件描述符fd实现对流的操作。<br>准确地说，流是带缓冲的IO（标准IO）才有的概念。<br>流有方向。对流的读写操作，可以理解为IO操作。如图所示。</p><p><img src="16.png" alt="图片"></p><p>如果流中没有数据，读取，就阻塞。进一步说，是用户缓冲区没有数据，无法读取数据。</p><p><img src="17.png" alt="图片"></p><p>如果流中数据已满，写入，就阻塞。进一步说，是用户缓冲区数据已满，无法写入数据。</p><p><img src="18.png" alt="图片"></p><h4 id="IO操作"><a href="#IO操作" class="headerlink" title="IO操作"></a>IO操作</h4><p>对于用户进程的一个读IO操作，包括以下阶段：<br>1.用户进程调用IO系统调用读数据。<br>2.内核先看下内核缓冲区是否有数据，如果没有数据，则从设备读取，先加载到内核缓冲区，再复制到用户进程缓冲区；如果有数据，直接复制到用户进程缓冲区（对于标准IO）。</p><p>直观的流程如图所示。</p><p><img src="19.png" alt="图片"></p><p>具体地说，对于一个网络IO输入操作，如果内核缓冲区无数据，包括以下阶段：</p><ol><li>用户进程调用Socket API</li><li>等待网络数据到达网卡这个硬件设备</li><li>通过DMA，直接从网卡读取到内核缓冲区</li><li>内核把内核缓冲区的数据复制到用户空间</li></ol><p>总结一下，一次完整的网络IO输入操作，是应用进程进行系统调用，内核从网卡读取数据写入内存，接着内核把数据从内存中复制到到用户空间的过程。<br>这个过程，有很多种IO模型可以处理，就引发了下文要讲的同步IO（包括阻塞IO、非阻塞IO、IO多路复用）和异步IO模型。</p><h4 id="IO就绪"><a href="#IO就绪" class="headerlink" title="IO就绪"></a>IO就绪</h4><p>我们常说的fd就绪，也就是IO就绪，是IO可读或可写了，即应用程序调用的内核系统调用返回结果了，可以从内核缓冲区读取数据或者写入数据到内核缓冲区。</p><h3 id="同步IO和异步IO"><a href="#同步IO和异步IO" class="headerlink" title="同步IO和异步IO"></a>同步IO和异步IO</h3><p>IO模型从大类上分，分为同步IO和异步IO。</p><h4 id="同步IO-synchronous-IO"><a href="#同步IO-synchronous-IO" class="headerlink" title="同步IO(synchronous IO)"></a>同步IO(synchronous IO)</h4><p>应用程序通过系统调用发送IO请求给内核后，必须等待IO返回后才继续执行后续代码。<br>同步IO有以下几种模型。</p><h5 id="阻塞式IO-blocking-IO"><a href="#阻塞式IO-blocking-IO" class="headerlink" title="阻塞式IO(blocking IO)"></a>阻塞式IO(blocking IO)</h5><p><img src="20.png" alt="图片"></p><ol><li>应用进程调用系统调用recvfrom，应用进程进入阻塞状态。</li><li>内核准备好数据，写入内核缓冲区。</li><li>内核将数据从内核缓冲区复制到用户空间。</li><li>应用进程被唤醒，进入执行状态，处理拿到的数据。</li></ol><blockquote><p>R1和R2阶段的应用进程都是阻塞的。</p></blockquote><h5 id="非阻塞式IO-nonblocking-IO"><a href="#非阻塞式IO-nonblocking-IO" class="headerlink" title="非阻塞式IO(nonblocking IO)"></a>非阻塞式IO(nonblocking IO)</h5><p><img src="21.png" alt="图片"></p><ol><li>应用进程轮询调用系统调用recvfrom（非阻塞方式），如果内核未准备好数据，返回错误EWOULDBLOCK。</li><li>内核准备好数据，写入内核缓冲区。</li><li>这次应用进程调用系统调用recvfrom，内核会返回非错误码数据，并将数据从内核缓冲区复制到用户空间。</li><li>应用进程被唤醒，进入执行状态，处理拿到的数据。</li></ol><blockquote><p>R1阶段的应用进程是非阻塞的，R2阶段的应用进程是阻塞的。</p></blockquote><h5 id="IO多路复用-IO-multiplexing"><a href="#IO多路复用-IO-multiplexing" class="headerlink" title="IO多路复用(IO multiplexing)"></a>IO多路复用(IO multiplexing)</h5><p><img src="22.png" alt="图片"></p><ol><li>应用进程调用系统调用select（这里以select为例，还有poll、epoll等IO多路复用器），进程进入阻塞状态。（这里的阻塞不同于阻塞式IO只等待一个socket fd，而是同时等待多个socket fd）</li><li>内核将可读的socket fd数据准备好，写入内核缓冲区。</li><li>应用进程收到select的返回结果，知道存在任意数量可读的socket fd。对于这些socket fd，应用进程遍历socket fd（select和poll是遍历所有fd，epoll是只遍历可读的fd），分别调用系统调用recvfrom，内核将数据从内核缓冲区复制到用户空间。</li><li>应用进程被唤醒，进入执行状态，处理拿到的数据（多个socket fd的返回结果）。</li></ol><blockquote><p>R1和R2阶段的应用进程都是阻塞的。</p></blockquote><h5 id="信号驱动IO-signal-driven-IO"><a href="#信号驱动IO-signal-driven-IO" class="headerlink" title="信号驱动IO(signal-driven IO)"></a>信号驱动IO(signal-driven IO)</h5><p><img src="23.png" alt="图片"></p><ol><li>应用进程内建立信号捕获函数，和socket fd关联。</li><li>应用进程调用系统调用sigaction，收到调用返回后，接着应用进程去执行其它代码。</li><li>当内核准备好数据，发送SIGIO信号给应用进程。</li><li>应用进程回调信号捕获函数，调用系统调用recvfrom。</li><li>应用进程被唤醒，进入执行状态，处理拿到的数据。</li></ol><blockquote><p>R1阶段的应用进程是非阻塞的，R2阶段的应用进程是阻塞的。</p></blockquote><h4 id="异步IO-asynchronous-IO"><a href="#异步IO-asynchronous-IO" class="headerlink" title="异步IO(asynchronous IO)"></a>异步IO(asynchronous IO)</h4><p>相对于同步IO，异步IO在用户进程调用系统调用aio_read以后，无论内核缓冲区数据是否准备好，都立即返回，不会阻塞当前进程，转而处理其它代码。</p><p><img src="24.png" alt="图片"></p><ol><li>应用进程调用系统调用aio_read，收到调用返回后，接着应用进程去执行其它代码。</li><li>内核准备好数据，并且将数据从内核缓冲区复制到用户空间。</li><li>内核发送aio_read中指定的信号给应用进程。</li><li>应用进程转而处理拿到的数据。</li></ol><blockquote><p>R1和R2阶段的应用进程都是非阻塞的。</p></blockquote><h3 id="为什么提出IO多路复用"><a href="#为什么提出IO多路复用" class="headerlink" title="为什么提出IO多路复用"></a>为什么提出IO多路复用</h3><p>以上在同步IO模型中已经介绍了IO多路复用。IO多路复用也是目前主流软件，如nginx、redis、kafka等使用的模型。它可以让一个线程在同一时刻监听多个socket fd。<br>技术的发展都是有迹可循，为了解决某种问题提出，那么IO多路复用是怎么产生的？</p><p>先介绍下阻塞等待和非阻塞忙轮询两种模型，分别对应上面同步IO中的阻塞式IO和非阻塞式IO。</p><h4 id="阻塞等待"><a href="#阻塞等待" class="headerlink" title="阻塞等待"></a>阻塞等待</h4><p>阻塞等待指的是被动地等待IO状态到来，即阻塞等待IO可读或可写。<br>阻塞等待的时候，CPU是空闲的，即不占用CPU的时间片（在上文已经说明原因）。<br>虽然不占用CPU时间片，但无法处理其它IO状态的到来（因为进程在阻塞状态等待IO可操作，无法进入就绪状态），即单个CPU无法并发处理多个IO请求。</p><p>优点：处理接收数据的时候，不浪费性能资源。<br>缺点：同一时刻，只能处理一个流的阻塞监听，即单个CPU不能并发处理多个IO请求。</p><p>虽然可以用阻塞+多线程/多进程的模型，实现多个CPU可以同一时刻监听多个IO状态。<br>但是开辟线程/进程浪费内存资源，而且切换线程/进程也浪费CPU。</p><h4 id="非阻塞忙轮询"><a href="#非阻塞忙轮询" class="headerlink" title="非阻塞忙轮询"></a>非阻塞忙轮询</h4><p>非阻塞忙轮询指的是主动地轮询IO状态，判断可读或可写。<br>非阻塞忙轮询判断IO状态的时候，CPU是忙碌的，即CPU时间片被占用。<br>注意：所以非阻塞忙轮询和异步是两个概念。</p><p>缺点：浪费CPU。</p><p>伪代码如下。CPU 大部分时间在做 while 和 for 判断处理，CPU 的利用率不高。</p><figure class="highlight basic"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span><span class="keyword">while</span> true &#123;</span><br><span class="line"><span class="symbol">2 </span> <span class="keyword">for</span> i in 流[] &#123;</span><br><span class="line"><span class="symbol">3 </span>   <span class="keyword">if</span> i has 数据 &#123;</span><br><span class="line"><span class="symbol">4 </span>     读 或者 其他处理</span><br><span class="line"><span class="symbol">5 </span>   &#125;</span><br><span class="line"><span class="symbol">6 </span> &#125;</span><br><span class="line"><span class="number">7</span>&#125;</span><br></pre></td></tr></table></figure><h4 id="IO多路复用解决的问题"><a href="#IO多路复用解决的问题" class="headerlink" title="IO多路复用解决的问题"></a>IO多路复用解决的问题</h4><p>最基础的网络编程伪代码如下。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span>创建socketint s = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>); <span class="comment">// 得到socket fd</span></span><br><span class="line"><span class="number">2</span>绑定bind(s, ...)</span><br><span class="line"><span class="number">3</span>监听<span class="section">listen</span>(s, ...)</span><br><span class="line"><span class="number">4</span>接受客户端连接int c = accept(s, ...)</span><br><span class="line"><span class="number">5</span>接收客户端数据recv(c, ...);</span><br><span class="line"><span class="number">6</span>将数据打印出来printf(...)</span><br></pre></td></tr></table></figure><p>先创建socket fd，依次调用bind、listen、accept，最后调用recv接收数据。recv是个阻塞方法，当程序运行到recv时，进程进入阻塞状态（不占用CPU资源），直到接收到数据，进程转到执行状态处理数据。进程阻塞在accept和recv。</p><p>但是有没有一种方式，既有阻塞等待不浪费CPU资源的优点，也能避免阻塞等待同一时刻只能处理一个流的问题，而是可以在同一时刻监听多个socket fd？即同时accept和recv多个socket fd。答案就是IO多路复用。</p><p>所以产生了select、poll、epoll等IO多路复用技术，目的是解决单线程同一时刻处理大量IO读写请求，并且不浪费CPU。</p><h4 id="什么是-IO-多路复用"><a href="#什么是-IO-多路复用" class="headerlink" title="什么是 IO 多路复用"></a>什么是 IO 多路复用</h4><p>阻塞等待只能同一时刻只能监听一个IO状态。<br>为了解决大量 IO 请求读写的问题，提出了 IO 多路复用。<br>如果同一流程想同一时刻监听多个IO状态。要么非阻塞忙轮询；要么用 select/epoll等IO多路复用器，告诉用户态有哪些 IO 可读可写，一起处理。即 IO 多路复用可以实现单线程在同一时刻可以监听多个IO状态。IO多路复用在非忙轮询状态，不浪费CPU。</p><h4 id="select-简介"><a href="#select-简介" class="headerlink" title="select 简介"></a>select 简介</h4><p>先上伪代码。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"> <span class="string">1while</span> <span class="literal">true</span> <span class="string">&#123;</span></span><br><span class="line"> <span class="number">2</span>  <span class="string">select(流[]);</span> <span class="string">//</span> <span class="string">阻塞。CPU</span> <span class="string">可以去做其他事。如果有流可读了，返回。</span></span><br><span class="line"> <span class="number">3</span></span><br><span class="line"> <span class="number">4</span>  <span class="string">//</span> <span class="string">有消息抵达</span></span><br><span class="line"> <span class="number">5</span>  <span class="string">for</span> <span class="string">i</span> <span class="string">in</span> <span class="string">流</span> <span class="string">[]</span> <span class="string">&#123;</span> <span class="string">//</span> <span class="string">需要依次判断所有的流哪个可读</span></span><br><span class="line"> <span class="number">6</span>    <span class="string">if</span> <span class="string">i</span> <span class="string">has</span> <span class="string">数据</span> <span class="string">&#123;</span> <span class="string">//</span> <span class="string">如果可读的流数量少，浪费性能</span></span><br><span class="line"> <span class="number">7</span>        <span class="string">读</span> <span class="string">或者</span> <span class="string">其他处理</span></span><br><span class="line"> <span class="number">8</span>    <span class="string">&#125;</span></span><br><span class="line"> <span class="number">9</span>  <span class="string">&#125;</span></span><br><span class="line"><span class="number">10</span><span class="string">&#125;</span></span><br></pre></td></tr></table></figure><p>进一步的代码。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"> <span class="number">1</span>int s = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>); </span><br><span class="line"> <span class="number">2</span>bind(s, ...);</span><br><span class="line"> <span class="number">3</span>listen(s, ...)</span><br><span class="line"> <span class="number">4</span>int fds[] = 存放需要监听的socket</span><br><span class="line"> <span class="number">5</span></span><br><span class="line"> <span class="number">6</span>while(<span class="number">1</span>)&#123;</span><br><span class="line"> <span class="number">7</span>  int n = select(..., fds, ...)</span><br><span class="line"> <span class="number">8</span>  for(int i=<span class="number">0</span>; i &lt; fds.count; i++)&#123;</span><br><span class="line"> <span class="number">9</span>    if(FD_ISSET(fds[i], ...))&#123;</span><br><span class="line"><span class="number">10</span>      <span class="comment">// fds[i]的数据处理</span></span><br><span class="line"><span class="number">11</span>    &#125;</span><br><span class="line"><span class="number">12</span>  &#125;</span><br><span class="line"><span class="number">13</span>&#125;</span><br></pre></td></tr></table></figure><p>select的流程是：</p><ol><li>应用进程调用系统调用select(fds)。</li><li>select阻塞直到有任意数量fd可读。</li><li>应用进程遍历fds，通过FD_ISSET判断哪些socket fd可读。</li><li>应用进程处理返回的数据。</li></ol><p>select的缺点是：</p><ol><li>应用进程每次select系统调用都需要应用空间复制整个fds列表到内核空间。</li><li>内核需要主动遍历n次，才能返回哪些fd可读可写，CPU浪费在了内核空间。</li><li>规定select的最多同时监听1024个socket fd。</li><li>应用进程被唤醒后，不知道那些socket fd可读，需要遍历所有fd。</li></ol><h4 id="epoll-简介"><a href="#epoll-简介" class="headerlink" title="epoll 简介"></a>epoll 简介</h4><p>先上伪代码。</p><figure class="highlight basic"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span><span class="keyword">while</span> true &#123;</span><br><span class="line"><span class="symbol">2 </span> 可处理的流[] = epoll_wait(epoll_fd); // 阻塞</span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="symbol">4 </span> // 有消息抵达</span><br><span class="line"><span class="symbol">5 </span> <span class="keyword">for</span> i in 可处理的流[] &#123;</span><br><span class="line"><span class="symbol">6 </span>   读 或者 其他处理</span><br><span class="line"><span class="symbol">7 </span> &#125;</span><br><span class="line"><span class="number">8</span>&#125;</span><br></pre></td></tr></table></figure><p>进一步的代码。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"> <span class="number">1</span>int s = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"> <span class="number">2</span>bind(s, ...);</span><br><span class="line"> <span class="number">3</span>listen(s, ...)</span><br><span class="line"> <span class="number">4</span>int epfd = epoll_create(...);</span><br><span class="line"> <span class="number">5</span>epoll_ctl(epfd, ...); <span class="comment">// 将所有需要监听的socket添加到epfd中</span></span><br><span class="line"> <span class="number">6</span></span><br><span class="line"> <span class="number">7</span>while(<span class="number">1</span>)&#123;</span><br><span class="line"> <span class="number">8</span>  int n = epoll_wait(...)</span><br><span class="line"> <span class="number">9</span>  for(接收到数据的socket fds)&#123;</span><br><span class="line"><span class="number">10</span>    <span class="comment">// fds[i]的数据处理</span></span><br><span class="line"><span class="number">11</span>  &#125;</span><br><span class="line"><span class="number">12</span>&#125;</span><br></pre></td></tr></table></figure><p>epoll的流程是：</p><ol><li>应用进程调用系统调用epoll_create，创建eventpoll对象，用于维护等待列表和就绪列表。</li><li>应用进程调用系统调用epoll_ctl，添加要监听的fd。</li><li>应用进程调用系统调用epoll_wait。</li><li>epoll_wait阻塞直到有任意数量fd可读。</li><li>应用进程遍历就绪列表，得到数据。</li><li>应用进程处理返回的数据。</li></ol><p>epoll的优点是：</p><ol><li>可以同时监听大量的socket fd。能够处理大量的链接请求(系统可以打开的文件数目) 。</li><li>应用进程被唤醒后，只需要遍历可读的fd。</li></ol><blockquote><p><code>cat /proc/sys/fs/file-max</code> 得到当前操作系统可以打开的最大文件描述符个数。</p></blockquote><h3 id="epoll详解"><a href="#epoll详解" class="headerlink" title="epoll详解"></a>epoll详解</h3><p>关于epoll更多细节，在《深入理解Linux IO模型(二)》讲述。</p><h3 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h3><p>UNIX环境高级编程（第3版）<a href="https://book.douban.com/subject/25900403/" rel="external nofollow noopener noreferrer" target="_blank">https://book.douban.com/subject/25900403/</a><br><a href="http://www.cse.iitm.ac.in/~chester/courses/15o_os/slides/5_Interrupts.pdf" rel="external nofollow noopener noreferrer" target="_blank">http://www.cse.iitm.ac.in/~chester/courses/15o_os/slides/5_Interrupts.pdf</a><br><a href="https://blog.packagecloud.io/eng/2016/10/11/monitoring-tuning-linux-networking-stack-receiving-data-illustrated/" rel="external nofollow noopener noreferrer" target="_blank">https://blog.packagecloud.io/eng/2016/10/11/monitoring-tuning-linux-networking-stack-receiving-data-illustrated/</a><br><a href="https://blog.packagecloud.io/eng/2016/06/22/monitoring-tuning-linux-networking-stack-receiving-data/" rel="external nofollow noopener noreferrer" target="_blank">https://blog.packagecloud.io/eng/2016/06/22/monitoring-tuning-linux-networking-stack-receiving-data/</a><br><a href="https://mp.weixin.qq.com/s/kWDKpgmcOQFjoBAK3LyPTg" rel="external nofollow noopener noreferrer" target="_blank">https://mp.weixin.qq.com/s/kWDKpgmcOQFjoBAK3LyPTg</a><br><a href="https://juejin.cn/post/6892687008552976398#heading-26" rel="external nofollow noopener noreferrer" target="_blank">https://juejin.cn/post/6892687008552976398#heading-26</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h3 id=&quot;深入理解Linux-IO模型-一&quot;&gt;&lt;a href=&quot;#深入理解Linux-IO模型-一&quot; class=&quot;headerlink&quot; title=&quot;深入理解Linux IO模型(一)&quot;&gt;&lt;/a&gt;深入理解Linux IO模型(一)&lt;/h3&gt;&lt;p&gt;原创 Pearl &lt;a </summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
    <category term="linux" scheme="http://www.shankusu.me/tags/linux/"/>
    
    <category term="epoll" scheme="http://www.shankusu.me/tags/epoll/"/>
    
    <category term="select" scheme="http://www.shankusu.me/tags/select/"/>
    
  </entry>
  
  <entry>
    <title>Redis的47连环炮，试试你能看住几个 -面前复习</title>
    <link href="http://www.shankusu.me/redis/Redis%E7%9A%8447%E8%BF%9E%E7%8E%AF%E7%82%AE/"/>
    <id>http://www.shankusu.me/redis/Redis%E7%9A%8447%E8%BF%9E%E7%8E%AF%E7%82%AE/</id>
    <published>2021-08-25T16:59:58.000Z</published>
    <updated>2021-08-25T17:19:06.395Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://www.zhihu.com/people/trojan-61" rel="external nofollow noopener noreferrer" target="_blank"><img src="https://pica.zhimg.com/v2-0e0b6e3604f41d4101fcfc182d11963b_xs.jpg?source=172ae18b" alt="java后端技术全栈"></a></p><p><a href="https://www.zhihu.com/people/trojan-61" rel="external nofollow noopener noreferrer" target="_blank">java后端技术全栈</a></p><h2 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h2><p>大家好，我是老田，前面我们已经分享了五篇连环炮文章，今天我们继续。今天我们接着来聊聊Redis。Redis已经成为我们开发者必备技能之一了，同时面试也是必问的。下面就来对Redis进行一个总结，然后赠送43连环炮。</p><p>Redis的43连环炮：</p><p>1、说说什么是Redis?</p><p>2、Redis 有什么优点和缺点？</p><p>3、Redis 的数据类型有哪些？</p><p>4、Redis 是单线程的吗？</p><p>5、Redis 为什么设计成单线程的？</p><p>6、Redis 和 Memcached 的区别有哪些？</p><p>7、请说说 Redis 的线程模型？</p><p>8、为什么 Redis 单线程模型也能效率这么高？</p><p>9、Redis 是单线程的，如何提高多核 CPU 的利用率？</p><p>10、Redis 的同步机制了解是什么？</p><p>11、什么是 Redis Pipelining？</p><p>12、Redis 有几种持久化方式？</p><p>13、说说RDB的 优缺点</p><p>14、说说AOF的 优缺点</p><p>15、两种持久化方式该如何选择？</p><p>16、面试官追问那如果突然机器掉电会怎样？</p><p>17、面试官追问 bgsave 的原理是什么？</p><p>18、Redis 有几种数据“过期”策略？</p><p>19、Redis 有哪几种数据“淘汰”策略？</p><p>20、一个字符串类型的值能存储最大容量是多少？</p><p>21、熟悉Redis的哪些客户端？</p><p>22、什么是 Redis 事务？</p><p>23、Redis 事务的注意点有哪些？</p><p>24、为什么Redis 事务不支持回滚？</p><p>25、Redis有哪些使用场景？</p><p>26、如何使用 Redis 实现分布式锁？</p><p>27、分布式锁的实现条件？</p><p>28、Redis和Zookeeper实现的分布式锁有什么区别，哪个更好的呢？</p><p>29、如何使用 Redis 实现分布式限流？</p><p>30、如何使用 Redis 实现消息队列？</p><p>31、Redis 高可用方案有哪些？</p><p>32、什么是 Redis 主从同步？</p><p>33、如何使用 Redis Sentinel 实现高可用？</p><p>34、如果使用 Redis Cluster 实现高可用？</p><p>35、说说 Redis 哈希槽的概念？</p><p>36、Redis Cluster 的主从复制模型是怎样的？</p><p>37、Redis 的哨兵有什么功能？</p><p>38、Redis 哨兵和集群的区别是什么？</p><p>39、缓存命中率表示什么？</p><p>40、假如 Redis 里面有 1 亿个 key，其中有 10w 个 key 是以某个固定的已知的前缀开头的，如果将它们全部找出来？</p><p>41、请说说你们生产环境中的 Redis 是怎么部署的？</p><p>42、你知道有哪些 Redis 分区实现方案？</p><p>43、如何提高 Redis 命中率？</p><p>44、怎么优化 Redis 的内存占用？</p><p>45、什么是缓存穿透？怎么解决？</p><p>46、什么是缓存雪崩？ 怎么解决？</p><p>47、对 Redis 进行性能优化，有些什么建议？</p><p>…..(不断完善ing)</p><h2 id="Redis核心知识总结"><a href="#Redis核心知识总结" class="headerlink" title="Redis核心知识总结"></a>Redis核心知识总结</h2><p>我给大家搞了一张Redis的核心知识总结思维导图：</p><p><img src="https://pic3.zhimg.com/80/v2-4b98247e0cad5e74a22f572bb7335046_1440w.jpg" alt="img"></p><p>需要思维导图，请私聊我，免费赠送</p><h2 id="1、说说什么是Redis"><a href="#1、说说什么是Redis" class="headerlink" title="1、说说什么是Redis?"></a>1、说说什么是Redis?</h2><blockquote><p>Redis 是一个开源（BSD许可），内存存储的数据结构服务器，可用作数据库，高速缓存和消息队列代理。它支持字符串、哈希表、列表、集合、有序集合，位图，HyperLogLogs 等数据类型。内置复制、Lua 脚本、LRU 收回、事务，以及不同级别磁盘持久化功能，同时通过 Redis Sentinel 提供高可用，通过 Redis Cluster 提供自动分区。根据月度排行网站 DB-Engines的数据，Redis 是最流行的键值对存储数据库。</p></blockquote><p>以上是官方的介绍，我们也可以做一个简单回答：</p><p>Redis 全称为：Remote Dictionary Server（远程数据服务），是一个基于内存且支持持久化的高性能 key-value 数据库。具备一下三个基本特征：</p><ul><li>多数据类型</li><li>持久化机制</li><li>主从同步</li></ul><h2 id="2、Redis-有什么优点和缺点？"><a href="#2、Redis-有什么优点和缺点？" class="headerlink" title="2、Redis 有什么优点和缺点？"></a>2、Redis 有什么优点和缺点？</h2><h2 id="Redis-优点"><a href="#Redis-优点" class="headerlink" title="Redis 优点"></a>Redis 优点</h2><ul><li>读写性能优异， Redis能读的速度是110000次/s，写的速度是81000次/s。</li><li>支持数据持久化，支持AOF和RDB两种持久化方式。</li><li>支持事务，Redis的所有操作都是原子性的，同时Redis还支持对几个操作合并后的原子性执行。</li><li>数据结构丰富，除了支持string类型的value外还支持hash、set、zset、list等数据结构。</li><li>支持主从复制，主机会自动将数据同步到从机，可以进行读写分离。</li></ul><h2 id="Redis-缺点"><a href="#Redis-缺点" class="headerlink" title="Redis 缺点"></a>Redis 缺点</h2><ul><li>数据库容量受到物理内存的限制，不能用作海量数据的高性能读写，因此Redis 适合的场景主要局限在较小数据量的高性能操作和运算上。</li><li>Redis 不具备自动容错和恢复功能，主机从机的宕机都会导致前端部分读写请求失败，需要等待机器重启或者手动切换前端的IP才能恢复。</li><li>主机宕机，宕机前有部分数据未能及时同步到从机，切换IP后还会引入数据不一致的问题，降低了系统的可用性。</li><li>Redis 较难支持在线扩容，在集群容量达到上限时在线扩容会变得很复杂。为避免这一问题，运维人员在系统上线时必须确保有足够的空间，这对资源造成了很大的浪费。</li></ul><h2 id="3、Redis-的数据类型有哪些？"><a href="#3、Redis-的数据类型有哪些？" class="headerlink" title="3、Redis 的数据类型有哪些？"></a>3、Redis 的数据类型有哪些？</h2><p>Redis 主要有以下几种数据类型：</p><ul><li>String：这是最简单的类型，就是普通的 set 和 get，做简单的 KV 缓存。</li><li>Hashe：这个是类似 map 的一种结构，这个一般就是可以将结构化的数据，比如一个对象（前提是这个对象没嵌套其他的对象）给缓存在 Redis 里，然后每次读写缓存的时候，可以就操作 hash 里的某个字段。</li><li>List：List 是有序列表，这个可以玩儿出很多花样。比如可以通过 list 存储一些列表型的数据结构，类似粉丝列表、文章的评论列表之类的东西。</li><li>Sets：是无序集合，自动去重。直接基于 set 将系统里需要去重的数据扔进去，自动就给去重了，如果你需要对一些数据进行快速的全局去重，你当然也可以基于 jvm 内存里的 HashSet 进行去重，但是如果你的某个系统部署在多台机器上呢？得基于 Redis 进行全局的 set 去重。</li><li>Sorted Set：排序的 set，去重但可以排序，写进去的时候给一个分数，自动根据分数排序。 可以用来做排行榜相关功能。</li></ul><blockquote><p>一般文章都是以Redis只有 5 种数据类型，还有 Bitmaps、HyperLogLogs、Streams 等。</p></blockquote><p>中文官网上的解释：</p><p><img src="1.jpg" alt="img"></p><h2 id="4、Redis-是单线程的吗？"><a href="#4、Redis-是单线程的吗？" class="headerlink" title="4、Redis 是单线程的吗？"></a>4、Redis 是单线程的吗？</h2><p>这里的单线程指的是 Redis 网络请求模块使用了一个线程（所以不需考虑并发安全性），即一个线程处理所有网络请求，其他模块仍用了多个线程。</p><h2 id="5、Redis-为什么设计成单线程的？"><a href="#5、Redis-为什么设计成单线程的？" class="headerlink" title="5、Redis 为什么设计成单线程的？"></a>5、Redis 为什么设计成单线程的？</h2><ol><li>绝大部分请求是纯粹的内存操作（非常快速）</li><li>采用单线程，避免了不必要的上下文切换和竞争条件</li><li>非阻塞 IO，内部采用 epoll，epoll 中的读、写、关闭、连接都转化成了事件，然后利用 epoll 的多路复用特性，避免 IO 代价。</li></ol><h2 id="6、Redis-和-Memcached-的区别有哪些？"><a href="#6、Redis-和-Memcached-的区别有哪些？" class="headerlink" title="6、Redis 和 Memcached 的区别有哪些？"></a>6、Redis 和 Memcached 的区别有哪些？</h2><p>从以下8个方面来讲：</p><ul><li>Redis 和 Memcache 都是将数据存放在内存中，都是内存数据库。不过 Memcache 还可用于缓存其他东西，例如图片、视频等等。</li><li>Memcache 仅支持key-value结构的数据类型，Redis不仅仅支持简单的key-value类型的数据，同时还提供list，set，hash等数据结构的存储。</li><li>虚拟内存– Redis 当物理内存用完时，可以将一些很久没用到的value 交换到磁盘分布式–设定 Memcache 集群，利用 magent 做一主多从; Redis 可以做一主多从。都可以一主一从</li><li>存储数据安全– Memcache 挂掉后，数据没了； Redis 可以定期保存到磁盘（持久化）</li><li>Memcache 的单个value最大 1m，Redis 的单个value最大 512m。</li><li>灾难恢复– Memcache 挂掉后，数据不可恢复; Redis 数据丢失后可以通过 aof 恢复</li><li>Redis 原生就支持集群模式， Redis3.0 版本中，官方便能支持Cluster模式了， Memcached 没有原生的集群模式，需要依赖客户端来实现，然后往集群中分片写入数据。</li><li>Memcached 网络IO模型是多线程，非阻塞IO复用的网络模型，原型上接近于 nignx。而 Redis使用单线程的IO复用模型，自己封装了一个简单的 AeEvent 事件处理框架，主要实现类epoll，kqueue 和 select，更接近于Apache早期的模式。</li></ul><h2 id="7、请说说-Redis-的线程模型？"><a href="#7、请说说-Redis-的线程模型？" class="headerlink" title="7、请说说 Redis 的线程模型？"></a>7、请说说 Redis 的线程模型？</h2><blockquote><p>这问题是因为前面回答问题的时候提到了 Redis 是基于非阻塞的IO复用模型。如果这个问题回答不上来，就相当于前面的回答是给自己挖坑，因为你答不上来，面试官对你的印象可能就要打点折扣了。</p></blockquote><p>Redis 内部使用文件事件处理器 file event handler，这个文件事件处理器是单线程的，所以Redis 才叫做单线程的模型。它采用 IO 多路复用机制同时监听多个 socket，根据 socket 上的事件来选择对应的事件处理器进行处理。</p><p>文件事件处理器的结构包含 4 个部分：</p><ul><li>多个 socket。</li><li>IO 多路复用程序。</li><li>文件事件分派器。</li><li>事件处理器（连接应答处理器、命令请求处理器、命令回复处理器）。</li></ul><p>多个 socket 可能会并发产生不同的操作，每个操作对应不同的文件事件，但是 IO 多路复用程序会监听多个 socket，会将 socket 产生的事件放入队列中排队，事件分派器每次从队列中取出一个事件，把该事件交给对应的事件处理器进行处理。</p><p>来看客户端与 Redis 的一次通信过程：</p><p><img src="2.jpg" alt="img"></p><p>下面来大致说一下这个图：</p><ol><li>客户端 Socket01 向 Redis 的 Server Socket 请求建立连接，此时 Server Socket 会产生一个AE_READABLE 事件，IO 多路复用程序监听到 server socket 产生的事件后，将该事件压入队列 中。文件事件分派器从队列中获取该事件，交给连接应答处理器。连接应答处理器会创建一个 能与客户端通信的 Socket01，并将该 Socket01 的 AE_READABLE 事件与命令请求处理器关 联。</li><li>假设此时客户端发送了一个 set key value 请求，此时 Redis 中的 Socket01 会产生AE_READABLE 事件，IO 多路复用程序将事件压入队列，此时事件分派器从队列中获取到该事 件，由于前面 Socket01 的 AE_READABLE 事件已经与命令请求处理器关联，因此事件分派器 将事件交给命令请求处理器来处理。命令请求处理器读取 Socket01 的 set key value 并在自己 内存中完成 set key value 的设置。操作完成后，它会将 Socket01 的 AE_WRITABLE 事件与令 回复处理器关联。</li><li>如果此时客户端准备好接收返回结果了，那么 Redis 中的 Socket01 会产生一个AE_WRITABLE 事件，同样压入队列中，事件分派器找到相关联的命令回复处理器，由命令回复处理器对 Socket01 输入本次操作的一个结果，比如 ok，之后解除 Socket01 的AE_WRITABLE 事件与命令回复处理器的关联。</li></ol><p>这样便完成了一次通信。 不要怕这段文字，结合图看，一遍不行两遍，实在不行可以网上查点资料结合着看，一定要搞清楚，否则前面吹的牛逼就白费了。</p><h2 id="8、为什么-Redis-单线程模型也能效率这么高？"><a href="#8、为什么-Redis-单线程模型也能效率这么高？" class="headerlink" title="8、为什么 Redis 单线程模型也能效率这么高？"></a>8、为什么 Redis 单线程模型也能效率这么高？</h2><p>可以从下面5个方面来回答：</p><ol><li>C语言实现，效率高</li><li>纯内存操作</li><li>基于非阻塞的IO复用模型机制</li><li>单线程的话就能避免多线程的频繁上下文切换问题</li><li>丰富的数据结构（全称采用hash结构，读取速度非常快，对数据存储进行了一些优化，比如亚索表，跳表等）</li></ol><h2 id="9、Redis-是单线程的，如何提高多核-CPU-的利用率？"><a href="#9、Redis-是单线程的，如何提高多核-CPU-的利用率？" class="headerlink" title="9、Redis 是单线程的，如何提高多核 CPU 的利用率？"></a>9、Redis 是单线程的，如何提高多核 CPU 的利用率？</h2><p>CPU不太可能是Redis的瓶颈，一般内存和网络才有可能是。例如使用Redis的管道（pipelining）在liunx系统上运行可以达到500K的RPS(requests per second)，因此，如果您的应用程序主要使用O(N) 或者O(log(N)) 的 命令，他们几乎不需要使用什么CPU。</p><p>然而，为了最大限度的使用CPU，可以在同一个服务器部署多个Redis的实例，并把他们当作不同的服务器来使用，在某些时候，无论如何一个服务器是不够的，所以，如果你想使用多个CPU，你可以考虑一下分片（shard）。</p><p>在Redis的客户端类库里面，比如RB（Ruby的客户端）和Predis（最常用的PHP客户端之一），能够使用一致性哈希（consistent hashing）来处理多个Redis实例。</p><h2 id="10、Redis-的同步机制了解是什么？"><a href="#10、Redis-的同步机制了解是什么？" class="headerlink" title="10、Redis 的同步机制了解是什么？"></a>10、Redis 的同步机制了解是什么？</h2><p>Redis可以使用主从同步，从从同步。第一次同步时，主节点做一次bgsave，并同时将后续修改操作记录到内存buffer，待完成后将rdb文件全量同步到复制节点，复制节点接受完成后将rdb镜像加载到内存。</p><p>加载完成后，再通知主节点将期间修改的操作记录同步到复制节点进行重放就完成了同步过程。</p><h2 id="11、什么是-Redis-Pipelining？"><a href="#11、什么是-Redis-Pipelining？" class="headerlink" title="11、什么是 Redis Pipelining？"></a>11、什么是 Redis Pipelining？</h2><p>Redis有着更为复杂的数据结构并且提供对他们的原子性操作，这是一个不同于其他数据库的进化路径。Redis的数据类型都是基于基本数据结构的同时对程序员透明，无需进行额外的抽象。</p><p>Redis运行在内存中但是可以持久化到磁盘，所以在对不同数据集进行高速读写时需要权衡内存，应为数据量不能大于硬件内存。在内存数据库方面的另一个优点是， 相比在磁盘上相同的复杂的数据结构，在内存中操作起来非常简单，这样Redis可以做很多内部复杂性很强的事情。</p><p>同时，在磁盘格式方面他们是紧凑的以追加的方式产生的，因为他们并不需要进行随机访问。</p><h2 id="12、Redis-有几种持久化方式？"><a href="#12、Redis-有几种持久化方式？" class="headerlink" title="12、Redis 有几种持久化方式？"></a>12、Redis 有几种持久化方式？</h2><p>面试的时候，如果不能完整回答出来，也不会有大问题。重点，在于有条理，对 RDB 和 AOF 有理解。</p><p>Redis 提供了两种方式，实现数据的持久化到硬盘。</p><p>1、【全量】RDB 持久化，是指在指定的时间间隔内将内存中的数据集快照写入磁盘。实际操作过程是，fork 一个子进程，先将数据集写入临时文件，写入成功后，再替换之前的文件，用二进制压缩存储。</p><p>2、【增量】AOF持久化，以日志的形式记录服务器所处理的每一个写、删除操作，查询操作不会记录，以文本的方式记录，可以打开文件看到详细的操作记录。</p><h2 id="13、说说RDB的-优缺点"><a href="#13、说说RDB的-优缺点" class="headerlink" title="13、说说RDB的 优缺点"></a>13、说说RDB的 优缺点</h2><h2 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h2><ul><li>灵活设置备份频率和周期。你可能打算每个小时归档一次最近 24 小时的数据，同时还要每天归档一次最近 30 天的数据。通过这样的备份策略，一旦系统出现灾难性故障，我们可以非常容易的进行恢复。</li><li>非常适合冷备份，对于灾难恢复而言，RDB 是非常不错的选择。因为我们可以非常轻松的将一个单独的文件压缩后再转移到其它存储介质上。推荐，可以将这种完整的数据文件发送到一些远程的安全存储上去，比如说 Amazon 的 S3 云服务上去，在国内可以是阿里云的 OSS 分布式存储上。</li><li>性能最大化。对于 Redis 的服务进程而言，在开始持久化时，它唯一需要做的只是 fork 出子进程，之后再由子进程完成这些持久化的工作，这样就可以极大的避免服务进程执行 IO 操作了。也就是说，RDB 对 Redis 对外提供的读写服务，影响非常小，可以让 Redis 保持高性能。</li><li>恢复更快。相比于 AOF 机制，RDB 的恢复速度更更快，更适合恢复数据，特别是在数据集非常大的情况。</li></ul><h2 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h2><ul><li>如果你想保证数据的高可用性，即最大限度的避免数据丢失，那么 RDB 将不是一个很好的选择。因为系统一旦在定时持久化之前出现宕机现象，此前没有来得及写入磁盘的数据都将丢失。所以，RDB 实际场景下，需要和 AOF 一起使用。</li><li>由于 RDB 是通过 fork 子进程来协助完成数据持久化工作的，因此，如果当数据集较大时，可能会导致整个服务器停止服务几百毫秒，甚至是 1 秒钟。所以，RDB 建议在业务低估，例如在半夜执行。</li></ul><h2 id="14、说说AOF的-优缺点"><a href="#14、说说AOF的-优缺点" class="headerlink" title="14、说说AOF的 优缺点"></a>14、说说AOF的 优缺点</h2><h2 id="优点-1"><a href="#优点-1" class="headerlink" title="优点"></a>优点</h2><p>1、该机制可以带来更高的数据安全性</p><p>，即数据持久性。Redis 中提供了 3 种同步策略，即每秒同步、每修改(执行一个命令)同步和不同步。</p><ul><li>事实上，每秒同步也是异步完成的，其效率也是非常高的，所差的是一旦系统出现宕机现象，那么这一秒钟之内修改的数据将会丢失。</li><li>而每修改同步，我们可以将其视为同步持久化，即每次发生的数据变化都会被立即记录到磁盘中。可以预见，这种方式在效率上是最低的。</li><li>至于不同步，无需多言，我想大家都能正确的理解它。</li></ul><p>2、由于该机制对日志文件的写入操作采用的是 append 模式，因此在写入过程中即使出现宕机现象，也不会破坏日志文件中已经存在的内容。</p><ul><li>因为以 append-only 模式写入，所以没有任何磁盘寻址的开销，写入性能非常高。</li><li>另外，如果我们本次操作只是写入了一半数据就出现了系统崩溃问题，不用担心，在 Redis 下一次启动之前，我们可以通过 redis-check-aof 工具来帮助我们解决数据一致性的问题。</li></ul><p>3、如果 AOF 日志过大，Redis 可以自动启用 rewrite 机制。即使出现后台重写操作，也不会影响客户端的读写。因为在 rewrite log 的时候，会对其中的指令进行压缩，创建出一份需要恢复数据的最小日志出来。再创建新日志文件的时候，老的日志文件还是照常写入。当新的 merge 后的日志文件 ready 的时候，再交换新老日志文件即可。</p><p>4、AOF 包含一个格式清晰、易于理解的日志文件用于记录所有的修改操作。事实上，我们也可以通过该文件完成数据的重建。</p><h2 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h2><p>1、对于相同数量的数据集而言，AOF 文件通常要大于 RDB 文件。RDB 在恢复大数据集时的速度比 AOF 的恢复速度要快。</p><p>2、根据同步策略的不同，AOF 在运行效率上往往会慢于 RDB，总之，每秒同步策略的效率是比较高的，同步禁用策略的效率和 RDB 一样高效。</p><p>3、以前 AOF 发生过 bug，就是通过 AOF 记录的日志，进行数据恢复的时候，没有恢复一模一样的数据出来。所以说，类似 AOF 这种较为复杂的基于命令日志/merge/回放的方式，比基于 RDB 每次持久化一份完整的数据快照文件的方式，更加脆弱一些，容易有 bug。不过 AOF 就是为了避免 rewrite 过程导致的 bug，因此每次 rewrite 并不是基于旧的指令日志进行 merge 的，而是基于当时内存中的数据进行指令的重新构建，这样健壮性会好很多。</p><h2 id="15、两种持久化方式该如何选择？"><a href="#15、两种持久化方式该如何选择？" class="headerlink" title="15、两种持久化方式该如何选择？"></a>15、两种持久化方式该如何选择？</h2><p>bgsave 做镜像全量持久化，AOF 做增量持久化。因为 bgsave 会耗费较长时间，不够实时，在停机的时候会导致大量丢失数据，所以需要 AOF 来配合使用。在 Redis 实例重启时，会使用 bgsave 持久化文件重新构建内存，再使用 AOF 重放近期的操作指令来实现完整恢复重启之前的状态。</p><p>一般来说， 如果想达到足以媲美PostgreSQL的数据安全性， 你应该同时使用两种持久化功能。如果你非常关心你的数据， 但仍然可以承受数分钟以内的数据丢失，那么你可以只使用RDB持久化。</p><p>有很多用户都只使用AOF持久化，但并不推荐这种方式：因为定时生成RDB快照（snapshot）非常便于进行数据库备份， 并且 RDB 恢复数据集的速度也要比AOF恢复的速度要快，除此之外， 使用RDB还可以避免之前提到的AOF程序的问题。</p><h2 id="16、面试官追问那如果突然机器掉电会怎样？"><a href="#16、面试官追问那如果突然机器掉电会怎样？" class="headerlink" title="16、面试官追问那如果突然机器掉电会怎样？"></a>16、面试官追问那如果突然机器掉电会怎样？</h2><p>取决于 AOF 日志 sync 属性的配置，如果不要求性能，在每条写指令时都 sync 一下磁盘，就不会丢失数据。但是在高性能的要求下每次都 sync 是不现实的，一般都使用定时 sync，比如 1 秒 1 次，这个时候最多就会丢失 1 秒的数据。 实际上，极端情况下，是最多丢失 2 秒的数据。因为 AOF 线程，负责每秒执行一次 fsync 操作，操作完成后，记录最后同步时间。主线程，负责对比上次同步时间，如果超过 2 秒，阻塞等待成功。</p><h2 id="17、面试官追问-bgsave-的原理是什么？"><a href="#17、面试官追问-bgsave-的原理是什么？" class="headerlink" title="17、面试官追问 bgsave 的原理是什么？"></a>17、面试官追问 bgsave 的原理是什么？</h2><p>fork 和 cow。fork 是指 Redis 通过创建子进程来进行 bgsave 操作。cow 指的是 copy on write，子进程创建后，父子进程共享数据段，父进程继续提供读写服务，写脏的页面数据会逐渐和子进程分离开来。 这里 bgsave 操作后，会产生 RDB 快照文件。</p><h2 id="18、Redis-有几种数据“过期”策略？"><a href="#18、Redis-有几种数据“过期”策略？" class="headerlink" title="18、Redis 有几种数据“过期”策略？"></a>18、Redis 有几种数据“过期”策略？</h2><p>Redis 的过期策略，就是指当 Redis 中缓存的 key 过期了，Redis 如何处理。</p><p>Redis 提供了 3 种数据过期策略：</p><ul><li>被动删除：当读/写一个已经过期的 key 时，会触发惰性删除策略，直接删除掉这个过期 key。</li><li>主动删除：由于惰性删除策略无法保证冷数据被及时删掉，所以 Redis 会定期主动淘汰一批已过期的 key。</li><li>主动删除：当前已用内存超过 maxmemory 限定时，触发主动清理策略。</li></ul><p>在 Redis 中，同时使用了上述 3 种策略，即它们非互斥的。</p><h2 id="19、Redis-有哪几种数据“淘汰”策略？"><a href="#19、Redis-有哪几种数据“淘汰”策略？" class="headerlink" title="19、Redis 有哪几种数据“淘汰”策略？"></a>19、Redis 有哪几种数据“淘汰”策略？</h2><p>Redis一共有六种数据淘汰策略：</p><p>1.noeviction:返回错误当内存限制达到，并且客户端尝试执行会让更多内存被使用的命令。</p><p>2.allkeys-lru: 尝试回收最少使用的键（LRU），使得新添加的数据有空间存放。</p><p>3.volatile-lru: 尝试回收最少使用的键（LRU），但仅限于在过期集合的键,使得新添加的数据有空间存放。</p><p>4.allkeys-random: 回收随机的键使得新添加的数据有空间存放。</p><p>5.volatile-random: 回收随机的键使得新添加的数据有空间存放，但仅限于在过期集合的键。</p><p>6.volatile-ttl: 回收在过期集合的键，并且优先回收存活时间（TTL）较短的键,使得新添加的数据有空间存放。</p><h2 id="20、一个字符串类型的值能存储最大容量是多少？"><a href="#20、一个字符串类型的值能存储最大容量是多少？" class="headerlink" title="20、一个字符串类型的值能存储最大容量是多少？"></a>20、一个字符串类型的值能存储最大容量是多少？</h2><p>512M</p><h2 id="21、熟悉Redis的哪些客户端？"><a href="#21、熟悉Redis的哪些客户端？" class="headerlink" title="21、熟悉Redis的哪些客户端？"></a>21、熟悉Redis的哪些客户端？</h2><h2 id="Jedis"><a href="#Jedis" class="headerlink" title="Jedis"></a>Jedis</h2><p>是我们最熟悉和最常用的客户端。轻量，简洁，便于集成和改造。</p><p>Jedis 多个线程使用一个连接的时候线程不安全。可以使用连接池，为每个请求创建不同的连接，基于 Apache common pool 实现。跟数据库一样，可以设置最大连接数等参数。Jedis 中有多种连接池的子类</p><p>Jedis 有 4 种工作模式：单节点、分片、哨兵、集群。</p><p>3 种请求模式：Client、Pipeline、事务。Client 模式就是客户端发送一个命令，阻塞等待服务端执行，然后读取 返回结果。Pipeline 模式是一次性发送多个命令，最后一次取回所有的返回结果，这种模式通过减少网络的往返时间和 io 读写次数，大幅度提高通信性能。第三种是事务模式。Transaction 模式已开启 Redis 的事务管理，事务模式开启后，所有的命令（除了 exec，discard，multi 和 watch）到达服务端以后不会立即执行，会进入一个等待队列。</p><h2 id="Luttece"><a href="#Luttece" class="headerlink" title="Luttece"></a>Luttece</h2><p>与 Jedis 相比，Lettuce 则完全克服了其线程不安全的缺点：Lettuce 是一个可伸缩的线程安全的 Redis 客户端，支持同步、异步和响应式模式（Reactive）。多个线程可以共享一个连接实例，而不必担心多线程并发问题。</p><p>同步调用：异步的结果使用 RedisFuture 包装，提供了大量回调的方法。</p><p>异步调用：它基于 Netty 框架构建，支持 Redis 的高级功能，如 Pipeline、发布订阅，事务、Sentinel，集群，支持连接池。Lettuce 是 Spring Boot 2.x 默认的客户端，替换了 Jedis。集成之后我们不需要单独使用它，直接调用 Spring 的 RedisTemplate 操作，连接和创建和关闭也不需要我们操心。</p><h2 id="Redisson"><a href="#Redisson" class="headerlink" title="Redisson"></a>Redisson</h2><p>是一个在 Redis 的基础上实现的 Java 驻内存数据网格（In-Memory Data Grid），提供了分布式和可扩展的 Java 数据结构。</p><h2 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h2><ol><li>基于 Netty 实现，采用非阻塞 IO，性能高</li><li>支持异步请求</li><li>支持连接池、pipeline、LUA Scripting、Redis Sentinel、Redis Cluster 不支持事务，官方建议以 LUA Scripting 代替事务</li><li>主从、哨兵、集群都支持。Spring 也可以配置和注入 RedissonClient。</li></ol><h2 id="实现分布式锁"><a href="#实现分布式锁" class="headerlink" title="实现分布式锁"></a>实现分布式锁</h2><p>在 Redisson 里面提供了更加简单的分布式锁的实现。</p><h2 id="22、什么是-Redis-事务？"><a href="#22、什么是-Redis-事务？" class="headerlink" title="22、什么是 Redis 事务？"></a>22、什么是 Redis 事务？</h2><p>可以一次性执行多条命令，本质上是一组命令的集合。一个事务中的所有命令都会序列化，然后按顺序地串行化执行，而不会被插入其他命令。</p><p>Redis的事务相关命令有：</p><p>（1）DISCARD：取消事务，放弃执行事务块中的所有命令</p><p>（2）EXEC：执行事务块中的命令</p><p>（3）MULTI：标记一个事务的开始</p><p>（4）UNWATCH：取消WATCH命令对所有 key 的监视</p><p>（5）WATCH key [key…]：监视一个（或多个）key，如果在事务之前执行这个（或者这些）key被其他命令所改动，那么事务将会被打断。</p><h2 id="23、Redis-事务的注意点有哪些？"><a href="#23、Redis-事务的注意点有哪些？" class="headerlink" title="23、Redis 事务的注意点有哪些？"></a>23、Redis 事务的注意点有哪些？</h2><ol><li>不支持回滚，如果事务中有错误的操作，无法回滚到处理前的状态，需要开发者处理。</li><li>在执行完当前事务内所有指令前，不会同时执行其他客户端的请求。</li></ol><h2 id="24、为什么Redis-事务不支持回滚？"><a href="#24、为什么Redis-事务不支持回滚？" class="headerlink" title="24、为什么Redis 事务不支持回滚？"></a>24、为什么Redis 事务不支持回滚？</h2><p>Redis 事务不支持回滚，如果遇到问题，会继续执行余下的命令。 这一点和关系型数据库不太一致。这样处理的原因有：</p><ol><li>只有语法错误，Redis才会执行失败，例如错误类型的赋值， 这就是说从程序层面完全可以捕获以及解决这些问题</li><li>支持回滚需要增加很多工作，不支持的情况下，Redis 可以保持简单、速度快的特性</li></ol><h2 id="25、Redis有哪些使用场景？"><a href="#25、Redis有哪些使用场景？" class="headerlink" title="25、Redis有哪些使用场景？"></a>25、Redis有哪些使用场景？</h2><p>常见Redis的使用场景如下：</p><h2 id="缓存数据"><a href="#缓存数据" class="headerlink" title="缓存数据"></a>缓存数据</h2><p>Redis提供了键过期功能，也提供了灵活的键淘汰策略，所以，现在Redis用在缓存的场合非常多。</p><h2 id="排行榜"><a href="#排行榜" class="headerlink" title="排行榜"></a>排行榜</h2><p>很多网站都有排行榜应用的，如京东的月度销量榜单、商品按时间的上新排行榜等。Redis提供的有序集合数据类构能实现各种复杂的排行榜应用。</p><h2 id="计数器"><a href="#计数器" class="headerlink" title="计数器"></a>计数器</h2><p>如电商网站商品的浏览量、视频网站视频的播放数等。为了保证数据实时效，每次浏览都得给+1，并发量高时如果每次都请求数据库操作无疑是种挑战和压力。</p><h2 id="分布式会话"><a href="#分布式会话" class="headerlink" title="分布式会话"></a>分布式会话</h2><p>集群模式下，在应用不多的情况下一般使用容器自带的session复制功能就能满足，当应用增多相对复杂的系统中，一般都会搭建以Redis等内存数据库为中心的session服务，session不再由容器管理，而是由session服务及内存数据库管理。</p><h2 id="分布式锁"><a href="#分布式锁" class="headerlink" title="分布式锁"></a>分布式锁</h2><p>分布式锁实现方案，常见有三种：数据库，Redis、zookeepr。Redis就是其中之一。</p><p>如全局ID、减库存、秒杀等场景，并发量不大的场景可以使用数据库的悲观锁、乐观锁来实现，但在并发量高的场合中，利用数据库锁来控制资源的并发访问是不太理想的，大大影响了数据库的性能。可以利用Redis的setnx功能来编写分布式的锁，如果设置返回1说明获取锁成功，否则获取锁失败，实际应用中要考虑的细节要更多。</p><h2 id="社交网络"><a href="#社交网络" class="headerlink" title="社交网络"></a>社交网络</h2><p>点赞、踩、关注/被关注、共同好友等是社交网站的基本功能，社交网站的访问量通常来说比较大，而且传统的关系数据库类型不适合存储这种类型的数据，Redis提供的哈希、集合等数据结构能很方便的的实现这些功能。</p><h2 id="最新列表"><a href="#最新列表" class="headerlink" title="最新列表"></a>最新列表</h2><p>Redis列表结构，LPUSH可以在列表头部插入一个内容ID作为关键字，LTRIM可用来限制列表的数量，这样列表永远为N个ID，无需查询最新的列表，直接根据ID去到对应的内容页即可。</p><h2 id="消息系统"><a href="#消息系统" class="headerlink" title="消息系统"></a>消息系统</h2><p>消息队列主要用于业务解耦、流量削峰及异步处理实时性低的业务。Redis提供了发布/订阅及阻塞队列功能，能实现一个简单的消息队列系统。但Redis不是一个专业的消息队列。建议使用其他消息队列：Kafka、RocketMQ、RabbitMQ等。</p><h2 id="26、如何使用-Redis-实现分布式锁？"><a href="#26、如何使用-Redis-实现分布式锁？" class="headerlink" title="26、如何使用 Redis 实现分布式锁？"></a>26、如何使用 Redis 实现分布式锁？</h2><p>使用redis实现分布式锁的思路：</p><p>1、setnx(String key,String value)</p><p>若返回1，说明设置成功，获取到锁；</p><p>若返回0，说明设置失败，已经有了这个key，说明其它线程持有锁，重试。</p><p>2、expire(String key, int seconds)</p><p>获取到锁（返回1）后，还需要用设置生存期，如果在多少秒内没有完成，比如发生机器故障、网络故障等，键值对过期，释放锁，实现高可用。</p><p>3、del(String key)</p><p>完成业务后需要释放锁。释放锁有2种方式：del删除key，或者expire将有效期设置为0（马上过期）。</p><p>在执行业务过程中，如果发生异常，不能继续往下执行，也应该马上释放锁。</p><p>如果你的项目中Redis是多机部署的，那么可以尝试使用Redisson实现分布式锁，这是Redis官方提供的Java组件。</p><h2 id="27、分布式锁的实现条件？"><a href="#27、分布式锁的实现条件？" class="headerlink" title="27、分布式锁的实现条件？"></a>27、分布式锁的实现条件？</h2><p>1、互斥性，和单体应用一样，要保证任意时刻，只能有一个客户端持有锁</p><p>2、可靠性，要保证系统的稳定性，不能产生死锁</p><p>3、一致性，要保证锁只能由加锁人解锁，不能产生A的加锁被B用户解锁的情况</p><h2 id="28、Redis和Zookeeper实现的分布式锁有什么区别，哪个更好的呢？"><a href="#28、Redis和Zookeeper实现的分布式锁有什么区别，哪个更好的呢？" class="headerlink" title="28、Redis和Zookeeper实现的分布式锁有什么区别，哪个更好的呢？"></a>28、Redis和Zookeeper实现的分布式锁有什么区别，哪个更好的呢？</h2><p>实现方式的不同，Redis 实现为去插入一条占位数据，而 ZK 实现为去注册一个临时节点。</p><p>遇到宕机情况时，Redis 需要等到过期时间到了后自动释放锁，而 ZK 因为是临时节点，在宕机时候已经是删除了节点去释放锁。</p><p>Redis 在没抢占到锁的情况下一般会去自旋获取锁，比较浪费性能，而 ZK 是通过注册监听器的方式获取锁，性能而言优于 Redis。</p><p>没有谁是最好的</p><p>对于性能要求很高的建议使用Redis来实现，否则，建议使用Zookeeper来实现。</p><h2 id="29、如何使用-Redis-实现分布式限流？"><a href="#29、如何使用-Redis-实现分布式限流？" class="headerlink" title="29、如何使用 Redis 实现分布式限流？"></a>29、如何使用 Redis 实现分布式限流？</h2><p>限流的目的是通过对并发访问/请求进行限速或者一个时间窗口内的的请求进行限速来保护系统，一旦达到限制速率则可以拒绝服务。</p><p>Redis限流的实现方式有3种，分别是：</p><p>1、基于Redis的setnx的操作，给指定的key设置了过期实践；</p><p>2、基于Redis的数据结构zset，将请求打造成一个zset数组；</p><p>3、基于Redis的令牌桶算法，输出速率大于输入速率，就要限流。</p><h2 id="30、如何使用-Redis-实现消息队列？"><a href="#30、如何使用-Redis-实现消息队列？" class="headerlink" title="30、如何使用 Redis 实现消息队列？"></a>30、如何使用 Redis 实现消息队列？</h2><p>Redis 的 list(列表) 数据结构常用来作为异步消息队列使用，使用rpush/lpush操作入队列，使用lpop 和 rpop来出队列。rpush 和 lpop 结合 或者lpush 和rpop 结合。</p><p>客户端是通过队列的 pop 操作来获取消息，然后进行处理。处理完了再接着获取消息，再进行处理。如此循环往复，这便是作为队列消费者的客户端的生命周期。</p><h2 id="31、Redis-高可用方案有哪些？"><a href="#31、Redis-高可用方案有哪些？" class="headerlink" title="31、Redis 高可用方案有哪些？"></a>31、Redis 高可用方案有哪些？</h2><h2 id="Redis-单副本"><a href="#Redis-单副本" class="headerlink" title="Redis 单副本"></a>Redis 单副本</h2><p>Redis 单副本，采用单个 Redis 节点部署架构，没有备用节点实时同步数据，不提供数据持久化和备份策略，适用于数据可靠性要求不高的纯缓存业务场景。</p><h2 id="Redis-多副本（主从）"><a href="#Redis-多副本（主从）" class="headerlink" title="Redis 多副本（主从）"></a>Redis 多副本（主从）</h2><p>Redis 多副本，采用主从（replication）部署结构，相较于单副本而言最大的特点就是主从实例间数据实时同步，并且提供数据持久化和备份策略。主从实例部署在不同的物理服务器上，根据公司的基础环境配置，可以实现同时对外提供服务和读写分离策略。</p><h2 id="Redis-Sentinel（哨兵）"><a href="#Redis-Sentinel（哨兵）" class="headerlink" title="Redis Sentinel（哨兵）"></a>Redis Sentinel（哨兵）</h2><p>Redis Sentinel 是社区版本推出的原生高可用解决方案，其部署架构主要包括两部分：Redis Sentinel 集群和 Redis 数据集群。</p><p>其中 Redis Sentinel 集群是由若干 Sentinel 节点组成的分布式集群，可以实现故障发现、故障自动转移、配置中心和客户端通知。Redis Sentinel 的节点数量要满足 2n+1（n&gt;=1）的奇数个。</p><h2 id="Redis-Cluster"><a href="#Redis-Cluster" class="headerlink" title="Redis Cluster"></a>Redis Cluster</h2><p>Redis Cluster 是社区版推出的 Redis 分布式集群解决方案，主要解决 Redis 分布式方面的需求，比如，当遇到单机内存，并发和流量等瓶颈的时候，Redis Cluster 能起到很好的负载均衡的目的。</p><p>Redis Cluster 集群节点最小配置 6 个节点以上（3 主 3 从），其中主节点提供读写操作，从节点作为备用节点，不提供请求，只作为故障转移使用。</p><p>Redis Cluster 采用虚拟槽分区，所有的键根据哈希函数映射到 0～16383 个整数槽内，每个节点负责维护一部分槽以及槽所印映射的键值数据。</p><h2 id="Redis-自研"><a href="#Redis-自研" class="headerlink" title="Redis 自研"></a>Redis 自研</h2><p>Redis 自研的高可用解决方案，主要体现在配置中心、故障探测和 failover 的处理机制上，通常需要根据企业业务的实际线上环境来定制化。</p><h2 id="32、什么是-Redis-主从同步？"><a href="#32、什么是-Redis-主从同步？" class="headerlink" title="32、什么是 Redis 主从同步？"></a>32、什么是 Redis 主从同步？</h2><p>Redis 的主从同步(replication)机制，允许 Slave 从 Master 那里，通过网络传输拷贝到完整的数据备份，从而达到主从机制。</p><ul><li>主数据库可以进行读写操作，当发生写操作的时候自动将数据同步到从数据库，而从数据库一般是只读的，并接收主数据库同步过来的数据。</li><li>一个主数据库可以有多个从数据库，而一个从数据库只能有一个主数据库。</li><li>第一次同步时，主节点做一次 bgsave 操作，并同时将后续修改操作记录到内存 buffer，待完成后将 RDB 文件全量同步到复制节点，复制节点接受完成后将 RDB 镜像加载到内存。加载完成后，再通知主节点将期间修改的操作记录同步到复制节点进行重放就完成了同步过程。</li></ul><p><strong>好处</strong></p><p>通过 Redis 的复制功，能可以很好的实现数据库的读写分离，提高服务器的负载能力。主数据库主要进行写操作，而从数据库负责读操作。</p><blockquote><p>实际上，我们不是非常推荐在 Redis 中，使用读写分离。主要有两个原因：<br>Redis Sentinel 只保证主节点的故障的失效转移，而例如说 Jedis 库，也只监听了主节点的变化，但是从节点故障的情况，Jedis 是不进行处理的。这就会导致，Jedis 读会访问到从节点，导致问题。当然，Redisson 库的功能比较强大，已经支持从节点的故障监听。如果到达需要读写分离的体量，一般写操作也不一定会少，可以考虑上 Redis Cluster 方案，更加可靠。</p></blockquote><h2 id="33、如何使用-Redis-Sentinel-实现高可用？"><a href="#33、如何使用-Redis-Sentinel-实现高可用？" class="headerlink" title="33、如何使用 Redis Sentinel 实现高可用？"></a>33、如何使用 Redis Sentinel 实现高可用？</h2><p>Redis 的 Sentinel 系统用于管理多个 Redis 服务器（instance）， 该系统执行以下三个任务：</p><ul><li>监控（Monitoring）： Sentinel 会不断地检查你的主服务器和从服务器是否运作正常。</li><li>提醒（Notification）： 当被监控的某个 Redis 服务器出现问题时， Sentinel 可以通过 API 向管理员或者其他应用程序发送通知。</li><li>自动故障迁移（Automatic failover）： 当一个主服务器不能正常工作时， Sentinel 会开始一次自动故障迁移操作， 它会将失效主服务器的其中一个从服务器升级为新的主服务器， 并让失效主服务器的其他从服务器改为复制新的主服务器； 当客户端试图连接失效的主服务器时， 集群也会向客户端返回新主服务器的地址， 使得集群可以使用新主服务器代替失效服务器。</li></ul><p>Redis Sentinel 是一个分布式系统， 你可以在一个架构中运行多个 Sentinel 进程（progress）， 这些进程使用流言协议（gossip protocols)来接收关于主服务器是否下线的信息， 并使用投票协议（agreement protocols）来决定是否执行自动故障迁移， 以及选择哪个从服务器作为新的主服务器。</p><p>虽然 Redis Sentinel 释出为一个单独的可执行文件 redis-sentinel，但实际上它只是一个运行在特殊模式下的 Redis 服务器， 你可以在启动一个普通 Redis 服务器时通过给定 –sentinel 选项来启动 Redis Sentinel。</p><h2 id="34、如果使用-Redis-Cluster-实现高可用？"><a href="#34、如果使用-Redis-Cluster-实现高可用？" class="headerlink" title="34、如果使用 Redis Cluster 实现高可用？"></a>34、如果使用 Redis Cluster 实现高可用？</h2><p>Redis Cluster 是社区版推出的 Redis 分布式集群解决方案，主要解决 Redis 分布式方面的需求，比如，当遇到单机内存，并发和流量等瓶颈的时候，Redis Cluster 能起到很好的负载均衡的目的。</p><p>Redis Cluster 集群节点最小配置 6 个节点以上（3 主 3 从），其中主节点提供读写操作，从节点作为备用节点，不提供请求，只作为故障转移使用。</p><p>Redis Cluster 采用虚拟槽分区，所有的键根据哈希函数映射到 0～16383 个整数槽内，每个节点负责维护一部分槽以及槽所印映射的键值数据。</p><h2 id="优点-2"><a href="#优点-2" class="headerlink" title="优点"></a>优点</h2><p>无中心架构；</p><p>数据按照 slot 存储分布在多个节点，节点间数据共享，可动态调整数据分布；</p><p>可扩展性：可线性扩展到 1000 多个节点，节点可动态添加或删除；</p><p>高可用性：部分节点不可用时，集群仍可用。通过增加 Slave 做 standby 数据副本，能够实现故障自动 failover，节点之间通过 gossip 协议交换状态信息，用投票机制完成 Slave 到 Master 的角色提升；</p><p>降低运维成本，提高系统的扩展性和可用性。</p><h2 id="缺点-2"><a href="#缺点-2" class="headerlink" title="缺点"></a>缺点</h2><p>Client 实现复杂，驱动要求实现 Smart Client，缓存 slots mapping 信息并及时更新，提高了开发难度，客户端的不成熟影响业务的稳定性。目前仅 JedisCluster 相对成熟，异常处理部分还不完善，比如常见的“max redirect exception”。</p><p>节点会因为某些原因发生阻塞（阻塞时间大于 clutser-node-timeout），被判断下线，这种 failover 是没有必要的。</p><p>数据通过异步复制，不保证数据的强一致性。</p><p>多个业务使用同一套集群时，无法根据统计区分冷热数据，资源隔离性较差，容易出现相互影响的情况。</p><p>Slave 在集群中充当“冷备”，不能缓解读压力，当然可以通过 SDK 的合理设计来提高 Slave 资源的利用率。</p><h2 id="35、说说-Redis-哈希槽的概念？"><a href="#35、说说-Redis-哈希槽的概念？" class="headerlink" title="35、说说 Redis 哈希槽的概念？"></a>35、说说 Redis 哈希槽的概念？</h2><p>Redis Cluster 没有使用一致性 hash，而是引入了哈希槽的概念。</p><p>Redis 集群有 16384 个哈希槽，每个 key 通过 CRC16 校验后对 16384 取模来决定放置哪个槽，集群的每个节点负责一部分 hash 槽。因为最大是 16384 个哈希槽，所以考虑 Redis 集群中的每个节点都能分配到一个哈希槽，所以最多支持 16384 个 Redis 节点。</p><p>为什么是 16384 呢？</p><p>主要考虑集群内的网络带宽，而 16384 刚好是 2K 字节大小。</p><h2 id="36、Redis-Cluster-的主从复制模型是怎样的？"><a href="#36、Redis-Cluster-的主从复制模型是怎样的？" class="headerlink" title="36、Redis Cluster 的主从复制模型是怎样的？"></a>36、Redis Cluster 的主从复制模型是怎样的？</h2><p>为了使在部分节点失败或者大部分节点无法通信的情况下集群仍然可用，所以集群使用了主从复制模型，每个节点都会有 N-1 个复制节点。</p><p>所以，Redis Cluster 可以说是 Redis Sentinel 带分片的加强版。也可以说：</p><ul><li>Redis Sentinel 着眼于高可用，在 master 宕机时会自动将 slave 提升为 master，继续提供服务。</li><li>Redis Cluster 着眼于扩展性，在单个 Redis 内存不足时，使用 Cluster 进行分片存储。</li></ul><h2 id="37、Redis-的哨兵有什么功能？"><a href="#37、Redis-的哨兵有什么功能？" class="headerlink" title="37、Redis 的哨兵有什么功能？"></a>37、Redis 的哨兵有什么功能？</h2><p>哨兵是 Redis 集群架构中非常重要的一个组件，主要功能如下：</p><ul><li>集群监控，负责监控 Redis Master 和 Slave 进程是否正常工作；</li><li>消息通知，如果某个 Redis 实例有故障，那么哨兵负责发送消息作为报警通知给管理员；</li><li>故障转移，如果 Master node 挂掉了，会自动转移到 Slave node 上；</li><li>配置中心，如果故障转移发生了，通知 Client 客户端新的 Master 地址。</li></ul><h2 id="38、Redis-哨兵和集群的区别是什么？"><a href="#38、Redis-哨兵和集群的区别是什么？" class="headerlink" title="38、Redis 哨兵和集群的区别是什么？"></a>38、Redis 哨兵和集群的区别是什么？</h2><p>Redis 的哨兵作用是管理多个 Redis 服务器，提供了监控、提醒以及自动的故障转移的功能。哨兵可以保证当主服务器挂了后，可以从从服务器选择一台当主服务器，把别的从服务器转移到读新的主机。Redis 哨兵的主要功能有：</p><ul><li>集群监控：对 Redis 集群的主从进程进行监控，判断是否正常工作。</li><li>消息通知：如果存在 Redis 实例有故障，那么哨兵可以发送报警消息通知管理员。</li><li>故障转移：如果主机（master）节点挂了，那么可以自动转移到从（slave）节点上。</li><li>配置中心：当存在故障时，对故障进行转移后，配置中心会通知客户端新的主机（master）地址。</li></ul><p>Redis 的集群的功能是为了解决单机 Redis 容量有限的问题，将数据按一定的规则分配到多台机器，对内存的每秒访问不受限于单台服务器，可受益于分布式集群高扩展性。</p><h2 id="39、缓存命中率表示什么？"><a href="#39、缓存命中率表示什么？" class="headerlink" title="39、缓存命中率表示什么？"></a>39、缓存命中率表示什么？</h2><p>通常来说，缓存命中率越高，缓存的收益越高，应用的性能也就越好。</p><ul><li>缓存命中： 可以同缓存中获取到需要的数据</li><li>缓存不命中：缓存中无法获取所需数据，需要再次查询数据库或者其他数据存储载体。</li></ul><blockquote><p>缓存命中率 = 缓存中获取数据次数/获取数据总次数</p></blockquote><h2 id="40、假如-Redis-里面有-1-亿个-key，其中有-10w-个-key-是以某个固定的已知的前缀开头的，如果将它们全部找出来？"><a href="#40、假如-Redis-里面有-1-亿个-key，其中有-10w-个-key-是以某个固定的已知的前缀开头的，如果将它们全部找出来？" class="headerlink" title="40、假如 Redis 里面有 1 亿个 key，其中有 10w 个 key 是以某个固定的已知的前缀开头的，如果将它们全部找出来？"></a>40、假如 Redis 里面有 1 亿个 key，其中有 10w 个 key 是以某个固定的已知的前缀开头的，如果将它们全部找出来？</h2><p>使用 keys 指令可以扫出指定模式的 key 列表。</p><ul><li>对方接着追问：如果这个 Redis 正在给线上的业务提供服务，那使用 keys 指令会有什么问题？</li><li>这个时候你要回答 Redis 关键的一个特性：Redis 的单线程的。keys 指令会导致线程阻塞一段时间，线上服务会停顿，直到指令执行完毕，服务才能恢复。这个时候可以使用 scan 指令，scan 指令可以无阻塞的提取出指定模式的 key 列表，但是会有一定的重复概率，在客户端做一次去重就可以了，但是整体所花费的时间会比直接用 keys 指令长。</li></ul><h2 id="41、请说说你们生产环境中的-Redis-是怎么部署的？"><a href="#41、请说说你们生产环境中的-Redis-是怎么部署的？" class="headerlink" title="41、请说说你们生产环境中的 Redis 是怎么部署的？"></a>41、请说说你们生产环境中的 Redis 是怎么部署的？</h2><p>请大家自信品尝，只要遇到此问题，98%的人必挂，下面是参考答案：</p><ul><li>Redis Cluster，10 台机器，5 台机器部署了 Redis 主实例，另外 5 台机器部署了 Redis 的从实例，每个主实例挂了一个从实例，5 个节点对外提供读写服务，每个节点的读写高峰 qps 可能可以达到每秒 5 万，5 台机器最多是 25 万读写请求每秒。</li><li>机器是什么配置？32G 内存 + 8 核 CPU + 1T 磁盘，但是分配给 Redis 进程的是 10G 内存，一般线上生产环境，Redis 的内存尽量不要超过 10G，超过 10G 可能会有问题。那么，5 台机器对外提供读写，一共有 50G 内存。</li><li>因为每个主实例都挂了一个从实例，所以是高可用的，任何一个主实例宕机，都会自动故障迁移，Redis 从实例会自动变成主实例继续提供读写服务。</li><li>你往内存里写的是什么数据？每条数据的大小是多少？商品数据，每条数据是 10kb。100 条数据是 1mb，10 万条数据是 1G。常驻内存的是 200 万条商品数据，占用内存是 20G，仅仅不到总内存的 50%。目前高峰期每秒就是 3500 左右的请求量。</li><li>其实大型的公司，会有基础架构的 Team 负责缓存集群的运维。</li></ul><h2 id="42、你知道有哪些-Redis-分区实现方案？"><a href="#42、你知道有哪些-Redis-分区实现方案？" class="headerlink" title="42、你知道有哪些 Redis 分区实现方案？"></a>42、你知道有哪些 Redis 分区实现方案？</h2><p>Redis 分区方案，主要分成两种类型：</p><ul><li>客户端分区，就是在客户端就已经决定数据会被存储到哪个 Redis 节点或者从哪个 Redis 节点读取。大多数客户端已经实现了客户端分区。案例：Redis Cluster 和客户端分区。</li><li>代理分区，意味着客户端将请求发送给代理，然后代理决定去哪个节点写数据或者读数据。代理根据分区规则决定请求哪些 Redis 实例，然后根据 Redis 的响应结果返回给客户端。案例：Twemproxy 和 Codis。</li></ul><p>查询路由(Query routing)的意思，是客户端随机地请求任意一个 Redis 实例，然后由 Redis 将请求转发给正确的 Redis 节点。Redis Cluster 实现了一种混合形式的查询路由，但并不是直接将请求从一个Redis 节点转发到另一个 Redis 节点，而是在客户端的帮助下直接 Redirect 到正确的 Redis 节点。</p><h2 id="43、如何提高-Redis-命中率？"><a href="#43、如何提高-Redis-命中率？" class="headerlink" title="43、如何提高 Redis 命中率？"></a>43、如何提高 Redis 命中率？</h2><p>提供缓存命中率，通常有如下方式：</p><ul><li>缓存预加载</li><li>增加缓存存储量</li><li>调整缓存存储数据类型</li><li>提升缓存更新频次</li></ul><h2 id="44、怎么优化-Redis-的内存占用？"><a href="#44、怎么优化-Redis-的内存占用？" class="headerlink" title="44、怎么优化 Redis 的内存占用？"></a>44、怎么优化 Redis 的内存占用？</h2><p>可以通过以下六种方式来对Redis的内存优化：</p><ul><li>redisObject 对象</li><li>缩减键值对象</li><li>共享对象池</li><li>字符串优化</li><li>编码优化</li><li>控制 key 的数量</li></ul><h2 id="45、什么是缓存穿透？怎么解决？"><a href="#45、什么是缓存穿透？怎么解决？" class="headerlink" title="45、什么是缓存穿透？怎么解决？"></a>45、什么是缓存穿透？怎么解决？</h2><p>大量的请求瞬时涌入系统，而这个数据在 Redis 中不存在，所有的请求都落到了数据库上把数据库打死。造成这种情况的原因有系统设计不合理、缓存数据更新不及时，或爬虫等恶意攻击。 解决办法有：</p><ol><li>使用布隆过滤器</li></ol><p>将查询的参数都存储到一个 bitmap 中，在查询缓存前，再找个新的 bitmap，在里面对参数进行验证。如果验证的 bitmap 中存在，则进行底层缓存的数据查询，如果 bitmap 中不存在查询参数，则进行拦截，不再进行缓存的数据查询。</p><ol><li>缓存空对象</li></ol><p>如果从数据库查询的结果为空，依然把这个结果进行缓存，那么当用 key 获取数据时，即使数据不存在，Redis 也可以直接返回结果，避免多次访问数据库。</p><p>但是缓存空值的缺点是：</p><ul><li>如果存在黑客恶意的随机访问，造成缓存过多的空值，那么可能造成很多内存空间的浪费。但是也可以对这些数据设置很短的过期时间来控制；</li><li>如果查询的 key 对应的 Redis 缓存空值没有过期，数据库这时有了新数据，那么会出现数据库和缓存数据不一致的问题。但是可以保证当数据库有数据后更新缓存进行解决。</li></ul><h2 id="46、什么是缓存雪崩？-怎么解决？"><a href="#46、什么是缓存雪崩？-怎么解决？" class="headerlink" title="46、什么是缓存雪崩？ 怎么解决？"></a>46、什么是缓存雪崩？ 怎么解决？</h2><p>缓存雪崩是指当大量缓存失效时，大量的请求访问直接请求数据库，导致数据库服务器无法抗住请求或挂掉的情况。这时网站常常会出现 502 错误，导致网站不可用问题。</p><p>在预防缓存雪崩时，建议遵守以下几个原则：</p><ul><li>合理规划缓存的失效时间，可以给缓存时间加一个随机数，防止统一时间过期；</li><li>合理评估数据库的负载压力，这有利于在合理范围内部分缓存失，数据库也可以正常访问；</li><li>对数据库进行过载保护或应用层限流，这种情况下一般是在网站处于大流量、高并发时，服务器整体不能承受时，可以采用的一种限流保护措施；</li><li>最后还可以考虑多级缓存设计，实现缓存的高可用。</li></ul><h2 id="47、对-Redis-进行性能优化，有些什么建议？"><a href="#47、对-Redis-进行性能优化，有些什么建议？" class="headerlink" title="47、对 Redis 进行性能优化，有些什么建议？"></a>47、对 Redis 进行性能优化，有些什么建议？</h2><p>1、Master 最好不要做任何持久化工作，如 RDB 内存快照和 AOF 日志文件。</p><p>2、Master 调用 BGREWRITEAOF 重写 AOF 文件，AOF 在重写的时候会占大量的 CPU 和内存资源，导致服务 load 过高，出现短暂服务暂停现象。</p><p>3、尽量避免在压力很大的主库上增加过多的从库。</p><p>4、主从复制不要用图状结构，用单向链表结构更为稳定，即：Master &lt;- Slave1 &lt;- Slave2 &lt;- Slave3… 。</p><p>5、Redis 主从复制的性能问题，为了主从复制的速度和连接的稳定性，Slave 和 Master 最好在同一个局域网内。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这种连环炮方式，仅仅是在大家时间很急迫的时候使用，“临时磨刀，不快也会发光”。但是，想长期发展、想更深次的学习、想变得更DIAO的话，我建议买本Redis电子书或者纸质书，看看官网，多多实战，方为王道。</p><p><strong>参考</strong></p><p><a href="https://link.zhihu.com/?target=http%3A//redis.cn/" rel="external nofollow noopener noreferrer" target="_blank">http://redis.cn/</a></p><p><a href="https://link.zhihu.com/?target=https%3A//www.jianshu.com/p/fb499fbbccbd" rel="external nofollow noopener noreferrer" target="_blank">https://www.jianshu.com/p/fb499fbbccbd</a></p><p><a href="https://link.zhihu.com/?target=https%3A//segmentfault.com/a/1190000014507534" rel="external nofollow noopener noreferrer" target="_blank">https://segmentfault.com/a/1190000014507534</a></p><p>发布于 04-14</p><p><a href="https://www.zhihu.com/topic/19557280" rel="external nofollow noopener noreferrer" target="_blank">Redis</a></p><p><a href="https://www.zhihu.com/topic/19579066" rel="external nofollow noopener noreferrer" target="_blank">程序员面试</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://www.zhihu.com/people/trojan-61&quot; rel=&quot;external nofollow noopener noreferrer&quot; target=&quot;_blank&quot;&gt;&lt;img src=&quot;https://pica.zhimg</summary>
      
    
    
    
    <category term="面试" scheme="http://www.shankusu.me/categories/面试/"/>
    
    <category term="redis" scheme="http://www.shankusu.me/categories/redis/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>redis基础数据结构skiplist</title>
    <link href="http://www.shankusu.me/redis/redis%E5%9F%BA%E7%A1%80%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84skiplist/"/>
    <id>http://www.shankusu.me/redis/redis%E5%9F%BA%E7%A1%80%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84skiplist/</id>
    <published>2021-08-25T07:18:17.000Z</published>
    <updated>2021-08-25T07:31:49.115Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://blog.csdn.net/weixin_45827856/article/details/103254965" rel="external nofollow noopener noreferrer" target="_blank">以下内容转载自csdn-Knight0xffff</a></p><blockquote><p><strong>太长不看版</strong></p><ul><li>跳跃表是有序集合的底层实现之一， 除此之外它在 Redis 中没有其他应用。</li><li>每个跳跃表节点的层高都是 1 至 64 之间的随机数</li><li>层高越高出现的概率越低，层高为i的概率为( 1 − p ) ∗ p i − 1 , ( p = 1 / 4 ) (1-p) <em> p^{i-1}, (p=1/4)(1−</em>p<em>)∗</em>p*<em>i</em>−1,(<em>p</em>=1/4)。</li><li>跳跃表中，分值可以重复， 但对象成员唯一。分值相同时，节点按照成员对象的大小进行排序。</li></ul></blockquote><p>本篇解析基于<a href="https://github.com/antirez/redis" rel="external nofollow noopener noreferrer" target="_blank">redis</a> 5.0.0版本，本篇涉及源码文件为t_zset.c, server.h。</p><h2 id="什么是跳跃表"><a href="#什么是跳跃表" class="headerlink" title="什么是跳跃表"></a>什么是跳跃表</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">跳表是一个随机化的数据结构，实质就是一种可以进行二分查找的有序链表。</span><br></pre></td></tr></table></figure><p>我们都知道在有序数组中进行查找，可以使用二分查找，将时间复杂度降为O(log n)。但是有序链表做不到，是因为有序链表获取某元素复杂度为O(n)，无法通过二分的思想去跳过一些元素的访问。</p><p>例如下图要查找元素50,就必须 5 -&gt; 6 -&gt; 10 -&gt; 30 -&gt; 49 这样去找，而不能说先看 中心元素49小于50，则开始从中心右边开始查找，跳过元素5，6，10， 30的访问。</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOS8xMS8yNC8xNmU5Yzg4MzBjNmY1MmNj?x-oss-process=image/format,png" alt="img"><br>而跳跃表则是通过在节点中提取索引的方式，实现有序链表的快速查找。本质上是一个空间(额外的步进指针)换时间的操作。例如下图：</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOS8xMS8yNC8xNmU5Yzg4ZDM3ZWNmYmU3?x-oss-process=image/format,png" alt="img"></p><p>这时查找元素50变成了 5 -&gt; 49,略过了中间元素6，10， 30。上图中通过首节点存储不同步长的指针将链表完美二分，但是实际上的跳表却类似与下面这张图的结构,大部分情况不是完美二分的：</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOS8xMS8yNC8xNmU5YzhhOGYyMGQ5ZGYx?x-oss-process=image/format,png" alt="img"></p><p>跳跃表采用了随机算法(层高越高概率越小)来决定层高，相同层之间通过指针相连。redis实现中某节点层高为i的概率为 ( 1 − p ) ∗ p i − 1 (1-p) <em> p^{i-1}(1−</em>p<em>)∗</em>p*<em>i</em>−1。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">为什么不采用最完美的二分结构？</span><br></pre></td></tr></table></figure><p>考虑一下，插入节点的情况。当中间插入一个节点，此时的二分结构会被打破，所以需要不断的进行调整。想想平衡树，红黑树复杂的再平衡操作，而此处的再平衡调整比之有过之而无不及。而使用随机算法进行层高选择的方法也可以实现O(logN)的平均复杂度，而且操作也相对简化的很多。</p><h2 id="跳跃表-redis实现-的空间复杂度"><a href="#跳跃表-redis实现-的空间复杂度" class="headerlink" title="跳跃表(redis实现)的空间复杂度"></a>跳跃表(redis实现)的空间复杂度</h2><h3 id="相关定义"><a href="#相关定义" class="headerlink" title="相关定义"></a>相关定义</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 层高最大值限制</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ZSKIPLIST_MAXLEVEL 64 <span class="comment">/* Should be enough for 2^64 elements */</span></span></span><br><span class="line"><span class="comment">// 层高是否继续增长的概率</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ZSKIPLIST_P 0.25      <span class="comment">/* Skiplist P = 1/4 */</span></span></span><br><span class="line"><span class="comment">// 跳表节点定义</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> &#123;</span></span><br><span class="line">    <span class="comment">// 存储内容</span></span><br><span class="line">    sds ele;</span><br><span class="line">    <span class="comment">// 分值,用于排序</span></span><br><span class="line">    <span class="keyword">double</span> score;</span><br><span class="line">    <span class="comment">// 后退指针</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">backward</span>;</span></span><br><span class="line">    <span class="comment">// 变长数组，记录层信息。层高越高跳过的节点越多(因为层高越高概率越低)</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistLevel</span> &#123;</span></span><br><span class="line">        <span class="comment">// 指向当前层下一个节点</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">forward</span>;</span></span><br><span class="line">        <span class="comment">// 当前节点与forward所指节点中间节点数</span></span><br><span class="line">        <span class="keyword">unsigned</span> <span class="keyword">long</span> span;</span><br><span class="line">    &#125; level[];</span><br><span class="line">&#125; zskiplistNode;</span><br><span class="line"><span class="comment">// 跳表结构管理节点</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">zskiplist</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">header</span>, *<span class="title">tail</span>;</span></span><br><span class="line">    <span class="comment">// 长度</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> length;</span><br><span class="line">    <span class="comment">// 跳表高度(所有节点最高层高)</span></span><br><span class="line">    <span class="keyword">int</span> level;</span><br><span class="line">&#125; zskiplist;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">zslRandomLevel</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 计算当前插入元素层高的随机函数</span></span><br><span class="line">    <span class="keyword">int</span> level = <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// (random()&amp;0xFFFF) &lt; (ZSKIPLIST_P * 0xFFFF) 概率为1/4</span></span><br><span class="line">    <span class="keyword">while</span> ((<span class="built_in">random</span>()&amp;<span class="number">0xFFFF</span>) &lt; (ZSKIPLIST_P * <span class="number">0xFFFF</span>))</span><br><span class="line">        level += <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> (level&lt;ZSKIPLIST_MAXLEVEL) ? level : ZSKIPLIST_MAXLEVEL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>层高为1概率为 1-p(不进while)</p><p>层高为2的概率为 p(进一次while) * (1 - p)(不进while)</p><p>层高为3的概率为 p(进一次while) <em> p(进一次while) </em> (1 - p)(不进while)</p><p>…</p><p>层高为n的概率为 ( 1 − p ) ∗ p n − 1 (1-p) <em> p^{n-1}(1−</em>p<em>)∗</em>p*<em>n</em>−1</p><p>层高的期望 E = 1 / ( 1 − p ) E = 1/(1-p)<em>E</em>=1/(1−<em>p</em>)</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">在概率论和统计学中，数学期望(mean)（或均值，亦简称期望）是试验中每次可能结果的概率乘以其结果的总和，是最基本的数学特征之一。它反映随机变量平均取值的大小</span><br></pre></td></tr></table></figure><p>在redis实现中 p=1/4, 层高期望为E约等于1.33，所以节点的平均层高约等于1.33是个常数，从而得出跳跃表的空间复杂度为O(n)。</p><h2 id="跳跃表-redis实现-相关操作"><a href="#跳跃表-redis实现-相关操作" class="headerlink" title="跳跃表(redis实现)相关操作"></a>跳跃表(redis实现)相关操作</h2><h3 id="创建跳跃表"><a href="#创建跳跃表" class="headerlink" title="创建跳跃表"></a>创建跳跃表</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">zskiplistNode *<span class="title">zslCreateNode</span><span class="params">(<span class="keyword">int</span> level, <span class="keyword">double</span> score, <span class="keyword">int</span> ele)</span> </span>&#123;</span><br><span class="line">    zskiplistNode *zn =</span><br><span class="line">        <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(*zn)+level*<span class="keyword">sizeof</span>(struct zskiplistLevel));</span><br><span class="line">    zn-&gt;score = score;</span><br><span class="line">    zn-&gt;ele = ele;</span><br><span class="line">    <span class="keyword">return</span> zn;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Create a new skiplist. */</span></span><br><span class="line"><span class="function">zskiplist *<span class="title">zslCreate</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> j;</span><br><span class="line">    zskiplist *zsl;</span><br><span class="line"></span><br><span class="line">    zsl = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(*zsl));</span><br><span class="line">    zsl-&gt;level = <span class="number">1</span>;</span><br><span class="line">    zsl-&gt;length = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 头节点层高为64(层高的最大限制)</span></span><br><span class="line">    zsl-&gt;header = zslCreateNode(ZSKIPLIST_MAXLEVEL,<span class="number">0</span>,<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; ZSKIPLIST_MAXLEVEL; j++) &#123;</span><br><span class="line">        zsl-&gt;header-&gt;level[j].forward = <span class="literal">NULL</span>;</span><br><span class="line">        zsl-&gt;header-&gt;level[j].span = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    zsl-&gt;header-&gt;backward = <span class="literal">NULL</span>;</span><br><span class="line">    zsl-&gt;tail = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">return</span> zsl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述代码中可以看到，头节点的层高数组直接为最大长度，因为每次查找都要从头部开始，而且整个跳跃表的高度是动态增加的，初始化时直接按照最大值申请高度，避免后续高度增加时为头节点重新分配内存。所以之前的跳跃表图例应该如下图所示：</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOS8xMS8yNS8xNmVhMGU1ZDJlZjY5OGMz?x-oss-process=image/format,png" alt="img"><br>因为有backward指针的存在，所以第一层可以看作是一个双向链表。</p><h3 id="插入节点"><a href="#插入节点" class="headerlink" title="插入节点"></a>插入节点</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">zslRandomLevel</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 计算当前插入元素层高的随机函数</span></span><br><span class="line">    <span class="keyword">int</span> level = <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// (random()&amp;0xFFFF) &lt; (ZSKIPLIST_P * 0xFFFF) 概率为1/4</span></span><br><span class="line">    <span class="keyword">while</span> ((<span class="built_in">random</span>()&amp;<span class="number">0xFFFF</span>) &lt; (ZSKIPLIST_P * <span class="number">0xFFFF</span>))</span><br><span class="line">        level += <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> (level&lt;ZSKIPLIST_MAXLEVEL) ? level : ZSKIPLIST_MAXLEVEL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">zskiplistNode *<span class="title">zslInsert</span><span class="params">(zskiplist *zsl, <span class="keyword">double</span> score, sds ele)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// update存放需要更新的节点</span></span><br><span class="line">    zskiplistNode *update[ZSKIPLIST_MAXLEVEL], *x;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> rank[ZSKIPLIST_MAXLEVEL];</span><br><span class="line">    <span class="keyword">int</span> i, level;</span><br><span class="line"></span><br><span class="line">    serverAssert(!isnan(score));</span><br><span class="line">    x = zsl-&gt;header;</span><br><span class="line">    <span class="comment">// 第一步，收集需要更新的节点与步长信息</span></span><br><span class="line">    <span class="keyword">for</span> (i = zsl-&gt;level<span class="number">-1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">        <span class="comment">/* store rank that is crossed to reach the insert position */</span></span><br><span class="line">        rank[i] = i == (zsl-&gt;level<span class="number">-1</span>) ? <span class="number">0</span> : rank[i+<span class="number">1</span>];</span><br><span class="line">        <span class="comment">// score可以重复，重复时使用ele大小进行排序</span></span><br><span class="line">        <span class="keyword">while</span> (x-&gt;level[i].forward &amp;&amp;</span><br><span class="line">                (x-&gt;level[i].forward-&gt;score &lt; score ||</span><br><span class="line">                    (x-&gt;level[i].forward-&gt;score == score &amp;&amp;</span><br><span class="line">                    sdscmp(x-&gt;level[i].forward-&gt;ele,ele) &lt; <span class="number">0</span>)))</span><br><span class="line">        &#123;</span><br><span class="line">            rank[i] += x-&gt;level[i].span;</span><br><span class="line">            x = x-&gt;level[i].forward;</span><br><span class="line">        &#125;</span><br><span class="line">        update[i] = x;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 第二步， 获取随机层高，补全需要更新的节点</span></span><br><span class="line">    level = zslRandomLevel();</span><br><span class="line">    <span class="keyword">if</span> (level &gt; zsl-&gt;level) &#123;</span><br><span class="line">        <span class="keyword">for</span> (i = zsl-&gt;level; i &lt; level; i++) &#123;</span><br><span class="line">            rank[i] = <span class="number">0</span>;</span><br><span class="line">            update[i] = zsl-&gt;header;</span><br><span class="line">            update[i]-&gt;level[i].span = zsl-&gt;length;</span><br><span class="line">        &#125;</span><br><span class="line">        zsl-&gt;level = level;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 第三步，创建并分层插入节点，同时更新同层前一节点步长信息</span></span><br><span class="line">    x = zslCreateNode(level,score,ele);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; level; i++) &#123;</span><br><span class="line">        x-&gt;level[i].forward = update[i]-&gt;level[i].forward;</span><br><span class="line">        update[i]-&gt;level[i].forward = x;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* update span covered by update[i] as x is inserted here */</span></span><br><span class="line">        x-&gt;level[i].span = update[i]-&gt;level[i].span - (rank[<span class="number">0</span>] - rank[i]);</span><br><span class="line">        update[i]-&gt;level[i].span = (rank[<span class="number">0</span>] - rank[i]) + <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 第四步，更新新增节点未涉及层节点的步长信息，以及跳表相关信息</span></span><br><span class="line">    <span class="comment">/* increment span for untouched levels */</span></span><br><span class="line">    <span class="keyword">for</span> (i = level; i &lt; zsl-&gt;level; i++) &#123;</span><br><span class="line">        update[i]-&gt;level[i].span++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    x-&gt;backward = (update[<span class="number">0</span>] == zsl-&gt;header) ? <span class="literal">NULL</span> : update[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">if</span> (x-&gt;level[<span class="number">0</span>].forward)</span><br><span class="line">        x-&gt;level[<span class="number">0</span>].forward-&gt;backward = x;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        zsl-&gt;tail = x;</span><br><span class="line">    zsl-&gt;length++;</span><br><span class="line">    <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>插入节点分为四步(举个栗子，边吃边看)：</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOS8xMS8yNS8xNmVhMGU1ZDJlZjY5OGMz?x-oss-process=image/format,png" alt="img"><br>假设现在我需要插入元素80，且获取到随机的层高为5(为了所有情况都覆盖到)。</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOS8xMS8yNS8xNmVhMWIwZDYzYjUyOTll?x-oss-process=image/format,png" alt="img"></p><ol><li>收集需要更新的节点与步长信息<ul><li>将插入新增节点后每层受影响节点存在update数组中，update[i]为第i + 1层会受影响节点(红框框出来的就是例子中可能会受影响的节点)。</li><li>将每层头节点与会受影响的节点中间存在节点数存在rank数组中，rank[i]为头节点与第i + 1层会受影响节点中间存在的节点数(rank为[6, 5, 3, 3])。</li></ul></li></ol><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOS8xMS8yNS8xNmVhMWJhNjAzZDUyMmZk?x-oss-process=image/format,png" alt="img"></p><ol><li>获取随机层高，补全需要更新的节点，同时可能更新跳表高度<ul><li>通过zslRandomLevel函数计算当前插入节点侧层高，层高越高出现的几率越小(我们指定了是5，实际是随机的)。</li><li>因为搜索需要更新节点是从跳跃表当前高度的那一层开始的，如果新插入的节点的层高比当前表高还高，那么高出的这几层的头节点也是需要更新信息的(第五层的头节点后继有人了，所以它也需要被更新)。</li><li>如果当前层高高于表高，则更新表高(表高从4变成5)。</li></ul></li></ol><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOS8xMS8yNS8xNmVhMWNlZWJkYmFmNjA2?x-oss-process=image/format,png" alt="img"></p><ol><li>创建并分层插入节点，同时更新同层前一节点步长信息<ul><li>创建节点，然后根据当前节点的层高，在每一层进行节点插入(和简单链表插入一样)。</li><li>更新下每层前一个节点(update[i]对应节点)与自身节点的步长信息。</li></ul></li><li>更新新增节点未涉及层节点的步长信息，以及跳表相关信息与节点自身的相关信息<ul><li>如果当前节点的层高比跳表高度低，那么高于当前节点层高的那些层中排在当前节点之后的节点步长信息都需要+1(因为在它和它的前一个节点之间插入了新元素)。</li><li>更新跳表长度与当前节点与第一层下一节点的后退指针(后退指针可以理解为只有底层链表有)。</li></ul></li></ol><h3 id="查找节点"><a href="#查找节点" class="headerlink" title="查找节点"></a>查找节点</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* Find the rank for an element by both score and key.</span></span><br><span class="line"><span class="comment"> * Returns 0 when the element cannot be found, rank otherwise.</span></span><br><span class="line"><span class="comment"> * Note that the rank is 1-based due to the span of zsl-&gt;header to the</span></span><br><span class="line"><span class="comment"> * first element. */</span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="title">zslGetRank</span><span class="params">(zskiplist *zsl, <span class="keyword">double</span> score, sds ele)</span> </span>&#123;</span><br><span class="line">    zskiplistNode *x;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> rank = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line"></span><br><span class="line">    x = zsl-&gt;header;</span><br><span class="line">    <span class="keyword">for</span> (i = zsl-&gt;level<span class="number">-1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">        <span class="keyword">while</span> (x-&gt;level[i].forward &amp;&amp;</span><br><span class="line">            (x-&gt;level[i].forward-&gt;score &lt; score ||</span><br><span class="line">                (x-&gt;level[i].forward-&gt;score == score &amp;&amp;</span><br><span class="line">                sdscmp(x-&gt;level[i].forward-&gt;ele,ele) &lt;= <span class="number">0</span>))) &#123;</span><br><span class="line">            rank += x-&gt;level[i].span;</span><br><span class="line">            x = x-&gt;level[i].forward;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* x might be equal to zsl-&gt;header, so test if obj is non-NULL */</span></span><br><span class="line">        <span class="keyword">if</span> (x-&gt;ele &amp;&amp; sdscmp(x-&gt;ele,ele) == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> rank;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Finds an element by its rank. The rank argument needs to be 1-based. */</span></span><br><span class="line"><span class="function">zskiplistNode* <span class="title">zslGetElementByRank</span><span class="params">(zskiplist *zsl, <span class="keyword">unsigned</span> <span class="keyword">long</span> rank)</span> </span>&#123;</span><br><span class="line">    zskiplistNode *x;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> traversed = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line"></span><br><span class="line">    x = zsl-&gt;header;</span><br><span class="line">    <span class="keyword">for</span> (i = zsl-&gt;level<span class="number">-1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">        <span class="keyword">while</span> (x-&gt;level[i].forward &amp;&amp; (traversed + x-&gt;level[i].span) &lt;= rank)</span><br><span class="line">        &#123;</span><br><span class="line">            traversed += x-&gt;level[i].span;</span><br><span class="line">            x = x-&gt;level[i].forward;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (traversed == rank) &#123;</span><br><span class="line">            <span class="keyword">return</span> x;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>redis实现中跳跃表和dict共同实现了zset，dict实现O(1)复杂度获取元素对应score，跳跃表用来处理区间查询的相关操作，同时因为score可以重复，所以跳跃表无需实现通过ele获取score(通过dict查)以及通过score获取ele(貌似也没有这个需求)。</p><p>一般查询需求有两个：</p><ul><li>根据rank查询节点，主要是为了通过该节点指针进行遍历获取某个区间的节点数据。</li><li>根据score与ele(score可能重复，所以需要ele)获取节点的rank,进行count之类的数值计算。</li></ul><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly91c2VyLWdvbGQtY2RuLnhpdHUuaW8vMjAxOS8xMS8yNS8xNmVhMWY2YzA0NDViZDUy?x-oss-process=image/format,png" alt="img"><br>大体的流程都是按照从左上方开始向右下方搜索的路线进行查询(如上图红线标记路径)。</p><h3 id="删除节点"><a href="#删除节点" class="headerlink" title="删除节点"></a>删除节点</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* Internal function used by zslDelete, zslDeleteByScore and zslDeleteByRank */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">zslDeleteNode</span><span class="params">(zskiplist *zsl, zskiplistNode *x, zskiplistNode **update)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; zsl-&gt;level; i++) &#123;</span><br><span class="line">        <span class="comment">// 被删除节点在第i层有节点，则update[i]为被删除节点的前一个节点</span></span><br><span class="line">        <span class="keyword">if</span> (update[i]-&gt;level[i].forward == x) &#123;</span><br><span class="line">            <span class="comment">// 步长 = 原步长 + 被删除节点步长 - 1（被删除节点）</span></span><br><span class="line">            update[i]-&gt;level[i].span += x-&gt;level[i].span - <span class="number">1</span>;</span><br><span class="line">            <span class="comment">// 指针越过被删除节点</span></span><br><span class="line">            update[i]-&gt;level[i].forward = x-&gt;level[i].forward;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 被删除节点在第i层无节点，则 步长 = 原步长 - 1(被删除节点)</span></span><br><span class="line">            update[i]-&gt;level[i].span -= <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (x-&gt;level[<span class="number">0</span>].forward) &#123;</span><br><span class="line">        <span class="comment">// 更新被删除节点下一节点的后退指针</span></span><br><span class="line">        x-&gt;level[<span class="number">0</span>].forward-&gt;backward = x-&gt;backward;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        zsl-&gt;tail = x-&gt;backward;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span>(zsl-&gt;level &gt; <span class="number">1</span> &amp;&amp; zsl-&gt;header-&gt;level[zsl-&gt;level<span class="number">-1</span>].forward == <span class="literal">NULL</span>)</span><br><span class="line">        zsl-&gt;level--;</span><br><span class="line">    zsl-&gt;length--;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">zslDelete</span><span class="params">(zskiplist *zsl, <span class="keyword">double</span> score, sds ele, zskiplistNode **node)</span> </span>&#123;</span><br><span class="line">    zskiplistNode *update[ZSKIPLIST_MAXLEVEL], *x;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line"></span><br><span class="line">    x = zsl-&gt;header;</span><br><span class="line">    <span class="keyword">for</span> (i = zsl-&gt;level<span class="number">-1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">        <span class="keyword">while</span> (x-&gt;level[i].forward &amp;&amp;</span><br><span class="line">                (x-&gt;level[i].forward-&gt;score &lt; score ||</span><br><span class="line">                    (x-&gt;level[i].forward-&gt;score == score &amp;&amp;</span><br><span class="line">                     sdscmp(x-&gt;level[i].forward-&gt;ele,ele) &lt; <span class="number">0</span>)))</span><br><span class="line">        &#123;</span><br><span class="line">            x = x-&gt;level[i].forward;</span><br><span class="line">        &#125;</span><br><span class="line">        update[i] = x;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* We may have multiple elements with the same score, what we need</span></span><br><span class="line"><span class="comment">     * is to find the element with both the right score and object. */</span></span><br><span class="line">    x = x-&gt;level[<span class="number">0</span>].forward;</span><br><span class="line">    <span class="keyword">if</span> (x &amp;&amp; score == x-&gt;score &amp;&amp; sdscmp(x-&gt;ele,ele) == <span class="number">0</span>) &#123;</span><br><span class="line">        zslDeleteNode(zsl, x, update);</span><br><span class="line">        <span class="keyword">if</span> (!node)</span><br><span class="line">            zslFreeNode(x);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            *node = x;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; <span class="comment">/* not found */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>删除节点与添加节点步骤类似，分为三步：</p><ol><li>收集需要更新的节点。</li><li>删除节点所在的层链表移除节点(和简单链表移除节点一样)，并更新前一节点的步长信息(update[i]所存节点)。</li><li>更新跳跃表高度与长度。</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://blog.csdn.net/weixin_45827856/article/details/103254965&quot; rel=&quot;external nofollow noopener noreferrer&quot; target=&quot;_blank&quot;&gt;以下内</summary>
      
    
    
    
    <category term="redis" scheme="http://www.shankusu.me/categories/redis/"/>
    
    
    <category term="redis" scheme="http://www.shankusu.me/tags/redis/"/>
    
    <category term="skiplist" scheme="http://www.shankusu.me/tags/skiplist/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础56-磁盘调度算法</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8056-%E7%A3%81%E7%9B%98%E8%B0%83%E5%BA%A6%E7%AE%97%E6%B3%95/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8056-%E7%A3%81%E7%9B%98%E8%B0%83%E5%BA%A6%E7%AE%97%E6%B3%95/</id>
    <published>2021-08-25T03:30:26.000Z</published>
    <updated>2021-08-25T03:31:16.621Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础56-磁盘调度算法"><a href="#操作系统基础56-磁盘调度算法" class="headerlink" title="操作系统基础56-磁盘调度算法"></a>操作系统基础56-磁盘调度算法</h1><p>2021-02-08 02:15·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><p>操作系统的职责之一是有效使用硬件。对于磁盘驱动器，满足这个要求具有较快的访问速度和较宽的磁盘带宽。<br>对于磁盘，访问时间包括两个主要部分：</p><ul><li>寻道时间：是磁臂移动磁头到包含目标扇区的柱面的时间；</li><li>旋转延迟：是磁盘旋转目标扇区到磁头下的额外时间；</li></ul><p>磁盘带宽是传输字节的总数除以从服务请求开始到最后传递结束时的总时间。通过管理磁盘 I/O 请求的处理次序，可以改善访问时间和带宽。<br>每当进程需要进行磁盘 I/O 操作时，它就向操作系统发出一个系统调用。这个请求需要一些信息：</p><ul><li>这个操作是输入还是输出；</li><li>传输的磁盘地址是什么；</li><li>传输的内存地址是什么；</li><li>传输的扇区数是多少；</li></ul><p>如果所需的磁盘驱动器和控制器空闲，则立即处理请求。如果磁盘驱动器或控制器忙，则任何新的服务请求都会添加磁盘驱动器的待处理请求队列。对于具有多个进程的一个多道程序系统，磁盘队列可能有多个待处理的请求。因此，当一个请求完成时，操作系统可以选择哪个待处理的请求服务。<br>操作系统如何进行选择？有多个磁盘调度算法可以使用。</p><h1 id="FCFS-调度"><a href="#FCFS-调度" class="headerlink" title="FCFS 调度"></a>FCFS 调度</h1><p>磁盘调度的最简单形式当然是<strong>先来先服务</strong>(FCFS)算法。虽然这种算法比较公平，但是它通常并不提供最快的服务。<br>例如，考虑一个磁盘队列，其 I/O 请求块的柱面的顺序如下：</p><blockquote><p>98,183,37,122,14,124,65,67</p></blockquote><p>如果磁头开始位于柱面 53，那么它首先从 53 移到 98，接着再到 183、37、122、14、124、65，最后到 67，磁头移动柱面的总数为 640。这种调度如图1所示。</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/a030e16f6c224c24ba9b3cf2c0e34bc1?from=pc" alt="操作系统基础56-磁盘调度算法"></p><p>图1 FCFS 磁盘调度</p><p>从 122 到 14 再到 124 的大摆动说明了这种调度的问题。如果对柱面 37 和 14 的请求一起处理，不管是在 122 和 124 之前或之后，总的磁头移动会大大减少，并且性能也会因此得以改善。</p><h1 id="SSTF调度"><a href="#SSTF调度" class="headerlink" title="SSTF调度"></a>SSTF调度</h1><p>在移动磁头到别处以便处理其他请求之前，处理靠近当前磁头位置的所有请求可能较为合理。这个假设是<strong>最短寻道时间优先</strong>(SSTF)算法的基础。<br>SSTF 算法选择处理距离当前磁头位置的最短寻道时间的请求。换句话说，SSTF 选择最接近磁头位置的待处理请求。<br>对于上面请求队列的示例，与开始磁头位置(53)的最近请求位于柱面65。一旦位于柱面 65，下个最近请求位于柱面 67。从那里，由于柱面 37 比 98 还要近，所以下次处理37。如此，会处理位于柱面 14 的请求，接着 98，122，124，最后183(图2)。</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/4838593fcc2548588fb23564e43b1d79?from=pc" alt="操作系统基础56-磁盘调度算法"></p><p>图2 SSTF磁盘调度</p><p>这种调度算法的磁头移动只有236个柱面，约为FCFS调度算法的磁头移动总数的三分之一多一点。显然，这种算法大大提高了性能。<br>SSTF调度本质上是一种最短作业优先（SJF）调度；与 SJF 调度一样，它可能会导致一些请求的饥饿。请记住，请求可能随时到达。假设在队列中有两个请求，分别针对柱面 14 和 186，而当处理来自 14 的请求时，另一个靠近 14 的请求来了，这个新的请求会下次处理，这样位于 186 的请求需要等待。当处理该请求时，另一个 14 附近的请求可能到达。<br>理论上，相互接近的一些请求会连续不断地到达，这样位于 186 上的请求可能永远得不到服务。当等待处理请求队列较长时，这种情况就很可能出现了。<br>虽然 SSTF算法比FCFS算法有了相当改进，但是并非最优的。对于这个例子，还可以做得更好：移动磁头从 53 到 37（虽然 37 并不是最近的），再到 14，再到 65、67、98、122、124、183。这种策略的磁头移动的柱面总数为 208。</p><h1 id="SCAN-调度"><a href="#SCAN-调度" class="headerlink" title="SCAN 调度"></a>SCAN 调度</h1><p>对于<strong>扫描算法</strong>，磁臂从磁盘的一端开始，向另一端移动；在移过每个柱面时，处理请求。当到达磁盘的另一端时，磁头移动方向反转，并继续处理。磁头连续来回扫描磁盘。SCAN算法有时称为电梯算法，因为磁头的行为就像大楼里面的电梯，先处理所有向上的请求，然后再处理相反方向的请求。<br>下面回到前面的例子来说明。在采用SCAN来调度柱面98、183、37、122、14、124、65和67 的请求之前，除了磁头的当前位置，还需知道磁头的移动方向。</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/ee7f22eb5bd84a64ac6f6ea049edb0d5?from=pc" alt="操作系统基础56-磁盘调度算法"></p><p>图3 SCAN磁盘调度</p><p>假设磁头朝0移动并且磁头初始位置还是53，磁头接下来处理37，然后14。在柱面0时，磁头会反转，移向磁盘的另一端，并处理柱面 65、67、98、122、124、183(图3)上的请求。如果请求刚好在磁头前方加入队列，则它几乎马上就会得到服务；如果请求刚好在磁头后方加入队列，则它必须等待，直到磁头移到磁盘的另一端，反转方向，并返回。<br>假设请求柱面的分布是均匀的，考虑当磁头移到磁盘一端并且反转方向时的请求密度。这时，紧靠磁头前方的请求相对较少，因为最近处理过这些柱面。磁盘另一端的请求密度却是最多。这些请求的等待时间也最长，那么为什么不先去那里？这就是下一个算法的想法。</p><h1 id="C-SCAN-调度"><a href="#C-SCAN-调度" class="headerlink" title="C-SCAN 调度"></a>C-SCAN 调度</h1><p>循环扫描(C-SCAN)调度是SCAN的一个变种，以提供更均匀的等待时间。像SCAN一样，C-SCAN 移动磁头从磁盘一端到磁盘另一端，并且处理行程上的请求。然而，当磁头到达另一端时，它立即返回到磁盘的开头，而并不处理任何回程上的请求(图4)。</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/93479c7f5a5f46fca9f183b022fe888b?from=pc" alt="操作系统基础56-磁盘调度算法"></p><p>图4 C-SCAN磁盘调度</p><p>C-SCAN调度算法基本上将这些柱面作为一个环链，将最后柱面连到首个柱面</p><h1 id="LOOK-调度"><a href="#LOOK-调度" class="headerlink" title="LOOK 调度"></a>LOOK 调度</h1><p>正如以上所述，SCAN和C-SCAN在磁盘的整个宽度内移动磁臂。实际上，这两种算法通常都不是按这种方式实施的。更常见的是，磁臂只需移到一个方向的最远请求为止。<br>遵循这种模式的SCAN算法和C-SCAN算法分别称为LOOK 和C-LOOK调度，因为它们在向特定方向移动时查看是否会有请求(图5)。</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/dfbad940ad1b4b4b936e52c1005e76fb?from=pc" alt="操作系统基础56-磁盘调度算法"></p><p>图5 C-LOOK 磁盘调度</p><h1 id="磁盘调度算法的选择"><a href="#磁盘调度算法的选择" class="headerlink" title="磁盘调度算法的选择"></a>磁盘调度算法的选择</h1><p>给出了如此多的磁盘调度算法，如何选择最佳的呢？<br>SSTF是常见的，并且具有自然的吸引力，因为它比FCFS具有更好的性能。对于磁盘负荷较大的系统，SCAN和C-SCAN表现更好，因为它们不太可能造成饥饿问题。对于任何特定的请求列表，可以定义最佳的执行顺序，但是计算最佳调度的所需时间可能得不到补偿。然而，对于任何调度算法，性能在很大程度上取决于请求的数量和类型。<br>例如，假设队列通常只有一个待处理请求。这样，所有调度算法都一样，因为如何移动磁头只有一个选择：它们都与FCFS调度一样。<br>文件分配方式可以大大地影响磁盘服务的请求。程序读取连续分配文件时，生成多个相近位置的磁盘请求，导致有限的磁头移动。相比之下，链接或索引的文件可能包括分散在磁盘上的多个块，导致更多的磁头移动。<br>目录和索引块的位置也很重要，因为每个文件必须打开才能使用，并且打开文件需要搜索目录结构，所以目录会被经常访问。假设目录条目位于第一个柱面，而文件数据位于最后柱面，在这种情况下，磁头必须移过整个磁盘的宽度。如果目录条目位于中间柱面，则磁头只需移过不到一半的磁盘。目录和索引块的内存缓存也有助于降低磁臂移动，尤其对于读请求。<br>由于这些复杂因素，磁盘调度算法应该作为操作系统的一个单独模块，这样如果需要，可以用不同的算法来替换。SSTF或 LOOK是默认算法的合理选择。<br>这里描述的调度算法只考虑了寻道距离，对于现代磁盘，旋转延迟几乎与平均寻道时间一样大。不过，操作系统很难通过调度来改善旋转等待延迟，因为现代磁盘没有透露逻辑块的物理位置。通过磁盘驱动器的控制器硬件内的磁盘调度算法，磁盘制造商一直在缓解这点。如果操作系统向控制器发送一批请求，那么控制器可以对这些请求进行排队和调度，以改善寻道时间和旋转延迟。<br>如果I/O性能是唯一的考虑，则操作系统会乐意将磁盘调度责任转交到磁盘硬件。然而实际上，操作系统对请求服务的顺序还有其他限制。<br>例如，按需调页比应用程序I/O具有更高的优先级，并且当缓存将要用尽可用页面时，写比读更重要。此外，可能需要保证一组磁盘写入的顺序，使得文件系统在系统崩溃时更加稳健。假设操作系统分配了一个磁盘页面给一个文件，应用程序将数据写入这个页面，但操作系统还没有机会来刷新文件系统的元数据到磁盘，想想可能发生什么。<br>为了适应这种要求，操作系统可能会选择自己的磁盘调度，将请求按批次（或一个一个地，对于有的 I/O类型）交到磁盘控制器。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础56-磁盘调度算法&quot;&gt;&lt;a href=&quot;#操作系统基础56-磁盘调度算法&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础56-磁盘调度算法&quot;&gt;&lt;/a&gt;操作系统基础56-磁盘调度算法&lt;/h1&gt;&lt;p&gt;2021-02-08 02:15·&lt;</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础55-磁盘冗余阵列RAID</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8055-%E7%A3%81%E7%9B%98%E5%86%97%E4%BD%99%E9%98%B5%E5%88%97RAID/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8055-%E7%A3%81%E7%9B%98%E5%86%97%E4%BD%99%E9%98%B5%E5%88%97RAID/</id>
    <published>2021-08-25T03:29:04.000Z</published>
    <updated>2021-08-25T03:30:00.372Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础55-磁盘冗余阵列RAID"><a href="#操作系统基础55-磁盘冗余阵列RAID" class="headerlink" title="操作系统基础55-磁盘冗余阵列RAID"></a>操作系统基础55-磁盘冗余阵列RAID</h1><p>2021-02-07 01:17·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><p>磁盘驱动器继续变得更小更便宜，如今在一台计算机系统上连接许多磁盘从经济上来说已经可行了。一个系统拥有大量磁盘，就有机会改善数据的读写速率，因为磁盘操作可以并行进行。此外，这种设置提供能力，以提高数据存储的可靠性，因为冗佘信息可以存储在多 个磁盘上。因此，单个磁盘的故障不会导致数据丢失。<br>多种磁盘组织技术统称为磁盘冗余阵列(RAID)技术，通常用于处理性能与可靠性问题。过去，RAID是由小且便宜的磁盘组成，可作为大且昂贵的磁盘的有效替代品。现在，RAID的使用主要是因为高可靠性和高数据传输率，而不是经济原因。因此，RAID中的 I 表示“独立”(independent)而不是“廉价”(inexpensive)。</p><h1 id="通过冗余提高可靠性"><a href="#通过冗余提高可靠性" class="headerlink" title="通过冗余提高可靠性"></a>通过冗余提高可靠性</h1><p>首先分析 RAID 的可靠性。N 个磁盘内的某个磁盘故障的机会远远高于单个特定磁盘故障的机会。假设单个磁盘的平均故障时间为 100000小时，那么100个磁盘中的某个磁盘的平均故障时间为100 000/100 = 1000小时或 41.66天，这并不长！如果只存储数据的一个副本，则每个磁盘故障会导致丢失大量数据，这样高的数据丢失率是不可接受的。<br>可靠性问题的解决是引入冗余。存储额外信息，这是平常不需要的，但是在磁盘故障时可以用于重建丢失的信息。因此，即使磁盘故障，数据也不会丢失。<br>最为简单(但最昂贵)的引入冗余的方法是，重复每个磁盘。这种技术称为镜像。由于镜像，每个逻辑磁盘由两个物理磁盘组成，并且每次写入都在两个磁盘上进行。这称为镜像卷。如果卷中的某个磁盘故障，则可以从另一卷中读取。只有在第一个损坏磁盘没有替换之前第二个磁盘又出错，才会丢失数据。<br>镜像卷的平均故障时间（这里的故障是数据丢失）取决于两个因素。一个是，单个磁盘的平均故障时间。另一个是平均维修时间，这是用于替换损坏磁盘并恢复其上数据的平均时间。假设两个磁盘的故障是独立的；即一个磁盘故障与另一个磁盘故障没有关联。那么，如果单个磁盘的平均故障时间为 100 000 小时，并且平均修补时间为10小时，则镜像磁盘系统的平均数据丢失时间为 100000^2/(2X10)= 500X10^6 小时或 57000 年。<br>需要注意，磁盘故障的独立性假设并不真正成立。电源故障和自然灾害，如地震、火灾、水灾，可能导致同时损坏两个磁盘。另外，成批生产磁盘的制造缺陷可以导致相关故障。随着磁盘老化，故障概率增加，从而增加了在替代第一磁盘时第二个磁盘故障的概率。然而，尽管所有这些考虑，镜像磁盘系统仍比单个磁盘系统提供更高的可靠性。<br>电源故障是一个特别的关注点，因为它们比自然灾害更为常见。即使使用磁盘镜像，如果对两个磁盘写入同样的块，而在两块完全写入之前电源故障，则这两块可能处于不一致的状态。<br>这个问题的一种解决方法是，先写一个副本，再写下一个。另一个是，为RAID阵列添加固态非易失性RAM的缓存。这种写回高速缓存在电源故障时会得到保护。这样，假设NVRAM 有某种错误避免和纠错功能，如ECC和镜像，写入可以认为在这一阶段已完成。</p><h1 id="通过并行处理提高性能"><a href="#通过并行处理提高性能" class="headerlink" title="通过并行处理提高性能"></a>通过并行处理提高性能</h1><p>现在分析多个磁盘的并行访问如何改善性能。通过磁盘镜像，读请求的处理速度可以加倍，因为读请求可以送到任一磁盘（只要成对的两个磁盘都能工作，情况几乎总是这样的）。例如，每次读取的传输速率是与单个磁盘系统相同，但是每单位时间的读取次数翻了一番。<br>采用多个磁盘，通过将数据分散在多个磁盘上，也可以改善传输率。最简单形式是，数据分条包括将每个字节分散在多个磁盘上，这种分条称为位级分条。<br>例如，如果有8个磁盘，则可以将每个字节的位i写到磁盘 i 上。这8个磁盘可作为单个磁盘使用，其扇区为正常扇区的8倍，更为重要的是它具有8倍的访问率。每个磁盘参与每个访问（读或写）；这样每秒所能处理的访问数量与单个磁盘的一样，但是每次访问的数据在同样时间内为单个磁盘系统的8倍。<br>位级分条可以推广到其他磁盘数量，它或者是8的倍数或者除以8。例如，如果采用4个磁盘阵列，则每个字节的位<strong>i</strong>和位4+i可存在磁盘<strong>i</strong>上。此外，分条不必按位级来进行。<br>例如，对于块级分条，文件的块可以分散在多个磁盘上；对于 n 个磁盘，文件的块<strong>i</strong>可存在磁盘 (i mod n)+ 1上。其他分条级别，如单个扇区或单块扇区的字节，也是可能的。块级分条是最常见的。<br>磁盘系统的并行化，通过分条实现，有两个主要目标：</p><ol><li>通过负载平衡，增加了多个小访问（即页面访问）的吞吐量。</li><li>降低大访问的响应时间。</li></ol><h1 id="RAID-级别"><a href="#RAID-级别" class="headerlink" title="RAID 级别"></a>RAID 级别</h1><p>镜像提供高可靠性，但是昂贵。分条提供高数据传输率，但并未改善可靠性。通过磁盘分条和“奇偶”位（下面将要讨论），在低代价下提供冗余可以有多种方案。这些方案有不同的性价折中，并分成不同的级别，称为RAID级别。这里讨论各种级别，下图显示了它们（图中，P表示纠错位，而C表示数据的第二副本）</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/6f451b9963684f7f8175700daf44f46f?from=pc" alt="操作系统基础55-磁盘冗余阵列RAID"></p><p>RAID的级别</p><p>对于图中所示的各种情况，4个磁盘用于存储数据，额外磁盘用于存储冗余信息以便故障恢复。</p><h1 id="RAID-级别-0"><a href="#RAID-级别-0" class="headerlink" title="RAID 级别 0"></a>RAID 级别 0</h1><p>RAID级别0为具有块分条但没有冗余（如镜像或奇偶位）的磁盘阵列，上图a) 所示。</p><h1 id="RAID-级别-1"><a href="#RAID-级别-1" class="headerlink" title="RAID 级别 1"></a>RAID 级别 1</h1><p>RAID级别1指磁盘镜像。 上图b) 显示了这种镜像组织。</p><h1 id="RAID-级别-2"><a href="#RAID-级别-2" class="headerlink" title="RAID 级别 2"></a>RAID 级别 2</h1><p>RAID 级别 2 也称为内存方式的差错纠正（ECC）组织。内存系统长期以来实现了基于奇偶位的错误检测。内存系统内的每个字节都有一个关联的奇偶位，以记录字节中为 1 的个数是偶数（parity = 0）或是奇数（parity = 1）。<br>如果字节的 1 个位发生了损坏（或是 1 变成 0 或是 0 变成 1），则字节的奇偶校验位改变，因此与所存储的奇偶校验位就不匹配。类似地，如果存储的奇偶校验位损坏了，则它不匹配计算的奇偶校验位。因此，单个位差错可为内存系统所检测。差错纠正方案存储两个或多个额外位，并且当单个位出错时可以重建数据。<br>ECC方法通过将字节分散在磁盘上，可以直接用于磁盘阵列。例如，每个字节的第 1 位可以存在磁盘 1 上，第 2 位在磁盘 2 上，等等，直到第 8 位存在磁盘 8 上；而纠错位存在其他磁盘上。这个方案如上图c) 所示，其中标记为 P 的磁盘存储了差错纠正位。<br>如果有一个磁盘故障了，则可从其他磁盘中读取字节的其余位和相关差错纠正位，以重构损坏的数据。请注意，对于4个磁盘的数据，RAID级别 2只用了3个额外磁盘，而RAID级别1则需要4个额外磁盘。</p><h1 id="RAID-级别-3"><a href="#RAID-级别-3" class="headerlink" title="RAID 级别 3"></a>RAID 级别 3</h1><p>RAID级别3或位交错奇偶校验结构，改进了级别 2。它考虑了如下事实：与内存系统不同，磁盘控制器能够检测到一个扇区是否正确读取，这样单个奇偶位可以用于差错检测和差错纠正。<br>方案如下：如果一个扇区损坏了，则我们就知道它是哪个扇区；而且通过计算其他磁盘扇区的相应位的奇偶值，可以得出损坏的位是 1 还是 0。如果剩余位的奇偶校验等于存储奇偶值，则丢失位为 0；否则，为 1。<br>RAID 级别 3 与级别 2 一样好，但是在额外磁盘的数量方面要更便宜（它只有一个额外磁盘），因此级别 2 在实际中并不使用。级别3如上图d) 所示。<br>与 RAID级别1相比，RAID级别3有两个优点：</p><ul><li>第一，多个普通磁盘只需一个奇偶磁盘，而级别 1 的每个磁盘都需一个镜像磁盘，因此级别3 降低了额外存储。</li><li>第二，由于采用 N 路分散数据，字节的读写分布在多个磁盘上，所以单块读写的传输速度是 RAID 级别 1 的 N 倍。从负面来说，由于每个磁盘都要参与每次 I/O 请求，RAID级别3的每秒 I/O 次数将更少。</li></ul><p>RAID级别3的（其他奇偶检验的RAID级别也有的）另一性能问题是，需要计算和写入奇偶校验位。与非奇偶检验位RAID阵列相比，这种开销导致写入更慢。<br>为了减轻这种性能损失，许多RAID存储阵列的控制器带有专门计算奇偶校验位的硬件。这种控制器将奇偶校验位计算从CPU转移到阵列。这种阵列还有NVRAM 缓存，以便在计算奇偶校验位时存储块，并且缓存从控制器到磁盘的写入。这种组合使得奇偶校验RAID几乎与非奇偶校验的一样快。事实上，做奇偶校验的带有缓存的RAID可以胜过非缓存非奇偶校验的RAID。</p><h1 id="RAID-级别-4"><a href="#RAID-级别-4" class="headerlink" title="RAID 级别 4"></a>RAID 级别 4</h1><p>RAID级别4或块交错奇偶校验结构采用块级分条，这与RAID 0一样，此外在一个单独的磁盘上保存其他 N 个磁盘的块的奇偶校验块。这种方案如上图 e) 所示。如果有一个磁盘故障，则通过奇偶校验块和其他磁盘的相应块恢复故障磁盘的块。<br>每个块的读只访问一个磁盘，可以允许其他磁盘处理其他请求。因此，虽然每个访问的数据传输速率更慢，但是多个读访问可以并行处理，导致了更高的总体 I/O 速率。大的读取传输速率很高，因为可以并行读取所有磁盘。大的写入也有很高传输速率，因为可以并行写入数据和奇偶校验。<br>小的独立写入不能平行执行。操作系统写入的数据小于一块要求，读取块，修改新数据，并写回。奇偶校验块也必须更新。这称为读-改-写周期。因此，单个写需要4次磁盘访问：两次读入两个旧块，两次写入两个新块。</p><h1 id="RAID-级别-5"><a href="#RAID-级别-5" class="headerlink" title="RAID 级别 5"></a>RAID 级别 5</h1><p>RAID级别5或块交错分布奇偶校验结构，不同于级别4，它将数据和奇偶校验分散在所有N+1个磁盘上，而不是将数据存在N个磁盘上并且奇偶校验存在单个磁盘上。<br>对于每块，一个磁盘存储奇偶校验，而其他的存储数据。例如，对于5个磁盘的阵列，第n块的奇偶校验保存在磁盘 (n mod 5) + 1 上；其他4个磁盘的第 n 块保存该奇偶块对应的真正数据。这种方案如上图f) 所示，其中P分布在所有磁盘上。奇偶校验块不能保存同一磁盘的块的奇偶校验，因为磁盘故障会导致数据及奇偶校验的丢失，因此无法恢复损失。<br>通过将奇偶校验分布在所有磁盘上，RAID 5避免了RAID 4方案的对单个奇偶校验磁盘的潜在过度使用。RAID 5是最常见的奇偶校验RAID系统。</p><h1 id="RAID-级别-6"><a href="#RAID-级别-6" class="headerlink" title="RAID 级别 6"></a>RAID 级别 6</h1><p>RAID 级别 6，也称为 P+Q 冗余方案，与RAID级别 5非常类似，但是保存了额外冗余信息以防范多个磁盘故障。除了使用奇偶校验，可以使用差错纠正码，如 Read-Solomon 码。<br>在图 1g) 所示的方案中，每 4 位的数据使用了 2 位的冗余数据，而不是像级别 5 那样的一个奇偶位，这个系统可以容忍两个磁盘故障。</p><h1 id="RAID-级别-0-1-和-1-0"><a href="#RAID-级别-0-1-和-1-0" class="headerlink" title="RAID 级别 0+1 和 1+0"></a>RAID 级别 0+1 和 1+0</h1><p>RAID 级别 0+1 为 RAID 级别 0 和级别 1 的组合。RAID 0 提供了性能，而 RAID 1 提供了可靠性。通常，它比 RAID 5 有更好的性能。它适用于高性能和高可靠性的环境。不过，与 RAID 1 一样，存储所需的磁盘数量也加倍了，所以也相对昂贵。对于 RAID 0+1，一组磁盘分成条，每一条镜像到另一条。<br>另一正在商业化的RAID选项是 RAID 1+0，即磁盘先镜像，再分条。这种 RAID 比 RAID 0+1 有一些理论上的优点。例如，如果 RAID 0+1 中的一个磁盘故障，那么整个条就不能访问，虽然所有其他条可用。对于RAID 1+0，如果单个磁盘不可用，但其镜像仍然如所有其他磁盘一样可用（下图 ）。</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/3870801c779b4d4083d3ae53d0e26ba7?from=pc" alt="操作系统基础55-磁盘冗余阵列RAID"></p><p>RAID 0+1 和 RAID 1+0</p><p>这里描述的基本RAID方案有很多变种。因此，对于不同RAID级别的精确定义，可能存在一定的混清。<br>RAID实现是另一可变之处。考虑RAID实现的如下层次：</p><ol><li>卷管理软件可以在内核或系统软件层中实现RAID。在这种情况下，存储硬件可以提供最少的功能，但仍是完整RAID解决方案的一部分。奇偶校验 RAID 的软件实现相当慢，因此通常采用 RAID 0、RAID 1 或 RAID 0+1。</li><li>RAID 实现可以采用主机总线适配器（HBA）硬件。只有直接连到 HBA 的磁盘才能成为给定 RAID 集的一部分。这个解决方案的成本很低，但不是很灵活。</li><li>RAID 实现可以采用存储阵列硬件。存储阵列可以创建各种级别的 RAID 集，甚至可以将这些集合分成更小的卷，再提供给操作系统。操作系统只需要在每个卷上实现文件系统。阵列可有多个连接可用，或可以是 SAN 的一部分，允许多个主机利用阵列功能。</li><li>RAID 实现可以采用磁盘虚拟化设备的 SAN 互连层。在这种情况下，设备位于主机和存储之间。它接受来自服务器的命令，并管理访问存储。例如，通过将每块写到两个单独的存储设备来提供镜像。</li></ol><p>其他特征，如快照和复制，在每个级别中都可以实现。快照是在最后一次更新之前文件系统的视图。复制涉及不同站点之间的自动复制写入，以提供冗余和失败恢复。<br>复制可以是同步或异步的。对于同步复制，在写入完成之前，必须在本地和远程的站点中写入每块；而对于异步复制，写入是定期地按组来进行的。如果主站点故障，则异步复制可能导致数据丢失，但是它更快且没有距离限制。<br>这些特征的实现，因RAID实现级别的不同而不同。例如，如果RAID实现采用软件，则每个主机可能需要实现和管理其自己的复制。然而，如果RAID实现采用存储阵列或SAN互连，则无论主机操作系统或其功能如何，都可以复制主机的数据。<br>大多数RAID实现的另一方面是热备份磁盘。热备份（hot spare）不是用于存储数据，但是配置成在磁盘故障时用作替换。例如，如果每对磁盘之一故障，则热备份可以用于重构镜像磁盘对。这样，就可以自动重建RAID级别，而无需等待替换故障磁盘。分配多个热备份允许多个磁盘故障而无需人工干预。</p><h1 id="RAID-级别的选择"><a href="#RAID-级别的选择" class="headerlink" title="RAID 级别的选择"></a>RAID 级别的选择</h1><p>RAID 级别有很多，系统设计人员如何选择 RAID 级别？<br>一个考虑是重构性能。如果磁盘故障，则重建数据的所需时间可能很大。如果要求持续提供数据，如高性能或交互式数据库系统，那么这可能是个重要因素。此外，重建性能影响平均故障时间。<br>重建性能随着使用 RAID 级别而异。RAID 级别 1 的重建最简单，因为可以从另一个磁盘来复制数据。对于其他级别，需要访问阵列内的所有其他磁盘，以便重建故障磁盘的数据。对于大磁盘集的 RAID 5 重建，可能需要几个小时。<br>RAID 级别 0 用于数据损失并不重要的高性能应用程序。RAID 级别 1，对于需要高可靠性和快速恢复的应用程序，很受欢迎。RAID 0+1 和 RAID 1+0 用于性能和可靠性都重要的应用，例如小型数据库。由于 RAID 1 的高空间开销，RAID 5 通常是存储大量数据的首选。虽然级别 6 并不为许多 RAID 实现所支持，但是它应该比级别 5 提供更好的可靠性。<br>RAID 系统设计人员和存储管理员还必须做出其他几个决定。例如，给定 RAID 集应有多少磁盘？每个奇偶校验位应保护多少位？如果阵列内的磁盘越多，则数据传输率就越高，但是系统就越昂贵。如果奇偶校验位保护的位越多，则由于奇偶校验位而导致的空间开销就越低，但是在第一个故障磁盘需要替换之前而第二个磁盘出现故障并且导致数据丢失的机会就越高。<br>RAID 概念已扩展到其他存储设备（包括磁带阵列），甚至无线系统的数据广播。当应用于磁带阵列时，即使磁带阵列内的一个磁带损坏，仍然可以利用 RAID 结构恢复数据。当应用于广播数据时，每块数据可分为短单元，并和奇偶校验单元一起广播。如果一个单元出于某种原因不能收到，则它可以通过其他单元来重构。通常，磁带驱动机器人包括多个磁带驱动器，可将数据分散在所有驱动器上以增加吞吐量和降低备份时间。</p><h1 id="RAID-的问题"><a href="#RAID-的问题" class="headerlink" title="RAID 的问题"></a>RAID 的问题</h1><p>RAID并不总是确保数据对操作系统和使用者是可用的。<br>例如，文件指针可能是错的，或文件结构内的指针可能是错的。如果没有正确恢复，则不完整的写入会导致数据损坏。一些其他进程也会偶然写出文件系统的结构。RAID 防范物理媒介错误，但不是其他硬件和软件错误。与软件和硬件错误一样，系统数据潜在危险也有许多。<br>Solaris ZFS文件系统采用创新方法来解决这些问题，即采用校验和，这是用于验证数据完整性的一种技术。ZFS维护所有块(包括数据和元数据)的内部校验和。这些校验和没有与正在进行校验的块放在一起；而是与块的指针放在一起(见下图 )</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/a3fd05f73dbc41a1b16ec8c9cca70788?from=pc" alt="操作系统基础55-磁盘冗余阵列RAID"></p><p>ZFS 校验所有的元数据与数据</p><p>考虑一个信息节点（存储文件系统元数据的结构），带有数据指针。每个数据块的校验和位于inode 内，如果数据有问题，则校验和会不正确，并且文件系统会知道它。如果数据是镜像的，有一个块具有正确的校验和，并且另有一个块具有不正确的校验和，那么ZFS会自动采用好的块来更新错误的块。<br>类似地，指向inode的目录条目具有inode的校验和。当访问目录时，inode 的任何问题会检测到。所有 ZFS结构都会进行校验和，以便提供比RAID磁盘集或标准文件系统更高级别的一致性、错误检测和错误纠正。因为ZFS的整体性能非常快，校验和计算与额外块读-改-写周期的额外开销不是明显的。<br>大多数的RAID实现的另一个问题是缺乏灵活性。考虑一个具有 20 个磁盘的存储阵列，它分为 4 组，每组有 5 个磁盘。5 个磁盘的组为 RAID 级别 5。因此，有 4 个单独的卷，每个都有文件系统。但是如果文件太大以致于不适合 5 个磁盘的组，怎么办？如果另一个文件系统需要很少的空间，怎么办？如果事先已经知道这些因素，则可以正确分配磁盘和卷。然而，很多时候磁盘的使用和需求随时间而变化。<br>即使存储阵列允许 20 个磁盘的集合创建成一个大的RAID集，其他问题可能出现。多个各种大小的卷可以创建在这个集上。但是有的卷管理器不允许我们改变卷的大小。在这种情况下，会有与上述相同的不匹配文件系统大小的问题。有些卷管理器允许更改大小，但是有些文件系统不允许文件系统生长或收缩。卷可以更改大小，但是文件系统需要重建以利用这些改变。<br>ZFS将文件系统管理和卷管理组合到一起，比这些功能的传统分开提供更强的功能。磁盘，或磁盘分区，通过RAID集组成存储池。每个池可以容纳一个或多个ZFS文件系统。整个池的可用空间可用于该池的所有文件系统。</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/d0c8853581634859b3286470eeedd5ca?from=pc" alt="操作系统基础55-磁盘冗余阵列RAID"></p><p>ZFS采用内存模型的malloc() 和 free()，为每个文件系统分配和释放存储。因此，存储使用没有人为限制，无需在卷之间重定位文件系统，或调整卷大小。ZFS提供配额来限制文件系统的大小，并提供预留以确保文件系统可以增长指定数量，但是文件系统所有者可以随时改变这些变量。上图a) 显示了传统卷和文件系统，而上图b) 显示了 ZFS 模型。</p><p>参考文档：《操作系统概念》</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础55-磁盘冗余阵列RAID&quot;&gt;&lt;a href=&quot;#操作系统基础55-磁盘冗余阵列RAID&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础55-磁盘冗余阵列RAID&quot;&gt;&lt;/a&gt;操作系统基础55-磁盘冗余阵列RAID&lt;/h1&gt;&lt;p&gt;20</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础54-磁盘管理</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8054-%E7%A3%81%E7%9B%98%E7%AE%A1%E7%90%86/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8054-%E7%A3%81%E7%9B%98%E7%AE%A1%E7%90%86/</id>
    <published>2021-08-25T03:27:55.000Z</published>
    <updated>2021-08-25T03:28:43.681Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础54-磁盘管理"><a href="#操作系统基础54-磁盘管理" class="headerlink" title="操作系统基础54-磁盘管理"></a>操作系统基础54-磁盘管理</h1><p>2021-02-06 01:09·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><p>操作系统还负责磁盘管理的其他几个方面。本节讨论磁盘初始化、磁盘引导、坏块恢复等。</p><h1 id="磁盘格式化"><a href="#磁盘格式化" class="headerlink" title="磁盘格式化"></a>磁盘格式化</h1><p>一个新的磁盘是一个空白盘，它只是一个磁性记录材料的盘子。在磁盘可以存储数据之前，它必须分成扇区，以便磁盘控制器能够读写，这个过程称为低级格式化或物理格式化。<br>低级格式化为每个扇区使用特殊的数据结构，填充磁盘。每个扇区的数据结构通常由头部、数据区域(通常为512字节大小)和尾部组成。头部和尾部包含了一些磁盘控制器的使用信息，如扇区号和纠错代码(ECC)。<br>当控制器通过正常I/O写入一个扇区的数据时，ECC采用根据数据区域所有字节而计算的新值来加以更新。在读取一个扇区时，ECC值会重新计算，并与原来存储的值相比较，如果存储和计算的数值不一样，则表示扇区数据区已损坏，并且磁盘扇区可能已坏。<br>ECC是纠错代码，因为它有足够的信息，以便在只有少数数据损坏时，控制器能够识别哪些位已经改变，并且计算它们的正确值应该是什么。然后它会报告可恢复的软错误。当读或写一个扇区时，控制器自动进行ECC处理。<br>大多数磁盘在工厂时作为制造过程的一部分就已低级格式化。这种格式化能让制造商测试磁盘，并且初始化逻辑块号到无损磁盘扇区的映射。对于许多磁盘，当磁盘控制器低级格式化磁盘时，还能指定在头部和尾部之间留下多长的数据区。通常有几个选择，如256、512和1024字节等。采用较大扇区来低级格式化磁盘，意味着每个磁道的扇区数会更少，但也意味着每个磁道的头部和尾部信息会更少，用户数据的可用空间会更多。有的操作系统只能处理512字节的扇区大小。<br>在可以使用磁盘存储文件之前，操作系统仍然需要将自己的数据结构记录在磁盘上。这分为两步：</p><ul><li>第一步是将磁盘分为由柱面组成的多个分区。操作系统可以将每个分区作为一个单独磁盘。例如，一个分区可以存储操作系统的可执行代码，而另一个分区存储用户数据；</li><li>第二步是逻辑格式化，或创建文件系统。在这一步，操作系统将初始的文件系统数据结构存储到磁盘上。这些数据结构包括空闲和已分配的空间和一个初始为空的目录。</li></ul><p>为了提高效率，大多数操作系统将块组合在一起变成更大的块，经常称为簇。磁盘I/O按<strong>块</strong>完成，而文件系统I/O按<strong>簇</strong>完成，有效确保了I/O具有更多的顺序访问和更少的随机访问的特点。<br>有些操作系统允许特殊程序将磁盘分区作为逻辑块的一个大的有序数组，而没有任何文件系统数据结构。这个数组有时称为原始磁盘，这个数组的I/O称为<strong>原始I/O</strong>。例如，有些数据库系统喜欢使用原始I/O，因为能够允许它们控制每条数据库记录存储的精确磁盘位置。原始I/O绕过所有文件系统服务，如缓冲区缓存、文件锁定、预取、空间分配、文件名和目录等。虽然某些应用程序可以通过原始分区来实现自己特殊的更为高效的存储服务，但是大多数应用程序在使用常规文件系统服务时会执行的更好。</p><h1 id="引导块"><a href="#引导块" class="headerlink" title="引导块"></a>引导块</h1><p>为了开始运行计算机，如打开电源或重启时，它必须有一个初始程序来运行。这个初始自举程序往往很简单。它初始化系统的所有部分，从CPU寄存器到设备控制器和内存，接着启动操作系统。为此，自举程序找到磁盘上的操作系统内核，加载到内存，并转到起始地址以便幵始操作系统的执行。<br>对于大多数计算机，自举程序处在只读存储器(<strong>ROM</strong>)中。这个位置非常方便，因为ROM不需要初始化而且位于固定位置，这便于处理器在上电或复位时开始执行。并且，由于ROM是只读的，不会受到计算机病毒的影响。它的问题是，改变这种自举代码需要改变ROM硬件芯片。<br>因此，大多数系统存储一个极小的自举程序在启动ROM中，它的作用是从磁盘上调入完整的引导程序。这个完整的引导程序可以轻松改变：可以简单地将新的版本写到磁盘。完整的引导程序存储在磁盘固定位置上的“启动块”。具有启动分区的磁盘称为启动磁盘或系统磁盘。<br>引导ROM内的代码指示磁盘控制器将引导块读到内存(这时不加载设备驱动程序)，然后开始执行代码。完整的自举程序比引导ROM的自举程序更加复杂。它可以从非固定的磁盘位置处加载整个操作系统，并且开始运行操作系统。即使如此，完整的自举程序可能很小。</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/94e33b50443f421b973e0f026de8f69a?from=pc" alt="操作系统基础54-磁盘管理"></p><p>Windows的磁盘引导</p><p>下面以Windows为例，分析引导过程。首先，请注意，Windows允许将磁盘分为多个分区：</p><ul><li>有一个分区为引导分区，包含操作系统和设备驱动程序。</li><li>Windows系统将引导代码存在磁盘的第一个扇区，它称为主引导记录（MBR）。引导首先运行驻留在系统ROM内存中的代码。这个代码指示系统从MBR中读取、引导代码。除了包含引导代码，MBR包含：一个表（以列出磁盘分区）和一个标志（以指示从哪个分区引导系统），如上图所示。</li><li>当系统找到引导分区，它读取分区的第一个扇区，称为引导扇区，并继续余下的引导过程，这包括加载各种子系统和系统服务</li></ul><h1 id="坏块"><a href="#坏块" class="headerlink" title="坏块"></a>坏块</h1><p>因为磁盘具有移动部件并且容错差（请记住，磁头恰好飞行在磁盘表面上方），容易出现故障。有时，故障是彻底的，在这种情况下，需要更换磁盘，并且从备份介质上将其内容恢复到新的磁盘。更为常见的是，一个或多个扇区坏掉。大多数磁盘出厂时就有坏块。这些坏块的处理多种多样，取决于使用的磁盘和控制器。<br>对于简单磁盘，如采用IDE控制器的磁盘，可以手动处理坏块。一种策略是，在格式化磁盘时扫描磁盘以便发现坏块。发现的任何坏块，标记为不可用，以便文件系统不再分配它们。如果在正常操作时块变坏了，则必须人工运行特殊程序（如Linux命令 <strong>badlocks</strong>），以便搜索坏块并锁定它们。坏块中的数据通常会丢失。<br>更为复杂的磁盘在恢复坏块时更为智能。它的控制器维护磁盘内的坏块列表。这个列表在出厂低级格式化时初始化，并且在磁盘使用寿命内更新。低级格式化将一些块放在一边作为备用，操作系统看不到这些块。控制器可以采用备用块来逻辑地替代坏块。这种方案称为扇区备用或扇区转寄。<br>典型的坏扇区事务可能如下：</p><ol><li>操作系统尝试读取逻辑块87。</li><li>控制器计算ECC，并且发现扇区是坏的。它向操作系统报告这一发现。</li><li>当下次重启操作系统时，可以运行特殊命令，以告诉控制器通过备用块替代坏块。</li><li>之后，每当系统试图访问逻辑块87时，这一请求转换成控制器的替代扇区的地址。</li></ol><p>请注意，控制器的这种重定向可能会使操作系统的磁盘调度算法失效。为此，大多数磁盘在格式化时为每个柱面保留了少量的备用块，还保留了一个备用柱面。当需要重新映射坏块时，控制器尽可能地使用同一柱面的备用扇区。<br>作为扇区备用的替代方案，有些控制器可以采用扇区滑动来替换坏块。这里有一个例子，假定逻辑块17变坏，并且第一个可用的备用块在扇区202之后。然后，扇区滑动重新映射从17到202的所有扇区，将它们全部下移一个扇区。也就是说，扇区202复制到备用扇区，扇区201到 202，200到 201，依次类推，直到扇区18复制到扇区19。按这种方式滑动扇区释放扇区18的空间，以使扇区17 能够映射到它。<br>坏块的更换一般不是全自动的，因为坏块的数据通常会丟失。一些软错误可能触发一个进程，以便复制块数据和备份或滑动块。然而，不可恢复的硬错误导致数据丢失。因此，任何使用坏块的文件必须修复（如从备份磁带中恢复），而且通常需要人工干预。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础54-磁盘管理&quot;&gt;&lt;a href=&quot;#操作系统基础54-磁盘管理&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础54-磁盘管理&quot;&gt;&lt;/a&gt;操作系统基础54-磁盘管理&lt;/h1&gt;&lt;p&gt;2021-02-06 01:09·&lt;a href=&quot;</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础53-磁盘连接</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8053-%E7%A3%81%E7%9B%98%E8%BF%9E%E6%8E%A5/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8053-%E7%A3%81%E7%9B%98%E8%BF%9E%E6%8E%A5/</id>
    <published>2021-08-25T03:26:11.000Z</published>
    <updated>2021-08-25T03:27:26.380Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础53-磁盘连接"><a href="#操作系统基础53-磁盘连接" class="headerlink" title="操作系统基础53-磁盘连接"></a>操作系统基础53-磁盘连接</h1><p>2021-02-04 02:24·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><p>计算机访问磁盘存储有3种方式：</p><ol><li>通过 I/O 端口（或直接连接存储(又称“直连式存储”，DAS))，小系统常采用这种方式；</li><li>通过分布式文件系统的远程主机，这称为网络连接存储(NAS)；</li><li>存储区域网络(SAN)适用于大型客户机-服务器环境；</li></ol><h1 id="直接连接存储-DAS"><a href="#直接连接存储-DAS" class="headerlink" title="直接连接存储(DAS)"></a>直接连接存储(DAS)</h1><p>直接连接存储是通过本地 I/O 端口来访问的存储。这些端口使用多种技术。典型的台式PC采用I/O 总线架构，如IDE或 ATA。这类架构允许每条I/O总线最多支持两个驱动器。SATA 为更新的、类似的、布线更加简化的一个协议。<br>高端工作站和服务器通常采用更复杂的I/O架构，例如光纤通道(FC)。FC是一个高速的串行架构，运行在光纤或四芯铜线上。它有两个变体。一个是大的交换结构，具有24位地址空间。这个变体预计未来将占主导地位，是存储域网(SAN)的基础。<br>由于通信的较大地址空间和交换性质，多个主机和存储设备可以连到架构，使得 I/O 通信具有极大的灵活性。另一个FC变体是FC仲裁环路(FC-AL)，可以寻址126个设备（驱动器和控制器）。<br>多种存储设备适合用作主机连接存储；包括硬盘驱动器、RAID阵列、CD、DVD 和磁带驱动器。对主机连接存储设备进行数据传输的I/O命令是，针对特定存储单元（例如总线 ID 和目标逻辑单元）的逻辑数据块的读和写。</p><h1 id="网络连接存储-NAS"><a href="#网络连接存储-NAS" class="headerlink" title="网络连接存储(NAS)"></a>网络连接存储(NAS)</h1><p>网络连接存储(NAS)设备是一种专用存储系统，可以通过数据网络来远程访问(如下图)。客户通过远程过程调用(RPC)访问网络连接存储，如UNIX系统的NFS或 Windows 机器的CIFS。远程过程调用通过 IP 网络(通常为向客户传输数据的局域网 LAN)的 TCP 或UDP来进行。因此，将NAS作另一个存储访问协议可能是最简单的。网络连接存储单元通常采用RPC接口软件来实现。</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/4954b66a418a4574b09569a7df96a412?from=pc" alt="操作系统基础53-磁盘连接"></p><p>网络连接存储</p><p>网络连接存储提供了一种方便方法，以便所有LAN上的计算机通过与本地主机连接存储一样方便的命名和访问，来共享存储池。然而，与主机本地的连接存储相比，这种方法似乎效率更低，并且性能更差。<br>Internet 小型计算机系统接口(Interface，iSCSI)是最新的网络连接存储协议。在本质上，它采用IP 网络协议来执行SCSI协议。从而，主机与存储之间的互连可能是网络，而不是 SCSI 电缆。因此，主机可以将存储当作好似直接连接的，即使存储远离主机。</p><h1 id="存储区域网络-SAN"><a href="#存储区域网络-SAN" class="headerlink" title="存储区域网络(SAN)"></a>存储区域网络(SAN)</h1><p>网络连接存储系统的缺点是存储 I/O 操作消耗数据网络的带宽，从而增加网络通信的延迟。这个问题对于大型客户机-服务器环境可能特别严重；服务器与客户机之间的通信和服务器与存储设备之间的通信，竞争通信带宽。</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/ff657492ba8341cbae4a8bfc8855dac6?from=pc" alt="操作系统基础53-磁盘连接"></p><p>存储区域网络</p><p>存储区域网络(SAN)为专用网络，釆用存储协议而不是网络协议连接服务器和存储单元，如上图 所示。SAN的优势在于灵活性，多个主机和多个存储阵列可以连接到同一个SAN上，存储可以动态分配到主机。<br>SAN交换机允许或禁止主机访问存储。例如，当主机的磁盘空间变低时，可以通过配置SAN来为主机提供更多存储。SAN可以让服务器集群共享同样的存储，让存储阵列包括多个直接主机连接。与存储阵列相比，SAN通常具有更多端口以及更多昂贵端口。<br>虽然FC是最常见的SAN互连，但是iSCSI使用正在增加。另一个SAN互连是Infmi-Band，这种专用总线架构提供硬件和软件，以支持服务器和存储单元的髙速互连网络。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础53-磁盘连接&quot;&gt;&lt;a href=&quot;#操作系统基础53-磁盘连接&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础53-磁盘连接&quot;&gt;&lt;/a&gt;操作系统基础53-磁盘连接&lt;/h1&gt;&lt;p&gt;2021-02-04 02:24·&lt;a href=&quot;</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础52-磁盘物理结构</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8052-%E7%A3%81%E7%9B%98%E7%89%A9%E7%90%86%E7%BB%93%E6%9E%84/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8052-%E7%A3%81%E7%9B%98%E7%89%A9%E7%90%86%E7%BB%93%E6%9E%84/</id>
    <published>2021-08-25T03:18:53.000Z</published>
    <updated>2021-08-25T03:20:55.325Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础52-磁盘物理结构"><a href="#操作系统基础52-磁盘物理结构" class="headerlink" title="操作系统基础52-磁盘物理结构"></a>操作系统基础52-磁盘物理结构</h1><p>2021-02-03 01:25·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><h1 id="磁盘"><a href="#磁盘" class="headerlink" title="磁盘"></a>磁盘</h1><p>由于内存通常太小而且不能永久保存所有数据和程序，因此计算机系统必须提供外存来备份内存。现代计算机系统采用磁盘（硬盘）作为信息（程序与数据）的主要在线存储介质。换句话说，硬盘或磁盘为现代计算机系统提供大量外存。</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/0d384fbf61f04559b79daa240200b3fb?from=pc" alt="操作系统基础52-磁盘物理结构"></p><p>硬盘的物理结构</p><p>在概念上，硬盘比较简单（图 1）。每个盘片为平的圆状，如同 CD 一样。普通盘片的直径为 1.8〜3.5 英寸（1 英寸=2.54 厘米）。盘片的两面都涂着磁质材料。通过在盘片上进行磁性记录可以保存信息。<br>读写磁头“飞行”在一个盘片的表面上方。磁头附着在磁臂上，磁臂将所有磁头作为一个整体而一起移动。盘片的表面逻辑地分成圆形磁道，再细分为扇区。同一磁臂位置的磁道集合形成了柱面。每个磁盘驱动器有数千个同心柱面，而每个磁道可能包括数百个扇区。常见磁盘驱动器的存储容量按 GB 来计算。<br>当使用磁盘时，驱动器电机高速旋转磁盘。大多数驱动器每秒旋转 60〜250 次，按每分钟转数（RPM）来计。普通驱动器的转速为 5400、7200、10 000 和 15 000RPM。</p><p>磁盘速度有两部分：</p><ul><li>传输速率：在驱动器和计算机之间的数据流的速率。</li><li>定位时间或随机访问时间包括两部分，寻道时间（移动磁臂到所要柱面的所需时间）和旋转延迟（旋转磁臂到所要扇区的所需时间）。</li></ul><p>典型的磁盘可以按每秒数兆字节的速率来传输，并且寻道时间和旋转延迟为数毫秒。<br>因为磁头飞行在极薄的空气垫上（以微米计），所以磁头有与磁盘表面接触的危险。虽然盘片涂有薄薄的保护层，但是磁头有时可能损坏磁盘表面。这个事故称为磁头碰撞。磁头碰撞通常无法修复，必须替换整个磁盘。<br>磁盘可以是可移动的，允许按需安装不同磁盘。可移动磁盘通常只有一个盘片，它保存在塑料盒内以防止不在驱动器内时被损坏。其他形式的可移动磁盘包括 CD、DVD、蓝光光盘以及称为闪存驱动器的可移动闪存设备（这是一种固态驱动器）。<br>磁盘驱动器通过称为 I/O 总线的一组电缆连到计算机。有多种可用总线，包括硬盘接口技术（ATA）、串行 ATA（SATA）、外部串行 ATA（eSATA）、通用串口总线（USB）、光纤通道（FC）。<br>数据传输总线由称为控制器的专门电子处理器来进行，主机控制器为总线的计算机端的控制器，磁盘控制器是磁盘驱动器内置的。为了执行磁盘 I/O 操作，计算机通过内存映射 I/O 端口，发送一个命令到主机控制器。接着，主机控制器通过消息将该命令送给磁盘控制器，并且磁盘控制器操作磁盘驱动器硬件，以执行命令。磁盘控制器通常具有内置缓存。磁盘驱动器的数据传输，在缓存和磁盘表面之间进行；而到主机的数据传输，则按更快速度在缓存和主机控制器之间进行。</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/c883cd833f67428da7160668d4cd0bec?from=pc" alt="操作系统基础52-磁盘物理结构"></p><h1 id="固态磁盘"><a href="#固态磁盘" class="headerlink" title="固态磁盘"></a>固态磁盘</h1><p>随着经济发展或技术革新，老旧技术有时按新的方式来使用，比如固态磁盘（SSD）的日益重要。<br>简单而言，SSD 为非易失性存储，可用作硬盘。这种技术，有多个变形，从带有电池（以允许在电源故障时维护状态）的 DRAM，到闪存技术，如单层单元（SLC）或多层单元（MLC）的芯片。<br>SSD 具有与传统硬盘相同的特性，但是会更可靠，因为没有移动部件；而且会更快，因为没有寻道时间或延迟。此外，其电源消耗更少。然而，与传统硬盘相比，SSD的每兆字节更昂贵，容量也更大，寿命也更短，所以用途有限。SSD的用途之一是存储数组，这可用于存储文件系统元数据以提高性能。SSD 也用于一些笔记本电脑，以便更小、更快、更节能。<br>因为 SSD 比磁盘驱动器要快得多，标准总线接口可能会对吞吐量造成重大限制。有些 SSD 设计成直接连到系统总线（例如，PCI）。SSD也改变其他方面的传统计算机设计。有些系统直接使用 SSD 来替代磁盘驱动器。而其他的用 SSD 作为缓存，以便在磁盘、SSD 和内存之间移动数据，来优化性能。</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/9129555819a94ee2b0ce5c9ca231cbff?from=pc" alt="操作系统基础52-磁盘物理结构"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础52-磁盘物理结构&quot;&gt;&lt;a href=&quot;#操作系统基础52-磁盘物理结构&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础52-磁盘物理结构&quot;&gt;&lt;/a&gt;操作系统基础52-磁盘物理结构&lt;/h1&gt;&lt;p&gt;2021-02-03 01:25·&lt;</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础51-磁盘空间的分配方法</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8051-%E7%A3%81%E7%9B%98%E7%A9%BA%E9%97%B4%E7%9A%84%E5%88%86%E9%85%8D%E6%96%B9%E6%B3%95/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8051-%E7%A3%81%E7%9B%98%E7%A9%BA%E9%97%B4%E7%9A%84%E5%88%86%E9%85%8D%E6%96%B9%E6%B3%95/</id>
    <published>2021-08-25T03:17:42.000Z</published>
    <updated>2021-08-25T03:18:26.754Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础51-磁盘空间的分配方法"><a href="#操作系统基础51-磁盘空间的分配方法" class="headerlink" title="操作系统基础51-磁盘空间的分配方法"></a>操作系统基础51-磁盘空间的分配方法</h1><p>2021-01-31 20:49·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><p>磁盘直接访问的特点在文件实现时提供了灵活性。在几乎每种情况下，很多文件都是存储在同一个磁盘上的。主要的问题是，如何为这些文件分配空间，以便有效使用磁盘空间和快速访问文件。<br>磁盘空间分配的主要常用方法有三个：<strong>连续分配、链接分配</strong>和<strong>索引分配</strong>。每个方法各有优缺点。虽然有些系统对这三种方法都支持。但是更为常见的是，一个系统只对同一文件系统类型的所有文件采用一种方法。</p><h1 id="连续分配"><a href="#连续分配" class="headerlink" title="连续分配"></a>连续分配</h1><p>连续分配方法要求，每个文件在磁盘上占有一组连续的块。磁盘地址为磁盘定义了一个线性排序。有了这个排序，假设只有一个作业正在访问磁盘，在块b之后访问块b+1通常不需要移动磁头。当需要磁头移动（从一个柱面的最后扇区到下一个柱面的第一扇区）时，只需要移动一个磁道。因此，用于访问连续分配文件的所需寻道数量最小，在确实需要寻道时所需的寻道时间也最小。<br>文件的连续分配可以用首块的磁盘地址和连续的块数来定义。如果文件有n块长并从位置b开始，则该文件将占有块b，b+1，b+2，…，b+n-1。每个文件的目录条目包括起始块的地址和该文件所分配区域的长度，参见下图</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/7bd9ab140bff47bf8c89f85ded015b24?from=pc" alt="操作系统基础51-磁盘空间的分配方法"></p><p>磁盘空间的连续分配</p><p>连续分配文件的访问非常容易。对于顺序访问，文件系统会记住上次引用的块的磁盘地址，如需要可读入下一块。对于直接访问一个文件的从块b开始的第i块，可以直接访问块b+i。因此，连续分配支持顺序访问和直接访问。<br>不过，连续分配也有一些问题。一个难题是，为新文件找到空间。用于管理空闲空间的系统决定了这个任务如何完成，虽然可以使用任何管理系统，但是有的系统会比其他的要慢。<br>连续分配问题可以作为通用动态存储分配问题的一个具体应用，即如何从一个空闲块列表中寻找一个满足大小为 n 的空间。从一组空闲块中寻找一个空闲块的最为常用的策略是，首次适合和最优适合。模拟结果显示在时间和空间使用方面，首次适合和最优适合都要比最坏适合更为高效。首次适合和最优适合在空间使用方面不相上下，但是首次适合一般更快。<br>所有这些算法都有外部碎片的问题。随着文件的分配和删除，可用磁盘空间被分成许多小片。只要空闲空间分成小片，就会存在外部碎片。当最大连续片不能满足需求时就有问题；存储空间分成了许多小片，其中没有一个足够大以存储数据。因磁盘存储总量和文件平均大小的不同，外部碎片可能是个小问题，但也可能是个大问题。<br>为了防止外部碎片引起的大量磁盘空间的浪费，将整个文件系统复制到另一个磁盘。原来的磁盘完全变成空的，从而创建了一个大的连续空闲空间。然后，通过从这个大的连续空闲空间采用连续分配方法，将这些文件复制回来。这种方案将所有空闲空间有效合并起来，解决了碎片问题。<br>这种合并的代价是时间，而且大硬盘的代价可能特别高。合并这些磁盘空间可能需要数小时，可能每周都需进行。有些系统要求，这个功能线下执行且文件系统要卸载。在这停机期间不能进行正常操作，因此生产系统应尽可能地避免合并。大多数的需要整理碎片的现代系统能够和正常的系统操作一起在线执行合并，但是性能下降可能很明显。<br>连续分配的另一个问题是，确定一个文件需要多少空间。当创建一个文件时，需要找到并分配它所需空间的总数。创建者（程序或人员）又如何知道所创建文件的大小？在某些情况下，这种判断可能相当简单（例如，复制一个现有文件），一般来说，输出文件的大小可能难以估计。<br>如果为文件分配的空间太小，则可能会发现文件无法扩展。特别是对于最优适合的分配策略，文件两侧的空间可能已经使用。因此，不能在原地让文件更大。<br>这时，有两种可能办法：</p><ol><li>终止用户程序，并给出适当的错误消息。这样，用户必须分配更多的空间，并再次运行该程序。这些重复的运行可能代价很高。为了防止这些问题，用户通常会高估所需的磁盘空间，从而造成相当大的空间浪费。</li><li>找一个更大的空间，将文件内容复制到新空间，并释放以前的空间。只要空间存在，就可以重复这些动作，不过如此可能耗时。然而，用户无需获知究竟发生了什么事情；而系统虽有问题仍继续运行，只不过会越来越慢。</li></ol><p>即使文件所需的空间总量事先已知，预先分配仍可能很低效。一个文件在很长时间内增长缓慢（数月或数年），仍必须按它的最终大小来分配足够空间，即使这个空间很长时间内不用。因此，该文件有一个很大的内部碎片。<br>为了最小化这些缺点，有些操作系统使用连续分配的修正方案。这里，最初分配一块连续空间。以后，当这个数量不够时，会添加另一块连续空间（称为扩展）。然后，文件块的位置就记录为：地址、块数、下一扩展的首块的指针。<br>在有些系统上，文件所有者可以设置扩展大小，但是如果所有者不正确，这种设置会导致低效。如果扩展太大，内部碎片可能仍然是个问题；随着不同大小的扩展的分配和删除，外部碎片可能也是个问题。</p><h1 id="链接分配"><a href="#链接分配" class="headerlink" title="链接分配"></a>链接分配</h1><p>链接分配解决了连续分配的所有问题。采用链接分配，每个文件是磁盘块的链表；磁盘块可能会散布在磁盘的任何地方。目录包括文件第一块和最后一块的指针。例如，一个有5块的文件可能从块 9开始，然而是块16、块1、块10，最后是块25（如下图）。每块都有下一块的一个指针。用户不能使用这些指针。因此，如果每块有512字节，并且磁盘地址（指针）需要4字节，则用户可以使用508字节。</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/c08809b7bfa34398b1e139087bf06db7?from=pc" alt="操作系统基础51-磁盘空间的分配方法"></p><p>磁盘空间的链接分配</p><p>要创建一个新文件，只需在目录中增加一个新的条目。采用链接分配，每个目录条目都有文件首个磁盘块的一个指针。这个指针初始化为 null，表示一个空的文件。大小字段也设置为 0。写文件导致空闲空间管理系统找到一个空闲块，这个新块会被写入，并链接到文件的尾部。读文件，只需按照块到块的指针来读块。<br>采用链接分配没有外部碎片，空闲空间列表的任何块可以用于满足请求。当创建文件时，并不需要说明文件的大小只，要有可用的空闲块，文件就可以继续增长。因此，无需合并磁盘空间。<br>然而，链接分配确实有缺点。主要问题是，它只能有效用于顺序访问文件。要找到文件的第 i 个块，必须从文件的开始起，跟着指针，找到第 i 块。每个指针的访问都需要一个磁盘读，有时需要磁盘寻道。因此，链接分配不能有效支持文件的直接访问。<br>另一个缺点是指针所需的空间。如果指针需要使用 512 字节块的 4 字节，则 0.78% 的磁盘空间会用于指针，而不是其他信息。每个文件需要比原来稍多的空间。<br>这个问题的通常解决方案是，将多个块组成簇，并按簇而不是按块来分配。例如，文件系统可以定义一个簇为 4 块，在磁盘上仅以簇为单位来操作。这样，指针所占的磁盘空间的百分比就要小得多。这种方法使得逻辑到物理块的映射仍然简单，但提高了磁盘吞吐量（因为需要更少的磁头移动），并且降低了块分配和空闲列表管理所需的空间。这种方法的代价增加了内部碎片；如果一个簇而不是块没有完全使用，则会浪费更多空间。簇可以改善许多算法的磁盘访问时间，因此用于大多数操作系统。<br>链接分配的另一个问题是可靠性。回想一下，文件是通过散布在磁盘上的指针链接起来的，操作系统软件错误或磁盘硬件故障可能导致获得一个错误指针，这个错误可能会导致链接到空闲空间列表或链接到另一个文件。一个不完全的解决方案是，采用双向链表；另一个是，每块存储文件名称和相对块号。然而，这些方案为每个文件增加了更多额外开销。<br>链接分配的一个重要变种是文件分配表(FAT)的使用。这个简单而有效的磁盘空间分配方法用于MS-DOS操作系统。每个卷的开头部分的磁盘用于存储该表。在该表中，每个磁盘块都有一个条目，并可按块号来索引。FAT的使用与链表相同。目录条目包含文件首块的块号。通过这个块号索引的表条目包含文件的下一块的块号。这条链会继续下去，直到最后一块；而最后一块的表条目的值为文件结束值。未使用的块用0作为表条目的值来表示。为文件分配一个新块只要简单找到第一个值为0的 FAT 条目，用新块的地址替换前面文件结束值，用文件结束值替代0。由块217、618、339 组成的文件的 FAT 结构如下图所示</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/7b5285b22ff14a9eb66c9b169a4a0197?from=pc" alt="操作系统基础51-磁盘空间的分配方法"></p><p>文件分配表</p><p>如果不对FAT采用缓存，FAT分配方案可能导致大量的磁头寻道时间。磁头必须移到卷的开头，读入FAT，找到所需块的位置，再移到块本身的位置。在最坏的情况下，每块都需要移动两次。优点是改善了随机访问时间；因为通过读入FAT信息，磁头能找到任何块的位置。</p><h1 id="索引分配"><a href="#索引分配" class="headerlink" title="索引分配"></a>索引分配</h1><p>链接分配解决了连续分配的外部碎片和大小声明的问题。然而，在没有FAT时，链接分配不能支持髙效的直接访问，因为块指针与块一起分散在整个磁盘上，并且必须按序读取。索引分配通过将所有指针放在一起，即索引块，解决了这个问题。<br>每个文件都有自己的索引块，这是一个磁盘块地址的数组。索引块的第<strong>i</strong>个条目指向文件的第<strong>i</strong>个块。目录包含索引块的地址(下图 )。当查找和读取第<strong>i</strong>个块时，采用第<strong>i</strong>个索引块条目的指针。</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/b0966992029646f083f77ed022ad7ab4?from=pc" alt="操作系统基础51-磁盘空间的分配方法"></p><p>磁盘空间的索引分配</p><p>当创建文件时，索引块的所有指针都设为null。当首次写入第i块时，先从空闲空间管理器中获得一块，再将其地址写到索引块的第i个条目。<br>索引分配支持直接访问，并且没有外部碎片问题，因为磁盘的任何空闲块可以满足更多空间的请求。然而，索引分配确实浪费空间。索引块指针的开销通常大于链接分配的指针开销。考虑一下常见情况，即一个文件只有一块或两块。采用链接分配，每块只浪费一个指针的空间。采用索引分配，即使只有一个或两个指针是非空的，也必须分配一个完整的索引块。<br>这一点提出了一个问题：索引块应为多大？每个文件必须有一个索引块，因此需要索引块尽可能小。然而，如果索引块太小，它不能为大的文件存储足够多的指针。因此，必须采取一种机制，以处理这个问题。此目的的机制包括：</p><ul><li>链接方案：一个索引块通常为一个磁盘块。因此，它本身能直接读写。为了支持大的文件，可以将多个索引块链接起来。例如，一个索引块可以包括一个含有文件名的头部和一组头100个磁盘块的地址。下一个地址（索引块的最后一个字）为 null（对于小文件），或者另一个索引块的指针(对于大文件)。</li><li>多级索引：链接表示的一种变种是，通过第一级索引块指向一组第二级的索引块，它又指向文件块。当访问一块时，操作系统通过第一级索引查找第二级索引块，再采 用这个块查找所需的数据块。这种做法可以持续到第三级或第四级，具体取决于最大文件大小。对于4096字节的块，可以在索引块中存入1024 个4字节的指针。两级索引支持 1 048 576 个数据块和 4GB 的最大文件。</li><li>组合方案：用于基于 UNIX 的文件系统，将索引块的前几个（如15）指针存在文件的 inode 中。这些指针的前12个指向直接块；即它们包含存储文件数据的块的地址。因此，小的文件(不超过12块)不需要单独的索引块。如果块大小为 4KB，则不超过48KB的数据可以直接访问。接下来的3个指针指向间接块。第一个指向一级间接块。一级间接块为索引块，它包含的不是数据，而是真正包含数据的块的地址。第二个指向二级间接块，它包含了一个块的地址，而这个块内的地址指向了一些块，这些块中又包含了指向真实数据块的指针。最后一个指针为三级间接块指针。下图显示了一个 UNIX 的 inode。</li></ul><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/357c1327e2f449da91edc203fed2416d?from=pc" alt="操作系统基础51-磁盘空间的分配方法"></p><p>UNIX 的 inode</p><p>采用这种方法，一个文件的块数可以超过许多操作系统所用的4字节的文件指针所能访问的空间。32位指针只能访问232字节，或4GB。许多UNIX和Linux现在支持64位的文件指针。这样的指针允许文件和文件系统为数艾字节。ZFS文件系统支持128位的文件 指针。<br>索引分配方案与链接分配一样在性能方面有所欠缺。尤其是，虽然索引块可以缓存在内存中，但是数据块可能分布在整个卷上。</p><h1 id="性能"><a href="#性能" class="headerlink" title="性能"></a>性能</h1><p>以上讨论的分配方法，在存储效率和数据块访问时间上有所不同。当操作系统选择合适方法来实现时，这两者都是重要依据。<br>在选择分配方法之前，需要确定系统是如何使用的。以顺序访问为主的系统和以随机访问为主的系统，不应采用相同的方法。<br>对于任何类型的访问，连续分配只需访问一次就能获得磁盘块。由于可以在内存中容易地保存文件的开始地址，所以可以立即计算第<strong>i</strong>块(或下一块)的磁盘地址，并直接读取。<br>对于链接分配，也可以在内存中保留下一块的地址，并直接读取。对于顺序访问，这种方法很好；然而，对于直接访问，对第i块的访问可能需要读i次磁盘。这个问题表明了，为什么链接分配不适用于需要直接访问的应用程序。<br>因此，有的系统通过使用连续分配支持直接访问的文件，通过链接分配支持顺序访问的文件。对于这些系统，在创建文件时必须声明使用的访问类型。用于顺序访问的文件可以链接分配，但不能用于直接访问。<br>用于直接访问的文件可以连续分配，能支持直接访问和顺序访问，但是在创建时必须声明其最大的文件大小。在这种情况下，操作系统必须具有适当的数据结构和算法，来支持两种分配方法。<br>文件可以从一种类型转成另一种类型，创建一个所需类型的新文件，将原来文件的内容复制过来，然后可以删除旧文件，再重新命名新文件。<br>索引分配更为复杂。如果索引块已在内存，则可以进行直接访问。然而，在内存中保存索引块需要相当大的空间。如果没有这个内存空间，则可能必须先读取索引块，再读取所需的数据块。对于两级索引，可能需要读取两次索引块。对于一个极大的文件，访问文件末尾附近的块需要首先读取所有的索引块，最后才能读入所需的数据块。因此，索引分配的性能取决于索引结构、文件大小以及所需块的位置。<br>有些系统将连续分配和索引分配组合起来：对于小文件(只有<strong>3</strong>或<strong>4</strong>块的)采用连续分配；当文件增大时，自动切换到索引分配。由于大多数文件较小，小文件的连续分配的效率又高，所以平均性能还是相当不错的。<br>还可以采用许多其他优化方法。鉴于CPU速度和磁盘速度的差距，操作系统采用数千条指令以节省一些磁头移动，不是不合理的。此外，随着时间的推移，这种差距会增加，以致于操作系统采用数十万条指令来优化磁头移动也是值得的。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础51-磁盘空间的分配方法&quot;&gt;&lt;a href=&quot;#操作系统基础51-磁盘空间的分配方法&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础51-磁盘空间的分配方法&quot;&gt;&lt;/a&gt;操作系统基础51-磁盘空间的分配方法&lt;/h1&gt;&lt;p&gt;2021-0</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础50-文件系统实现</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8050-%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E5%AE%9E%E7%8E%B0/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8050-%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E5%AE%9E%E7%8E%B0/</id>
    <published>2021-08-25T03:16:12.000Z</published>
    <updated>2021-08-25T03:21:44.346Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础50-文件系统实现"><a href="#操作系统基础50-文件系统实现" class="headerlink" title="操作系统基础50-文件系统实现"></a>操作系统基础50-文件系统实现</h1><p>2021-01-30 02:32·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><p>文件系统提供了在线存储和访问包括数据和程序在内的文件内容的机制，文件系统永久地驻留在外存上，外存可以永久存储大量数据。</p><h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><ul><li><strong>1、</strong>在磁盘上，文件系统可能包括如下信息：<br>  ①如何启动所存储的操作系统②总的块数③空闲块的数目和位置④目录结构以及各个具体文件等<strong>2、</strong>磁盘结构包括：<br>  ①(每个卷的)<strong>引导控制块(boot control block\Volume)</strong>：通常为分区的第一块。如果该分区没有OS，则为空。(其他名称：引导块(Linux)、分区引导扇区(WindowsNT))<br>  ②(每个卷的)<strong>卷控制块(Volume control block (/volume）</strong>：包括分区详细信息，如分区的块数、块的大小、空闲块的数量和指针、空闲FCB的数量和指针等(亦称为超级块(Linux)、主控文件表(WindowsNT))<br>  ③<strong>目录结构</strong>：用来组织文件<br>  ④<strong>文件控制块</strong>（<strong>FCB</strong>）：包括很多文件信息，如文件许可、拥有者、大小和数据块的位置等<br>  <strong>3、</strong>一个典型的文件控制块包括：<br>  文件权限、文件日期、文件所有者，组，ACL、文件尺寸、文件数据块</li></ul><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/6c42b8afcb5242008421b91aff8b12c9?from=pc" alt="操作系统基础50-文件系统实现"></p><p>典型文件控制块</p><h1 id="文件系统结构"><a href="#文件系统结构" class="headerlink" title="文件系统结构"></a>文件系统结构</h1><p>1、磁盘提供大量的外存空间来维持文件系统，磁盘的两个特点，使其成为存储多个文件的方便媒介：<br>①<strong>可以原地重写</strong>；可以从磁盘上读一块，修改该块，并将它写回到原来的位置<br>②<strong>可以直接访问磁盘上的任意一块信息</strong>。(随机或顺序方式)<br>2、为了改善I/O效率，内存与磁盘之间的I/O转移是以<strong>块</strong>为单位而不是以<strong>字节</strong>为单位来进行的，每个块为一个或多个扇区<br>3、为了提供对磁盘的高效且便捷的访问，操作系统通过文件系统来轻松地存储、定位、提取数据。文件系统的两个设计问题<br>①<strong>如何定义文件系统对用户的接口</strong><br>②<strong>创建数据结构和算法来将逻辑文件系统映射到物理外存设备上</strong><br>4、文件系统按层来进行组织，包括如下（由上向下一次递进）：<br>①应用程序<br>②逻辑文件系统(<strong>logical file system</strong>)：管理元数据：文件系统的所有结构数据，而不包括实际数据（或文件内容）；根据给定符号文件名来管理目录结构；逻辑文件系统通过文件控制块(FCB)来维护文件结构<br>③文件组织模块(<strong>file-organization module</strong>)：知道文件及其逻辑块和物理块，空闲空间管理器，可以将逻辑块地址转换成基本文件系统所用的物理块地址<br>④基本文件系统(<strong>basic file system</strong>)：向合适的设备驱动程序发送一般命令就可对磁盘上的物理块进行读写<br>⑤I/O控制<strong>(I/O control</strong>)：由设备驱动程序和中断处理程序组成，实现内存与磁盘之间的信息转移<br>⑥设备(<strong>devices</strong>)</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/47232896f8654106b6a2512b4ae98fa4?from=pc" alt="操作系统基础50-文件系统实现"></p><p>　设计中每层利用较低层的功能来创建新的功能来对更高层来服务。</p><p>　采用分层的结构实现文件系统，能够最大限度地减小重复的代码。</p><h1 id="文件系统实现"><a href="#文件系统实现" class="headerlink" title="文件系统实现"></a>文件系统实现</h1><p><strong>创建并打开文件的过程</strong></p><p>为了创建一个新文件，应用程序调用逻辑文件系统（其有目录结构形式）。逻辑文件系统分配一个新的FCB。系统把目录信息读入内存，用新的文件名更新该目录和FCB，并写回磁盘。</p><p>系统调用open()会首先搜索系统范围内的打开文件表来确定某个文件是否已经被其他进程使用，如果是，则在单个进程的打开文件表中创建一项，并指向现有的系统范围内的打开文件表。</p><p>当打开文件时，根据给定文件名来搜索目录结构。部分目录结构通常缓存在目录中加快读写速度。</p><p>下图总结了文件系统实现的操作结构:</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/146f79ea7c4c4586b02aadd31bbc59ba?from=pc" alt="操作系统基础50-文件系统实现"></p><p>内存中文件系统结构</p><h1 id><a href="#" class="headerlink" title=" "></a> </h1><p>　　<strong>分区与安装</strong></p><p>　　磁盘布局因为操作系统而异，一个磁盘可以分成多个分区，或者一个卷可以横跨多个磁盘上的数个分区。</p><p>　　“生”磁盘是指没有合适文件系统的磁盘空间。</p><p>　　引导信息能保存在各个分区，它有自己的格式，因为在引导系统时系统并没有文件系统设备驱动程序，所以并不能解释文件系统格式。</p><p>　　根分区包括操作系统内核和其他系统文件，在引导时装入内存，其他卷根据不同操作系统可以在引导时自动装入或在此后手动装入。</p><p>　　<strong>虚拟文件系统(Virtual File System,VFS )</strong></p><p>　　绝大多数操作系统都是通过面向对象技术来简化、组织和模块化实现过程，使用这些方法允许不同文件系统类型可以通过同样的结构来实现，这也包括网络文件，采用数据结构和子程序，可以分开基本系统调用和实现细节，因此，文件系统，主要分为三个层次：从上到下依次是<strong>文件系统接口-</strong>&gt;<strong>VFS接口</strong>-&gt;<strong>本地文件系统</strong>。</p><p>　　第二层<strong>VFS</strong>为虚拟文件系统，主要由两个作用：</p><p>　　1）<strong>VFS</strong>层通过定义一个清晰的<strong>VFS</strong>接口，以将文件系统的通用接口操作和具体实现分开。多个VFS接口的实现可以共存在同一台机器，它允许访问已装在本地的多个类型的文件。</p><p>　　2）<strong>VFS</strong>提供了在网络上唯一标识一个文件的机制。</p><p>　　VFS能区分本地文件和远程文件，根据文件系统类型来进一步区分不同本地文件。</p><p>　　VFS根据文件系统类型调用特定文件类型操作以处理本地请求，通过调用NFS协议程序来处理远程请求。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础50-文件系统实现&quot;&gt;&lt;a href=&quot;#操作系统基础50-文件系统实现&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础50-文件系统实现&quot;&gt;&lt;/a&gt;操作系统基础50-文件系统实现&lt;/h1&gt;&lt;p&gt;2021-01-30 02:32·&lt;</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础49-文件共享与保护</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8049-%E6%96%87%E4%BB%B6%E5%85%B1%E4%BA%AB%E4%B8%8E%E4%BF%9D%E6%8A%A4/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8049-%E6%96%87%E4%BB%B6%E5%85%B1%E4%BA%AB%E4%B8%8E%E4%BF%9D%E6%8A%A4/</id>
    <published>2021-08-25T03:14:52.000Z</published>
    <updated>2021-08-25T03:15:49.107Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础49-文件共享与保护"><a href="#操作系统基础49-文件共享与保护" class="headerlink" title="操作系统基础49-文件共享与保护"></a>操作系统基础49-文件共享与保护</h1><p>2021-01-29 02:34·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/bc2abd1f53eb406ba6848e352c66a3f4?from=pc" alt="操作系统基础49-文件共享与保护"></p><h1 id="文件共享"><a href="#文件共享" class="headerlink" title="文件共享"></a>文件共享</h1><p><strong>1、多用户</strong></p><p>Linux是一个真实的、完整的多用户多任务操作系统，多用户多任务就是可以在系统上建立多个用户，而多个用户可以在同一时间内登录同一个系统执行各自不同的任务，而互不影响，例如某台linux服务器上有4个用户，分别是root、www、ftp和mysql，在同一时间内，root用户可能在查看系统日志，管理维护系统，www用户可能在修改自己的网页程序，ftp用户可能在上传软件到服务器，mysql用户可能在执行自己的SQL查询，每个用户互不干扰，有条不紊的进行着自己的工作，而每个用户之间不能越权访问，比如www用户不能执行mysql用户的SQL查询操作，ftp用户也不能修改www用户的网页程序，因此可知，不同用户具有不同的权限，每个用户是在权限允许的范围内完成不同的任务，linux正是通过这种权限的划分与管理，实现了多用户多任务的运行机制。</p><p>为了实现共享与保护，多用户系统必须要比单用户系统维护更多的文件和目录属性。现在大多数系统都采用文件(或目录)<strong>所有者</strong>(owner)(或用户(user))和<strong>组</strong>(group)的概念。</p><p><strong>2、远程文件系统</strong><br>NFS：网络文件系统<br>DFS：分布式文件系统<br>1)客户机-服务器模型</p><p>2)分布式信息系统<br>为了便于管理客户机-服务器服务，提供用于远程计算所需信息的统一访问</p><p>3)故障模式<br>远程文件系统很容易出现各种故障，比如掉线等。当故障出现时，客户机系统并不将此作为本地文件系统故障那样进行处理，但会终止对故障服务器的所有操作，或者等候直至服务器再次可用。这种故障语义由远程文件系统协议所定义和实现。通常，终止操作代价较大，绝大多数分布式文件系统在故障出现时，选择等候。</p><p>那么故障恢复时，客户机和服务器之间可能需要一定的状态信息。NFS采用了一种简单的方法实现了无状态的DFS，但它不够安全。</p><p><strong>3、一致性语义</strong><br>多用户操作情况下，如何保持文件一致性。<br>1) UNIX语义<br>用户的写操作，可以被其他所见<br>允许用户共享文件指针位置，移动文件指针会影响其他用户</p><p>采用UNIX语义，一个文件与单个物理映射关联，互斥访问。</p><p>2)会话语义<br>用户写操作不能立即被打开同一文件的其他用户所见<br>文件关闭后，修改只能被新打开的会话所见，已经打开的用户看不到</p><p>采用这种语义，一个文件可以对多个物理映射进行关联，并发性很好。</p><p>3)不可修改共享文件语义<br>既然是共享文件，就不能修改，只读。</p><p>在分布式系统中实现这种语义最简单。</p><h1 id="保护"><a href="#保护" class="headerlink" title="保护"></a>保护</h1><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/1e5f8a372a394e29aba9aab608ad11a0?from=pc" alt="操作系统基础49-文件共享与保护"></p><p>文件的保护包括可靠性保护和防止非法访问。可靠性通常由文件备份提供；这里说的保护是指防止非法访问。<br>1、访问类型<br>通过限制可进行的文件访问类型，比如说：<br>读、写、执行、添加、删除、列表清单</p><p>2、访问控制<br>文件对应访问控制列表，而此列表与所有者、组、其他成员组合使用。</p><p>3、其他保护方式<br>文件加密码。</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/fd19ec0f4e114bdeb2a248d9141c865f?from=pc" alt="操作系统基础49-文件共享与保护"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础49-文件共享与保护&quot;&gt;&lt;a href=&quot;#操作系统基础49-文件共享与保护&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础49-文件共享与保护&quot;&gt;&lt;/a&gt;操作系统基础49-文件共享与保护&lt;/h1&gt;&lt;p&gt;2021-01-29 02:</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础48-目录结构</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8048-%E7%9B%AE%E5%BD%95%E7%BB%93%E6%9E%84/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8048-%E7%9B%AE%E5%BD%95%E7%BB%93%E6%9E%84/</id>
    <published>2021-08-25T03:13:41.000Z</published>
    <updated>2021-08-25T03:14:30.985Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础48-目录结构"><a href="#操作系统基础48-目录结构" class="headerlink" title="操作系统基础48-目录结构"></a>操作系统基础48-目录结构</h1><p>2021-01-28 02:06·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><h1 id="1-存储结构"><a href="#1-存储结构" class="headerlink" title="1. 存储结构"></a>1. 存储结构</h1><p>文件存储在随机存取设备上，包括硬盘、光盘和固态(基于内存)盘。</p><p>一个磁盘可以划分多个分区(partition)，每个分区可以有多个单独的文件系统。存储设备可以组成RAID集。</p><p>分区可用于限制单个文件系统的大小，包含文件系统的分区通常称为卷(volume)。卷可以是设备的一部分，或整个设备，或由多个设备组成的RAID集。</p><p>包含文件系统的每个卷也应包含有关系统内的文件信息。这些信息保存在设备目录(device directory)或卷目录表(volume table of content)中。设备目录(更常称为<strong>目录</strong>(directory))记录卷上的所有文件的信息，如名称、位置、大小和类型等</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/40872eaedc8c497da7447f462d8a0300?from=pc" alt="操作系统基础48-目录结构"></p><p>典型的文件系统结构</p><h1 id="2-目录概述"><a href="#2-目录概述" class="headerlink" title="2. 目录概述"></a>2. 目录概述</h1><p>文件系统实现对文件的“按名存取”。</p><p><strong>文件系统需要建立这样一种数据结构</strong>，以实现文件名与文件物理位置之间的映射关系，体现这种对应关系的数据结构称为<strong>文件目录</strong>。目录的每个叶节点保证通过文件名检索到，又包含这个文件物理位置的信息。</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/28915df328f44b3aa6fe7ed7e02e1803?from=pc" alt="操作系统基础48-目录结构"></p><p><strong>目录可看做符号表，它能将文件名称转换成目录条目</strong></p><p><strong> 目录相关操作：</strong></p><p><strong>搜索文件</strong></p><p><strong>创建文件</strong></p><p><strong>删除文件</strong></p><p><strong>遍历目录</strong></p><p><strong>重命名文件</strong></p><p><strong>跟踪文件系统</strong>： 管理文件，包括备份和释放磁盘空间。</p><h1 id="3-单层目录结构"><a href="#3-单层目录结构" class="headerlink" title="3. 单层目录结构"></a>3. 单层目录结构</h1><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/2e3f06572fa548c1ac0756389ec8a5e5?from=pc" alt="操作系统基础48-目录结构"></p><p>单级目录</p><p><strong>优点</strong>： 结构简单，便于支持。</p><p><strong>缺点</strong>： 命名重复问题、用户分组问题、不同用户间文件共享问题。</p><h1 id="两级目录结构"><a href="#两级目录结构" class="headerlink" title="两级目录结构"></a>两级目录结构</h1><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/04600e6050d94e11b3cab07dfa09cac2?from=pc" alt="操作系统基础48-目录结构"></p><p>两级目录结构</p><p><strong>优点</strong>： 命名问题解决、搜索高效。</p><p><strong>缺点</strong>： 不同用户间文件共享问题(有的系统简单地不允许本地用户文件被其他用户访问)。</p><h1 id="5-树形目录"><a href="#5-树形目录" class="headerlink" title="5. 树形目录"></a>5. 树形目录</h1><p>将目录结构扩展为任意高度的树，不再按照用户区分不同子目录，所以可以通过路径访问到其他用户下的文件。</p><p><strong>绝对路径</strong>： 从根开始给出路径上的目录直到目录文件。</p><p><strong>相对路径</strong>： 从当前目录开始定义路径。</p><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/3031bef38b63472bae95b82e4beffb24?from=pc" alt="操作系统基础48-目录结构"></p><p>树形结构目录</p><p><strong>优点</strong>： 易于管理，搜索高效。</p><p><strong>缺点</strong>： 多用户对同一文件的共享问题。</p><h1 id="6-无环图目录"><a href="#6-无环图目录" class="headerlink" title="6. 无环图目录"></a>6. 无环图目录</h1><p>运行目录含有共享子目录和文件，共享文件不用于文件的复制，对于一个共享文件，只存在一个真正的文件。当多用户在一个组工作时，只需要把共享文件目录设为每个用户目录下的子目录就可完成分组共享。</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/e3e09ac79bc04aceaacbe75c6aac67a8?from=pc" alt="操作系统基础48-目录结构"></p><p>无环图目录</p><p><strong>优点</strong>： 实现了文件共享</p><p><strong>缺点</strong>：</p><p>同一文件有多个绝对路径，遍历时重复计数。</p><p>删除共享文件时会留下悬空指针。</p><p><strong>解决：</strong></p><p><strong>提出符号链接</strong>： 又称<strong>软链接</strong>概念，即指向共享目录的指针，用绝对路径名实现，通过路径名定位真正的文件获得解析，与共享文件唯一的绝对路径区别。即它存的不是真正的文件节点，而是指向真正文件节点的指针。即指向指针的指针。</p><p><strong>引入链接概念</strong>后，当实际文件被删除后，链接保留，当用户访问链接访问文件时再告知用户链接失效。Windows,Unix都采用这种方式。</p><h1 id="7-通用图目录"><a href="#7-通用图目录" class="headerlink" title="7. 通用图目录"></a>7. 通用图目录</h1><p>当目录中允许环存在时，存在重复遍历问题和删除时因为自身引用，导致引用计数无法为0而永远无法被删除的问题。</p><p><strong>解决方案</strong>：</p><p>在无限循环的遍历中设置最大遍历次数，如果超出则自动跳出。</p><p>遍历两遍，第一遍标记所有可以访问到的空间，第二遍将没有访问到的空间加入空闲空间链表。</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/ed86b8606eb64e29ac8ea9e035da7281?from=pc" alt="操作系统基础48-目录结构"></p><p>通用图目录</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础48-目录结构&quot;&gt;&lt;a href=&quot;#操作系统基础48-目录结构&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础48-目录结构&quot;&gt;&lt;/a&gt;操作系统基础48-目录结构&lt;/h1&gt;&lt;p&gt;2021-01-28 02:06·&lt;a href=&quot;</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础47-文件访问方法</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8047-%E6%96%87%E4%BB%B6%E8%AE%BF%E9%97%AE%E6%96%B9%E6%B3%95/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8047-%E6%96%87%E4%BB%B6%E8%AE%BF%E9%97%AE%E6%96%B9%E6%B3%95/</id>
    <published>2021-08-25T03:12:32.000Z</published>
    <updated>2021-08-25T03:13:18.096Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础47-文件访问方法"><a href="#操作系统基础47-文件访问方法" class="headerlink" title="操作系统基础47-文件访问方法"></a>操作系统基础47-文件访问方法</h1><p>2021-01-27 01:58·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><p>文件存储信息。当使用时，必须访问这种信息，并将其读到计算机内存。文件信息可按多种方式来访问。</p><h1 id="顺序访问-sequential-access"><a href="#顺序访问-sequential-access" class="headerlink" title="顺序访问(sequential access)"></a>顺序访问(sequential access)</h1><p>顺序访问就是打开文件后，一个字节一个字节的读，一个20Kb的文件，必须先读完前10Kb（即使不做任何操作），才能访问到第11Kb。</p><p>顺序访问是通过一个<strong>指针</strong>操作的，指针可以按顺序移动（有的系统允许向前或者向后跳过n个记录），也可以重新设置到开始位置(reset)</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/00e6a83df82c4dd483cfef5ed2b67baf?from=pc" alt="操作系统基础47-文件访问方法"></p><p>顺序访问</p><p>大多数操作系统按顺序访问文件。 换句话说，大多数文件需要由操作系统按顺序访问。</p><p>现代文字系统确实提供了直接访问和索引访问的概念，但最常用的方法是<strong><em>顺序访问</em></strong>，因为大多数文件(如文本文件，音频文件，视频文件等)需要按顺序访问。</p><h1 id="直接访问-direct-access"><a href="#直接访问-direct-access" class="headerlink" title="直接访问(direct access)"></a>直接访问(direct access)</h1><p><strong><em>直接访问(direct access)</em></strong>也叫<strong><em>相对访问(relative access)</em></strong>，其原理是基于磁盘的特性的，磁盘能够随时访问其任意位置。</p><p>支持直接访问的文件中，文件由固定长度的逻辑记录组成，通过这种逻辑记录（如每一个块的编号，记录等）能够做到访问文件的任意位置。</p><p>直接访问可以立即访问大量信息，所以极为有用。</p><p>在数据库系统中，直接访问通常是必需的。 在大多数情况下，需要从数据库中过滤信息。 在这种情况下，顺序访问可能非常慢并且效率低下。</p><p>假设每个存储块存储4条记录，并且知道需要的记录存储在第10个块中。在这种情况下，顺序访问将不会执行，因为它将遍历所有块以访问所需的记录。</p><p>直接访问将提供所需的结果，尽管操作系统必须执行一些复杂的任务，例如确定所需的块号。 但是，这通常在数据库应用程序中实现。</p><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/82149ce3b1704ffd97e9098c2b5f9fcd?from=pc" alt="操作系统基础47-文件访问方法"></p><p>直接访问</p><h1 id="其他方式"><a href="#其他方式" class="headerlink" title="其他方式"></a>其他方式</h1><p>其他访问方法可以建立在直接访问方式之上。这些方法通常涉及创建文件索引。索引(index)，如书后的索引，包括各块的指针。通过创建文件索引，实现更快捷的文件访问，还有创建二级索引的索引方式。</p><p>如果文件可以在任何字段上排序，那么可以将索引分配给一组特定的记录。 但是，特定的记录可以通过其索引来访问。索引只不过是文件中记录的地址。</p><p>在索引访问中，在大型数据库中搜索变得非常快捷，但需要在内存中留出一些额外的空间来存储索引值。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础47-文件访问方法&quot;&gt;&lt;a href=&quot;#操作系统基础47-文件访问方法&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础47-文件访问方法&quot;&gt;&lt;/a&gt;操作系统基础47-文件访问方法&lt;/h1&gt;&lt;p&gt;2021-01-27 01:58·&lt;</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>操作系统基础46-文件系统</title>
    <link href="http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8046-%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F/"/>
    <id>http://www.shankusu.me/os/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%9F%BA%E7%A1%8046-%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F/</id>
    <published>2021-08-25T03:11:07.000Z</published>
    <updated>2021-08-25T03:12:05.954Z</updated>
    
    <content type="html"><![CDATA[<h1 id="操作系统基础46-文件系统"><a href="#操作系统基础46-文件系统" class="headerlink" title="操作系统基础46-文件系统"></a>操作系统基础46-文件系统</h1><p>2021-01-26 01:05·<a href="https://www.toutiao.com/c/user/token/MS4wLjABAAAAQN9l7B0O7YxEym7_bMZQaTheh3CQz_W6SC-j3N36vPo/?source=tuwen_detail" rel="external nofollow noopener noreferrer" target="_blank">重学IT的老猫</a></p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/d9d47449d6ba4cd289f2cd1bac9a8af8?from=pc" alt="操作系统基础46-文件系统"></p><p>计算机可以在各种存储介质（诸如磁盘、磁带和光盘）上存储信息。为了方便使用计算机系统，操作系统提供了信息存储的统一逻辑视图。操作系统对存储设备的物理属性加以抽象，从而定义逻辑存储单位，即文件(<strong>file</strong>)。文件由操作系统映射到物理设备上。这些存储设备通常是非易失性的，因此在系统重新启动之间内容可以持久。</p><p>文件是记录在外存上的相关信息的命名组合。从用户角度来看，文件是逻辑外存的最小分配单元，也就是说，数据只有通过文件才能写到外存。<br>通常，文件表示程序（源形式和目标形式）和数据。数据文件可以是数字的、字符的、字符数字的或二进制的。文件可以是自由形式的，例如文本文件，或者可以是具有严格格式的。通常，文件为位、字节、行或记录的序列，其含义由文件的创建者和用户定义。因此，文件概念非常通用。<br>文件信息由创建者定义。文件可存储许多不同类型的信息，如源程序或可执行程序、数字或文本数据、照片、音乐、视频等。文件具有某种定义的结构，这取决于其类型。比如：</p><p>文本文件为按行（可能还有页）组织的字符序列；</p><p>源文件为函数序列，而每个函数包括声明和可执行语句；</p><p>可执行文件为一系列代码段，以供加载程序调入内存并执行。</p><p><img src="https://p3-tt.byteimg.com/origin/dfic-imagehandler/4ff507ae-ef7d-4c17-b203-75d48a3a8c62?from=pc" alt="操作系统基础46-文件系统"></p><h1 id="文件属性"><a href="#文件属性" class="headerlink" title="文件属性"></a>文件属性</h1><p>文件被命名以方便用户，并且通过名称可以引用。名称通常为字符串，例如 example.c。有的系统区分名称内的大小写字符，而其他系统则不区分。当文件被命名后，它就独立于进程、用户，甚至创建它的系统。<br>例如，一个用户可能创建文件example.c，而另一个用户可能通过这个名称来编辑它。文件所有者可能会将文件写入USB盘，或作为email 附件发送，或复制到网络上并且在目标系统上仍可称为example.c。<br>文件的属性因操作系统而异，但通常包括：</p><p>名称：符号文件名是以人类可读形式来保存的唯一信息。</p><p>标识符：这种唯一标记（通常为数字）标识文件系统的文件，它是文件的非人类可读的名称。</p><p>类型：支持不同类型文件的系统需要这种信息。</p><p>位置：该信息为指向设备与设备上文件位置的指针。</p><p>尺寸：该属性包括文件的当前大小（以字节、字或块为单位）以及可能允许的最大尺寸。</p><p>保护：访问控制信息确定谁能进行读取、写入、执行等。</p><p>时间、日期和用户标识：文件创建、最后修改和最后使用的相关信息可以保存。这些数据用于保护、安全和使用监控。<br>有些较新的文件系统还支持扩展文件属性，包括文件的字符编码和安全功能，如文件校验和。<br>所有文件的信息保存在目录结构中，该目录结构也保存在外存上。通常，目录条目由文件的名称及其唯一标识符组成。根据标识符可定位其他文件属性。记录每个文件的这些信息可能超过 1KB 字节。在具有许多文件的系统中，目录本身的大小可能有数兆字节。由于目录（如文件）必须是非易失性的，因此必须存在设备上，并根据需要而被调入内存。</p><h1 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h1><p>文件为抽象数据类型。为了正确定义文件，需要考虑可以对文件执行的操作。操作系统可以提供系统调用，来创建、写入、读取、重新定位、删除及截断文件。<br>下面讨论操作系统如何执行这 6个基本文件操作：</p><ol><li>创建文件：创建文件需要两个步骤。首先，必须在文件系统中为文件找到空间；其次，必须在目录中创建新文件的条目。</li><li>写文件：为了写文件，使用一个系统调用指定文件名称和要写入文件的信息。根据给定的文件名称，系统搜索目录以查找文件位置。系统应保留写指针（write pointer），用于指向需要进行下次写操作的文件位置。每当发生写操作时，写指针必须被更新。</li><li>读文件：为了读文件，使用一个系统调用，指明文件名称和需要文件的下一个块应该放在哪里（在内存中）。同样，搜索目录以找到相关条目，系统需要保留一个读指针，指向要进行下一次读取操作的文件位置。一旦发生了读取，读指针必须被更新。因为进程通常从文件读取或写到文件，所以当前操作位置可以作为进程的当前文件位置指针。读和写操作都使用相同的指针，可节省空间并降低系统复杂性。</li><li>重新定位文件：搜索目录以寻找适当的条目，并且将当前文件位置指针重新定位到给定值。重新定位文件不需要涉及任何实际的 I/O。这个文件操作也称为文件定位。</li><li>删除文件：为了删除文件，在目录中搜索给定名称的文件。找到关联的目录条目后，释放所有文件空间，以便它可以被其他文件重复使用，并删除目录条目。</li><li>截断文件：用户可能想要删除文件的内容，但保留它的属性。不是强制用户删除文件再创建文件，这个功能允许所有属性保持不变（除了文件长度），但让文件重置为零，并释放它的文件空间。</li></ol><p>这6个基本操作组成了所需文件操作的最小集合。其他常见操作包括：将新信息附加到现有文件的末尾和重命名现有文件。然后，这些基本操作可以组合起来实现其他文件操作。<br>例如，创建一个文件副本，或复制文件到另一 I/O 设备，如打印机或显示器，可以这样来完成：创建一个新文件，从旧文件读入并写出到新文件。还希望有文件操作，以用于获取和设置文件的各种属性的操作。例如，可能需要操作以允许用户确定文件状态，如文件长度；设置文件属性，如文件所有者等。<br>以上提及的大多数文件操作涉及搜索目录，以得到命名文件的相关条目。为了避免这种不断的搜索，许多系统要求，在首次使用文件之前进行系统调用 open()。操作系统有一个打开文件表以用于维护所有打开文件的信息。当请求文件操作时，可通过该表的索引指定文件，而不需要搜索。当文件最近不再使用时，进程关闭它，操作系统从打开文件表中删除它的条目。系统调用 create() 和 delete() 是针对关闭文件而不是打开文件而进行操作的。<br>有的系统在首次引用文件时，会隐式地打开它。当打开文件的作业或程序终止时，将自动关闭它。然而，大多数操作系统要求，程序员在使用文件之前通过系统调用 open() 显式地打开它。操作 open() 根据文件名搜索目录，以将目录条目复制到打开文件表。<br>调用 open() 也会接受访问模式信息，如创建、只读、读写、只附加等。根据文件权限，检查这种模式。如果允许请求模式，则会为进程打开文件。系统调用 open() 通常返回一个指针，以指向打开文件表的对应条目。这个指针，而不是实际的文件名，会用于所有I/O操作，以避免任何进一步搜索，并简化系统调用接口。<br>对于多个进程可以同时打开文件的环境，操作open() 和 close() 的实现更加复杂。在多个不同的应用程序同时打开同一个文件的系统中，这可能发生。通常，操作系统采用两级的内部表：每个进程表和整个系统表。每个进程表跟踪它打开的所有文件。该表所存的是进程对文件的使用信息。例如，每个文件的当前文件指针就存在这里，文件访问权限和记账信息也存在这里。<br>单个进程表的每个条目相应地指向整个系统的打开文件表。系统表包含与进程无关的信息，如文件在磁盘上的位置、访问日期和文件大小。一旦有进程打开了一个文件，系统表就包含该文件的条目。当另一个进程执行调用open()，只要简单地在其进程打开表中增加一个条目，并指向系统表的相应条目。通常，系统打开文件表为每个文件关联一个打开计数，用于表示多少进程打开了这个文件。每次close() 递减打开计数；当打开计数为0时，表示不再使用该文件，并且可从系统打开文件表中删除这个文件条目。<br>总而言之，每个打开文件具有如下关联信息：</p><p>文件指针：对于没有将文件偏移作为系统调用 read() 和 write() 参数的系统，系统必须跟踪上次读写位置，以作为当前文件位置指针。该指针对操作文件的每个进程是唯一的，因此必须与磁盘文件属性分开保存。</p><p>文件打开计数：当文件关闭时，操作系统必须重新使用它的打开文件表的条目，否则表的空间会不够用。多个进程可能打开同一文件，这样在删除它的打开文件表条目之前，系统必须等待最后一个进程关闭这个文件。文件打开计数跟踪打开和关闭的次数，在最后关闭时为0。然后，系统可以删除这个条目。</p><p>文件的磁盘位置：大多数文件操作要求系统修改文件数据。查找磁盘上的文件所需的信息保存在内存中，以便系统不必为每个操作从磁盘上读取该信息。</p><p>访问权限：每个进程采用访问模式打开文件。这种信息保存在进程的打开文件表中，因此操作系统可以允许或拒绝后续的I/O请求。<br>有的操作系统提供功能，用于锁定打开的文件（或文件的部分）。文件锁允许一个进程锁定文件，以防止其他进程访问它。文件锁对于多个进程共享的文件很有用，例如，系统中的多个进程可以访问和修改的系统日志文件。<br>文件锁提供类似于读者一写者锁。共享锁类似于读者锁，以便多个进程可以并发获取它。独占锁类似于写者锁，一次只有一个进程可以获取这样的锁。重要的是要注意，并非所有操作系统都提供两种类型的锁，有些系统仅提供独占文件锁。<br>另外，操作系统可以提供强制或建议文件锁定机制。如果锁是强制性的，则一旦进程获取独占锁，操作系统就阻止任何其他进程访问锁定的文件。<br>例如，假设有一个进程获取了文件 system.log 的独占锁。如果另一进程（如文本编辑器）尝试打开 system.log，则操作系统将阻止访问，直到独占锁被释放。即使文本编辑器并没明确地获取锁，也会发生这种情况。或者，如果锁是建议性的，则操作系统不会阻止文本编辑器获取对 system.log 的访问。相反，必须编写文本编辑器，以便在访问文件之前手动获取锁。<br>换句话说，如果锁定方案是强制性的，则操作系统确保锁定完整性。对于建议锁定，软件开发人员应确保适当地获取和释放锁。作为一般规则，Windows操作系统采用强制锁定，UNIX系统采用建议锁定。<br>使用文件锁定，与普通进程同步一样，还是需要谨慎的。例如，程序员在具有强制锁定的系统上开发时，应小心地确保只有在访问文件时才锁定独占文件。否则，他们将阻止其他进程也对文件进行访问。此外，必须采取一些措施，来确保两个或更多进程在尝试获取文件锁时不会卷入死锁。</p><h1 id="文件类型"><a href="#文件类型" class="headerlink" title="文件类型"></a>文件类型</h1><p>当设计文件系统（甚至整个操作系统）时，总是需要考虑操作系统是否应该识别和支持文件类型。如果操作系统识别文件的类型，则它就能按合理的方式来操作文件。<br>例如，一个经常发生的错误就是，用户尝试输出二进制目标形式的一个程序。这种尝试通常会产生垃圾；然而，如果操作系统已得知一个文件是二进制目标程序，则尝试可以成功。<br>实现文件类型的常见技术是将类型作为文件名的一部分。文件名分为两部分，即名称和扩展，通常由句点分开（表 1）。这样，用户和操作系统仅从文件名就能得知文件的类型。大多数操作系统允许用户将文件名命名为字符序列，后跟一个句点，再以由附加字符组成的 扩展名结束，示例包括 resume.docx、server.c 和 ReaderTliread.cpp。</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/ae3a8b7f29e6415297dc0c964254b46e?from=pc" alt="操作系统基础46-文件系统"></p><p>操作系统使用扩展名来指示文件类型和可用于文件的操作类型。例如，只有扩展名为 .com、.exe 或 .sli 的文件才能执行。.com 和 .exe 文件是两种形式的二进制可执行文件，而 .sh 文件是外壳脚本，包含 ASCII 格式的操作系统命令。<br>应用程序也使用扩展名来表示所感兴趣的文件类型。例如，Java 编译器的源文件具有 .java 扩展名，Microsoft Word 字处理程序的文件以 .doc 或 .docx 扩展名来结束。这些扩展名不总是必需的，因此用户可以不用扩展名（节省打字）来指明文件，应用程序根据给定的名称和预期的扩展名来查找文件。因为这些扩展名没有操作系统的支持，所以它们只能作为应用程序的“提示”。<br>下面考虑 Mac OS X 操作系统。这个系统的每个文件都有类型，例如 .app (用于应用程序）。每个文件还有一个创建者属性，用来包含创建它的程序名称。这种属性是由操作系统在调用 create() 时设置的，因此其使用由系统强制和支持。<br>例如，由字处理器创建的文件采用字处理器名称作为创建者。当用户通过在表示文件的图标上双击鼠标以打开文件，就会自动调用字处理器，并加载文件以便编辑。<br>UNIX 系统采用位于某些文件开始部分的<strong>幻数</strong>，大致表明文件类型，如可执行程序、shell 脚本、PDF 文件等。不是所有文件都有幻数，因此系统特征不能仅仅基于这种信息。UNIX 也不记录创建程序的名称。UNIX允许文件名扩展提示，但是操作系统既不强制也不依赖这些扩展名；这些扩展名主要帮助用户确定文件内容的类型。扩展名可以由给定的应用程序采用或忽略，但这是由应用程序的开发者所决定的。</p><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/fbaf62f36a744d92a74116cc983be321?from=pc" alt="操作系统基础46-文件系统"></p><h1 id="文件结构"><a href="#文件结构" class="headerlink" title="文件结构"></a>文件结构</h1><p>文件类型也可用于指示文件的内部结构。我们知道，源文件和目标文件具有一定结构，以便匹配读取它们的程序的期望。此外，有些文件必须符合操作系统理解的所需结构。<br>例如，操作系统要求可执行文件具有特定的结构，以便可以确定将文件加载到内存的哪里以及第一条指令的位置是什么。有些操作系统将这种想法扩展到系统支持的一组文件结构，以便采用特殊操作来处理具有这些结构的文件。<br>让操作系统支持多个文件结构带来一个缺点，操作系统会变得太复杂。如果操作系统定义了5个不同的文件结构，则它需要包含代码，以便支持这些文件结构。此外，可能需要将每个文件定义为操作系统支持的文件类型之一。如果新应用程序需要按操作系统不支持的方式来组织信息，则可能导致严重的问题。<br>例如，假设有个系统支持两种类型的文件：文本文件（由回车符和换行符分隔的 ASCII 字符组成）和可执行的二进制文件。现在，如果我们（作为用户）想要定义一个加密的文件，以保护内容不被未经授权的人读取，则我们可能会发现两种文件类型都不合适。加密文件不是 ASCII 文本行，而是（看起来）随机位。虽然加密文件看起来是二进制文件，但是它不是可执行的。因此，我们可能要么必须绕过或滥用操作系统文件类型机制，要么放弃加密方案。<br>有些操作系统强加（并支持）最小数量的文件结构。UNIX、Windows 等都采用这种方案。UNIX 认为每个文件为 8 位字节序列，而操作系统并不对这些位做出解释。这种方案提供了最大的灵活性，但是支持的也很少。每个应用程序必须包含自己的代码，以便按适当结构来解释输入文件。但是，所有操作系统必须支持至少一种结构，即可执行文件的结构，以便系统能够加载和运行程序。</p><h1 id="内部文件结构"><a href="#内部文件结构" class="headerlink" title="内部文件结构"></a>内部文件结构</h1><p>在内部，定位文件的偏移对操作系统来说可能是比较复杂的。磁盘系统通常具有明确定义的块大小，这是由扇区大小决定的。所有磁盘 I/O 按块（物理记录）为单位执行，而所有块的大小相同。物理记录大小不太可能刚好匹配期望的逻辑记录的长度。逻辑记录的长度甚至可能不同。这个问题的常见解决方案是，将多个逻辑记录包装到物理块中。<br>例如，UNIX 操作系统将所有文件定义为简单的字节流。每个字节可以通过距文件的开始（或结束）的偏移来单独寻址。在这种情况下，逻辑记录大小为 1 字节。根据需要，文件系统通常会自动将字节打包以存入物理磁盘块，或从磁盘块中解包得到字节（每块可为 512 字节）。<br>逻辑记录大小、物理块大小和打包技术确定了每个物理块可有多少逻辑记录。打包可以通过用户应用程序或操作系统来完成。不管如何，文件都可当作块的序列。所有基本 I/O 功能都以块为单位来进行。从逻辑记录到物理块的转换是个相对简单的软件问题。<br>由于磁盘空间总是按块为单位来分配的，因此每个文件的最后一块的某些部分通常会被浪费。例如，如果每个块是 512 字节，则 1949 字节的文件将分得 4 个块（2048 字节），最后 99 字节就浪费了。按块（而不是字节）为单位来保持一切而浪费的字节称为内部碎片。所有文件系统都有内部碎片，块大小越大，内部碎片也越大。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;操作系统基础46-文件系统&quot;&gt;&lt;a href=&quot;#操作系统基础46-文件系统&quot; class=&quot;headerlink&quot; title=&quot;操作系统基础46-文件系统&quot;&gt;&lt;/a&gt;操作系统基础46-文件系统&lt;/h1&gt;&lt;p&gt;2021-01-26 01:05·&lt;a href=&quot;</summary>
      
    
    
    
    <category term="os" scheme="http://www.shankusu.me/categories/os/"/>
    
    
    <category term="os" scheme="http://www.shankusu.me/tags/os/"/>
    
  </entry>
  
</feed>
