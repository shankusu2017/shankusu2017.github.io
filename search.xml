<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>setjmp和longjmp函数使用详解</title>
    <url>/c/setjmp%E5%92%8Clongjmp%E5%87%BD%E6%95%B0%E4%BD%BF%E7%94%A8%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/chenyiming_1990/article/details/8683413" rel="external nofollow noopener noreferrer" target="_blank">转载自大米粒的blog</a></p>
<p>在网上看到的，觉得很有用，copy过来的。</p>
<p>非局部跳转语句—setjmp和longjmp函数。非局部指的是，这不是由普通C语言goto，语句在一个函数内实施的跳转，而是在栈上跳过若干调用帧，返回到当前函数调用路径上的某一个函数中。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;setjmp.h&gt;</span></span></span><br><span class="line"><span class="function">Int <span class="title">setjmp</span><span class="params">(jmp_buf  env)</span></span>;</span><br><span class="line">       返回值：若直接调用则返回<span class="number">0</span>，若从longjmp调用返回则返回非<span class="number">0</span>值的longjmp中的val值</span><br><span class="line"><span class="function">Void <span class="title">longjmp</span><span class="params">(jmp_buf env,<span class="keyword">int</span> val)</span></span>;</span><br><span class="line">        调用此函数则返回到语句setjmp所在的地方，其中env 就是setjmp中的 env，而val 则是使setjmp的返回值变为val。</span><br><span class="line">        当检查到一个错误时,则以两个参数调用longjmp函数，第一个就是在调用setjmp时所用的env，第二个参数是具有非<span class="number">0</span>值的val，它将成为从setjmp处返回的值。使用第二个参数的原因是对于一个setjmp可以有多个longjmp。</span><br></pre></td></tr></table></figure></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;setjmp.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> jmp_buf buf;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">second</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"second\n"</span>);         <span class="comment">// 打印</span></span><br><span class="line">    longjmp(buf,<span class="number">1</span>);             <span class="comment">// 跳回setjmp的调用处 - 使得setjmp返回值为1</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">first</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    second();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"first\n"</span>);          <span class="comment">// 不可能执行到此行</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;   </span><br><span class="line">    <span class="keyword">if</span> ( ! setjmp(buf) ) &#123;</span><br><span class="line">        first();                <span class="comment">// 进入此行前，setjmp返回0</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;                    <span class="comment">// 当longjmp跳转回，setjmp返回1，因此进入此行</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"main\n"</span>);       <span class="comment">// 打印</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上述程序将输出:<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">second</span><br><span class="line">main</span><br></pre></td></tr></table></figure></p>
<p>注意到虽然first()子程序被调用，”first”不可能被打印。”main”被打印，因为条件语句if ( ! setjmp(buf) )被执行第二次。　</p>
<p>使用setjmp和longjmp要注意以下几点：</p>
<ol>
<li><p>setjmp与longjmp结合使用时，它们必须有严格的先后执行顺序，也即先调用setjmp函数，之后再调用longjmp函数，以恢复到先前被保存的“程序执行点”。否则，如果在setjmp调用之前，执行longjmp函数，将导致程序的执行流变的不可预测，很容易导致程序崩溃而退出</p>
</li>
<li><p>longjmp必须在setjmp调用之后，而且longjmp必须在setjmp的作用域之内。具体来说，在一个函数中使用setjmp来初始化一个全局标号，然后只要该函数未曾返回，那么在其它任何地方都可以通过longjmp调用来跳转到 setjmp的下一条语句执行。实际上setjmp函数将发生调用处的局部环境保存在了一个jmp_buf的结构当中，只要主调函数中对应的内存未曾释放 （函数返回时局部内存就失效了），那么在调用longjmp的时候就可以根据已保存的jmp_buf参数恢复到setjmp的地方执行。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>c</category>
      </categories>
      <tags>
        <tag>c</tag>
      </tags>
  </entry>
  <entry>
    <title>Goroutine 的切换过程涉及了什么</title>
    <url>/go/Goroutine%20%E7%9A%84%E5%88%87%E6%8D%A2%E8%BF%87%E7%A8%8B%E6%B6%89%E5%8F%8A%E4%BA%86%E4%BB%80%E4%B9%88/</url>
    <content><![CDATA[<p>GCTT:anxk Go语言中文网 <a href="https://mp.weixin.qq.com/s/r0y4Fweq-YGo1FZrsNsl3A" rel="external nofollow noopener noreferrer" target="_blank"></a></p>
<p>点击上方蓝色“Go语言中文网”关注，每天一起学 Go</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e0dda0b377c4e8f6c613e523208ae875.png" alt></p>
<p>Illustration created for “A Journey With Go”, made from the original Go Gopher, created by Renee French.</p>
<p>本文基于 Go 1.13 版本。</p>
<p>Goroutine 很轻，它只需要 2Kb 的内存堆栈即可运行。另外，它们运行起来也很廉价，将一个 Goroutine 切换到另一个的过程不牵涉到很多的操作。在深入 Goroutine 切换过程之前，让我们回顾一下 Goroutine 的切换在更高的层次上是如何进行的。</p>
<p>在继续阅读本文之前，我强烈建议您阅读我的文章 Go：Goroutine、操作系统线程和 CPU 管理[1] 以了解本文中涉及的一些概念。</p>
<p><strong>案例</strong><br>Go 根据两种断点将 Goroutine 调度到线程上：</p>
<ul>
<li><p>当 Goroutine 因为系统调用、互斥锁或通道而被阻塞时，goroutine 将进入睡眠模式（等待队列），并允许 Go 调度运行另一个处于就绪状态的 goroutine；</p>
</li>
<li><p>在函数调用时，如果 Goroutine 必须增加其堆栈，这会使 Go 调度另一个 Goroutine 以避免运行中的 Goroutine 独占 CPU 时间片；</p>
</li>
</ul>
<p>在这两种情况下，运行调度程序的 g0 会替换当前的 goroutine，然后选出下一个将要运行的 Goroutine 替换 g0 并在线程上运行。</p>
<p>有关 g0 的更多信息，建议您阅读我的文章 Go：特殊的 Goroutine g0[2]。</p>
<p>将一个运行中的 Goroutine 切换到另一个的过程涉及到两个切换：</p>
<ul>
<li>将运行中的 g 切换到 g0：</li>
</ul>
<p>!()[<a href="https://img-blog.csdnimg.cn/img_convert/8e94cc30b8ff144c738b4d9848091bd9.png]" rel="external nofollow noopener noreferrer" target="_blank">https://img-blog.csdnimg.cn/img_convert/8e94cc30b8ff144c738b4d9848091bd9.png]</a></p>
<p>将 g0 切换到下一个将要运行的 g：</p>
<p>图片</p>
<p>在 Go 中，goroutine 的切换相当轻便，其中需要保存的状态仅仅涉及以下两个：</p>
<ul>
<li><p>Goroutine 在停止运行前执行的指令，程序当前要运行的指令是记录在程序计数器（PC）中的， Goroutine 稍后将在同一指令处恢复运行；</p>
</li>
<li><p>Goroutine 的堆栈，以便在再次运行时还原局部变量；</p>
</li>
</ul>
<p>让我们看看实际情况下的切换是怎样进行的。</p>
<p>程序计数器<br>这里通过基于通道的 生产者/消费者模式 来举例说明，其中一个 Goroutine 产生数据，而另一些则消费数据，代码如下：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e5ca784ed123f7edb816f3dd444170d0.png" alt></p>
<p>消费者仅仅是打印从 0 到 99 的偶数。我们将注意力放在第一个 goroutine（生产者）上，它将数字添加到缓冲区。当缓冲区已满时，它将在发送消息时被阻塞。此时，Go 必须切换到 g0 并调度另一个 Goroutine 来运行。</p>
<p>如前所述，Go 首先需要保存当前执行的指令，以便稍后在同一条指令上恢复 goroutine。程序计数器（PC）保存在 Goroutine 的内部结构中：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d018b1dc7653ebef6eda3d3157522e8f.png" alt></p>
<p>可以通过 go tool objdump 命令找到对应的指令及其地址，这是生产者的指令：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bd6a303906090466c9298bee866585d7.png" alt></p>
<p>程序逐条指令的执行直到在函数 runtime.chansend1 处阻塞在通道上。Go 将当前程序计数器保存到当前 Goroutine 的内部属性中。在我们的示例中，Go 使用运行时的内部地址 0x4268d0 和方法 runtime.chansend1 保存程序计数器：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/acceb0417b7aa55a41badb6c7bf9ea2f.png" alt></p>
<p>然后，当 g0 唤醒 Goroutine 时，它将在同一指令处继续执行，继续将数值循环的推入通道。现在，让我们将视线移到 Goroutine 切换期间堆栈的管理。</p>
<h1 id="堆栈"><a href="#堆栈" class="headerlink" title="堆栈"></a>堆栈</h1><p>在被阻塞之前，正在运行的 Goroutine 具有其原始堆栈，该堆栈包含临时存储器，例如变量 i：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/490db6ac002dc4e2cb195ea50c646e6c.png" alt></p>
<p>然后，当它在通道上阻塞时，goroutine 将切换到 g0 及其堆栈（更大的堆栈）：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/fab4f27775dd95c2fc407802237abbcc.png" alt></p>
<p>在切换之前，堆栈将被保存，以便在 Goroutine 再次运行时进行恢复：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0ee6bf2eace5edc2cbaa7bbf886a6e4b.png" alt></p>
<p>现在，我们对 Goroutine 切换中涉及的不同操作有了一个完整的了解，让我们继续看看它是如何影响性能的。</p>
<p>我们应该注意，诸如 arm 等 CPU 架构需要再保存一个寄存器，即 LR 链接寄存器。</p>
<p><strong>性能</strong><br>我们仍然使用上述的程序来测量一次切换所需的时间。但是，由于切换时间取决于寻找下一个要调度的 Goroutine 所花费的时间，因此无法提供完美的性能视图。在函数调用情况下进行的切换要比阻塞在通道上的切换执行更多的操作，这也会影响到性能。</p>
<p>让我们总结一下我们将要测量的操作：</p>
<ul>
<li><p>当前 g 阻塞在通道上并切换到 g0：</p>
<ul>
<li><p>PC 和堆栈指针一起保存在内部结构中</p>
</li>
<li><p>将 g0 设置为正在运行的 goroutine</p>
</li>
<li><p>g0 的堆栈替换当前堆栈</p>
</li>
</ul>
</li>
<li><p>g0 寻找新的 Goroutine 来运行；</p>
</li>
<li><p>g0 使用所选的 Goroutine 进行切换：</p>
<ul>
<li><p>PC 和堆栈指针是从其内部结构中获取的</p>
</li>
<li><p>程序跳转到对应的 PC 地址</p>
</li>
</ul>
</li>
</ul>
<p>结果如下：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c315e119d28ab19560cfe71241da0ea2.png" alt></p>
<p>从 g 到 g0 或从 g0 到 g 的切换是相当迅速的，它们只包含少量固定的指令。相反，对于调度阶段，调度程序需要检查许多资源以便确定下一个要运行的 goroutine，根据程序的不同，此阶段可能会花费更多的时间。</p>
<h2 id="该基准测试给出了性能的数量级估计，由于没有标准的工具可以衡量它，所以我们并不能完全依赖于这个结果。此外，性能也取决于-CPU-架构、机器（本文使用的机器是-Mac-2-9-GHz-双核-Intel-Core-i5）以及正在运行的程序。"><a href="#该基准测试给出了性能的数量级估计，由于没有标准的工具可以衡量它，所以我们并不能完全依赖于这个结果。此外，性能也取决于-CPU-架构、机器（本文使用的机器是-Mac-2-9-GHz-双核-Intel-Core-i5）以及正在运行的程序。" class="headerlink" title="该基准测试给出了性能的数量级估计，由于没有标准的工具可以衡量它，所以我们并不能完全依赖于这个结果。此外，性能也取决于 CPU 架构、机器（本文使用的机器是 Mac 2.9 GHz 双核 Intel Core i5）以及正在运行的程序。"></a>该基准测试给出了性能的数量级估计，由于没有标准的工具可以衡量它，所以我们并不能完全依赖于这个结果。此外，性能也取决于 CPU 架构、机器（本文使用的机器是 Mac 2.9 GHz 双核 Intel Core i5）以及正在运行的程序。</h2><p>via: <a href="https://medium.com/a-journey-with-go/go-what-does-a-goroutine-switch-actually-involve-394c202dddb7" rel="external nofollow noopener noreferrer" target="_blank">https://medium.com/a-journey-with-go/go-what-does-a-goroutine-switch-actually-involve-394c202dddb7</a></p>
<p>作者：Vincent Blanchon[3]译者：anxk[4]校对：polaris1119[5]</p>
<p>本文由 GCTT[6] 原创编译，Go 中文网[7] 荣誉推出</p>
<p>参考资料<br>Go：Goroutine、操作系统线程和 CPU 管理: <a href="https://medium.com/a-journey-with-go/go-goroutine-os-thread-and-cpu-management-2f5a5eaf518a" rel="external nofollow noopener noreferrer" target="_blank">https://medium.com/a-journey-with-go/go-goroutine-os-thread-and-cpu-management-2f5a5eaf518a</a></p>
<p>Go：特殊的 Goroutine g0: <a href="https://medium.com/a-journey-with-go/go-g0-special-goroutine-8c778c6704d8" rel="external nofollow noopener noreferrer" target="_blank">https://medium.com/a-journey-with-go/go-g0-special-goroutine-8c778c6704d8</a></p>
<p>Vincent Blanchon: <a href="https://medium.com/@blanchon.vincent" rel="external nofollow noopener noreferrer" target="_blank">https://medium.com/@blanchon.vincent</a></p>
<p>anxk: <a href="https://github.com/anxk" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/anxk</a></p>
<p>polaris1119: <a href="https://github.com/polaris1119" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/polaris1119</a></p>
<p>GCTT: <a href="https://github.com/studygolang/GCTT" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/studygolang/GCTT</a></p>
<p>Go 中文网: <a href="https://studygolang.com/" rel="external nofollow noopener noreferrer" target="_blank">https://studygolang.com/</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>Go：Goroutine, OS线程 以及 CPU管理</title>
    <url>/go/Go%EF%BC%9AGoroutine,%20OS%E7%BA%BF%E7%A8%8B%20%E4%BB%A5%E5%8F%8A%20CPU%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<p><a href="https://studygolang.com/articles/24986?fr=sidebar" rel="external nofollow noopener noreferrer" target="_blank">中文翻译</a> <a href="https://medium.com/a-journey-with-go/go-goroutine-os-thread-and-cpu-management-2f5a5eaf518a" rel="external nofollow noopener noreferrer" target="_blank">英文原文</a></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/ee06766dac7ef695b03463d38ad1b32f.png" alt></p>
<p>操作系统的线程创建以及切换是需要开销的，会影响程序的性能。Go致力于尽可能地从内核中获取优势，所以从最开始的时候设计就考虑到了并发性。</p>
<p><strong>M，P，G 编排</strong><br>为了解决这个问题，Go有他自己的调度者，负责在线程上分配goroutines。这个协调者由3个概念组成，如下：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">The main concepts are:</span><br><span class="line">G - goroutine.</span><br><span class="line">M - worker thread, or machine. 工作线程或机器</span><br><span class="line">P - processor, a resource that is required to execute Go code.</span><br><span class="line">    M must have an associated P to execute Go code[...].</span><br><span class="line">    处理者，负责执行Go代码， 每个M必须有一个关联的P去执行Go代码</span><br></pre></td></tr></table></figure></p>
<p>三者关系图如下：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f60a086aadff20195f4dc0963b296b82.png" alt></p>
<p>每一个goruntine（G）运行在操作系统线程（M）上并分配一个逻辑CPU（P）。我们用一个简单的例子来看看Go是如何管理他们的：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">   <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line">   wg.Add(<span class="number">2</span>)</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">      <span class="built_in">println</span>(<span class="string">`hello`</span>)</span><br><span class="line">      wg.Done()</span><br><span class="line">   &#125;()</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">      <span class="built_in">println</span>(<span class="string">`world`</span>)</span><br><span class="line">      wg.Done()</span><br><span class="line">   &#125;()</span><br><span class="line"> </span><br><span class="line">   wg.Wait()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>Go首先会基于机器逻辑CPUs的数量来创建不同的P，并将他们储存成一个空闲的P的列表</p>
<p> <img src="https://img-blog.csdnimg.cn/img_convert/397ec9016645c87900916271790df9b0.png" alt></p>
<p>然后，当新的goroutine或者goroutine准备运行的时候会唤醒一个空闲的P，这个P会创建一个关联到操作系统线程的M</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/ec8f20a1715165fd84a44efda5dc63c2.png" alt></p>
<p>然而，当P，M不工作的，假如，没有goruntine在等待被执行的时候，就会返回一个系统调用syscall，或者甚至被垃圾回收强制停止，放回空闲P/M链表中。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/16a6b18aced1115fa17dc1ebdf1cfdeb.png" alt></p>
<p>在程序运行时候，Go已经创建了一些OS线程以及关联上M。在我们的例子中，第一个负责打印hello的goroutine会使用主goroutine， 而第二个goroutine从空闲列表中获取一个P和M</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/6608d187b237f1d9680e0746d89e11a2.png" alt></p>
<p>现在我们已经对goroutines以及线程管理有一个大概了解了，让我们看看在什么时候Go会出现M数量比P多的情况以及goroutines是如何管理这种系统调用的。</p>
<h1 id="系统调用-System-calls"><a href="#系统调用-System-calls" class="headerlink" title="系统调用 System calls"></a>系统调用 System calls</h1><p>Go通过在运行时封装系统调用来进行优化，无论阻塞与否。这个封装会自动将P与M的关联切断，然后允许第二个线程M来运行P。我们来看看下面一个读取文件例子：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">   buf := <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">0</span>, <span class="number">2</span>)</span><br><span class="line"> </span><br><span class="line">   fd, _ := os.Open(<span class="string">"number.txt"</span>)</span><br><span class="line">   fd.Read(buf)</span><br><span class="line">   fd.Close()</span><br><span class="line"> </span><br><span class="line">   <span class="built_in">println</span>(<span class="keyword">string</span>(buf)) <span class="comment">// 42</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>下面是图片演示整个执行过程</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3518552dc3e60e17d5addc31bd2781c8.png" alt></p>
<p>P0现在在空闲列表中处于可被使用状态。一旦系统调用退出时，Go遵循下面的规制直到其中一个条件满足</p>
<ul>
<li>尝试去获得一个一模一样的P，在我们的例子中就是P0，然后恢复执行</li>
<li>尝试获取空闲列表中的P，然后恢复执行</li>
<li>将goroutine到全局队列中，然后将其关联的M放回到空闲列表中<br>然而，Go同样需要处理当资源还没准备好的情况，例如HTTP请求这种非阻塞I/O。在这种情况下，第一个系统调用，同样会遵循上述规制但是不会成功，因为资源还没有准备好，这时会强迫Go使用network poller以及暂停goroutine。如下例子：<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">   http.Get(<span class="string">`https://httpstat.us/200`</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>当第一个系统调用执行完成并明确地说资源还没准备好的时候，goroutine会暂停直到network poller通知其说资源已经准备好了。在这种情况下，线程M是不会被阻塞的。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e28c7a43694ac6e05f35c769b226c534.png" alt></p>
<p>当Go协调程序重新查找待完成工作时，goroutine会被重新执行一次。这个协调者在成功获取一个他所等待的消息以后，会问network poller是否有goroutine在等待运行。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3e294b47f9c6c95263c5a3a5858d319c.png]" alt></p>
<p>如果有多于一个goroutine准备好的时候，其余的goroutine会进入全局的可执行队列中等待被执行。</p>
<h1 id="OS线程的限制-Restriction-in-term-of-OS-threads"><a href="#OS线程的限制-Restriction-in-term-of-OS-threads" class="headerlink" title="OS线程的限制 Restriction in term of OS threads"></a>OS线程的限制 Restriction in term of OS threads</h1><p>当系统调用时，Go不会限制可以阻塞的OS线程的数量，官方解释：</p>
<p>GOMAXPROCS变量限制了可以同时执行用户级Go代码的操作系统线程的数量。 对于代表Go代码的系统调用中可以阻止的线程数量没有限制； GOMAXPROCS函数可查询并更改限制。</p>
<p>这段代码解释这个情况<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">   <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">for</span> i := <span class="number">0</span>;i &lt; <span class="number">100</span> ;i++  &#123;</span><br><span class="line">      wg.Add(<span class="number">1</span>)</span><br><span class="line"> </span><br><span class="line">      <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">         http.Get(<span class="string">`https://httpstat.us/200?sleep=10000`</span>)</span><br><span class="line"> </span><br><span class="line">         wg.Done()</span><br><span class="line">      &#125;()</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   wg.Wait()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>下面是跟踪工具里面展示的线程数量</p>
<p> <img src="https://img-blog.csdnimg.cn/img_convert/8ad356871177ea1e5cd1e81efe7d21c3.png" alt></p>
<p>由于Go将线程的使用进行了优化，当goroutines被阻塞时候可以被重新利用，也就解释了为什么这个数与循环数并不匹配。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>Go：defer 语句如何工作</title>
    <url>/go/Go%EF%BC%9Adefer%20%E8%AF%AD%E5%8F%A5%E5%A6%82%E4%BD%95%E5%B7%A5%E4%BD%9C/</url>
    <content><![CDATA[<p><a href="https://studygolang.com/articles/28431" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>Go：defer 语句如何工作</p>
<p>ℹ️ 这篇文章基于 Go 1.12。</p>
<p>defer 语句是在函数返回前执行一段代码的便捷方法，如 Golang 规范所描述：</p>
<blockquote>
<p>延迟函数（ deferred functions ）在所在函数返回前，以与声明相反的顺序立即被调用</p>
</blockquote>
<p>以下是 LIFO (后进先出)实现的例子：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">   <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">      <span class="built_in">println</span>(<span class="string">`defer 1`</span>)</span><br><span class="line">   &#125;()</span><br><span class="line">   <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">      <span class="built_in">println</span>(<span class="string">`defer 2`</span>)</span><br><span class="line">   &#125;()</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">defer</span> <span class="number">2</span> &lt;- 后进先出</span><br><span class="line"><span class="keyword">defer</span> <span class="number">1</span></span><br></pre></td></tr></table></figure></p>
<p>来看一下内部的实现，然后再看一个更复杂的案例。</p>
<p><strong>内部实现</strong><br>Go 运行时（runtime）使用一个链表来实现 LIFO。实际上，一个 defer 结构体持有一个指向下一个要被执行的 defer 结构体的指针：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> _defer <span class="keyword">struct</span> &#123;</span><br><span class="line">   siz     <span class="keyword">int32</span></span><br><span class="line">   started <span class="keyword">bool</span></span><br><span class="line">   sp      <span class="keyword">uintptr</span></span><br><span class="line">   pc      <span class="keyword">uintptr</span></span><br><span class="line">   fn      *funcval</span><br><span class="line">   _panic  *_panic</span><br><span class="line">   link    *_defer <span class="comment">// 下一个要被执行的延迟函数</span></span><br></pre></td></tr></table></figure></p>
<p>当一个新的 defer 方法被创建的时候，它被附加到当前的 Goroutine 上，然后之前的 defer 方法作为下一个要执行的函数被链接到新创建的方法上：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">newdefer</span><span class="params">(siz <span class="keyword">int32</span>)</span> *_<span class="title">defer</span></span> &#123;</span><br><span class="line">   <span class="keyword">var</span> d *_defer</span><br><span class="line">   gp := getg() <span class="comment">// 获取当前 goroutine</span></span><br><span class="line">   [...]</span><br><span class="line">   <span class="comment">// 延迟列表现在被附加到新的 _defer 结构体</span></span><br><span class="line">   d.link = gp._defer</span><br><span class="line">   gp._defer = d <span class="comment">// 新的结构现在是第一个被调用的</span></span><br><span class="line">   <span class="keyword">return</span> d</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>现在，后续调用会从栈的顶部依次出栈延迟函数：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">deferreturn</span><span class="params">(arg0 <span class="keyword">uintptr</span>)</span></span> &#123;</span><br><span class="line">   gp := getg() <span class="comment">// 获取当前 goroutine</span></span><br><span class="line">   d:= gp._defer <span class="comment">// 拷贝延迟函数到一个变量上</span></span><br><span class="line">   <span class="keyword">if</span> d == <span class="literal">nil</span> &#123; <span class="comment">// 如果不存在延迟函数就直接返回</span></span><br><span class="line">      <span class="keyword">return</span></span><br><span class="line">   &#125;</span><br><span class="line">   [...]</span><br><span class="line">   fn := d.fn <span class="comment">// 获取要调用的函数</span></span><br><span class="line">   d.fn = <span class="literal">nil</span> <span class="comment">// 重置函数</span></span><br><span class="line">   gp._defer = d.link <span class="comment">// 把下一个 _defer 结构体依附到 Goroutine 上</span></span><br><span class="line">   freedefer(d) <span class="comment">// 释放 _defer 结构体</span></span><br><span class="line">   jmpdefer(fn, <span class="keyword">uintptr</span>(unsafe.Pointer(&amp;arg0))) <span class="comment">// 调用该函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>如我们所见，并没有循环地去调用延迟函数，而是一个接一个地出栈。这一行为可以通过生成汇编代码得到验证：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一个延迟函数</span></span><br><span class="line"><span class="number">0x001d</span> <span class="number">00029</span> (main.<span class="keyword">go</span>:<span class="number">6</span>)   MOVL   $<span class="number">0</span>, (SP)</span><br><span class="line"><span class="number">0x0024</span> <span class="number">00036</span> (main.<span class="keyword">go</span>:<span class="number">6</span>)   PCDATA $<span class="number">2</span>, $<span class="number">1</span></span><br><span class="line"><span class="number">0x0024</span> <span class="number">00036</span> (main.<span class="keyword">go</span>:<span class="number">6</span>)   LEAQ   <span class="string">""</span>.main.func1·f(SB), AX</span><br><span class="line"><span class="number">0x002b</span> <span class="number">00043</span> (main.<span class="keyword">go</span>:<span class="number">6</span>)   PCDATA $<span class="number">2</span>, $<span class="number">0</span></span><br><span class="line"><span class="number">0x002b</span> <span class="number">00043</span> (main.<span class="keyword">go</span>:<span class="number">6</span>)   MOVQ   AX, <span class="number">8</span>(SP)</span><br><span class="line"><span class="number">0x0030</span> <span class="number">00048</span> (main.<span class="keyword">go</span>:<span class="number">6</span>)   CALL   runtime.deferproc(SB)</span><br><span class="line"><span class="number">0x0035</span> <span class="number">00053</span> (main.<span class="keyword">go</span>:<span class="number">6</span>)   TESTL  AX, AX</span><br><span class="line"><span class="number">0x0037</span> <span class="number">00055</span> (main.<span class="keyword">go</span>:<span class="number">6</span>)   JNE    <span class="number">117</span></span><br><span class="line"><span class="comment">// 第二个延迟函数</span></span><br><span class="line"><span class="number">0x0039</span> <span class="number">00057</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  MOVL   $<span class="number">0</span>, (SP)</span><br><span class="line"><span class="number">0x0040</span> <span class="number">00064</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  PCDATA $<span class="number">2</span>, $<span class="number">1</span></span><br><span class="line"><span class="number">0x0040</span> <span class="number">00064</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  LEAQ   <span class="string">""</span>.main.func2·f(SB), AX</span><br><span class="line"><span class="number">0x0047</span> <span class="number">00071</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  PCDATA $<span class="number">2</span>, $<span class="number">0</span></span><br><span class="line"><span class="number">0x0047</span> <span class="number">00071</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  MOVQ   AX, <span class="number">8</span>(SP)</span><br><span class="line"><span class="number">0x004c</span> <span class="number">00076</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  CALL   runtime.deferproc(SB)</span><br><span class="line"><span class="number">0x0051</span> <span class="number">00081</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  TESTL  AX, AX</span><br><span class="line"><span class="number">0x0053</span> <span class="number">00083</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  JNE    <span class="number">101</span></span><br><span class="line"><span class="comment">// main 函数结束</span></span><br><span class="line"><span class="number">0x0055</span> <span class="number">00085</span> (main.<span class="keyword">go</span>:<span class="number">18</span>)  XCHGL  AX, AX</span><br><span class="line"><span class="number">0x0056</span> <span class="number">00086</span> (main.<span class="keyword">go</span>:<span class="number">18</span>)  CALL   runtime.deferreturn(SB)</span><br><span class="line"><span class="number">0x005b</span> <span class="number">00091</span> (main.<span class="keyword">go</span>:<span class="number">18</span>)  MOVQ   <span class="number">16</span>(SP), BP</span><br><span class="line"><span class="number">0x0060</span> <span class="number">00096</span> (main.<span class="keyword">go</span>:<span class="number">18</span>)  ADDQ   $<span class="number">24</span>, SP</span><br><span class="line"><span class="number">0x0064</span> <span class="number">00100</span> (main.<span class="keyword">go</span>:<span class="number">18</span>)  RET</span><br><span class="line"><span class="number">0x0065</span> <span class="number">00101</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  XCHGL  AX, AX</span><br><span class="line"><span class="number">0x0066</span> <span class="number">00102</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  CALL   runtime.deferreturn(SB)</span><br><span class="line"><span class="number">0x006b</span> <span class="number">00107</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  MOVQ   <span class="number">16</span>(SP), BP</span><br><span class="line"><span class="number">0x0070</span> <span class="number">00112</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  ADDQ   $<span class="number">24</span>, SP</span><br><span class="line"><span class="number">0x0074</span> <span class="number">00116</span> (main.<span class="keyword">go</span>:<span class="number">10</span>)  RET</span><br></pre></td></tr></table></figure></p>
<p>deferproc 方法被调用了两次，并且内部调用了 newdefer 方法，我们之前已经看到该方法将我们的函数注册为延迟函数。之后，在函数的最后，在 deferreturn 函数的帮助下，延迟方法会被一个接一个地调用。</p>
<p>Go 标准库向我们展示了结构体 _defer 同样链接了一个 _panic *_panic 属性。来通过另一个例子看下它在哪里会起作用。</p>
<p><strong>延迟和返回值</strong><br>如规范所描述，延迟函数访问返回的结果的唯一方法是使用命名返回参数：</p>
<blockquote>
<p>如果延迟函数是一个匿名函数（ function literal ），并且所在函数存在命名返回参数，同时该命名返回参数在匿名函数的作用域中，匿名函数可能会在返回参数返回前访问并修改它们。</p>
</blockquote>
<p>这里有个例子：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">   fmt.Printf(<span class="string">"with named param, x: %d\n"</span>, namedParam())</span><br><span class="line">   fmt.Printf(<span class="string">"without named param, x: %d\n"</span>, notNamedParam())</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">namedParam</span><span class="params">()</span> <span class="params">(x <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">   x = <span class="number">1</span></span><br><span class="line">   <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123; x = <span class="number">2</span> &#125;()</span><br><span class="line">   <span class="keyword">return</span> x</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">notNamedParam</span><span class="params">()</span> <span class="params">(<span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">   x := <span class="number">1</span></span><br><span class="line">   <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123; x = <span class="number">2</span> &#125;()</span><br><span class="line">   <span class="keyword">return</span> x</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>with named param, x: 2<br>without named param, x: 1<br>确实就像这篇“defer, panic 和 recover”博客所描述的一样，一旦确定这一行为，我们可以将其与 recover 函数混合使用：</p>
<blockquote>
<p>recover 函数 是一个用于重新获取对恐慌（panicking）goroutine 控制的内置函数。recover 函数仅在延迟函数内部时才有效。</p>
</blockquote>
<p>如我们所见，_defer 结构体链接了一个 _panic 属性，该属性在 panic 调用期间被链接。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">gopanic</span><span class="params">(e <span class="keyword">interface</span>&#123;&#125;)</span></span> &#123;</span><br><span class="line">   [...]</span><br><span class="line">   <span class="keyword">var</span> p _panic</span><br><span class="line">   [...]</span><br><span class="line">   d := gp._defer <span class="comment">// 当前附加的 defer 函数</span></span><br><span class="line">   [...]</span><br><span class="line">   d._panic = (*_panic)(noescape(unsafe.Pointer(&amp;p)))</span><br><span class="line">   [...]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>确实，在发生 panic 的情况下，调用延迟函数之前会调用 gopanic 方法：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0x0067 00103 (main.go:21)   CALL   runtime.gopanic(SB)</span><br><span class="line">0x006c 00108 (main.go:21)  UNDEF</span><br><span class="line">0x006e 00110 (main.go:16)  XCHGL  AX, AX</span><br><span class="line">0x006f 00111 (main.go:16)  CALL   runtime.deferreturn(SB)</span><br></pre></td></tr></table></figure></p>
<p>这里是一个 recover 函数利用命名返回参数的例子：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">   fmt.Printf(<span class="string">"error from err1: %v\n"</span>, err1())</span><br><span class="line">   fmt.Printf(<span class="string">"error from err2: %v\n"</span>, err2())</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">err1</span><span class="params">()</span> <span class="title">error</span></span> &#123;</span><br><span class="line">   <span class="keyword">var</span> err error</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">      <span class="keyword">if</span> r := <span class="built_in">recover</span>(); r != <span class="literal">nil</span> &#123;</span><br><span class="line">         err = errors.New(<span class="string">"recovered"</span>)</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;()</span><br><span class="line">   <span class="built_in">panic</span>(<span class="string">`foo`</span>)</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> err</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">err2</span><span class="params">()</span> <span class="params">(err error)</span></span> &#123;</span><br><span class="line">   <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">      <span class="keyword">if</span> r := <span class="built_in">recover</span>(); r != <span class="literal">nil</span> &#123;</span><br><span class="line">         err = errors.New(<span class="string">"recovered"</span>)</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;()</span><br><span class="line">   <span class="built_in">panic</span>(<span class="string">`foo`</span>)</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">return</span> err</span><br><span class="line">&#125;</span><br><span class="line">error from err1: &lt;<span class="literal">nil</span>&gt;</span><br><span class="line">error from err2: recovered</span><br></pre></td></tr></table></figure></p>
<p>两者的结合是我们可以正常使用 recover 函数将我们希望的 error 返回给调用方。 作为这篇关于延迟函数的文章的总结，让我们来看看延迟函数的提升。</p>
<p><strong>性能提升</strong><br>Go 1.8是提升 defer 的最近的一个版本（译者注：目前 Go 1.14 才是提升 defer 性能的最近的一个版本），我们可以通过运行 Go 的基准测试来看到这些提升（在 1.7 和 1.8 之间进行对比）：</p>
<ul>
<li>name         old time/op  new time/op  delta</li>
<li>Defer-4      99.0ns ± 9%  52.4ns ± 5%  -47.04%  (p=0.000 n=9+10)</li>
<li>Defer10-4    90.6ns ± 13%  45.0ns ± 3%  -50.37%  (p=0.000 n=10+10)<br>这样的提升得益于这个提升分配方式的 CL ，避免了栈的增长。</li>
</ul>
<p>不带参数的 defer 语句避免内存拷贝也是一个优化。下面是带参数和不带参数的延迟函数的基准测试：</p>
<ul>
<li>name     old time/op  new time/op  delta</li>
<li>Defer-4  51.3ns ± 3%  45.8ns ± 1%  -10.72%  (p=0.000 n=10+10)<br>由于第二个优化，现在速度也提高了 10%。</li>
</ul>
<p>via: <a href="https://medium.com/a-journey-with-go/go-how-does-defer-statement-work-1a9492689b6e" rel="external nofollow noopener noreferrer" target="_blank">https://medium.com/a-journey-with-go/go-how-does-defer-statement-work-1a9492689b6e</a></p>
<p>作者：Vincent Blanchon  译者：dust347  校对：unknwon</p>
<p>本文由 GCTT 原创编译，Go语言中文网 荣誉推出</p>
<p>本文由 GCTT 原创翻译，Go语言中文网 首发。也想加入译者行列，为开源做一些自己的贡献么？欢迎加入 GCTT！<br>翻译工作和译文发表仅用于学习和交流目的，翻译工作遵照 CC-BY-NC-SA 协议规定，如果我们的工作有侵犯到您的权益，请及时联系我们。<br>欢迎遵照 CC-BY-NC-SA 协议规定 转载，敬请在正文中标注并保留原文/译文链接和作者/译者等信息。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go g0,特殊的Goroutinue</title>
    <url>/go/go%20g0,%E7%89%B9%E6%AE%8A%E7%9A%84Goroutinue/</url>
    <content><![CDATA[<p><a href="https://studygolang.com/articles/28443" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>ℹ️ 这篇文章基于 Go 1.13。</p>
<p>在 Go 中创建的所有 Goroutine 都会被一个内部的调度器所管理。Go 调度器尝试为所有的 Goroutine 分配运行时间，并且在当前的 Goroutine 阻塞或者终止的时候，Go 调度器会通过运行 Goroutine 的方式使所有 CPU 保持忙碌状态。这个调度器实际上是作为一个特殊的 Goroutine 运行的。</p>
<h1 id="调度-goroutine"><a href="#调度-goroutine" class="headerlink" title="调度 goroutine"></a>调度 goroutine</h1><p>Go 使用 GOMAXPROCS 变量限制同时运行的 OS 线程数量，这意味着 Go 必须对每个运行着的线程上的 Goroutine 进行调度和管理。这个调度的功能被委托给了一个叫做 g0 的特殊的 goroutine， g0 是为每个 OS 线程创建的第一个 goroutine：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f26eb1817c8e097da6890ecfb5004206.png" alt></p>
<p>之后，g0 会把就绪状态的 Goroutine 调度到线程上去运行。</p>
<p>我建议你阅读我的文章“ Go：Goroutine，OS 线程和 CPU 管理”，来了解更多关于 P，M，G 模型的信息。</p>
<p>为了更好的理解 g0 的调度策略，来回顾下 channel 的用法。下面是一个 Goroutine 在向 channel 发送数据是阻塞的例子：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">ch := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">int</span>)</span><br><span class="line">[...]</span><br><span class="line">ch &lt;- v</span><br></pre></td></tr></table></figure></p>
<p>当在 channel 上阻塞时，当前的 Goroutine 会被停放（ parked ），即处于等待状态（ waiting mode ），并且不会被放在任何 Goroutine 队列中：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/fcb37340e2f7534a09c504de46592ed0.png" alt></p>
<p>之后，g0 会替换 Goroutine 并进行一轮调度：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/048bb6c6b0d2498d52a64a09a0d9428b.png" alt></p>
<p>本地队列在调度过程中具有优先级，2 号 Goroutine 会被运行：<br><img src="https://img-blog.csdnimg.cn/img_convert/4d60ab4f4b4e17d4dc3f05095434cea9.png" alt><br>我建议你阅读我的文章<a href="https://medium.com/a-journey-with-go/go-work-stealing-in-go-scheduler-d439231be64d" rel="external nofollow noopener noreferrer" target="_blank">“ Go: Go 调度器中的工作窃取（Work-Stealing）”</a> 来了解更多关于调度优先级的细节。</p>
<p>一旦有接收者读取 channel 中的数据，7 号 Goroutine 就会解除阻塞状态：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">v := &lt;-ch</span><br></pre></td></tr></table></figure></p>
<p>收到消息的 Goroutine 会切换到 g0，并且通过放入本地队列的方式将该 Goroutine 从停放状态解锁：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0e556cf972acb93d0d14c2cf44815a32.png" alt></p>
<p>虽然这个特殊的 Goroutine 管理调度策略，但这并不是它唯一的工作，它还负责着更多的工作。</p>
<h1 id="职责"><a href="#职责" class="headerlink" title="职责"></a>职责</h1><p>与普通 Goroutine 不同的是，g0 有着固定且更大的栈，这使得在需要更大的栈的时候，以及栈不宜增长的时候，Go 可以进行操作。在 g0 的职责中，我们可以列出：</p>
<ul>
<li>Goroutine 创建。当调用 go func(){ … }() 或 go myFunction() 时，Go 会在把它们放入本地队列前，将函数的创建委托给 g0 去做：<br><img src="https://img-blog.csdnimg.cn/img_convert/3337e60b449ef662cd174aa5699192c0.png" alt></li>
</ul>
<p>新创建的 Goroutine 优先运行，并且被放在本地队列的顶部。</p>
<p>建议阅读我的文章<a href="https://medium.com/a-journey-with-go/go-concurrency-scheduler-affinity-3b678f490488" rel="external nofollow noopener noreferrer" target="_blank">“Go：并发与调度器亲和性（ Go: Concurrency &amp; Scheduler Affinity ）”</a>了解更多关于 Goroutine 优先级的信息。</p>
<ul>
<li>defer 函数分配。</li>
<li>垃圾收集操作，比如 STW（ stopping the world ），扫描 Goroutine 的栈，以及一些标记清理操作。</li>
<li>栈增长。当需要的时候，Go 会增加 Goroutine 的大小。这个操作是由 g0 的 prolog 函数完成的。<br>这个特殊的 Goroutine 涉及许多其他操作（较大空间的对象分配，cgo 等），需要较大的栈来保证我们的程序进行更高效的管理操作，以保持程序的低内存打印效率。</li>
</ul>
<p>via: <a href="https://medium.com/a-journey-with-go/go-g0-special-goroutine-8c778c6704d8" rel="external nofollow noopener noreferrer" target="_blank">https://medium.com/a-journey-with-go/go-g0-special-goroutine-8c778c6704d8</a></p>
<p>作者：Vincent Blanchon  译者：dust347  校对：polaris1119</p>
<p>本文由 GCTT 原创编译，Go语言中文网 荣誉推出</p>
<p>本文由 GCTT 原创翻译，Go语言中文网 首发。也想加入译者行列，为开源做一些自己的贡献么？欢迎加入 GCTT！<br>翻译工作和译文发表仅用于学习和交流目的，翻译工作遵照 CC-BY-NC-SA 协议规定，如果我们的工作有侵犯到您的权益，请及时联系我们。<br>欢迎遵照 CC-BY-NC-SA 协议规定 转载，敬请在正文中标注并保留原文/译文链接和作者/译者等信息。<br>文章仅代表作者的知识和看法，如有不同观点，请楼下排队吐槽</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go 定时器 ticker和timer</title>
    <url>/go/go%20%E5%AE%9A%E6%97%B6%E5%99%A8%20ticker%E5%92%8Ctimer/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/lanyang123456/article/details/79794183" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>两种类型的定时器：ticker和timer。两者有什么区别呢？请看如下代码：</p>
<p>ticker<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">        <span class="string">"fmt"</span></span><br><span class="line">        <span class="string">"time"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">        d := time.Duration(time.Second*<span class="number">2</span>)</span><br><span class="line">        t := time.NewTicker(d)</span><br><span class="line">        <span class="keyword">defer</span> t.Stop()</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">for</span> &#123;</span><br><span class="line">                &lt;- t.C</span><br><span class="line">                fmt.Println(<span class="string">"timeout..."</span>)</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>timeout…<br>timeout…<br>timeout…</p>
<p>解析</p>
<p>ticker只要定义完成，从此刻开始计时，不需要任何其他的操作，每隔固定时间都会触发。</p>
<p>timer<br> <figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">        <span class="string">"fmt"</span></span><br><span class="line">        <span class="string">"time"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> </span><br><span class="line">        d := time.Duration(time.Second*<span class="number">2</span>)</span><br><span class="line">        t := time.NewTimer(d)</span><br><span class="line">        <span class="keyword">defer</span> t.Stop()</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">for</span> &#123;</span><br><span class="line">            &lt;- t.C</span><br><span class="line">            fmt.Println(<span class="string">"timeout..."</span>)</span><br><span class="line">		    <span class="comment">// need reset</span></span><br><span class="line">		    t.Reset(time.Second*<span class="number">2</span>)</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>output:</p>
<p>timeout…<br>timeout…<br>timeout…</p>
<p>解析</p>
<p>使用timer定时器，超时后需要重置，才能继续触发。</p>
<p>如果callback中有sleep操作，那么ticker也不会在sleep期间发送多个timeout_signal，可以认为用了一个容量为1的channel</p>
<p>实操如下<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">d := time.Duration(time.Second * <span class="number">1</span>)</span><br><span class="line">t := time.NewTicker(d)</span><br><span class="line"><span class="keyword">defer</span> t.Stop()</span><br><span class="line">log.Println(<span class="string">"timeout..."</span>)</span><br><span class="line">time.Sleep(<span class="number">10</span> * time.Second)</span><br><span class="line"> </span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line">	&lt;-t.C</span><br><span class="line">	log.Println(<span class="string">"timeout..."</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>15:12:44 timeout…<br>15:12:54 timeout…<br>15:12:55 timeout…</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go 使用protobuf</title>
    <url>/go/go%20%E4%BD%BF%E7%94%A8protobuf/</url>
    <content><![CDATA[<p><a href="https://segmentfault.com/a/1190000009277748" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a>发布于 2017-05-03</p>
<p><strong>为什么要使用protobuf</strong><br>最近的项目中，一直使用Json做数据传输。Json用起来的确很方便。但相对于protobuf数据量更大些。做一个移动端应用，为用户省点流量还是很有必要的。正好也可以学习一下protobuf的使用</p>
<ul>
<li><p>跟Json相比protobuf性能更高，更加规范</p>
</li>
<li><p>编解码速度快，数据体积小</p>
</li>
</ul>
<p>使用统一的规范，不用再担心大小写不同导致解析失败等蛋疼的问题了</p>
<ul>
<li><p>但也失去了一些便利性</p>
</li>
<li><p>改动协议字段，需要重新生成文件。</p>
</li>
</ul>
<p>数据没有可读性</p>
<p><strong>安装</strong><br>在go中使用protobuf，有两个可选用的包goprotobuf（go官方出品）和gogoprotobuf。<br>gogoprotobuf完全兼容google protobuf，它生成的代码质量和编解码性能均比goprotobuf高一些</p>
<p><strong>安装protoc</strong><br>首先去<a href="https://github.com/google/pro.." rel="external nofollow noopener noreferrer" target="_blank">https://github.com/google/pro..</a>. 上下载protobuf的编译器protoc，windows上可以直接下到exe文件(linux则需要编译)，最后将下载好的可执行文件拷贝到GOPATH的bin目录下(GOPATH的bin目录下(GOPATH/bin目录最好添加到系统环境变量里)</p>
<p><strong>安装protobuf库文件</strong><br>go get github.com/golang/protobuf/proto<br>goprotobuf<br>安装插件</p>
<p>go get github.com/golang/protobuf/protoc-gen-go<br>生成go文件</p>
<p>protoc –go_out=. *.proto<br>gogoprotobuf<br><strong>安装插件</strong></p>
<p>gogoprotobuf有两个插件可以使用</p>
<p>protoc-gen-gogo：和protoc-gen-go生成的文件差不多，性能也几乎一样(稍微快一点点)</p>
<p>protoc-gen-gofast：生成的文件更复杂，性能也更高(快5-7倍)<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">//gogo</span><br><span class="line">go get github.com/gogo/protobuf/protoc-gen-gogo</span><br><span class="line"> </span><br><span class="line">//gofast</span><br><span class="line">go get github.com/gogo/protobuf/protoc-gen-gofast</span><br></pre></td></tr></table></figure></p>
<p><strong>安装gogoprotobuf库文件</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">go get github.com/gogo/protobuf/proto</span><br><span class="line">go get github.com/gogo/protobuf/gogoproto  //这个不装也没关系</span><br></pre></td></tr></table></figure></p>
<p>生成go文件<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">//gogo</span><br><span class="line">protoc --gogo_out=. *.proto</span><br><span class="line"> </span><br><span class="line">//gofast</span><br><span class="line">protoc --gofast_out=. *.proto</span><br></pre></td></tr></table></figure></p>
<p><strong>性能测试</strong><br>这里只是简单的用go test测试了一下<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">//goprotobuf</span><br><span class="line"><span class="string">"编码"</span>：447ns/op</span><br><span class="line"><span class="string">"解码"</span>：422ns/op</span><br><span class="line"> </span><br><span class="line">//gogoprotobuf-go</span><br><span class="line"><span class="string">"编码"</span>：433ns/op</span><br><span class="line"><span class="string">"解码"</span>：427ns/op</span><br><span class="line"> </span><br><span class="line">//gogoprotobuf-fast</span><br><span class="line"><span class="string">"编码"</span>：112ns/op</span><br><span class="line"><span class="string">"解码"</span>：112ns/op</span><br></pre></td></tr></table></figure></p>
<p><strong>go_protobuf的简单使用</strong><br>test.proto<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">syntax = <span class="string">"proto3"</span>;  <span class="comment">//指定版本，必须要写（proto3、proto2）  </span></span><br><span class="line"><span class="keyword">package</span> proto;</span><br><span class="line"> </span><br><span class="line">enum FOO </span><br><span class="line">&#123; </span><br><span class="line">    X = <span class="number">0</span>; </span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//message是固定的。UserInfo是类名，可以随意指定，符合规范即可</span></span><br><span class="line">message UserInfo&#123;</span><br><span class="line">    <span class="keyword">string</span> message = <span class="number">1</span>;   <span class="comment">//消息</span></span><br><span class="line">    <span class="keyword">int32</span> length = <span class="number">2</span>;    <span class="comment">//消息大小</span></span><br><span class="line">    <span class="keyword">int32</span> cnt = <span class="number">3</span>;      <span class="comment">//消息计数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>client_protobuf.go<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"bufio"</span></span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    <span class="string">"net"</span></span><br><span class="line">    <span class="string">"os"</span></span><br><span class="line">    stProto <span class="string">"proto"</span></span><br><span class="line">    <span class="string">"time"</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">//protobuf编解码库,下面两个库是相互兼容的，可以使用其中任意一个</span></span><br><span class="line">    <span class="string">"github.com/golang/protobuf/proto"</span></span><br><span class="line">    <span class="comment">//"github.com/gogo/protobuf/proto"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    strIP := <span class="string">"localhost:6600"</span></span><br><span class="line">    <span class="keyword">var</span> conn net.Conn</span><br><span class="line">    <span class="keyword">var</span> err error</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//连接服务器</span></span><br><span class="line">    <span class="keyword">for</span> conn, err = net.Dial(<span class="string">"tcp"</span>, strIP); err != <span class="literal">nil</span>; conn, err = net.Dial(<span class="string">"tcp"</span>, strIP) &#123;</span><br><span class="line">        fmt.Println(<span class="string">"connect"</span>, strIP, <span class="string">"fail"</span>)</span><br><span class="line">        time.Sleep(time.Second)</span><br><span class="line">        fmt.Println(<span class="string">"reconnect..."</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    fmt.Println(<span class="string">"connect"</span>, strIP, <span class="string">"success"</span>)</span><br><span class="line">    <span class="keyword">defer</span> conn.Close()</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//发送消息</span></span><br><span class="line">    cnt := <span class="number">0</span></span><br><span class="line">    sender := bufio.NewScanner(os.Stdin)</span><br><span class="line">    <span class="keyword">for</span> sender.Scan() &#123;</span><br><span class="line">        cnt++</span><br><span class="line">        stSend := &amp;stProto.UserInfo&#123;</span><br><span class="line">            Message: sender.Text(),</span><br><span class="line">            Length:  *proto.Int(<span class="built_in">len</span>(sender.Text())),</span><br><span class="line">            Cnt:     *proto.Int(cnt),</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">//protobuf编码</span></span><br><span class="line">        pData, err := proto.Marshal(stSend)</span><br><span class="line">        <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="built_in">panic</span>(err)</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">//发送</span></span><br><span class="line">        conn.Write(pData)</span><br><span class="line">        <span class="keyword">if</span> sender.Text() == <span class="string">"stop"</span> &#123;</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>server_protobuf.go<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    <span class="string">"net"</span></span><br><span class="line">    <span class="string">"os"</span></span><br><span class="line">    stProto <span class="string">"proto"</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//protobuf编解码库,下面两个库是相互兼容的，可以使用其中任意一个</span></span><br><span class="line">    <span class="string">"github.com/golang/protobuf/proto"</span></span><br><span class="line">    <span class="comment">//"github.com/gogo/protobuf/proto"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">//监听</span></span><br><span class="line">    listener, err := net.Listen(<span class="string">"tcp"</span>, <span class="string">"localhost:6600"</span>)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="built_in">panic</span>(err)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        conn, err := listener.Accept()</span><br><span class="line">        <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="built_in">panic</span>(err)</span><br><span class="line">        &#125;</span><br><span class="line">        fmt.Println(<span class="string">"new connect"</span>, conn.RemoteAddr())</span><br><span class="line">        <span class="keyword">go</span> readMessage(conn)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//接收消息</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">readMessage</span><span class="params">(conn net.Conn)</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> conn.Close()</span><br><span class="line">    buf := <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">4096</span>, <span class="number">4096</span>)</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        <span class="comment">//读消息</span></span><br><span class="line">        cnt, err := conn.Read(buf)</span><br><span class="line">        <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="built_in">panic</span>(err)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        stReceive := &amp;stProto.UserInfo&#123;&#125;</span><br><span class="line">        pData := buf[:cnt]</span><br><span class="line"></span><br><span class="line">        <span class="comment">//protobuf解码</span></span><br><span class="line">        err = proto.Unmarshal(pData, stReceive)</span><br><span class="line">        <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="built_in">panic</span>(err)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        fmt.Println(<span class="string">"receive"</span>, conn.RemoteAddr(), stReceive)</span><br><span class="line">        <span class="keyword">if</span> stReceive.Message == <span class="string">"stop"</span> &#123;</span><br><span class="line">            os.Exit(<span class="number">1</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go post请求常用的几种方式</title>
    <url>/go/go%20post%E8%AF%B7%E6%B1%82%E5%B8%B8%E7%94%A8%E7%9A%84%E5%87%A0%E7%A7%8D%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/mofiu/article/details/79925994" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>post请求常用的几种方式，记录一下<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">httpPost</span><span class="params">()</span></span> &#123;</span><br><span class="line">    resp, err := http.Post(<span class="string">"https://www.abcd123.top/api/v1/login"</span>,</span><br><span class="line">        <span class="string">"application/x-www-form-urlencoded"</span>,</span><br><span class="line">        strings.NewReader(<span class="string">"username=test&amp;password=ab123123"</span>))</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(err)</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">defer</span> resp.Body.Close()</span><br><span class="line">    body, err := ioutil.ReadAll(resp.Body)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    fmt.Println(<span class="keyword">string</span>(body))</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">httpPostForm</span><span class="params">()</span></span> &#123;</span><br><span class="line">    resp, err := http.PostForm(<span class="string">"https://www.denlery.top/api/v1/login"</span>,</span><br><span class="line">        url.Values&#123;<span class="string">"username"</span>: &#123;<span class="string">"auto"</span>&#125;, <span class="string">"password"</span>: &#123;<span class="string">"auto123123"</span>&#125;&#125;)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">defer</span> resp.Body.Close()</span><br><span class="line">    body, err := ioutil.ReadAll(resp.Body)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br><span class="line">    fmt.Println(<span class="keyword">string</span>(body))</span><br><span class="line"> </span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">httpPostJson</span><span class="params">()</span></span> &#123;</span><br><span class="line">    jsonStr :=[]<span class="keyword">byte</span>(<span class="string">`&#123; "username": "auto", "password": "auto123123" &#125;`</span>)</span><br><span class="line">    url:= <span class="string">"https://www.denlery.top/api/v1/login"</span></span><br><span class="line">    req, err := http.NewRequest(<span class="string">"POST"</span>, url, bytes.NewBuffer(jsonStr))</span><br><span class="line">    req.Header.Set(<span class="string">"Content-Type"</span>, <span class="string">"application/json"</span>)</span><br><span class="line"> </span><br><span class="line">    client := &amp;http.Client&#123;&#125;</span><br><span class="line">    resp, err := client.Do(req)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">defer</span> resp.Body.Close()</span><br><span class="line"> </span><br><span class="line">    statuscode := resp.StatusCode</span><br><span class="line">    hea := resp.Header</span><br><span class="line">    body, _ := ioutil.ReadAll(resp.Body)</span><br><span class="line">    fmt.Println(<span class="keyword">string</span>(body))</span><br><span class="line">    fmt.Println(statuscode)</span><br><span class="line">    fmt.Println(hea)</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go 垃圾回收：三色算法</title>
    <url>/go/go%20%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%EF%BC%9A%E4%B8%89%E8%89%B2%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p><a href="https://juejin.cn/post/6844903897253806088" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>三色算法<br>go垃圾回收器的操作都是基于三色算法，这篇文章主要来说明此算法。</p>
<p>注意：三色算法并不是go独有的，它也会在其它编程语言中使用到</p>
<p>严格来说，在Go中这个算法的官方名称是叫做<strong>三色标记清除算法</strong>（tricolor mark-and-sweep algorithm）。它可以和程序一起并发工作并且使用<strong>写屏障</strong>（write barrier）。这就意味着，当Go程序员运行起来，go调度器去负责应用程序的调度，而垃圾回收器会像调度器处理常规应用程序一样，去使用多个goroutines去进行工作。</p>
<p>这个算法背后的核心思想是由Edsger W. Dijkstra，Leslie Lamport，A.J.Martin，C.S.Scholten和E.F.M.Steffens这些大佬提出的。算法首先发表在论文On-the-fly Garbage Collection：An Exercise in Cooperation上面。三色标记清除算法背后的首要原则就是它把堆中的对象根据它们的颜色分到不同集合里面。</p>
<p>现在让我们来谈谈每种颜色集合代表的含义。<strong>黑色集合</strong>是为了确保没有任何指针指向白色集合。但是<strong>白色集合</strong>中的对象允许有指针指向黑色集合，因为这不会对垃圾回收器的操作产生影响。<strong>灰色集合</strong>可能会有指针指向白色集合里的对象。白色集合中的对象就是垃圾回收的候选对象。</p>
<p>注意到没有任何对象可以从黑色集合进到白色集合，这允许算法能够去操作并且清除白色集合里的对象。此外，没有任何黑色集合里的指针对象能够直接指向白色集合中的对象。</p>
<p>当垃圾回收开始，全部对象标记为白色，然后垃圾回收器会遍历所有根对象并把它们标记为灰色。<strong>根对象</strong>就是程序能直接访问到的对象，包括全局变量以及栈里面的东西。这些对象大多数取决于特定程序的go代码。在这之后，垃圾回收器选取一个灰色的对象，把它变为黑色，然后开始寻找去确定这个对象是否有指针指向白色集合的对象。这意味着当一个灰色对象由于被其它对象的指针所指而扫描到的时候，这个灰色对象会被标记为黑色。如果扫描发现这个灰色对象有一个或者更多指针指向白色对象时，会把所指向的白色对象放到灰色集合里。只要有灰色集合对象存在，这个过程就会一直进行下去。之后，白色集合里的对象就是没人访问的对象，并且它们所占用的内存可以被回收重用。因此，在这个点上，我们说白色集合里的元素被垃圾回收了。</p>
<p>如果垃圾回收过程中，一个灰色对象在某些情况变为不可达状态，它在那次垃圾回收中就不会被回收了，但是不是说下次也不会回收！</p>
<p>在这个过程中，运行应用程序被叫做<strong>修改器</strong>（mutator）。mutator去运行一个小的方法叫做写屏障（write barrier），每次堆中的指针被修改写屏障都会去执行。如果堆中对象的指针被修改，就意味着那个对象现在是可触达的，写屏障会把它标记为灰色并把它放到灰色集合中。</p>
<p>mutator负责保持黑色集合中没有任何元素的指针去指向白色集合中的元素。这是在写屏障方法的帮助下完成的。如果维持这个不变状态失败的话，会毁坏垃圾回收过程，并且很可能会以一种丑陋和非预期的方式破坏你的程序。</p>
<p>堆可以看成许多连接对象的图，如下所示，展示了单独一个垃圾回收的过程。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0a182527652b338796a3149a3bca42ef.png" alt></p>
<p>我们有三种不同颜色：黑色、白色和黑色。当算法开始的时候，所有对象标记为白色。随着算法继续进行，白色对象移到了其它两种颜色集合的一种里面。最后留在白色集合里面的对象会在将来某个时间点被清理掉。</p>
<p>在前面的图里，你可以看到白色对象E，它是在白色集合里而且可以访问对象F，E不会被任何其它的对象访问到因为没有其它指向E的指针，这使得E成为了垃圾回收的最佳候选人！另外，对象A、B和C是根对象而且总是可达的，因此它们不会被垃圾回收掉。</p>
<p>接下来，算法会去处理留下的灰色集合元素，这意味着对象A和F会进入到黑色集合里。对象A会进入到黑色集合是因为它是一个根元素，而F会进入黑色集合是因为它没有指向任何其它对象但是是在灰色集合里。在对象A被垃圾回收之后，对象F会变成不可达状态并且会在下一次垃圾回收器的处理循环中被回收掉。</p>
<p>Go允许你通过在你的Go代码里放一个runtime.GC()的声明来手动去开启一次垃圾回收。但是，要记住一点，runtime.GC()会阻塞调用器，并且它可能会阻塞整个程序，尤其是如果你想运行一个非常繁忙的而且有很多对象的go程序。这种情况发生，主要是因为你不能在其他任何事都在频繁变化的时候去处理垃圾回收，因为这种情况不会给垃圾回收器机会，去清楚地确定白色、黑色和灰色集合里的成员。这种垃圾回收状态也被称作是<strong>垃圾回收安全点</strong>(garbage collection safe-point)。</p>
<p>你可以在<a href="https://github.com/golang/go/blob/master/src/runtime/mgc.go里找到垃圾回收器相关的高级go代码。你可以学习这个如果你想了解更多关于垃圾回收操作的东西。" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/golang/go/blob/master/src/runtime/mgc.go里找到垃圾回收器相关的高级go代码。你可以学习这个如果你想了解更多关于垃圾回收操作的东西。</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go内存详解</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go内存详解</tag>
      </tags>
  </entry>
  <entry>
    <title>go 语言中关于包导入必学的8个知识点</title>
    <url>/go/go%20%E8%AF%AD%E8%A8%80%E4%B8%AD%E5%85%B3%E4%BA%8E%E5%8C%85%E5%AF%BC%E5%85%A5%E5%BF%85%E5%AD%A6%E7%9A%848%E4%B8%AA%E7%9F%A5%E8%AF%86%E7%82%B9/</url>
    <content><![CDATA[<p>Hi，大家好，我是明哥<a href="https://juejin.im/post/6844904167073382408" rel="external nofollow noopener noreferrer" target="_blank"></a>。</p>
<p>在自己学习 Golang 的这段时间里，我写了详细的学习笔记放在我的个人微信公众号 《Go编程时光》，对于 Go 语言，我也算是个初学者，因此写的东西应该会比较适合刚接触的同学，如果你也是刚学习 Go 语言，不防关注一下，一起学习，一起成长。</p>
<p>我的在线博客：golang.iswbm.com 我的 Github：github.com/iswbm/GolangCodingTime</p>
<ol>
<li>单行导入与多行导入<br>在 Go 语言中，一个包可包含多个 .go 文件（这些文件必须得在同一级文件夹中），只要这些 .go 文件的头部都使用 package 关键字声明了同一个包。</li>
</ol>
<p>导入包主要可分为两种方式：</p>
<p>单行导入<br>import “fmt”<br>import “sync” 复制代码<br>多行导入<br>import(<br>    “fmt”<br>    “sync”<br>)复制代码<br>如你所见，Go 语言中 导入的包，必须得用双引号包含，在这里吐槽一下。</p>
<ol start="2">
<li>使用别名<br>在一些场景下，我们可能需要对导入的包进行重新命名，比如</li>
</ol>
<p>我们导入了两个具有同一包名的包时产生冲突，此时这里为其中一个包定义别名<br>import (<br>    “crypto/rand”<br>    mrand “math/rand” // 将名称替换为mrand避免冲突<br>)复制代码<br>我们导入了一个名字很长的包，为了避免后面都写这么长串的包名，可以这样定义别名<br>import hw “helloworldtestmodule”复制代码<br>防止导入的包名和本地的变量发生冲突，比如 path 这个很常用的变量名和导入的标准包冲突。<br>import pathpkg “path”复制代码</p>
<ol start="3">
<li>使用点操作<br>如里在我们程序内部里频繁使用了一个工具包，比如 fmt，那每次使用它的打印函数打印时，都要 包名+方法名。</li>
</ol>
<p>对于这种使用高频的包，可以在导入的时，就把它定义会 “自己人”（方法是使用一个 . ），自己人的话，不分彼此，它的方法，就是我们的方法。</p>
<p>从此，我们打印再也不用加 fmt 了。</p>
<p>import . “fmt”</p>
<p>func main() {<br>    Println(“hello, world”)<br>}复制代码<br>但这种用法，会有一定的隐患，就是导入的包里可能有函数，会和我们自己的函数发生冲突。</p>
<ol start="4">
<li>包的初始化<br>每个包都允许有一个 init 函数，当这个包被导入时，会执行该包的这个 init 函数，做一些初始化任务。</li>
</ol>
<p>对于 init 函数的执行有两点需要注意</p>
<p>init 函数优先于 main 函数执行</p>
<p>在一个包引用链中，包的初始化是深度优先的。比如，有这样一个包引用关系：main→A→B→C，那么初始化顺序为</p>
<p>C.init→B.init→A.init→main复制代码</p>
<ol start="5">
<li>包的匿名导入<br>当我们导入一个包时，如果这个包没有被使用到，在编译时，是会报错的。</li>
</ol>
<p>但是有些情况下，我们导入一个包，只想执行包里的 init 函数，来运行一些初始化任务，此时怎么办呢？</p>
<p>可以使用匿名导入，用法如下，其中下划线为空白标识符，并不能被访问</p>
<p>// 注册一个PNG decoder<br>import _ “image/png”复制代码<br>由于导入时，会执行 init 函数，所以编译时，仍然会将这个包编译到可执行文件中。</p>
<ol start="6">
<li>导入的是路径还是包？<br>当我们使用 import 导入 testmodule/foo 时，初学者，经常会问，这个 foo 到底是一个包呢，还是只是包所在目录名？</li>
</ol>
<p>import “testmodule/foo”复制代码<br>为了得出这个结论，专门做了个试验（请看「第七点里的代码示例」），最后得出的结论是：</p>
<p>导入时，是按照目录导入。导入目录后，可以使用这个目录下的所有包。<br>出于习惯，包名和目录名通常会设置成一样，所以会让你有一种你导入的是包的错觉。</p>
<ol start="7">
<li>相对导入和绝对导入<br>据我了解在 Go 1.10 之前，好像是不支持相对导入的，在 Go 1.10 之后才可以。</li>
</ol>
<p>绝对导入：从 $GOPATH/src 或 $GOROOT 或者 $GOPATH/pkg/mod 目录下搜索包并导入</p>
<p>相对导入：从当前目录中搜索包并开始导入。就像下面这样<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"./module1"</span></span><br><span class="line">    <span class="string">"../module2"</span></span><br><span class="line">    <span class="string">"../../module3"</span></span><br><span class="line">    <span class="string">"../module4/module5"</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure></p>
<p>分别举个例子吧</p>
<p>一、使用绝对导入</p>
<p>有如下这样的目录结构（注意确保当前目录在 GOPATH 下）</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/4360ba85f412a7b38e1b4714f7d392a1.png" alt></p>
<p>其中 main.go 是这样的<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"app/utilset"</span>   <span class="comment">// 这种使用的就是绝对路径导入</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    utils.PrintHello()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>而在 main.go 的同级目录下，还有另外一个文件夹 utilset ，为了让你理解 「第六点：import 导入的是路径而不是包」，我在 utilset 目录下定义了一个 hello.go 文件，这个go文件定义所属包为 utils。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> utils</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">PrintHello</span><span class="params">()</span></span>&#123;</span><br><span class="line">    fmt.Println(<span class="string">"Hello, 我在 utilset 目录下的 utils 包里"</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>运行结果如下</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/4a7efe692ddd41d3d62486122cbc92af.png" alt></p>
<p>二、使用相对导入</p>
<p>还是上面的代码，将绝对导入改为相对导入后</p>
<p>将 GOPATH 路径设置回去（请对比上面使用绝对路径的 GOPATH）</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2761d4241dd9d3cb78a85b38d0377d7c.png" alt></p>
<p>然后再次运行</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/9c50f2c93560fad63c96328f45934cc2.png" alt></p>
<p>总结一下，使用相对导入，有两点需要注意</p>
<p>项目不要放在 $GOPATH/src 下，否则会报错（比如我修改当前项目目录为GOPATH后，运行就会报错）</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/6e15c215b699d42d194478daaa5a8f62.png" alt><br>Go Modules 不支持相对导入，在你开启 GO111MODULE 后，无法使用相对导入。</p>
<p>最后，不得不说的是：使用相对导入的方式，项目可读性会大打折扣，不利用开发者理清整个引用关系。</p>
<p>所以一般更推荐使用绝对引用的方式。使用绝对引用的话，又要谈及优先级了</p>
<ol start="8">
<li>包导入路径优先级<br>前面一节，介绍了三种不同的包依赖管理方案，不同的管理模式，存放包的路径可能都不一样，有的可以将包放在 GOPATH 下，有的可以将包放在 vendor 下，还有些包是内置包放在 GOROOT 下。</li>
</ol>
<p>那么问题就来了，如果在这三个不同的路径下，有一个相同包名但是版本不同的包，我们导入的时候，是选择哪个进行导入呢？</p>
<p>这就需要我们搞懂，在 Golang 中包搜索路径优先级是怎样的？</p>
<p>这时候就需要区分，是使用哪种模式进行包的管理的。</p>
<p>如果使用 govendor</p>
<p>当我们导入一个包时，它会：</p>
<p>先从项目根目录的 vendor 目录中查找<br>最后从 $GOROOT/src 目录下查找<br>然后从 $GOPATH/src 目录下查找<br>都找不到的话，就报错。<br>为了验证这个过程，我在创建中创建一个 vendor 目录后，就开启了 vendor 模式了，我在 main.go 中随便导入一个包 pkg，由于这个包是我随便指定的，当然会找不到，找不到就会报错， Golang 会在报错信息中打印中搜索的过程，从这个信息中，就可以看到 Golang 的包查找优先级了。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/63eae9f4eda330f62391ba7df7c481a9.png" alt></p>
<p>如果使用 go modules</p>
<p>你导入的包如果有域名，都会先在 $GOPATH/pkg/mod 下查找，找不到就连网去该网站上寻找，找不到或者找到的不是一个包，则报错。</p>
<p>而如果你导入的包没有域名（比如 “fmt”这种），就只会到 $GOROOT 里查找。</p>
<p>还有一点很重要，当你的项目下有 vendor 目录时，不管你的包有没有域名，都只会在 vendor 目录中想找。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/cd4b80e6fd7f18baf4ca9db37a9fab7c.png" alt></p>
<p>通常vendor 目录是通过 go mod vendor 命令生成的，这个命令会将项目依赖全部打包到你的项目目录下的 verdor 文件夹中。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go中的init函数以及main函数</title>
    <url>/go/go%E4%B8%AD%E7%9A%84init%E5%87%BD%E6%95%B0%E4%BB%A5%E5%8F%8Amain%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p><a href="https://www.cnblogs.com/TimLiuDream/p/9929934.html" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>首先我们看一个例子：init函数：</p>
<p>init 函数可在package main中，可在其他package中，可在同一个package中出现多次。</p>
<p>main函数</p>
<p>main 函数只能在package main中。</p>
<p>执行顺序</p>
<p>golang里面有两个保留的函数：init函数（能够应用于所有的package）和main函数（只能应用于package main）。这两个函数在定义时不能有任何的参数和返回值。</p>
<p>虽然一个package里面可以写任意多个init函数，但这无论是对于可读性还是以后的可维护性来说，我们都强烈建议用户在一个package中每个文件只写一个init函数。</p>
<p>go程序会自动调用init()和main()，所以你不需要在任何地方调用这两个函数。每个package中的init函数都是可选的，但package main就必须包含一个main函数。</p>
<p>程序的初始化和执行都起始于main包。</p>
<p>如果main包还导入了其它的包，那么就会在编译时将它们依次导入。有时一个包会被多个包同时导入，那么它只会被导入一次（例如很多包可能都会用到fmt包，但它只会被导入一次，因为没有必要导入多次）。</p>
<p>当一个包被导入时，如果该包还导入了其它的包，那么会先将其它包导入进来，然后再对这些包中的包级常量和变量进行初始化，接着执行init函数（如果有的话），依次类推。</p>
<p>等所有被导入的包都加载完毕了，就会开始对main包中的包级常量和变量进行初始化，然后执行main包中的init函数（如果存在的话），最后执行main函数。下图详细地解释了整个执行过程：<br><img src="https://img-blog.csdnimg.cn/img_convert/8cc2759948da08815f99405ccce0c6f3.png" alt><br>首先我们看一个例子：</p>
<p>代码结构：<br><img src="https://img-blog.csdnimg.cn/img_convert/0bee8f7034f85ff09d41f1a04074d11d.png" alt><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">Lib1.<span class="keyword">go</span></span><br><span class="line"></span><br><span class="line">复制代码</span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> InitLib1</span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">init</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(<span class="string">"lib1"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">Lib2.<span class="keyword">go</span></span><br><span class="line"><span class="keyword">package</span> InitLib2</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">init</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(<span class="string">"lib2"</span>)</span><br><span class="line">&#125;</span><br><span class="line">main.<span class="keyword">go</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    _ <span class="string">"GolangTraining/InitLib1"</span></span><br><span class="line">    _ <span class="string">"GolangTraining/InitLib2"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">init</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(<span class="string">"libmain init"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(<span class="string">"libmian main"</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>代码很简单，只是一些简单的输出</p>
<p>lib1<br>lib2<br>libmain init<br>libmian main<br>输出的顺序与我们上面图给出的顺序是一致的</p>
<p>那我们现在就改动一个地方，Lib1包导入Lib2，main包不管<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">Lib1</span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> InitLib1</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    _ <span class="string">"GolangTraining/InitLib2"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">init</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(<span class="string">"lib1"</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出：</p>
<p>lib2<br>lib1<br>libmain init<br>libmian main<br>main包以及Lib1包都导入了Lib2，但是只出现一次，并且最先输出，</p>
<p>说明如果一个包会被多个包同时导入，那么它只会被导入一次，而先输出lib2是因为main包中导入Lib1时，Lib1又导入了Lib2，会首先初始化Lib2包的东西</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go单例模式</title>
    <url>/go/go%E5%8D%95%E4%BE%8B%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<p><a href="https://mp.weixin.qq.com/s/JAlt0JQt8hkPCxYbmOMFJw" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p><strong>Go语言中的单例模式</strong><br>在过去的几年中，Go语言的发展是惊人的，并且吸引了很多由其他语言（Python、PHP、Ruby）转向Go语言的跨语言学习者。</p>
<p>在过去的很长时间里，很多开发人员和初创公司都习惯使用Python、PHP或Ruby快速开发功能强大的系统，并且大多数情况下都不需要担心内部事务如何工作，也不需要担心线程安全性和并发性。直到最近几年，多线程高并发的系统开始流行起来，我们现在不仅需要快速开发功能强大的系统，而且还要保证被开发的系统能够足够快速运行。（我们真是太难了图片）</p>
<p>对于被Go语言天生支持并发的特性吸引来的跨语言学习者来说，我觉着掌握Go语言的语法并不是最难的，最难的是突破既有的思维定势，真正理解并发和使用并发来解决实际问题。</p>
<p>Go语言太容易实现并发了，以至于它在很多地方被不正确的使用了。</p>
<p><strong>常见的错误</strong><br>有一些错误是很常见的，比如不考虑并发安全的单例模式。就像下面的示例代码：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> singleton</span><br><span class="line"> </span><br><span class="line"><span class="keyword">type</span> singleton <span class="keyword">struct</span> &#123;&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> instance *singleton</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GetInstance</span><span class="params">()</span> *<span class="title">singleton</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> instance == <span class="literal">nil</span> &#123;</span><br><span class="line">        instance = &amp;singleton&#123;&#125;   <span class="comment">// 不是并发安全的</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> instance</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>在上述情况下，多个goroutine可以执行第一个检查，并且它们都将创建该singleton类型的实例并相互覆盖。无法保证它将在此处返回哪个实例，并且对该实例的其他进一步操作可能与开发人员的期望不一致。</p>
<p>不好的原因是，如果有代码保留了对该单例实例的引用，则可能存在具有不同状态的该类型的多个实例，从而产生潜在的不同代码行为。这也成为调试过程中的一个噩梦，并且很难发现该错误，因为在调试时，由于运行时暂停而没有出现任何错误，这使非并发安全执行的可能性降到了最低，并且很容易隐藏开发人员的问题。</p>
<p><strong>激进的加锁</strong><br>也有很多对这种并发安全问题的糟糕解决方案。使用下面的代码确实能解决并发安全问题，但会带来其他潜在的严重问题，通过加锁把对该函数的并发调用变成了串行。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> mu Sync.Mutex</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GetInstance</span><span class="params">()</span> *<span class="title">singleton</span></span> &#123;</span><br><span class="line">    mu.Lock()                    <span class="comment">// 如果实例存在没有必要加锁</span></span><br><span class="line">    <span class="keyword">defer</span> mu.Unlock()</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> instance == <span class="literal">nil</span> &#123;</span><br><span class="line">        instance = &amp;singleton&#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> instance</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>在上面的代码中，我们可以看到在创建单例实例之前通过引入Sync.Mutex和获取Lock来解决并发安全问题。问题是我们在这里执行了过多的锁定，即使我们不需要这样做，在实例已经创建的情况下，我们应该简单地返回缓存的单例实例。在高度并发的代码基础上，这可能会产生瓶颈，因为一次只有一个goroutine可以获得单例实例。</p>
<p>因此，这不是最佳方法。我们必须考虑其他解决方案。</p>
<p><strong>Check-Lock-Check模式</strong><br>在C ++和其他语言中，确保最小程度的锁定并且仍然是并发安全的最佳和最安全的方法是在获取锁定时利用众所周知的Check-Lock-Check模式。该模式的伪代码表示如下。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> check() &#123;</span><br><span class="line">    lock() &#123;</span><br><span class="line">        <span class="keyword">if</span> check() &#123;</span><br><span class="line">            <span class="comment">// 在这里执行加锁安全的代码</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>该模式背后的思想是，你应该首先进行检查，以最小化任何主动锁定，因为IF语句的开销要比加锁小。其次，我们希望等待并获取互斥锁，这样在同一时刻在那个块中只有一个执行。但是，在第一次检查和获取互斥锁之间，可能有其他goroutine获取了锁，因此，我们需要在锁的内部再次进行检查，以避免用另一个实例覆盖了实例。</p>
<p>如果将这种模式应用于我们的GetInstance()方法，我们会写出类似下面的代码：<br><figure class="highlight smali"><table><tr><td class="code"><pre><span class="line">func GetInstance() *singleton &#123;</span><br><span class="line">   <span class="built_in"> if </span>instance == nil &#123;     // 不太完美 因为这里不是完全原子的</span><br><span class="line">        mu.Lock()</span><br><span class="line">        defer mu.Unlock()</span><br><span class="line"> </span><br><span class="line">       <span class="built_in"> if </span>instance == nil &#123;</span><br><span class="line">           <span class="built_in"> instance </span>= &amp;singleton&#123;&#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">   <span class="built_in"> return </span>instance</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>通过使用sync/atomic这个包，我们可以原子化加载并设置一个标志，该标志表明我们是否已初始化实例。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">"sync"</span></span><br><span class="line"><span class="keyword">import</span> <span class="string">"sync/atomic"</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> initialized <span class="keyword">uint32</span></span><br><span class="line">... <span class="comment">// 此处省略</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GetInstance</span><span class="params">()</span> *<span class="title">singleton</span></span> &#123;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> atomic.LoadUInt32(&amp;initialized) == <span class="number">1</span> &#123;  <span class="comment">// 原子操作 </span></span><br><span class="line">            <span class="keyword">return</span> instance</span><br><span class="line">      &#125;</span><br><span class="line"> </span><br><span class="line">    mu.Lock()</span><br><span class="line">    <span class="keyword">defer</span> mu.Unlock()</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> initialized == <span class="number">0</span> &#123;</span><br><span class="line">         instance = &amp;singleton&#123;&#125;</span><br><span class="line">         atomic.StoreUint32(&amp;initialized, <span class="number">1</span>)</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> instance</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>但是……这看起来有点繁琐了，我们其实可以通过研究Go语言和标准库如何实现goroutine同步来做得更好。</p>
<p><strong>Go语言惯用的单例模式</strong><br>我们希望利用Go惯用的方式来实现这个单例模式。我们在标准库sync中找到了Once类型。它能保证某个操作仅且只执行一次。下面是来自Go标准库的源码（部分注释有删改）。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Once is an object that will perform exactly one action.</span></span><br><span class="line"><span class="keyword">type</span> Once <span class="keyword">struct</span> &#123;</span><br><span class="line">    <span class="comment">// done indicates whether the action has been performed.</span></span><br><span class="line">    <span class="comment">// It is first in the struct because it is used in the hot path.</span></span><br><span class="line">    <span class="comment">// The hot path is inlined at every call site.</span></span><br><span class="line">    <span class="comment">// Placing done first allows more compact instructions on some architectures (amd64/x86),</span></span><br><span class="line">    <span class="comment">// and fewer instructions (to calculate offset) on other architectures.</span></span><br><span class="line">    done <span class="keyword">uint32</span></span><br><span class="line">    m    Mutex</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(o *Once)</span> <span class="title">Do</span><span class="params">(f <span class="keyword">func</span>()</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> atomic.LoadUint32(&amp;o.done) == <span class="number">0</span> &#123; <span class="comment">// check</span></span><br><span class="line">        <span class="comment">// Outlined slow-path to allow inlining of the fast-path.</span></span><br><span class="line">        o.doSlow(f)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(o *Once)</span> <span class="title">doSlow</span><span class="params">(f <span class="keyword">func</span>()</span>)</span> &#123;</span><br><span class="line">    o.m.Lock()                          <span class="comment">// lock</span></span><br><span class="line">    <span class="keyword">defer</span> o.m.Unlock()</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> o.done == <span class="number">0</span> &#123;                    <span class="comment">// check</span></span><br><span class="line">        <span class="keyword">defer</span> atomic.StoreUint32(&amp;o.done, <span class="number">1</span>)</span><br><span class="line">        f()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这说明我们可以借助这个实现只执行一次某个函数/方法，once.Do()的用法如下：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">once.Do(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">// 在这里执行安全的初始化</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure></p>
<p>下面就是单例实现的完整代码，该实现利用sync.Once类型去同步对GetInstance()的访问，并确保我们的类型仅被初始化一次。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> singleton</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"sync"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="keyword">type</span> singleton <span class="keyword">struct</span> &#123;&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> instance *singleton</span><br><span class="line"><span class="keyword">var</span> once sync.Once</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GetInstance</span><span class="params">()</span> *<span class="title">singleton</span></span> &#123;</span><br><span class="line">    once.Do(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        instance = &amp;singleton&#123;&#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="keyword">return</span> instance</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>因此，使用sync.Once包是安全地实现此目标的首选方式，类似于Objective-C和Swift（Cocoa）实现dispatch_once方法来执行类似的初始化。</p>
<p><strong>结论</strong><br>当涉及到并发和并行代码时，需要对代码进行更仔细的检查。始终让你的团队成员执行代码审查，因为这样的事情很容易就会被发现。</p>
<p>所有刚转到Go语言的新开发人员都必须真正了解并发安全性如何工作以更好地改进其代码。即使Go语言本身通过允许你在对并发性知识知之甚少的情况下设计并发代码，也完成了许多繁重的工作。在某些情况下，单纯的依靠语言特性也无能为力，你仍然需要在开发代码时应用最佳实践。</p>
<p>翻译自<a href="http://marcio.io/2015/07/singleton-pattern-in-go/，考虑到可读性内容与原文略有差异。" rel="external nofollow noopener noreferrer" target="_blank">http://marcio.io/2015/07/singleton-pattern-in-go/，考虑到可读性内容与原文略有差异。</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>世界最美的街景</title>
    <url>/%E5%85%B4%E8%B6%A3%E7%88%B1%E5%A5%BD/%E4%B8%96%E7%95%8C%E6%9C%80%E7%BE%8E%E7%9A%84%E8%A1%97%E6%99%AF/</url>
    <content><![CDATA[<p>以下内容转载自 <a href="https://www.toutiao.com/i6719284510678073870/" rel="external nofollow noopener noreferrer" target="_blank">https://www.toutiao.com/i6719284510678073870/</a></p>
<p>鲁稚的阳台2019-07-30 10:55:37</p>
<p>虽然我们没有翅膀，无法像天上的鸟儿飞得那样高去欣赏世界，但是，我们有智慧、有能力借助能飞上天空的相机去俯瞰美丽的世界，欣赏美丽的风光……</p>
<p><strong>阿根廷布宜诺斯艾利斯</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/9c2aa9c0b047acab12b51c2b94a5ad46.png" alt="世界上最美的街景"></p>
<p>Photo by ins@alepetra_</p>
<p><strong>芬兰赫尔辛基(Helsinki)</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2d7825c4832164ff7419de428532fa23.png" alt="世界上最美的街景"></p>
<p>Photo by ins@nk7</p>
<p><strong>芬兰罗瓦涅米(Rovaniemi)</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a6bc862adb7c66684de9b43f57923b32.png" alt="世界上最美的街景"></p>
<p>Photo by ins@janiylinampa</p>
<p><strong>阿联酋迪拜哈利法塔（Burj Khalifa）</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0e0cadd45254f00a03ae6123c2933414.png" alt="世界上最美的街景"></p>
<p>Photo by ins@michokhoury</p>
<p><strong>阿联酋迪拜棕榈岛</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/976799be352ba6a7e86fc81996c0b2d0.png" alt="世界上最美的街景"></p>
<p>Photo by ins@iamdocgelo</p>
<p><strong>法国巴黎埃菲尔(Eiffel)铁塔</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/89e8cad38ae01f3a2840031fe3c38b28.png" alt="世界上最美的街景"></p>
<p>Photo by ins@kev_mrc</p>
<p><strong>法国巴黎凯旋门</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5ce855cdc8830bfde027779cedb5e238.png" alt="世界上最美的街景"></p>
<p>Photo by ins@nk7</p>
<p><strong>法国里尔(Lille)</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d24a389f291ce5717b5cfb217bb11298.png" alt="世界上最美的街景"></p>
<p>Photo by ins@elensham</p>
<p><strong>梵蒂冈</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/8177da48a95dbaaa95355f057a4de6a1.png" alt="世界上最美的街景"></p>
<p>Photo by ins@nk7</p>
<p><strong>荷兰阿姆斯特丹市约丹(Jordaan)</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/74a5294413629658bc98d0adb99eb232.png" alt="世界上最美的街景"></p>
<p>Photo by <a href="mailto:ins@kus.bakisi" rel="external nofollow noopener noreferrer" target="_blank">ins@kus.bakisi</a></p>
<p><strong>克罗地亚希尼贝克（Sibenik）</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/88ab76c0b0a1f475099816756505fdcc.png" alt="世界上最美的街景"></p>
<p>Photo by <a href="mailto:ins@toni.krstulovic" rel="external nofollow noopener noreferrer" target="_blank">ins@toni.krstulovic</a></p>
<p><strong>挪威罗弗敦（Lofoten）诺德兰县</strong></p>
<p><strong>（ Nordland）Hamnøy</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/8677bea74d018f07389b884a903bf95f.png" alt="世界上最美的街景"></p>
<p>Photo by ins@evolumina</p>
<p><strong>斯洛文尼亚布莱德湖(Bled Lake)</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d326c0ff4598df37969e6da45e6066bc.png" alt="世界上最美的街景"></p>
<p>Photo by ins@kev_mrc</p>
<p><strong>西班牙巴塞罗那圣家堂</strong></p>
<p><strong>（Basílica de la Sagrada Familia）</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/79e8f06974799cff2b4f8a72467dc38e.png" alt="世界上最美的街景"></p>
<p>Photo by ins@willcheyney</p>
<p><strong>匈牙利布达佩斯</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e77e79c01ff74cfc71c31c9e302739b5.png" alt="世界上最美的街景"></p>
<p>Photo by ins@krenn_imre</p>
<p><strong>意大利罗马</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/ae89e303b6243622a65771abb0116ba4.png" alt="世界上最美的街景"></p>
<p>Photo by ins@baroudeurfrancais</p>
<p><strong>意大利罗马斗兽场</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/fd70df7824a393b3e121e79e7109407c.png" alt="世界上最美的街景"></p>
<p>Photo by ins@mariciu</p>
<p><strong>意大利托斯卡纳（Tuscany）</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d16dd70c3dd4cf51e1c324e109b10dea.png" alt="世界上最美的街景"></p>
<p>Photo by ins@sennarelax</p>
<p><strong>意大利威尼斯</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e9c69471fe094cc881b43111ed60b7f5.png" alt="世界上最美的街景"></p>
<p>Photo by ins@karamintheworld</p>
<p><strong>意大利五渔村里奥马焦雷(Riomaggiore)</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/463ea287fc302010cc84635bfbbdf93f.png" alt="世界上最美的街景"></p>
<p>Photo by ins@kev_mrc</p>
<p><strong>希腊，莱斯沃斯岛，Molyvos</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/239393ab9f7746698ff3a9d23f30a747.png" alt="世界上最美的街景"></p>
<p><strong>德国，波恩</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d7add3cc83d53cee26b6ed821a1f716b.png" alt="世界上最美的街景"></p>
<p><strong>罗马尼亚，爱的隧道</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/eed3365f2f6f056194c87d11a4bb7509.png" alt="世界上最美的街景"></p>
<p><strong>瑞典，斯德哥尔摩</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/fc7944bee7391dc7ef2c4573467ece0a.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>西班牙，瓦伦西亚</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d037f942cf06769d745e3aa0aad6f624.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>澳大利亚，新南威尔士州，格拉夫顿</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/9f9a9fd37fa9111381928c3b2253ad49.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>希腊，伯罗奔尼撒半岛，纳夫普利奥</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/79de9f99270f3147e60efd449c800c38.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>南非，天玺</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5d02f48c6f44e9c7a714a67f51d64b66.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>意大利，斯佩罗</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2a3cbbecef7fc479171a39bdd9466dc1.png" alt="世界上最美的街景"></p>
<p><strong>巴西，阿雷格里港</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f21601c35f3f04a81dbbcf10b50823da.png" alt="世界上最美的街景"></p>
<p><strong>中国台湾</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/9c9adf8e89de52000eb81496665e7a56.png" alt="世界上最美的街景"></p>
<p><strong>美国，华盛顿</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/445f5d93a7f1bb22d0eb118c1a3903a8.png" alt="世界上最美的街景"></p>
<p><strong>西班牙，赫雷斯</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/399807a510136c320af05f771ff0578b.png" alt="世界上最美的街景"></p>
<p><strong>意大利，波西塔诺</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/86bd2533c559aa5e8f839b8da296e160.png" alt="世界上最美的街景"></p>
<p><strong>葡萄牙，马德拉岛，丰沙尔</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2d5ff60629fa5e27375c58f40e335e65.png" alt="世界上最美的街景"></p>
<p><strong>澳大利亚，布里斯班</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/aabaf5736071f9511111b26850e2372b.png" alt="世界上最美的街景"></p>
<p><strong>日本，紫藤隧道</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3c89b05019d90ca005f2905819e24836.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>美国，丹佛</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/b415eb6034dca6322497e2edd7b2d4de.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>津巴布韦，哈拉雷，密尔顿大道</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/155e1a60079e648877ab2447a61e9db0.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>美国，萨凡纳</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3e838a3acb1402e81f46a25ef49e5cdb.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>波兰，Kaluszyn</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/8eb26c6e4f730c38c925189daf0c5d19.png" alt="世界上最美的街景"></p>
<p>…………………………………….</p>
<p><strong>美国，北卡罗来纳州，奥顿人工车道</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e78ed8d314db3080f08ed0a53b66cf96.png" alt="世界上最美的街景"></p>
]]></content>
      <categories>
        <category>兴趣爱好</category>
        <category>旅游</category>
      </categories>
      <tags>
        <tag>兴趣爱好</tag>
        <tag>旅游</tag>
      </tags>
  </entry>
  <entry>
    <title>哔哩哔哩（B站）语言类学习视频大汇总！</title>
    <url>/%E5%85%B4%E8%B6%A3%E7%88%B1%E5%A5%BD/%E5%93%94%E5%93%A9%E5%93%94%E5%93%A9%EF%BC%88B%E7%AB%99%EF%BC%89%E8%AF%AD%E8%A8%80%E7%B1%BB%E5%AD%A6%E4%B9%A0%E8%A7%86%E9%A2%91%E5%A4%A7%E6%B1%87%E6%80%BB%EF%BC%81/</url>
    <content><![CDATA[<p>以下内容转载自 <a href="https://www.toutiao.com/w/a1657382084896772/" rel="external nofollow noopener noreferrer" target="_blank">https://www.toutiao.com/w/a1657382084896772/</a></p>
<p>哔哩哔哩（B站）语言类学习视频大汇总！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d2027cac5b36c8e9df3fb3ceafc56a96.png" alt="img"><img src="https://img-blog.csdnimg.cn/img_convert/21b55d02faac794506073f85e596037f.png" alt="img"><img src="https://img-blog.csdnimg.cn/img_convert/f7a2da53d0e3a0f57bf0a548540177c1.png" alt="img"><img src="https://img-blog.csdnimg.cn/img_convert/8235fb907b4a38ce01da7703099050a6.png" alt="img"><img src="https://img-blog.csdnimg.cn/img_convert/7424e38bc523581affcf5f99b9b936d1.png" alt="img"><img src="https://img-blog.csdnimg.cn/img_convert/92a553a1239b561eda58a37156c53cb0.png" alt="img"></p>
]]></content>
      <categories>
        <category>兴趣爱好，语言</category>
      </categories>
      <tags>
        <tag>兴趣爱好，语言</tag>
      </tags>
  </entry>
  <entry>
    <title>lua代码建议的阅读顺序</title>
    <url>/lua/lua%E4%BB%A3%E7%A0%81%E5%BB%BA%E8%AE%AE%E7%9A%84%E9%98%85%E8%AF%BB%E9%A1%BA%E5%BA%8F/</url>
    <content><![CDATA[<p><a href="https://www.reddit.com/comments/63hth/ask_reddit_which_oss_codebases_out_there_are_so/c02pxbp" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h1 id="Recommended-reading-order"><a href="#Recommended-reading-order" class="headerlink" title="Recommended reading order:"></a>Recommended reading order:</h1><ul>
<li><p>lmathlib.c, lstrlib.c: get familiar with the external C API. Don’t bother with the pattern matcher though. Just the easy functions.</p>
</li>
<li><p>lapi.c: Check how the API is implemented internally. Only skim this to get a feeling for the code. Cross-reference to lua.h and luaconf.h as needed.</p>
</li>
<li><p>lobject.h: tagged values and object representation. skim through this first. you’ll want to keep a window with this file open all the time.</p>
</li>
<li><p>lstate.h: state objects. ditto.</p>
</li>
<li><p>lopcodes.h: bytecode instruction format and opcode definitions. easy.</p>
</li>
<li><p>lvm.c: scroll down to luaV_execute, the main interpreter loop. see how all of the instructions are implemented. skip the details for now. reread later.</p>
</li>
<li><p>ldo.c: calls, stacks, exceptions, coroutines. tough read.</p>
</li>
<li><p>lstring.c: string interning. cute, huh?</p>
</li>
<li><p>ltable.c: hash tables and arrays. tricky code.</p>
</li>
<li><p>ltm.c: metamethod handling, reread all of lvm.c now.</p>
</li>
<li><p>You may want to reread lapi.c now.</p>
</li>
<li><p>ldebug.c: surprise waiting for you. abstract interpretation is used to find object names for tracebacks. does bytecode verification, too.</p>
</li>
<li><p>lparser.c, lcode.c: recursive descent parser, targetting a register-based VM. start from chunk() and work your way through. read the expression parser and the code generator parts last.</p>
</li>
<li><p>lgc.c: incremental garbage collector. take your time.</p>
</li>
<li><p>Read all the other files as you see references to them. Don’t let your stack get too deep though.</p>
</li>
</ul>
]]></content>
      <categories>
        <category>lua</category>
        <category>lua知识点</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>lua知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>必须长期坚持的习惯</title>
    <url>/%E7%94%9F%E6%B4%BB/%E5%BF%85%E9%A1%BB%E9%95%BF%E6%9C%9F%E5%9D%9A%E6%8C%81%E7%9A%84%E4%B9%A0%E6%83%AF/</url>
    <content><![CDATA[<h3 id="长期坚持的好习惯"><a href="#长期坚持的好习惯" class="headerlink" title="长期坚持的好习惯"></a>长期坚持的好习惯</h3><ol>
<li>不在生气时做决定</li>
<li>定期反省和复盘</li>
<li>每天学习至少30分钟</li>
<li>保持谦虚和低调</li>
<li>聚会时少玩手机</li>
<li>不轻易说出秘密</li>
<li>吃饭时细嚼慢咽</li>
<li>不耀自己的钱财</li>
<li>每天运动半小时</li>
<li>经常保持微笑</li>
<li>请字在前，谢字在后</li>
<li>递接东西时用双手</li>
<li>重要节日记得送礼物</li>
<li>不强求或强迫他人</li>
<li>不背后评论议论人</li>
<li>站有站相坐有坐相</li>
<li>记下重要亲友的生日</li>
<li>定时清理手机和电脑</li>
<li>重要文件记得备份</li>
</ol>
]]></content>
      <categories>
        <category>生活，习惯</category>
      </categories>
      <tags>
        <tag>生活，习惯</tag>
      </tags>
  </entry>
  <entry>
    <title>C4：4个函数，528行代码实现可自举的 C语言编译器</title>
    <url>/lua/C4%EF%BC%9A4%E4%B8%AA%E5%87%BD%E6%95%B0%EF%BC%8C528%E8%A1%8C%E4%BB%A3%E7%A0%81%E5%AE%9E%E7%8E%B0%E5%8F%AF%E8%87%AA%E4%B8%BE%E7%9A%84%20C%E8%AF%AD%E8%A8%80%E7%BC%96%E8%AF%91%E5%99%A8/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>云风的Blog-Lua GC的源码解剖6</title>
    <url>/lua/%E4%BA%91%E9%A3%8E%E7%9A%84Blog-Lua%20GC%20%E7%9A%84%E6%BA%90%E7%A0%81%E5%89%96%E6%9E%906/</url>
    <content><![CDATA[<p><a href="https://blog.codingnow.com/2011/04/lua_gc_6.html" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h3 id="Lua-GC-的源码剖析-6-完结"><a href="#Lua-GC-的源码剖析-6-完结" class="headerlink" title="Lua GC 的源码剖析 (6) 完结"></a>Lua GC 的源码剖析 (6) 完结</h3><p>GC 中最繁杂的 mark 部分已经谈完了。剩下的东西很简单。今天一次可以写完。</p>
<p>sweep 分两个步骤，一个是清理字符串，另一个是清理其它对象。看代码，lgc.c 573 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">case GCSsweepstring: &#123;</span><br><span class="line">  <span class="function"><span class="title">lu_mem</span> old = g-&gt;</span>totalbytes;</span><br><span class="line">  <span class="function"><span class="title">sweepwholelist</span>(L, &amp;g-&gt;</span><span class="function"><span class="title">strt</span>.hash[g-&gt;</span>sweepstrgc++]);</span><br><span class="line">  <span class="function"><span class="title">if</span> (g-&gt;</span><span class="function"><span class="title">sweepstrgc</span> &gt;= g-&gt;</span>strt.size)  <span class="comment">/* nothing more to sweep? */</span></span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>gcstate = GCSsweep;  <span class="comment">/* end sweep-string phase */</span></span><br><span class="line">  <span class="function"><span class="title">lua_assert</span>(old &gt;= g-&gt;</span>totalbytes);</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">estimate</span> -= old - g-&gt;</span>totalbytes;</span><br><span class="line">  return GCSWEEPCOST;</span><br><span class="line">&#125;</span><br><span class="line">case GCSsweep: &#123;</span><br><span class="line">  <span class="function"><span class="title">lu_mem</span> old = g-&gt;</span>totalbytes;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">sweepgc</span> = sweeplist(L, g-&gt;</span>sweepgc, GCSWEEPMAX);</span><br><span class="line">  <span class="function"><span class="title">if</span> (*g-&gt;</span>sweepgc == NULL) &#123;  <span class="comment">/* nothing more to sweep? */</span></span><br><span class="line">    checkSizes(L);</span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>gcstate = GCSfinalize;  <span class="comment">/* end sweep phase */</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="title">lua_assert</span>(old &gt;= g-&gt;</span>totalbytes);</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">estimate</span> -= old - g-&gt;</span>totalbytes;</span><br><span class="line">  return GCSWEEPMAX*GCSWEEPCOST;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在 GCSsweepstring 中，每步调用 sweepwholelist 清理 strt 这个 hash 表中的一列。理想状态下，所有的 string 都被 hash 散列开没有冲突，这每一列上有一个 string 。我们可以读读 lstring.c 的 68 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">if</span> (tb-&gt;</span><span class="function"><span class="title">nuse</span> &gt; cast(lu_int32, tb-&gt;</span><span class="function"><span class="title">size</span>) &amp;&amp; tb-&gt;</span>size &lt;= MAX_INT/<span class="number">2</span>)</span><br><span class="line">  <span class="function"><span class="title">luaS_resize</span>(L, tb-&gt;</span>size*<span class="number">2</span>);  <span class="comment">/* too crowded */</span></span><br></pre></td></tr></table></figure>
<p>当 hash 表中的 string 数量(nuse) 大于 hash 表的列数(size) 时，lua 将 hash 表的列数扩大一倍。就是按一列一个元素来估计的。</p>
<p>值得一提的是，分布执行的 GC ，在这个阶段，string 对象是有可能清理不干净的。当 GCSsweepstring 步骤中，step 间若发生以上 string table 的 hash 表扩容事件，那么 string table 将被 rehash 。一些来不及清理的 string 很有可能被打乱放到已经通过 GCSsweepstring 的表列里。一旦发生这种情况，部分 string 对象则没有机会在当次 GC 流程中被重置为白色。在某些极端情况下，即使你调用 fullgc 一次也不能彻底的清除垃圾。</p>
<p>关于 string 对象，还有个小地方需要了解。lua 是复用相同值的 TString 的，且同值 string 绝对不能有两份。而 GC 的分步执行，可能会导致一些待清理的 TString 又复活。所以在它在创建新的 TString 对象时做了检查，见 lstring.c 86 行：</p>
<figure class="highlight gauss"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (ts-&gt;tsv.len == l &amp;&amp; (<span class="built_in">memcmp</span>(str, <span class="built_in">getstr</span>(ts), l) == <span class="number">0</span>)) &#123;</span><br><span class="line">  <span class="comment">/* string may be dead */</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">isdead</span>(<span class="built_in">G</span>(L), o)) <span class="built_in">changewhite</span>(o);</span><br><span class="line">  <span class="keyword">return</span> ts;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>相同的问题也存在于 upvalue 。同样有类似检查。</p>
<p>此处 GCSWEEPCOST 应该是一个经验值。前面我们知道 singlestep 返回的这个步骤大致执行的时间。这样可以让 <code>luaC_step</code> 这个 api 每次执行的时间大致相同。mark 阶段是按扫描的字节数确定这个值的。而每次释放一个 string 的时间大致相等（和 string 的长度无关），GCSWEEPCOST 就是释放一个对象的开销了。</p>
<p>GCSsweep 清理的是整个 GCObject 链表。这个链表很长，所以也是分段完成的。记录遍历位置的指针是 sweepgc ，每次遍历 GCSWEEPMAX 个。无论遍历是否清理，开销都是差不太多的。因为对于存活的对象，需要把颜色位重置；需要清理的对象则需要释放内存。每趟 GCSsweep 的开销势必比 GCSsweepstring 大。大致估算时间为 GCSWEEPMAX*GCSWEEPCOST 。</p>
<p>真正的清理工作通过 lgc.c 408 行的 sweeplist 函数进行。</p>
<figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line">static GCObject **sweeplist (lua_State *L, GCObject **p, lu_mem count) &#123;</span><br><span class="line">  GCObject *curr;</span><br><span class="line">  global_State *g = G(L);</span><br><span class="line">  int deadmask = otherwhite(g);</span><br><span class="line">  <span class="keyword">while</span> ((curr = *p) != NULL &amp;&amp; count-- &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (curr-&gt;gch.tt == LUA_TTHREAD)  <span class="comment">/* sweep open upvalues of each thread */</span></span><br><span class="line">      sweepwholelist<span class="function"><span class="params">(L, &amp;gco2th(curr)-&gt;openupval)</span>;</span></span><br><span class="line"><span class="function">    <span class="title">if</span> <span class="params">((curr-&gt;gch.marked ^ WHITEBITS) &amp; deadmask)</span> &#123;  /* <span class="title">not</span> <span class="title">dead</span>? */</span></span><br><span class="line"><span class="function">      <span class="title">lua_assert</span><span class="params">(!isdead(g, curr) || testbit(curr-&gt;gch.marked, FIXEDBIT))</span>;</span></span><br><span class="line"><span class="function">      <span class="title">makewhite</span><span class="params">(g, curr)</span>;  /* <span class="title">make</span> <span class="title">it</span> <span class="title">white</span> <span class="params">(<span class="keyword">for</span> next cycle)</span> */</span></span><br><span class="line"><span class="function">      <span class="title">p</span> = &amp;<span class="title">curr</span>-&gt;</span>gch.next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;  <span class="comment">/* must erase `curr' */</span></span><br><span class="line">      lua_assert(isdead(g, curr) || deadmask == bitmask(SFIXEDBIT));</span><br><span class="line">      *p = curr-&gt;gch.next;</span><br><span class="line">      <span class="keyword">if</span> (curr == g-&gt;rootgc)  <span class="comment">/* is the first element of the list? */</span></span><br><span class="line">        g-&gt;rootgc = curr-&gt;gch.next;  <span class="comment">/* adjust first */</span></span><br><span class="line">      freeobj(L, curr);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>后半段比较好理解，当对象存活的时候，调用 makewhite ；死掉则调用 freeobj 。sweeplist 的起点并不是从 rootgc 而是 sweepgc （它们的值可能相同），所以对于头节点，需要做一点调整。</p>
<p>前面的 sweep open upvalues of each thread 需要做一点解释。为什么 upvalues 需要单独清理？这要从 upvalue 的储存说起。</p>
<p>upvalues 并不是放在整个 GCObject 链表中的。而是存在于每个 thread 自己的 L 中（openupval 域）。为何要这样设计？因为和 string table 类似，upvalues 需要唯一性，即引用相同变量的对象只有一个。所以运行时需要对当前 thread 的已有 upvalues 进行遍历。Lua 为了节省内存，并没有为 upvalues 多申请一个指针放置额外的链表。就借用 GCObject 本身的单向链表。所以每个 thread 拥有的 upvalues 就自成一链了。相关代码可以参考 lfunc.c 53 行处的 <code>luaF_findupval</code> 函数。</p>
<p>但也不是所有 upvalues 都是这样存放的。closed upvalue 就不再需要存在于 thread 的链表中。在 <code>luaF_close</code> 函数中将把它移到其它 GCObject 链中。见 lfunc.c 106 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">unlinkupval(uv);</span><br><span class="line"><span class="function"><span class="title">setobj</span>(L, &amp;uv-&gt;</span><span class="function"><span class="title">u</span>.value, uv-&gt;</span>v);</span><br><span class="line"><span class="function"><span class="title">uv</span>-&gt;</span><span class="function"><span class="title">v</span> = &amp;uv-&gt;</span>u.value;  <span class="comment">/* now current value lives here */</span></span><br><span class="line">luaC_linkupval(L, uv);  <span class="comment">/* link upvalue into `gcroot' list */</span></span><br></pre></td></tr></table></figure>
<p>另，某些 upvalue 天生就是 closed 的。它们可以直接通过 <code>luaF_newupval</code> 构造出来。</p>
<p>按道理来说，对 openvalues 的清理会增加单次 **sweeplist 的负荷，当记入 singlestep 的返回值。但这样会导致 sweeplist 接口变复杂，实现的代价也会增加。鉴于 thread 通常不多，GC 开销也只是一个估算值，也就没有特殊处理了。</p>
<hr>
<p>GC 的最后一个流程为 GCSfinalize 。</p>
<p>它通过 GCTM 函数，每次调用一个需要回收的 userdata 的 gc 元方法。见 lgc.c 的 446 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">static void GCTM (lua_State *L) &#123;</span><br><span class="line">  global_State *g = G(L);</span><br><span class="line">  GCO<span class="function"><span class="title">bject</span> *o = g-&gt;</span><span class="function"><span class="title">tmudata</span>-&gt;</span>gch.next;  <span class="comment">/* get first element */</span></span><br><span class="line">  Udata *udata = rawgco2u(o);</span><br><span class="line">  const TValue *tm;</span><br><span class="line">  <span class="comment">/* remove udata from `tmudata' */</span></span><br><span class="line">  <span class="function"><span class="title">if</span> (o == g-&gt;</span>tmudata)  <span class="comment">/* last element? */</span></span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>tmudata = NULL;</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">tmudata</span>-&gt;</span><span class="function"><span class="title">gch</span>.next = udata-&gt;</span>uv.next;</span><br><span class="line">  <span class="function"><span class="title">udata</span>-&gt;</span><span class="function"><span class="title">uv</span>.next = g-&gt;</span><span class="function"><span class="title">mainthread</span>-&gt;</span>next;  <span class="comment">/* return it to `root' list */</span></span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">mainthread</span>-&gt;</span>next = o;</span><br><span class="line">  makewhite(g, o);</span><br><span class="line">  <span class="function"><span class="title">tm</span> = fasttm(L, udata-&gt;</span>uv.metatable, TM_GC);</span><br><span class="line">  <span class="keyword">if</span> (tm != NULL) &#123;</span><br><span class="line">    <span class="function"><span class="title">lu_byte</span> oldah = L-&gt;</span>allowhook;</span><br><span class="line">    <span class="function"><span class="title">lu_mem</span> oldt = g-&gt;</span>GCthreshold;</span><br><span class="line">    L-&gt;allowhook = <span class="number">0</span>;  <span class="comment">/* stop debug hooks during GC tag method */</span></span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>GC<span class="function"><span class="title">threshold</span> = 2*g-&gt;</span>totalbytes;  <span class="comment">/* avoid GC steps */</span></span><br><span class="line">    <span class="function"><span class="title">setobj2s</span>(L, L-&gt;</span>top, tm);</span><br><span class="line">    <span class="function"><span class="title">setuvalue</span>(L, L-&gt;</span>top+<span class="number">1</span>, udata);</span><br><span class="line">    L-&gt;top += <span class="number">2</span>;</span><br><span class="line">    <span class="function"><span class="title">luaD_call</span>(L, L-&gt;</span>top - <span class="number">2</span>, <span class="number">0</span>);</span><br><span class="line">    L-&gt;allowhook = oldah;  <span class="comment">/* restore hooks */</span></span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>GCthreshold = oldt;  <span class="comment">/* restore threshold */</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代码逻辑很清晰。需要留意的是，gc 元方法里应避免再触发 GC 。所以这里采用修改 GCthreshold 为比较较大值来回避。这其实不能完全避免 GC 的重入。甚至用户错误的编写代码也可能主动触发，但通常问题不大。因为最坏情况也只是把 GCthreshold 设置为一个不太正确的值，并不会引起逻辑上的错误。</p>
<hr>
<p>后记：</p>
<p>终于把这个系列写完了。接下来的工作，我想做一些分析，看能否以最小代价（尽量少用锁）改造出一个多线程版本的 GC 。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>云风的Lua-GC解剖</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>云风的Lua-GC解剖</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua52内部实现-Function</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-Function/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8313434" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>基本概念<br>Lua函数引入了很多functional programming的概念，这些概念使得Lua函数在使用上可谓“功能强大，简洁统一”，非常符合程序员的“审美观”。<br>函数在Lua中属于“带有词法范围的一类值”(first-class values with proper lexical scoping)。</p>
<p>所谓“一类值”（first-class values），应该符合以下条件：</p>
<ul>
<li>可以被保存到变量和数据结构中</li>
<li>可以被当作子程序的参数和返回值</li>
<li>可以在运行期被创建</li>
<li>具有内在一致性，也就是不依赖于任何给定的名称<br>大多数语言的基本数据类型，比如int，就属于“一类值”。很多语言中的函数实现，只能满足其中一些条件。比如在C中可以将函数指针保存到变量中，可以将函数指针当作参数和返回值。这样的函数实现一般不会被算作“一类值”。<br>在Lua中，所有的值都是“一类”值，包括函数本身。函数可以被保存到任何的变量或者table中，可以被当作参数和返回值使用，所有的函数（更准确的说应该是closure）都是运行期被创建的，函数本身并没有名字，名字只是对函数的引用而已。</li>
</ul>
<p>First-class function为lua带来了很多FP的概念，比如“高阶函数”(Higher-order functions)，“匿名函数”(Anonymous functions”)。这些功能在很多语言中都是通过特殊的语法来支持的，而在lua中则不需要。</p>
<p>所谓“词法范围”（lexical scoping)，</p>
<p>Functions in Lua are first-class values with proper lexical scoping.</p>
<p>first class value</p>
<p><a href="http://en.wikipedia.org/wiki/First-class_citizen" rel="external nofollow noopener noreferrer" target="_blank">http://en.wikipedia.org/wiki/First-class_citizen</a></p>
<p>can be stored in variables and data structures<br>can be passed as a parameter to a subroutine<br>can be returned as the result of a subroutine<br>can be constructed at run-time<br>has intrinsic identity (independent of any given name)</p>
<p>lexical scoping</p>
<p>static scoping</p>
<p>在Lua中，lua closure和c closure都使用一个Closure结构体来表示一个closure对象：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">CClosure</span> &#123;</span></span><br><span class="line">  ClosureHeader;</span><br><span class="line">  lua_CFunction f;</span><br><span class="line">  TValue upvalue[<span class="number">1</span>];  <span class="comment">/* list of upvalues */</span></span><br><span class="line">&#125; CClosure;</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LClosure</span> &#123;</span></span><br><span class="line">  ClosureHeader;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">Proto</span> *<span class="title">p</span>;</span></span><br><span class="line">  UpVal *upvals[<span class="number">1</span>];  <span class="comment">/* list of upvalues */</span></span><br><span class="line">&#125; LClosure;</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> Closure &#123;</span><br><span class="line">  CClosure c;</span><br><span class="line">  LClosure l;</span><br><span class="line">&#125; Closure;</span><br></pre></td></tr></table></figure></p>
<p>closure代表一个函数的可调用实例。无论时lua closure还是c closure，都包含两部分信息：函数体和upvalue。</p>
<p>Lua closure中的p是对</p>
<p>函数数据类型在lua中使用LUA_TFUNCTION来标识。在内部，LUA_TFUNCTION又被分成三种子类型：LUA_TLCL，LUA_TLCF和LUA_TCCL。</p>
<p>LUA_TLCF代表一个轻量级的C function。所谓的轻量级</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua52内部实现:GC3</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-GC3/</url>
    <content><![CDATA[<h1 id="GC步骤"><a href="#GC步骤" class="headerlink" title="GC步骤"></a>GC步骤</h1><p>global_State中的gcstate记录了当前incremental gc的执行状态。根据执行状态，整个gc过程被分为很多步执行。</p>
<h1 id="GCSpause"><a href="#GCSpause" class="headerlink" title="GCSpause"></a>GCSpause</h1><p>GCSpause状态标志着当前没有开始gc。gc一旦开始，第一步要做的就是标识所有的root对象。root对象包括global_State引用的mainthread对象，registry table，全局的metatable和上次gc所产生的还没有进行finalize的垃圾对象。标识工作就是将white对象设置成gray，是通过函数reallymarkobject进行的。reallymarkobject会根据不同的对象作不同的处理。<br>对于string对象，本身没有对其它对象的引用，就可以立即设置成black，无需等待后面的遍历。对于userdata对象，只会引用到一个metatable和env，所以直接mark后也可以立即设置成black。对于upvalue对象，直接mark引用的对象。所有root对象会被设置成gray状态，等待下一步的propagate。第一步完成后，gc状态会切换成GCSpropagate。</p>
<h1 id="GCSpropagate"><a href="#GCSpropagate" class="headerlink" title="GCSpropagate"></a>GCSpropagate</h1><p>这一步主要就是将所有gray对象变成black，并将其引用到的white对象变成gray，直到没有gray对象存在为止。<br>在GCSpropagate状态下，barrier会起作用。Lua并不监控所有的引用变化，否则会非常影响效率会。一些我们认为经常会发生变化的地方，比如stack的引用变化，就不用barrier。</p>
<ul>
<li>barrier的位置</li>
<li>lapi.c moveto luaC_barrier</li>
<li>lapi.c lua_load luaC_barrier</li>
<li>lapi.c lua_setupvalue luaC_barrier</li>
<li>lcode.c addk luaC_barrier</li>
<li>lvm.c OP_SETUPVAL luaC_barrier</li>
<li>lapi.c lua_rawset lua_rawseti lua_rawsetp  luaC_barrierback</li>
<li>ltable.c luaH_newkey  luaC_barrierback</li>
<li>lvm.c luaV_settable luaC_barrierback</li>
<li>lvm.c OP_SETLIST luaC_barrierback</li>
<li>lapi.c lua_setmetatable luaC_objbarrier userdate-&gt;mt</li>
<li>lapi.c lua_setuservalue luaC_objbarrier</li>
<li>lapi.c lua_upvaluejoin luaC_objbarrier</li>
<li>ldo.c f_parser luaC_objbarrier initialize upvalue</li>
<li>lparser.c luaC_objbarrier</li>
<li>lapi.c lua_setmetatable luaC_objbarrierback</li>
</ul>
<p>何时进行gc</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua52内部实现:GC2</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-GC2/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8684869" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h1 id="GCObject"><a href="#GCObject" class="headerlink" title="GCObject"></a>GCObject</h1><p>Lua使用union GCObject来表示所有的垃圾回收对象：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">182</span> <span class="comment">/*</span></span><br><span class="line"><span class="comment">183 ** Union of all collectable objects</span></span><br><span class="line"><span class="comment">184 */</span></span><br><span class="line"><span class="number">185</span> <span class="keyword">union</span> GCObject &#123;</span><br><span class="line"><span class="number">186</span>   GCheader gch;  <span class="comment">/* common header */</span></span><br><span class="line"><span class="number">187</span>   <span class="keyword">union</span> TString ts;</span><br><span class="line"><span class="number">188</span>   <span class="keyword">union</span> Udata u;</span><br><span class="line"><span class="number">189</span>   <span class="keyword">union</span> Closure cl;</span><br><span class="line"><span class="number">190</span>   <span class="class"><span class="keyword">struct</span> <span class="title">Table</span> <span class="title">h</span>;</span></span><br><span class="line"><span class="number">191</span>   <span class="class"><span class="keyword">struct</span> <span class="title">Proto</span> <span class="title">p</span>;</span></span><br><span class="line"><span class="number">192</span>   <span class="class"><span class="keyword">struct</span> <span class="title">UpVal</span> <span class="title">uv</span>;</span></span><br><span class="line"><span class="number">193</span>   <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span> <span class="title">th</span>;</span>  <span class="comment">/* thread */</span></span><br><span class="line"><span class="number">194</span> &#125;;</span><br></pre></td></tr></table></figure></p>
<p>这就相当于在C++中，将所有的GC对象从GCheader派生，他们都共享GCheader。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">74</span> <span class="comment">/*</span></span><br><span class="line"><span class="comment">75 ** Common Header for all collectable objects (in macro form, to be</span></span><br><span class="line"><span class="comment">76 ** included in other objects)</span></span><br><span class="line"><span class="comment">77 */</span></span><br><span class="line"><span class="number">78</span> <span class="meta">#<span class="meta-keyword">define</span> CommonHeader    GCObject *next; lu_byte tt; lu_byte marked</span></span><br><span class="line"><span class="number">79</span> </span><br><span class="line"><span class="number">80</span> </span><br><span class="line"><span class="number">81</span> <span class="comment">/*</span></span><br><span class="line"><span class="comment">82 ** Common header in struct form</span></span><br><span class="line"><span class="comment">83 */</span></span><br><span class="line"><span class="number">84</span> <span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">GCheader</span> &#123;</span></span><br><span class="line"><span class="number">85</span>   CommonHeader;  </span><br><span class="line"><span class="number">86</span> &#125; GCheader;</span><br></pre></td></tr></table></figure></p>
<p>marked这个标志用来记录对象与GC相关的一些标志位。其中0和1位用来表示对象的white状态和垃圾状态。当垃圾回收的标识阶段结束后，剩下的white对象就是垃圾对象。由于lua并不是立即清除这些垃圾对象，而是一步步逐渐清除，所以这些对象还会在系统中存在一段时间。这就需要我们能够区分出同样为white状态的垃圾对象和非垃圾对象。Lua使用两个标志位来表示white，就是为了高效的解决这个问题。这个标志位会轮流被当作white状态标志，另一个表示垃圾状态。在global_State中保存着一个currentwhite，来表示当前是那个标志位用来标识white。每当GC标识阶段完成，系统会切换这个标志位，这样原来为white的所有对象不需要遍历就变成了垃圾对象，而真正的white对象则使用新的标志位标识。</p>
<p>第2个标志位用来表示black状态，而既非white也非black就是gray状态。</p>
<p>除了short string和open upvalue之外，所有的GCObject都通过next被串接到全局状态global_State中的allgc链表上。我们可以通过遍历allgc链表来访问系统中的所有GCObject。short string被字符串标单独管理。open upvalue会在被close时也连接到allgc上。</p>
<h1 id="引用关系"><a href="#引用关系" class="headerlink" title="引用关系"></a>引用关系</h1><p>垃圾回收过程通过对象之间的引用关系来标识对象。以下是lua对象之间在垃圾回收标识过程中需要遍历的引用关系：<br><img src="https://img-my.csdn.net/uploads/201304/07/1365325183_1984.png" alt></p>
<p>所有字符串对象，无论是长串还是短串，都没有对其他对象的引用。</p>
<p>usedata对象会引用到一个metatable和一个env table。</p>
<p>Upval对象通过v引用一个TValue，再通过这个TValue间接引用一个对象。在open状态下，这个v指向stack上的一个TValue。在close状态下，v指向Upval自己的TValue。</p>
<p>Table对象会通过key，value引用到其他对象，并且如果数组部分有效，也会通过数组部分引用。并且，table会引用一个metatable对象。</p>
<p>Lua closure会引用到Proto对象，并且会通过upvalues数组引用到Upval对象。</p>
<p>C closure会通过upvalues数组引用到其他对象。这里的upvalue与lua closure的upvalue完全不是一个意思。</p>
<p>Proto对象会引用到一些编译期产生的名称，常量，以及内嵌于本Proto中的Proto对象。</p>
<p>Thread对象通过stack引用其他对象。</p>
<h1 id="barrier"><a href="#barrier" class="headerlink" title="barrier"></a>barrier</h1><p>在《 原理》中我们说过，incremental gc在mark阶段，为了保证“所有的black对象都不会引用white对象”这个不变性，需要使用barrier。<br>barrier被分为“向前”和“向后”两种。</p>
<p>luaC_barrier_函数用来实现“向前”的barrier。“向前”的意思就是当一个black对象需要引用一个white对象时，立即mark这个white对象。这样white对象就变为gray对象，等待下一步的扫描。这也就是帮助gc向前标识一步。luaC_barrier_函数被用在以下引用变化处：</p>
<ul>
<li>虚拟机执行过程中或者通过api修改close upvalue对其他对象的引用</li>
<li>通过api设置userdata或table的metatable引用</li>
<li>通过api设置userdata的env table引用</li>
<li>编译构建proto对象过程中proto对象对其他编译产生对象的引用</li>
</ul>
<p>luaC_barrierback_函数用来实现“向后”的barrier。“向后”的意思就是当一个black对象需要引用一个white对象时，将已经扫描过的black对象再次变为gray对象，等待重新扫描。这也就是将gc的mark后退一步。luaC_barrierback_目前只用于监控table的key和value对象引用的变化。Table是lua中最主要的数据结构，连全局变量都是被保存在一个table中，所以table的变化是比较频繁的，并且同一个引用可能被反复设置成不同的对象。对table的引用使用“向前”的barrier，逐个扫描每次引用变化的对象，会造成很多不必要的消耗。而使用“向后”的barrier就等于将table分成了“未变”和“已变”两种状态。只要一个table改变了一次，就将其变成gray，等待重新扫描。被变成gray的table在被重新扫描之前，无论引用再发生多少次变化也都无关紧要了。<br>引用关系变化最频繁的要数thread对象了。thread通过stack引用其他对象，而stack作为运行期栈，在一直不停地被修改。如果要监控这些引用变化，肯定会造成执行效率严重下降。所以lua并没有在所有的stack引用变化处加入barrier，而是直接假设stack就是变化的。所以thread对象就算被扫描完成，也不会被设置成black，而是再次设置成gray，等待再次扫描。</p>
<h1 id="Upvalue"><a href="#Upvalue" class="headerlink" title="Upvalue"></a>Upvalue</h1><p>Upvalue对象在垃圾回收中的处理是比较特殊的。<br>对于open状态的upvalue，其v指向的是一个stack上的TValue，所以open upvalue与thread的关系非常紧密。引用到open upvalue的只可能是其从属的thread，以及lua closure。如果没有lua closure引用这个open upvalue，就算他一定被thread引用着，也已经没有实际的意义了，应该被回收掉。也就是说thread对open upvalue的引用完全是一个弱引用。所以Lua没有将open upvalue当作一个独立的可回收对象，而是将其清理工作交给从属的thread对象来完成。在mark过程中，open upvalue对象只使用white和gray两个状态，来代表是否被引用到。通过上面的引用关系可以看到，有可能引用open upvalue的对象只可能被lua closure引用到。所以一个gray的open upvalue就代表当前有lua closure正在引用他，而这个lua closure不一定在这个thread的stack上面。在清扫阶段，thread对象会遍历所有从属于自己的open upvalue。如果不是gray，就说明当前没有lua closure引用这个open upvalue了，可以被销毁。</p>
<p>当退出upvalue的语法域或者thread被销毁，open upvalue会被close。所有close upvalue与thread已经没有弱引用关系，会被转化为一个普通的可回收对象，和其他对象一样进行独立的垃圾回收。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua5.2内部实现:Garbage Collection(1) 原理</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-GC1-%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8558103" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>Lua5.2采用垃圾回收机制对所有的lua对象(GCObject)进行管理。Lua虚拟机会定期运行GC，释放掉已经不再被被引用到的lua对象。</p>
<h1 id="基本算法"><a href="#基本算法" class="headerlink" title="基本算法"></a>基本算法</h1><p>基本的垃圾回收算法被称为”mark-and-sweep”算法。算法本身其实很简单。</p>
<p>首先，系统管理着所有已经创建了的对象。每个对象都有对其他对象的引用。root集合代表着已知的系统级别的对象引用。我们从root集合出发，就可以访问到系统引用到的所有对象。而没有被访问到的对象就是垃圾对象，需要被销毁。</p>
<p>我们可以将所有对象分成三个状态：</p>
<ol>
<li>White状态，也就是待访问状态。表示对象还没有被垃圾回收的标记过程访问到。</li>
<li>Gray状态，也就是待扫描状态。表示对象已经被垃圾回收访问到了，但是对象本身对于其他对象的引用还没有进行遍历访问。</li>
<li>Black状态，也就是已扫描状态。表示对象已经被访问到了，并且也已经遍历了对象本身对其他对象的引用。<br>基本的算法可以描述如下：<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">当前所有对象都是White状态;</span><br><span class="line">将root集合引用到的对象从White设置成Gray，并放到Gray集合中;</span><br><span class="line"><span class="keyword">while</span>(Gray集合不为空)</span><br><span class="line">&#123;</span><br><span class="line">	从Gray集合中移除一个对象O，并将O设置成Black状态;</span><br><span class="line">	<span class="keyword">for</span>(O中每一个引用到的对象O1) &#123;</span><br><span class="line">		<span class="keyword">if</span>(O1在White状态) &#123;</span><br><span class="line">			将O1从White设置成Gray，并放到到Gray集合中；</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span>(任意一个对象O)&#123;</span><br><span class="line">	<span class="keyword">if</span>(O在White状态)</span><br><span class="line">		销毁对象O;</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		将O设置成White状态;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h1 id="Incremental-Garbage-Collection"><a href="#Incremental-Garbage-Collection" class="headerlink" title="Incremental Garbage Collection"></a>Incremental Garbage Collection</h1><p>上面的算法如果一次性执行，在对象很多的情况下，会执行很长时间，严重影响程序本身的响应速度。其中一个解决办法就是，可以将上面的算法分步执行，这样每个步骤所耗费的时间就比较小了。我们可以将上述算法改为以下下几个步骤。<br>首先标识所有的root对象：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">当前所有对象都是White状态;</span><br><span class="line">将root集合引用到的对象从White设置成Gray，并放到Gray集合中;</span><br><span class="line">``</span><br><span class="line">遍历访问所有的gray对象。如果超出了本次计算量上限，退出等待下一次遍历:</span><br><span class="line">```c</span><br><span class="line">&lt;p&gt;<span class="keyword">while</span>(Gray集合不为空,并且没有超过本次计算量的上限)&lt;/p&gt;&#123;</span><br><span class="line">	从Gray集合中移除一个对象O，并将O设置成Black状态;</span><br><span class="line">	<span class="keyword">for</span>(O中每一个引用到的对象O1) &#123;</span><br><span class="line">		<span class="keyword">if</span>(O1在White状态) &#123;</span><br><span class="line">			将O1从White设置成Gray，并放到到Gray集合中；</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>销毁垃圾对象：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(任意一个对象O)&#123;</span><br><span class="line">	<span class="keyword">if</span>(O在White状态)</span><br><span class="line">		销毁对象O;</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		将O设置成White状态;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>在每个步骤之间，由于程序可以正常执行，所以会破坏当前对象之间的引用关系。black对象表示已经被扫描的对象，所以他应该不可能引用到一个white对象。当程序的改变使得一个black对象引用到一个white对象时，就会造成错误。解决这个问题的办法就是设置barrier。barrier在程序正常运行过程中，监控所有的引用改变。如果一个black对象需要引用一个white对象，存在两种处理办法：</p>
<ol>
<li>将white对象设置成gray，并添加到gray列表中等待扫描。这样等于帮助整个GC的标识过程向前推进了一步。</li>
<li>将black对象该回成gray，并添加到gray列表中等待扫描。这样等于使整个GC的标识过程后退了一步。</li>
</ol>
<p>这种垃圾回收方式被称为”Incremental Garbage Collection”(简称为”IGC”，Lua所采用的就是这种方法。使用”IGC”并不是没有代价的。IGC所检测出来的垃圾对象集合比实际的集合要小，也就是说，有些在GC过程中变成垃圾的对象，有可能在本轮GC中检测不到。不过，这些残余的垃圾对象一定会在下一轮GC被检测出来，不会造成泄露。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua52内部实现:TString</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-TString/</url>
    <content><![CDATA[<p><a href="http://blog.csdn.net/yuanlin2008/article/details/8423923" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>Lua使用TString结构体代表一个字符串对象。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** Header for string value; string bytes follow the end of this structure</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> TString &#123;</span><br><span class="line">  L_Umaxalign dummy;  <span class="comment">/* ensures maximum alignment for strings */</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    CommonHeader;</span><br><span class="line">    lu_byte extra;  <span class="comment">/* reserved words for short strings; "has hash" for longs */</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> hash;</span><br><span class="line">    <span class="keyword">size_t</span> len;  <span class="comment">/* number of characters in string */</span></span><br><span class="line">  &#125; tsv;</span><br><span class="line">&#125; TString;</span><br></pre></td></tr></table></figure></p>
<p>hash用来记录字符串对应的哈希值，len用来记录字符串长度。</p>
<p>这个结构体其实只是字符串对象的头数据，后面紧跟着字符窜内容和一个’\0’结尾。字符串”abc”对应的TString对象应该如下图所示：</p>
<p><img src="https://img-my.csdn.net/uploads/201302/05/1360054355_6651.png" alt><br>字符串对象的最终大小应该是TString的大小+字符串长度+1。</p>
<p>TString对应的lua对象类型为LUA_TSTRING。根据字符串长度（luaconf.h中的LUAI_MAXSHORTLEN，默认为40）的不同，TString对象还被分成两种子类型：LUA_TSHRSTR（短字符串）和LUA_TLNGSTR（长字符串）。</p>
<p>对于短字符串，在实际使用中一般用来作为索引或者需要进行字符串比较。不同于其他的对象，Lua并不是将其连接到全局的allgc对象链表上，而是将其放到全局状态global_State中的字符串表中进行管理。这个字符串表是一个stringtable类型的全局唯一的哈希表。当需要创建一个短字符串对象时，会首先在这个表中查找已有对象。所有的短字符串都是全局唯一的，不会存在两个相同的短字符串对象。如果需要比较两个短字符串是否相等，只需要看他们指向的是否是同一个TString对象就可以了，速度非常快。如果短字符串对象的extra &gt; 0，表示这是一个系统保留的字符串。extra的值直接对应着词法分析时的一个token值，这样可以加速词法分析的速度。</p>
<p>对于长字符串，与短字符串相反，在实际使用中一般只是用来存储文本数据，很少需要比较或者索引。所以长字符串直接被挂接到allgc链表上当作普通的对象来处理。Lua不会对新创建的长字符串对象计算哈希值，也不保证长字符串对象的唯一性。当长字符串需要被用来当作索引时，会为其计算一次哈希值，并使用extra来记录是否已经为其计算了哈希值。</p>
<p>计算字符串的哈希值需要遍历字符串的每一个字符，如果字符串很长，会非常影响效率。对于大于等于32的字符串，Lua不会遍历每个字符，而是按照一定的间隔获取字符，最多遍历31次。这使得字符串的哈希值计算与字符串长度无关。</p>
<p>如果要将Lua部署到web上面来处理大量的基于文本的http请求，文本处理的安全性就变得尤为重要。为了防止Hash DoS攻击，字符串哈希值生成需要一个seed，这个seed是每次启动Lua时随机生成的。这就使不同的Lua实例对于同一个字符串会有不同的哈希值。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua52内部实现-编译系统2-跳转的处理</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E7%BC%96%E8%AF%91%E7%B3%BB%E7%BB%9F2-%E8%B7%B3%E8%BD%AC%E7%9A%84%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8518743" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>跳转用来控制程序的指令流程。Lua使用OP_JMP指令来执行一个跳转，有关OP_JMP的详细介绍，可以参见《虚拟机指令》。跳转可以分为条件跳转和非条件跳转。非条件跳转比较简单，我们可以先从这里入手。</p>
<h1 id="goto"><a href="#goto" class="headerlink" title="goto"></a>goto</h1><p>在Lua5.2中，goto和label是新加入的statement，用来执行非条件跳转。这两个statement分别在lparser.c中的gotostat和labelstat函数中被解析。上一篇中讲过，在全局数据Dyndata中，保存着一个goto列表和一个label列表，goto和label使用一个相同的数据结构Labeldesc表示。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* description of pending goto statements and label statements */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Labeldesc</span> &#123;</span></span><br><span class="line">  TString *name;  <span class="comment">/* label identifier */</span></span><br><span class="line">  <span class="keyword">int</span> pc;  <span class="comment">/* position in code */</span></span><br><span class="line">  <span class="keyword">int</span> <span class="built_in">line</span>;  <span class="comment">/* line where it appeared */</span></span><br><span class="line">  lu_byte nactvar;  <span class="comment">/* local level where it appears in current block */</span></span><br><span class="line">&#125; Labeldesc;</span><br></pre></td></tr></table></figure></p>
<p>name用来表示label的名称，用来相互查找。如果是label，pc表示这个label对应的当前函数指令集合的位置，也就是待跳转的指令位置；如果是goto，则代表为这个goto生成的OP_JMP指令的位置。nactvar代表解析此goto或者label时，函数有多少个有效的局部变量，用来在跳转时决定需要关闭哪些upvalue。</p>
<p>gotostat接受一个已经为之生成好了的OP_JMP指令的位置，首先通过newlabelentry为这个goto在ls-&gt;dyd-&gt;gt中生成一个Labeldesc，用来表示未处理的goto，然后调用findlabel尝试处理这个goto。findlabel会在当前的block中查找已经定义了的label。如果找到，就调用closegoto，将这个goto对应的OP_JMP指令的跳转位置设置成label的pc，并且还要决定是否需要在OP_JMP指令中关闭一些局部变量对应的upvalue；如果没有找到，这个goto对应的OP_JMP指令的跳转位置就是一个NO_JUMP，表示未决位置，等待后面再处理。</p>
<p>与gotostat的处理类似，labelstat在处理label时，也会首先查找已经定义但未决的goto。如果找到了goto，也要修改其OP_JMP指令的跳转位置。整个goto和label语法分析的代码比较直接，并不难理解。而这里比较晦涩的是Lua对OP_JMP指令的处理方法。对于OP_JMP的处理还会在后面的关系和逻辑运算，以及条件跳转中使用。所以，理解Lua对OP_JMP指令的处理是理解其他编译部分的基础。</p>
<h1 id="OP-JMP"><a href="#OP-JMP" class="headerlink" title="OP_JMP"></a>OP_JMP</h1><p>前面讲过，由于Lua是一编编译，所以在真正生成指令时，很多东西是没法决定的。比如OP_JMP指令，要跳转的位置可能还没有定义，所以不能知道具体的跳转位置。Lua会将这些指令先生成到函数proto的指令列表中，然后记录下他们在指令列表的位置。当可以确定时，再通过这个位置找到生成好的指令，对其进行修改。这就是指令回填。</p>
<p>对于一个OP_JMP指令，有两个指令参数需要回填：待关闭的upvalue起始id A和跳转偏移量sBx。当通过luaK_jump函数生成一个OP_JMP指令时，这个指令的A会被初始化成0，而sBx被初始化成NO_JUMP，并返回一个int代表这个OP_JMP指令的位置。我们保存这个返回值就可以找到这个指令，对其进行回填。</p>
<p>在语法分析过程中(比如后面要讲到的关系和逻辑运算)，有可能会生成一系列的OP_JMP指令，他们的跳转目标是一致的，从而形成一个跳转指令集合。Lua使用了链表的方式保存这种指令集合。这个链表每个节点就是OP_JMP指令本身，使用sBx来指向链表的下一个OP_JMP指令。如果sBx为NO_JUMP，表示链表的尾节点。所以我们只需要一个指向头节点的位置，就可以遍历整个OP_JMP指令链表。lcode.c中对于OP_JMP指令的处理函数，都是基于一个跳转链表的，只不过在一些情况下，链表中只有一个节点而已。如果需要回填一个具体的跳转地址，就会遍历链表，将每个节点的跳转地址都修正到目标位置。<br><img src="https://img-my.csdn.net/uploads/201301/21/1358741149_4644.png" alt><br>回填跳转地址可以分为两类处理：</p>
<p>一种是已经生成的指令位置，Lua会立即将这些跳转指令修改成目标位置。</p>
<p>另一种是当前位置，也就是接下来要生成的指令的位置。在处理当前位置时，Lua并没有立即修改，而是将待修改的跳转指令串接到当前FuncState中的jpc链表上。当使用luaK_code生成下一条指令时，首先会调用dischargejpc函数，将jpc链表上的所有跳转指令修改到这个位置。这等于是延迟回填。之所以这样处理，其实是为了跳转的优化。我们回头看一下luaK_jump函数，它在生成OP_JMP指令时，会将当前jpc串接到新生成的jpc上，并且将jpc清空。这个处理实际的意思是，当生成一个OP_JMP指令时，如果有其他的OP_JMP指令需要跳转到此处，其实就等于间接跳转到新生成的跳转指令的目标位置。所以这些跳转指令不用再跳转到此处，而是直接跳转到新生成的OP_JMP的目标位置，将两步跳转合并成一步。这些指令与新生成跳转指令的目标是一致的，所以可以合并成一个跳转指令集合，等待后面一起回填。</p>
<p>我们接下来在来回顾一下与跳转相关的指令生成api。</p>
<p>luaK_jump用来生成一个新的跳转指令。luaK_concat函数用来将两个链表连接起来合成一个链表。luaK_patchlist用来回填一个链表的跳转位置。luaK_patchtohere用来将一个链表准备回填到当前位置。luaK_patchclose用来回填一个链表中的A，也就是需要关闭的upvalue id，具体可以查看goto的处理。</p>
<p>以上是Lua处理跳转的相关内容。跳转的处理还与逻辑和关系表达式密切相关，我们会在后面表达式部分再进行详细讲解。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua52内部实现-虚拟机指令1概述</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E8%99%9A%E6%8B%9F%E6%9C%BA%E6%8C%87%E4%BB%A41%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8423951" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>Lua一直把虚拟机执行代码的效率作为一个非常重要的设计目标。而采用什么样的指令系统的对于虚拟机的执行效率来说至关重要。</p>
<p><strong>Stack based vs Register based VM</strong><br>根据指令获取操作数方式的不同，我们可以把虚拟机的实现分为stack based和register based。</p>
<p><strong>Stack based vm</strong><br>对于大多数的虚拟机，比如JVM，Python，都采用传统的stack based vm。<br>Stack based vm的指令一般都是在当前stack中获取和保存操作数的。比如一个简单的加法赋值运算:a=b+c，对于stack based vm，一般会被转化成如下的指令:<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">push b; // 将变量b的值压入stack</span><br><span class="line">push c; // 将变量c的值压入stack</span><br><span class="line">add;    // 将stack顶部的两个值弹出后相加，将结果压入stack</span><br><span class="line">mov a;  // 将stack顶部结果放到a中</span><br></pre></td></tr></table></figure></p>
<p>由于Stack based vm的指令都是基于当前stack来查找操作数的，这就相当于所有操作数的存储位置都是运行期决定的，在编译器的代码生成阶段不需要额外为在哪里存储操作数费心，所以stack based的编译器实现起来相对比较简单直接。也正因为这个原因，每条指令占用的存储空间也比较小。</p>
<p>但是，对于一个简单的运算，stack based vm会使用过多的指令组合来完成，这样就增加了整体指令集合的长度。vm会使用同样多的迭代次数来执行这些指令，这对于效率来说会有很大的影响。并且，由于操作数都要放到stack上面，使得移动这些操作数的内存复制大大增加，这也会影响到效率。</p>
<p><strong>Register based vm</strong><br>Lua 采用的是register based vm。</p>
<p>Register based vm的指令都是在已经分配好的寄存器中存取操作数。对于上面的运算，register based vm一般会使用如下的指令:<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">add a b c; // 将b与c对应的寄存器的值相加，将结果保存在a对应的寄存器中</span><br></pre></td></tr></table></figure></p>
<p>Register based vm的指令可以直接对应标准的3地址指令，用一条指令完成了上面多条指令的计算工作，并且有效地减少了内存复制操作。这样的指令系统对于效率有很大的帮助。</p>
<p>不过，在编译器设计上，就要在代码生成阶段对寄存器进行分配，增加了实现的复杂度。并且每条指令所占用的存储空间也相应的增加了。</p>
<p><strong>Lua虚拟机指令简介</strong><br>Lua的指令使用一个32bit的unsigned integer表示。所有指令的定义都在lopcodes.h文件中，使用一个enum OpCode代表指令类型。在lua5.2中，总共有40种指令(id从0到39)。根据指令参数的不同，可以将所有指令分为4类:</p>
<p><img src="https://img-my.csdn.net/uploads/201301/10/1357792149_6544.png" alt><br>除了sBx之外，所有的指令参数都是unsigned integer类型。sBx可以表示负数，但表示方法比较特殊。sBx的18bit可表示的最大整数为262143，这个数的一半131071用来表示0，所以-1可以表示为-1+131071，也就是131070，而+1可以表示为+1+131071，也就是131072。</p>
<p>ABC一般用来存放指令操作数据的地址，而地址可以分成3种：</p>
<ul>
<li>寄存器id</li>
<li>常量表id</li>
<li>upvalue id<br>Lua使用当前函数的stack作为寄存器使用，寄存器id从0开始。当前函数的stack与寄存器数组是相同的概念。stack(n)其实就是register(n)。<br>每一个函数prototype都有一个属于本函数的常量表，用于存放编译过程中函数所用到的常量。常量表可以存放nil,boolean,number和string类型的数据，id从1开始。</li>
</ul>
<p>每一个函数prototype中都有一个upvalue描述表，用于存放在编译过程中确定的本函数所使用的upvalue的描述。在运行期，通过OP_CLOSURE指令创建一个closure时，会根据prototype中的描述，为这个closure初始化upvalue表。upvalue本身不需要使用名称，而是通过id进行访问。</p>
<p>A被大多数指令用来指定计算结果的目标寄存器地址。很多指令使用B或C同时存放寄存器地址和常量地址，并通过最左面的一个bit来区分。在指令生成阶段，如果B或C需要引用的常量地址超出了表示范围，则首先会生成指令将常量装载到临时寄存器，然后再将B或C改为使用该寄存器地址。</p>
<p>在lopcodes.h中，对于每个指令，在源码注释中都有简单的操作描述。本文接下来将针对每一个指令做更详细的描述，并给出关于这个指令的示例代码。示例代码可以帮助我们构建出一个指令使用的具体上下文，有助于进一步理解指令的作用。对指令上下文的理解还可以作为进一步研究lua的编译和代码生成系统的基础。</p>
<p>在分析过程中，我们使用luac来显示示例代码所生成的指令。luac的具体使用方式为:<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">luac -l -l test.lua</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua5.2内部实现:虚拟机指令(2) MOVE &amp; LOAD</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E8%99%9A%E6%8B%9F%E6%9C%BA%E6%8C%87%E4%BB%A42/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8491112" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>| name      | args    | desc        |<br>| OP_MOVE |    A B        | R(A) := R(B) |<br>OP_MOVE用来将寄存器B中的值拷贝到寄存器A中。由于Lua是register based vm，大部分的指令都是直接对寄存器进行操作，而不需要对数据进行压栈和弹栈，所以需要OP_MOVE指令的地方并不多。最直接的使用之处就是将一个local变量复制给另一个local变量时:<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a;</span><br><span class="line"><span class="keyword">local</span> b = a;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1	[1]	LOADNIL  	0 0</span><br><span class="line">2	[2]	MOVE     	1 0</span><br><span class="line">3	[2]	RETURN   	0 1</span><br></pre></td></tr></table></figure>
<p>在编译过程中，Lua会将每个local变量都分配到一个指定的寄存器中。在运行期，lua使用local变量所对应的寄存器id来操作local变量，而local变量的名字除了提供debug信息外，没有其他作用。</p>
<p>在这里a被分配给register 0，b被分配给register 1。第二行的MOVE表示将a(register 0)的值赋给b(register 1)。其他使用的地方基本都是对寄存器的位置有特殊要求的地方，比如函数参数的传递等等。</p>
<p>name    args    desc<br>OP_LOADK    A Bx    R(A) := Kst(Bx)<br>LOADK将Bx表示的常量表中的常量值装载到寄存器A中。很多其他指令，比如数学操作指令，其本身可以直接从常量表中索引操作数，所以可以不依赖于LOADK指令。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a=<span class="number">1</span>;</span><br><span class="line"><span class="keyword">local</span> b=<span class="string">"foo"</span>;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line">	<span class="number">1</span>	<span class="string">[1]</span>	<span class="string">LOADK</span>    	<span class="number">0</span> <span class="number">-1</span>	<span class="string">;</span> <span class="number">1</span></span><br><span class="line">	<span class="number">2</span>	<span class="string">[2]</span>	<span class="string">LOADK</span>    	<span class="number">1</span> <span class="number">-2</span>	<span class="string">;</span> <span class="string">"foo"</span></span><br><span class="line">	<span class="number">3</span>	<span class="string">[2]</span>	<span class="string">RETURN</span>   	<span class="number">0</span> <span class="number">1</span></span><br><span class="line"><span class="string">constants</span> <span class="string">(2)</span> <span class="attr">for 0x80048eb0:</span></span><br><span class="line">	<span class="number">1</span>	<span class="number">1</span></span><br><span class="line">	<span class="number">2</span>	<span class="string">"foo"</span></span><br></pre></td></tr></table></figure>
<p>name    args    desc<br>OP_LOADKX    A    R(A) := Kst(extra arg)<br>LOADKX是lua5.2新加入的指令。当需要生成LOADK指令时，如果需要索引的常量id超出了Bx所能表示的有效范围，那么就生成一个LOADKX指令，取代LOADK指令，并且接下来立即生成一个EXTRAARG指令，并用其Ax来存放这个id。5.2的这个改动使得一个函数可以处理超过262143个常量。</p>
<p>name    args    desc<br>OP_LOADBOOL    A B C    R(A) := (Bool)B; if (C) pc++<br>LOADBOOL将B所表示的boolean值装载到寄存器A中。B使用0和1分别代表false和true。C也表示一个boolean值，如果C为1，就跳过下一个指令。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">local</span> a = <span class="literal">true</span>;</span><br><span class="line">	1	[1]	LOADBOOL 	0 1 0</span><br><span class="line">	2	[1]	RETURN   	0 1</span><br></pre></td></tr></table></figure></p>
<p>C在这里的作用比较特殊。要了解C的具体用处，首先要知道lua中对于逻辑和关系表达式是如何处理的，比如：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = <span class="number">1</span> &lt; <span class="number">2</span></span><br></pre></td></tr></table></figure></p>
<p>对于上面的代码，一般我们会认为lua应该先对1&lt;2求出一个boolean值，然后放入到a中。然而实际上产生出来的代码为：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">	1	[1]	LT       	1 -1 -2	; 1 2</span><br><span class="line">	2	[1]	JMP      	0 1	; to 4</span><br><span class="line">	3	[1]	LOADBOOL 	0 0 1</span><br><span class="line">	4	[1]	LOADBOOL 	0 1 0</span><br><span class="line">	5	[1]	RETURN   	0 1</span><br><span class="line">constants (2) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">	1	1</span><br><span class="line">	2	2</span><br></pre></td></tr></table></figure></p>
<p>可以看到，lua生成了LT和JMP指令，另外再加上两个LOADBOOL对于a赋予不同的boolean值。LT（后面会详细讲解）指令本身并不产生一个boolean结果值，而是配合后面紧跟的JMP实现true和false的不同跳转。如果LT评估为true，就继续执行，也就是执行到JMP，然后调转到4，对a赋予true；否则就跳过下一条指令到达第三行，对a赋予false，并且跳过下一个指令。所以上面的代码实际的意思被转化为：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a;</span><br><span class="line"><span class="keyword">if</span> <span class="number">1</span> &lt; <span class="number">2</span> <span class="keyword">then</span></span><br><span class="line">    a = <span class="literal">true</span>;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    a = <span class="literal">false</span>;</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p>
<p>逻辑或者关系表达式之所以被设计成这个样子，主要是为if语句和循环语句所做的优化。不用将整个表达式估值成一个boolean值后再决定跳转路径，而是评估过程中就可以直接跳转，节省了很多指令。<br>C的作用就是配合这种使用逻辑或关系表达式进行赋值的操作，他节省了后面必须跟的一个JMP指令。</p>
<p>name    args    desc<br>OP_LOADNIL    A B    R(A), R(A+1), …, R(A+B) := nil<br>LOADNIL将使用A到B所表示范围的寄存器赋值成nil。用范围表示寄存器主要为了对以下情况进行优化：</p>
<p>local a,b,c;<br>    1    [1]    LOADNIL      0 2<br>    2    [1]    RETURN       0 1<br>对于连续的local变量声明，使用一条LOADNIL指令就可以完成，而不需要分别进行赋值。</p>
<p>对于一下情况<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a;</span><br><span class="line"><span class="keyword">local</span> b = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">local</span> c;</span><br><span class="line">	<span class="number">1</span>	[<span class="number">1</span>]	LOADNIL  	<span class="number">0</span> <span class="number">0</span></span><br><span class="line">	<span class="number">2</span>	[<span class="number">2</span>]	LOADK    	<span class="number">1</span> <span class="number">-1</span>	; <span class="number">0</span></span><br><span class="line">	<span class="number">3</span>	[<span class="number">3</span>]	LOADNIL  	<span class="number">2</span> <span class="number">0</span></span><br></pre></td></tr></table></figure></p>
<p>在Lua5.2中，a和c不能被合并成一个LOADNIL指令。所以以上写法理论上会生成更多的指令，应该予以避免，而改写成<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a,c;</span><br><span class="line"><span class="keyword">local</span> b = <span class="number">0</span>;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua5.2内部实现:虚拟机指令(3) Upvalues &amp; Globals</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E8%99%9A%E6%8B%9F%E6%9C%BA%E6%8C%87%E4%BB%A43/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8491144" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>在编译期，如果要访问变量a时，会依照以下的顺序决定变量a的类型：</p>
<ol>
<li>a是当前函数的local变量</li>
<li>a是外层函数的local变量，那么a是当前函数的upvalue</li>
<li>a是全局变量<br>local变量本身就存在于当前的register中，所有的指令都可以直接使用它的id来访问。而对于upvalue，lua则有专门的指令负责获取和设置。</li>
</ol>
<p>全局变量在lua5.1中也是使用专门的指令，而5.2对这一点做了改变。Lua5.2种没有专门针对全局变量的指令，而是把全局表放到最外层函数的名字为”_ENV”的upvalue中。对于全局变量a，相当于编译期帮你改成了_ENV.a来进行访问。</p>
<p>name    args    desc<br>OP_GETUPVAL    A B C    R(A) := UpValue[B]<br>OP_SETUPVAL    A B    UpValue[B] := R(A)<br>OP_GETTABUP    A B C    R(A) := UpValue[B][RK(C)]<br>OP_SETTABUP    A B C    UpValue[A][RK(B)] := RK(C)<br>GETUPVAL将B为索引的upvalue的值装载到A寄存器中。SETUPVAL将A寄存器的值保存到B为索引的upvalue中。</p>
<p>GETTABUP将B为索引的upvalue当作一个table，并将C做为索引的寄存器或者常量当作key获取的值放入寄存器A。SETTABUP将A为索引的upvalue当作一个table，将C寄存器或者常量的值以B寄存器或常量为key，存入table。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> u = <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span><span class="params">()</span></span> </span><br><span class="line">	<span class="keyword">local</span> l;</span><br><span class="line">	u = <span class="number">1</span>; </span><br><span class="line">	l = u;</span><br><span class="line">	g = <span class="number">1</span>;</span><br><span class="line">	l = g;</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">main &lt;test.lua:0,0&gt; (4 instructions at 0x80048eb0)</span><br><span class="line">0+ params, 2 slots, 1 upvalue, 1 <span class="built_in">local</span>, 2 constants, 1 <span class="keyword">function</span></span><br><span class="line">	1	[1]	LOADK    	0 -1	; 0</span><br><span class="line">	2	[8]	CLOSURE  	1 0	; 0x80049140</span><br><span class="line">	3	[2]	SETTABUP 	0 -2 1	; _ENV <span class="string">"f"</span></span><br><span class="line">	4	[8]	RETURN   	0 1</span><br><span class="line">constants (2) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">	1	0</span><br><span class="line">	2	<span class="string">"f"</span></span><br><span class="line">locals (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">	0	u	2	5</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">	0	_ENV	1	0</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> &lt;test.lua:2,8&gt; (7 instructions at 0x80049140)</span><br><span class="line">0 params, 2 slots, 2 upvalues, 1 <span class="built_in">local</span>, 2 constants, 0 <span class="built_in">functions</span></span><br><span class="line">	1	[3]	LOADNIL  	0 0</span><br><span class="line">	2	[4]	LOADK    	1 -1	; 1</span><br><span class="line">	3	[4]	SETUPVAL 	1 0	; u</span><br><span class="line">	4	[5]	GETUPVAL 	0 0	; u</span><br><span class="line">	5	[6]	SETTABUP 	1 -2 -1	; _ENV <span class="string">"g"</span> 1</span><br><span class="line">	6	[7]	GETTABUP 	0 1 -2	; _ENV <span class="string">"g"</span></span><br><span class="line">	7	[8]	RETURN   	0 1</span><br><span class="line">constants (2) <span class="keyword">for</span> 0x80049140:</span><br><span class="line">	1	1</span><br><span class="line">	2	<span class="string">"g"</span></span><br><span class="line">locals (1) <span class="keyword">for</span> 0x80049140:</span><br><span class="line">	0	l	2	8</span><br><span class="line">upvalues (2) <span class="keyword">for</span> 0x80049140:</span><br><span class="line">	0	u	1	0</span><br><span class="line">	1	_ENV	0	0</span><br></pre></td></tr></table></figure>
<p>上面的代码片段生成一个主函数和一个内嵌函数。根据前面说到的变量规则，在内嵌函数中，l是local变量，u是upvalue，g由于既不是local变量，也不是upvalue，当作全局变量处理。我们先来看内嵌函数，生成的指令从17行开始。<br>第17行的LOADNIL前面已经讲过，为local变量赋值。下面的LOADK和SETUPVAL组合，完成了u = 1。因为1是一个常量，存在于常量表中，而lua没有常量与upvalue的直接操作指令，所以需要先把常量1装在到临时寄存器1种，然后将寄存器1的值赋给upvalue 0，也就是u。第20行的GETUPVAL将upvalue u赋给local变量l。第21行开始的SETTABUP和GETTABUP就是前面提到的对全局变量的处理了。g=1被转化为_ENV.g=1。_ENV是系统预先设置在主函数中的upvalue，所以对于全局变量g的访问被转化成对upvalue[_ENV][g]的访问。SETTABUP将upvalue 1(_ENV代表的upvalue)作为一个table，将常量表2（常量”g”）作为key的值设置为常量表1（常量1）；GETTABUP则是将upvalue 1作为table，将常量表2为key的值赋给寄存器0（local l）</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua5.2内部实现:虚拟机指令(5）Arithmetic</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E8%99%9A%E6%8B%9F%E6%9C%BA%E6%8C%87%E4%BB%A45/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/pyf09/article/details/92852103" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>name    args    desc<br>OP_ADD    A B C    R(A) := RK(B) + RK(C)<br>OP_SUB    A B C    R(A) := RK(B) - RK(C)<br>OP_MUL    A B C    R(A) := RK(B) * RK(C)<br>OP_DIV    A B C    R(A) := RK(B) / RK(C)<br>OP_MOD    A B C    R(A) := RK(B) % RK(C)<br>OP_POW    A B C    R(A) := RK(B) ^ RK(C)<br>上表中的指令都是与lua本身的二元操作符一一对应的标准3地址指令。B和C两个操作数计算的结果存入A中。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = <span class="number">1</span>;</span><br><span class="line">a = a + <span class="number">1</span>;</span><br><span class="line">a = a - <span class="number">1</span>;</span><br><span class="line">a = a * <span class="number">1</span>;</span><br><span class="line">a = a / <span class="number">1</span>;</span><br><span class="line">a = a % <span class="number">1</span>;</span><br><span class="line">a = a ^ <span class="number">1</span>;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">main &lt;test.lua:0,0&gt; (8 instructions at 0x80048eb0)</span><br><span class="line">0+ params, 2 slots, 1 upvalue, 1 <span class="built_in">local</span>, 1 constant, 0 <span class="built_in">functions</span></span><br><span class="line">    1    [1]    LOADK        0 -1    ; 1</span><br><span class="line">    2    [2]    ADD          0 0 -1    ; - 1</span><br><span class="line">    3    [3]    SUB          0 0 -1    ; - 1</span><br><span class="line">    4    [4]    MUL          0 0 -1    ; - 1</span><br><span class="line">    5    [5]    DIV          0 0 -1    ; - 1</span><br><span class="line">    6    [6]    MOD          0 0 -1</span><br><span class="line">    7    [7]    POW          0 0 -1    ; - 1</span><br><span class="line">    8    [7]    RETURN       0 1</span><br><span class="line">constants (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">    1    1</span><br><span class="line">locals (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">    0    a    2    9</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">    0    _ENV    1    0</span><br></pre></td></tr></table></figure>
<p>可以看到，生成的指令没有多余的操作，每个指令都对应一个完整的二元计算操作。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name    args    desc</span><br><span class="line">OP_UNM    A B    R(A) := -R(B)</span><br><span class="line">OP_NOT    A B    A B R(A) := not R(B)</span><br></pre></td></tr></table></figure></p>
<p>上表中指令对应’-‘和’not’一元操作符，表示将B取反或not后放入A中。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">local</span> b = <span class="keyword">not</span> a;</span><br><span class="line"><span class="keyword">local</span> c = -a;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    LOADK        0 -1    ; 1</span><br><span class="line">2    [2]    NOT          1 0</span><br><span class="line">3    [3]    UNM          2 0</span><br><span class="line">4    [3]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>在编译和指令生成阶段，lua还支持所有一元和二元操作符表达式的常量表达式折叠”（const expression folding）优化。也就是如果计算操作数如果都是数字常量，可以在编译期计算出结果，就直接使用这个结果值，而不用生成计算指令。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = <span class="number">1</span> + <span class="number">1</span>;</span><br><span class="line"><span class="keyword">local</span> b = <span class="keyword">not</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    LOADK        0 -1    ; 2</span><br><span class="line">2    [2]    LOADBOOL     1 0 0</span><br><span class="line">3    [2]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>从生成的结果可以看到1+1并没有生成对应的OP_ADD，而是直接把结果2赋值给了a。并且也没有为not 1生成OP_NOT指令，而是直接将false赋值给了b。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">name    args    desc</span><br><span class="line">OP_LEN    A B    R(A) := length of R(B)</span><br></pre></td></tr></table></figure></p>
<p>LEN直接对应’#’操作符，返回B对象的长度，并保存到A中。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = #<span class="string">"foo"</span>;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    LOADK        0 -1    ; <span class="string">"foo"</span></span><br><span class="line">2    [1]    LEN          0 0</span><br><span class="line">3    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name    args    desc</span><br><span class="line">OP_CONCAT    A B C    R(A) := R(B).. ... ..R(C)</span><br></pre></td></tr></table></figure>
<p>CONCAT将B和C指定范围内的字符串按顺序传接到一起，将结果存入到A。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = <span class="string">"foo1"</span>..<span class="string">"foo2"</span>..<span class="string">"foo3"</span>;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    LOADK        0 -1    ; <span class="string">"foo1"</span></span><br><span class="line">2    [1]    LOADK        1 -2    ; <span class="string">"foo2"</span></span><br><span class="line">3    [1]    LOADK        2 -3    ; <span class="string">"foo3"</span></span><br><span class="line">4    [1]    CONCAT       0 0 2</span><br><span class="line">5    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua5.2内部实现:虚拟机指令(6）FUNCTION</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E8%99%9A%E6%8B%9F%E6%9C%BA%E6%8C%87%E4%BB%A46/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8494636" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<blockquote>
<p>name    args    desc<br>OP_CALL    A B C    A B C   R(A), … ,R(A+C-2) := R(A)(R(A+1), … ,R(A+B-1))<br>CALL执行一个函数调用。寄存器A中存放函数对象，所有参数按顺序放置在A后面的寄存器中。B－1表示参数个数 。如果参数列表的最后一个表达式是变长的，则B会设置为0，表示使用A＋1到当前栈顶作为参数。函数调用的返回值会按顺序存放在从寄存器A开始的C-1个寄存器中。如果C为0,表示返回值的个数由函数决定。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">f();</span><br></pre></td></tr></table></figure></p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    GETTABUP     0 0 -1    ; _ENV <span class="string">"f"</span></span><br><span class="line">2    [1]    CALL         0 1 1</span><br><span class="line">3    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>第一行取得全局变量f的值，保存到寄存器0。第二行CALL调用寄存器0中的函数，参数和返回值都是0。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> t = &#123;f(...)&#125;;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    NEWTABLE     0 0 0</span><br><span class="line">2    [1]    GETTABUP     1 0 -1    ; _ENV <span class="string">"f"</span></span><br><span class="line">3    [1]    VARARG       2 0</span><br><span class="line">4    [1]    CALL         1 0 0</span><br><span class="line">5    [1]    SETLIST      0 0 1    ; 1</span><br><span class="line">6    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>第一行NETTABLE创建一个表放到寄存器0中。第二行获取全局变量f放到寄存器1中。第三行VARARG表示使用当前函数的变长参数列表。第四行的CALL调用寄存器1中的函数，B为0，代表参数是变长的。前面讲过，如果表的构造的最后一项是多返回值的表达式，则这个表会接受所有的返回值。这里就是这种情况，表的构造会接受函数所有的返回值，所以C也为0。</p>
<p>name    args    desc<br>OP_TAILCALL    A B C    A B C   return R(A)(R(A+1), … ,R(A+B-1))<br>如果一个return statement只有一个函数调用表达式，这个函数调用指令CALL会被改为TAILCALL指令。TAILCALL不会为要调用的函数增加调用堆栈的深度，而是直接使用当前调用信息。ABC操作数与CALL的意思一样，不过C永远都是0。TAILCALL在执行过程中，只对lua closure进行tail call处理，对于c closure，其实与CALL没什么区别。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> f();</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    GETTABUP     0 0 -1    ; _ENV <span class="string">"f"</span></span><br><span class="line">2    [1]    TAILCALL     0 1 0</span><br><span class="line">3    [1]    RETURN       0 0</span><br><span class="line">4    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>上面如果f是一个lua closure，那么执行到第二行后，此函数就会返回了，不会执行到后面第三行的RETURN。如果f是一个c closure，那就和CALL一样调用这个函数，然后依赖第三行的RETURN返回。这就是为什么TAILCALL后面还会己跟着生成一个RETURN的原因。</p>
<p>name    args    desc<br>OP_RETURN    A B    return R(A), … ,R(A+B-2)<br>RETURE将返回结果存放到寄存器A到寄存器A＋B－2中。如果返回的为变长表达式，则B会被设置为0，表示将寄存器A到当前栈顶的所有值返回。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    LOADK        0 -1    ; 1</span><br><span class="line">2    [1]    RETURN       0 2</span><br><span class="line">3    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>RETURN只能从寄存器返回数据，所以第一行LOADK先将常量1装载道寄存器0，然后返回。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> ...;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    VARARG       0 0</span><br><span class="line">2    [1]    RETURN       0 0</span><br><span class="line">3    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>因为返回的为变长表达式，B为0。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name    args    desc</span><br><span class="line">OP_CLOSURE    A Bx    R(A) := closure(KPROTO[Bx])</span><br><span class="line">CLOSURE为指定的函数prototype创建一个closure，并将这个closure保存到寄存器A中。Bx用来指定函数prototype的id。</span><br><span class="line"></span><br><span class="line"><span class="built_in">local</span> <span class="keyword">function</span> f()</span><br><span class="line">end</span><br><span class="line">main &lt;test.lua:0,0&gt; (2 instructions at 0x102a016f0)</span><br><span class="line">0+ params, 2 slots, 1 upvalue, 1 <span class="built_in">local</span>, 0 constants, 1 <span class="keyword">function</span></span><br><span class="line">    1    [2]    CLOSURE      0 0    ; 0x102a019b0</span><br><span class="line">    2    [2]    RETURN       0 1</span><br><span class="line">constants (0) <span class="keyword">for</span> 0x102a016f0:</span><br><span class="line">locals (1) <span class="keyword">for</span> 0x102a016f0:</span><br><span class="line">    0    f    2    3</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0x102a016f0:</span><br><span class="line">    0    _ENV    1    0</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> &lt;test.lua:1,2&gt; (1 instruction at 0x102a019b0)</span><br><span class="line">0 params, 2 slots, 0 upvalues, 0 locals, 0 constants, 0 <span class="built_in">functions</span></span><br><span class="line">    1    [2]    RETURN       0 1</span><br><span class="line">constants (0) <span class="keyword">for</span> 0x102a019b0:</span><br><span class="line">locals (0) <span class="keyword">for</span> 0x102a019b0:</span><br><span class="line">upvalues (0) <span class="keyword">for</span> 0x102a019b0:</span><br></pre></td></tr></table></figure></p>
<p>上面生成了一个主函数和一个子函数，CLOSURE将为这个索引为0的子函数生成一个closure，并保存到寄存器0中。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name    args    desc</span><br><span class="line">OP_VARARG    A B    R(A), R(A+1), ..., R(A+B-2) = vararg</span><br></pre></td></tr></table></figure>
<p>VARARG直接对应’…’运算符。VARARG拷贝B-1个参数到从A开始的寄存器中，如果不足，使用nil补充。如果B为0，表示拷贝实际的参数数量。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = ...;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    VARARG       0 2</span><br><span class="line">2    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>上面第一行表示拷贝B-1个，也就是1个变长参数到寄存器0,也就是local a中。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">f(...);</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    GETTABUP     0 0 -1    ; _ENV <span class="string">"f"</span></span><br><span class="line">2    [1]    VARARG       1 0</span><br><span class="line">3    [1]    CALL         0 0 1</span><br><span class="line">4    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>由于函数调用最后一个参数可以接受不定数量的参数，所以第二行生成的VARARG的B参数为0。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name    args    desc</span><br><span class="line">OP_SELF    A B C    </span><br><span class="line">R(A+1) := R(B); R(A) := R(B)[RK(C)]</span><br></pre></td></tr></table></figure>
<p>SELF是专门为“:”运算符准备的指令。从寄存器B表示的table中，获取出C作为key的closure，存入寄存器A中，然后将table本身存入到寄存器A＋1中，为接下来调用这个closure做准备。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">a:b();</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    GETTABUP     0 0 -1    ; _ENV <span class="string">"a"</span></span><br><span class="line">2    [1]    SELF         0 0 -2    ; <span class="string">"b"</span></span><br><span class="line">3    [1]    CALL         0 2 1</span><br><span class="line">4    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>看一下与上面语法等价的表示方法生成的指令：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">a.b(a);</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1    [1]    GETTABUP     0 0 -1    ; _ENV <span class="string">"a"</span></span><br><span class="line">2    [1]    GETTABLE     0 0 -2    ; <span class="string">"b"</span></span><br><span class="line">3    [1]    GETTABUP     1 0 -1    ; _ENV <span class="string">"a"</span></span><br><span class="line">4    [1]    CALL         0 2 1</span><br><span class="line">5    [1]    RETURN       0 1</span><br></pre></td></tr></table></figure>
<p>比使用“:”操作符多使用了一个指令。所以，如果需要使用这种面向对象调用的语义时，应该尽量使用”:”。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua5.2内部实现:虚拟机指令(4) Table</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E8%99%9A%E6%8B%9F%E6%9C%BA%E6%8C%87%E4%BB%A44/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8493104" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>name    args    desc<br>OP_NEWTABLE    A B C    R(A) := {} (size = B,C)<br>NEWTABLE在寄存器A处创建一个table对象。B和C分别用来存储这个table数组部分和hash部分的初始大小。初始大小是在编译期计算出来并生成到这个指令中的，目的是使接下来对table的初始化填充不会造成rehash而影响效率。B和C使用“floating point byte”的方法来表示成(eeeeexxx)的二进制形式，其实际值为(1xxx) * 2^(eeeee-1)。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = &#123;&#125;;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1	[1]	NEWTABLE 	0 0 0</span><br><span class="line">2	[1]	RETURN   	0 1</span><br></pre></td></tr></table></figure>
<p>上面代码生成一个空的table，放入local变量a，B和C参数都为0。</p>
<p>name    args    desc<br>OP_SETLIST    A B C    R(A)[(C-1)*FPF+i] := R(A+i), 1 &lt;= i &lt;= B<br>SETLIST用来配合NEWTABLE，初始化表的数组部分使用的。A为保存待设置表的寄存器，SETLIST要将A下面紧接着的寄存器列表(1–B)中的值逐个设置给表的数组部分。</p>
<p>当表需要初始化数组元素数量比较小的情况下，例如:<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = &#123;<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>&#125;;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">	1	[1]	NEWTABLE 	0 3 0</span><br><span class="line">	2	[1]	LOADK    	1 -1	; 1</span><br><span class="line">	3	[1]	LOADK    	2 -1	; 1</span><br><span class="line">	4	[1]	LOADK    	3 -1	; 1</span><br><span class="line">	5	[1]	SETLIST  	0 3 1	; 1</span><br><span class="line">	6	[1]	RETURN   	0 1</span><br><span class="line">constants (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">	1	1</span><br></pre></td></tr></table></figure>
<p>第1行先用NEWTABLE构建一个具有3个数组元素的表，让到寄存器0中；然后使用3个LOADK向下面3个寄存器装入常量1；最后使用SETLIST设置表的1~3为寄存器1~寄存器3。<br>如果需要创建一个很大的表，其中包含很多的数组元素，使用如上方法就会遇到一个问题。将这些指按顺序放到寄存器时，会超出寄存器的范围。解决的办法就是按照一个固定大小，将这些数组元素分批进行设置。在Lua中，每批的数量由lopcodes.h中的LFIELDS_PER_FLUSH定义，数量为50。所以，大数量的设置会按照50个一批，先将值设置到表下面的寄存器，然后设置给对应的表项。C代表的就是这一次调用SETLIST设置的是第几批。回到上面的例子，因为只有3个表项，所以1批就搞定了，C的值为1。</p>
<p>下面是一个大表的设置：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = </span><br><span class="line">&#123;</span><br><span class="line">	<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>,<span class="number">0</span>,</span><br><span class="line">	<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>,<span class="number">0</span>,</span><br><span class="line">	<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>,<span class="number">0</span>,</span><br><span class="line">	<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>,<span class="number">0</span>,</span><br><span class="line">	<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>,<span class="number">0</span>,</span><br><span class="line">	<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">	1	[1]	NEWTABLE 	0 30 0</span><br><span class="line">	2	[3]	LOADK    	1 -1	; 1</span><br><span class="line">	3	[3]	LOADK    	2 -2	; 2 </span><br><span class="line">...</span><br><span class="line">	50	[7]	LOADK    	49 -9	; 9</span><br><span class="line">	51	[7]	LOADK    	50 -10	; 0</span><br><span class="line">	52	[7]	SETLIST  	0 50 1	; 1</span><br><span class="line">	53	[8]	LOADK    	1 -1	; 1</span><br><span class="line">	54	[8]	LOADK    	2 -2	; 2</span><br><span class="line">	55	[9]	LOADK    	3 -3	; 3</span><br><span class="line">	56	[9]	SETLIST  	0 3 2	; 2</span><br><span class="line">	57	[9]	RETURN   	0 1</span><br><span class="line">constants (10) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">	1	1</span><br><span class="line">	2	2</span><br><span class="line">	3	3</span><br><span class="line">	4	4</span><br><span class="line">	5	5</span><br><span class="line">	6	6</span><br><span class="line">	7	7</span><br><span class="line">	8	8</span><br><span class="line">	9	9</span><br><span class="line">	10	0</span><br></pre></td></tr></table></figure>
<p>可以看到，这个表的初始化使用了两个SETLIST指令。第一个处理前50个，C为1，设置id从(C-1)<em>50 + 1开始，也就是1。第二个处理余下的3个，C为2，设置的id从(C-1)</em>50 + 1开始，也就是51。<br>如果数据非常大，导致需要的批次超出了C的表示范围，那么C会被设置成0，然后在SETLIST指令后面生成一个EXTRAARG指令，并用其Ax来存储批次。这与前面说到的LOADKX的处理方法一样，都是为处理超大数据服务的。</p>
<p>如果使用核能产生多个返回值的表达式（… 和 函数调用）初始化数组项，如果这个初始化不是表构造的最后一项，那么只有第一个返回值会被设置到数组项；如果是最后一项，那么SETLIST中的B会被设置为0，表示从A+1到当前栈顶都用来设置。</p>
<p>SETLIST只负责初始化表的数组部分，对于hash部分，还是通过SETTABLE来初始化。</p>
<p>name    args    desc<br>OP_GETTABLE    A B C    R(A) := R(B)[RK(C)]<br>OP_SETTABLE    A B C    R(A)[RK(B)] := RK(C)<br>GETTABLE使用C表示的key，将寄存器B中的表项值获取到寄存器A中。SETTABLE设置寄存器A的表的B项为C代表的值。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">local</span> a = &#123;&#125;;</span><br><span class="line">a.x = 1;</span><br><span class="line"><span class="built_in">local</span> b = a.x;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1	[1]	NEWTABLE 	0 0 0</span><br><span class="line">2	[2]	SETTABLE 	0 -1 -2	; <span class="string">"x"</span> 1</span><br><span class="line">3	[3]	GETTABLE 	1 0 -1	; <span class="string">"x"</span></span><br><span class="line">4	[3]	RETURN   	0 1</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua5.2内部实现:虚拟机指令(8) LOOP</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E8%99%9A%E6%8B%9F%E6%9C%BA%E6%8C%87%E4%BB%A48/</url>
    <content><![CDATA[<p><a href="http://blog.csdn.net/yuanlin2008/article/details/8505183" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>Lua5.2种除了for循环之外，其他的各种循环都使用关系和逻辑指令，配合JMP指令来完成。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">while</span>(a &lt; <span class="number">10</span>) <span class="keyword">do</span></span><br><span class="line">    a = a + <span class="number">1</span>;</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1       [1]     LOADK           0 -1    ; 0</span><br><span class="line">2       [2]     LT              0 0 -2  ; - 10</span><br><span class="line">3       [2]     JMP             0 2     ; to 6</span><br><span class="line">4       [3]     ADD             0 0 -3  ; - 1</span><br><span class="line">5       [3]     JMP             0 -4    ; to 2</span><br><span class="line">6       [4]     RETURN          0 1</span><br></pre></td></tr></table></figure>
<p>第二行使用LT对寄存器0和敞亮10进行比较，如果小于成立，跳过第三行的JMP，运行第四行的ADD指令，将a加1，然后运行第五行的JMP，跳转回第二行，重新判断条件。如果小于不成立，则直接运行下一个JMP指令，跳转到第六行结束。<br>对于for循环，Lua5.2使用了两套专门的指令，分别对应numeric for loop和generic for loop。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name	args	desc</span><br><span class="line">OP_FORLOOP	A sBx	R(A)+=R(A+2);</span><br><span class="line"><span class="keyword">if</span> R(A) &lt;?= R(A+1) <span class="keyword">then</span> &#123; pc+=sBx; R(A+3)=R(A) &#125;</span><br><span class="line">OP_FORPREP	A sBx	R(A)-=R(A+2); pc+=sBx</span><br></pre></td></tr></table></figure></p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a;</span><br><span class="line"><span class="keyword">for</span> i = <span class="number">1</span>, <span class="number">10</span> <span class="keyword">do</span></span><br><span class="line">    a = i;</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">main &lt;test.lua:0,0&gt; (8 instructions at 0x80048eb0)</span><br><span class="line">0+ params, 5 slots, 1 upvalue, 5 locals, 2 constants, 0 <span class="built_in">functions</span></span><br><span class="line">        1       [1]     LOADNIL         0 0</span><br><span class="line">        2       [2]     LOADK           1 -1    ; 1</span><br><span class="line">        3       [2]     LOADK           2 -2    ; 10</span><br><span class="line">        4       [2]     LOADK           3 -1    ; 1</span><br><span class="line">        5       [2]     FORPREP         1 1     ; to 7</span><br><span class="line">        6       [3]     MOVE            0 4</span><br><span class="line">        7       [2]     FORLOOP         1 -2    ; to 6</span><br><span class="line">        8       [4]     RETURN          0 1</span><br><span class="line">constants (2) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">        1       1</span><br><span class="line">        2       10</span><br><span class="line">locals (5) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">        0       a       2       9</span><br><span class="line">        1       (<span class="keyword">for</span> index)     5       8</span><br><span class="line">        2       (<span class="keyword">for</span> <span class="built_in">limit</span>)     5       8</span><br><span class="line">        3       (<span class="keyword">for</span> step)      5       8</span><br><span class="line">        4       i       6       7</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">        0       _ENV    1       0</span><br></pre></td></tr></table></figure>
<p>Numeric for loop内部使用了3个局部变量来控制循环，他们分别是”for index”，“for limit”和“for step”。“for index”用作存放初始值和循环计数器，“for limit”用作存放循环上限，“for step”用作存放循环步长。对于上面的程序，三个值分别是1，10和1。这三个局部变量对于使用者是不可见得，我们可以在生成代码的locals表中看到这3个局部变量，他们的有效范围为第五行道第八行，也就是整个for循环。还有一个使用到的局部变量，就是使用者自己指定的计数器，上例中为”i”。我们可以看到，这个局部变量的有效范围为6~7行，也就是循环的内部。这个变量在每次循环时都被设置成”for index”变量来使用。<br>上例中2~4行初始化循环使用的3个内部局部变量。第五行FORPREP用于准备这个循环，将for index减去一个for step，然后跳转到第七行。第七行的FORLOOP将for index加上一个for step，然后与for limit进行比较。如果小于等于for limit，则将i设置成for index，然后跳回第六行。否则就退出循环。我们可以看到，i并不用于真正的循环计数，而只是在每次循环时被赋予真正的计数器for index的值而已，所以在循环中修改i不会影响循环计数。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name	args	desc</span><br><span class="line">OP_TFORCALL	A C	R(A+3), ... ,R(A+2+C) := R(A)(R(A+1), R(A+2));</span><br><span class="line">OP_TFORLOOP	A sBx	<span class="keyword">if</span> R(A+1) ~= nil <span class="keyword">then</span> &#123; R(A)=R(A+1); pc += sBx &#125;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> i,v <span class="keyword">in</span> <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span> <span class="keyword">do</span></span><br><span class="line">    a = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">main &lt;test.lua:0,0&gt; (8 instructions at 0x80048eb0)</span><br><span class="line">0+ params, 6 slots, 1 upvalue, 5 locals, 4 constants, 0 <span class="built_in">functions</span></span><br><span class="line">        1       [1]     LOADK           0 -1    ; 1</span><br><span class="line">        2       [1]     LOADK           1 -2    ; 2</span><br><span class="line">        3       [1]     LOADK           2 -3    ; 3</span><br><span class="line">        4       [1]     JMP             0 1     ; to 6</span><br><span class="line">        5       [2]     SETTABUP        0 -4 -1 ; _ENV <span class="string">"a"</span> 1</span><br><span class="line">        6       [1]     TFORCALL        0 2</span><br><span class="line">        7       [1]     TFORLOOP        2 -3    ; to 5</span><br><span class="line">        8       [3]     RETURN          0 1</span><br><span class="line">constants (4) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">        1       1</span><br><span class="line">        2       2</span><br><span class="line">        3       3</span><br><span class="line">        4       <span class="string">"a"</span></span><br><span class="line">locals (5) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">        0       (<span class="keyword">for</span> generator) 4       8</span><br><span class="line">        1       (<span class="keyword">for</span> state)     4       8</span><br><span class="line">        2       (<span class="keyword">for</span> control)   4       8</span><br><span class="line">        3       i       5       6</span><br><span class="line">        4       v       5       6</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">        0       _ENV    1       0</span><br></pre></td></tr></table></figure>
<p>Generic for loop内部也使用了3个局部变量来控制循环，分别是”for generator”，“for state”和“for control”。for generator用来存放迭代使用的closure，每次迭代都会调用这个closure。for state和for control用于存放传给for generator的两个参数。Generic for loop还使用自定义的局部变量i，v，用来存储for generator的返回值。</p>
<p>上例中1~3行使用in后面的表达式列表(1,2,3)初始化3个内部使用的局部变量。第四行JMP调转到第六行。TFORCALL教用寄存器0(for generator)中的closure，传入for state和for control，并将结果返回给自定义局部变量列表i和v。第七行调用TFORLOOP进行循环条件判断，判断i是否为空。如果不为空，将i的值赋给for control，然后跳转到第五行，进行循环。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua5.2内部实现:虚拟机指令(7) 关系和逻辑指令</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E8%99%9A%E6%8B%9F%E6%9C%BA%E6%8C%87%E4%BB%A47/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8504200" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name	args	desc</span><br><span class="line">OP_JMP	A sBx	pc+=sBx; <span class="keyword">if</span> (A) close all upvalues &gt;= R(A) + 1</span><br></pre></td></tr></table></figure></p>
<p>JMP执行一个跳转，sBx表示跳转的偏移位置，被加到当前指向下一指令的指令指针上。如果sBx为0，表示没有任何跳转；1表示跳过下一个指令；-1表示重新执行当前指令。如果A&gt;0，表示需要关闭所有从寄存器A+1开始的所有local变量。实际执行的关闭操作只对upvalue有效。</p>
<p>JMP最直接的使用就是对应lua5.2新加入的goto语句：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">::l::</span><br><span class="line">```bash</span><br></pre></td></tr></table></figure></p>
<p>goto l;<br>        1       [1]     JMP             0 -1    ; to 1<br>        2       [2]     RETURN          0 1<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">这是一个无限循环。第一行JMP的sBx为<span class="number">-1</span>，表示重新执行JMP。</span><br><span class="line">```lua</span><br><span class="line"> <span class="keyword">do</span></span><br><span class="line">     <span class="keyword">local</span> a;</span><br><span class="line">     <span class="function"><span class="keyword">function</span> <span class="title">f</span><span class="params">()</span></span> a = <span class="number">1</span> <span class="keyword">end</span></span><br><span class="line"> <span class="keyword">end</span></span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">main &lt;test.lua:0,0&gt; (5 instructions at 0x80048eb0)</span><br><span class="line">0+ params, 2 slots, 1 upvalue, 1 <span class="built_in">local</span>, 1 constant, 1 <span class="keyword">function</span></span><br><span class="line">        1       [2]     LOADNIL         0 0</span><br><span class="line">        2       [3]     CLOSURE         1 0     ; 0x80049128</span><br><span class="line">        3       [3]     SETTABUP        0 -1 1  ; _ENV <span class="string">"f"</span></span><br><span class="line">        4       [3]     JMP             1 0     ; to 5</span><br><span class="line">        5       [4]     RETURN          0 1</span><br><span class="line">constants (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">        1       <span class="string">"f"</span></span><br><span class="line">locals (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">        0       a       2       5</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0x80048eb0:</span><br><span class="line">        0       _ENV    1       0</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> &lt;test.lua:3,3&gt; (3 instructions at 0x80049128)</span><br><span class="line">0 params, 2 slots, 1 upvalue, 0 locals, 1 constant, 0 <span class="built_in">functions</span></span><br><span class="line">        1       [3]     LOADK           0 -1    ; 1</span><br><span class="line">        2       [3]     SETUPVAL        0 0     ; a</span><br><span class="line">        3       [3]     RETURN          0 1</span><br><span class="line">constants (1) <span class="keyword">for</span> 0x80049128:</span><br><span class="line">        1       1</span><br><span class="line">locals (0) <span class="keyword">for</span> 0x80049128:</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0x80049128:</span><br><span class="line">        0       a       1       0</span><br></pre></td></tr></table></figure>
<p>上面的代码在do block中创建了一个局部变量a，并且a作为upvalue在函数f中被引用到。到退出do block是，a会退出他的有效域，并且关闭他对应的upvalue。Lua5.2中去除了以前专门处理关闭upvalue的指令CLOSE，而把这个功能加入到了JMP中。所以，生成的指令第四行的JMP在这里没有执行跳转，而只是为了关闭a的upvalue。<br>JMP其他的功能就是配合逻辑和关系指令（统称为test指令），实现程序的条件跳转。每个test辑指令与JMP搭配，都会将接下来生成的指令分为两个集合，满足条件的为true集合，否则为false集合。当test条件满足时，指令指针回+1，跳过后面紧跟的JMP指令，然后继续执行。当test条件不满足时，则继续执行，也就到了JMP，然后跳转到分支代码。</p>
<p><img src="https://img-my.csdn.net/uploads/201301/15/1358221143_7162.png" alt></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name	args	desc</span><br><span class="line">OP_EQ	A B C	<span class="keyword">if</span> ((RK(B) == RK(C)) ~= A) <span class="keyword">then</span> pc++</span><br><span class="line">OP_LT	A B C	<span class="keyword">if</span> ((RK(B) &lt;  RK(C)) ~= A) <span class="keyword">then</span> pc++</span><br><span class="line">OP_LE	A B C	<span class="keyword">if</span> ((RK(B) &lt;= RK(C)) ~= A) <span class="keyword">then</span> pc++</span><br></pre></td></tr></table></figure>
<p>关系指令对RK(B)和RK(C)进行比较，然后将比较结果与A指定的boolean值进行比较，来决定最终的boolean值。A在这里为每个关系指令提供了两种比较目标，满足和不满足。比如OP_LT何以用来实现“&lt;”和“&gt;”。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a,b,c;</span><br><span class="line">a = b &lt; c;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1       [1]     LOADNIL         0 2</span><br><span class="line">2       [2]     LT              1 1 2</span><br><span class="line">3       [2]     JMP             0 1     ; to 5</span><br><span class="line">4       [2]     LOADBOOL        0 0 1</span><br><span class="line">5       [2]     LOADBOOL        0 1 0</span><br><span class="line">6       [2]     RETURN          0 1</span><br></pre></td></tr></table></figure>
<p>第二行的LT对寄存器1和2进行LT比较，如果结果为true，则继续执行后面的JMP，跳转到第五行的LOADBOOL，将寄存器0赋值为true；如果结果为false，则跳过后面的JMP，执行第四行的LOADBOOL，将寄存器0赋值为false。我们前面讲过关于LOADBOOL，第四行执行后会跳过第五行的赋值。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name	args	desc</span><br><span class="line">OP_TEST	A C	<span class="keyword">if</span> not (R(A) &lt;=&gt; C) <span class="keyword">then</span> pc++</span><br><span class="line">OP_TESTSET	A B C	<span class="keyword">if</span> (R(B) &lt;=&gt; C) <span class="keyword">then</span> R(A) := R(B) <span class="keyword">else</span> pc++</span><br></pre></td></tr></table></figure></p>
<p>逻辑指令用于实现and和or逻辑运算符，或者在条件语句中判断一个寄存器。TESTSET将寄存器B转化成一个boolean值，然后与C进行比较。如果不相等，跳过后面的JMP指令。否则将寄存器B的值赋给寄存器A，然后继续执行。TEST是TESTSET的简化版，不需要赋值操作。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a,b,c;</span><br><span class="line">a = b <span class="keyword">and</span> c;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1       [1]     LOADNIL         0 2</span><br><span class="line">2       [2]     TESTSET         0 1 0</span><br><span class="line">3       [2]     JMP             0 1     ; to 5</span><br><span class="line">4       [2]     MOVE            0 2</span><br><span class="line">5       [2]     RETURN          0 1</span><br></pre></td></tr></table></figure>
<p>第二行的TESTSET将寄存器1的值与false比较。如果不成立，跳过JMP，执行第四行的MOVE，将寄存器2的值赋给寄存器0。否则，将寄存器1的值赋给寄存器0；然后执行后面的JMP。<br>上面的代码等价于<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> b <span class="keyword">then</span></span><br><span class="line"> a = c</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line"> a = b</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>垃圾回收GC浅谈</title>
    <url>/algorithm/%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6GC%E6%B5%85%E8%B0%88/</url>
    <content><![CDATA[<p><a href="https://juejin.cn/post/6844903857609244685" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p><strong>关于内存</strong><br>计算机通过两个机制，去实现内存的高效使用。</p>
<p>第一种机制是虚拟内存。硬盘的容量其实是远远大于内存的(RAM)，虚拟内存会在内存不足的时候，把不经常访问的内存的数据写到硬盘里。虽然说硬盘容量比较大，但是它的访问速度却很慢。如果内存和硬盘交换数据过于频繁，处理速度就会下降，计算机就会看上去像卡死了一样，这种现象被叫做抖动(Thrushing)。造成电脑蓝屏的主要原因之一就是抖动。</p>
<p>第二种机制就是垃圾回收(GC)。</p>
<p>虚拟内存的东西在计算机组成原理和操作系统的教科书里有相关的章节去讲。由于内容很多我就不多叙述了。主要来讲一下GC的事情。</p>
<p><strong>GC</strong><br>之前学习java以及参加java相关的面试，被问到关于相关GC的事情一直很是头疼，看了好多遍还是记不住，脑袋里只有隐隐约约的一些关键字，什么老年代、新生代、full GC什么的。具体流程一被问就GG。</p>
<p>现在想想，其实GC就是计算机帮助你去进行内存的回收。你用不到的数据如果一直占着内存，那么你的程序能用的内存就越来越少，所以需要进行内存的管理。比如你在写C、C++程序的时候，需要自己去管理内存，在堆上申请的内存最后需要自己手动释放，释放的内存会被操作系统重新利用。如果你认为某些内存空间“可能还会被用到”，或者是干脆忘记释放内存，这些无法访问的内存空间就会一直保留下来，造成内存浪费，最终导致性能下降和产生抖动。管理大量分配的内存空间，对于人来说其实是很困难的。</p>
<p>对于像Java、go这些语言，它们有自己的一套内存管理的机制，内存空间释放的自动化也就是GC，从某种程度上解放了程序员的双手。</p>
<p><strong>术语</strong><br>垃圾(Garbage)</p>
<p>垃圾(Garbage)就是需要回收的对象。作为编程人员你知道对象什么时候不需要，但是计算机无法判断。因此，如果程序直接或者间接地引用一个对象，那么它就会被计算机标记为“存活”；相反的，没有被引用到的对象就被视为“死亡”。把这些“死亡”的对象找出来，然后作为垃圾进行回收，这就是GC的本质。</p>
<p>根（Root）</p>
<p>根(Root)，就是判断对象是否可被引用的起始点。不同语言和编译器对根有不同规定，但是基本上是将变量和运行栈空间作为根。</p>
<p><strong>GC算法<br>标记清除方式</strong><br>标记清除原理非常简单，首先从根开始，将可能被引用的对象用递归的方式进行标记，然后将没有标记上的对象作为垃圾进行回收。</p>
<p>图1.1</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a68fe017f78626c2e94915f5dfc8502b.png" alt></p>
<p>图1.1的<br>(1)显示了随着程序的运行进而分配出的一些对象的状态，一个对象可以对其他的对象进行引用。<br>(2)部分GC开始执行，从根开始对可能被引用的对象打上标记。大多数情况下，这种标记是通过对象内部的标志(Flag)来实现的。被标记的对象我们将它涂黑。<br>(3)中，被标记的对象所能够引用的对象也被打上标记。重复这一步骤，可以将从根开始可能被间接引用到的对象全部打上标记。到此为止的操作，称作标记阶段(Mark phase)。标记阶段完成是，被标记的对象就被看做是“存活”对象。<br>(4)中，将全部对象按顺序扫描一遍，将没有标记的对象进行回收。这一步操作称作清除阶段(Sweep phase)。在扫描的同时，还需要将存活对象的标记清除掉，以便下一次GC去处理。</p>
<p>标记清除算法的处理时间，是和存活对象数与对象总数两者的和相关的。</p>
<p>标记清除方式的优点：可以处理循环引用的对象。缺点：在分配了大量对象，并且其中只有一小部分存活的情况下，由于清除阶段还要对大量“死亡”的对象进行扫描，会导致消耗大量不必要的时间。</p>
<p><strong>复制收集方式</strong><br>复制收集(Copy and Collection)方式试图解决标记清除的缺点。这种算法中，会从根开始，被引用的对象复制到另外的空间中，然后再将复制的对象所能够引用的对象递归的方式不断复制下去。</p>
<p>图1.2</p>
<p> <img src="https://img-blog.csdnimg.cn/img_convert/fdaefe0ffe5152ccf95c3d0e92ce9b41.png" alt></p>
<p>(1)是GC开始前的状态。<br>(2)部分中，旧对象空间之外，准备出一块新空间，然后将可能从根被引用的对象复制到新空间中。<br>(3)部分中，从已经复制的对象开始，再将可以被引用的对象复制到新空间中(串到了后面)，“死亡”对象就被留存在了旧空间中。<br>(4)中，将旧空间废弃掉，就可以将这部分所占的空间一下子全部释放掉，而没有必要再扫描每个对象。下次GC的时候，现在的新空间就当做了未来的旧空间。</p>
<p>通过图1.2可以发现，复制收集方式，只有类似标记清除的标记阶段，而不存在需要扫描所有对象的情况。但是相比之下，复制收集将对象复制一份所需要的开销会比较大，因此在“存活”对象比例较高的情况下，反而会比较不利。</p>
<p>这种算法的另一个好处就是它具有局部性(Locality)。在复制收集过程中，会按照对象被引用的顺序将对象复制到新空间中。因此关系较近的对象被放在距离较近的内存空间中的可能性会提供，这被称为局部性。局部性高的情况，内存缓存会更容易有效运作，程序的性能也会得到提高。</p>
<p><strong>引用计数方式</strong><br>引用计数的基本原理是，在每一个对象中保存该对象的引用计数，当引用发生增减时对计数进行更新。这让我想到了C++里的智能指针(shared_ptr)，曾经被面试手写shared_ptr的场景历历在目啊。</p>
<p>引用计数的增减，一般发生在变量赋值、对象内容更新、函数结束(局部变量不再被引用)等时间点。当一个对象的引用计数变为0的时候，则说明它将来不会再被引用，因此可以释放相应的内存空间。</p>
<p>图1.3</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/7867a8e8aa54aa0afad88c66e0e382a6.png" alt></p>
<p>(1)里面，所有对象中都保存着自己被多少其他的对象进行引用的数量(引用计数)。<br>(2)中，当对象引用发生变化的时候，引用计数也跟着变。这里由于B到D的引用失效了，于是对象D的引用计数变为0，由于D的引用计数为0，因此由D到对象C和E的引用数也分别相应减少。结果，对象E的引用计数变为0，于是E也对应释放掉了。<br>(3)中，引用计数为0的对象被释放，“存活”的对象保留了下来。能够注意到，在整个GC的过程中，并不需要对所有对象进行扫描。</p>
<p>这种方式最大优点，就是容易实现。它的另外一个优点就是，当对象不再被引用的瞬间就会被释放。其它的GC机制很难预测对象什么时候会被释放，而这种方式是立即被释放的。因此，由GC产生的中断时间(Pause time)就比较短。</p>
<p>当然这种方式也有缺点。最大的缺点就是，无法释放循环引用的对象。图1.4中A、B、C之间互相循环引用，它的引用计数永远不会为0，也就永远不会被释放。</p>
<p>图1.4</p>
<p> <img src="https://img-blog.csdnimg.cn/img_convert/898bda1498403a33b961fb56dfc200cd.png" alt></p>
<p>它的另外一个缺点就是，如果在必要的增减计数的时候遗漏掉了增减操作，或者是增减计数出错，就会产生内存错误。如果是手动管理计数就很容易产生bug。</p>
<p>它的最后一个缺点就是引用计数管理不适合并行处理。如果多个线程同时对引用计数进行增减，引用数值就会产生不一致的问题从而导致内存错误。因此引用计数必须采用独占的方式。如果引用计数频繁发生，每次需要使用加锁等并发控制机制的话，也会造成很大的额外开销。<br><strong><br>改良版GC</strong><br>GC的基本算法，大体上都是上面的三种方式或者是它们的衍生品。现在通过三种方式进行融合，出现一些其他高级的GC方式，即分代回收、增量回收和并行回收。有些情况也会对这些改良版的gc方式进行组合使用。</p>
<p><strong>分代回收(Generational GC)</strong><br>分代回收的目的，就是在程序运行期间，将GC所消耗的时间尽量的缩短。</p>
<p>它基于这样一个一般程序的性质，即大部分对象都会在短时间内成为垃圾，经过一定时间依然存活的对象往往有较长的寿命。对于刚分配不久的“年轻”对象进行重点扫描，应该可以更有效的回收大部分垃圾。</p>
<p>分代回收中，按照对象生成时间进行分代。刚刚生成不久的对象划分为新生代(Young generation)，而存活了较长时间的对象划分为旧生代(Old generation)。不同实现可能还会有更多划分。如果上面的对象寿命假说成立的话，只要扫描回收新生代对象，就可以回收掉废弃对象中的很大一部分。</p>
<p>这种只扫描新生代对象的回收操作，被称作小回收(Minor GC)。它的具体步骤如下。</p>
<p>首先从根开始一次常规扫描，找到“存活”对象。可以使用复制收集算法或者标记清除算法，但是大部分使用了复制收集。需要注意的是，在扫描的过程中，如果遇到属于旧生代的对象，则不对该对象进行递归扫描。这样需要扫描的对象就会大量减少。</p>
<p>然后，将第一次扫描后残留下来的对象划分到旧生代。具体来说，如果使用复制收集算法，只要将复制目标空间设置为旧生代就可以了；标记清除方式的话，则采用在对象上设置某种标志的方式。</p>
<p>现在有一个问题，如果有从旧生代到新生代对象的引用怎么办？如果只扫描新生代，那么旧生代对新生代的引用就不会被检测到。这样一来，如果一个年轻的对象只有来自旧生代的引用，就会被误认为“死亡”。因此在分代回收中，会对对象的更新进行监视，将从旧生代对新生代的引用，记录在记录集(remembered set)的表中。在执行小回收过程中，这个记录集也作为一个根(root)来对待。</p>
<p>图1.5</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/22c61fc10372e491f12d7738d32b217c.png" alt></p>
<p>没有引用任何其它对象的旧生代F对象，会通过大回收(Full gc)操作进行回收。保证分代回收正确工作，必须使记录集的内容保持更新。</p>
<p>记录引用的子程序工作方式如下。设有两个对象A、B，当对A内容进行改写，并加入对B的引用时，如果A属于旧生代，B属于新生代，则将该引用添加到记录集中。</p>
<p>这种检查程序需要对所有涉及修改对象内容的地方进行保护，因此也被称为写屏障(Write barrier)。写屏障也用在很多其他GC算法中，比如Go的gc算法也用到了写屏障。</p>
<p>虽然旧生代中的对象寿命一般比较长，但是最终也会“死亡”。随着程序运行，旧生代中“死亡”的对象不断增加。为了避免这些“死亡”的旧生代对象白占内存空间，偶尔需要对包括旧生代在内的全部区域进行一次扫描回收。这种以全部区域为对象的GC操作叫Full GC</p>
<p>分代收集通过减少GC中扫描的对象数量进而缩短GC带来的平均中断时间，但是最终还是需要一次Full GC，因此最大中断时间并没有改善。GC的性能也会被程序行为、分代数量、Full GC的触发条件等因素大幅左右。</p>
<p><strong>增量回收</strong><br>实时性要求很高的程序中，相比缩短GC平均中断时间，缩短GC的最大中断时间更加重要。</p>
<p>GC最大的中断时间要限定在一个时间范围内，但是一般GC算法没办法保证，因为GC产生的中断时间和对象的数量和状态有关系。因此为了维持程序的实时性，不等到GC全部完成，而是将GC操作细分成多个部分逐一执行。这种方式就是增量回收（Incremental GC）</p>
<p>增量回收过程中程序本身会继续运行，为了避免对象之间的引用关系改变而导致GC回收出错，增量回收也使用了写屏障，来讲新被引用的对象作为扫描的起始点记录下来。</p>
<p>由于增量回收过程式分步渐进式的，可以将中断时间控制在一定长度之内。但是相应的中断操作需要消耗一定时间，GC消耗的总时间会相应增加。</p>
<p><strong>并行回收</strong><br>现在的计算机基本上都有多个CPU核心。而并行回收方式正是通过最大限度利用多CPU的处理能力来进行GC操作。</p>
<p>并行回收的基本原理是，在原有程序运行的同时进行GC操作，这点和增量回收是相似的。相对于在一个CPU上进行任务分割的增量回收方式，并行回收可以利用多CPU的性能，尽可能让这些GC任务并行（同时）进行。由于程序功能运行和GC操作是同时发生的，就会遇到和前面相同的问题，所以并行回收也需要利用写屏障来对对象当前的状态信息保持更新。</p>
<p>但是让GC完全并行，而不影响原有程序运行时做不到的，在GC的某些特定阶段还是需要暂停原有程序的运行。</p>
<p><strong>总结</strong><br>目前的GC算法基本上都是上述算法的变体或者是组合，例如java里的分代回收，golang中的三色标记法。有时候因为对象之间的引用状态发生了变化，结果导致了本来是“存活”对象却被回收掉，这时候需要写屏障(Write barrier)来进行记录和保护。当我们理解了上面的事情，其他具体编程语言的GC算法就不难理解了。</p>
<p>参考资料<br>《代码的未来》– 作者：松本行弘 译者：周自恒<br><a href="https://juejin.im/post/6844903513248497677" rel="external nofollow noopener noreferrer" target="_blank">juejin.im/post/684490…</a><br><a href="http://legendtkl.com/2017/04/28/golang-gc/" rel="external nofollow noopener noreferrer" target="_blank">legendtkl.com/2017/04/28/…</a></p>
]]></content>
      <categories>
        <category>algorithm</category>
        <category>gc</category>
      </categories>
      <tags>
        <tag>algorithm</tag>
        <tag>gc</tag>
      </tags>
  </entry>
  <entry>
    <title>Goroutine 数量控制在多少合适，会影响 GC 和调度？</title>
    <url>/go/Goroutine%20%E6%95%B0%E9%87%8F%E6%8E%A7%E5%88%B6%E5%9C%A8%E5%A4%9A%E5%B0%91%E5%90%88%E9%80%82%EF%BC%8C%E4%BC%9A%E5%BD%B1%E5%93%8D%20GC%20%E5%92%8C%E8%B0%83%E5%BA%A6%EF%BC%9F/</url>
    <content><![CDATA[<p>大家好，我是煎鱼<a href="https://mp.weixin.qq.com/s/uWP2X6iFu7BtwjIv5H55vw" rel="external nofollow noopener noreferrer" target="_blank"></a>。</p>
<p>前几天在读者交流群里看到一位小伙伴，发出了一个致命提问，那就是：“单机的 goroutine 数量控制在多少比较合适？”。</p>
<p>也许你和群内小伙伴第一反应一样，会答复 “控制多少，我觉得没有定论”。</p>
<p>紧接着延伸出了更进一步的疑惑：“goroutine 太多了会影响 gc 和调度吧，主要是怎么预算这个数是合理的呢？”</p>
<p>这是本文要进行探讨的主体，因此本文的结构会是先探索基础知识，再一步步揭开，深入理解这个问题。</p>
<h1 id="Goroutine-是什么"><a href="#Goroutine-是什么" class="headerlink" title="Goroutine 是什么"></a>Goroutine 是什么</h1><p>Go 语言作为一个新生编程语言，其令人喜爱的特性之一就是 goroutine。Goroutine 是一个由 Go 运行时管理的轻量级线程，一般称其为 “协程”。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">go</span> f(x, y, z)</span><br></pre></td></tr></table></figure></p>
<p>操作系统本身是无法明确感知到 Goroutine 的存在的，Goroutine 的操作和切换归属于 “用户态” 中。</p>
<p>Goroutine 由特定的调度模式来控制，以 “多路复用” 的形式运行在操作系统为 Go 程序分配的几个系统线程上。</p>
<p>同时创建 Goroutine 的开销很小，初始只需要 2-4k 的栈空间。Goroutine 本身会根据实际使用情况进行自伸缩，非常轻量。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">say</span><span class="params">(s <span class="keyword">string</span>)</span></span> &#123;</span><br><span class="line"> <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">9999999</span>; i++ &#123;</span><br><span class="line">  time.Sleep(<span class="number">100</span> * time.Millisecond)</span><br><span class="line">  fmt.Println(s)</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> <span class="keyword">go</span> say(<span class="string">"煎鱼"</span>)</span><br><span class="line"> say(<span class="string">"你好"</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>人称可以开几百几千万个的协程小霸王，是 Go 语言的得意之作之一。</p>
<h1 id="调度是什么"><a href="#调度是什么" class="headerlink" title="调度是什么"></a>调度是什么</h1><p>既然有了用户态的代表 Goroutine，操作系统又看不到他。必然需要有某个东西去管理他，才能更好的运作起来。</p>
<p>这指的就是 Go 语言中的调度，最常见、面试最爱问的 GMP 模型。因此接下来将会给大家介绍一下 Go 调度的基础知识和流程。</p>
<p>下述内容摘自煎鱼和 p 神写的《Go 语言编程之旅》中的章节内容。</p>
<h1 id="调度基础知识"><a href="#调度基础知识" class="headerlink" title="调度基础知识"></a>调度基础知识</h1><p>Go scheduler 的主要功能是针对在处理器上运行的 OS 线程分发可运行的 Goroutine，而我们一提到调度器，就离不开三个经常被提到的缩写，分别是：</p>
<ul>
<li><p>G：Goroutine，实际上我们每次调用 go func 就是生成了一个 G。</p>
</li>
<li><p>P：Processor，处理器，一般 P 的数量就是处理器的核数，可以通过 GOMAXPROCS 进行修改。</p>
</li>
<li><p>M：Machine，系统线程。</p>
</li>
</ul>
<p>这三者交互实际来源于 Go 的 M: N 调度模型。也就是 M 必须与 P 进行绑定，然后不断地在 M 上循环寻找可运行的 G 来执行相应的任务。</p>
<h1 id="调度流程"><a href="#调度流程" class="headerlink" title="调度流程"></a>调度流程</h1><p>我们以 GMP 模型的工作流程图进行简单分析，官方图如下:</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3461afeddf415bbf0d1d04b4400d45dd.png" alt></p>
<ol>
<li><p>当我们执行 go func() 时，实际上就是创建一个全新的 Goroutine，我们称它为 G。</p>
</li>
<li><p>新创建的 G 会被放入 P 的本地队列（Local Queue）或全局队列（Global Queue）中，准备下一步的动作。需要注意的一点，这里的 P 指的是创建 G 的 P。</p>
</li>
<li><p>唤醒或创建 M 以便执行 G。</p>
</li>
<li><p>不断地进行事件循环</p>
</li>
<li><p>寻找在可用状态下的 G 进行执行任务</p>
</li>
<li><p>清除后，重新进入事件循环</p>
</li>
</ol>
<p>在描述中有提到全局和本地这两类队列，其实在功能上来讲都是用于存放正在等待运行的 G，但是不同点在于，本地队列有数量限制，不允许超过 256 个。</p>
<p>并且在新建 G 时，会优先选择 P 的本地队列，如果本地队列满了，则将 P 的本地队列的一半的 G 移动到全局队列。</p>
<p>这可以理解为调度资源的共享和再平衡。</p>
<h1 id="窃取行为"><a href="#窃取行为" class="headerlink" title="窃取行为"></a>窃取行为</h1><p>我们可以看到图上有 steal 行为，这是用来做什么的呢，我们都知道当你创建新的 G 或者 G 变成可运行状态时，它会被推送加入到当前 P 的本地队列中。</p>
<p>其实当 P 执行 G 完毕后，它也会 “干活”，它会将其从本地队列中弹出 G，同时会检查当前本地队列是否为空，如果为空会随机的从其他 P 的本地队列中尝试窃取一半可运行的 G 到自己的名下。</p>
<p>官方图如下：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e7e7207b54e6074cea49995f318ef192.png" alt></p>
<p>在这个例子中，P2 在本地队列中找不到可以运行的 G，它会执行 work-stealing 调度算法，随机选择其它的处理器 P1，并从 P1 的本地队列中窃取了三个 G 到它自己的本地队列中去。</p>
<p>至此，P1、P2 都拥有了可运行的 G，P1 多余的 G 也不会被浪费，调度资源将会更加平均的在多个处理器中流转。</p>
<h1 id="有没有什么限制"><a href="#有没有什么限制" class="headerlink" title="有没有什么限制"></a>有没有什么限制</h1><p>在前面的内容中，我们针对 Go 的调度模型和 Goroutine 做了一个基本介绍和分享。</p>
<p>接下来我们回到主题，思考 “goroutine 太多了，会不会有什么影响”。</p>
<p>在了解 GMP 的基础知识后，我们要知道在协程的运行过程中，真正干活的 GPM 又分别被什么约束？</p>
<p>煎鱼带大家分别从 GMP 来逐步分析。</p>
<h1 id="M-的限制"><a href="#M-的限制" class="headerlink" title="M 的限制"></a>M 的限制</h1><p>第一，要知道在<strong>协程的执行中，真正干活的是 GPM 中的哪一个？</strong></p>
<p>那势必是 M（系统线程） 了，因为 G 是用户态上的东西，最终执行都是得映射，对应到 M 这一个系统线程上去运行。</p>
<p>那么 M 有没有限制呢？</p>
<p>答案是：有的。在 Go 语言中，<strong>M 的默认数量限制是 10000，</strong>如果超出则会报错：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">GO: runtime: program exceeds 10000-thread <span class="built_in">limit</span></span><br></pre></td></tr></table></figure></p>
<p>通常只有在 Goroutine 出现阻塞操作的情况下，才会遇到这种情况。这可能也预示着你的程序有问题。</p>
<p>若确切是需要那么多，还可以通过 debug.SetMaxThreads 方法进行设置。</p>
<h1 id="G-的限制"><a href="#G-的限制" class="headerlink" title="G 的限制"></a>G 的限制</h1><p>第二，那 G 呢，Goroutine 的创建数量是否有限制？</p>
<p>答案是：没有。但理论上会受内存的影响，假设一个 Goroutine 创建需要 4k（via @GoWKH）：</p>
<ul>
<li><p>4k * 80,000 = 320,000k ≈ 0.3G内存</p>
</li>
<li><p>4k * 1,000,000 = 4,000,000k ≈ 4G内存</p>
</li>
</ul>
<p>以此就可以相对计算出来一台单机在通俗情况下，所能够创建 Goroutine 的大概数量级别。</p>
<p>注：Goroutine 创建所需申请的 2-4k 是需要连续的内存块。</p>
<h1 id="P-的限制"><a href="#P-的限制" class="headerlink" title="P 的限制"></a>P 的限制</h1><p>第三，那 P 呢，P 的数量是否有限制，受什么影响？</p>
<p>答案是：有限制。# P 的数量受环境变量 GOMAXPROCS 的直接影响。 #</p>
<p>环境变量 GOMAXPROCS 又是什么？在 Go 语言中，通过设置 GOMAXPROCS，用户可以调整调度中 P（Processor）的数量。</p>
<p>另一个重点在于，与 P 相关联的的 M（系统线程），是需要绑定 P 才能进行具体的任务执行的，因此 P 的多少会影响到 Go 程序的运行表现。</p>
<p>P 的数量基本是受本机的核数影响，没必要太过度纠结他。</p>
<p>那 P 的数量是否会影响 Goroutine 的数量创建呢？</p>
<p>答案是：不影响。且 Goroutine 多了少了，P 也该干嘛干嘛，不会带来灾难性问题。</p>
<h1 id="何为之合理"><a href="#何为之合理" class="headerlink" title="何为之合理"></a>何为之合理</h1><p>在介绍完 GMP 各自的限制后，我们回到一个重点，就是 “Goroutine 数量怎么预算，才叫合理？”。</p>
<p>“合理” 这个词，是需要看具体场景来定义的，可结合上述对 GPM 的学习和了解。得出：</p>
<p>M：有限制，默认数量限制是 10000，可调整。</p>
<p>G：没限制，但受内存影响。</p>
<p>P：受本机的核数影响，可大可小，不影响 G 的数量创建。</p>
<p>Goroutine 数量在 MG 的可控限额以下，多个把个、几十个，少几个其实没有什么影响，就可以称其为 “合理”。</p>
<h1 id="真实情况"><a href="#真实情况" class="headerlink" title="真实情况"></a>真实情况</h1><p>在真实的应用场景中，没法如此简单的定义。如果你 Goroutine：</p>
<p>在频繁请求 HTTP，MySQL，打开文件等，那假设短时间内有几十万个协程在跑，那肯定就不大合理了（可能会导致  too many files open）。</p>
<p>常见的 Goroutine 泄露所导致的 CPU、Memory 上涨等，还是得看你的 Goroutine 里具体在跑什么东西。</p>
<p>还是得看 Goroutine 里面跑的是什么东西。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>在这篇文章中，分别介绍了 Goroutine、GMP、调度模型的基本知识，针对如下问题进行了展开：</p>
<p>单机的 goroutine 数量控制在多少比较合适？</p>
<p>goroutine 太多了会影响 gc 和调度吧，主要是怎么预算这个数是合理的呢？</p>
<p>单机的 goroutine 数量只要控制在限额以下的，都可以认为是 “合理”。</p>
<p>真实场景得看具体里面跑的是什么，跑的如果是 “资源怪兽”，只运行几个 Goroutine 都可以跑死。</p>
<p>因此想定义 “预算”，就得看跑的什么了。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go struct详解</title>
    <url>/go/go%20struct%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<p>Go Struct超详细讲解<br>原创作者，<a href="https://juejin.im/post/6844903814168838151" rel="external nofollow noopener noreferrer" target="_blank">程序员读书</a></p>
<p>Go语言中提供了对struct的支持,struct,中文翻译称为结构体，与数组一样，属于复合类型，并非引用类型。</p>
<p>Go语言的struct，与C语言中的struct或其他面向对象编程语言中的类(class)类似，可以定义字段(属性)和方法，但也有很不同的地方，需要深入学习，才能区分他们之间的区别。</p>
<p>注意复合类型与引用类型之间的区别，这应该也是值传递和引用传递的区别吧。</p>
<p>定义<br>使用struct关键字可以定义一个结构体,结构体中的成员，称为结构体的字段或属性。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Member <span class="keyword">struct</span> &#123;</span><br><span class="line">    id          <span class="keyword">int</span></span><br><span class="line">    name, email <span class="keyword">string</span></span><br><span class="line">    gender, age <span class="keyword">int</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上面的代码中，我们定义了一个包含5个字段的结构体，可以看到，相同类型name和email、gender和age在同一行中定义，但比较好的编程习惯是每一行只定义一个字段,如：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Member <span class="keyword">struct</span> &#123;</span><br><span class="line">    id     <span class="keyword">int</span></span><br><span class="line">    name   <span class="keyword">string</span></span><br><span class="line">    email  <span class="keyword">string</span></span><br><span class="line">    gender <span class="keyword">int</span></span><br><span class="line">    age    <span class="keyword">int</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>当然，结构体也可以不包含任何字段，称为空结构体，struct{}表示一个空的结构体，注意，直接定义一个空的结构体并没有意义，但在并发编程中，channel之间的通讯，可以使用一个struct{}作为信号量。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">ch := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">struct</span>&#123;&#125;)</span><br><span class="line">ch &lt;- <span class="keyword">struct</span>&#123;&#125;&#123;&#125;</span><br></pre></td></tr></table></figure></p>
<p>使用<br>上面的例子中，我们定义了Member结构体类型，接下就可以这个自定义的类型创建变量了。</p>
<p>直接定义变量，这个使用方式并没有为字段赋初始值，因此所有字段都会被自动赋予自已类型的零值，比如name的值为空字符串””，age的值为0。</p>
<p>var m1 Member//所有字段均为空值<br>复制代码<br>使用字面量创建变量，这种使用方式，可以在大括号中为结构体的成员赋初始值，有两种赋初始值的方式，一种是按字段在结构体中的顺序赋值，下面代码中m2就是使用这种方式，这种方式要求所有的字段都必须赋值，因此如果字段太多，每个字段都要赋值，会很繁琐，另一种则使用字段名为指定字段赋值，如下面代码中变量m3的创建，使用这种方式，对于其他没有指定的字段，则使用该字段类型的零值作为初始化值。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> m2 = Member&#123;<span class="number">1</span>,<span class="string">"小明"</span>,<span class="string">"xiaoming@163.com"</span>,<span class="number">1</span>,<span class="number">18</span>&#125; <span class="comment">// 简短变量声明方式：m2 := Member&#123;1,"小明","xiaoming@163.com",1,18&#125;</span></span><br><span class="line"><span class="keyword">var</span> m3 = Member&#123;id:<span class="number">2</span>,<span class="string">"name"</span>:<span class="string">"小红"</span>&#125;<span class="comment">// 简短变量声明方式：m3 := Member&#123;id:2,"name":"小红"&#125;</span></span><br></pre></td></tr></table></figure></p>
<p>访问字段</p>
<p>通过变量名，使用逗号(.)，可以访问结构体类型中的字段，或为字段赋值，也可以对字段进行取址(&amp;)操作。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">fmt.Println(m2.name)<span class="comment">//输出：小明</span></span><br><span class="line">m3.name = <span class="string">"小花"</span></span><br><span class="line">fmt.Println(m3.name)<span class="comment">//输出：小花</span></span><br><span class="line">age := &amp;m3.age</span><br><span class="line">*age = <span class="number">20</span></span><br><span class="line">fmt.Println(m3.age)<span class="comment">//20</span></span><br></pre></td></tr></table></figure></p>
<p>指针结构体</p>
<p>结构体与数组一样，都是值传递，比如当把数组或结构体作为实参传给函数的形参时，会复制一个副本，所以为了提高性能，一般不会把数组直接传递给函数，而是使用切片(引用类型)代替，而把结构体传给函数时，可以使用指针结构体。</p>
<p>指针结构体，即一个指向结构体的指针,声明结构体变量时，在结构体类型前加*号，便声明一个指向结构体的指针，如：</p>
<p>注意，指针类型为引用类型，声明结构体指针时，如果未初始化，则初始值为nil,只有初始化后，才能访问字段或为字段赋值。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> m1 *Member</span><br><span class="line">m1.name = <span class="string">"小明"</span><span class="comment">//错误用法，未初始化,m1为nil</span></span><br><span class="line">m1 = &amp;Member&#123;&#125;</span><br><span class="line">m1.name = <span class="string">"小明"</span><span class="comment">//初始化后，结构体指针指向某个结构体地址，才能访问字段，为字段赋值。</span></span><br></pre></td></tr></table></figure></p>
<p>另外，使用Go内置new()函数，可以分配内存来初始化结构休，并返回分配的内存指针，因为已经初始化了，所以可以直接访问字段。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> m2 = <span class="built_in">new</span>(Member)</span><br><span class="line">m2.name = <span class="string">"小红"</span></span><br></pre></td></tr></table></figure></p>
<p>我们知道，如果将结构体转给函数，只是复制结构体的副本，如果在函数内修改结构体字段值，外面的结构体并不会受影响，而如果将结构体指针传给函数，则在函数中使用指针对结构体所做的修改，都会影响到指针指向的结构体。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    m1 := Member&#123;&#125;</span><br><span class="line">    m2 := <span class="built_in">new</span>(Member)</span><br><span class="line">    Change(m1,m2)</span><br><span class="line">    fmt.Println(m1,m2)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Change</span><span class="params">(m1 Member,m2 *Member)</span></span>&#123;</span><br><span class="line">    m1.Name = <span class="string">"小明"</span></span><br><span class="line">    m2.Name = <span class="string">"小红"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可见性<br>上面的例子中，我们定义结构体字段名首字母是小写的，这意味着这些字段在包外不可见,因而无法在其他包中被访问，只允许包内访问。</p>
<p>下面的例子中，我们将Member声明在member包中，而后在main包中创建一个变量，但由于结构体的字段包外不可见，因此无法为字段赋初始值，无法按字段还是按索引赋值，都会引发panic错误。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> member</span><br><span class="line"><span class="keyword">type</span> Member <span class="keyword">struct</span> &#123;</span><br><span class="line">    id     <span class="keyword">int</span></span><br><span class="line">    name   <span class="keyword">string</span></span><br><span class="line">    email  <span class="keyword">string</span></span><br><span class="line">    gender <span class="keyword">int</span></span><br><span class="line">    age    <span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line">fun main()&#123;</span><br><span class="line">    <span class="keyword">var</span> m = member.Member&#123;<span class="number">1</span>,<span class="string">"小明"</span>,<span class="string">"xiaoming@163.com"</span>,<span class="number">1</span>,<span class="number">18</span>&#125;<span class="comment">//会引发panic错误</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>因此，如果想在一个包中访问另一个包中结构体的字段，则必须是大写字母开头的变量，即可导出的变量，如：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Member <span class="keyword">struct</span> &#123;</span><br><span class="line">    Id     <span class="keyword">int</span></span><br><span class="line">    Name   <span class="keyword">string</span></span><br><span class="line">    Email  <span class="keyword">string</span></span><br><span class="line">    Gender <span class="keyword">int</span></span><br><span class="line">    Age    <span class="keyword">int</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>Tags<br>在定义结构体字段时，除字段名称和数据类型外，还可以使用反引号为结构体字段声明元信息，这种元信息称为Tag，用于编译阶段关联到字段当中,如我们将上面例子中的结构体修改为：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Member <span class="keyword">struct</span> &#123;</span><br><span class="line">    Id     <span class="keyword">int</span>    <span class="string">`json:"id,-"`</span></span><br><span class="line">    Name   <span class="keyword">string</span> <span class="string">`json:"name"`</span></span><br><span class="line">    Email  <span class="keyword">string</span> <span class="string">`json:"email"`</span></span><br><span class="line">    Gender <span class="keyword">int</span>    <span class="string">`json:"gender,"`</span></span><br><span class="line">    Age    <span class="keyword">int</span>    <span class="string">`json:"age"`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上面例子演示的是使用encoding/json包编码或解码结构体时使用的Tag信息。</p>
<p>Tag由反引号括起来的一系列用空格分隔的key:”value”键值对组成，如：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">Id <span class="keyword">int</span> <span class="string">`json:"id" gorm:"AUTO_INCREMENT"`</span></span><br></pre></td></tr></table></figure></p>
<p>特性<br>下面总结几点结构体的相关特性：</p>
<p>值传递</p>
<p>结构体与数组一样，是复合类型，无论是作为实参传递给函数时，还是赋值给其他变量，都是值传递，即复一个副本。</p>
<p>没有继承</p>
<p>Go语言是支持面向对象编程的，但却没有继承的概念，在结构体中，可以通过组合其他结构体来构建更复杂的结构体。</p>
<p>结构体不能包含自己</p>
<p>一个结构体，并没有包含自身，比如Member中的字段不能是Member类型，但却可能是*Member。</p>
<p>方法<br>在Go语言中，将函数绑定到具体的类型中，则称该函数是该类型的方法，其定义的方式是在func与函数名称之间加上具体类型变量，这个类型变量称为方法接收器，如：</p>
<p>注意，并不是只有结构体才能绑定方法，任何类型都可以绑定方法，只是我们这里介绍将方法绑定到结构体中。</p>
<p>func setName(m Member,name string){//普通函数<br>    m.Name = name<br>}</p>
<p>func (m Member)setName(name string){//绑定到Member结构体的方法<br>    m.Name = name<br>}<br>复制代码<br>从上面的例子中，我们可以看出，通过方法接收器可以访问结构体的字段，这类似其他编程语言中的this关键词，但在Go语言中，只是一个变量名而已，我们可以任意命名方法接收器。</p>
<p>调用结构体的方法，与调用字段一样：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">m := Member&#123;&#125;</span><br><span class="line">m.setName(<span class="string">"小明"</span>)</span><br><span class="line">fmt.Println(m.Name)<span class="comment">//输出为空</span></span><br></pre></td></tr></table></figure></p>
<p>上面的代码中，我们会很奇怪，不是调用setName()方法设置了字段Name的值了吗？为什么还是输出为空呢？</p>
<p>这是因为，结构体是值传递，当我们调用setName时，方法接收器接收到是只是结构体变量的一个副本，通过副本对值进行修复，并不会影响调用者，因此，我们可以将方法接收器定义为指针变量，就可达到修改结构体的目的了。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(m *Member)</span><span class="title">setName</span><span class="params">(name <span class="keyword">string</span>)</span></span>&#123;/将Member改为*Member</span><br><span class="line">    m.Name = name</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">m := Member&#123;&#125;</span><br><span class="line">m.setName(<span class="string">"小明"</span>)</span><br><span class="line">fmt.Println(m.Name)<span class="comment">//小明</span></span><br></pre></td></tr></table></figure></p>
<p>方法和字段一样，如果首字母为小写，则只允许在包内可见，在其他包中是无法访问的，因此，如果要在其他包中访问setName,则应该将方法名改为SetName。</p>
<p>组合<br>我们知道，结构体中并没有继承的概念，其实，在Go语言中也没有继承的概念，Go语言的编程哲学里，推荐使用组合的方式来达到代码复用效果。</p>
<p>什么是组合</p>
<p>组合，可以理解为定义一个结构体中，其字段可以是其他的结构体，这样，不同的结构体就可以共用相同的字段。</p>
<p>注意，在记得我们前面提过的，结构体不能包含自身，但可能包含指向自身的结构体指针。</p>
<p>例如，我们定义了一个名为Animal表示动物，如果我们想定义一个结构体表示猫，如：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Animal <span class="keyword">struct</span> &#123;</span><br><span class="line">    Name   <span class="keyword">string</span>  <span class="comment">//名称</span></span><br><span class="line">    Color  <span class="keyword">string</span>  <span class="comment">//颜色</span></span><br><span class="line">    Height <span class="keyword">float32</span> <span class="comment">//身高</span></span><br><span class="line">    Weight <span class="keyword">float32</span> <span class="comment">//体重</span></span><br><span class="line">    Age    <span class="keyword">int</span>     <span class="comment">//年龄</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//奔跑</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a Animal)</span><span class="title">Run</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(a.Name + <span class="string">"is running"</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//吃东西</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a Animal)</span><span class="title">Eat</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(a.Name + <span class="string">"is eating"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">type</span> Cat <span class="keyword">struct</span> &#123;</span><br><span class="line">    a Animal</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> c = Cat&#123;</span><br><span class="line">	    a: Animal&#123;</span><br><span class="line">            Name:   <span class="string">"猫猫"</span>,</span><br><span class="line">            Color:  <span class="string">"橙色"</span>,</span><br><span class="line">            Weight: <span class="number">10</span>,</span><br><span class="line">            Height: <span class="number">30</span>,</span><br><span class="line">            Age:    <span class="number">5</span>,</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;</span><br><span class="line">    fmt.Println(c.a.Name)</span><br><span class="line">    c.a.Run()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到，我们定义Cat结构体时，可以把Animal结构体作为Cat的字段。</p>
<p>匿名字段</p>
<p>上面的例子，我们看到，把Animal结构体作为Cat的字段时，其变量名为a，所以我们访问Animal的方法时，语法为c.a.Run(),这种通过叶子属性访问某个字段类型所带的方法和字段用法非常繁琐。</p>
<p>Go语言支持直接将类型作为结构体的字段，而不需要取变量名，这种字段叫匿名字段，如：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Lion <span class="keyword">struct</span> &#123;</span><br><span class="line">	Animal <span class="comment">//匿名字段</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">var</span> lion = Lion&#123;</span><br><span class="line">        Animal&#123;</span><br><span class="line">            Name:  <span class="string">"小狮子"</span>,</span><br><span class="line">            Color: <span class="string">"灰色"</span>,</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;</span><br><span class="line">    lion.Run()</span><br><span class="line">    fmt.Println(lion.Name)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>通过上面例子，可以看到，通过匿名字段组合其他类型，而后访问匿名字段类型所带的方法和字段时，不需要使用叶子属性，非常方便。</p>
<p>小结<br>在Go语言编程中，结构体大概算是使用得最多的数据类型了，通过定义不同字段和方法的结构体，抽象组合不同的结构体，这大概便是Go语言中对面向对象编程了。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>详解Go逃逸分析</title>
    <url>/go/%E8%AF%A6%E8%A7%A3Go%E9%80%83%E9%80%B8%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<p>原文链接<a href="https://juejin.cn/post/6917201712029630478" rel="external nofollow noopener noreferrer" target="_blank">机器铃砍菜刀</a></p>
<p>Go是一门带有垃圾回收的现代语言，它抛弃了传统C/C++的开发者需要手动管理内存的方式，实现了内存的主动申请和释放的管理。Go的垃圾回收，让堆和栈的概念对程序员保持透明，它增加的逃逸分析与GC，使得程序员的双手真正地得到了解放，给了开发者更多的精力去关注软件设计本身。</p>
<p>就像《CPU缓存体系对Go程序的影响》文章中说过的一样，“你不一定需要成为一名硬件工程师，但是你确实需要了解硬件的工作原理”。Go虽然帮我们实现了内存的自动管理，我们仍然需要知道其内在原理。内存管理主要包括两个动作：分配与释放。逃逸分析就是服务于内存分配，为了更好理解逃逸分析，我们先谈一下堆栈。</p>
<p>堆和栈</p>
<p>应用程序的内存载体，我们可以简单地将其分为堆和栈。</p>
<p>在Go中，栈的内存是由编译器自动进行分配和释放，栈区往往存储着函数参数、局部变量和调用函数帧，它们随着函数的创建而分配，函数的退出而销毁。一个goroutine对应一个栈，栈是调用栈（call stack）的简称。一个栈通常又包含了许多栈帧（stack frame），它描述的是函数之间的调用关系，每一帧对应一次尚未返回的函数调用，它本身也是以栈形式存放数据。</p>
<p>举例：在一个goroutine里，函数A()正在调用函数B()，那么这个调用栈的内存布局示意图如下。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/fbd21177b93826eda5b41c6f610b7f13.png" alt></p>
<p>与栈不同的是，应用程序在运行时只会存在一个堆。狭隘地说，内存管理只是针对堆内存而言的。程序在运行期间可以主动从堆上申请内存，这些内存通过Go的内存分配器分配，并由垃圾收集器回收。</p>
<p>栈是每个goroutine独有的，这就意味着栈上的内存操作是不需要加锁的。而堆上的内存，有时需要加锁防止多线程冲突（为什么要说有时呢，因为Go的内存分配策略学习了TCMalloc的线程缓存思想，他为每个处理器P分配了一个mcache，从mcache分配内存也是无锁的）。</p>
<p>而且，对于程序堆上的内存回收，还需要通过标记清除阶段，例如Go采用的三色标记法。但是，在栈上的内存而言，它的分配与释放非常廉价。简单地说，它只需要两个CPU指令：一个是分配入栈，另外一个是栈内释放。而这，只需要借助于栈相关寄存器即可完成。</p>
<p>另外还有一点，栈内存能更好地利用CPU的缓存策略。因为它们相较于堆而言是更连续的。</p>
<p>逃逸分析</p>
<p>那么，我们怎么知道一个对象是应该放在堆内存，还是栈内存之上呢？可以官网的FAQ（地址：golang.org/doc/faq）中找到…</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2784feee73aee22f252f9d1ed1f1da7c.png" alt></p>
<p>如果可以，Go编译器会尽可能将变量分配到到栈上。但是，当编译器无法证明函数返回后，该变量没有被引用，那么编译器就必须在堆上分配该变量，以此避免悬挂指针（dangling pointer）。另外，如果局部变量非常大，也会将其分配在堆上。</p>
<p>那么，Go是如何确定的呢？答案就是：逃逸分析。编译器通过逃逸分析技术去选择堆或者栈，逃逸分析的基本思想如下：检查变量的生命周期是否是完全可知的，如果通过检查，则可以在栈上分配。否则，就是所谓的逃逸，必须在堆上进行分配。</p>
<p>Go语言虽然没有明确说明逃逸分析规则，但是有以下几点准则，是可以参考的。</p>
<p>逃逸分析是在编译器完成的，这是不同于jvm的运行时逃逸分析;<br>如果变量在函数外部没有引用，则优先放到栈中；<br>如果变量在函数外部存在引用，则必定放在堆中；<br>我们可通过go build -gcflags ‘-m -l’命令来查看逃逸分析结果，其中-m 打印逃逸分析信息，-l禁止内联优化。下面，我们通过一些案例，来熟悉一些常见的逃逸情况。</p>
<p>情况一：变量类型不确定<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	a := <span class="number">666</span></span><br><span class="line">	fmt.Println(a)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>逃逸分析结果如下<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"> $ <span class="keyword">go</span> build -gcflags <span class="string">'-m -l'</span> main.<span class="keyword">go</span></span><br><span class="line"># command-line-arguments</span><br><span class="line">./main.<span class="keyword">go</span>:<span class="number">7</span>:<span class="number">13</span>: ... argument does not escape</span><br><span class="line">./main.<span class="keyword">go</span>:<span class="number">7</span>:<span class="number">13</span>: a escapes to heap</span><br></pre></td></tr></table></figure></p>
<p>可以看到，分析结果告诉我们变量a逃逸到了堆上。但是，我们并没有外部引用啊，为啥也会有逃逸呢？为了看到更多细节，可以在语句中再添加一个-m参数。得到信息如下<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"> $ go build -gcflags <span class="string">'-m -m -l'</span> main.go</span><br><span class="line"><span class="comment"># command-line-arguments</span></span><br><span class="line">./main.go:7:13: a escapes to heap:</span><br><span class="line">./main.go:7:13:   flow: &#123;storage <span class="keyword">for</span> ... argument&#125; = &amp;&#123;storage <span class="keyword">for</span> a&#125;:</span><br><span class="line">./main.go:7:13:     from a (spill) at ./main.go:7:13</span><br><span class="line">./main.go:7:13:     from ... argument (slice-literal-element) at ./main.go:7:13</span><br><span class="line">./main.go:7:13:   flow: &#123;heap&#125; = &#123;storage <span class="keyword">for</span> ... argument&#125;:</span><br><span class="line">./main.go:7:13:     from ... argument (spill) at ./main.go:7:13</span><br><span class="line">./main.go:7:13:     from fmt.Println(... argument...) (call parameter) at ./main.go:7:13</span><br><span class="line">./main.go:7:13: ... argument does not escape</span><br><span class="line">./main.go:7:13: a escapes to heap</span><br></pre></td></tr></table></figure></p>
<p>a逃逸是因为它被传入了fmt.Println的参数中，这个方法参数自己发生了逃逸。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Println</span><span class="params">(a ...<span class="keyword">interface</span>&#123;&#125;)</span> <span class="params">(n <span class="keyword">int</span>, err error)</span></span></span><br></pre></td></tr></table></figure></p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">因为fmt.Println的函数参数为<span class="keyword">interface</span>类型，编译期不能确定其参数的具体类型，所以将其分配于堆上。</span><br></pre></td></tr></table></figure>
<p>情况二：暴露给外部指针<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">foo</span><span class="params">()</span> *<span class="title">int</span></span> &#123;</span><br><span class="line">	a := <span class="number">666</span></span><br><span class="line">	<span class="keyword">return</span> &amp;a</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	_ = foo()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>逃逸分析如下，变量a发生了逃逸。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"> $ go build -gcflags <span class="string">'-m -m -l'</span> main.go</span><br><span class="line"><span class="comment"># command-line-arguments</span></span><br><span class="line">./main.go:4:2: a escapes to heap:</span><br><span class="line">./main.go:4:2:   flow: ~r0 = &amp;a:</span><br><span class="line">./main.go:4:2:     from &amp;a (address-of) at ./main.go:5:9</span><br><span class="line">./main.go:4:2:     from <span class="built_in">return</span> &amp;a (<span class="built_in">return</span>) at ./main.go:5:2</span><br><span class="line">./main.go:4:2: moved to heap: a</span><br></pre></td></tr></table></figure></p>
<p>这种情况直接满足我们上述中的原则：变量在函数外部存在引用。这个很好理解，因为当函数执行完毕，对应的栈帧就被销毁，但是引用已经被返回到函数之外。如果这时外部从引用地址取值，虽然地址还在，但是这块内存已经被释放回收了，这就是非法内存，问题可就大了。所以，很明显，这种情况必须分配到堆上。</p>
<p>情况三：变量所占内存较大<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">foo</span><span class="params">()</span></span> &#123;</span><br><span class="line">	s := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="number">10000</span>, <span class="number">10000</span>)</span><br><span class="line">	<span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="built_in">len</span>(s); i++ &#123;</span><br><span class="line">		s[i] = i</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	foo()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>逃逸分析结果<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build -gcflags <span class="string">'-m -m -l'</span> main.go</span><br><span class="line"><span class="comment"># command-line-arguments</span></span><br><span class="line">./main.go:4:11: make([]int, 10000, 10000) escapes to heap:</span><br><span class="line">./main.go:4:11:   flow: &#123;heap&#125; = &amp;&#123;storage <span class="keyword">for</span> make([]int, 10000, 10000)&#125;:</span><br><span class="line">./main.go:4:11:     from make([]int, 10000, 10000) (too large <span class="keyword">for</span> stack) at ./main.go:4:11</span><br><span class="line">./main.go:4:11: make([]int, 10000, 10000) escapes to heap</span><br></pre></td></tr></table></figure></p>
<p>可以看到，当我们创建了一个容量为10000的int类型的底层数组对象时，由于对象过大，它也会被分配到堆上。这里我们不禁要想一个问题，为啥大对象需要分配到堆上。</p>
<p>这里需要注意，在上文中没有说明的是：在Go中，执行用户代码的goroutine是一种用户态线程，其调用栈内存被称为用户栈，它其实也是从堆区分配的，但是我们仍然可以将其看作和系统栈一样的内存空间，它的分配和释放是通过编译器完成的。与其相对应的是系统栈，它的分配和释放是操作系统完成的。在GMP模型中，一个M对应一个系统栈（也称为M的g0栈），M上的多个goroutine会共享该系统栈。</p>
<p>不同平台上的系统栈最大限制不同。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">ulimit</span> -s</span><br><span class="line">8192</span><br></pre></td></tr></table></figure></p>
<p>以x86_64架构为例，它的系统栈大小最大可为8Mb。我们常说的goroutine初始大小为2kb，其实说的是用户栈，它的最小和最大可以在runtime/stack.go中找到，分别是2KB和1GB。</p>
<p>// The minimum size of stack used by Go code<br>_StackMin = 2048<br>…<br>var maxstacksize uintptr = 1 &lt;&lt; 20 // enough until runtime.main sets it for real<br>复制代码<br>而堆则会大很多，从1.11之后，Go采用了稀疏的内存布局，在Linux的x86-64架构上运行时，整个堆区最大可以管理到256TB的内存。所以，为了不造成栈溢出和频繁的扩缩容，大的对象分配在堆上更加合理。那么，多大的对象会被分配到堆上呢。</p>
<p>通过测试，小菜刀发现该大小为64KB（这在Go内存分配中是属于大对象的范围：&gt;32kb），即s :=make([]int, n, n)中，一旦n达到8192，就一定会逃逸。注意，网上有人通过fmt.Println(unsafe.Sizeof(s))得到s的大小为24字节，就误以为只需分配24个字节的内存，这是错误的，因为实际还有底层数组的内存需要分配。</p>
<p>情况四：变量大小不确定</p>
<p>我们将情况三种的示例，简单更改一下。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">foo</span><span class="params">()</span></span> &#123;</span><br><span class="line">	n := <span class="number">1</span></span><br><span class="line">	s := <span class="built_in">make</span>([]<span class="keyword">int</span>, n)</span><br><span class="line">	<span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="built_in">len</span>(s); i++ &#123;</span><br><span class="line">		s[i] = i</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	foo()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>得到逃逸分析结果如下<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build -gcflags <span class="string">'-m -m -l'</span> main.go</span><br><span class="line"><span class="comment"># command-line-arguments</span></span><br><span class="line">./main.go:5:11: make([]int, n) escapes to heap:</span><br><span class="line">./main.go:5:11:   flow: &#123;heap&#125; = &amp;&#123;storage <span class="keyword">for</span> make([]int, n)&#125;:</span><br><span class="line">./main.go:5:11:     from make([]int, n) (non-constant size) at ./main.go:5:11</span><br><span class="line">./main.go:5:11: make([]int, n) escapes to heap</span><br></pre></td></tr></table></figure></p>
<p>这次，我们在make方法中，没有直接指定大小，而是填入了变量n，这时Go逃逸分析也会将其分配到堆区去。可见，为了保证内存的绝对安全，Go的编译器可能会将一些变量不合时宜地分配到堆上，但是因为这些对象最终也会被垃圾收集器处理，所以也能接受。</p>
<p>总结</p>
<p>本文只列举了逃逸分析的部分例子，实际的情况还有很多，理解思想最重要。这里就不过多列举了。</p>
<p>既然Go的堆栈分配对于开发者来说是透明的，编译器已经通过逃逸分析为对象选择好了分配方式。那么我们还可以从中获益什么？</p>
<p>答案是肯定的，理解逃逸分析一定能帮助我们写出更好的程序。知道变量分配在栈堆之上的差别，那么我们就要尽量写出分配在栈上的代码，堆上的变量变少了，可以减轻内存分配的开销，减小gc的压力，提高程序的运行速度。</p>
<p>所以，你会发现有些Go上线项目，它们在函数传参的时候，并没有传递结构体指针，而是直接传递的结构体。这个做法，虽然它需要值拷贝，但是这是在栈上完成的操作，开销远比变量逃逸后动态地在堆上分配内存少的多。当然该做法不是绝对的，如果结构体较大，传递指针将更合适。</p>
<p>因此，从GC的角度来看，指针传递是个双刃剑，需要谨慎使用，否则线上调优解决GC延时可能会让你崩溃。</p>
<ol>
<li>闭包造成的逃逸</li>
<li>返回指向栈变量的指针</li>
<li>申请大对象造成的逃逸</li>
<li>申请可变长空间造成的逃逸</li>
<li>返回局部引用的 slice 造成的逃逸</li>
<li>返回局部引用的 map 造成的逃逸</li>
<li>返回函数造成的逃逸</li>
<li>逃逸是在编译期间完成的，主要是决定是在栈中或者堆中分配内存?</li>
</ol>
]]></content>
      <categories>
        <category>go</category>
        <category>go内存详解</category>
      </categories>
      <tags>
        <tag>gc</tag>
        <tag>s</tag>
      </tags>
  </entry>
  <entry>
    <title>深入 Go Golang 内存分配超级棒的文章：Go 内存分配器可视化指南</title>
    <url>/go/%E6%B7%B1%E5%85%A5%20Go%20Golang%20%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E8%B6%85%E7%BA%A7%E6%A3%92%E7%9A%84%E6%96%87%E7%AB%A0%EF%BC%9AGo%20%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E5%99%A8%E5%8F%AF%E8%A7%86%E5%8C%96%E6%8C%87%E5%8D%97/</url>
    <content><![CDATA[<p><a href="https://www.toutiao.com/i6777555732402274830/" rel="external nofollow noopener noreferrer" target="_blank">翻译地址</a> <a href="https://github.com/coldnight/go-memory-allocator-visual-guide" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>当我第一次开始尝试理解 Go 语言的内存分配器时，整个过程让我抓狂。一切看起来都像一个神秘的黑盒子。因为几乎所有技术魔法（technical wizardry）都隐藏在抽象之下，所以你需要一层一层的剥离才能去理解它。</p>
<p>我们将通过这篇文章来一层层的剥离这些细节。如果你想学习所有关于 Go 内存分配器的知识，那么这篇文章正适合你。</p>
<p><strong>物理内存和虚拟内存</strong><br>每一个内存分配器都需要运行在由底层操作系统管理的虚拟内存空间（Virtual Memory Space）之上。</p>
<p>下图是一个物理内存单元（Physical Memory Cell）的简要说明（非精准）<br><img src="https://img-blog.csdnimg.cn/img_convert/757b3f20604f0c9b8f01099636b6dee7.png" alt></p>
<p>A simple illustration of a Physical Memory Cell</p>
<p>一个内存单元的概述经过大大简化之后描述如下：</p>
<ol>
<li>地址线（Address line）（晶体管做的开关）用于访问电容器（数据到数据线（Data Lines））。</li>
<li>如果地址线有电流流动（显式为红色），数据线可以写入到电容器，所以电容器带电，逻辑值表示 “1”。</li>
<li>如果地址线没有电流流动（显式为绿色），数据线不可以写入到电容器，所以电容器不带电，逻辑值表示 “0”</li>
<li>当 CPU 需要从 RAM 中“读取”值，则顺着“地址线（ADDRESS LINE）”（关闭开关）发送一个电流。如果电容器带电，则电流流向“数据线（DATA LINE）”（值为 1）；否则没有电流流向数据线，所以电容器保持不带电（值为 0）。<br>下图简单的描述 CPU 和物理内存单元如何交互</li>
</ol>
<p><img src="https://img-blog.csdnimg.cn/img_convert/b15016dc346084277954e3f51fc859a3.png" alt></p>
<p>Simple Illustration of how a Physical Memory Cell interacts with CPU</p>
<p><strong>数据总线（Data Bus）</strong>：用于在 CPU 和内存中间传输数据。</p>
<p>还有一点关于地址线（Address line）和按字节寻址（Addressable bytes）。</p>
<p>下图是 CPU 和物理内存之间地址线的说明</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/dba1b3b1438ee07b15f7a93ecde48744.png" alt></p>
<p>Illustrative Representation of an Address Line between CPU and Physical Memory.</p>
<ol>
<li>DRAM 中的每一个字节都分配了一个唯一的数字标识符（地址）。“物理字节 != 地址线的数量（Physical bytes present != Number of address line）”（e.g. 16 位 Intel 8088、PAE）</li>
<li>每一个“地址线”可以发送 1-bit 的值，用于表示给定字节地址中的“一个位（SINGLE BIT）”</li>
<li>在我们的上面给出的图中，我们有 32 个地址线。所以每个 字节（BYTE） 都有“32 位”作为地址。</li>
<li>[ 00000000000000000000000000000000 ] —  低内存地址</li>
<li>[ 11111111111111111111111111111111 ] —  高内存地址</li>
<li>由于我们每字节都有一个 32 位的地址，所以我们的地址空间包含 2 的 32 次方个可寻址字节（bytes）（4GB）。<br>综上所述，可寻址的字节数量取决于地址总线的数量，所以对于 64 个地址线最大可寻址 2 的 64 次方个字节数（16 EB），但是由于大部分架构实际上仅使用 48-bit 地址线（AMD）和 42-bit 地址线（Intel）作为 64-bit 指针，所以理论上允许 256TB 物理内存（Linux 在 x86-64 下通过 with 4 level page tables 允许每个处理器 128TB 地址空间，Windows 192TB）。</li>
</ol>
<p>由于物理内存的大小是受限制的，所以进程运行在自身的内存沙盒内 – “虚拟内存地址（virtual address space）”，称作 虚拟内存（Virtual Memory）。</p>
<p><strong>字节的地址在这个虚拟地址空间内不再和处理器放在地址总线上的地址相同。</strong>因此必须建立转换数据结构和系统将虚拟地址空间中的字节映射到物理字节。</p>
<p>虚拟地址表示参见下图（/proc/$PID/maps）：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bc389acfd5744779430df0b6bbc10260.png" alt></p>
<p>Virtual Address Space Representation</p>
<p>综上所述当 CPU 执行一个指令需要引用内存地址时。首先将在 VMA（Virtual Memory Areas）中的逻辑地址转换为线性地址。这个转换通过 MMU 完成。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0f6740ce883c37e4fec92d211eefc0ae.png" alt></p>
<p>This is not a physical diagram, only a depiction. address translation process not included for simpl</p>
<p>由于逻辑地址太大几乎很难独立的管理，所以引入术语 页（pages） 进行管理。当必要的分页操作被激活后，虚拟地址空间被分成更小的称作页的区域（大部分操作系统下是 4KB，可以修改）。页是虚拟内存中数据内存管理的最小单元。虚拟内存不存储任何内容，只是简单的将程序地址空间映射到底层物理内存之上。</p>
<p>独立的进程只能使用 VMA 作为他们的地址。所以当我们的程序需要更多 “堆内存（heap memory）时发生了什么？</p>
<p>下图是简单的汇编代码用于分配更多的堆内存</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/db7f3aeefce22897148991117bc3f7a4.png" alt></p>
<p>A simple assembly code asking for more heap memory.</p>
<p>下图描述堆内存的增长</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/8da99ecb767181f619abc41ec29e08be.png" alt></p>
<p>heap memory increment</p>
<p>应用程序通过系统调用 brk[1]（sbrk/mmap 等）获得内存。内核仅更新堆 VMA 并调用它。</p>
<p>当前时间点实际上不分配页帧且新页在物理内存中并不存在。这也是 VSZ 和 RSS 大小的不同点。  </p>
<p>内存分配器<br>通过对“虚拟地址空间”基本了解和它对在堆分配的意义，内存分配器现在变得更加容易解释。</p>
<p>如果堆上有足够的空间的满足我们代码的内存申请，内存分配器可以完成内存申请无需内核参与，否则将通过操作系统调用（brk）进行扩展堆，通常是申请一大块内存。（对于 malloc 大默认指的是大于 MMAP_THRESHOLD 个字节 - 128KB）。</p>
<p>但是，内存分配器除了更新 brk address 还有其他职责。其中主要的一项就是如何减少 内部（internal）和外部（external）碎片和如何快速分配当前块。考虑我们的程序以串行的方式（p1 到 p4）通过 malloc(size) 函数申请一块连续的内存然后通过 free(pointer)函数进行释放。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/cdfbd2ac4f75a49faf6363a6f14dfbb4.png" alt></p>
<p>An external fragmentation demonstration</p>
<p>在 p4 阶段由于内存碎片化即使我们有足够的内存块依然无法满足申请的 6 个连续的内存块。</p>
<p>所以我们该如何减少内存碎片化呢 ？答案取决是使用哪种内存分配算法，也就是使用哪个底层库。</p>
<p>我们将简单看一下一个和 Go 内存分配器建模相近的内存分配器：TCMalloc。</p>
<p><strong>TCMalloc</strong><br>TCMalloc 的核心思想是将内存分为多个级别缩小锁的粒度。在 TCMalloc 内存管理内部分为两个部分：线程内存（thread memory)和页堆（page heap）。</p>
<p><strong>线程内存</strong><br>每一个内存页都被分为多个固定分配大小规格的空闲列表（free list） 用于减少碎片化。这样每一个线程都可以获得一个用于无锁分配小对象的缓存，这样可以让并行程序分配小对象（&lt;=32KB）非常高效。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/9bceaa471793ca81fdcb2601939c589d.png" alt></p>
<p>Thread Cache (Each Thread gets this Thread Local Thread Cache)</p>
<p><strong>页堆</strong><br>TCMalloc 管理的堆由一组页组成，一组连续的页面被表示为 span。当分配的对象大于 32KB，将使用页堆（Page Heap）进行内存分配。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0d3a1e702754c866c10d64fb430cf1e9.png" alt></p>
<p>Page Heap (for span management)</p>
<p>当没有足够的空间分配小对象则会到页堆获取内存。如果页堆页没有足够的内存，则页堆会向操作系统申请更多的内存。</p>
<p>Note: 即使 Go 的内存分配器最初是基于 TCMalloc，但是现在已经有很大的不同。</p>
<p><strong>Go 内存分配器</strong><br>我们知道 Go 运行时（Go Runtime）调度器在调度时会将 Goroutines(G) 绑定到 逻辑处理器（P）(Logical Processors） 运行。类似的，Go 实现的 TCMalloc 将内存页（Memory Pages）分为 67 种不同大小规格的块。</p>
<p>如果你不熟悉 Go 的调度器可以先参见《Go scheduler: Ms, Ps &amp; Gs》,然后继续阅读。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3b1b1cfe0d9b8e2cbf2ed082255afa1e.png" alt></p>
<p>Size Classes in Go</p>
<p>如果页的规格大小为 1KB 那么 Go 管理粒度为 8192B 内存将被切分为 8 个像下图这样的块。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/315ddd8b912f3c12e47a6cfe5cb19d12.png" alt></p>
<p>8 KB page divided into a size class of 1KB (In Go pages are maintained at the granularity of 8KB)</p>
<p>Go 中这些页通过 mspan 结构体进行管理。</p>
<p><strong>mspan</strong><br>简单的说，mspan 是一个包含页起始地址、页的 span 规格和页的数量的双端链表。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d8167eb921bafc7a4d2b9c35312debd9.png" alt></p>
<p>Illustrative Representation of a mspan in Go memory allocator</p>
<p>mcache</p>
<p>Go 像 TCMalloc 一样为每一个 逻辑处理器（P）（Logical Processors） 提供一个本地线程缓存（Local Thread Cache）称作 mcache，所以如果 Goroutine 需要内存可以直接从 mcache中获取，由于在同一时间只有一个 Goroutine 运行在 逻辑处理器（P）（Logical Processors）上，所以中间不需要任何锁的参与。</p>
<p>mcache 包含所有大小规格的 mspan 作为缓存。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/7cfd872d5a7359acba0256135e4439e0.png" alt></p>
<p>Illustrative Representation of a Relationship between P, mcache, and mspan in Go.</p>
<p>由于每个 P 都拥有各自的 mcache，所以从 mcache 分配内存无需持有锁。</p>
<p>对于每一种大小规格都有两个类型：</p>
<p>scan – 包含指针的对象。<br>noscan – 不包含指针的对象。<br>采用这种方法的好处之一就是进行垃圾回收时 noscan 对象无需进一步扫描是否引用其他活跃的对象。</p>
<p>mcache 的作用是什么？</p>
<p>&lt;=32K 字节的对象直接使用相应大小规格的 mspan 通过 mcache 分配</p>
<p>当 mcache 没有可用空间时会发生什么？</p>
<p>从 mcentral 的 mspans 列表获取一个新的所需大小规格的 mspan。</p>
<p><strong>mcentral</strong><br>mcentral 对象收集所有给定规格大小的 span。每一个 mcentral 都包含两个 mspan 的列表：</p>
<p>empty mspanList – 没有空闲对象或 span 已经被 mcache 缓存的 span 列表<br>nonempty mspanList – 有空闲对象的 span 列表<br><img src="https://img-blog.csdnimg.cn/img_convert/a6e2b1560c4377405a13273c5783a61e.png" alt></p>
<p>Illustrative Representation of a mcentral</p>
<p>每一个 mcentral 结构体都维护在 mheap 结构体内。</p>
<p><strong>mheap</strong><br>Go 使用 mheap 对象管理堆，只有一个全局变量。持有虚拟地址空间。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/af3fbbf41762e5a5635c580aac66864b.png" alt></p>
<p>Illustrative Representation of a mheap.</p>
<p>就上我们从上图看到的：mheap 存储了 mcentral 的数组。这个数组包含了各个的 span 的 mcentral。</p>
<p>central [numSpanClasses]struct {<br>      mcentral mcentral<br>    pad      [sys.CacheLineSize unsafe.Sizeof(mcentral{})%sys.CacheLineSize]byte<br>}<br>由于我们有各个规格的 span 的 mcentral，当一个 mcache 从 mcentral 申请 mspan 时，只需要在独立的 mcentral 级别中使用锁，所以其它任何 mcache 在同一时间申请不同大小规格的 mspan 将互不受影响可以正常申请。</p>
<p>对齐填充（Padding）用于确保 mcentrals 以 CacheLineSize 个字节数分隔，所以每一个 MCentral.lock 都可以获取自己的缓存行（cache line），以避免伪共享（false sharing）[2]问题。</p>
<p>当 mcentral 列表空的时候会发生什么？mcentral 从 mheap 获取一系列页用于需要的大小规格的 span。</p>
<p>free[_MaxMHeapList]mSpanList：一个 spanList 数组。每一个 spanList 中的 mspan 包含 1 ~ 127（_MaxMHeapList - 1）个页。例如，free[3] 是一个包含 3 个页的 mspan 链表。free 表示 free list，表示未分配。对应 busy list。<br>freelarge mSpanList：一个 mspan 的列表。每一个元素(mspan)的页数大于 127。通过 mtreap 结构体管理。对应 busylarge。<br>大于 32K 的对象被定义为大对象，直接通过 mheap 分配。这些大对象的申请是以一个全局锁为代价的，因此任何给定的时间点只能同时供一个 P 申请。</p>
<p><strong>对象分配流程</strong></p>
<ul>
<li>大于 32K 的大对象直接从 mheap 分配。</li>
<li>小于 16B 的使用 mcache 的微型分配器分配</li>
<li>对象大小在 16B ~ 32K 之间的的，首先通过计算使用的大小规格，然后使用 mcache 中对应大小规格的块分配</li>
<li>如果对应的大小规格在 mcache 中没有可用的块，则向 mcentral 申请</li>
<li>如果 mcentral 中没有可用的块，则向 mheap 申请，并根据 BestFit 算法找到最合适的 mspan。如果申请到的 mspan 超出申请大小，将会根据需求进行切分，以返回用户所需的页数。剩余的页构成一个新的 mspan 放回 mheap 的空闲列表。</li>
<li>如果 mheap 中没有可用 span，则向操作系统申请一系列新的页（最小 1MB）。但是 Go 会在操作系统分配超大的页（称作 arena）。分配一大批页会减少和操作系统通信的成本。<br><strong>所有在堆上的内存申请都来自 arena。让我们看看 arena 是什么。</strong></li>
</ul>
<p><strong>Go 虚拟内存</strong><br>让我们看一个简单的 Go 程序的内存情况</p>
<p>func main() {<br>    for {}<br>}<br><img src="https://img-blog.csdnimg.cn/img_convert/76c3af6e91b62c6d8f679184d6851f57.png" alt></p>
<p>process stats for a program</p>
<p>从上面可以即使是一个简单的程序虚拟空间占用页大概 ~100MB 左右，但是 RSS 仅仅占用 696KB。让我们先搞清楚这之间的差异。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bc6bd670856985a354aaec18fa00f224.png" alt></p>
<p>map and smap stats.</p>
<p>这里有一块内存区域大小在 ~ 2MB、64MB 和 32MB。这些是什么？</p>
<p><strong>Arena</strong><br>事实证明 Go 的虚拟内存布局中包含一系列 arenas。初始的堆映射是一个 arena，如 64MB（基于 go 1.11.5）。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/086abb2e6e629a1d38546f8b5ae92bd3.png" alt></p>
<p>current incremental arena size on a different system.</p>
<p>所以当前内存根据我们的程序需要以小增量映射，并且初始于一个 arena（~64MB）。</p>
<p>请首先记住这些数字。主题开始改变。早期 Go 需要预先保留一个连续的虚拟地址，在一个 64-bit 的系统 arena 的大小是 512GB。（如果分配的足够大且 被 mmap 拒绝 会发生什么？）</p>
<p>这些 arenas 就是我们所说的堆。在 Go 中每一个 arena 都以 8192B 的粒度的页进行管理。</p>
<p>下图表示一个 64MB 的 arena</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/722069407caa1d39b975e72d14670e9f.png" alt></p>
<p>Single arena ( 64 MB ).</p>
<p>Go 同时存在其他两个块：span 和 bitmap。两者都在堆外分配并且包含每个 arena 的元数据。大多用于垃圾回收期间（所以我们就讨论到这）。</p>
<p>在我们刚刚讨论的 Go 的内存分配策略种类里，只涉及到内存分配奇妙且多样性的冰山一角。</p>
<p>然而，Go 内存管理的一般思想是使用不同的内存结构为不同大小的对象使用不同的内存缓存级别来分配内存。将一个从操作系统接收的连续地址的块切分到多级缓存来减少锁的使用，同时根据指定的大小分配内存减少内存碎片以提高内存分配的效率和在内存释放之后加快 GC 运行的速度。</p>
<p>现在我们将通过下图结束 Go 内存分配可视化指南。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/8a95d61e37a177839e4cbf1c888f3797.png" alt></p>
<p>Visual Overview of Runtime Memory Allocator.</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go内存详解</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go内存详解</tag>
      </tags>
  </entry>
  <entry>
    <title>7部史诗级自然纪录片，孩子14岁前要看完！</title>
    <url>/%E5%85%B4%E8%B6%A3%E7%88%B1%E5%A5%BD/7%E9%83%A8%E5%8F%B2%E8%AF%97%E7%BA%A7%E8%87%AA%E7%84%B6%E7%BA%AA%E5%BD%95%E7%89%87%EF%BC%8C%E5%AD%A9%E5%AD%9014%E5%B2%81%E5%89%8D%E8%A6%81%E7%9C%8B%E5%AE%8C%EF%BC%81/</url>
    <content><![CDATA[<h1 id="7部史诗级自然纪录片，孩子14岁前要看完！"><a href="#7部史诗级自然纪录片，孩子14岁前要看完！" class="headerlink" title="7部史诗级自然纪录片，孩子14岁前要看完！"></a>7部史诗级自然纪录片，孩子14岁前要看完！</h1><p>以下内容转载自 <a href="https://www.toutiao.com/i6636908732494119437/" rel="external nofollow noopener noreferrer" target="_blank">https://www.toutiao.com/i6636908732494119437/</a></p>
<p>原创布谷学习2018-12-21 12:01:00</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/45cca0c4afff53a9ff334c14bd6eb94f.gif" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p>文、编辑 | 云雀儿</p>
<p>来源 | 布谷听听（iBookgood）</p>
<p>世界那么大，你想去看看吗？</p>
<p>似乎穷极一生，我们也看不完这个世界，不过纪录片却可以帮我们做到。</p>
<p>孩子无论大小，都会喜欢关于自然的纪录片，喜欢看在野外的动物是如何生活的。好的自然纪录片不但能满足孩子们的好奇心，激发他们探索的热情，更是一种对心灵的滋养。</p>
<p><strong>都说美国的孩子从小看Discovery、看国家地理频道等，除了学到了不少知识、保持想象力以及了解宇宙和周遭世界，也让孩子们对大自然和生命一直充满了敬畏之心！</strong></p>
<p><strong>看到那些或者威风凛凛，或者呆萌可爱的动物们，他们都在全力生存，努力去代代传承，孩子没办法不被触动。</strong></p>
<p>拜伦曾经说过：我不是不爱人类，而是更爱大自然。</p>
<p>看到我们赖以生存的地球是如此美好，孩子才能保持激情，积极面对生活，积极去成长。</p>
<p>相信这7部纪录片，一定能让我们和孩子真正地理解自然，带着敬畏之心去守护他们。</p>
<p><strong>1. 地球脉动</strong></p>
<p>在豆瓣几乎满分的纪录片！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/13f73b1a6c9af517346297b0b78abe33.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p>推荐指数：★★★★★★</p>
<p><strong>豆瓣评分：</strong> 9.7</p>
<p><strong>影片简介</strong>：</p>
<p>从南极到北极，从赤道到寒带，从非洲草原到热带雨林，再从荒凉峰顶到深邃大海，难以数计的生物以极其绝美的身姿呈现在世人面前。</p>
<p>我们看到了奥卡万戈洪水的涨落及其周边赖以生存的动物们的生存状态，看到了罕见的雪豹在漫天大雪中猎食的珍贵画面；看到了冰原上企鹅、北极熊、海豹等生物相互依存的严苛情景，也见识了生活在大洋深处火山口高温环境下的惊奇生物。</p>
<p><strong>关 键 词 ：全面 高分</strong></p>
<p><strong>推荐理由：</strong></p>
<p><strong>最美的镜头永远诞生在最偏远和危险的地区，</strong>摄制组历尽艰辛，期间还采用美国军方发明的空中摄影机拍摄，可从一公里外拍摄某物体的超清晰特写。</p>
<p><strong>暑往寒来，流风回雪之间，大自然的万物演绎着生命的传奇，美得让人窒息。</strong>有些画面也会让孩子笑破肚皮，有些也会让我们和孩子难过地想哭，这些不可思议的画面，让我们超近距离地感受大自然的奇妙与壮美。</p>
<p><strong>你可以和孩子聊聊这些：</strong></p>
<p>\1. 让孩子总结一下看到哪里的风景最震撼。</p>
<p>\2. 让孩子谈谈对自然演变的看法。</p>
<p><strong>三张图感受一下这部片：</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/358584e5796ac1d8a6933939c1254905.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/39d91307cbb0a75ed189024f02d9e90a.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/4f1226471bf507a535ecbd035f08f6e9.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><strong>2. 可爱的动物</strong></p>
<p>最搞笑的纪录片</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/41a6e619397d9a0b65b6605a14edc8d9.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p>推荐指数：★★★★★★</p>
<p><strong>豆瓣评分：</strong>9.5</p>
<p><strong>影片简介</strong>：</p>
<p>这部纪录片由西至东介绍了纳米比亚沙漠中的红白沙漠、绿洲与卡里哈利沙漠中的动物居民。导演以愉悦的心情、平等的心态展示了狒狒家族的沙上杂技、被未婚妻强迫返工的织布鸟小伙子、吃下了腐烂发酵果实导致醉酒的动物们……</p>
<p>同时亦有严酷干旱季节中自然淘汰的伤感段落——鹈鹕父母们不得不抛弃尚未学会飞翔的子女，龟裂的地面上留下一行行干瘪的幼小身体……</p>
<p><strong>关 键 词 ：幽默 可爱</strong></p>
<p><strong>推荐理由：</strong></p>
<p><strong>四年沙漠中的艰苦拍摄，换来的是满满的生机和可爱。</strong>动物界本是残酷的，可在导演的眼里，这一切大自然的生物，不论好坏都是上帝的恩赐。</p>
<p><strong>这部片子片如其名，真是太可爱了，特别适合孩子看，可以说最搞笑的纪录片了。</strong>用尾巴伪装成叶子捕猎的蛇，打高难度360度跟斗的猴子，还有看到蛇会吓”晕”的拂拂，甚至连各种平时觉得恶心的毛毛虫，在本片中都显得可爱异常！</p>
<p><strong>你可以和孩子聊聊这些：</strong></p>
<p>\1. 和孩子一起模仿一下动物的各种奇葩动作。</p>
<p>\2. 一起分析一下动物们为什么会有这种举动。</p>
<p><strong>三张图感受一下这部片：</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/912e48a4c92bd7ad0e6e34cdaf7252d2.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/323a76588a5b5ae56c4853c444ab72e7.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/72a1254dd1c126394081a5a9d3c43054.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><strong>3. 海洋</strong></p>
<p>看到海洋的神秘唯美</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/6b371f829e6257a05da928902f664e18.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p>推荐指数：★★★★★</p>
<p><strong>豆瓣评分：</strong>9.0</p>
<p><strong>影片简介</strong>：</p>
<p>海洋是什么？这部电影告诉你答案。这是一部以环保为主题，令人心旷神怡、叹为观止的生态学纪录片。本片聚焦于覆盖着地球表面的四分之三的“蓝色领土”。导演深入探索这个幽深而富饶的神秘世界、完整地呈现海洋的壮美辽阔。</p>
<p>真实的动物世界的冒险远比动画片中的故事来的精彩——巨大的水母群、露脊鲸、大白鲨、企鹅等。毫不吝啬在镜头前展示他们旺盛的生命力，让人叹为观止。</p>
<p><strong>关 键 词 ：海底 环保</strong></p>
<p><strong>推荐理由：</strong></p>
<p><strong>与其他自然类纪录片不同，《海洋》的重点不在传播知识，而在纪录。</strong>纪录大自然的美丽，纪录自然界的生命，也记录了人类的丑陋。</p>
<p><strong>它让我们看到了海洋的神秘唯美，也向我们展示了遭受人类破坏的海洋有多么残破脆弱。</strong>相信看过这部片子以后，会在孩子们心里埋下一颗善良的种子，时刻提醒自己地球不只属于人类，也属于生活在这里的每一个生灵。</p>
<p><strong>你可以和孩子聊聊这些：</strong></p>
<p>\1. 谈谈怎样去加强自然保护。</p>
<p>\2. 问问孩子最喜欢哪种海洋生物？</p>
<p><strong>三张图感受一下这部片：</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/fcad06b3c5dae2ab24b65262d6247d0c.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/7f82f9b2b9f0b04ebe04104205a776ba.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/37e0e711e72a41521ce7d3b2a876dba3.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><strong>4. 大迁徙</strong></p>
<p>动物们从哪儿来，又要到哪儿去呢？</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d2e4fe36b6aa2f3afa0dd38e41eeec15.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p>推荐指数：★★★★★</p>
<p><strong>豆瓣评分：</strong>9.4</p>
<p><strong>影片简介</strong>：</p>
<p>每天地球上都有数以百万计的动物加入迁徙行列，不论是天上飞的还是地上跑的。牠们是动物界的浪人，一生下来就注定了飘泊的命运。从最小的蝴蝶到最大的牛羚，这些旅者的生存取决于随时上路的能力。</p>
<p>国家地理频道以前所未有的方式展现这些壮丽、惊人又极具启发性的故事。耗费三年制作，遍及全球并以尖端技术拍摄，呈现了生死奋斗与重生的故事，这是世上最感人的故事。</p>
<p><strong>关 键 词 ：漂泊 旅行的动物</strong></p>
<p><strong>推荐理由：</strong></p>
<p><strong>孩子可能会好奇，换季的时候，那些天空中的鸟到底会迁徙到哪里去呢？这部纪录片就可以告诉他。</strong></p>
<p>在这里，孩子会看到数百万只的动物，数千里的路程，数百个故事。他们从哪儿来，又要到哪儿去呢？一起感受神奇的大自然，赞叹、敬畏、热爱吧！</p>
<p><strong>你可以和孩子聊聊这些：</strong></p>
<p>\1. 让孩子说说动物为何会迁徙？</p>
<p>\2. 让孩子说说亲眼看见过哪些动物迁徙。</p>
<p><strong>三张图感受一下这部片：</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2c4221e7abbeb5fd8140b537e8ec08dc.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5c3d681b7005ece30c590e3171dc68aa.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/52d37b9af2d7127a6106932f76eca82e.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><strong>5. 大猫</strong></p>
<p>大猫，你敢吸吗？</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/1498554860d2c8f9d344ef628722f41e.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p>推荐指数：★★★★★</p>
<p><strong>豆瓣评分：</strong>9.5</p>
<p><strong>影片简介</strong>：</p>
<p>同一个家族，40张不同的面孔，有的风驰电掣，有的力大无穷，有的小巧玲珑，有的健硕无比，它们在地球的各个角落繁衍生息，从冰天雪地的北方，到干燥炙热的沙漠，从最偏远的不毛之地，到现代城市的中心，它们就是猫科动物。</p>
<p>本片将有非洲狮、虎、美洲豹、金钱豹、猎豹、云豹、雪豹、美洲狮、猞猁、豹猫、虎猫、渔猫、黑足猫、兔狲、薮猫、沙漠猫…等等很多野生猫科动物纷纷出镜。</p>
<p><strong>关 键 词 ：猫科动物 吸猫</strong></p>
<p><strong>推荐理由：</strong></p>
<p>现在大家都爱吸猫，可大猫你们敢吸吗？这部纪录片也许会颠覆你的认知，让你从此迷上大猫们哦，BBC出品妥妥的品质保障，来一场猫奴们的狂欢吧！</p>
<p><strong>这也是非常适合孩子观看的猫科动物小百科，让孩子对虎、豹、猫从此不迷糊，发自内心地热爱动物。</strong></p>
<p><strong>你可以和孩子聊聊这些：</strong></p>
<p>\1. 说说大猫们的区别。</p>
<p>\2. 给孩子讲讲流浪猫的生存状态。</p>
<p><strong>三张图感受一下这部片：</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/9a61e28f4d7bb385d5462f6a627d2d64.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/676c4ec9d713cc09555db4bac3541bd5.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/115b471afaf93fd3d47c98bc69b6cdc6.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><strong>6. 企鹅群里有特务</strong></p>
<p>憨态可掬的企鹅，哪个孩子抗拒得了？</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/4bfab1df7dbd9e98bbeaa15939c368ea.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p>推荐指数：★★★★★</p>
<p><strong>豆瓣评分：</strong>9.6</p>
<p><strong>影片简介</strong>：</p>
<p>为了贴近拍摄到企鹅真实的生活状态，BBC派出约五十人组成的摄影团队，在将近一年的时间里，对生活在热带秘鲁的洪堡企鹅、生活在亚南极海岛上的跳岩企鹅，以及生活在寒带南极的帝企鹅进行了跟踪拍摄。</p>
<p>这次剧组别出心裁，派出“特务”混入企鹅的队伍中，比如将摄像机藏入遥控企鹅体内，或干脆将摄像机伪装成一枚企鹅蛋。也因此得以在超近距离观察这群憨态可掬的小家伙超真实的生活画面。</p>
<p><strong>关 键 词 ： 特务 企鹅</strong></p>
<p><strong>推荐理由：</strong></p>
<p><strong>别样的视角，展现出企鹅族群不平凡的一面。憨态可掬的企鹅，哪个孩子抗拒得了呢？</strong>与其他纪录片不同，影片几乎没有让人揪心难过的镜头。</p>
<p>不论是暴风雪中孤独行走的帝企鹅，还是在海狮群中勇敢穿梭的洪堡企鹅，或是那些差点被天敌掳走的小生命，每每总能化险为夷，在影片中都有令人欣慰宽心的结局。<strong>片子本身就是走轻松愉快路线，把不愉快的一句带过，加之以轻松的音乐和配音，非常适合孩子观看。</strong></p>
<p><strong>你可以和孩子聊聊这些：</strong></p>
<p>\1. 让孩子说说最喜欢哪种企鹅？</p>
<p>\2. 和孩子介绍一下，在南极生活会是什么样？</p>
<p><strong>三张图感受一下这部片：</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/939270d4d873c80a188b063b2516abe6.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3004d0e4c932c00c737ca03621ee05f0.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e035dc973c01b722a92dd652c935e927.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><strong>7. 红色翅膀：火烈鸟故事</strong></p>
<p>用诗意的镜头看火烈鸟</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3d41b69960f245867ba96e401eba4b42.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p>推荐指数：★★★★★</p>
<p><strong>豆瓣评分：</strong>9.1</p>
<p><strong>影片简介</strong>：</p>
<p>一部关于火烈鸟生活周期的自然纪录片，影片拍摄于坦桑尼亚北部的纳特龙湖。 在一个遥远且被人遗忘的野外，还存在着大自然最后的伟大杰作：数百万有着红色翅膀的的火烈鸟正展开双翼，经历着出生、成长和死亡的生命旅程。</p>
<p>影片中有着太多此前从未被摄影机记录过的雄伟影像，并且记录了这群濒临灭绝的稀有生物是如何为了生存而抗争的。</p>
<p><strong>关 键 词 ：火烈鸟 濒危</strong></p>
<p><strong>推荐理由：</strong></p>
<p>这种玫瑰色的火烈鸟是最富有色彩、最神奇也是异常漂亮的鸟群。这几年，火烈鸟的图案大受欢迎。如果你家孩子也喜欢火烈鸟，不妨带他了解一下火烈鸟真正的生活。</p>
<p><strong>这是一部很诗意的纪录片，每一个画面都异常美好，而那些高贵、热情的火烈鸟看得人心潮澎湃。</strong>影片真实记录了这群濒临灭绝的稀有生物是如何为了生存而抗争。相信孩子们会在这里发现，自然永远是灵感和美好的最佳探索地。</p>
<p><strong>你可以和孩子聊聊这些：</strong></p>
<p>\1. 问问孩子最喜欢什么动物？</p>
<p>\2. 让孩子想想怎么能保护火烈鸟？</p>
<p><strong>三张图感受一下这部片：</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a44663bea71165156ecba6e8f8d858fc.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/b843d8b5e2a2cc3bffcd3e9ac9f9e718.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/21e20dcf31c23807d15b290a5c6525cb.png" alt="7部史诗级自然纪录片，孩子14岁前要看完！"></p>
<p>古语说：秀才不出门，全知天下事。在纪录片面前，我们确实做到了。</p>
<p><strong>大自然的未知实在是太迷人了，在我们一生目之所及、行之所至十分有限的情况下，有人替我们去看一看，查清楚，也不枉此生了。</strong></p>
<p>就让这7部优质的自然动物纪录片，带着孩子们欣赏地球上各个美丽的角落、享受打动人心的时刻吧。</p>
<p>让孩子爱动物，保护动物，时刻保持对生命的敬畏吧。</p>
]]></content>
      <categories>
        <category>兴趣爱好，大自然</category>
      </categories>
      <tags>
        <tag>兴趣爱好，大自然</tag>
      </tags>
  </entry>
  <entry>
    <title>云风的Blog-Lua GC的源码解剖1</title>
    <url>/lua/%E4%BA%91%E9%A3%8E%E7%9A%84Blog-Lua%20GC%20%E7%9A%84%E6%BA%90%E7%A0%81%E5%89%96%E6%9E%901/</url>
    <content><![CDATA[<p><a href="https://blog.codingnow.com/2011/03/lua_gc_1.html" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h3 id="Lua-GC-的源码剖析-1"><a href="#Lua-GC-的源码剖析-1" class="headerlink" title="Lua GC 的源码剖析 (1)"></a>Lua GC 的源码剖析 (1)</h3><p>最近发现在大数据量的 lua 环境中，GC 占据了很多的 CPU 。差不多是整个 CPU 时间的 20% 左右。希望着手改进。这样，必须先对 lua 的 gc 算法极其实现有一个详尽的理解。我之前读过 lua 的源代码，由于 lua 源码版本变迁，这个工作还需要再做一次。这次我重新阅读了 lua 5.1.4 的源代码。从今天起，做一个笔记，详细分析一下 lua 的 gc 是如何实现的。阅读代码整整花掉了我一天时间。但写出来恐怕比阅读时间更长。我会分几天写在 blog 上。</p>
<hr>
<p>Lua 采用一个简单的标记清除算法的 GC 系统。</p>
<p>在 Lua 中，一共只有 9 种数据类型，分别为 nil 、boolean 、lightuserdata 、number 、string 、 table 、 function 、 userdata 和 thread 。其中，只有 string table function thread 四种在 vm 中以引用方式共享，是需要被 GC 管理回收的对象。其它类型都以值形式存在。</p>
<p>但在 Lua 的实现中，还有两种类型的对象需要被 GC 管理。分别是 proto （可以看作未绑定 upvalue 的函数）， upvalue （多个 upvalue 会引用同一个值）。</p>
<p>Lua 是以 union + type 的形式保存值。具体定义可见 lobject.h 的 56 - 75 行：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** Union of all Lua values</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> &#123;</span><br><span class="line">  GCObject *gc;</span><br><span class="line">  <span class="keyword">void</span> *p;</span><br><span class="line">  lua_Number n;</span><br><span class="line">  <span class="keyword">int</span> b;</span><br><span class="line">&#125; Value;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** Tagged Values</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TValuefields    Value value; int tt</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">lua_TValue</span> &#123;</span></span><br><span class="line">  TValuefields;</span><br><span class="line">&#125; TValue;</span><br></pre></td></tr></table></figure>
<p>我们可以看到，Value 以 union 方式定义。如果是需要被 GC 管理的对象，就以 GCObject 指针形式保存，否则直接存值。在代码的其它部分，并不直接使用 Value 类型，而是 TValue 类型。它比 Value 多了一个类型标识。用 int tt 记录。通常的系统中，每个 TValue 长为 12 字节。btw, 在 <a href="http://www.lua.org/doc/jucs05.pdf" rel="external nofollow noopener noreferrer" target="_blank">The implementation of Lua 5.0</a> 中作者讨论了，在 32 位系统下，为何不用某种 trick 把 type 压缩到前 8 字节内。</p>
<p>所有的 GCObject 都有一个相同的数据头，叫作 CommonHeader ，在 lobject.h 里 43 行 以宏形式定义出来的。使用宏是源于使用上的某种便利。C 语言不支持结构的继承。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CommonHeader    GCObject *next; lu_byte tt; lu_byte marked</span></span><br></pre></td></tr></table></figure>
<p>从这里我们可以看到：所有的 GCObject 都用一个单向链表串了起来。每个对象都以 tt 来识别其类型。marked 域用于标记清除的工作。</p>
<p>标记清除算法是一种简单的 GC 算法。每次 GC 过程，先以若干根节点开始，逐个把直接以及间接和它们相关的节点都做上标记。对于 Lua ，这个过程很容易实现。因为所有 GObject 都在同一个链表上，当标记完成后，遍历这个链表，把未被标记的节点一一删除即可。</p>
<p>Lua 在实际实现时，其实不只用一条链表维系所有 GCObject 。这是因为 string 类型有其特殊性。所有的 string 放在一张大的 hash 表中。它需要保证系统中不会有值相同的 string 被创建两份。顾 string 是被单独管理的，而不串在 GCObject 的链表中。</p>
<p>回头来看看 <code>lua_State</code> 这个类型。这是写 C 和 Lua 交互时用的最多的数据类型。顾名思义，它表示了 lua vm 的某种状态。从实现上来说，更接近 lua 的一个 thread 以及其间包含的相关数据（堆栈、环境等等）。事实上，一个 <code>lua_State</code> 也是一个类型为 thread 的 GCObject 。见其定义于 lstate.h 97 行。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** `per thread' state</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span> &#123;</span></span><br><span class="line">  CommonHeader;</span><br><span class="line">  lu_byte status;</span><br><span class="line">  StkId top;  <span class="comment">/* first free slot in the stack */</span></span><br><span class="line">  StkId base;  <span class="comment">/* base of current function */</span></span><br><span class="line">  global_State *l_G;</span><br><span class="line">  CallInfo *ci;  <span class="comment">/* call info for current function */</span></span><br><span class="line">  <span class="keyword">const</span> Instruction *savedpc;  <span class="comment">/* `savedpc' of current function */</span></span><br><span class="line">  StkId stack_last;  <span class="comment">/* last free slot in the stack */</span></span><br><span class="line">  StkId <span class="built_in">stack</span>;  <span class="comment">/* stack base */</span></span><br><span class="line">  CallInfo *end_ci;  <span class="comment">/* points after end of ci array*/</span></span><br><span class="line">  CallInfo *base_ci;  <span class="comment">/* array of CallInfo's */</span></span><br><span class="line">  <span class="keyword">int</span> stacksize;</span><br><span class="line">  <span class="keyword">int</span> size_ci;  <span class="comment">/* size of array `base_ci' */</span></span><br><span class="line">  <span class="keyword">unsigned</span> short nCcalls;  <span class="comment">/* number of nested C calls */</span></span><br><span class="line">  <span class="keyword">unsigned</span> short baseCcalls;  <span class="comment">/* nested C calls when resuming coroutine */</span></span><br><span class="line">  lu_byte hookmask;</span><br><span class="line">  lu_byte allowhook;</span><br><span class="line">  <span class="keyword">int</span> basehookcount;</span><br><span class="line">  <span class="keyword">int</span> hookcount;</span><br><span class="line">  lua_Hook hook;</span><br><span class="line">  TValue l_gt;  <span class="comment">/* table of globals */</span></span><br><span class="line">  TValue env;  <span class="comment">/* temporary place for environments */</span></span><br><span class="line">  GCObject *openupval;  <span class="comment">/* list of open upvalues in this stack */</span></span><br><span class="line">  GCObject *gclist;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">lua_longjmp</span> *<span class="title">errorJmp</span>;</span>  <span class="comment">/* current error recover point */</span></span><br><span class="line">  <span class="keyword">ptrdiff_t</span> errfunc;  <span class="comment">/* current error handling function (stack index) */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>一个完整的 lua 虚拟机在运行时，可有多个 <code>lua_State</code> ，即多个 thread 。它们会共享一些数据。这些数据放在 <code>global_State *l_G</code> 域中。其中自然也包括所有 GCobject 的链表。</p>
<p>所有的 string 则以 stringtable 结构保存在 stringtable strt 域。string 的值类型为 TString ，它和其它 GCObject 一样，拥有 CommonHeader 。但需要注意，CommonHeader 中的 next 域却和其它类型的单向链表意义不同。它被挂接在 stringtable 这个 hash 表中。</p>
<p>除 string 外的 GCObject 链表头在 rootgc （ lstate.h 75 行）域中。初始化时，这个域被初始化为主线程。见 lstate.c 170 行，<code>lua_newstate</code> 函数中：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">g-&gt;rootgc = obj2gco(<span class="name">L</span>)<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>每当一个新的 GCobject 被创建出来，都会被挂接到这个链表上，挂接函数有两个，在 lgc.c 687 行的</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">void luaC_link (lua_State *L, GCObject *o, lu_byte tt) &#123;</span><br><span class="line">  global_State *g = G(L);</span><br><span class="line">  <span class="function"><span class="title">o</span>-&gt;</span><span class="function"><span class="title">gch</span>.next = g-&gt;</span>rootgc;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>rootgc = o;</span><br><span class="line">  <span class="function"><span class="title">o</span>-&gt;</span>gch.marked = luaC_white(g);</span><br><span class="line">  <span class="function"><span class="title">o</span>-&gt;</span>gch.tt = tt;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void luaC_linkupval (lua_State *L, UpVal *uv) &#123;</span><br><span class="line">  global_State *g = G(L);</span><br><span class="line">  GCObject *o = obj2gco(uv);</span><br><span class="line">  <span class="function"><span class="title">o</span>-&gt;</span><span class="function"><span class="title">gch</span>.next = g-&gt;</span>rootgc;  <span class="comment">/* link upvalue into `rootgc' list */</span></span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>rootgc = o;</span><br><span class="line">  <span class="keyword">if</span> (isgray(o)) &#123; </span><br><span class="line">    <span class="function"><span class="title">if</span> (g-&gt;</span>gcstate == GCSpropagate) &#123;</span><br><span class="line">      gray2black(o);  <span class="comment">/* closed upvalues need barrier */</span></span><br><span class="line">      <span class="function"><span class="title">luaC_barrier</span>(L, uv, uv-&gt;</span>v);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;  <span class="comment">/* sweep phase: sweep it (turning it into white) */</span></span><br><span class="line">      makewhite(g, o);</span><br><span class="line">      <span class="function"><span class="title">lua_assert</span>(g-&gt;</span><span class="function"><span class="title">gcstate</span> != GCSfinalize &amp;&amp; g-&gt;</span>gcstate != GCSpause);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>upvalue 在 C 中类型为 UpVal ，也是一个 GCObject 。但这里被特殊处理。为什么会这样？因为 Lua 的 GC 可以分步扫描。别的类型被新创建时，都可以直接作为一个白色节点（新节点）挂接在整个系统中。但 upvalue 却是对已有的对象的间接引用，不是新数据。一旦 GC 在 mark 的过程中（ gc 状态为 GCSpropagate ），则需增加屏障 <code>luaC_barrier</code> 。对于这个问题，会在以后详细展开。</p>
<p>lua 还有另一种数据类型创建时的挂接过程也被特殊处理。那就是 userdata 。见 lstring.c 的 95 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">Udata *luaS_newudata (lua_State *L, size_t s, Table *e) &#123;</span><br><span class="line">  Udata *u;</span><br><span class="line">  <span class="keyword">if</span> (s &gt; MAX_SIZET - sizeof(Udata))</span><br><span class="line">    luaM_toobig(L);</span><br><span class="line">  u = cast(Udata *, luaM_malloc(L, s + sizeof(Udata)));</span><br><span class="line">  <span class="function"><span class="title">u</span>-&gt;</span>uv.marked = luaC_white(G(L));  <span class="comment">/* is not finalized */</span></span><br><span class="line">  <span class="function"><span class="title">u</span>-&gt;</span>uv.tt = LUA_TUSERDATA;</span><br><span class="line">  <span class="function"><span class="title">u</span>-&gt;</span>uv.len = s;</span><br><span class="line">  <span class="function"><span class="title">u</span>-&gt;</span>uv.metatable = NULL;</span><br><span class="line">  <span class="function"><span class="title">u</span>-&gt;</span>uv.env = e;</span><br><span class="line">  <span class="comment">/* chain it on udata list (after main thread) */</span></span><br><span class="line">  <span class="function"><span class="title">u</span>-&gt;</span><span class="function"><span class="title">uv</span>.next = G(L)-&gt;</span><span class="function"><span class="title">mainthread</span>-&gt;</span>next;</span><br><span class="line">  G(L)-&gt;<span class="function"><span class="title">mainthread</span>-&gt;</span>next = obj2gco(u);</span><br><span class="line">  return u;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里并没有调用 <code>luaC_link</code> 来挂接新的 Udata 对象，而是直接使用的</p>
<figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* chain it on udata list (after main thread) */</span></span><br><span class="line">u-&gt;uv.next = G<span class="function"><span class="params">(L)</span>-&gt;</span>mainthread-&gt;next;</span><br><span class="line">G<span class="function"><span class="params">(L)</span>-&gt;</span>mainthread-&gt;next = obj2gco(u);</span><br></pre></td></tr></table></figure>
<p>把 u 挂接在 mainthread 之后。</p>
<p>从前面的 mainstate 创建过程可知。mainthread 一定是 GCObject 链表上的最后一个节点（除 Udata 外）。这是因为挂接过程都是向链表头添加的。</p>
<p>这里，就可以把所有 userdata 全部挂接在其它类型之后。这么做的理由是，所有 userdata 都可能有 gc 方法（其它类型则没有）。需要统一去调用这些 gc 方面，则应该有一个途径来单独遍历所有的 userdata 。除此之外，userdata 和其它 GCObject 的处理方式则没有区别，顾依旧挂接在整个 GCObject 链表上而不需要单独再分出一个链表。</p>
<p>处理 userdata 的流程见 lgc.c 的 127 行</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* move `dead' udata that need finalization to list `tmudata' */</span></span><br><span class="line"><span class="function"><span class="keyword">size_t</span> <span class="title">luaC_separateudata</span> <span class="params">(lua_State *L, <span class="keyword">int</span> all)</span> </span>&#123;</span><br></pre></td></tr></table></figure>
<p>这个函数会把所有带有 gc 方法的 userdata 挑出来，放到一个循环链表中。这个循环链表在 <code>global_State</code> 的 tmudata 域。需要调用 gc 方法的这些 userdata 在当个 gc 循环是不能被直接清除的。所以在 mark 环节的最后，会被重新 mark 为不可清除节点。见 lgc.c 的 545 行：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line"><span class="keyword">marktmu(g); </span> <span class="comment">/* mark `preserved' userdata */</span></span><br></pre></td></tr></table></figure>
<p>这样，可以保证在调用 gc 方法环节，这些对象的内存都没有被释放。但因为这些对象被设置了 finalized 标记。（通过 markfinalized ），下一次 gc 过程不会进入 tmudata 链表，将会被正确清理。</p>
<p>具体 userdata 的清理流程，会在后面展开解释。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>云风的Lua-GC解剖</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>云风的Lua-GC解剖</tag>
      </tags>
  </entry>
  <entry>
    <title>云风的Blog-Lua GC的源码解剖2</title>
    <url>/lua/%E4%BA%91%E9%A3%8E%E7%9A%84Blog-Lua%20GC%20%E7%9A%84%E6%BA%90%E7%A0%81%E5%89%96%E6%9E%902/</url>
    <content><![CDATA[<p><a href="https://blog.codingnow.com/2011/03/lua_gc_2.html" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h3 id="Lua-GC-的源码剖析-2"><a href="#Lua-GC-的源码剖析-2" class="headerlink" title="Lua GC 的源码剖析 (2)"></a>Lua GC 的源码剖析 (2)</h3><p>早期的 Lua GC 采用的是 stop the world 的实现。一旦发生 gc 就需要等待整个 gc 流程走完。如果你用 lua 处理较少量数据，或是数据增删不频繁，这样做不是问题。但当处理的数据量变大时，对于实时性要求较高的应用，比如网络游戏服务器，这个代价则是不可忽略的。lua 本身是个很精简的系统，但不代表处理的数据量也一定很小。</p>
<p>从 Lua 5.1 开始，GC 的实现改为分步的。虽然依旧是 stop the world ，但是，每个步骤都可以分阶段执行。这样，每次停顿的时间较小。随之，这部分的代码也相对复杂了。分步执行最关键的问题是需要解决在 GC 的步骤之间，如果数据关联的状态发生了变化，如果保证 GC 的正确性。GC 的分步执行相对于一次执行完，总的时间开销的差别并不是零代价的。只是在实现上，要尽量让额外增加的代价较小。</p>
<p>先来看 GC 流程的划分。</p>
<p>lua 的 GC 分为五个大的阶段。GC 处于哪个阶段（代码中被称为状态），依据的是 <code>global_State</code> 中的 gcstate 域。状态以宏形式定义在 lgc.h 的 14 行。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** Possible states of the Garbage Collector</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GCSpause    0</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GCSpropagate    1</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GCSsweepstring  2</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GCSsweep    3</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GCSfinalize 4</span></span><br></pre></td></tr></table></figure>
<p>状态的值的大小也暗示着它们的执行次序。需要注意的是，GC 的执行过程并非每步骤都拥塞在一个状态上。</p>
<p>GCSpause 阶段是每个 GC 流程的启始步骤。只是标记系统的根节点。见 lgc.c 的 561 行。</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="keyword">switch</span> (g-&gt;gcstate) &#123;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">GCSpause:</span> &#123;</span><br><span class="line">    markroot(L);  <span class="comment">/* start a new collection */</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>markroot 这个函数所做之事，就是标记主线程对象，标记主线程的全局表、注册表，以及为全局类型注册的元表。标记的具体过程我们后面再讲。</p>
<p>GCSpause 阶段执行完，立刻就将状态切换到了 GCSpropagate 。这是一个标记流程。这个流程会分步完成。当检测到尚有对象待标记时，迭代标记（反复调用 propagatemark）；最终，会有一个标记过程不可被打断，这些操作放在一个叫作 atomic 的函数中执行。见 lgc.c 的 565 行:</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> GCSpropagate: &#123;</span><br><span class="line">  <span class="keyword">if</span> (g-&gt;gray)</span><br><span class="line">    <span class="function"><span class="keyword">return</span> <span class="title">propagatemark</span><span class="params">(g)</span></span>;</span><br><span class="line">  <span class="keyword">else</span> &#123;  <span class="comment">/* no more `gray' objects */</span></span><br><span class="line">    atomic(L);  <span class="comment">/* finish mark phase */</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里可能需要顺带提一下的是 gray 域。顾名思义，它指 GCObject 中的灰色节点链。何为灰色，即处于白色和黑色之间的状态。关于节点的颜色，马上就会展开分析。</p>
<p>接下来就是清除流程了。</p>
<p>前面我们提到过，string 在 Lua 中是单独管理的，所以也需要单独清除。GCSsweepstring 阶段干的就是这个事情。string table 以 hash 表形式管理所有的 string 。GCSsweepstring 中，每个步骤(step) 清理 hash 表的一列。代码见 lgc.c 的 573 行</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">case GCSsweepstring: &#123;</span><br><span class="line">  <span class="function"><span class="title">lu_mem</span> old = g-&gt;</span>totalbytes;</span><br><span class="line">  <span class="function"><span class="title">sweepwholelist</span>(L, &amp;g-&gt;</span><span class="function"><span class="title">strt</span>.hash[g-&gt;</span>sweepstrgc++]);</span><br><span class="line">  <span class="function"><span class="title">if</span> (g-&gt;</span><span class="function"><span class="title">sweepstrgc</span> &gt;= g-&gt;</span>strt.size)  <span class="comment">/* nothing more to sweep? */</span></span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>gcstate = GCSsweep;  <span class="comment">/* end sweep-string phase */</span></span><br><span class="line">  <span class="function"><span class="title">lua_assert</span>(old &gt;= g-&gt;</span>totalbytes);</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">estimate</span> -= old - g-&gt;</span>totalbytes;</span><br><span class="line">  return GCSWEEPCOST;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里可以看到 estimate 和 totalbytes 两个域，从名字上可以知道，它们分别表示了 lua vm 占用的内存字节数以及实际分配的字节数。</p>
<p>ps. 如果你自己实现过内存管理器，当知道内存管理本身是有额外的内存开销的。如果有必要精确控制内存数量，我个人倾向于结合内存管理器统计准确的内存使用情况。比如你向内存管理器索要 8 字节内存，实际的内存开销很可能是 12 字节，甚至更多。如果想做这方面的修改，让 lua 的 gc 能更真实的反应内存实际使用情况，推荐修改 lmem.c 的 76 行，<code>luaM_realloc_</code> 函数。所有的 lua 中的内存使用变化都会通过这个函数。</p>
<p>从上面这段代码中，我们还见到了 GCSWEEPCOST 这个神秘数字。这个数字用于控制 GC 的进度。这超出了今天的话题。留待以后分析。</p>
<p>接下来就是对所有未标记的其它 GCObject 做清理工作了。即 GCSsweep 阶段。它和上面的 GCSsweepstring 类似。</p>
<p>最后是 GCSfinalize 阶段。如果在前面的阶段发现了需要调用 gc 元方法的 userdata 对象，将在这个阶段逐个调用。做这件事情的函数是 GCTM 。</p>
<p>前面已经谈到过，所有拥有 gc 元方法的 userdata 对象以及其关联的数据，实际上都不会在之前的清除阶段被清除。（由于单独做过标记）所有的元方法调用都是安全的。而它们的实际清除，则需等到下一次 GC 流程了。或是在 <code>lua_close</code> 被调用时清除。</p>
<p>ps. <code>lua_close</code> 并不做完整的 gc 工作，只是简单的处理所有 userdata 的 gc 元方法，以及释放所有用到的内存。它是相对廉价的。</p>
<hr>
<p>接下来我们看看 GC 标记流程涉及的一些概念。</p>
<p>简单的说，lua 认为每个 GCObject （需要被 GC 收集器管理的对象）都有一个颜色。一开始，所有节点都是白色的。新创建出来的节点也被默认设置为白色。</p>
<p>在标记阶段，可见的节点，逐个被设置为黑色。有些节点比较复杂，它会关联别的节点。在没有处理完所有关联节点前，lua 认为它是灰色的。</p>
<p>节点的颜色被储存在 GCObject 的 CommonHeader 里，放在 marked 域中。为了节约内存，是以位形式存放。marked 是一个单字节量。总共可以储存 8 个标记。而 lua 5.1.4 用到了 7 个标记位。在 lgc.h 的 41 行，有其解释：</p>
<figure class="highlight nimrod"><table><tr><td class="code"><pre><span class="line">/*</span><br><span class="line">** <span class="type">Layout</span> <span class="keyword">for</span> bit use <span class="keyword">in</span> `marked' field:</span><br><span class="line">** bit <span class="number">0</span> - <span class="keyword">object</span> <span class="keyword">is</span> white (<span class="keyword">type</span> <span class="number">0</span>)</span><br><span class="line">** bit <span class="number">1</span> - <span class="keyword">object</span> <span class="keyword">is</span> white (<span class="keyword">type</span> <span class="number">1</span>)</span><br><span class="line">** bit <span class="number">2</span> - <span class="keyword">object</span> <span class="keyword">is</span> black</span><br><span class="line">** bit <span class="number">3</span> - <span class="keyword">for</span> userdata: has been finalized</span><br><span class="line">** bit <span class="number">3</span> - <span class="keyword">for</span> tables: has weak keys</span><br><span class="line">** bit <span class="number">4</span> - <span class="keyword">for</span> tables: has weak values</span><br><span class="line">** bit <span class="number">5</span> - <span class="keyword">object</span> <span class="keyword">is</span> fixed (should <span class="keyword">not</span> be collected)</span><br><span class="line">** bit <span class="number">6</span> - <span class="keyword">object</span> <span class="keyword">is</span> <span class="string">"super"</span> fixed (only the main thread)</span><br><span class="line">*/</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">#define WHITE0BIT   0</span></span><br><span class="line"><span class="comment">#define WHITE1BIT   1</span></span><br><span class="line"><span class="comment">#define BLACKBIT    2</span></span><br><span class="line"><span class="comment">#define FINALIZEDBIT    3</span></span><br><span class="line"><span class="comment">#define KEYWEAKBIT  3</span></span><br><span class="line"><span class="comment">#define VALUEWEAKBIT    4</span></span><br><span class="line"><span class="comment">#define FIXEDBIT    5</span></span><br><span class="line"><span class="comment">#define SFIXEDBIT   6</span></span><br><span class="line"><span class="comment">#define WHITEBITS   bit2mask(WHITE0BIT, WHITE1BIT)</span></span><br></pre></td></tr></table></figure>
<p>lua 定义了一组宏来操作这些标记位，代码就不再列出。只需要打开 lgc.h 就能很轻松的理解这些宏的函数。</p>
<p>白色和黑色是分别标记的。当一个对象非白非黑时，就认为它是灰色的。</p>
<p>为什么有两个白色标记位？这是 lua 采用的一个小技巧。在 GC 的标记流程结束，但清理流程尚未作完前。一旦对象间的关系发生变化，比如新增加了一个对象。这些对象的生命期是不可预料的。最安全的方法是把它们标记为不可清除。但我们又不能直接把对象设置为黑色。因为清理过程结束，需要把所有对象设置回白色，方便下次清理。lua 实际上是单遍扫描，处理完一个节点就重置一个节点的颜色的。简单的把新创建出来的对象设置为黑，有可能导致它在 GC 流程结束后，再也没机会变回白色了。</p>
<p>简单的方法就是设置从第三种状态。也就是第 2 种白色。</p>
<p>在 Lua 中，两个白色状态是一个乒乓开关，当前需要删除 0 型白色节点时， 1 型白色节点就是被保护起来的；反之也一样。</p>
<p>当前的白色是 0 型还是 1 型，见 <code>global_State</code> 的 currentwhite 域。otherwhite() 用于乒乓切换。获得当前白色状态，使用定义在 lgcc.h 77 行的宏：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">#define luaC_white(<span class="name">g</span>)   cast(<span class="name">lu_byte</span>, (<span class="name">g</span>)-&gt;currentwhite &amp; WHITEBITS)</span><br></pre></td></tr></table></figure>
<p>FINALIZEDBIT 用于标记 userdata 。当 userdata 确认不被引用，则设置上这个标记。它不同于颜色标记。因为 userdata 由于 gc 元方法的存在，释放所占内存是需要延迟到 gc 元方法调用之后的。这个标记可以保证元方法不会被反复调用。</p>
<p>KEYWEAKBIT 和 VALUEWEAKBIT 用于标记 table 的 weak 属性，无需多言。</p>
<p>FIXEDBIT 可以保证一个 GCObject 不会在 GC 流程中被清除。为什么要有这种状态？关键在于 lua 本身会用到一个字符串，它们有可能不被任何地方引用，但在每次接触到这个字符串时，又不希望反复生成。那么，这些字符串就会被保护起来，设置上 FIXEDBIT 。</p>
<p>在 lstring.h 的 24 行定义有：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">#define luaS_fix(<span class="name">s</span>) l_setbit((<span class="name">s</span>)-&gt;tsv.marked, FIXEDBIT)</span><br></pre></td></tr></table></figure>
<p>可以把一个字符串设置为被保护的。</p>
<p>典型的应用场合见 llex.c 的 64 行：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaX_init</span> <span class="params">(lua_State *L)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> i;</span><br><span class="line">  <span class="keyword">for</span> (i=<span class="number">0</span>; itsv.reserved = cast_byte(i+<span class="number">1</span>);  <span class="comment">/* reserved word */</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以及 ltm.c 的 30 行：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaT_init</span> <span class="params">(lua_State *L)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">char</span> *<span class="keyword">const</span> luaT_eventname[] = &#123;  <span class="comment">/* ORDER TM */</span></span><br><span class="line">    <span class="string">"__index"</span>, <span class="string">"__newindex"</span>,</span><br><span class="line">    <span class="string">"__gc"</span>, <span class="string">"__mode"</span>, <span class="string">"__eq"</span>,</span><br><span class="line">    <span class="string">"__add"</span>, <span class="string">"__sub"</span>, <span class="string">"__mul"</span>, <span class="string">"__div"</span>, <span class="string">"__mod"</span>,</span><br><span class="line">    <span class="string">"__pow"</span>, <span class="string">"__unm"</span>, <span class="string">"__len"</span>, <span class="string">"__lt"</span>, <span class="string">"__le"</span>,</span><br><span class="line">    <span class="string">"__concat"</span>, <span class="string">"__call"</span></span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">int</span> i;</span><br><span class="line">  <span class="keyword">for</span> (i=<span class="number">0</span>; itmname[i] = luaS_new(L, luaT_eventname[i]);</span><br><span class="line">    luaS_fix(G(L)-&gt;tmname[i]);  <span class="comment">/* never collect these names */</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以元方法为例，如果我们利用 lua 标准 api 来模拟 metatable 的行为，就不可能写的和原生的 meta 机制高效。因为，当我们取到一个 table 的 key ，想知道它是不是 <code>__index</code> 时，要么我们需要调用 strcmp 做比较；要么使用 <code>lua_pushlstring</code> 先将需要比较的 string 压入 <code>lua_State</code> ，然后再比较。</p>
<p>我们知道 lua 中值一致的 string 共享了一个 string 对象，即 TString 地址是一致的。比较两个 lua string 的代价非常小（只需要比较一个指针），比 C 函数 strcmp 高效。但 <code>lua_pushlstring</code> 却有额外开销。它需要去计算 hash 值，查询 hash 表 (string table) 。</p>
<p>lua 的 GC 算法并不做内存整理，它不会在内存中迁移数据。实际上，如果你能肯定一个 string 不会被清除，那么它的内存地址也是不变的，这样就带来的优化空间。ltm.c 中就是这样做的。</p>
<p>见 lstate.c 的 93 行：</p>
<figure class="highlight smali"><table><tr><td class="code"><pre><span class="line">TString *tmname[TM_N];  /*<span class="built_in"> array </span>with tag-method names */</span><br></pre></td></tr></table></figure>
<p><code>global_State</code> 中 tmname 域就直接以 TString 指针的方式记录了所有元方法的名字。换作标准的 lua api 来做的话，通常我们需要把这些 string 放到注册表，或环境表中，才能保证其不被 gc 清除，且可以在比较时拿到。lua 自己的实现则利用 FIXEDBIT 做了一步优化。</p>
<p>最后，我们来看看 SFIXEDBIT 。其实它的用途只有一个，就是标记主 mainthread 。也就是一切的起点。我们调用 <code>lua_newstate</code> 返回的那个结构。</p>
<p>为什么需要把这个结构特殊对待？因为即使到 <code>lua_close</code> 的那一刻，这个结构也是不能随意清除的。我们来看看世界末日时，程序都执行了什么？见 lstate.c 的 105 行。</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">static void close_state (<span class="name">lua_State</span> *L) &#123;</span><br><span class="line">  global_State *g = G(<span class="name">L</span>)<span class="comment">;</span></span><br><span class="line">  luaF_close(<span class="name">L</span>, L-&gt;stack)<span class="comment">;  /* close all upvalues for this thread */</span></span><br><span class="line">  luaC_freeall(<span class="name">L</span>)<span class="comment">;  /* collect all objects */</span></span><br><span class="line">  lua_assert(<span class="name">g-&gt;rootgc</span> == obj2gco(<span class="name">L</span>))<span class="comment">;</span></span><br><span class="line">  lua_assert(<span class="name">g-&gt;strt</span>.nuse == <span class="number">0</span>)<span class="comment">;</span></span><br><span class="line">  luaM_freearray(<span class="name">L</span>, G(<span class="name">L</span>)-&gt;strt.hash, G(<span class="name">L</span>)-&gt;strt.size, TString *);</span><br><span class="line">  luaZ_freebuffer(L, &amp;g-&gt;buff);</span><br><span class="line">  freestack(L, L);</span><br><span class="line">  lua_assert(g-&gt;totalbytes == sizeof(LG));</span><br><span class="line">  (*g-&gt;frealloc)(<span class="name">g-&gt;ud</span>, fromstate(<span class="name">L</span>), state_size(<span class="name">LG</span>), <span class="number">0</span>)<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这是 <code>lua_close</code> 的最后一个步骤。 <code>luaC_freeall</code> 将释放所有的 GCObject ，但不包括有 SFIXEDBIT 的 mainthread 对象 。见 lgc.c 484 行</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">void luaC_freeall (<span class="name">lua_State</span> *L) &#123;</span><br><span class="line">  global_State *g = G(<span class="name">L</span>)<span class="comment">;</span></span><br><span class="line">  int i<span class="comment">;</span></span><br><span class="line">  g-&gt;currentwhite = WHITEBITS | bitmask(<span class="name">SFIXEDBIT</span>)<span class="comment">;  /* mask to collect all elements */</span></span><br><span class="line">  sweepwholelist(<span class="name">L</span>, <span class="symbol">&amp;g-&gt;rootgc</span>)<span class="comment">;</span></span><br><span class="line">  for (<span class="name">i</span> = <span class="number">0</span><span class="comment">; i &lt; g-&gt;strt.size; i++)  /* free all string lists */</span></span><br><span class="line">    sweepwholelist(<span class="name">L</span>, <span class="symbol">&amp;g-&gt;strt</span>.hash[i])<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里 FIXEDBIT 是被无视的，而在此之前，FIXEDBIT 被保护着。见 lstate.c 的 153 行（<code>lua_newstate</code> 函数）：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">g-&gt;currentwhite = bit2mask(<span class="name">WHITE0BIT</span>, FIXEDBIT)<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>这么做很容易理解，lua 世界的起源，一切根数据都放在这个对象中，如果被提前清理，后面的代码就会出问题。真正释放这个对象不是在 GC 中，而是最后那句：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">lua_assert(<span class="name">g-&gt;totalbytes</span> == sizeof(<span class="name">LG</span>))<span class="comment">;</span></span><br><span class="line">(<span class="name">*g-&gt;frealloc</span>)(<span class="name">g-&gt;ud</span>, fromstate(<span class="name">L</span>), state_size(<span class="name">LG</span>), <span class="number">0</span>)<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>顺带还 assert 了一下，最终，世界上是不是只剩下这个结构。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>云风的Lua-GC解剖</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>云风的Lua-GC解剖</tag>
      </tags>
  </entry>
  <entry>
    <title>云风的Blog-Lua GC的源码解剖3</title>
    <url>/lua/%E4%BA%91%E9%A3%8E%E7%9A%84Blog-Lua%20GC%20%E7%9A%84%E6%BA%90%E7%A0%81%E5%89%96%E6%9E%903/</url>
    <content><![CDATA[<p><a href="https://blog.codingnow.com/2011/03/lua_gc_3.html" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h3 id="Lua-GC-的源码剖析-3"><a href="#Lua-GC-的源码剖析-3" class="headerlink" title="Lua GC 的源码剖析 (3)"></a>Lua GC 的源码剖析 (3)</h3><p>有了前几天的基础，我们可以从顶向下来读 lua gc 部分的代码了。</p>
<p>我们知道，lua 对外的 API 中，一切个 gc 打交道的都通过 <code>lua_gc</code> 。C 语言构建系统时，一般不讲设计模式。但模式还是存在的。若要按《设计模式》中的分类，这应该归于 Facade 模式。代码在 lapi.c 的 895 行：</p>
<figure class="highlight zephir"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** Garbage-collection function</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line">LUA_API <span class="keyword">int</span> lua_gc (lua_State *L, <span class="keyword">int</span> what, <span class="keyword">int</span> data) &#123;</span><br><span class="line">  <span class="keyword">int</span> res = <span class="number">0</span>;</span><br><span class="line">  global_State *g;</span><br><span class="line">  lua_lock(L);</span><br><span class="line">  g = G(L);</span><br><span class="line">  <span class="keyword">switch</span> (what) &#123;</span><br><span class="line">    <span class="keyword">case</span> LUA_GCSTOP: &#123;</span><br><span class="line">      g-&gt;GCthreshold = MAX_LUMEM;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_GCRESTART: &#123;</span><br><span class="line">      g-&gt;GCthreshold = g-&gt;totalbytes;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_GCCOLLECT: &#123;</span><br><span class="line">      luaC_fullgc(L);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_GCCOUNT: &#123;</span><br><span class="line">      <span class="comment">/* GC values are expressed in Kbytes: #bytes/2^10 */</span></span><br><span class="line">      res = cast_int(g-&gt;totalbytes &gt;&gt; <span class="number">10</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_GCCOUNTB: &#123;</span><br><span class="line">      res = cast_int(g-&gt;totalbytes &amp; <span class="number">0x3ff</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_GCSTEP: &#123;</span><br><span class="line">      lu_mem a = (cast(lu_mem, data) &lt;&lt; <span class="number">10</span>);</span><br><span class="line">      <span class="keyword">if</span> (a &lt;= g-&gt;totalbytes)</span><br><span class="line">        g-&gt;GCthreshold = g-&gt;totalbytes - a;</span><br><span class="line">      <span class="keyword">else</span></span><br><span class="line">        g-&gt;GCthreshold = <span class="number">0</span>;</span><br><span class="line">      <span class="keyword">while</span> (g-&gt;GCthreshold &lt;= g-&gt;totalbytes) &#123;</span><br><span class="line">        luaC_step(L);</span><br><span class="line">        <span class="keyword">if</span> (g-&gt;gcstate == GCSpause) &#123;  <span class="comment">/* end of cycle? */</span></span><br><span class="line">          res = <span class="number">1</span>;  <span class="comment">/* signal it */</span></span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_GCSETPAUSE: &#123;</span><br><span class="line">      res = g-&gt;gcpause;</span><br><span class="line">      g-&gt;gcpause = data;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_GCSETSTEPMUL: &#123;</span><br><span class="line">      res = g-&gt;gcstepmul;</span><br><span class="line">      g-&gt;gcstepmul = data;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">default</span>: res = <span class="number">-1</span>;  <span class="comment">/* invalid option */</span></span><br><span class="line">  &#125;</span><br><span class="line">  lua_unlock(L);</span><br><span class="line">  <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从代码可见，对内部状态的访问，都是直接访问 global state 表的。GC 控制则是调用内部 api 。lua 中对外的 api 和内部模块交互的 api 都是分开的。这样层次分明。内部子模块一般名为 <code>luaX_xxx</code> X 为子模块代号。对于收集器相关的 api 一律以 <code>luaC_xxx</code> 命名。这些 api 定义在 lgc.h 中。</p>
<p>此间提到的 api 有两个：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">LUAI_FUNC <span class="keyword">void</span> <span class="title">luaC_step</span> <span class="params">(lua_State *L)</span></span>;</span><br><span class="line"><span class="function">LUAI_FUNC <span class="keyword">void</span> <span class="title">luaC_fullgc</span> <span class="params">(lua_State *L)</span></span>;</span><br></pre></td></tr></table></figure>
<p>用于分步 GC 已经完整 GC 。</p>
<p>另一个重要的 api 是：</p>
<figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">#define luaC_checkGC(L) &#123; \</span></span><br><span class="line">  condhardstacktests(luaD_reallocstack(L, L-&gt;stacksize - EXTRA_STACK - <span class="number">1</span>)); <span class="string">\</span></span><br><span class="line">  <span class="keyword">if</span> <span class="function"><span class="params">(G(L)-&gt;totalbytes &gt;= G(L)-&gt;GCthreshold)</span> \</span></span><br><span class="line"><span class="function">    <span class="title">luaC_step</span><span class="params">(L)</span>; &#125;</span></span><br></pre></td></tr></table></figure>
<p>它以宏形式定义出来，用于自动的 GC 。如果我们审查 lapi.c ldo.c lvm.c ，会发现大部分会导致内存增长的 api 中，都调用了它。保证 gc 可以随内存使用增加而自动进行。</p>
<p>这里插几句。</p>
<p>使用自动 gc 会有一个问题。它很可能使系统的峰值内存占用远超过实际需求量。原因就在于，收集行为往往发生在调用栈很深的地方。当你的应用程序呈现出某种周期性（大多数包驱动的服务都是这样）。在一个服务周期内，往往会引用众多临时对象，这个时候做 mark 工作，会导致许多临时对象也被 mark 住。</p>
<p>一个经验方法是，调用 <code>LUA_GCSTOP</code> 停止自动 GC。在周期间定期调用 gcstep 且使用较大的 data 值，在有限个周期做完一整趟 gc 。</p>
<p>另，condhardstacktests 是一个宏，通常是不开启的。</p>
<hr>
<p>先来看 <code>luaC_fullgc</code> 。它用来执行完整的一次 gc 动作。fullgc 并不是仅仅把当前的流程走完。因为之前的 gc 行为可能执行了一半，可能有一些半路加进来的需要回收的对象。所以在走完一趟流程后，fullgc 将阻塞着再完整跑一遍 gc 。整个流程有一些优化的余地。即，前半程的 gc 流程其实不必严格执行，它并不需要真的去清除什么。只需要把状态恢复。这个工作是如何做到的呢？见 lgc.c 的 637 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">void luaC_fullgc (lua_State *L) &#123;</span><br><span class="line">  global_State *g = G(L);</span><br><span class="line">  <span class="function"><span class="title">if</span> (g-&gt;</span>gcstate &lt;= GCSpropagate) &#123;</span><br><span class="line">    <span class="comment">/* reset sweep marks to sweep all elements (returning them to white) */</span></span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>sweepstrgc = <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">sweepgc</span> = &amp;g-&gt;</span>rootgc;</span><br><span class="line">    <span class="comment">/* reset other collector lists */</span></span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>gray = NULL;</span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>grayagain = NULL;</span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>weak = NULL;</span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>gcstate = GCSsweepstring;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="title">lua_assert</span>(g-&gt;</span><span class="function"><span class="title">gcstate</span> != GCSpause &amp;&amp; g-&gt;</span>gcstate != GCSpropagate);</span><br><span class="line">  <span class="comment">/* finish any pending sweep phase */</span></span><br><span class="line">  <span class="function"><span class="title">while</span> (g-&gt;</span>gcstate != GCSfinalize) &#123;</span><br><span class="line">    <span class="function"><span class="title">lua_assert</span>(g-&gt;</span><span class="function"><span class="title">gcstate</span> == GCSsweepstring || g-&gt;</span>gcstate == GCSsweep);</span><br><span class="line">    singlestep(L);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>比较耗时的 mark 步骤被简单跳过了（如果它还没进行完的话）。和正常的 mark 流程不同，正常的 mark 流程最后，会将白色标记反转。见 lgc.c 548 行，atomic 函数：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">/* flip current white */</span><br><span class="line">g-&gt;currentwhite = cast_byte(<span class="name">otherwhite</span>(<span class="name">g</span>))<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>在 fullgc 的前半程中，直接跳过了 GCSpropagate ，重置了内部状态，但没有翻转白色标记。这会导致后面的 sweep 流程不会真的释放那些白色对象。sweep 工作实际做的只是把所有对象又重新设置回白色而已。</p>
<p>接下来就是一个完整不被打断的 gc 过程了。</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">markroot</span>(L);</span><br><span class="line"><span class="selector-tag">while</span> (g-&gt;gcstate != GCSpause) &#123;</span><br><span class="line">  <span class="selector-tag">singlestep</span>(L);</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">setthreshold</span>(g);</span><br></pre></td></tr></table></figure>
<p>从根开始 mark ，直到整个 gc 流程执行完毕。最后，重新设置了 GCthreshold 。注：调用 fullgc 会重置 GCthreshold ，所以如果你曾经调用 <code>LUA_GCSTOP</code> 暂停自动 GC 的话（也是通过修改 GCthreshold 实现） ，记得再调用一次。</p>
<p>stepgc 要相对复杂一些。在 lua 手册的 2.10 解释了　garbage-collector pause　和 step multiplier 的意义，却没有给出精确定义。<code>lua_gc</code> 的说明里，也只说“<code>LUA_GCSTEP</code>: 发起一步增量垃圾收集。 步数由 data 控制（越大的值意味着越多步）， 而其具体含义（具体数字表示了多少）并未标准化。 如果你想控制这个步数，必须实验性的测试 data 的值。 如果这一步结束了一个垃圾收集周期，返回返回 1 。 并没有给出准确的含义。实践中，我们也都是以经验取值。</p>
<p>回到源代码，我们就能搞清楚它们到底是什么了。</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">case LUA_GCSETPAUSE: &#123;</span><br><span class="line">  <span class="function"><span class="title">res</span> = g-&gt;</span>gcpause;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>gcpause = <span class="keyword">data</span>;</span><br><span class="line">  break;</span><br><span class="line">&#125;</span><br><span class="line">case LUA_GCSETSTEPMUL: &#123;</span><br><span class="line">  <span class="function"><span class="title">res</span> = g-&gt;</span>gcstepmul;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>gcstepmul = <span class="keyword">data</span>;</span><br><span class="line">  break;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里只是设置 gcpause gcstepmul　。gcpause 实际只在 lgc.c 59 行的 setthreshold 宏中用到</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">#<span class="function"><span class="title">define</span> setthreshold(g)  (g-&gt;</span>GC<span class="function"><span class="title">threshold</span> = (g-&gt;</span><span class="function"><span class="title">estimate</span>/100) * g-&gt;</span>gcpause)</span><br></pre></td></tr></table></figure>
<p>看见，GCSETPAUSE 其实是通过调整 GCthreshold 来实现的。当 GCthreshold 足够大时，<code>luaC_step</code> 不会被 <code>luaC_checkGC</code> 自动触发。事实上，GCSTOP 正是通过设置一个很大的 GCthreshold 值来实现的。</p>
<figure class="highlight elixir"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="symbol">LUA_GCSTOP:</span> &#123;</span><br><span class="line">  g-&gt;GCthreshold = MAX_LUMEM;</span><br><span class="line">  <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>gcpause 值的含义很文档一致，用来表示和实际内存使用量 estimate 的比值（放大 100 倍）。一旦内存使用量超过这个阀值，就会出发 GC 的工作。</p>
<p>要理解 gcstepmul ，就要从 <code>lua_gc</code> 的 <code>LUA_GCSTEP</code> 的实现看起。</p>
<figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> LUA_GCSTEP: &#123;</span><br><span class="line">  lu_mem a = (cast(lu_mem, data) &lt;&lt; <span class="number">10</span>);</span><br><span class="line">  <span class="keyword">if</span> (a &lt;= g-&gt;totalbytes)</span><br><span class="line">    g-&gt;GCthreshold = g-&gt;totalbytes - a;</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    g-&gt;GCthreshold = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (g-&gt;GCthreshold &lt;= g-&gt;totalbytes) &#123;</span><br><span class="line">    luaC_step(L);</span><br><span class="line">    <span class="keyword">if</span> (g-&gt;gcstate == GCSpause) &#123;  <span class="comment">/* end of cycle? */</span></span><br><span class="line">      res = <span class="number">1</span>;  <span class="comment">/* signal it */</span></span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>step 的长度 data 被放大了 1024 倍。在 lgc.c 的 26 行，也可以看到</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GCSTEPSIZE  1024u</span></span><br></pre></td></tr></table></figure>
<p>我们姑且可以认为 data 的单位是 KBytes ，和 lua 总共占用的内存 totalbytes 有些关系。</p>
<p>ps. 这里 totalbytes 是严格通过 Alloc 管理的内存量。而前面提到的 estimate 则不同，它是一个估算量，比 totalbytes 要小。这是因为，前面也提到过，userdata 的回收比较特殊。被检测出已经访问不到的 userdata 占用的内存并不会马上释放（保证 gc 元方法的安全调用），但 estimate 会抛去这部分，不算在实际内存使用量内。</p>
<p>见 lgc.c 544 行</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">udsize</span> = luaC_separateudata(L, <span class="number">0</span>)<span class="comment">;  /* separate userdata to be finalized */</span></span><br></pre></td></tr></table></figure>
<p>以及 lgc.c 553 行</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">estimate</span> = g-&gt;</span>totalbytes - udsize;  <span class="comment">/* first estimate */</span></span><br></pre></td></tr></table></figure>
<p>从代码逻辑，我们暂时可以把 data 理解为，需要处理的字节数量（以 K bytes 为单位）。如果需要处理的数据量超过了 totalbytes ，自然就可以把 GCthreshold 设置为 0 了。</p>
<p>实际上不能完全这么理解。因为 GC 过程并不是一点点回收内存，同时可用内存越来越多。GC 分标记(mark) 清除(sweep) 调用 userdata 元方法等几个阶段。只有中间的清除阶段是真正释放内存的。所以可用内存的增加（ totalbytes 减少）过程，时间上并不是线性的。通常标记的开销更大。为了让 gcstep 的每个步骤消耗的时间更平滑，就得有手段动态调整 GCthreshold 值。它和 totalbytes 最终影响了每个 step 的时间。</p>
<p>下面的关注焦点转向 <code>luaC_step</code>　，见 lgc.c 的 611 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">void luaC_step (lua_State *L) &#123;</span><br><span class="line">  global_State *g = G(L);</span><br><span class="line">  <span class="function"><span class="title">l_mem</span> lim = (GCSTEPSIZE/100) * g-&gt;</span>gcstepmul;</span><br><span class="line">  <span class="keyword">if</span> (lim == <span class="number">0</span>)</span><br><span class="line">    lim = (MAX_LUMEM-<span class="number">1</span>)/<span class="number">2</span>;  <span class="comment">/* no limit */</span></span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">gcdept</span> += g-&gt;</span><span class="function"><span class="title">totalbytes</span> - g-&gt;</span>GCthreshold;</span><br><span class="line">  <span class="keyword">do</span> &#123;</span><br><span class="line">    lim -= singlestep(L);</span><br><span class="line">    <span class="function"><span class="title">if</span> (g-&gt;</span>gcstate == GCSpause)</span><br><span class="line">      break;</span><br><span class="line">  &#125; <span class="keyword">while</span> (lim &gt; <span class="number">0</span>);</span><br><span class="line">  <span class="function"><span class="title">if</span> (g-&gt;</span>gcstate != GCSpause) &#123;</span><br><span class="line">    <span class="function"><span class="title">if</span> (g-&gt;</span>gcdept &lt; GCSTEPSIZE)</span><br><span class="line">      <span class="function"><span class="title">g</span>-&gt;</span>GC<span class="function"><span class="title">threshold</span> = g-&gt;</span><span class="function"><span class="title">totalbytes</span> + GCSTEPSIZE;  /* - lim/g-&gt;</span>gcstepmul;*/</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="function"><span class="title">g</span>-&gt;</span>gcdept -= GCSTEPSIZE;</span><br><span class="line">      <span class="function"><span class="title">g</span>-&gt;</span>GC<span class="function"><span class="title">threshold</span> = g-&gt;</span>totalbytes;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="function"><span class="title">lua_assert</span>(g-&gt;</span><span class="function"><span class="title">totalbytes</span> &gt;= g-&gt;</span>estimate);</span><br><span class="line">    setthreshold(g);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从代码我们可以看到，GC 的核心其实在于 singlestep 函数。<code>luaC_step</code> 每次调用多少次 singlestep 跟 gcstepmul 的值有关。</p>
<p>如果是自动进行的 GC ，当 totalbytes 大于等于 GCthreshold 时，就会触发 <code>luaC_step</code> 。每次 <code>luaC_step</code> ，GCthreshold 都会被调高 1K (GCSTEPSIZE) 直到 GCthreshold 追上 totalbytes 。这个追赶过程通常发生在 mark 流程。因为这个流程中，totalbytes 是只增不减的。</p>
<p>如果是手控 GC ，每次 gcstep 调用执行多少次 <code>luaC_step</code> 则跟 data 值有关。大体上是 1 就表示一次（在 mark 过程中就是这样）到了 sweep 流程就不一定了。这和 singlestep 调用次数，即 gcstepmul 的值有关。它影响了 totalbytes 的减小速度。</p>
<p>所以，一两句话很难严格定义出这些控制 GC 步进量的参数的含义，只能慢慢阅读代码，看看实现了。</p>
<p>在 lua 手册的 2.10 这样描述“step multiplier 控制了收集器相对内存分配的速度。 更大的数字将导致收集器工作的更主动的同时，也使每步收集的尺寸增加。 小于 1 的值会使收集器工作的非常慢，可能导致收集器永远都结束不了当前周期。 缺省值为 2 ，这意味着收集器将以内存分配器的两倍速运行。”</p>
<p>从代码看，这绝非严格定义。至少从今天已经分析的代码中还看不出这一点。</p>
<p>gcstepmul 的值和内存增涨速度如何产生联系？明天再写 :)</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>云风的Lua-GC解剖</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>云风的Lua-GC解剖</tag>
      </tags>
  </entry>
  <entry>
    <title>云风的Blog-Lua GC的源码解剖5</title>
    <url>/lua/%E4%BA%91%E9%A3%8E%E7%9A%84Blog-Lua%20GC%20%E7%9A%84%E6%BA%90%E7%A0%81%E5%89%96%E6%9E%905/</url>
    <content><![CDATA[<p><a href="https://blog.codingnow.com/2011/03/lua_gc_5.html" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h3 id="Lua-GC-的源码剖析-5"><a href="#Lua-GC-的源码剖析-5" class="headerlink" title="Lua GC 的源码剖析 (5)"></a>Lua GC 的源码剖析 (5)</h3><p>今天来说说 write barrier 。</p>
<p>在 GC 的扫描过程中，由于分步执行，难免会出现少描了一半时，那些已经被置黑的对象又被修改，需要重新标记的情况。这就需要在改写对象时，建立 write barrier 。在扫描过程中触发 write barrier 的操作影响的对象被正确染色，或是把需要再染色的对象记录下来，留到 mark 的最后阶段 atomic 完成。</p>
<p>和 barrier 相关的 API 有四个，定义在 lgc.h 86 行：</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line"><span class="attr">#define luaC_barrier(L,p,v) &#123; if (valiswhite(v) &amp;&amp; isblack(obj2</span>gco<span class="comment">(p)</span>))  \</span><br><span class="line">    luaC_barrierf<span class="comment">(L,obj2gco(p)</span>,gcvalue<span class="comment">(v)</span>); &#125;</span><br><span class="line"></span><br><span class="line"><span class="attr">#define luaC_barriert(L,t,v) &#123; if (valiswhite(v) &amp;&amp; isblack(obj2</span>gco<span class="comment">(t)</span>))  \</span><br><span class="line">    luaC_barrierback<span class="comment">(L,t)</span>; &#125;</span><br><span class="line"></span><br><span class="line"><span class="attr">#define luaC_objbarrier(L,p,o)  \</span></span><br><span class="line"><span class="attr">    &#123; if (iswhite(obj2</span>gco<span class="comment">(o)</span>) &amp;&amp; isblack<span class="comment">(obj2gco(p)</span>)) \</span><br><span class="line">        luaC_barrierf<span class="comment">(L,obj2gco(p)</span>,obj<span class="number">2</span>gco<span class="comment">(o)</span>); &#125;</span><br><span class="line"></span><br><span class="line"><span class="attr">#define luaC_objbarriert(L,t,o)  \</span></span><br><span class="line"><span class="attr">   &#123; if (iswhite(obj2</span>gco<span class="comment">(o)</span>) &amp;&amp; isblack<span class="comment">(obj2gco(t)</span>)) luaC_barrierback<span class="comment">(L,t)</span>; &#125;</span><br></pre></td></tr></table></figure>
<p><code>luaC_barrier</code> 和 <code>luaC_objbarrier</code> 功能上相同，只不过前者是针对 TValue 的，后者则针对 GCObject 。它用于把 v 向 p 关联时，当 v 为白色且 p 为黑色时，调用 <code>luaC_barrierf</code> 。</p>
<p><code>luaC_barriert</code> 和 <code>luaC_objbarriert</code> 则是用于将 v 关联到 t 时，调用 <code>luaC_barrierback</code> 。当然前提条件也是 v 为白色，且 t 为黑色。</p>
<p>为何 table 要被特殊对待？</p>
<p>因为 table 的引用关系是 1 对 N ，往往修改同一个 table 是很频繁的。而其它对象之间则是有限的联系。分开处理可以减少 barrier 本身的开销。</p>
<p><code>luaC_barrierf</code> 用于把新建立联系的对象立刻标记。它的实现在 lgc.c 的 663 行：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">void luaC_barrierf (<span class="name">lua_State</span> *L, GCObject *o, GCObject *v) &#123;</span><br><span class="line">  global_State *g = G(<span class="name">L</span>)<span class="comment">;</span></span><br><span class="line">  lua_assert(<span class="name">isblack</span>(<span class="name">o</span>) <span class="symbol">&amp;&amp;</span> iswhite(<span class="name">v</span>) <span class="symbol">&amp;&amp;</span> !isdead(<span class="name">g</span>, v) <span class="symbol">&amp;&amp;</span> !isdead(<span class="name">g</span>, o))<span class="comment">;</span></span><br><span class="line">  lua_assert(<span class="name">g-&gt;gcstate</span> != GCSfinalize <span class="symbol">&amp;&amp;</span> g-&gt;gcstate != GCSpause)<span class="comment">;</span></span><br><span class="line">  lua_assert(<span class="name">ttype</span>(<span class="name">&amp;o-&gt;gch</span>) != LUA_TTABLE)<span class="comment">;</span></span><br><span class="line">  /* must keep invariant? */</span><br><span class="line">  if (g-&gt;gcstate == GCSpropagate)</span><br><span class="line">    reallymarkobject(g, v);  /* restore invariant */</span><br><span class="line">  else  /* don't mind */</span><br><span class="line">    makewhite(g, o);  /* mark as white just to avoid other barriers */</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>简而言之，当 GC 处于 GCSpropagate 状态（mark 流程中），就标记 v 。否则，把与之联系的节点 o 从黑色置为白色。前面我们已经说过，lua 的 GC 采用了两种白色标记，乒乓切换。在 mark 流程结束后，白色状态被切换。这个时候调用 makewhite 并不会导致 sweep 过程清除这个节点。同时，由于 o 变为白色，就可以减少 barrier 的开销。即，再有对 o 的修改，不会产生 <code>luaC_barrierf</code> 的函数调用了。</p>
<p><code>luaC_barrierback</code> 会把要修改的 table 退回 gary 集合，留待将来继续标记。见代码 lgc.c 的 676 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">void luaC_barrierback (lua_State *L, Table *t) &#123;</span><br><span class="line">  global_State *g = G(L);</span><br><span class="line">  GCObject *o = obj2gco(t);</span><br><span class="line">  lua_assert(isblack(o) &amp;&amp; !isdead(g, o));</span><br><span class="line">  <span class="function"><span class="title">lua_assert</span>(g-&gt;</span><span class="function"><span class="title">gcstate</span> != GCSfinalize &amp;&amp; g-&gt;</span>gcstate != GCSpause);</span><br><span class="line">  black2gray(o);  <span class="comment">/* make table gray (again) */</span></span><br><span class="line">  <span class="function"><span class="title">t</span>-&gt;</span><span class="function"><span class="title">gclist</span> = g-&gt;</span>grayagain;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>grayagain = o;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里提到了 grayagain 链，昨天已经看到，这个链表会推迟到 atomic 阶段处理。把 table 设会灰色可使后续对其的修改不再触发 <code>luaC_barrierback</code> 。</p>
<p>注意，对弱表的修改是不会触发 <code>luaC_barrierback</code> 的。因为弱表不会是黑色。弱表也不能触发它。因为每个 GCObject 都只能存在于一个链表上。弱表自己挂接到 weak 链，就不能向 grayagain 挂接。这就解释了 lgc.c 285 行：</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="comment">(traversetable(g, h)</span>)  <span class="comment">/* table is weak? */</span></span><br><span class="line">  black<span class="number">2</span>gray<span class="comment">(o)</span>;  <span class="comment">/* keep it gray */</span></span><br></pre></td></tr></table></figure>
<p>为何要把弱表从黑色设回灰色。</p>
<p>因为弱表又分 key weak 和 value weak ，它倒不一定完全都包含了弱引用。在扫描过程中，对弱表的修改又不会触发 barrier 。这会导致最终扫描完成后，某些弱表里可能还藏有一些新建立的强应用关系。到这里，我们便可以解释昨天遗留的另一个问题，弱表为何到了 atomic 节点需要再次 mark 。见 lgc.c 的 533 行：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">/* remark weak tables */</span><br><span class="line">g-&gt;gray = g-&gt;weak<span class="comment">;</span></span><br><span class="line">g-&gt;weak = NULL<span class="comment">;</span></span><br><span class="line">lua_assert(!iswhite(<span class="name">obj2gco</span>(<span class="name">g-&gt;mainthread</span>)))<span class="comment">;</span></span><br><span class="line">markobject(<span class="name">g</span>, L)<span class="comment">;  /* mark running thread */</span></span><br><span class="line">markmt(<span class="name">g</span>)<span class="comment">;  /* mark basic metatables (again) */</span></span><br><span class="line">propagateall(<span class="name">g</span>)<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>再次 mark basic metatables 的原因是，设置基本类型的全局 metatable 并没有设置 write barrier （这组 metatable 比较特殊，不适合用以上的 barrier 函数）。见 lapi.c 的 710 行：</p>
<figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">switch</span> (ttype(obj)) &#123;</span><br><span class="line">  <span class="keyword">case</span> LUA_TTABLE: &#123;</span><br><span class="line">    hvalue<span class="function"><span class="params">(obj)</span>-&gt;</span>metatable = mt;</span><br><span class="line">    <span class="keyword">if</span> (mt)</span><br><span class="line">      luaC_objbarriert(L, hvalue(obj), mt);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">case</span> LUA_TUSERDATA: &#123;</span><br><span class="line">    uvalue<span class="function"><span class="params">(obj)</span>-&gt;</span>metatable = mt;</span><br><span class="line">    <span class="keyword">if</span> (mt)</span><br><span class="line">      luaC_objbarrier(L, rawuvalue(obj), mt);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  default: &#123;</span><br><span class="line">    G<span class="function"><span class="params">(L)</span>-&gt;</span>mt[ttype(obj)] = mt;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里，修改 table 的 metatable 调用了 <code>luaC_objbarriert</code> ，修改 userdata 的 metatable 调用了 <code>luaC_objbarrier</code> ，修改全局 metatable 是直接进行的。</p>
<p>如果审查所有的代码，会发现 barrier 的调用分布在很多地方。大多数情况下，barrier 的开销并不大，就是一次条件判断。但 barrier 也会被执行很频繁，性能必须有保障，这也是为什么 barrier 相关 api 都用宏来实现的缘故。</p>
<hr>
<p>理解了 write barrier 的实现，我们就可以回头来看 propagatemark 过程了。</p>
<p>propagatemark 每次会标记一个灰色对象所有的关联节点，并把自己置为黑色。见 lgc.c 的 273 行。</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** traverse one gray object, turning it to black.</span></span><br><span class="line"><span class="comment">** Returns `quantity' traversed.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">static l_mem propagatemark <span class="comment">(global_State *g)</span> &#123;</span><br><span class="line">  GCObject *o = g-&gt;gray;</span><br><span class="line">  lua_assert<span class="comment">(isgray(o)</span>);</span><br><span class="line">  gray<span class="number">2</span>black<span class="comment">(o)</span>;</span><br><span class="line">  switch <span class="comment">(o-&gt;gch.tt)</span> &#123;</span><br></pre></td></tr></table></figure>
<p>之后就是对需要迭代标记的具体类型的处理了。</p>
<p>对 table 的处理是这样的，见 lgc.c 的 282 行：</p>
<figure class="highlight gams"><table><tr><td class="code"><pre><span class="line">case LUA_TTABLE: &#123;</span><br><span class="line">  <span class="keyword">Table</span> *h <span class="comment">= gco2h(o)</span>;</span><br><span class="line">  g-&gt;gray = h-&gt;gclist;</span><br><span class="line">  <span class="keyword">if</span> (traversetable(g, h))  <span class="comment">/* table is weak? */</span></span><br><span class="line">    black2gray(o);  <span class="comment">/* keep it gray */</span></span><br><span class="line">  return sizeof(<span class="keyword">Table</span>) + sizeof(TValue) <span class="comment">* h-&gt;sizearray +</span></span><br><span class="line">                         sizeof(Node) * sizenode(h);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从 gray 链上取掉当前 table 节点，然后迭代这个 table ，如果是一个弱表，则把其还原为灰色。</p>
<p>traversetable 枚举了 table 中的所有引用项。并对弱表做了处理。见 lgc.c 的 166 行：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (mode &amp;&amp; ttisstring(mode)) &#123;  <span class="comment">/* is there a weak mode? */</span></span><br><span class="line">  weakkey = (strchr(svalue(mode), <span class="string">'k'</span>) != <span class="keyword">NULL</span>);</span><br><span class="line">  weakvalue = (strchr(svalue(mode), <span class="string">'v'</span>) != <span class="keyword">NULL</span>);</span><br><span class="line">  <span class="keyword">if</span> (weakkey || weakvalue) &#123;  <span class="comment">/* is really weak? */</span></span><br><span class="line">    h-&gt;marked &amp;= ~(KEYWEAK | VALUEWEAK);  <span class="comment">/* clear bits */</span></span><br><span class="line">    h-&gt;marked |= cast_byte((weakkey &lt;&lt; KEYWEAKBIT) |</span><br><span class="line">                           (weakvalue &lt;&lt; VALUEWEAKBIT));</span><br><span class="line">    h-&gt;gclist = g-&gt;weak;  <span class="comment">/* must be cleared after GC, ... */</span></span><br><span class="line">    g-&gt;weak = obj2gco(h);  <span class="comment">/* ... so put in the appropriate list */</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>被扫描到的弱表把自己挂到了 weak 链上。单独保存一个链表是因为弱表在扫描的最后阶段还需要把里面不存在的弱引用项清理掉。关于清理工作，是由 atomic 函数中对 cleartable 调用完成的。昨天已经讲过，无庸赘述。</p>
<p>对 function 的 mark 工作在 traverseclosure 函数中进行，lgc.c 224 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">static void traverseclosure (global_State *g, Closure *cl) &#123;</span><br><span class="line">  <span class="function"><span class="title">markobject</span>(g, cl-&gt;</span>c.env);</span><br><span class="line">  <span class="function"><span class="title">if</span> (cl-&gt;</span>c.isC) &#123;</span><br><span class="line">    int i;</span><br><span class="line">    <span class="keyword">for</span> (i=<span class="number">0</span>; ic.nupvalues; i++)  <span class="comment">/* mark its upvalues */</span></span><br><span class="line">      <span class="function"><span class="title">markvalue</span>(g, &amp;cl-&gt;</span>c.upvalue[i]);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">else</span> &#123;</span><br><span class="line">    int i;</span><br><span class="line">    <span class="function"><span class="title">lua_assert</span>(cl-&gt;</span><span class="function"><span class="title">l</span>.nupvalues == cl-&gt;</span><span class="function"><span class="title">l</span>.p-&gt;</span>nups);</span><br><span class="line">    <span class="function"><span class="title">markobject</span>(g, cl-&gt;</span>l.p);</span><br><span class="line">    <span class="keyword">for</span> (i=<span class="number">0</span>; il.nupvalues; i++)  <span class="comment">/* mark its upvalues */</span></span><br><span class="line">      <span class="function"><span class="title">markobject</span>(g, cl-&gt;</span>l.upvals[i]);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>lua 中 C Function 和 Lua Function 的内部结构是不同的。所以是分别独立的代码。主要就是对 upvalue 以及环境表的标记了。</p>
<p>对 thread 的处理相对复杂一点。见 lgc.c 297 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">case LUA_TTHREAD: &#123;</span><br><span class="line">  lua_State *th = gco2th(o);</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">gray</span> = th-&gt;</span>gclist;</span><br><span class="line">  <span class="function"><span class="title">th</span>-&gt;</span><span class="function"><span class="title">gclist</span> = g-&gt;</span>grayagain;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>grayagain = o;</span><br><span class="line">  black2gray(o);</span><br><span class="line">  traversestack(g, th);</span><br><span class="line">  <span class="function"><span class="title">return</span> sizeof(lua_State) + sizeof(TValue) * th-&gt;</span>stacksize +</span><br><span class="line">                             <span class="function"><span class="title">sizeof</span>(CallInfo) * th-&gt;</span>size_ci;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里的链表操作可以简述为，把自己从 gray 链上摘下，放到 grayagain 链表中。并把自己保持为灰色状态。</p>
<p>这是因为 thread 关联的对象其实是运行堆栈，堆栈是随着运行过程不断变化的。如果目前尚处于 mark 流程没有结束的话，后面发生变化是很有可能的。而 stack 上数据的修改是不经过 write barrier 的。也没有必要为最频繁的 stack 修改做 barrier ，那样对性能影响就很大了。最简单的办法就是把 thread 推迟到 atomic 阶段重扫描一次。</p>
<p>atomic 函数，lgc.c 537 行的</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">markobject<span class="comment">(g, L)</span>;  <span class="comment">/* mark running thread */</span></span><br></pre></td></tr></table></figure>
<p>也是这个用意。</p>
<p>ps. lua 的 GC 在实现时，即使要推迟扫描，也要先做一次。都是为了 atomic 阶段尽量短，把有可能遍历的部分先遍历完。</p>
<p>TPROTO 对象的遍历函数在 lgc.c 的 199 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** All marks are conditional because a GC may happen while the</span></span><br><span class="line"><span class="comment">** prototype is still being created</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">static void traverseproto (global_State *g, Proto *f) &#123;</span><br><span class="line">  int i;</span><br><span class="line">  <span class="function"><span class="title">if</span> (f-&gt;</span><span class="function"><span class="title">source</span>) stringmark(f-&gt;</span>source);</span><br><span class="line">  <span class="keyword">for</span> (i=<span class="number">0</span>; isizek; i++)  <span class="comment">/* mark literals */</span></span><br><span class="line">    <span class="function"><span class="title">markvalue</span>(g, &amp;f-&gt;</span>k[i]);</span><br><span class="line">  <span class="keyword">for</span> (i=<span class="number">0</span>; isizeupvalues; i++) &#123;  <span class="comment">/* mark upvalue names */</span></span><br><span class="line">    <span class="function"><span class="title">if</span> (f-&gt;</span>upvalues[i])</span><br><span class="line">      <span class="function"><span class="title">stringmark</span>(f-&gt;</span>upvalues[i]);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (i=<span class="number">0</span>; isizep; i++) &#123;  <span class="comment">/* mark nested protos */</span></span><br><span class="line">    <span class="function"><span class="title">if</span> (f-&gt;</span>p[i])</span><br><span class="line">      <span class="function"><span class="title">markobject</span>(g, f-&gt;</span>p[i]);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (i=<span class="number">0</span>; isizelocvars; i++) &#123;  <span class="comment">/* mark local-variable names */</span></span><br><span class="line">    <span class="function"><span class="title">if</span> (f-&gt;</span>locvars[i].varname)</span><br><span class="line">      <span class="function"><span class="title">stringmark</span>(f-&gt;</span>locvars[i].varname);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如注释所言，这里有很多的判断。这缘于 prototype 的创建过程是可能分步的，中间有可能被 GC 打断。prototype 的创建伴随着诸多 GCObject 的创建。新对象的创建（大多数是 string）带来的是新的内存申请，很容易触发自动 GC 的进行。如果对 prototype 的创建过程有兴趣，可以去阅读 lparser.c 的 <code>luaY_parser</code> 函数，以及 lundump.c 中的 <code>luaU_undump</code> 函数。</p>
<p>以上就是所有需要迭代遍历的数据类型了。</p>
<p>一切准备妥当后，GC 将切换到 GCSsweepstring 。见 lgc.c 548 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* flip current white */</span></span><br><span class="line"><span class="function"><span class="title">g</span>-&gt;</span>currentwhite = cast_byte(otherwhite(g));</span><br><span class="line"><span class="function"><span class="title">g</span>-&gt;</span>sweepstrgc = <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">sweepgc</span> = &amp;g-&gt;</span>rootgc;</span><br><span class="line"><span class="function"><span class="title">g</span>-&gt;</span>gcstate = GCSsweepstring;</span><br><span class="line"><span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">estimate</span> = g-&gt;</span>totalbytes - udsize;  <span class="comment">/* first estimate */</span></span><br></pre></td></tr></table></figure>
<p>这时，userdata 的 gc 元方法虽未调用，所有不再有引用的 userdata 不会在这趟 GC 环节中清理出去，但它们占据的空间已经被 estimate 忽略。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>云风的Lua-GC解剖</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>云风的Lua-GC解剖</tag>
      </tags>
  </entry>
  <entry>
    <title>云风的Blog-Lua GC的源码解剖4</title>
    <url>/lua/%E4%BA%91%E9%A3%8E%E7%9A%84Blog-Lua%20GC%20%E7%9A%84%E6%BA%90%E7%A0%81%E5%89%96%E6%9E%904/</url>
    <content><![CDATA[<p><a href="https://blog.codingnow.com/2011/03/lua_gc_4.html" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h3 id="Lua-GC-的源码剖析-4"><a href="#Lua-GC-的源码剖析-4" class="headerlink" title="Lua GC 的源码剖析 (4)"></a>Lua GC 的源码剖析 (4)</h3><p>今天来看一下 mark 过程是怎样实现的。</p>
<p>所有的 GC 流程，都从 singlestep 函数开始。singlestep 就是一个最简单的状态机。GC 状态简单的从一个状态切换到下一个状态，循环不止。状态标识放在 global state 的 gcstate 域中。这一点前面谈过。</p>
<p>开始的两个状态和 mark 过程有关。</p>
<p>初始的 GCSpause 状态下，执行 markroot 函数。我们来看一下 markroot 的代码。见 lgc.c 的 501 行。</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* mark root set */</span></span><br><span class="line">static void markroot (lua_State *L) &#123;</span><br><span class="line">  global_State *g = G(L);</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>gray = NULL;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>grayagain = NULL;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>weak = NULL;</span><br><span class="line">  <span class="function"><span class="title">markobject</span>(g, g-&gt;</span>mainthread);</span><br><span class="line">  <span class="comment">/* make global table be traversed before main stack */</span></span><br><span class="line">  <span class="function"><span class="title">markvalue</span>(g, gt(g-&gt;</span>mainthread));</span><br><span class="line">  markvalue(g, registry(L));</span><br><span class="line">  markmt(g);</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>gcstate = GCSpropagate;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>主要是把状态复位的操作以及标记根节点。</p>
<p>lua 采用是 <a href="http://en.wikipedia.org/wiki/Garbage_collection_(computer_science" rel="external nofollow noopener noreferrer" target="_blank">Tri-colour marking GC 算法</a>#Tri-colour_marking) 。需要维护一个灰色节点集。这里用 gary 存放。grayagain 保存着需要原子操作标记的灰色节点。</p>
<p>weak 保存着需要清理的 weak 表。</p>
<p>这里的 markobject 和 markvalue 都是用宏实现的。区别仅在于操作的是 GCObject 还是 TValue 。最终都是通过调用 reallymarkobject 实现的。markmt 也是直接 mark 的 global state 中的若干元表。这些元表作用于基本的几个数据类型。这些加起来成为整个数据的根。对 lua 语言有所了解的话，不难理解这几行实现。</p>
<p>核心的实现见 lgc.c 的 69 行</p>
<figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line">static <span class="literal">void</span> reallymarkobject (global_State *g, GCObject *o) &#123;</span><br><span class="line">  lua_assert(iswhite(o) &amp;&amp; !isdead(g, o));</span><br><span class="line">  white2gray(o);</span><br><span class="line">  <span class="keyword">switch</span> (o-&gt;gch.tt) &#123;</span><br><span class="line">    <span class="keyword">case</span> LUA_TSTRING: &#123;</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_TUSERDATA: &#123;</span><br><span class="line">      Table *mt = gco2u<span class="function"><span class="params">(o)</span>-&gt;</span>metatable;</span><br><span class="line">      gray2black(o);  <span class="comment">/* udata are never gray */</span></span><br><span class="line">      <span class="keyword">if</span> (mt) markobject(g, mt);</span><br><span class="line">      markobject<span class="function"><span class="params">(g, gco2u(o)-&gt;env)</span>;</span></span><br><span class="line"><span class="function">      <span class="title">return</span>;</span></span><br><span class="line"><span class="function">    &#125;</span></span><br><span class="line"><span class="function">    <span class="title">case</span> <span class="title">LUA_TUPVAL</span>: &#123;</span></span><br><span class="line"><span class="function">      <span class="title">UpVal</span> *<span class="title">uv</span> = <span class="title">gco2uv</span><span class="params">(o)</span>;</span></span><br><span class="line"><span class="function">      <span class="title">markvalue</span><span class="params">(g, uv-&gt;v)</span>;</span></span><br><span class="line"><span class="function">      <span class="title">if</span> <span class="params">(uv-&gt;v == &amp;uv-&gt;u.value)</span>  /* <span class="title">closed</span>? */</span></span><br><span class="line"><span class="function">        <span class="title">gray2black</span><span class="params">(o)</span>;  /* <span class="title">open</span> <span class="title">upvalues</span> <span class="title">are</span> <span class="title">never</span> <span class="title">black</span> */</span></span><br><span class="line"><span class="function">      <span class="title">return</span>;</span></span><br><span class="line"><span class="function">    &#125;</span></span><br><span class="line"><span class="function">    <span class="title">case</span> <span class="title">LUA_TFUNCTION</span>: &#123;</span></span><br><span class="line"><span class="function">      <span class="title">gco2cl</span><span class="params">(o)</span>-&gt;</span>c.gclist = g-&gt;gray;</span><br><span class="line">      g-&gt;gray = o;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_TTABLE: &#123;</span><br><span class="line">      gco2h<span class="function"><span class="params">(o)</span>-&gt;</span>gclist = g-&gt;gray;</span><br><span class="line">      g-&gt;gray = o;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_TTHREAD: &#123;</span><br><span class="line">      gco2th<span class="function"><span class="params">(o)</span>-&gt;</span>gclist = g-&gt;gray;</span><br><span class="line">      g-&gt;gray = o;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> LUA_TPROTO: &#123;</span><br><span class="line">      gco2p<span class="function"><span class="params">(o)</span>-&gt;</span>gclist = g-&gt;gray;</span><br><span class="line">      g-&gt;gray = o;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    default: lua_assert(<span class="number">0</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>它按 GCObject 的实际类型来 mark 它。reallymarkobject 的时间复杂度是 O(1) 的，它不会递归标记相关对象，虽然大多数 GCObject 都关联有其它对象。</p>
<p>保证 O(1) 时间使得标记过程可以均匀分摊在逐个短小的时间片内，不至于停止世界太久。这里就需要用到三色标记法。</p>
<p>reallymarkobject 进入时，先把对象设置为灰色（通过 white2gray 这个宏）。然后再根据具体类型，当一个对象的所有关联对象都被标记后，再从灰色转为黑色。</p>
<p>因为 TSTRING 一定没有关联对象，而且所有的字符串都是统一独立处理的。这里可以做一个小优化，不需要设置为黑色，只要不是白色就可以清理。所以此处不必染黑。</p>
<p>但 TUSERDATA 则不同，它是跟其它对象一起处理的。标记 userdata 就需要调用 gray2black 了。另外，还需要标记 userdata 的元表和环境表。</p>
<p>TUPVAL 是一个特殊的东西。在 lua 编程，以及写 C 代码和 lua 交互时，都看不到这种类型。它用来解决多个 closure 共享同一个 upvalue 的情况。实际上是对一个 upvalue 的引用。问什么 TUPVAL 会有 open 和 closed 两种状态？应该这样理解。</p>
<p>当一个 lua 函数本执行的时候，和 C 语言不一样，它不仅可以看到当前层次上的 local 变量，还可以看到上面所有层次的 local 变量。这个可见性是由 lua 解析器解析你的 lua 代码时定位的（换句话说，就是在“编译”期决定的）。那些不属于你的函数当前层次上的 local 变量，就称之为 upvalue 。upvalue 这个概念是由 parser 引入的。在 Lua 中，任何一个 function 其实都是由 proto 和运行时绑定的 upvalue 构成的。proto 将如何绑定 upvalue 是在 parser 生成的 bytecode 里描述清楚了的。如果对这一块的实现代码有兴趣，可以参考 lvm.c 的 719 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">case OP_CLOSURE: &#123;</span><br><span class="line">  Proto *p;</span><br><span class="line">  Closure *ncl;</span><br><span class="line">  int nup, j;</span><br><span class="line">  <span class="function"><span class="title">p</span> = cl-&gt;</span><span class="function"><span class="title">p</span>-&gt;</span>p[GETARG_Bx(i)];</span><br><span class="line">  <span class="function"><span class="title">nup</span> = p-&gt;</span>nups;</span><br><span class="line">  <span class="function"><span class="title">ncl</span> = luaF_newLclosure(L, nup, cl-&gt;</span>env);</span><br><span class="line">  <span class="function"><span class="title">ncl</span>-&gt;</span>l.p = p;</span><br><span class="line">  <span class="function"><span class="title">for</span> (j=0; jl.upvals[j] = cl-&gt;</span>upvals[GETARG_B(*pc)];</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">      lua_assert(GET_OPCODE(*pc) == OP_MOVE);</span><br><span class="line">      <span class="function"><span class="title">ncl</span>-&gt;</span>l.upvals[j] = luaF_findupval(L, base + GETARG_B(*pc));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  setclvalue(L, ra, ncl);</span><br><span class="line">  Protect(luaC_checkGC(L));</span><br><span class="line">  continue;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们会看到，调用 <code>luaF_newLclosure</code> 生成完一个 Lua Closure 后，会去填那张 upvalue 表。当 upvalue 尚在堆栈上时，其实是调用 <code>luaF_findupval</code> 去生成一个对堆栈上的特定值之引用的 TUPVAL 对象的。<code>luaF_findupval</code> 的实现不再列在这里，它的主要作用就是保证对堆栈相同位置的引用之生成一次。生成的这个对象就是 open 状态的。所有 open 的 TUPVAL 用一个双向链表串起来，挂在 global state 的 uvhead 中。</p>
<p>一旦函数返回，某些堆栈上的变量就会消失，这时，还被某些 upvalue 引用的变量就必须找个地方妥善安置。这个安全的地方就是 TUPVAL 结构之中。修改引用指针的结果，就被认为是 close 了这个 TUPVAL 。相关代码可以去看 lfunc.c 中 <code>luaF_close</code> 的实现。</p>
<p>走题太多。现在回来看 TUPVAL 的 mark 过程。mark upvalue 引用的对象之后，closed TUPVAL 就可以置黑了。为何 open 状态的 TUPVAL 需要留为灰色待处理呢？这是因为 open TUPVAL 是易变的。GC 分步执行，我们无法预料在 mark 流程走完前，堆栈上被引用的数据会不会发生变化。</p>
<p>事实上，在 mark 的最后一个步骤，我们会看到所有的 open TUPVAL 被再次 mark 一次。做这件事情的函数是 lgc.c 516 行的：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">static void remarkupvals (global_State *g) &#123;</span><br><span class="line">  UpVal *uv;</span><br><span class="line">  <span class="function"><span class="title">for</span> (uv = g-&gt;</span><span class="function"><span class="title">uvhead</span>.u.l.next; uv != &amp;g-&gt;</span><span class="function"><span class="title">uvhead</span>; uv = uv-&gt;</span>u.l.next) &#123;</span><br><span class="line">    <span class="function"><span class="title">lua_assert</span>(uv-&gt;</span><span class="function"><span class="title">u</span>.l.next-&gt;</span><span class="function"><span class="title">u</span>.l.prev == uv &amp;&amp; uv-&gt;</span><span class="function"><span class="title">u</span>.l.prev-&gt;</span>u.l.next == uv);</span><br><span class="line">    <span class="keyword">if</span> (isgray(obj2gco(uv)))</span><br><span class="line">      <span class="function"><span class="title">markvalue</span>(g, uv-&gt;</span>v);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其它的数据类型处理都很简单。把自己挂在 gray 链表上即可。这条链表将在后面的 GCSpropagate 步骤被处理到。</p>
<hr>
<p>GC 状态为 GCSpropagate 时，singlestep 就是调用 propagatemark 处理 global state 中的 gray 链。</p>
<p>今天白天因为有些其他工作做，所以没有留太多时间写这篇 Blog ，propagatemark 内部的细节分析得等到下一篇了。从总体看来，propagatemark 标记了各种 GCObject 的直接相关级的对象。</p>
<p>到这里，我们可以回答昨天遗留下来的一个问题：GC 的分步过程是如何控制进度的。</p>
<p>从前面分析过的代码上来，singlestep 的返回值决定了 GC 的进度。在 GC 进行过程中，如何知道做完整个 GC 流程的时间，以及目前做的进度呢？</p>
<p>我们可以依靠的数据只有内存管理器辖下的内存数量。大致上，整个 GC 需要的总时间和 GCObject 的数量成正比。但每种类型的 GCObject 的处理时间复杂度却各不相同。仔细衡量每种类型的处理时间差别不太现实，它可能和具体机器也有关系。但我们却可以大体上认为，占用内存较多的对象，处理时间也更长一点。这里不包括 string 和 userdata 。它们即使占用内存较多，但却没有增加 mark 的时间。虽然不太精确，但对 table ，function ，thread 这些类型来说，这种估算方式却比较接近。</p>
<p>所以在 propagatemark 过程中，每 mark 一个灰色节点，就返回了它实际占用的内存字节数作为 quantity 值。</p>
<p>如果回顾昨天看过的代码。</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">l_mem</span> lim = (GCSTEPSIZE/100) * g-&gt;</span>gcstepmul;</span><br><span class="line"><span class="keyword">if</span> (lim == <span class="number">0</span>)</span><br><span class="line">  lim = (MAX_LUMEM-<span class="number">1</span>)/<span class="number">2</span>;  <span class="comment">/* no limit */</span></span><br><span class="line"><span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">gcdept</span> += g-&gt;</span><span class="function"><span class="title">totalbytes</span> - g-&gt;</span>GCthreshold;</span><br><span class="line"><span class="keyword">do</span> &#123;</span><br><span class="line">  lim -= singlestep(L);</span><br><span class="line">  <span class="function"><span class="title">if</span> (g-&gt;</span>gcstate == GCSpause)</span><br><span class="line">    break;</span><br><span class="line">&#125; <span class="keyword">while</span> (lim &gt; <span class="number">0</span>);</span><br></pre></td></tr></table></figure>
<p>我们就能理解，当 gcstepmul 为 100 时，每次由 <code>luaC_checkGC</code> 满足条件后调用一次 <code>luaC_step</code> ，大致就会标记 1K 的数据（当处于 mark 环节中时）。而每次 <code>luaC_step</code> 会递增 GCthreshold 1K 。</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">if</span> (g-&gt;</span>gcstate != GCSpause) &#123;</span><br><span class="line">  <span class="function"><span class="title">if</span> (g-&gt;</span>gcdept &lt; GCSTEPSIZE)</span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>GC<span class="function"><span class="title">threshold</span> = g-&gt;</span><span class="function"><span class="title">totalbytes</span> + GCSTEPSIZE;  /* - lim/g-&gt;</span>gcstepmul;*/</span><br><span class="line">  <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>gcdept -= GCSTEPSIZE;</span><br><span class="line">    <span class="function"><span class="title">g</span>-&gt;</span>GC<span class="function"><span class="title">threshold</span> = g-&gt;</span>totalbytes;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>依赖自动 GC ，只有 totalbytes 持续增长才会驱动 <code>luaC_step</code> 不断工作下去的。但增长速度如果快于 mark 速度，那么就会产生问题。这正是 lua 手册中所警告的：“step multiplier 控制了收集器相对内存分配的速度。 更大的数字将导致收集器工作的更主动的同时，也使每步收集的尺寸增加。 小于 1 的值会使收集器工作的非常慢，可能导致收集器永远都结束不了当前周期。”</p>
<p>我们也可以看到，自动 GC 过程处于 sweep 流程时，收集器也并不那么急于释放内存了。因为释放内存会导致 totalbytes 减少，如果没有新的对象分配出来，totalbytes 是不会增加到触发新的 <code>luaC_step</code> 的。</p>
<p>手动分步 GC 则是另一番光景。<code>lua_gc</code> GCSTEP 使用的 data 值，在处于 mark 流程时，便可以认为扫描的内存字节数乘上 step multiplier 这个系数。当然这只是大致上个估计。毕竟内存不是理想中的逐字节扫描的。</p>
<p>等待所有灰色节点都处理完，我们就可以开始清理那些剩下的白色节点了吗？不，因为 mark 是分步执行的，中间 lua 虚拟机中的对象关系可能又发生的变化。所以在开始清理工作前，我们还需要做最后一次扫描。这个过程不可以再被打断。</p>
<p>让我们看看相关的处理函数。见 lgc.c 526 行：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">static void atomic (lua_State *L) &#123;</span><br><span class="line">  global_State *g = G(L);</span><br><span class="line">  size_t udsize;  <span class="comment">/* total size of userdata to be finalized */</span></span><br><span class="line">  <span class="comment">/* remark occasional upvalues of (maybe) dead threads */</span></span><br><span class="line">  remarkupvals(g);</span><br><span class="line">  <span class="comment">/* traverse objects cautch by write barrier and by 'remarkupvals' */</span></span><br><span class="line">  propagateall(g);</span><br><span class="line">  <span class="comment">/* remark weak tables */</span></span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">gray</span> = g-&gt;</span>weak;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>weak = NULL;</span><br><span class="line">  <span class="function"><span class="title">lua_assert</span>(!iswhite(obj2gco(g-&gt;</span>mainthread)));</span><br><span class="line">  markobject(g, L);  <span class="comment">/* mark running thread */</span></span><br><span class="line">  markmt(g);  <span class="comment">/* mark basic metatables (again) */</span></span><br><span class="line">  propagateall(g);</span><br><span class="line">  <span class="comment">/* remark gray again */</span></span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">gray</span> = g-&gt;</span>grayagain;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>grayagain = NULL;</span><br><span class="line">  propagateall(g);</span><br><span class="line">  udsize = luaC_separateudata(L, <span class="number">0</span>);  <span class="comment">/* separate userdata to be finalized */</span></span><br><span class="line">  marktmu(g);  <span class="comment">/* mark `preserved' userdata */</span></span><br><span class="line">  udsize += propagateall(g);  <span class="comment">/* remark, to propagate `preserveness' */</span></span><br><span class="line">  <span class="function"><span class="title">cleartable</span>(g-&gt;</span>weak);  <span class="comment">/* remove collected objects from weak tables */</span></span><br><span class="line">  <span class="comment">/* flip current white */</span></span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>currentwhite = cast_byte(otherwhite(g));</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>sweepstrgc = <span class="number">0</span>;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">sweepgc</span> = &amp;g-&gt;</span>rootgc;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span>gcstate = GCSsweepstring;</span><br><span class="line">  <span class="function"><span class="title">g</span>-&gt;</span><span class="function"><span class="title">estimate</span> = g-&gt;</span>totalbytes - udsize;  <span class="comment">/* first estimate */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代码中注释的很详细。propagateall 是用来迭代所有的灰色节点的。所以每个关键步骤结束后，都需要调用一下保证处理完所有可能需要标记的对象。第一个步骤是那些 open 的 TUPVAL ，在前面已经提及。然后是处理弱表。</p>
<p>弱表是个比较特殊的东西，这里，弱表需要反复 mark ，跟 write barrier 的行为有关，超出了今天的话题。write barrier 还会引起另一些对象再 mark ，即对 grayagain 链的处理。都留到明天再谈。</p>
<p>接下来处理 userdata 的部分，<code>luaC_separateudata</code> 之前已经解释过了。</p>
<p>所以的一切都标记完后，那些不被引用的对象就都被分离出来了。可以安全的清理所有尚被引用的弱表。cleartable 就是干的这件事情。实现没有什么好谈的，一目了然。这里也不列代码了。不过值得一提的是对可以清理的项的判定函数：iscleared 。见 lgc.c 的 329 行。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** The next function tells whether a key or value can be cleared from</span></span><br><span class="line"><span class="comment">** a weak table. Non-collectable objects are never removed from weak</span></span><br><span class="line"><span class="comment">** tables. Strings behave as `values', so are never removed too. for</span></span><br><span class="line"><span class="comment">** other objects: if really collected, cannot keep them; for userdata</span></span><br><span class="line"><span class="comment">** being finalized, keep them in keys, but not in values</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">iscleared</span> <span class="params">(<span class="keyword">const</span> TValue *o, <span class="keyword">int</span> iskey)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"is cleared %p,%d\n"</span>, o,iskey);</span><br><span class="line">  <span class="keyword">if</span> (!iscollectable(o)) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">if</span> (ttisstring(o)) &#123;</span><br><span class="line">    stringmark(rawtsvalue(o));  <span class="comment">/* strings are `values', so are never weak */</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> iswhite(gcvalue(o)) ||</span><br><span class="line">    (ttisuserdata(o) &amp;&amp; (!iskey &amp;&amp; isfinalized(uvalue(o))));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果你对 for userdata being finalized, keep them in keys, but not in values 这一句表示困惑的话。可以读读 <a href="http://lua-users.org/lists/lua-l/2009-03/msg00438.html" rel="external nofollow noopener noreferrer" target="_blank">Roberto 对此的解释</a> 。</p>
<p>和那些被清理掉的 userdata 一样。当 userdata 被标为白色，在下一个 GC 周期中，它便不再认为是 isfinalized 状态。对应弱表中的 k/v 就被真的被清除掉了。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>云风的Lua-GC解剖</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>云风的Lua-GC解剖</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua52内部实现-编译系统1-概述</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E7%BC%96%E8%AF%91%E7%B3%BB%E7%BB%9F1-%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8486463" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>Lua是一个轻量级高效率的语言。这种轻量级和高效率不仅体现在它本身虚拟机的运行效率上，而且也体现在他整个的编译系统的实现上。因为绝大多数的lua脚本需要运行期动态的加载编译，如果编译过程本身非常耗时，或者占用很多的内存，也同样会影响到整体的运行效率，使你感觉这个语言不够“动态”。正是因为编译系统实现的非常出色，我们在实际使用lua时基本感觉不到这个过程的存在。</p>
<p>要实现一个Lua的编译系统可能不是很困难，但要高效的实现，还是有一定挑战的。这需要一些精妙的设计和实现技巧，也值得我们花一些时间去一探究竟。</p>
<h1 id="输入与输出"><a href="#输入与输出" class="headerlink" title="输入与输出"></a>输入与输出</h1><p>编译系统的工作就是将符合语法规则的chunk转换成可运行的closure。要了解编译系统，首先要了解作为输入的chunk和最为输出的closure以及他们的对应关系。</p>
<p>说到closure，就不能不先说一下proto。closure对象是lua运行期一个函数的实例对象，我们在运行期调用的都是一个closure。而proto对象是lua内部代表一个closure原型的对象，有关函数的大部分信息都保存在这里。这些信息包括：</p>
<ul>
<li>指令列表：包含了函数编译后生成的虚拟机指令。</li>
<li>常量表：这个函数运行期需要的所有常量，在指令中，常量使用常量表id进行索引。</li>
<li>子proto表：所有内嵌于这个函数的proto列表，在OP_CLOSURE指令中的proto id就是索引的这个表。</li>
<li>局部变量描述：这个函数使用到的所有局部变量名称，以及生命期。由于所有的局部变量运行期都被转化成了寄存器id，所以这些信息只是debug使用。</li>
<li>Upvalue描述：设个函数所使用到的Upvalue的描述，用来在创建closure时初始化Upvalue。<br>每个closure都对应着自己的proto，而运行期一个proto可以产生多个closure来代表这个函数实例。</li>
</ul>
<p><img src="https://img-my.csdn.net/uploads/201301/18/1358486658_7085.png" alt></p>
<p>由此可见，closure是运行期的对象，与运行期关系更大；而与编译期相关的其实是proto对象，他才是编译过程真正需要生成的目标对象。</p>
<p>Chunk代表一段符合Lua的语法的代码。我们可以调用lua_load api，将一个chunk进行编译。lua_load根据当前chunk生成一个mainfunc proto，然后为这个proto创建一个closure放到当前的栈顶，等待接下来的执行。Chunk内部的每个function statement也都会生成一个对应的proto，保存在外层函数的子函数列表中。所有最外层的function statement的proto会被保存到mainfunc proto的子函数列表中。所以，整个编译过程会生成一个以mainfunc为根节点的proto树。</p>
<p><img src="https://img-my.csdn.net/uploads/201301/18/1358488840_6386.png" alt></p>
<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>按照功能划分，整个编译系统被划分成以下3个模块：</p>
<ul>
<li>词法分析模块llex.h .c</li>
<li>语法分析模块lparser.h .c</li>
<li>指令生成模块lcode.h .c<br>Lua并没有使用llex和yacc，而是使用完全手写的词法和语法分析器。使用手写分析器的原因首先是考虑到效率。并且yacc/bison本身生成的代码在可移植性上有一些问题，无发达到Lua高可移植性的设计目标。还有就是手写分析器可以在C stack上面分配编译过程中需要的数据对象，这个我们后面会讲到。对于一个chunk，Lua在对其分析的过程中直接生成最终的指令，没有多余的对源代码或语法结构的遍历。也就是说Lua对源代码进行一次遍历就生成最终结果。</li>
</ul>
<h1 id="词法分析"><a href="#词法分析" class="headerlink" title="词法分析"></a>词法分析</h1><p>Lua的词法分析模块比较简单而且独立，就是将源代码拆分成一个个token，提供给语法分析使用。语法分析程序会调用luaX_next来获取下一个单词，然后进行语法分析。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> &#123;</span><br><span class="line">  lua_Number r;</span><br><span class="line">  TString *ts;</span><br><span class="line">&#125; SemInfo;  <span class="comment">/* semantics information */</span></span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Token</span> &#123;</span></span><br><span class="line">  <span class="keyword">int</span> token;</span><br><span class="line">  SemInfo seminfo;</span><br><span class="line">&#125; Token;</span><br></pre></td></tr></table></figure></p>
<p>Token用来表示一个单词，它包括类型token和语义seminfo。类型使用一个int来表示，既可以是一个字符，也可以是一个enum RESERVED。enum RESERVED从257开始，就是为了留给字符使用。如果类型是一个TK_NUMBER，seminfo.r就用来表示这个数字；如果是TK_NAME或者TK_STRING，seminfo.ts就表示对应的字符串。<br>LexState结构体的用途其实与其名称不是很贴切。LexState不仅用于保存当前的词法分析状态信息，而且也保存了整个编译系统的全局状态，这个我们在后面会讲到。</p>
<h1 id="语法分析和指令生成"><a href="#语法分析和指令生成" class="headerlink" title="语法分析和指令生成"></a>语法分析和指令生成</h1><p>语法分析器是整个编译过程的驱动器。通过对luaY_parser函数的调用，启动整个编译过程。语法分析采用“递归下降”的方法，从词法分析器中读取下一个token，然后根据这个token和lua的语法规则，将高层的语法规则分解成底层的语法规则，进一步进行分析。根据语法，“递归”在lua语法中只出现在两个地方，一个是statement函数，一个是subexpr函数。这也就是在这两个函数中调用enterlevel和leaveleavel，对当前调用深度进行检测的原因。如果递归太深，编译会报错。在分析的过程中，词法分析器会调用指令生成器，直接生成最终的指令。<br>从宏观上讲，整个编译过程就是生成proto tree的过程。语法分析器从mainfunc出发，开始分析和生成mainfunc的proto。在生成一个proto的过程中，生成的指令直接保存到proto的指令列表中。当遇到function statement或者local function statement时，首先生成子函数的proto，然后回来继续。通过这样的遍历方式，最终构建出一个proto tree。</p>
<p>在编译过程中，使用FuncState结构体来保存一个函数编译的状态数据。每个FuncState都有一个prev变量用来引用外围函数的FuncState，使当前所有没有分析完成的FuncState形成一个栈结构。栈底是mainfunc的FuncState，栈顶是当前正在分析的FuncState。每当开始分析一个新的函数时，会创建一个新的FuncState与之对应，将当前的FuncState保存在新的FuncState的prev中，并将当前的FuncState指向新的FuncState，这相当于压栈(open_func)。等待这个新函数分析完成后，当前的FuncState就没用了，将当前的FuncState恢复成原来的FuncState，这相当于弹栈(close_func)。</p>
<p>Dyndata是一个全局数据，他本身也是一个栈。对应上面的FuncState栈，Dyndata保存了每个FuncState对应的局部变量描述列表，goto列表和label列表。这些数据会跟着当前FuncState进行压栈和弹栈。</p>
<p>前面说过，整个编译系统的全局状态都保存在LexState中。LexState中与全局状态相关的主要是两个变量。fs指向当前正在编译的函数的FuncState。而dyd则指向全局的Dyndata数据。</p>
<p><img src="https://img-my.csdn.net/uploads/201301/18/1358500685_9493.png" alt></p>
<p>FuncState本身通过f保存对于Proto的引用。所有过程中生成的指令都直接保存到Proto的指令列表中。FuncState还通过h引用到一个table，用于在编译过程中生成proto的常量表。这个table使用常量值作为key，常量的id作为value。当编译过程中发现一个常量时，会首先使用这个常量值在这个表中查找。如果找到，说明前面已经将这个常量加入到常量表了，直接使用其id。否者，向常量表中添加一个常量，并相应的添加到这个表中。</p>
<p>对于一个FuncState本身的分析也是有层次关系的。一个函数本身使用block来控制局部变量的有效范围。函数本身就是一个block，里面的想while statement，for statement等等，还会形成子block。函数内的这些block会形成一个以函数本身block为根节点的block tree。Lua使用BlockCnt来保存一个block的数据。与FuncState的分析方法类似，BlockCnt使用一个previous变量保存外围block的引用，形成一个栈结构。enterblock和leaveblock函数负责压栈和弹栈。在FuncState中，bl用来指向当前的block。</p>
<p><img src="https://img-my.csdn.net/uploads/201301/18/1358503549_4311.png" alt></p>
<p>纵观整个语法分析过程，Lua其实就是按照深度优先的顺序，遍历了FuncState tree，以及子结构BlockCnt tree。在遍历的过程中，所有的编译状态(FuncState,BlockCnt)都是在C stack中保存，这就表示只保存当前正在处理的编译状态，而那些已经处理完成的在弹栈时被丢弃。在分析具体的语法结构时也是如此，Lua并被有完整的构建一个语法树对象，而是将过程中的语法结构保存在函数栈中，分析完立刻丢弃。所以就算用Lua来分析一个很大的程序，也不会占用过多的内存。不过，这也为指令生成带来了很多麻烦。比如向后跳转，在分析过程中还无法知道具体的跳转地址。如果构建了完整的语法树，就可以在最后去解决这些未决的地址。Lua使用了一些技巧来解决这些问题，我会在后面的文章中详细讲述。</p>
<p>在C stack中保存编译状态数据还有一个原因，是和编译系统的报错机制相关。编译系统整体的报错机制采用与虚拟机运行期一致的异常处理机制，也就是longjump。当出错时，直接跳出到最外层进行处理，此时所有当前的编译状态数据要能自动销毁掉。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua52内部实现-编译系统3-表达式</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E7%BC%96%E8%AF%91%E7%B3%BB%E7%BB%9F3-%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<p><a href="http://blog.csdn.net/yuanlin2008/article/details/8516325" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>表达式(expression)在编程语言中代表一个可以返回值的语法单位，比如常量表达式，变量表达式，函数调用表达式，算术、关系和逻辑表达式等等。对于函数式编程语言来说，几乎所有的语句都是表达式，可以被估值。而对于命令式语言，一般会将语句分成表达式和陈述语句(statement)。表达式可以被估值，而普通的陈述语句用来执行命令。根据具体的语法，这两种类型不一定会有明确的界限。比如在C中，a = b既是一个用来赋值的陈述语句，又是一个表达式，而作为表达式的结果是最终的a值。所以，像c = a = b这样的语句是成立的，意思是将a = b作为表达式，并将值赋给c。</p>
<p>而在Lua中，表达式的描述要明确的多。a = b属于一个赋值statement，而不属于表达式，所以c = a = b会产生语法错误。唯一即可以当作expression又可以当作statement使用的就是call。call本身会调用函数，返回函数的返回值，而作为statement时，返回值被忽略。</p>
<p>根据Lua5.2完整的BNF，我们可以看到Lua中仅有以下地方需要使用表达式：</p>
<ul>
<li>变量赋值，等号左边必须是一个变量表达式，右边是一个任意表达式</li>
<li>局部变量的初始化，等号右边是任意表达式</li>
<li>if statement的条件表达式和循环的条件表达式<br>在需要表达式的地方，通过调用expr函数，并传入一个expdesc结构体对象，对表达式进行解析。表达式的解析是一个递归下降的过程。下降分析将高层的表达式分解成底层表达式或表达式的组合，而递归则发生在expr函数的递归调用上，也就是说在解析过程中还会用表达式本身来描述高层表达式。当解析到BNF的终结符时，会返回上一层处理，然后再一层层的处理后返回。expr函数最终会填充传入的expdesc结构体，作为最高层的根表达式，交给更高层的语义，也就是上面需要表达式的地方进行处理。<br>Lua关于递归下降分析的每个函数的注释中都有代表这个函数的BNF范式，我们可以很容易的浏览这些代码，不需要过多的解释。真正需要理解的是表达式与指令生成相关的部分，这也是整个Lua编译系统里面比较晦涩的地方。我们可以首先通过一个简单的例子，在宏观上了解一下语法分析和指令生成的全过程。</li>
</ul>
<p>对于下面的chunk<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">c = a.b + <span class="number">1</span></span><br></pre></td></tr></table></figure></p>
<p>我们最终可以生成如下指令<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">main &lt;test.lua:0,0&gt; (5 instructions at 0x80048eb8)</span><br><span class="line">0+ params, 2 slots, 1 upvalue, 0 locals, 4 constants, 0 <span class="built_in">functions</span></span><br><span class="line">        1       [1]     GETTABUP        0 0 -2  ; _ENV <span class="string">"a"</span></span><br><span class="line">        2       [1]     GETTABLE        0 0 -3  ; <span class="string">"b"</span></span><br><span class="line">        3       [1]     ADD             0 0 -4  ; - 1</span><br><span class="line">        4       [1]     SETTABUP        0 -1 0  ; _ENV <span class="string">"c"</span></span><br><span class="line">        5       [1]     RETURN          0 1</span><br><span class="line">constants (4) <span class="keyword">for</span> 0x80048eb8:</span><br><span class="line">        1       <span class="string">"c"</span></span><br><span class="line">        2       <span class="string">"a"</span></span><br><span class="line">        3       <span class="string">"b"</span></span><br><span class="line">        4       1</span><br><span class="line">locals (0) <span class="keyword">for</span> 0x80048eb8:</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0x80048eb8:</span><br><span class="line">        0       _ENV    1       0</span><br></pre></td></tr></table></figure></p>
<p>整个的递归下降语法分析过程可以用下图表示。<br><img src="https://img-my.csdn.net/uploads/201301/22/1358847194_3500.png" alt></p>
<p>由于我们目前需要讲解的是表达式，这里为了讲解方便，这里省略了一些过程。接下来我们对这些步骤逐一进行解说。</p>
<ol>
<li>exprstat函数调用suffixedexp函数，对赋值语句的左边的后缀表达式进行分析。</li>
<li>这里没有展开suffixedexp函数，我们目前只需要知道它会返回一个VINDEXED表达式。</li>
<li>exprstat调用expr函数，对赋值右面的表达式进行分析。如上所述，expr函数是解析表达式的总入口，他接受一个expdesc结构体，开始分析。</li>
<li>expr调用subexpr</li>
<li>subexpr函数首先调用simpleexp，来分析“+”号左边的表达式。</li>
<li>simpleexp调用suffixedexp函数，将这个表达式当成后缀表达式开始分析。</li>
<li>suffixedexp函数首先调用primaryexp函数，分析主表达式，也就是a。</li>
<li>primaryexp调用singlevar函数，将a当作一个变量进行分析。</li>
<li>singlevar没有找到名字为”a”的局部变量或upvalue，将”a”当作全局变量处理，也就是将”a”变成“_ENV.a”来处理。这里已经到了递归下降分析的最低端，最终创建一个VINDEXED的表达式给上层，table为upvalue “_ENV”，key为常量”a“。</li>
<li>继续返回VINDEXED表达式给上层。</li>
<li>suffixedexp将这个VINDEXED表达式传给fieldsel，对后缀进行分析。</li>
<li>fieldsel首先根据这个VINDEXED表达式的table和key生成指令1，这个指令的目标寄存器为临时分配的寄存器0。然后以寄存器0为table，”b“为key，生成一个新的VINDEXED表达式返回给上层。</li>
<li>继续返回VINDEXED表达式给上层。</li>
<li>继续返回VINDEXED表达式给上层。</li>
<li>subexp调用subexp本身，开始对”+“号右边的表达式进行分析。</li>
<li>subexp调用simpleexp，分析这个”1“。</li>
<li>simpleexp为这个”1”生成一个VKNUM表达式，返回给上层。</li>
<li>继续返回VKNUM表达式给上层。</li>
<li>subexp首先根据+号左边的VINDEXED表达式的table和key生成指令2，这个指令的目标寄存器为临时分配的寄存器0。然后生成指令3的加法运算，操作数为寄存器0和VNUM表达式对应的常量id。指令3的目标寄存器还不能确定，所以创建一个VRELOCABLE表达式返回给上层。</li>
<li>这时整个表达式已经解析完毕，返回VRELOCABLE表达式给上层，等待进一步的处理。</li>
<li>将VRELOCABLE表达式对应的指令3的目标寄存器回填成临时分配的寄存器0，然后将寄存器0的内容赋值给左边的VINDEXED表达式，也就是生成指令4。</li>
</ol>
<p>通过上面的分析过程我们可以看到，Lua整体的语法分析过程就是对语法树的一次性的先续遍历的过程。对于表达式的分析，首先要分析子表达式，并为其生成指令来获取表达式的值，存入临时寄存器，然后父表达式再使用子表达式的分析结果和临时寄存器作为参数，来生成获取值的指令。所有在过程中使用的子表达式的expdesc结构体对象全部在函数的调用栈上分配，待分析完成返回后，就被丢弃掉了。由于Lua本身的指令是基于寄存器的，一条指令所能完成的任务相对比较复杂，所以有些情况下在子表达式分析过程中不能完全获得所需要的信息。这是就需要将表达式分析所得的信息返回给上一层父表达式，也就是子表达式的使用者，由上一层做最终的指令生成。或者先生成子表达式指令，然后在上一层分析中进行指令的回填修改。我们在上例中就可以清晰地看到这种情况。<br>在《虚拟机指令》中我们提到过，Lua使用的是register based vm，所以相对于stack based vm来说，整个编译和指令生成过程要更复杂。寄存器在Lua中的第一个用处就是存储局部变量的值，所有局部变量在编译后，都不再使用名称，而是寄存器id进行访问。而另一个用处就是存储表达式估值过程中的临时值。当对一个表达式进行估值时，可能先要对其子表达式进行估值，将估值结果存储到一个临时的寄存器，然后使用这个结果再进行下一步的估值计算。寄存器为一个id从0开始的数组。在编译过程中，Lua使用FuncState中的freereg变量记录当前空闲寄存器的起始id。在开始编译一个FuncState时，freereg被设置成0，表示所有寄存器都可以被分配。当遇到一个局部变量或者临时值时，就分配出一个id为当前freereg的寄存器，然后将freereg++。局部变量会在语法域内一直占用这个寄存器，而临时值会在使用完其值后立即被释放，也就是freereg–。由于临时值会在表达式估值完成后全部释放掉，所以局部变量被分配的寄存器肯定是从0开始并且是连续的，中间不会被临时值占用。</p>
<p>总的来说，局部变量与临时值没有什么本质区别，都是用来存放函数计算过程中表达式的值得，唯一区别就在于临时值不占用寄存器，而局部变量会一直占用寄存器，并且可以被程序访问。</p>
<p>上面的例子中，12，19和21步中都需要临时寄存器的分配。我们看到在需要临时寄存器的指令生成之后，临时寄存器就被被释放掉了，所以每次分配时都会将寄存器0分配给临时值使用，而不会一直占用寄存器0。</p>
<p>在后面的文章中，我将会按照分类对表达式进行详细的讲解。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>探索Lua52内部实现-编译系统4-表达式分类</title>
    <url>/lua/%E6%8E%A2%E7%B4%A2Lua52%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0-%E7%BC%96%E8%AF%91%E7%B3%BB%E7%BB%9F4-%E8%A1%A8%E8%BE%BE%E5%BC%8F%E5%88%86%E7%B1%BB/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/yuanlin2008/article/details/8534583" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h1 id="常量表达式"><a href="#常量表达式" class="headerlink" title="常量表达式"></a>常量表达式</h1><p>常量表达式在Lua用来表示”nil”，“true”，“false”，字符串和数字的值。在BNF中常量表达式属于终结符，也就是语法解析的最底端，在simpleexp函数中被解析出来，并创建对应类型的expdesc对象。VNIL，VTRUE和VFALSE这三个类型本身就对应3个固定的值，没有什么额外的数据。VKNUM类型代表数字常量，需要在nval中存放从词法分析中得到的lua_Number。VK类型用来表示一个通常意义上的常量表达式，使用info来存储他所代表的常量值在常量表中的id。字符串常量就被直接创建成VK类型，然后将其对应的字符串值保存到常量表中，并将id保存到info中。<br>由于常量表达式的值是一个常量，所以本身不需要生成任何用于估值计算的指令，完全为高层语义的指令生成提供服务。当高层语义要将常量装入一个寄存器时，比如local a＝”foo”，会调用discharge2reg函数，生成OP_LOADK指令。Lua中的很多指令都可以直接使用常量作为操作数，比如算数指令。当高层语义要将常量当作其他指令的参数时，会调用luaK_exp2RK函数，返回这个常量对应的id。</p>
<p>其实Lua本身使用VK一种类型就可以表示所有的常量表达式，而其他类型常量表达式完全是为了优化才使用的。VNIL和VTRUE，VFALSE类型分别都有对应OP_LOADNIL和OP_LOADBOOL指令将常量值装入寄存器，所以不需要将其放到常量表中。我们在《虚拟机指令》中讲过这些指令的特殊用法。而VNUM的作用是支持算数运算的常量优化(constfolding)，如果被优化掉了，也就不需要在常量表中出现。VNUM会在discharge2reg的时候，也就是真正需要使用其常量值得时候，才被放到常量表中。当VNIL，VTRUE，VFALSE和VNUM需要被用在其他指令的参数时，luaK_exp2RK将其全部转化为VM类型，并在常量表中创建对应的常量值。VNIL，VTRUE和VFALSE还会在逻辑表达式中被用于优化。</p>
<h1 id="变量表达式"><a href="#变量表达式" class="headerlink" title="变量表达式"></a>变量表达式</h1><p>VLOCAL代表局部变量表达式，在info中保存局部变量对应的寄存器id。<br>VUPVAL代表upvalue变量表达式，在info中保存upvalue的id。</p>
<p>VINDEXED代表对一个表进行索引的变量表达式，比如a.b或者a[1]，使用ind结构体保存数据。idx保存用来索引的key的id，他可能是一个寄存器id或者常量id；t保存被索引表的id，他可能是一个寄存器id或者upvalue id；vt表示t的类型是寄存器id(VLOCAL)还是upvalue id(VUPVAL)。</p>
<p>singlevar函数用来解析一个变量表达式。singlevar调用函数singlevaraux，来查找变量名对应的表达式类型。singlevaraux首先在当前FuncState的局部变量中查找。如果找到，就创建一个VLOCAL表达式。否则，就递归向上查找外围函数的upvalue，并最终返回一个VUPVAL表达式。这个查找的过程也是创建upvalue的过程。当在外围函数中找到对应变量名的局部变量后，会在这个外围函数的所有内嵌函数中创建对应的upvalue。如下图所示：<br><img src="https://img-my.csdn.net/uploads/201301/24/1358999040_2961.png" alt></p>
<p>函数func(N)内嵌于func(N-1)。当func3使用了变量a，并在func0中找到了局部变量a时，会在func1~3中创建a对应的upvalue。func1中的upvalue是对上一级中的局部变量的直接引用，所以isstack为1，idx代表局部变量的寄存器id。其他的都是对上一级upvalue的引用，所以isstack为0，idx代表上一级upvalue的id。</p>
<p>如果singlevaraux最终没有能找到符合变量名的局部变量或者upvalue，singlevar函数就将这个变量名当作全局变量进行处理。首先singlevar会再次调用singlevaraux，查找名称为“_ENV”的upvalue。这个upvalue已经在最外层的mainfunc中创建了，所以一定能找到。然后将_ENV对应的upvalue变量表达式当作table，用变量名当作key，通过luaK_indexed函数创建一个VINDEXED表达式。这个操作等于将varname转化为_ENV.varname进行访问。</p>
<p>变量表达式的估值计算是从变量中获取值。luaK_dischargevars函数为变量表达式生成估值计算的指令。对于VLOCAL类型，值就存在于局部变量对应的寄存器中，不需要生成任何获取指令，也不需要分配寄存器来存储临时值。VLOCAL被转化为VNONRELOC类型，代表已经为这个表达式生成了指令，并且也分配了寄存器保存这个值。对于VUPVAL类型，需要产生指令OP_GETUPVAL来获取其值。而对于VINDEXED类型，根据vt的不同，需要产生OP_GETTABLE或者OP_GETTABUP指令来获取其值。VUPVAL和VINDEXED都被转化为VRELOCABLE类型，表示获取指令已经生成，但是指令的目标寄存器(A)还没有确定，等待回填。回填后，VRELOCABLE类型会转化成VNONRELOC类型。</p>
<p>变量表达式除了用来获取变量值，还有另外一个用途，就是在赋值语句中当作赋值的目标，也就是将其他表达式的值存储到这个变量表达式中。这个工作是由luaK_storevar函数完成的。luaK_storevar根据被赋值的变量表达式的不同类型，生成不同的赋值指令。对于VLOCAL，不需要额外的指令，只需要将赋值表达式的目标寄存器回填成局部变量对应的寄存器就可以了。对于VUPVAL，需要生成OP_SETUPVAL指令。而对于VINDEXED，则需要生成OP_SETTABLE或者OP_SETTABUP指令。</p>
<h1 id="多返回值表达式"><a href="#多返回值表达式" class="headerlink" title="多返回值表达式"></a>多返回值表达式</h1><p>VCALL表达式对应着一个函数调用的OP_CALL指令。VVARARG表达式对应”…”，也就是OP_VARARG指令。他们都可以有多个返回值。在需要单值的上下文中，通过调用luaK_setoneret函数，将表达式设置成单返回值。VCALL的返回值位置是固定的，就是用来存放被调用closure的寄存器，所以被转化为VNONRELOC类型。VVARARG的目标寄存器待定，被转化为VRELOCABLE类型。在需要多个返回值的上下文中，通过调用luaK_setreturns函数，回填指令中的返回值数量。</p>
<h1 id="算数表达式"><a href="#算数表达式" class="headerlink" title="算数表达式"></a>算数表达式</h1><p>所有关于操作符的解析都在subexpr函数中进行，这里处理一元和二元操作符以及优先级关系。对于一元操作符，会调用luaK_prefix生成代码。对于二元操作符，会首先调用luaK_infix处理第一个前面的表达式，然后分析出后面的表达式，再调用luaK_posfix对这两个表达式进行处理。<br>算术表达式最终是在codearith函数中创建的。首先，codearith会调用constfolding函数，尝试优化。如果两个被操作表达式都是数字常量，就直接计算出结果赋给第一个常量表达式。如果不能被优化，codearith函数直接为算术表达式生成对应的算数指令，并且将表达式类型设置成VRELOCABLE，等待回填生成指令的目标寄存器。</p>
<p>关# 系表达式 #<br>关系表达式(&gt;,&gt;=,&lt;,&lt;=,==,~=)在遵循传统意义上应该对两个待比较的表达式的关系进行评估，然后将评估结果的boolean值作为表达式的估值，提供给高层使用。而Lua并没有这样处理，而是将传统的语义转化成一个条件分支，直接代表着“这个表达式为true或false时应该做些什么”。这样的转换完全是为了执行效率。在实际使用中，关系表达式大多数被用在条件跳转判断中，而不是赋值。这样实现可以直接连接后面的分支，省了很多不必要的步骤。<br>关系表达式在codecomp函数中创建。首先，将需要比较的两个子表达式取出目标寄存器或者常量id。然后通过调用condjump函数，为表达式生成指令，并将表达式设置成VJMP类型。condjump函数会生成一个测试指令和一个跳转指令OP_JMP。测试指令包括OP_EQ，OP_LT，OP_LE。这些测试指令会比较两个表达式的值。如果满足测试条件，就继续执行；否则，就跳过下一条指令执行。测试指令与后面的OP_JMP指令配合到一起，就形成了一个条件跳转。这样的表示方法也是Lua中条件跳转的唯一表示方法。条件跳转形成了两个执行分支：</p>
<ul>
<li>当测试条件满足，就跳转到一个现在还未知的位置，也就是true分支。</li>
<li>当测试条件不满足，就继续运行OP_JMP后面的指令，也就是false分支。<br><img src="https://img-my.csdn.net/uploads/201301/25/1359093780_9593.png" alt><br>这个OP_JMP指令的位置会被保存到表达式的info中，等待后继的回填处理。</li>
</ul>
<h1 id="逻辑表达式"><a href="#逻辑表达式" class="headerlink" title="逻辑表达式"></a>逻辑表达式</h1><p>逻辑表达式(and,or)的处理是最复杂的。表达式e1 and e2的语义是：如果e1为true，整个表达式的值为e2；否则整个表达式的值为e1。表达式e1 or e2的语义与and相反：如果e1为false，整个表达式的值为e1；否则整个表达式的值为e2。所以，逻辑表达式其实是两个待评估表达式的二选一。<br>首先要先说明一下expdesc结构体中的t和f变量。这两个变量实际上是两个OP_JMP指令的链表(关于跳转链表，在这里已经讲过)，我们称之为patch list。由于关系表达式和逻辑表达式的特殊处理方式，这两个patch list代表本表达式被评估为true或者false时的跳出指令列表。通过将一个地址回填给patch list，就将对这个表达式的评估直接引导到对应的执行分支。任何类型的表达式都可能带有patch list。如果有patch list，说明这个表达式本身或者子表达式使用了关系或者逻辑表达式。</p>
<p>patch list中的元素是在luaK_goiftrue和luaK_goiffalse函数中被添加到patch list中的。luaK_goiftrue函数调用jumponcond生成一个OP_TESTSET和一个OP_JMP指令。与关系表达式的处理类似，这两个指令形成了两个执行分支：向下执行的true分支和跳出的false分支。这个跳出的OP_JMP指令会被连接到表达式的f中，代表表达式为false时执行的跳出，并等待回填。如果表达式有t存在，调用luaK_patchtohere，将所有true跳出回填到下面继续执行的代码。这代表了除了本表达式评估为true会继续执行外，所有该表达式被评估为true的跳出也应该跳转到此处继续执行。如果表达式是一个关系表达式，也就是VJMP类型，其本身就是一个逻辑跳转，直接将其info中指向的OP_JMP指令连接到f中，而不需要为其生成OP_TESTSET指令了。luaK_goiffalse与luaK_goifture相反，当表达式为true时跳出。</p>
<p>对于关系表达式e1 and e2，首先在luaK_infix中为e1调用luaK_goiftrue，生成对e1的测试跳转指令，这就代表如果e1为true，继续执行；否则跳出。此时e1中的f中已经包含了e1的false跳出。然后解析e2，并在luaK_posfix中，将e1的f串接到e2的f上，并将e2作为作为整个表达式解析返回给高层的表达式。<br><img src="https://img-my.csdn.net/uploads/201301/28/1359370396_6863.png" alt><br>对于or的处理，与and类似，这里不再累述。</p>
<p>OP_TESTSET与其他测试指令类似，唯一的特殊点在于OP_TESTSET在执行跳转时会将被评估的表达式的值赋给一个未知的寄存器。这个操作时专门针对逻辑表达式的语义设计的。如果将逻辑表达式e1 and e2的值赋值给一个变量，那么这个变量的值并不是true或者false，而是e1或者e2。所以，在跳转时，说明此跳转已经取了e1的值，要先做一个对未知寄存器的赋值，然后等待回填这个寄存器。</p>
<h1 id="表达式的用途"><a href="#表达式的用途" class="headerlink" title="表达式的用途"></a>表达式的用途</h1><p>根据上面描述，表达式在Lua中本质上有两种用途。<br>首先是被当作跳转条件使用。在if，while等结构中，都需要一个表达式来代表跳转条件。在当作条状条件处理时，Lua会使用上面讲过的luaK_goiftrue或者luaK_goiffalse，来评估一个表达式。此时表达式本身的值已经没有意义，只需要将t或者f回填成分支起始指令未知就搞定了。如果t或f中有OP_TESTSET指令，会被替换成OP_TEST指令。</p>
<p>其次是将表达式的值保存到寄存器中，比如给变量赋值。由于上面关系和逻辑表达式的特殊处理，表达式的值已经不仅仅是对本身的估值，还要考虑到patch list所对应的值。我们可以将patch list中的条件跳转分为两种。使用OP_TESTSET作为条件指令的，也就是关系表达式产生的条件跳转，我们称之为赋值条件跳转TS。上面讲过，如果走到这个分支，表达式的最终值就是OP_TESTSET所设置的值。其他所有不使用OP_TESTSET的测试指令，也就是关系表达式所产生的跳转，我们称之为非赋值条件跳转T。分条件跳转最后应该产生true或者false值。</p>
<p><img src="https://img-my.csdn.net/uploads/201301/29/1359425663_8771.png" alt><br>所以，表达式的值最终会由很多的分支导致很多的值。将表达式的值赋值给寄存器的操作，在exp2reg函数中完成。要将这些值赋给一个寄存器，首先调用discharge2reg，将自己的值存入指定寄存器，然后处理patch list。对于非赋值条件跳转，我们需要为true和false值各补上一个OP_LOADBOOL指令作为赋值操作，并将t中的所有T回填到true，将f中所有的T回填到false。而对于赋值条件跳转，不仅要将跳转回填到赋值的最后，并且需要回填所有OP_TESTSET指令的目标寄存器，也就是将这些值赋值给指定寄存器。patchlistaux (FuncState *fs, int list, int vtarget, int reg, int dtarget)函数就是回填patch list用的。此函数会遍历patchlist上的每个条件跳转，如果是OP_TESTSET，就回填寄存器reg，并回填跳转地址vtarget，否则，回填跳转地址dtarget。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>探索Lua52内部实现</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>探索Lua52内部实现</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part10：userdata</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part10/</url>
    <content><![CDATA[<p><a href="http://manistein.club/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="构建Lua解释器Part10：userdata"><a href="#构建Lua解释器Part10：userdata" class="headerlink" title="构建Lua解释器Part10：userdata"></a>构建Lua解释器Part10：userdata</h1><h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>​    本章节，我开始对<a href="https://github.com/Manistein/dummylua-tutorial" rel="external nofollow noopener noreferrer" target="_blank">dummylua</a>的userdata的设计与实现，进行论述。它的大体设计与实现，仍然是仿照了lua5.3的标准，由于，所有的内容，都是我自己理解后，重新实现，因此在一些实现细节上略有不同，但是整体设计思路遵循了lua的设计思想。本章的篇幅不会很长，因为userdata这个部分并不是非常复杂，因此我这里也会速战速决，将userdata的一些核心思想论述清楚，就将本章完结。</p>
<h1 id="userdata的数据结构"><a href="#userdata的数据结构" class="headerlink" title="userdata的数据结构"></a>userdata的数据结构</h1><p>​    userdata是用来存放，用户自定义的数据结构实例的，userdata的种类有两种，一种是lightuserdata，还有一种则是full userdata。light userdata是Value结构中的一个变量类型，本质是一个void* 指针</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> lua_Value &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gc</span>;</span></span><br><span class="line">    <span class="keyword">void</span>* p;</span><br><span class="line">    <span class="keyword">int</span> b;</span><br><span class="line">    lua_Integer i;</span><br><span class="line">    lua_Number n;</span><br><span class="line">    lua_CFunction f;</span><br><span class="line">&#125; Value;</span><br></pre></td></tr></table></figure>
<p>light userdata的内存，需要用户自行管理，而full userdata则是通过lua的gc机制进行管理，我们本章论述的也正是full userdata。后文中，所有指代userdata的地方，均是指full userdata。对full userdata的操作实现，一般是在c层进行的，后面会有使用的例子。<br>    首先我们要看的是userdata的数据结构，在dummylua中，它的定义如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CommonHeader struct GCObject* next; lu_byte tt_; lu_byte marked</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Udata</span> &#123;</span></span><br><span class="line">	CommonHeader;  			<span class="comment">// GC公共头部</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">Table</span>* <span class="title">metatable</span>;</span><span class="comment">// userdata可以设置metatable，一般用于设置__gc域，在userdata被回收之前，</span></span><br><span class="line">							<span class="comment">// __gc函数会被调用，一般用于回收系统资源等</span></span><br><span class="line">	<span class="keyword">int</span> ttuv_;				<span class="comment">// 相当于TValue的tt_，用来指代user_变量的类型</span></span><br><span class="line">	<span class="keyword">int</span> len;				<span class="comment">// 自定义域的大小</span></span><br><span class="line">	Value user_;			<span class="comment">// 本质就是TValue的value_部分，这里将TValue拆成了两个部分</span></span><br><span class="line">&#125; Udata;</span><br></pre></td></tr></table></figure>
<p>注释，对每个字段有了一个大致的说明，后续内容，会对他们进行较为详细的解释和说明。</p>
<h1 id="userdata的接口"><a href="#userdata的接口" class="headerlink" title="userdata的接口"></a>userdata的接口</h1><p>​    上一小节，介绍了userdata的基本数据结构，本小节则会介绍userdata相关的主要接口，分别是luaS_newuserdata，getudatamem，setuservalue，getuservalue。我们先来看一下luaS_newuserdata的定义：</p>
<figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastring.c</span></span><br><span class="line"><span class="function">Udata* <span class="title">luaS_newuserdata</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> <span class="built_in">size</span>)</span></span>;</span><br></pre></td></tr></table></figure>
<p>这个接口，做了什么事情呢？就是创建了一个userdata实例，这个userdata实例的内存大小，就是Udata头部+传入的size大小，我们可以看一下userdata实例的构成：</p>
<figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line"><span class="code">+------------+</span>----------------+</span><br><span class="line">|Header:Udata|   user domain  |</span><br><span class="line"><span class="code">+------------+</span>----------------+</span><br></pre></td></tr></table></figure>
<p>前面的Header，就是sizeof(Udata)的大小，而后面的user domain，则是luaS_newuserdata的第二个参数size来指定，比方，现在我们定义了一个Vector3的数据结构：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Vector3</span> &#123;</span></span><br><span class="line">	<span class="keyword">float</span> x;</span><br><span class="line">	<span class="keyword">float</span> y;</span><br><span class="line">	<span class="keyword">float</span> z;</span><br><span class="line">&#125; Vector3;</span><br></pre></td></tr></table></figure>
<p>现在我们要创建，这个Vector3关联的userdata，那么它的创建代码则如下所示：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">Udata* u = luaS_newuserdata(<span class="name">L</span>, sizeof(<span class="name">Vector3</span>))<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>第二个参数，size的大小则是sizeof(Vector3)的大小，也就是12，那么此时，user domain的大小则是12 bytes的大小。<br>    在完成了userdata实例的创建之后，我们要在c层，获取我们自定义的结构实例的指针，如何获取它呢，我们需要一个接口getudatamem</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line"><span class="selector-id">#define</span> <span class="selector-tag">getudatamem</span>(o) (cast(char*,o)+sizeof(Udata))</span><br></pre></td></tr></table></figure>
<p>这个宏的作用在，则是将userdata实例中，user domain部分的指针拿到，获取的结果所指向的位置，如下’^‘所示：</p>
<figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line"><span class="code">+------------+</span>----------------+</span><br><span class="line">|Header:Udata|   user domain  |</span><br><span class="line"><span class="code">+------------+</span>----------------+</span><br><span class="line"><span class="code">             ^</span></span><br></pre></td></tr></table></figure>
<p>通过这个宏，我们可以获取结构自定义结构变量的实例，具体方法如下所示：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">Vector3* v3 = (<span class="name">Vector3*</span>)getudatamem(<span class="name">u</span>)<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>接下来，我们就可以对v3所指向的内存块进行对应的处理了。<br>    userdata还有一个重要的接口，就是setuservalue，这是一个宏，它的作用是，将一个TValue实例，赋值到Udata头部中，分别将TValue实例的tt_赋值给Udata的ttuv_，将TValue的value_赋值给Udata的user_字段中，其定义如下所示：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line">#define setuservalue(u, o) \</span><br><span class="line">			(<span class="function"><span class="title">u</span>)-&gt;</span><span class="function"><span class="title">ttuv_</span> = (o)-&gt;</span><span class="function"><span class="title">tt_</span>; (u)-&gt;</span><span class="function"><span class="title">user_</span> = (o)-&gt;</span>value_</span><br></pre></td></tr></table></figure>
<p>同样的，获取它的接口如下所示：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line">#define getuservalue(u, o) \</span><br><span class="line">			(<span class="function"><span class="title">o</span>)-&gt;</span><span class="function"><span class="title">tt_</span> = (u)-&gt;</span><span class="function"><span class="title">ttuv_</span>; (o)-&gt;</span><span class="function"><span class="title">value_</span> = (u)-&gt;</span>user_</span><br></pre></td></tr></table></figure>
<p>到这里，我们就完成了userdata相关的接口的讨论了。</p>
<h1 id="userdata的gc处理"><a href="#userdata的gc处理" class="headerlink" title="userdata的gc处理"></a>userdata的gc处理</h1><p>​    关于lua的gc机制，我在Part2里已经有非常详细的论述了，在后面的章节中，我还会再出一篇，再次，进一步论述gc算法。有关gc算法的具体步骤，可以查看Part2，本小节，我将集中精力，梳理一下userdata的标记和清除阶段的逻辑处理。<br>​    userdata在标记阶段，其处理逻辑如下所示：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c</span></span><br><span class="line">void reallymarkobject(struct lua_State* L, struct GCObject* gco) &#123;</span><br><span class="line">    struct global_State* g = G(L);</span><br><span class="line">    white2gray(gco);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">switch</span>(gco-&gt;tt_) &#123;</span><br><span class="line">        <span class="keyword">case</span> LUA_TTHREAD:&#123;</span><br><span class="line">            linkgclist(gco2th(gco), g-&gt;gray);            </span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_TTABLE:&#123;</span><br><span class="line">            linkgclist(gco2tbl(gco), g-&gt;gray);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> LUA_TLCL:&#123;</span><br><span class="line">			linkgclist(gco2lclosure(gco), g-&gt;gray);</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> LUA_TCCL:&#123;</span><br><span class="line">			linkgclist(gco2cclosure(gco), g-&gt;gray);</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> LUA_TPROTO:&#123;</span><br><span class="line">			linkgclist(gco2proto(gco), g-&gt;gray);</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_SHRSTR:&#123; </span><br><span class="line">            gray2black(gco);</span><br><span class="line">            struct TString* ts = gco2ts(gco);</span><br><span class="line">            g-&gt;GCmemtrav += sizelstring(ts-&gt;shrlen);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_LNGSTR:&#123;</span><br><span class="line">            gray2black(gco);</span><br><span class="line">            struct TString* ts = gco2ts(gco);</span><br><span class="line">            g-&gt;GCmemtrav += sizelstring(ts-&gt;u.lnglen);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> LUA_TUSERDATA: &#123;</span><br><span class="line">			gray2black(gco);</span><br><span class="line"></span><br><span class="line">			TValue uvalue;</span><br><span class="line">			Udata* u = gco2u(gco);</span><br><span class="line">			getuservalue(u, &amp;uvalue);</span><br><span class="line">			<span class="keyword">if</span> (u-&gt;metatable) &#123;</span><br><span class="line">				markobject(L, u-&gt;metatable);</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (iscollectable(&amp;uvalue) &amp;&amp; iswhite(gcvalue(&amp;uvalue))) &#123;</span><br><span class="line">				reallymarkobject(L, gcvalue(&amp;uvalue));</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			g-&gt;GCmemtrav += sizeof(Udata);</span><br><span class="line">			g-&gt;GCmemtrav += u-&gt;len;</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:<span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们已知了，luaS_newuserdata接口，其实是在luastring模块里的，因为它的创建和使用逻辑和luastring非常类似，在标记阶段也是，userdata实例本身，在标记为灰色后，直接标记成黑色，并且将它的metatable(如果存在的话)标记为灰色，并且放入gray列表中，此外如果userdata的user域存在，且是一个gc实例，那么它也需要被标记为灰色，并且放入gray列表中。这里，我们需要注意几个问题，一个就是userdata在标记扫描阶段，直接整个被标记为黑色，并不会对user domain内部的任何域进行检查和处理。<br>    接下来，我们来看一下userdata的清除逻辑，如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> lu_mem <span class="title">freeobj</span><span class="params">(struct lua_State* L, struct GCObject* gco)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">switch</span>(gco-&gt;tt_) &#123;</span><br><span class="line">        <span class="keyword">case</span> LUA_SHRSTR: &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">gco2ts</span>(<span class="title">gco</span>);</span></span><br><span class="line">            luaS_remove(L, ts);</span><br><span class="line">            lu_mem sz = sizelstring(ts-&gt;shrlen);</span><br><span class="line">            luaM_free(L, ts, sz); </span><br><span class="line">            <span class="keyword">return</span> sz; </span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_LNGSTR: &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">gco2ts</span>(<span class="title">gco</span>);</span></span><br><span class="line">            lu_mem sz = sizelstring(ts-&gt;u.lnglen);</span><br><span class="line">            luaM_free(L, ts, sz);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_TTABLE: &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">Table</span>* <span class="title">tbl</span> = <span class="title">gco2tbl</span>(<span class="title">gco</span>);</span></span><br><span class="line">            lu_mem sz = <span class="keyword">sizeof</span>(struct Table) + tbl-&gt;arraysize * <span class="keyword">sizeof</span>(TValue) + twoto(tbl-&gt;lsizenode) * <span class="keyword">sizeof</span>(Node);</span><br><span class="line">            luaH_free(L, tbl);</span><br><span class="line">            <span class="keyword">return</span> sz;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> LUA_TTHREAD: &#123;</span><br><span class="line">			<span class="comment">// TODO</span></span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> LUA_TLCL: &#123;</span><br><span class="line">			<span class="class"><span class="keyword">struct</span> <span class="title">LClosure</span>* <span class="title">cl</span> = <span class="title">gco2lclosure</span>(<span class="title">gco</span>);</span></span><br><span class="line">			lu_mem sz = <span class="keyword">sizeof</span>(LClosure);</span><br><span class="line">			luaF_freeLclosure(L, cl);</span><br><span class="line">			<span class="keyword">return</span> sz;</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> LUA_TCCL: &#123;</span><br><span class="line">			<span class="class"><span class="keyword">struct</span> <span class="title">CClosure</span>* <span class="title">cc</span> = <span class="title">gco2cclosure</span>(<span class="title">gco</span>);</span></span><br><span class="line">			lu_mem sz = <span class="keyword">sizeof</span>(struct CClosure);</span><br><span class="line">			luaF_freeCclosure(L, cc);</span><br><span class="line">			<span class="keyword">return</span> sz;</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> LUA_TPROTO: &#123;</span><br><span class="line">			<span class="class"><span class="keyword">struct</span> <span class="title">Proto</span>* <span class="title">f</span> = <span class="title">gco2proto</span>(<span class="title">gco</span>);</span></span><br><span class="line">			lu_mem sz = luaF_sizeproto(L, f);</span><br><span class="line">			luaF_freeproto(L, f);</span><br><span class="line">			<span class="keyword">return</span> sz;</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> LUA_TUSERDATA: &#123;</span><br><span class="line">			Udata* u = gco2u(gco);</span><br><span class="line">			lu_mem sz = <span class="keyword">sizeof</span>(Udata) + u-&gt;len;</span><br><span class="line">			luaM_free(L, u, sz);</span><br><span class="line">			<span class="keyword">return</span> sz;</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:&#123;</span><br><span class="line">            lua_assert(<span class="number">0</span>);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们可以看case LUA_TUSERDATA的那部分逻辑，可以看到的是，userdata实例是整个被释放掉，未对userdata内部的user domian部分做任何的处理，也就是说如果user domain内部包含了堆内存实例的指针，这部分需要用户自己进行处理。</p>
<h1 id="userdata的user-domain域内部的堆内存清理"><a href="#userdata的user-domain域内部的堆内存清理" class="headerlink" title="userdata的user domain域内部的堆内存清理"></a>userdata的user domain域内部的堆内存清理</h1><p>​    前面，我们提到了，user domain域内，如果包含了指向堆内存的指针，那么这部分需要我们进行处理，需要怎么处理呢？lua的清除逻辑，并没有提供这样的机会，但是，我们前面说过，userdata有一个metatable域，为userdata设置一个metatable，并且这个metatable如果包含一个名为__gc的函数，那么在userdata被gc回收之前，会首先调用这个函数，我们来看一个伪代码，假设userdata的metatable是如下所示：</p>
<figure class="highlight clojure"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">	__gc = function(<span class="name">udata</span>) release(<span class="name">udata</span>) end</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么在udata实例，被gc回收之前，上面这个__gc函数会被调用，该函数的参数，就是userdata实例本身，release函数是用户自己在c层实现的函数，导出给lua层使用的，这个release函数，将在c层逻辑中，对udata的user domain域中，包含的堆内存实例进行释放操作，避免内存泄露。</p>
<h1 id="userdata的使用例子"><a href="#userdata的使用例子" class="headerlink" title="userdata的使用例子"></a>userdata的使用例子</h1><p>​    接下来，我们要看的则是userdata的使用例子，这里直接引用《Programing in Lua》的28.1 <a href="https://www.lua.org/pil/28.1.html" rel="external nofollow noopener noreferrer" target="_blank">userdata</a>一节，这里就不再赘述。</p>
<h1 id="本章的example"><a href="#本章的example" class="headerlink" title="本章的example"></a>本章的example</h1><p>​    本章也写了一个测试用例，这个测试用例创建了一个userdata实例，并且为它设置了一个包含_ _gc函数的metatable，最后将这个userdata实例，从栈中移除，接着调用了fullgc函数，最后显示的结果是，这个__gc函数被调用。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// p10_test.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"p10_test.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"../common/luastring.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"../vm/luagc.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"../common/luatable.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Vector3</span> &#123;</span></span><br><span class="line">	<span class="keyword">float</span> x;</span><br><span class="line">	<span class="keyword">float</span> y;</span><br><span class="line">	<span class="keyword">float</span> z;</span><br><span class="line">&#125; Vector3;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">gcfunc</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">	Udata* u = lua_touserdata(L, <span class="number">-1</span>);</span><br><span class="line">	Vector3* v3 = (Vector3*)getudatamem(u);</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">"total_size:%d x:%f, y:%f, z:%f"</span>, u-&gt;len, v3-&gt;x, v3-&gt;y, v3-&gt;z);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test_create_object</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">	Udata* u = luaS_newuserdata(L, <span class="keyword">sizeof</span>(Vector3));</span><br><span class="line"></span><br><span class="line">	Vector3* v3 = (Vector3*)getudatamem(u);</span><br><span class="line">	v3-&gt;x = <span class="number">10.0f</span>;</span><br><span class="line">	v3-&gt;y = <span class="number">10.0f</span>;</span><br><span class="line">	v3-&gt;z = <span class="number">10.0f</span>;</span><br><span class="line"></span><br><span class="line">	L-&gt;top-&gt;tt_ = LUA_TUSERDATA;</span><br><span class="line">	L-&gt;top-&gt;value_.gc = obj2gco(u);</span><br><span class="line">	increase_top(L);</span><br><span class="line"></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">Table</span>* <span class="title">t</span> = <span class="title">luaH_new</span>(<span class="title">L</span>);</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gco</span> = <span class="title">obj2gco</span>(<span class="title">t</span>);</span></span><br><span class="line">	TValue tv;</span><br><span class="line">	tv.tt_ = LUA_TTABLE;</span><br><span class="line">	tv.value_.gc = gco;</span><br><span class="line">	setobj(L-&gt;top, &amp;tv);</span><br><span class="line">	increase_top(L);</span><br><span class="line"></span><br><span class="line">	lua_pushCclosure(L, gcfunc, <span class="number">0</span>);</span><br><span class="line">	lua_setfield(L, <span class="number">-2</span>, <span class="string">"__gc"</span>);</span><br><span class="line"></span><br><span class="line">	lua_setmetatable(L, <span class="number">-2</span>);</span><br><span class="line">	L-&gt;top--;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">p10_test_main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">L</span> = <span class="title">luaL_newstate</span>();</span></span><br><span class="line">	luaL_openlibs(L);</span><br><span class="line"></span><br><span class="line">	test_create_object(L);</span><br><span class="line">	luaC_fullgc(L);</span><br><span class="line"></span><br><span class="line">	luaL_close(L);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>执行后，得到的结果为：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">total_size</span><span class="selector-pseudo">:12</span> <span class="selector-tag">x</span><span class="selector-pseudo">:10.0</span>, <span class="selector-tag">y</span><span class="selector-pseudo">:10.0</span>, <span class="selector-tag">z</span><span class="selector-pseudo">:10.0</span></span><br></pre></td></tr></table></figure>
<p>这说明，创建出来的userdata，在fullgc过后，被清除掉了。</p>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>​    本章，我大致介绍了userdata的设计与实现，与以往的一些重型篇幅的章节相比，本章内容非常少，并且对前面一些章节的论述有所依赖，因此如果读者通读了前面的章节，理解这个章节中关联的部分，也不会非常困难。本章节，我也是在实现了userdata相关的逻辑之后，写下的，因此读者可以在Part10的代码目录中，找到对应的实现，下一个章节，我将论述upvalue的设计与实现。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part9：metatable</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part9/</url>
    <content><![CDATA[<p><a href="http://manistein.club/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="构建Lua解释器Part9：metatable"><a href="#构建Lua解释器Part9：metatable" class="headerlink" title="构建Lua解释器Part9：metatable"></a>构建Lua解释器Part9：metatable</h1><p> <em>Posted on December 8, 2020</em></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>​    本章，我们将进入到metatable的探索之中。由于这块本身比较简单，而且我也不打算罗列代码细节，因此本章的篇幅不会很大。只是对一些我认为比较关键的部分，进行说明。首先，本章的主要任务，首先是简要介绍metatable是做什么的，然后简要说明一下，它如何被设置，接着介绍metatable的访问域，双目运算操作域、单目运算操作域等。最后会告诉读者，本章的实现逻辑位于dummylua工程的哪些部位。</p>
<h1 id="什么是metatable？"><a href="#什么是metatable？" class="headerlink" title="什么是metatable？"></a>什么是metatable？</h1><p>​    metatable是什么？它的中文译名是元表。简单的来说，它是一种改变table行为的机制。如果没有这种机制，我们无法对两个table进行加减乘除运算，无法对table进行比较运算。而metatable机制的存在，则为这种提供了可行性。此外，metatable为不同的table，提供了公共域，这个是lua实现面向对象机制的基础。我们为一个table设置metatable的方式也非常简单，如下所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> tbl = <span class="built_in">setmetatable</span>(&#123;&#125;, &#123; <span class="built_in">__index</span> = <span class="function"><span class="keyword">function</span><span class="params">(t, k)</span></span> <span class="built_in">print</span>(<span class="string">"hello world"</span>) <span class="keyword">end</span> &#125;)</span><br></pre></td></tr></table></figure>
<p>上面的例子中，table的metatable，则是setmetatable的第二个参数。而如何获取一个metatable呢？如下例子得以展示：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">local mt = getmetable(<span class="name">tbl</span>)</span><br></pre></td></tr></table></figure>
<p>在lua53的中文文档中[1]，是这么来解释metatable和tag method的：</p>
<blockquote>
<p>Lua 中的每个值都可以有一个 元表。 这个 元表 就是一个普通的 Lua 表， 它用于定义原始值在特定操作下的行为。 如果你想改变一个值在特定操作下的行为，你可以在它的元表中设置对应域。 例如，当你对非数字值做加操作时， Lua 会检查该值的元表中的 “<strong> add” 域下的函数。 如果能找到，Lua 则调用这个函数来完成加这个操作。 在元表中事件的键值是一个双下划线（</strong> ）加事件名的字符串； 键关联的那些值被称为 元方法。 在上一个例子中，__add 就是键值， 对应的元方法是执行加操作的函数。</p>
</blockquote>
<p>你可以使用 setmetatable 来替换一张表的元表。在 Lua 中，你不可以改变表以外其它类型的值的元表 （除非你使用调试库）； 若想改变这些非表类型的值的元表，请使用 C API。</p>
<h1 id="metable的-index和-newindex域"><a href="#metable的-index和-newindex域" class="headerlink" title="metable的 index和 newindex域"></a>metable的<strong> index和</strong> newindex域</h1><p>​    metatable中有两个重要的域，它们分别是以<strong> index和</strong> newindex作为key的域。我们通过一些例子，来理解这两个机制。首先我们来看看__ index的情况：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> tbl = <span class="built_in">setmetatable</span>(&#123;&#125;, &#123; <span class="built_in">__index</span> = <span class="function"><span class="keyword">function</span><span class="params">(t, k)</span></span> <span class="built_in">print</span>(<span class="string">"hello world"</span>) <span class="keyword">end</span> &#125;)</span><br><span class="line"><span class="built_in">print</span>(tbl.hello)</span><br></pre></td></tr></table></figure>
<p>例1<br>这是一个会使用到metatable的__ index域的例子，在开始正式讨论之前，我们首先要对例子本身作一些说明。</p>
<ul>
<li>我们的local变量tbl，实质就是setmetatable函数的第一个参数</li>
<li>tbl被设置了一个metatable，它就是setmetatable的第二个参数</li>
<li>tbl的metatable存在__ index域</li>
<li>print(tbl.hello)这个语句中，hello是tbl不存在的key</li>
<li>__ index所指向的是一个函数，它有两个参数t和k，其中，t表示tbl这个表，而k则表示tbl的缺省域hello</li>
</ul>
<p>当我们的一个table，通过一个key，访问一个value时，虚拟机会做哪些操作呢？我们先来看上面这个例子，它是__ index所指的是function的情况：</p>
<ol>
<li>判断tbl中，是否存在被访问的key：hello，并且value不为nil。如果存在，则返回tbl.hello，否则进入下一步</li>
<li>判断tbl是否设置了metatable，如果没有设置，则tbl.hello这个操作，返回的结果是nil。否则进入下一步</li>
<li>判断tbl的metatable是否存在__ index域，如果不存在，tbl.hello这个操作，返回的结果是nil。否则进入下一步</li>
<li>调用__ index所指向的函数，并执行，如果函数有返回值，或者在函数内有对tbl.hello赋值，那么tbl.hello的结果为这个返回值，或者是被赋予的值</li>
<li>调用结束</li>
</ol>
<p>在例1中，print(tbl.hello)的结果为nil，并且输出了“hello world”，其结果如下所示：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">hello world</span><br><span class="line"><span class="literal">nil</span></span><br></pre></td></tr></table></figure>
<p>例1展示了一个简单的例子，我们现在来看一下例2：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> mt = <span class="built_in">setmetatable</span>(&#123;&#125;, &#123; <span class="built_in">__index</span> = <span class="function"><span class="keyword">function</span><span class="params">(t, k)</span></span> <span class="built_in">print</span>(<span class="string">"1111"</span>) <span class="keyword">end</span> &#125;)</span><br><span class="line">mt.<span class="built_in">__index</span> = <span class="function"><span class="keyword">function</span><span class="params">(t, k)</span></span> <span class="built_in">print</span>(<span class="string">"2222"</span>) <span class="keyword">end</span></span><br><span class="line"><span class="keyword">local</span> tbl = <span class="built_in">setmetatable</span>(&#123;&#125;, mt)</span><br><span class="line"><span class="built_in">print</span>(tbl.hello)</span><br></pre></td></tr></table></figure>
<p>例2<br>上面这个例子，输出的结果为：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">2222</span></span><br><span class="line"><span class="literal">nil</span></span><br></pre></td></tr></table></figure>
<p>因为tbl的metatable是mt，且mt的<strong> index域是个函数，且hello在tbl中并不存在，所以直接调用了mt.</strong> index函数。在访问一个table的缺省域的时候，如果该table有metatable，并且<strong> index域是个函数，那么直接调用它。这里不管tbl的metatable的metatable是否存在，存在的话</strong> index域是什么，它都只调用tbl的metatable的<strong> index函数。<br>    我们通过两个例子，讨论了metatable的</strong> index域是函数的情况，现在我们来看看metatable的__ index域是table的情况。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> mt0 = &#123; hello = <span class="string">"hello world"</span> &#125;</span><br><span class="line"><span class="keyword">local</span> mt1 = <span class="built_in">setmetatable</span>(&#123;&#125;, &#123;<span class="built_in">__index</span> = mt0&#125;)</span><br><span class="line">mt1.key2 = <span class="string">"key2"</span></span><br><span class="line"><span class="keyword">local</span> tbl = <span class="built_in">setmetatable</span>(&#123;&#125;, &#123;<span class="built_in">__index</span> = mt1&#125;)</span><br><span class="line"><span class="built_in">print</span>(tbl.hello)</span><br><span class="line"><span class="built_in">print</span>(tbl.key)</span><br><span class="line"><span class="built_in">print</span>(tbl.key2)</span><br></pre></td></tr></table></figure>
<p>例3<br>例3的输出结果为：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">hello world</span><br><span class="line"><span class="literal">nil</span></span><br><span class="line">key2</span><br></pre></td></tr></table></figure>
<p>hello是tbl的缺省域，因为tbl中找不到hello，所以要到tbl的metatable中查找<strong> index域，这里的</strong> index域指向一个table mt1，因此会到mt1中查找mt1.hello，又因为mt1中不存在key为hello的域，因此要到mt1的metatable中查找，而mt1的metatable的__ index域指向了mt0，因此将mt0.hello返回。同样的，tbl.key的访问逻辑，也执行类似的操作，只是因为mt0没有设置metatable，所以最终tbl.key的值为nil。而对于tbl.key2，操作逻辑也是类似的，只是因为mt1中存在key2这个域，因此直接返回该值，而不继续到metatable中查找。</p>
<p>​    在完成了<strong> index域的论述以后，我们接下来看看</strong> newindex域。我们还是要通过几个例子，对它进行说明，首先来看看例4：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> tbl = <span class="built_in">setmetatable</span>(&#123;&#125;, &#123; <span class="built_in">__newindex</span> = <span class="function"><span class="keyword">function</span><span class="params">(t, k, v)</span></span> <span class="built_in">print</span>(t, k, v) <span class="keyword">end</span> &#125;)</span><br><span class="line">tbl.key = <span class="string">"key"</span></span><br><span class="line"><span class="built_in">print</span>(tbl.key)</span><br></pre></td></tr></table></figure>
<p>例4<br>例4中，key原本不存在于tbl中，由于tbl设置了一个metatable，并且<strong> newindex域有设置一个函数，因此例4中的赋值操作，会触发元方法</strong> newindex，但是赋值操作并不会生效（也就是调用了<strong> newindex函数以后，tbl.key仍然是nil值）。而我们的</strong> newindex函数有3个参数，分别是t、k和v，他们分别代表tbl，key和“key”。如果我们的tbl原本就有tbl.key这个域的时候，对tbl.key重新赋值，并不会触发元方法。例4的输出结果如下所示：</p>
<figure class="highlight gams"><table><tr><td class="code"><pre><span class="line"><span class="keyword">table</span>: 0x12b88c0	<span class="comment">key</span>	<span class="comment">key</span></span><br><span class="line">nil</span><br></pre></td></tr></table></figure>
<p>当需要触发<strong> newindex的元方法的时候，不论metatable是否还有metatable，只要其有</strong> newindex，并且是个函数，那么就会执行它，并终止流程。接下来来看一下__ newindex是table的情况，我们来看看例5：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> mt0 = &#123;&#125;</span><br><span class="line"><span class="keyword">local</span> mt1 = <span class="built_in">setmetatable</span>(&#123;&#125;, &#123; <span class="built_in">__newindex</span> = mt0 &#125;)</span><br><span class="line"><span class="keyword">local</span> mt2 = <span class="built_in">setmetatable</span>(&#123; key1 = <span class="string">"111"</span>&#125;, &#123; <span class="built_in">__newindex</span> = mt1 &#125;)</span><br><span class="line"><span class="keyword">local</span> tbl = <span class="built_in">setmetatable</span>(&#123;&#125;, &#123; <span class="built_in">__newindex</span> = mt2 &#125;)</span><br><span class="line">tbl.key = <span class="string">"key"</span></span><br><span class="line">tbl.key1 = <span class="string">"222"</span></span><br><span class="line"><span class="built_in">print</span>(tbl.key)</span><br><span class="line"><span class="built_in">print</span>(mt0.key)</span><br><span class="line"><span class="built_in">print</span>(mt2.key1)</span><br></pre></td></tr></table></figure>
<p>例5<br>上面例子输出的结果为：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">nil</span><br><span class="line"><span class="type">key</span></span><br><span class="line"><span class="number">222</span></span><br></pre></td></tr></table></figure>
<p>这个例子说明了几个内容：</p>
<ul>
<li>为表tbl从来不存在的key域赋值，tbl.key = “key”这个操作会触发元方法</li>
<li>tbl会到其元表中的__ newindex域mt2，查找是否存在key，因为没找到，因此需要去mt1中查找</li>
<li>mt1中也没找到key，所以要去mt1的元表的__ newindex域中去查找，也就是mt0中。</li>
<li>mt0中也没找到key，但是mt0已经没有metatable，所以直接将”key”的值，赋值到mt0.key中，但是tbl.key并没有被赋值</li>
</ul>
<p>从上面的论述可以看出，当一个table有设置metatable，并且<strong> newindex方法有被设置，因此当给这个table添加新域的时候，会触发元方法</strong> newindex，如果<strong> newindex是个表，则会尝试从中去查找新设置操作的key值，如果找到则直接设置新值，如果找不到则需要继续触发metatable的元方法</strong> newindex。如果没找到元表，则直接将结果设置到该table中。读者可以观察一下tbl.key1 = “222”以及例子打印结果的情况。</p>
<h1 id="双目运算事件"><a href="#双目运算事件" class="headerlink" title="双目运算事件"></a>双目运算事件</h1><p>​    上面一节中，我对<strong> index和</strong> newindex进行了简要的说明，大概介绍清楚了这两个元访问事件的运作机制，现在要对双目运算事件进行简要的说明。lua层的setmetatable，只支持对table赋值metatable的操作，而如果要对其他类型进行metatable的设置操作，需要借助C的API。我们的双目运算，如四则运算，如字符串拼接运算，是有默认的支持类型的，比如四则运算，只支持数值类型的数据运算，而字符串拼接，默认类型则是字符串类型。我们的metatable机制，可以用来实现面向对象的设计方法，限于篇幅，本章不对这些内容进行详细的讨论。使用元表来实现面向对象的机制时，table不可避免地要作为对象存在。那么对象之间如果有双目运算，那怎么处理呢？以加法为例，+操作符，默认只支持数值类型的运算，table是不支持的，除非你为__ add域设置了元方法，如例6所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> tbl1 = <span class="built_in">setmetatable</span>(&#123; value = <span class="number">1</span> &#125;, &#123;<span class="built_in">__add</span> = <span class="function"><span class="keyword">function</span><span class="params">(lhs, rhs)</span></span> <span class="keyword">return</span> lhs.value + rhs.value &#125;)</span><br><span class="line"><span class="keyword">local</span> tbl2 = &#123; value = <span class="number">2</span> &#125;</span><br><span class="line"><span class="keyword">local</span> ret = tbl1 + tbl2</span><br><span class="line"><span class="built_in">print</span>(ret)</span><br></pre></td></tr></table></figure>
<p>例6<br>其输出结果为：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span></span><br></pre></td></tr></table></figure>
<p>因为tbl1设置了<strong> add域的元方法，因此虚拟机在进行加法运算的时候，当操作数不都是数值类型时，就触发tbl1的</strong> add域元方法。现在假设另一种情况，如果tbl1没有设置__ add域的元方法，而是把这个元方法在tbl2设置了，那么结果仍然是一致的。因为虚拟机首先会找左操作数是否有对应的元方法，如果没有则会去右操作数去查找，如果都没有则抛错。其他情况留给读者去推导。lua除了双目元方法，还有单目的，这里就不一一列举了，引用lua53中文文档的一些内容，供读者查阅：</p>
<blockquote>
<p><strong> add: + 操作。 如果任何不是数字的值（包括不能转换为数字的字符串）做加法， Lua 就会尝试调用元方法。 首先、Lua 检查第一个操作数（即使它是合法的）， 如果这个操作数没有为 “</strong>add” 事件定义元方法， Lua 就会接着检查第二个操作数。 一旦 Lua 找到了元方法， 它将把两个操作数作为参数传入元方法， 元方法的结果（调整为单个值）作为这个操作的结果。 如果找不到元方法，将抛出一个错误。<br><strong>sub: - 操作。 行为和 “add” 操作类似。
</strong>mul: * 操作。 行为和 “add” 操作类似。<br><strong>div: / 操作。 行为和 “add” 操作类似。
</strong>mod: % 操作。 行为和 “add” 操作类似。<br><strong>pow: ^ （次方）操作。 行为和 “add” 操作类似。
</strong>unm: - （取负）操作。 行为和 “add” 操作类似。<br><strong>idiv: // （向下取整除法）操作。 行为和 “add” 操作类似。
</strong>band: &amp; （按位与）操作。 行为和 “add” 操作类似， 不同的是 Lua 会在任何一个操作数无法转换为整数时 （参见 §3.4.3）尝试取元方法。<br><strong>bor: | （按位或）操作。 行为和 “band” 操作类似。
</strong>bxor: ~ （按位异或）操作。 行为和 “band” 操作类似。<br><strong>bnot: ~ （按位非）操作。 行为和 “band” 操作类似。
</strong>shl: &lt;&lt; （左移）操作。 行为和 “band” 操作类似。<br><strong>shr: &gt;&gt; （右移）操作。 行为和 “band” 操作类似。
</strong>concat: .. （连接）操作。 行为和 “add” 操作类似， 不同的是 Lua 在任何操作数即不是一个字符串 也不是数字（数字总能转换为对应的字符串）的情况下尝试元方法。<br><strong>len: # （取长度）操作。 如果对象不是字符串，Lua 会尝试它的元方法。 如果有元方法，则调用它并将对象以参数形式传入， 而返回值（被调整为单个）则作为结果。 如果对象是一张表且没有元方法， Lua 使用表的取长度操作（参见 §3.4.7）。 其它情况，均抛出错误。
</strong>eq: == （等于）操作。 和 “add” 操作行为类似， 不同的是 Lua 仅在两个值都是表或都是完全用户数据 且它们不是同一个对象时才尝试元方法。 调用的结果总会被转换为布尔量。<br><strong>lt: &lt; （小于）操作。 和 “add” 操作行为类似， 不同的是 Lua 仅在两个值不全为整数也不全为字符串时才尝试元方法。 调用的结果总会被转换为布尔量。
</strong> le: &lt;= （小于等于）操作。 和其它操作不同， 小于等于操作可能用到两个不同的事件。 首先，像 “lt” 操作的行为那样，Lua 在两个操作数中查找 “<strong>le” 元方法。 如果一个元方法都找不到，就会再次查找 “</strong>lt” 事件， 它会假设 a &lt;= b 等价于 not (b &lt; a)。 而其它比较操作符类似，其结果会被转换为布尔量。<br><strong>index: 索引 table[key]。 当 table 不是表或是表 table 中不存在 key 这个键时，这个事件被触发。 此时，会读出 table 相应的元方法。<br>尽管名字取成这样， 这个事件的元方法其实可以是一个函数也可以是一张表。 如果它是一个函数，则以 table 和 key 作为参数调用它。 如果它是一张表，最终的结果就是以 key 取索引这张表的结果。 （这个索引过程是走常规的流程，而不是直接索引， 所以这次索引有可能引发另一次元方法。）
</strong>newindex: 索引赋值 table[key] = value 。 和索引事件类似，它发生在 table 不是表或是表 table 中不存在 key 这个键的时候。 此时，会读出 table 相应的元方法。 同索引过程那样， 这个事件的元方法即可以是函数，也可以是一张表。 如果是一个函数， 则以 table、 key、以及 value 为参数传入。 如果是一张表， Lua 对这张表做索引赋值操作。 （这个索引过程是走常规的流程，而不是直接索引赋值， 所以这次索引赋值有可能引发另一次元方法。）<br>一旦有了 “newindex” 元方法， Lua 就不再做最初的赋值操作。 （如果有必要，在元方法内部可以调用 rawset 来做赋值。）<br>__call: 函数调用操作 func(args)。 当 Lua 尝试调用一个非函数的值的时候会触发这个事件 （即 func 不是一个函数）。 查找 func 的元方法， 如果找得到，就调用这个元方法， func 作为第一个参数传入，原来调用的参数（args）后依次排在后面。</p>
</blockquote>
<h1 id="dummylua的metatable实现"><a href="#dummylua的metatable实现" class="headerlink" title="dummylua的metatable实现"></a>dummylua的metatable实现</h1><p>​    dummylua新增了luatm.h|c文件，主要用于元表模块的初始化，元表设置和获取的操作，同时在global_State中添加了对应的数据结构（存放基础类型的元表的结构、元方法键值字符串存放结构等），在luavm.h|c中添加了luaV_finishset和luaV_finishset的逻辑，主要是用来执行元方法访问的一些逻辑操作，读者可以直接到阅读这些逻辑。</p>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>​    本章的篇幅很小，没有对元表实现太多的着墨，重点讲了元表访问操作的一些逻辑，metatable这块，我没有花费很大的篇幅着墨，很大原因是因为它相对来说较为简单，并且大家也比较熟悉，所以这里采取了速战速决的策略。本章作为lua语言特性讲解的第一章，后面将陆续介绍，userdata、upvalue、weektable、coroutine和require机制等。</p>
<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><p>[1] <a href="http://cloudwu.github.io/lua53doc/manual.html#2.4" rel="external nofollow noopener noreferrer" target="_blank">元表及元方法</a></p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>supervisor常用命令</title>
    <url>/%E8%BF%90%E7%BB%B4/supervisor%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<p><a href="https://www.jianshu.com/p/f8735b039c67" rel="external nofollow noopener noreferrer" target="_blank">转载自博客</a></p>
<h5 id="查看任务状态"><a href="#查看任务状态" class="headerlink" title="查看任务状态"></a>查看任务状态</h5><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"># supervisorctl status</span><br><span class="line">SmartCoin                        RUNNING   pid <span class="number">13203</span>, uptime <span class="number">0</span>:<span class="number">04</span>:<span class="number">05</span></span><br><span class="line">coin                             RUNNING   pid <span class="number">30744</span>, uptime <span class="number">17</span> days, <span class="number">20</span>:<span class="number">45</span>:<span class="number">18</span></span><br><span class="line">deepwellserver                   RUNNING   pid <span class="number">30257</span>, uptime <span class="number">30</span> days, <span class="number">4</span>:<span class="number">13</span>:<span class="number">01</span></span><br><span class="line">jingtumassetapi                  RUNNING   pid <span class="number">14536</span>, uptime <span class="number">45</span> days, <span class="number">19</span>:<span class="number">18</span>:<span class="number">08</span></span><br><span class="line">moac                             RUNNING   pid <span class="number">20015</span>, uptime <span class="number">15</span> days, <span class="number">5</span>:<span class="number">15</span>:<span class="number">11</span></span><br><span class="line"><span class="built_in">new</span>                              RUNNING   pid <span class="number">10041</span>, uptime <span class="number">43</span> days, <span class="number">22</span>:<span class="number">41</span>:<span class="number">56</span></span><br><span class="line">nginx                            RUNNING   pid <span class="number">18752</span>, uptime <span class="number">22</span>:<span class="number">59</span>:<span class="number">40</span></span><br><span class="line">redis                            RUNNING   pid <span class="number">14542</span>, uptime <span class="number">45</span> days, <span class="number">19</span>:<span class="number">18</span>:<span class="number">08</span></span><br><span class="line">sonyflakeserver                  FATAL     can<span class="string">'t find command '</span><span class="keyword">go</span><span class="string">'</span></span><br><span class="line"><span class="string">sparkportal                      RUNNING   pid 26073, uptime 1 day, 23:11:17</span></span><br><span class="line"><span class="string">sparkportal2                     RUNNING   pid 25732, uptime 1 day, 23:11:21</span></span><br><span class="line"><span class="string">sparkportal3                     RUNNING   pid 25834, uptime 1 day, 23:11:20</span></span><br><span class="line"><span class="string">sparkportal4                     RUNNING   pid 25974, uptime 1 day, 23:11:18</span></span><br><span class="line"><span class="string">sparkuser                        RUNNING   pid 26957, uptime 9 days, 23:07:21</span></span><br><span class="line"><span class="string">sparkwallet                      RUNNING   pid 29045, uptime 5 days, 15:11:58</span></span><br><span class="line"><span class="string">summaryservice                   RUNNING   pid 14535, uptime 45 days, 19:18:08</span></span><br></pre></td></tr></table></figure>
<p>第一列是服务名；第二列是运行状态，RUNNING表示运行中，FATAL 表示运行失败，STARTING表示正在启动,STOPED表示任务已停止；　第三/四列是进程号,最后是任务已经运行的时间。</p>
<p>查看单个任务状态: <code>supervisorctl status　服务名</code></p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"># <span class="selector-tag">supervisorctl</span> <span class="selector-tag">status</span> <span class="selector-tag">sparkportal</span></span><br><span class="line"><span class="selector-tag">sparkportal</span>                      <span class="selector-tag">RUNNING</span>   <span class="selector-tag">pid</span> 26073, <span class="selector-tag">uptime</span> 1 <span class="selector-tag">day</span>, 23<span class="selector-pseudo">:12</span><span class="selector-pseudo">:10</span></span><br></pre></td></tr></table></figure>
<h5 id="启动-停止-重启任务"><a href="#启动-停止-重启任务" class="headerlink" title="启动/停止/重启任务"></a>启动/停止/重启任务</h5><ol>
<li>启动任务<br><code>supervisorctl start 服务名</code></li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># supervisorctl stop sparkportal</span></span><br><span class="line">sparkportal: stopped</span><br><span class="line"><span class="comment">#supervisorctl status sparkportal</span></span><br><span class="line">sparkportal                      STOPPED   Jan 05 01:59 PM</span><br></pre></td></tr></table></figure>
<ol>
<li>停止任务<br><code>supervisorctl stop 服务名</code></li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># supervisorctl start sparkportal</span></span><br><span class="line">sparkportal: started</span><br><span class="line"><span class="comment"># supervisorctl status sparkportal</span></span><br><span class="line">sparkportal                      RUNNING   pid 32207, uptime 0:00:05</span><br></pre></td></tr></table></figure>
<ol>
<li>重启任务<br><code>supervisorctl restart 服务名</code></li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># supervisorctl restart sparkportal</span></span><br><span class="line">sparkportal: stopped</span><br><span class="line">sparkportal: started</span><br><span class="line"><span class="comment"># supervisorctl status sparkportal</span></span><br><span class="line">sparkportal                      RUNNING   pid 4952, uptime 0:00:03</span><br></pre></td></tr></table></figure>
<h5 id="新增任务"><a href="#新增任务" class="headerlink" title="新增任务"></a>新增任务</h5><ol>
<li>任务模板</li>
</ol>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line">[<span class="meta">program:&lt;服务名&gt;</span>]</span><br><span class="line">command=&lt;启动命令&gt;</span><br><span class="line">process_name=%(program_name)s ; <span class="function">process_name <span class="title">expr</span> (<span class="params"><span class="keyword">default</span> %(program_name</span>)s)</span></span><br><span class="line"><span class="function">numprocs</span>=<span class="number">1</span>                    ; <span class="function">number of processes copies to <span class="title">start</span> (<span class="params">def <span class="number">1</span></span>)</span></span><br><span class="line"><span class="function">directory</span>=&lt;运行目录&gt;                ; <span class="function">directory to cwd to before <span class="title">exec</span> (<span class="params">def no cwd</span>)</span></span><br><span class="line"><span class="function"></span>;umask=<span class="number">022</span>                     ; <span class="function">umask <span class="keyword">for</span> <span class="title">process</span> (<span class="params"><span class="keyword">default</span> None</span>)</span></span><br><span class="line"><span class="function"></span>;priority=<span class="number">999</span>                  ; <span class="function">the relative start <span class="title">priority</span> (<span class="params"><span class="keyword">default</span> <span class="number">999</span></span>)</span></span><br><span class="line"><span class="function">autostart</span>=<span class="literal">true</span>                ; <span class="function">start at supervisord <span class="title">start</span> (<span class="params"><span class="keyword">default</span>: <span class="literal">true</span></span>)</span></span><br><span class="line"><span class="function">autorestart</span>=unexpected        ; whether/<span class="function"><span class="keyword">when</span> to <span class="title">restart</span> (<span class="params"><span class="keyword">default</span>: unexpected</span>)</span></span><br><span class="line"><span class="function">startsecs</span>=<span class="number">1</span>                   ; <span class="function">number of secs prog must stay <span class="title">running</span> (<span class="params">def. <span class="number">1</span></span>)</span></span><br><span class="line"><span class="function">startretries</span>=<span class="number">3</span>                ; max <span class="meta"># of serial start failures (default 3)</span></span><br><span class="line">exitcodes=<span class="number">0</span>,<span class="number">2</span>                 ; <span class="string">'expected'</span> <span class="function">exit codes <span class="keyword">for</span> <span class="title">process</span> (<span class="params"><span class="keyword">default</span> <span class="number">0</span>,<span class="number">2</span></span>)</span></span><br><span class="line"><span class="function">stopsignal</span>=QUIT               ; <span class="function">signal used to kill <span class="title">process</span> (<span class="params"><span class="keyword">default</span> TERM</span>)</span></span><br><span class="line"><span class="function">stopwaitsecs</span>=<span class="number">10</span>               ; <span class="function">max num secs to wait b4 <span class="title">SIGKILL</span> (<span class="params"><span class="keyword">default</span> <span class="number">10</span></span>)</span></span><br><span class="line"><span class="function">stopasgroup</span>=<span class="literal">false</span>             ; <span class="function">send stop signal to the UNIX process <span class="title">group</span> (<span class="params"><span class="keyword">default</span> <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function">killasgroup</span>=<span class="literal">false</span>             ; <span class="function">SIGKILL the UNIX process <span class="title">group</span> (<span class="params">def <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function"></span>;user=skywell                  ; setuid to <span class="keyword">this</span> UNIX account to run the program</span><br><span class="line">;redirect_stderr=<span class="literal">true</span>          ; <span class="function">redirect proc stderr to <span class="title">stdout</span> (<span class="params"><span class="keyword">default</span> <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function">stdout_logfile</span>=/<span class="keyword">var</span>/log/&lt;服务名&gt;.log        ; stdout log path, NONE <span class="keyword">for</span> none; <span class="keyword">default</span> AUTO</span><br><span class="line">stdout_logfile_maxbytes=<span class="number">1</span>MB   ; max <span class="meta"># logfile bytes b4 rotation (default 50MB)</span></span><br><span class="line">stdout_logfile_backups=<span class="number">1</span>     ; <span class="meta"># of stdout logfile backups (default 10)</span></span><br><span class="line">stdout_capture_maxbytes=<span class="number">1</span>MB   ; number of bytes <span class="keyword">in</span> <span class="string">'capturemode'</span> (<span class="keyword">default</span> <span class="number">0</span>)</span><br><span class="line">stdout_events_enabled=<span class="literal">false</span>   ; <span class="function">emit events <span class="keyword">on</span> stdout <span class="title">writes</span> (<span class="params"><span class="keyword">default</span> <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function">stderr_logfile</span>=/<span class="keyword">var</span>/log/&lt;服务名&gt;.err        ; stderr log path, NONE <span class="keyword">for</span> none; <span class="keyword">default</span> AUTO</span><br><span class="line">stderr_logfile_maxbytes=<span class="number">1</span>MB   ; max <span class="meta"># logfile bytes b4 rotation (default 50MB)</span></span><br><span class="line">stderr_logfile_backups=<span class="number">10</span>     ; <span class="meta"># of stderr logfile backups (default 10)</span></span><br><span class="line">stderr_capture_maxbytes=<span class="number">1</span>MB   ; number of bytes <span class="keyword">in</span> <span class="string">'capturemode'</span> (<span class="keyword">default</span> <span class="number">0</span>)</span><br><span class="line">stderr_events_enabled=<span class="literal">false</span>   ; <span class="function">emit events <span class="keyword">on</span> stderr <span class="title">writes</span> (<span class="params"><span class="keyword">default</span> <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function">environment</span>=A=<span class="string">"1"</span>,B=<span class="string">"2"</span>,HOME=<span class="string">"/home/skywell"</span>       ; <span class="function">process environment <span class="title">additions</span> (<span class="params">def no adds</span>)</span></span><br><span class="line"><span class="function">serverurl</span>=AUTO                ; <span class="function"><span class="keyword">override</span> serverurl <span class="title">computation</span> (<span class="params">childutils</span>)</span></span><br></pre></td></tr></table></figure>
<p>首先添加任务描述文件，在/etc/supervisor目录下新建文件sparkportal.conf，　将上面任务模板内容复制进文件sparkportal.conf中,将&lt;服务名&gt;替换为任务名sparkportal，将&lt;启动命令&gt;替换为<code>node www.js</code>，将&lt;运行目录&gt;替换为程序所在目录<code>/usr/local/sparkportal/bin</code>。</p>
<p>sparkportal的配置文件为</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line">[<span class="meta">program:sparkportal</span>]</span><br><span class="line">command=node www.js</span><br><span class="line">process_name=%(program_name)s ; <span class="function">process_name <span class="title">expr</span> (<span class="params"><span class="keyword">default</span> %(program_name</span>)s)</span></span><br><span class="line"><span class="function">numprocs</span>=<span class="number">1</span>                    ; <span class="function">number of processes copies to <span class="title">start</span> (<span class="params">def <span class="number">1</span></span>)</span></span><br><span class="line"><span class="function">directory</span>=/usr/local/sparkportal/bin                ; <span class="function">directory to cwd to before <span class="title">exec</span> (<span class="params">def no cwd</span>)</span></span><br><span class="line"><span class="function"></span>;umask=<span class="number">022</span>                     ; <span class="function">umask <span class="keyword">for</span> <span class="title">process</span> (<span class="params"><span class="keyword">default</span> None</span>)</span></span><br><span class="line"><span class="function"></span>;priority=<span class="number">999</span>                  ; <span class="function">the relative start <span class="title">priority</span> (<span class="params"><span class="keyword">default</span> <span class="number">999</span></span>)</span></span><br><span class="line"><span class="function">autostart</span>=<span class="literal">true</span>                ; <span class="function">start at supervisord <span class="title">start</span> (<span class="params"><span class="keyword">default</span>: <span class="literal">true</span></span>)</span></span><br><span class="line"><span class="function">autorestart</span>=unexpected        ; whether/<span class="function"><span class="keyword">when</span> to <span class="title">restart</span> (<span class="params"><span class="keyword">default</span>: unexpected</span>)</span></span><br><span class="line"><span class="function">startsecs</span>=<span class="number">1</span>                   ; <span class="function">number of secs prog must stay <span class="title">running</span> (<span class="params">def. <span class="number">1</span></span>)</span></span><br><span class="line"><span class="function">startretries</span>=<span class="number">3</span>                ; max <span class="meta"># of serial start failures (default 3)</span></span><br><span class="line">exitcodes=<span class="number">0</span>,<span class="number">2</span>                 ; <span class="string">'expected'</span> <span class="function">exit codes <span class="keyword">for</span> <span class="title">process</span> (<span class="params"><span class="keyword">default</span> <span class="number">0</span>,<span class="number">2</span></span>)</span></span><br><span class="line"><span class="function">stopsignal</span>=QUIT               ; <span class="function">signal used to kill <span class="title">process</span> (<span class="params"><span class="keyword">default</span> TERM</span>)</span></span><br><span class="line"><span class="function">stopwaitsecs</span>=<span class="number">10</span>               ; <span class="function">max num secs to wait b4 <span class="title">SIGKILL</span> (<span class="params"><span class="keyword">default</span> <span class="number">10</span></span>)</span></span><br><span class="line"><span class="function">stopasgroup</span>=<span class="literal">false</span>             ; <span class="function">send stop signal to the UNIX process <span class="title">group</span> (<span class="params"><span class="keyword">default</span> <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function">killasgroup</span>=<span class="literal">false</span>             ; <span class="function">SIGKILL the UNIX process <span class="title">group</span> (<span class="params">def <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function"></span>;user=skywell                  ; setuid to <span class="keyword">this</span> UNIX account to run the program</span><br><span class="line">;redirect_stderr=<span class="literal">true</span>          ; <span class="function">redirect proc stderr to <span class="title">stdout</span> (<span class="params"><span class="keyword">default</span> <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function">stdout_logfile</span>=/<span class="keyword">var</span>/log/sparkportal.log        ; stdout log path, NONE <span class="keyword">for</span> none; <span class="keyword">default</span> AUTO</span><br><span class="line">stdout_logfile_maxbytes=<span class="number">1</span>MB   ; max <span class="meta"># logfile bytes b4 rotation (default 50MB)</span></span><br><span class="line">stdout_logfile_backups=<span class="number">1</span>     ; <span class="meta"># of stdout logfile backups (default 10)</span></span><br><span class="line">stdout_capture_maxbytes=<span class="number">1</span>MB   ; number of bytes <span class="keyword">in</span> <span class="string">'capturemode'</span> (<span class="keyword">default</span> <span class="number">0</span>)</span><br><span class="line">stdout_events_enabled=<span class="literal">false</span>   ; <span class="function">emit events <span class="keyword">on</span> stdout <span class="title">writes</span> (<span class="params"><span class="keyword">default</span> <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function">stderr_logfile</span>=/<span class="keyword">var</span>/log/sparkportal.err        ; stderr log path, NONE <span class="keyword">for</span> none; <span class="keyword">default</span> AUTO</span><br><span class="line">stderr_logfile_maxbytes=<span class="number">1</span>MB   ; max <span class="meta"># logfile bytes b4 rotation (default 50MB)</span></span><br><span class="line">stderr_logfile_backups=<span class="number">10</span>     ; <span class="meta"># of stderr logfile backups (default 10)</span></span><br><span class="line">stderr_capture_maxbytes=<span class="number">1</span>MB   ; number of bytes <span class="keyword">in</span> <span class="string">'capturemode'</span> (<span class="keyword">default</span> <span class="number">0</span>)</span><br><span class="line">stderr_events_enabled=<span class="literal">false</span>   ; <span class="function">emit events <span class="keyword">on</span> stderr <span class="title">writes</span> (<span class="params"><span class="keyword">default</span> <span class="literal">false</span></span>)</span></span><br><span class="line"><span class="function">environment</span>=A=<span class="string">"1"</span>,B=<span class="string">"2"</span>,HOME=<span class="string">"/home/skywell"</span>       ; <span class="function">process environment <span class="title">additions</span> (<span class="params">def no adds</span>)</span></span><br><span class="line"><span class="function">serverurl</span>=AUTO                ; <span class="function"><span class="keyword">override</span> serverurl <span class="title">computation</span> (<span class="params">childutils</span>)</span></span><br></pre></td></tr></table></figure>
<ol>
<li>增加任务<br><code>supervisorctl update</code></li>
</ol>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line"><span class="meta"># supervisorctl update</span></span><br><span class="line">sparkportal: added process <span class="keyword">group</span></span><br></pre></td></tr></table></figure>
<p>该命令会将sparkportal.conf所描述的任务启动并纳入管理。然后运用查看任务命令即可查看新增任务的运行状态，如若运行失败，可查看/usr/log目录下的相关日志分析原因。</p>
<h5 id="设置环境变量"><a href="#设置环境变量" class="headerlink" title="设置环境变量"></a>设置环境变量</h5><p>在配置文件找到environment所在行，若没有没有最下面增加environment=变量名=”变量值”即可，如果多个环境变量用逗号分隔，例如<code>environment=变量名1=&quot;变量值１&quot;,变量名2=&quot;变量值2&quot;</code>。</p>
<p>将nodejs运行环境设置为生产环境，增加如下代码：<br><code>environment=NODE_ENV=production</code></p>
]]></content>
      <categories>
        <category>运维</category>
      </categories>
      <tags>
        <tag>运维</tag>
      </tags>
  </entry>
  <entry>
    <title>Golang 切片与函数参数陷阱</title>
    <url>/go/Golang%20%E5%88%87%E7%89%87%E4%B8%8E%E5%87%BD%E6%95%B0%E5%8F%82%E6%95%B0%E9%99%B7%E9%98%B1/</url>
    <content><![CDATA[<p><a href="https://studygolang.com/articles/9876" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>线性结构是计算机最常用的数据结构之一。无论是数组（arrary）还是链表（list），在编程中不可或缺。golang也有数组，不同于别的语言，golang还提供了切片（slice）。切片比数组有更好的灵活性，具有某些动态特性。然而切片又不像动态语言的列表（Python list）。不明白切片的基本实现，写程序的时候容易掉“坑”里。<br><strong>slice参数</strong><br>本来写一个堆排序，使用了golang的slice来做堆，可是发现在pop数据的时候，切片不改变。进而引发了golang函数切片的参数，是传值还是传引用呢？我们知道slice相比array是引用类型。那么直觉上告诉我们如果函数修改了参数的切片，那么外层的切片变量也会变啦。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> </span><br><span class="line">    slice := []<span class="keyword">int</span>&#123;<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;</span><br><span class="line">    fmt.Printf(<span class="string">"slice: %v slice addr %p \n"</span>, slice, &amp;slice)</span><br><span class="line"> </span><br><span class="line">    ret := changeSlice(slice)</span><br><span class="line">    fmt.Printf(<span class="string">"slice: %v ret: %v slice addr %p \n"</span>, slice, &amp;slice, ret)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">changeSlice</span><span class="params">(s []<span class="keyword">int</span>)</span> []<span class="title">int</span></span> &#123;</span><br><span class="line">    s[<span class="number">1</span>] = <span class="number">111</span></span><br><span class="line">    <span class="keyword">return</span> s</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>结果和假设的一样：</p>
<ol>
<li>slice: [0 1 2 3], slice addr: 0xc4200660c0 </li>
<li>slice: [0 111 2 3], ret: [0 111 2 3], slice addr: 0xc4200660c0<br>changeSlice函数修改了切片，变量 slice也跟着修改了。可是如果轻易就下结论，切片参数是按照引用传递，那么下面的现象就需要一种说法了：<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">changeSlice</span><span class="params">(s []<span class="keyword">int</span>)</span> []<span class="title">int</span></span> &#123;</span><br><span class="line">    fmt.Printf(<span class="string">"func: %p \n"</span>, &amp;s)</span><br><span class="line">    s[<span class="number">1</span>] = <span class="number">111</span></span><br><span class="line">    <span class="keyword">return</span> s</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>我们在函数中打出参数 s 的地址，可以看见这个地址和main函数中的slice竟然不是同一个。为了了解这个，我们需要了解golang中的slice基本实现。</p>
<p><strong>slice基本实现</strong><br>Golang中的slice，是一个看似array却不是array的复合结构。切片顾名思义，就是数组切下来的一个片段。slice结构大致存储了三个部分，第一部分为指向底层数组的指针ptr，其次是切片的大小len和切片的容量cap：</p>
<pre><code>+--------+
|        |
|  ptr   |+------------+-------+-----------+
|        |                     |           |
+--------+                     |           |
|        |                     |           |
|        |                     |           |
|  len 5 |                     |           |
|        |                     |           |
+--------+                     v           v
|        |             +-----+-----+-----+-----+----+
|        |             |     |     |     |     |    |
|  cap 5 |     [5]int  |  0  |  1  |  2  |  3  | 4  |
|        |             +-----+-----+-----+-----+----+
+--------+



 slice := arr[1:4]             arr := [5]int{0,1,2,3,4}
</code></pre><p>有一个数组arr是一个包含五个int类型的结构，它的切片slice只是从其取了 1到3这几个数字。我们同样可以再生成一个切片 slice2 := arr[2:5], 所取的就是数组后面的连续块。他们共同使用arr作为底层的结构，可以看见共用了数字的第3，4个元素。修改其中任何一个，都能改变两个切片的值。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> </span><br><span class="line">    arr := [<span class="number">5</span>]<span class="keyword">int</span>&#123;<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>&#125;</span><br><span class="line">    fmt.Println(arr)</span><br><span class="line"> </span><br><span class="line">    slice := arr[<span class="number">1</span>:<span class="number">4</span>]</span><br><span class="line">    slice2 := arr[<span class="number">2</span>:<span class="number">5</span>]</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"arr %v, slice1 %v, slice2 %v, %p %p %p\n"</span>, arr, slice, slice2, &amp;arr, &amp;slice, &amp;slice2)</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"arr[2]%p slice[1] %p slice2[0]%p\n"</span>, &amp;arr[<span class="number">2</span>], &amp;slice[<span class="number">1</span>], &amp;slice2[<span class="number">0</span>])</span><br><span class="line"> </span><br><span class="line">    arr[<span class="number">2</span>] = <span class="number">2222</span></span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"arr %v, slice1 %v, slice2 %v\n"</span>, arr, slice, slice2)</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">    slice[<span class="number">1</span>] = <span class="number">1111</span></span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"arr %v, slice1 %v, slice2 %v\n"</span>, arr, slice, slice2)</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出的值为：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[0 1 2 3 4]</span><br><span class="line">arr [0 1 2 3 4], slice1 [1 2 3], slice2 [2 3 4], 0xc42006e0c0 0xc4200660c0 0xc4200660e0</span><br><span class="line">arr[2]0xc42006e0d0 slice[1] 0xc42006e0d0 slice2[0]0xc42006e0d0</span><br><span class="line">arr [0 1 2222 3 4], slice1 [1 2222 3], slice2 [2222 3 4]</span><br><span class="line">arr [0 1 1111 3 4], slice1 [1 1111 3], slice2 [1111 3 4]</span><br></pre></td></tr></table></figure></p>
<p>由此可见，数组的切片，只是从数组上切一段数据下来，不同的切片，其实是共享这些底层的数据数据。不过这些切片本身是不一样的对象，其内存地址都不一样。</p>
<p>从数组中切一块下来形成切片很好理解，有时候我们用make函数创建切片，实际上golang会在底层创建一个匿名的数组。如果从新的slice再切，那么新创建的两个切片都共享这个底层的匿名数组。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> </span><br><span class="line">    slice := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="number">5</span>)</span><br><span class="line">    <span class="keyword">for</span> i:=<span class="number">0</span>; i&lt;<span class="built_in">len</span>(slice);i++&#123;</span><br><span class="line">        slice[i] = i</span><br><span class="line">    &#125;</span><br><span class="line">    fmt.Printf(<span class="string">"slice %v \n"</span>, slice)</span><br><span class="line"> </span><br><span class="line">    slice2 := slice[<span class="number">1</span>:<span class="number">4</span>]</span><br><span class="line">    fmt.Printf(<span class="string">"slice %v, slice2 %v \n"</span>, slice, slice2)</span><br><span class="line"> </span><br><span class="line">    slice[<span class="number">1</span>] = <span class="number">1111</span></span><br><span class="line">    fmt.Printf(<span class="string">"slice %v, slice2 %v \n"</span>, slice, slice2)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">slice [0 1 2 3 4] </span><br><span class="line">slice [0 1 2 3 4], slice2 [1 2 3] </span><br><span class="line">slice [0 1111 2 3 4], slice2 [1111 2 3]</span><br></pre></td></tr></table></figure></p>
<p><strong>slice的复制</strong><br>既然slice的创建依赖于数组，有时候新生成的slice会修改，但是又不想修改原来的切片或者数组。此时就需要针对原来的切片进行复制了。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> </span><br><span class="line">    slice := []<span class="keyword">int</span>&#123;<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>&#125;</span><br><span class="line"> </span><br><span class="line">    slice2 := slice[<span class="number">1</span>:<span class="number">4</span>]</span><br><span class="line"> </span><br><span class="line">    slice3 := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="built_in">len</span>(slice2))</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">for</span> i, e := <span class="keyword">range</span> slice2 &#123;</span><br><span class="line">        slice3[i] = e</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"slice %v, slice3 %v \n"</span>, slice, slice3)</span><br><span class="line"> </span><br><span class="line">    slice[<span class="number">1</span>] = <span class="number">1111</span></span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"slice %v, slice3 %v \n"</span>, slice, slice3)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出：</p>
<p>slice [0 1 2 3 4], slice3 [1 2 3]<br>slice [0 1111 2 3 4], slice3 [1 2 3]<br>由此可见，新创建的slice3，不会因为slice和slice2的修改而改变slice3。复制很有用，因此golang实现了一个内建的函数copy， copy有两个参数，第一个参数是复制后的对象，第二个是复制前的数组切片对象。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> </span><br><span class="line">    slice := []<span class="keyword">int</span>&#123;<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>&#125;</span><br><span class="line">    slice2 := slice[<span class="number">1</span>:<span class="number">4</span>]</span><br><span class="line"> </span><br><span class="line">    slice4 := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="built_in">len</span>(slice2))</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">copy</span>(slice4, slice2)</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"slice %v, slice4 %v \n"</span>, slice, slice4)</span><br><span class="line">    slice[<span class="number">1</span>] = <span class="number">1111</span></span><br><span class="line">    fmt.Printf(<span class="string">"slice %v, slice4 %v \n"</span>, slice, slice4)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>slice4是从slice2中copy生成，slice和slice4底层的匿名数组是不一样的。因此修改他们不会影响彼此。</p>
<p><strong>slice 追加</strong><br>append 简介</p>
<p>创建复制切片都是常用的操作，还有一个追加元素或者追加数组也是很常用的功能。golang提供了append函数用于给切片追加元素。append第一个参数为原切片，随后是一些可变参数，用于将要追加的元素或多个元素。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> </span><br><span class="line">    slice := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line">    slice[<span class="number">0</span>] = <span class="number">111</span></span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"slice %v, slice addr %p, len %d, cap %d \n"</span>, slice, &amp;slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice))</span><br><span class="line"> </span><br><span class="line">    slice = <span class="built_in">append</span>(slice, <span class="number">222</span>)</span><br><span class="line">    fmt.Printf(<span class="string">"slice %v, slice addr %p, len %d, cap %d \n"</span>, slice, &amp;slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice))</span><br><span class="line"> </span><br><span class="line">    slice = <span class="built_in">append</span>(slice, <span class="number">333</span>)</span><br><span class="line">    fmt.Printf(<span class="string">"slice %v, slice addr %p, len %d, cap %d \n"</span>, slice, &amp;slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice))</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出结果为：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">slice [111], slice addr 0xc4200660c0, len 1, <span class="built_in">cap</span> 2 </span><br><span class="line">slice [111 222], slice addr 0xc4200660c0, len 2, <span class="built_in">cap</span> 2 </span><br><span class="line">slice [111 222 333], slice addr 0xc4200660c0, len 3, <span class="built_in">cap</span> 4</span><br></pre></td></tr></table></figure></p>
<p>切片容量</p>
<p>无论数组还是切片，都有长度限制。也就是追加切片的时候，如果元素正好在切片的容量范围内，直接在尾部追加一个元素即可。如果超出了最大容量，再追加元素就需要针对底层的数组进行复制和扩容操作了。</p>
<p>这里有一个切片容量的概念，从数组中切数据，切片的容量应该是切片的最后一个数据，和数组剩下元素的大小，再加上现有切片的大小。</p>
<p>数组 [0, 1, 2, 3, 4] 中，数组有5个元素。如果切片 s = [1, 2, 3]，那么3在数组的索引为3，也就是数组还剩最后一个元素的大小，加上s已经有3个元素，因此最后s的容量为 1 + 3 = 4。如果切片是<br>s1 = [4]，4的索引再数组中是最大的了，数组空余的元素为0，那么s1的容量为 0 + 1 = 1。具体如下表：</p>
<p>切片    切片字面量    数组剩下空间    长度    容量<br>s[1:3]    [1 2]    2    2    4<br>s[1:1]    []    4    0    4<br>s[4:4]    []    1    0    1<br>s[4:5]    [4]    0    1    1<br>尽管上面的第二个和第三个切片的长度一样，但是他们的容量不一样。容量与最终append的策略有关系。</p>
<p>append简单实现</p>
<p>我们已经知道，切片都依赖底层的数组结构，即使是直接创建的切片，也会生成一个匿名的数组。使用append时候，本质上是针对底层依赖的数组进行操作。如果切片的容量大于长度，给切片追加元素其实是修改底层数中，切片元素后面的元素。如果容量满了，就不能在原来的数组上修改，而是要创建一个新的数组，当然golang是通过创建一个新的切片实现的，因为新切片必然也有一个新的数组，并且这个数组的长度是原来的2倍，使用动态规划算法的简单实现。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> </span><br><span class="line">    arr := [<span class="number">3</span>]<span class="keyword">int</span>&#123;<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>&#125;</span><br><span class="line"> </span><br><span class="line">    slice := arr[<span class="number">1</span>:<span class="number">2</span>]</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"arr %v len %d, slice %v  len %d, cap %d, \n"</span>, arr, <span class="built_in">len</span>(arr), slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice))</span><br><span class="line"> </span><br><span class="line">    slice[<span class="number">0</span>] = <span class="number">333</span></span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"arr %v len %d, slice %v  len %d, cap %d, \n"</span>, arr, <span class="built_in">len</span>(arr), slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice))</span><br><span class="line"> </span><br><span class="line">    slice = <span class="built_in">append</span>(slice, <span class="number">4444</span>)</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"arr %v len %d, slice %v  len %d, cap %d, \n"</span>, arr, <span class="built_in">len</span>(arr), slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice))</span><br><span class="line"> </span><br><span class="line">    slice = <span class="built_in">append</span>(slice, <span class="number">5555</span>)</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"arr %v len %d, slice %v  len %d, cap %d, \n"</span>, arr, <span class="built_in">len</span>(arr), slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice))</span><br><span class="line"> </span><br><span class="line">    slice[<span class="number">0</span>] = <span class="number">333</span></span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"arr %v len %d, slice %v  len %d, cap %d, \n"</span>, arr, <span class="built_in">len</span>(arr), slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">arr [0 1 2] len 3, slice [1]  len 1, <span class="built_in">cap</span> 2, </span><br><span class="line">arr [0 333 2] len 3, slice [333]  len 1, <span class="built_in">cap</span> 2, </span><br><span class="line">arr [0 333 444] len 3, slice [333 444]  len 2, <span class="built_in">cap</span> 2, </span><br><span class="line">arr [0 333 444] len 3, slice [333 444 555]  len 3, <span class="built_in">cap</span> 4, </span><br><span class="line">arr [0 333 444] len 3, slice [333 444 555]  len 3, <span class="built_in">cap</span> 4,</span><br></pre></td></tr></table></figure></p>
<p>小于容量的append</p>
<p>重输出，我们来画一下这个动态过程的图示：</p>
<pre><code>+----+----+----+                           +----+----+----+                             +----+----+----+
|    |    |    |                           |    |    |    |                             |    |    |    |
</code></pre><p> arr   | 0  |  1 |  2 |                     arr   | 0  |333 | 2  |                       arr   | 0  |333 |444 |<br>       +—-+—-+—-+                           +—-+—-+—-+                             +—-+—-+—-+<br>               ^                                          ^                                            ^    ^<br>               |                                          |                                            |    |<br>               |                                          |                                            |    |<br>               |                 slic0] = 333             |              slice = append(slice, 444)    +—-+<br>               |               +—————–&gt;        |                +—————-&gt;          |<br>               |                                          |                                            |<br>            +–+–+—-+—-+                          +–+–+—-+—-+                            +–+–+—-+—-+<br>            |     |    |    |                          |     |    |    |                            |     |    |    |<br>            | p   | 1  | 2  |                          | p   | 1  | 2  |                            | p   | 2  | 2  |<br>            +—–+—-+—-+                          +—–+—-+—-+                            +—–+—-+—-+</p>
<pre><code>slice :=arr[1:2]                           slice :=arr[1:2]                             slice :=arr[1:2]
</code></pre><p>arr 是一个含有三个元素的数组，slice从arr中切了一个元素，由于切片的最后一个元素1是数组的索引是1，距离数组的最大长度还是1，因此slice的容量为2。当修改slice的第一个元素，由于slice底层是arr数组，因此arr的第二个元素也相应被修改。使用append方法给slice追加元素的时候，由于slice的容量还未满，因此等同于扩展了slice指向数组的内容，可以理解为重新切了一个数组内容附给slice，同时修改了数组的内容。</p>
<p>超出容量的append</p>
<p>如果接着append一个元素，那么数组肯定越界。此时append的原理大致如下：</p>
<ol>
<li>创建一个新的临时切片t，t的长度和slice切片的长度一样，但是t的容量是slice切片的2倍，一个动态规划的方式。新建切片的时候，底层也创建了一个匿名的数组，数组的长度和切片容量一样。</li>
<li>复制s里面的元素到t里，即填入匿名数组中。然后把t赋值给slice，现在slice的指向了底层的匿名数组。</li>
<li><p>转变成小于容量的append方法。</p>
<pre><code>+----+----+----+                                +----+----+----+----+----+----+
|    |    |    |                                |    |    |    |    |    |    |
</code></pre><p>arr   | 0  |333 |444 |                                | 333| 444|    |    |    |    |</p>
<pre><code>+----+----+----+                                +----+----+----+----+----+----+
        ^    ^                                    ^     ^
        |    |                                    |     |
        |    |                                    +-----+
        +----+           +---------------&gt;        |
        |                                         |
        |                                         +
     +--+--+----+----+                          +-----+-----+-----+
     |     |    |    |                          |     |     |     |
     | p   | 2  | 2  |                          | p   |  2  |  6  |
     +-----+----+----+                          +-----+-----+-----+

     slice :=arr[1:2]                          t := make([]int, len=2, cap=6)

                                                           +
                                                           |
                                                           |
                                                           |
                                                           |
                                                           v
</code></pre></li>
</ol>
<pre><code>+----+----+----+----+----+----+
|    |    |    |    |    |    |
| 333| 444|555 |    |    |    |
+----+----+----+----+----+----+
  ^         ^
  |         |
  +----+----+
  |
  |
  +
+-----+-----+-----+
|     |     |     |
| p   |  3  |  6  |
+-----+-----+-----+

 slice = t
</code></pre><p>上面的图示描述了大于容量的时候append的操作原理。新生成的切片其依赖的数组和原来的数组就没有关系了，因此在修改新的切片元素，旧的数组也不会有关系。至于临时的切片t，将会被golang的gc回收。当然arr或它衍生的切片都没有应用的时候，也会被gc所回收。</p>
<p>slice和array的关系十分密切，通过两者的合理构建，既能实现动态灵活的线性结构，也能提供访问元素的高效性能。当然，这种结构也不是完美无暇，共用底层数组，在部分修改操作的时候，可能带来副作用，同时如果一个很大的数组，那怕只有一个元素被切片应用，那么剩下的数组都不会被垃圾回收，这往往也会带来额外的问题。</p>
<p><strong>作为函数参数的切片</strong><br>直接改变切片</p>
<p>回到最开始的问题，当函数的参数是切片的时候，到底是传值还是传引用？从changeSlice函数中打出的参数s的地址，可以看出肯定不是传引用，毕竟引用都是一个地址才对。然而changeSlice函数内改变了s的值，也改变了原始变量slice的值，这个看起来像引用的现象，实际上正是我们前面讨论的切片共享底层数组的实现。</p>
<p>即切片传递的时候，传的是数组的值，等效于从原始切片中再切了一次。原始切片slice和参数s切片的底层数组是一样的。因此修改函数内的切片，也就修改了数组。</p>
<pre><code>                                         +-----+----+-----+
                                         |     |    |     |
          +-----------------------------+| p   |  3 |  3  |
          |          +                   +-----+----+-----+
          |          |
          |          |                     s
          |          |
          |          |
          v          v
        +----+----+-----+
        |    |    |     |
arr     | 0  |  1 |  2  |
        +----+----+-----+
          ^           ^
          |           |
          |           |
          +-----------+
          |
          |
        +-+--+----+-----+
        |    |    |     |
        |  p |  3 |  3  |
        +----+----+-----+
           slice
</code></pre><p>例如下面的代码：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">    slice := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="built_in">len</span>(slice); i++ &#123;</span><br><span class="line">        slice[i] = i</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"slice %v %p \n"</span>, slice, &amp;slice)</span><br><span class="line"> </span><br><span class="line">    ret := changeSlice(slice)</span><br><span class="line">    fmt.Printf(<span class="string">"slice %v %p, ret %v \n"</span>, slice, &amp;slice, ret)</span><br><span class="line"> </span><br><span class="line">    ret[<span class="number">1</span>] = <span class="number">1111</span></span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"slice %v %p, ret %v \n"</span>, slice, &amp;slice, ret)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">changeSlice</span><span class="params">(s []<span class="keyword">int</span>)</span> []<span class="title">int</span></span> &#123;</span><br><span class="line">    fmt.Printf(<span class="string">"func s %v %p \n"</span>, s, &amp;s)</span><br><span class="line">    s = <span class="built_in">append</span>(s, <span class="number">3</span>)</span><br><span class="line">    <span class="keyword">return</span> s</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">slice [0 1] 0xc42000a1e0 </span><br><span class="line">func s [0 1] 0xc42000a260 </span><br><span class="line">slice [0 1] 0xc42000a1e0, ret [0 1 3] </span><br><span class="line">slice [0 1111] 0xc42000a1e0, ret [0 1111 3]</span><br></pre></td></tr></table></figure></p>
<p>从输出可以看出，当slice传递给函数的时候，新建了切片s。在函数中给s进行了append一个元素，由于此时s的容量足够到，并没有生成新的底层数组。当修改返回的ret的时候，ret也共用了底层的数组，因此修改ret的原始，相应的也看到了slice的改变。</p>
<p>append 操作</p>
<p>如果在函数内，append操作超过了原始切片的容量，将会有一个新建底层数组的过程，那么此时再修改函数返回切片，应该不会再影响原始切片。例如下面代码：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"> <span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    slice := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="number">2</span>, <span class="number">2</span>)</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="built_in">len</span>(slice); i++ &#123;</span><br><span class="line">        slice[i] = i</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"slice %v %p \n"</span>, slice, &amp;slice)</span><br><span class="line"> </span><br><span class="line">    ret := changeSlice(slice)</span><br><span class="line">    fmt.Printf(<span class="string">"slice %v %p, ret %v \n"</span>, slice, &amp;slice, ret)</span><br><span class="line"> </span><br><span class="line">    ret[<span class="number">1</span>] = <span class="number">-1111</span></span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"slice %v %p, ret %v \n"</span>, slice, &amp;slice, ret)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">changeSlice</span><span class="params">(s []<span class="keyword">int</span>)</span> []<span class="title">int</span></span> &#123;</span><br><span class="line">    fmt.Printf(<span class="string">"func s %v %p \n"</span>, s, &amp;s)</span><br><span class="line">    s[<span class="number">0</span>] = <span class="number">-1</span></span><br><span class="line">    s = <span class="built_in">append</span>(s, <span class="number">3</span>)</span><br><span class="line">    s[<span class="number">1</span>] =  <span class="number">1111</span></span><br><span class="line">    <span class="keyword">return</span> s</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">slice [<span class="number">0</span> <span class="number">1</span>] <span class="number">0xc42000a1a0</span> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">s</span> [0 1] 0<span class="title">xc42000a200</span></span> </span><br><span class="line">slice [<span class="number">-1</span> <span class="number">1</span>] <span class="number">0xc42000a1a0</span>, ret [<span class="number">-1</span> <span class="number">1111</span> <span class="number">3</span>] </span><br><span class="line">slice [<span class="number">-1</span> <span class="number">1</span>] <span class="number">0xc42000a1a0</span>, ret [<span class="number">-1</span> <span class="number">-1111</span> <span class="number">3</span>]</span><br></pre></td></tr></table></figure></p>
<p>从输出可以很清楚的看到了我们的猜想。 即函数中先改变s第一个元素的值，由于slice和s都共用了底层数组，因此无论原始切片slice还是ret，第一个元素都是-1.然后append操作之后，因为超出了s的容量，因此会新建底层数组，虽然s变量没变，但是他的底层数组变了，此时修改s第一个元素，并不会影响原始的slice切片。也就是slice[1]还是1，而ret[1]则是-1。最后在外面修改ret[1]为 -1111，也不会影响原始的切片slice。</p>
<p>通过上面的分析，我们大致可以下结论，slice或者array作为函数参数传递的时候，本质是传值而不是传引用。传值的过程复制一个新的切片，这个切片也指向原始变量的底层数组。（个人感觉称之为传切片可能比传值的表述更准确）。函数中无论是直接修改切片，还是append创建新的切片，都是基于共享切片底层数组的情况作为基础。也就是最外面的原始切片是否改变，取决于函数内的操作和切片本身容量。</p>
<p>传引用方式</p>
<p>array和slice作为参数传递的过程基本上是一样的，即传递他们切片。有时候我们需要处理传递引用的形式。golang提供了指针很方便实现类似的功能。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    slice := []<span class="keyword">int</span>&#123;<span class="number">0</span>, <span class="number">1</span>&#125;</span><br><span class="line">    fmt.Printf(<span class="string">"slice %v %p \n"</span>, slice, &amp;slice)</span><br><span class="line"> </span><br><span class="line">    changeSlice(&amp;slice)</span><br><span class="line">    fmt.Printf(<span class="string">"slice %v %p \n"</span>, slice, &amp;slice)</span><br><span class="line"> </span><br><span class="line">    slice[<span class="number">1</span>] = <span class="number">-1111</span></span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"slice %v %p \n"</span>, slice, &amp;slice)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">changeSlice</span><span class="params">(s *[]<span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">    fmt.Printf(<span class="string">"func s %v %p \n"</span>, *s, s)</span><br><span class="line">    (*s)[<span class="number">0</span>] = <span class="number">-1</span></span><br><span class="line">    *s = <span class="built_in">append</span>(*s, <span class="number">3</span>)</span><br><span class="line">    (*s)[<span class="number">1</span>] =  <span class="number">1111</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">slice [0 1] 0xc42000a1e0 </span><br><span class="line">func s [0 1] 0xc42000a1e0 </span><br><span class="line">slice [-1 1111 3] 0xc42000a1e0 </span><br><span class="line">slice [-1 -1111 3] 0xc42000a1e0</span><br></pre></td></tr></table></figure></p>
<p>从输出可以看到，传递给函数的是slice的指针，函数内对对s的操作本质上都是对slice的操作。并且也可以从函数内打出的s地址看到，至始至终就只有一个切片。虽然在append过程中会出现临时的切片或数组。</p>
<p><strong>总结</strong><br>golang提供了array和slice两种序列结构。其中array是值类型。slice则是复合类型。slice是基于array实现的。slice的第一个内容为指向数组的指针，然后是其长度和容量。通过array的切片可以切出slice，也可以使用make创建slice，此时golang会生成一个匿名的数组。</p>
<p>因为slice依赖其底层的array，修改slice本质是修改array，而array又是有大小限制，当超过slice的容量，即数组越界的时候，需要通过动态规划的方式创建一个新的数组块。把原有的数据复制到新数组，这个新的array则为slice新的底层依赖。</p>
<p>数组还是切片，在函数中传递的不是引用，是另外一种值类型，即通过原始变量进行切片传入。函数内的操作即对切片的修改操作了。当然，如果为了修改原始变量，可以指定参数的类型为指针类型。传递的就是slice的内存地址。函数内的操作都是根据内存地址找到变量本身。</p>
<p>参考资料：Go 切片：用法和本质</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言panic,recover的实现</title>
    <url>/go/Go%E8%AF%AD%E8%A8%80panic,recover%E7%9A%84%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/0JTBGHr-bV4ikLva-8ghEw" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-07-02</p>
<p>本文主要分析Go语言的panic/recover在AMD64 Linux平台下的实现，包括：</p>
<p>主动调用 panic() 函数所引发的panic的处理流程，比如go代码中直接调用panic()函数或编译器插入的对panic()的调用；</p>
<p>非法操作所导致的panic的处理流程，这主要跟信号处理流程有关。</p>
<p>阅读本文所必需的预备知识：</p>
<p>defer/panic/recover 的基本用法；</p>
<p>defer 的实现机制；</p>
<p>mcall/gogo 函数的实现。</p>
<p>panic/recover例子</p>
<p>先来热一下身，大家可以先想想下面几个例子的输出会是什么，检测一下自己对panic/recover的理解。</p>
<p>例1:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> catch(<span class="string">"f"</span>)</span><br><span class="line"> </span><br><span class="line">    g()</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">g</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> m()</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">panic</span>(<span class="string">"g panic"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">m</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="built_in">panic</span>(<span class="string">"m panic"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">catch</span><span class="params">(funcname <span class="keyword">string</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> r := <span class="built_in">recover</span>(); r != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(funcname, <span class="string">"recover:"</span>, r)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">例<span class="number">2</span>:</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> catch(<span class="string">"f"</span>)</span><br><span class="line"> </span><br><span class="line">    g()</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">g</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> m()</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">panic</span>(<span class="string">"g panic"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">m</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> catch(<span class="string">"m"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">catch</span><span class="params">(funcname <span class="keyword">string</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> r := <span class="built_in">recover</span>(); r != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(funcname, <span class="string">"recover:"</span>, r)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">例<span class="number">3</span>:</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> catch(<span class="string">"f"</span>)</span><br><span class="line"> </span><br><span class="line">    g()</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">catch</span><span class="params">(funcname <span class="keyword">string</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> r := <span class="built_in">recover</span>(); r != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(funcname, <span class="string">"recover:"</span>, r)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">g</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> m()</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">panic</span>(<span class="string">"g panic"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">m</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> catch(<span class="string">"m"</span>)</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">panic</span>(<span class="string">"m panic"</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>panic/recover要点简介</p>
<p>为了更好的理解panic/recover的实现代码，我们首先需要了解几个与之有关的要点：</p>
<p>当panic发生之后，程序从正常的执行流程跳转到执行panic发生之前通过defer语句注册的defered函数，直到某个defered函数通过recover函数捕获了panic后再恢复正常的执行流程，如果没有recover则当所有的defered函数被执行完成之后结束程序；</p>
<p>defer语句会被编译器翻译成对runtime包中deferproc()函数的调用，该函数会把defered函数打包成一个_defer结构体对象挂入goroutine对应的g结构体对象的_defer链表中，_defer对象除了保存有defered函数的地址以及该函数需要的参数外，还会分别把call deferproc指令的下一条指令的地址以及此时函数调用栈顶指针保存在_defer.pc和_defer.sp成员之中，用于recover时恢复程序的正常执行流程；</p>
<p>当某个defered函数通过recover()函数捕获到一个panic之后，程序将从该defered函数对应的_defer结构体对象的pc成员所保存的指令地址处开始执行；</p>
<p>panic可以嵌套，当发生panic之后在执行defer函数时又发生了panic即为嵌套。每个还未被recover的panic都会对应着一个_panic结构体对象，它们会被依次挂入g结构体的_panic链表之中，最近一次发生的panic位于_panic链表表头，最早发生的panic位于链表尾。</p>
<p>下面对第2点和第3点做个简单的说明。假设有如下程序片段：</p>
<p>例4<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    f()</span><br><span class="line">    fmt.Println(<span class="string">"main"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> catch(<span class="string">"f"</span>)      <span class="comment">// 1</span></span><br><span class="line"> </span><br><span class="line">    <span class="built_in">panic</span>(<span class="string">"f panic"</span>)</span><br><span class="line"> </span><br><span class="line">    fmt.Println(<span class="string">"f continue"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">catch</span><span class="params">(funcname <span class="keyword">string</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> r := <span class="built_in">recover</span>(); r != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(funcname, <span class="string">"recover:"</span>, r)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>f()函数运行时会发生panic，但该panic会被它通过defer注册的catch函数所捕获从而恢复程序的正常执行流程，上一篇文章我们提到过deferproc函数有个隐含的返回值与panic/recover有关，下面我们通过f()函数再来看一下相关的汇编指令片段：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">......</span><br><span class="line"># 对应<span class="keyword">defer</span> catch(<span class="string">"f"</span>)</span><br><span class="line"><span class="number">0x0000000000487245</span> &lt;+<span class="number">69</span>&gt;: callq   <span class="number">0x426c00</span> &lt;runtime.deferproc&gt;</span><br><span class="line"><span class="number">0x000000000048724a</span> &lt;+<span class="number">74</span>&gt;: test   %eax,%eax</span><br><span class="line"><span class="number">0x000000000048724c</span> &lt;+<span class="number">76</span>&gt;: jne   <span class="number">0x48726c</span> &lt;main.f+<span class="number">108</span>&gt;</span><br><span class="line">......</span><br><span class="line"># 对应<span class="built_in">panic</span>(<span class="string">"f panic"</span>)</span><br><span class="line"><span class="number">0x0000000000487265</span> &lt;+<span class="number">101</span>&gt;: callq   <span class="number">0x427880</span> &lt;runtime.gopanic&gt;</span><br><span class="line">......</span><br><span class="line"><span class="number">0x000000000048726c</span> &lt;+<span class="number">108</span>&gt;: nop</span><br><span class="line"><span class="number">0x000000000048726d</span> &lt;+<span class="number">109</span>&gt;: callq   <span class="number">0x427490</span> &lt;runtime.deferreturn&gt;  </span><br><span class="line">......</span><br></pre></td></tr></table></figure></p>
<p>该代码片段前3条指令对应着f()函数中注释1处的一行go代码，最后两条指令通过调用deferreturn函数去执行defered函数，如果f函数不发生panic，其执行流程我们在上一篇文章中已经详细介绍过，但此处的f函数会发生panic，其流程稍有不同：</p>
<p>当f函数执行到callq  0x426c00 &lt;runtime.deferproc&gt;指令时，deferproc函数将会把catch函数的地址及其参数”f”、下一条指令的地址0x000000000048724a以及当前的栈顶指针打包成一个_defer结构体对象挂入当前goroutine的_defer链表，然后通过eax寄存器隐形的返回一个0值；</p>
<p>因为从deferproc函数返回时，eax寄存器的值为0，所以0x000000000048724a &lt;+74&gt;: test   %eax,%eax这条指令不会导致下一条jne指令发生跳转，于是程序会继续执行到0x0000000000487265 &lt;+101&gt;处的callq   0x427880 &lt;runtime.gopanic&gt;指令；</p>
<p>gopanic函数内部会去调用defered函数即catch(“f”)，因为catch()函数调用了recover，这将导致CPU跳转到f()函数的0x000000000048724a &lt;+74&gt;: test   %eax,%eax指令处继续执行，这看起来就像deferproc函数再一次返回了，但这次eax寄存器的值却会被设置成1，所以执行这条test指令将导致下一条jne指令跳转到0x000000000048726c &lt;+108&gt;: nop处继续执行，此时已经恢复了程序的正常执行流程；</p>
<p>调用deferreturn函数，因为例4程序只有一个defered函数，而且已经被gopanic调用了，所以这里的deferreturn函数并不会调用任何defered函数就直接返回到了f()函数，然后从f函数返回到main函数继续执行main函数的fmt.Println(“main”)语句。</p>
<p>主动调用panic()函数</p>
<p>一般来说，Go程序在两种情况下会发生panic：</p>
<p>主动调用panic()函数，这包括go代码中直接调用以及由编译器插入的调用，比如编译器会插入代码检查访问数组/slice是否越界，同时还会插入调用panic()的代码，运行时如果越界就会去调用panic()函数；</p>
<p>非法操作，比如向只读内存写入数据，访问非法内存等也会发生panic。这种情况在Linux平台（其它平台不熟悉）下是通过信号(signal)机制来实现对panic()函数的调用。</p>
<p>我们先来看主动调用panic函数时panic/recover的流程。</p>
<p>通过反汇编可以得知go代码中对panic()/recover()函数的调用会被编译器翻译成对runtime包中的gopanic()以及gorecover()函数的调用。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/<span class="built_in">panic</span>.<span class="keyword">go</span> : <span class="number">453</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// The implementation of the predeclared function panic.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">gopanic</span><span class="params">(e <span class="keyword">interface</span>&#123;&#125;)</span></span> &#123;</span><br><span class="line">    gp := getg()</span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//panic可以嵌套，比如发生了panic之后运行defered函数又发生了panic，如上面的例3。</span></span><br><span class="line">    <span class="comment">//最新的panic会被挂入goroutine对应的g结构体对象的_panic链表的表头</span></span><br><span class="line">    <span class="keyword">var</span> p _panic  <span class="comment">//创建_panic结构体对象</span></span><br><span class="line">    p.arg = e  <span class="comment">//panic的参数</span></span><br><span class="line">    p.link = gp._panic</span><br><span class="line">    gp._panic = (*_panic)(noescape(unsafe.Pointer(&amp;p)))</span><br><span class="line"> </span><br><span class="line">    atomic.Xadd(&amp;runningPanicDefers, <span class="number">1</span>)</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        d := gp._defer  <span class="comment">//取出_defer链表头的defered函数</span></span><br><span class="line">        <span class="keyword">if</span> d == <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="keyword">break</span>  <span class="comment">//没有defer函数将会跳出循环，然后打印栈信息然后结束程序</span></span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">// If defer was started by earlier panic or Goexit (and, since we're back here, that triggered a new panic),</span></span><br><span class="line">        <span class="comment">// take defer off list. The earlier panic or Goexit will not continue running.</span></span><br><span class="line">        <span class="keyword">if</span> d.started &#123;</span><br><span class="line">            <span class="comment">//到这里一定发生了panic嵌套，即在defered函数中又发生了panic，请参考本文开头的例1</span></span><br><span class="line">            <span class="comment">//d.started = true是panic嵌套的充分条件，但并不是必要条件，也就是说</span></span><br><span class="line">            <span class="comment">//即使d.started为false也是可能发生嵌套的，请结合defer的处理流程并参考本文开头的例3</span></span><br><span class="line">            <span class="comment">//最近发生的一次panic并没有被recover所以取消上一次发生的panic</span></span><br><span class="line">            <span class="keyword">if</span> d._panic != <span class="literal">nil</span> &#123;</span><br><span class="line">                d._panic.aborted = <span class="literal">true</span></span><br><span class="line">            &#125;</span><br><span class="line">            d._panic = <span class="literal">nil</span></span><br><span class="line">            d.fn = <span class="literal">nil</span></span><br><span class="line">            gp._defer = d.link</span><br><span class="line">            freedefer(d)</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">// Mark defer as started, but keep on list, so that traceback</span></span><br><span class="line">        <span class="comment">// can find and update the defer's argument frame if stack growth</span></span><br><span class="line">        <span class="comment">// or a garbage collection happens before reflectcall starts executing d.fn.</span></span><br><span class="line">        d.started = <span class="literal">true</span>  <span class="comment">//用于判断是否发生了嵌套panic</span></span><br><span class="line"> </span><br><span class="line">        <span class="comment">// Record the panic that is running the defer.</span></span><br><span class="line">        <span class="comment">// If there is a new panic during the deferred call, that panic</span></span><br><span class="line">        <span class="comment">// will find d in the list and will mark d._panic (this panic) aborted.</span></span><br><span class="line">        <span class="comment">//把panic和defer函数关联起来</span></span><br><span class="line">        d._panic = (*_panic)(noescape(unsafe.Pointer(&amp;p)))</span><br><span class="line"> </span><br><span class="line">        <span class="comment">//在panic中记录当前panic的栈顶位置，用于recover判断</span></span><br><span class="line">        p.argp = unsafe.Pointer(getargp(<span class="number">0</span>))</span><br><span class="line">        <span class="comment">//通过reflectcall函数调用defered函数</span></span><br><span class="line">        <span class="comment">//如果defered函数再次发生panic而且并未被该defered函数recover，则reflectcall永远不会返回，参考例2。</span></span><br><span class="line">        <span class="comment">//如果defered函数并没有发生过panic或者发生了panic但该defered函数成功recover了新发生的panic，</span></span><br><span class="line">        <span class="comment">//则此函数会返回继续执行后面的代码。</span></span><br><span class="line">        reflectcall(<span class="literal">nil</span>, unsafe.Pointer(d.fn), deferArgs(d), <span class="keyword">uint32</span>(d.siz), <span class="keyword">uint32</span>(d.siz))</span><br><span class="line">        p.argp = <span class="literal">nil</span></span><br><span class="line"> </span><br><span class="line">        <span class="comment">// reflectcall did not panic. Remove d.</span></span><br><span class="line">        <span class="keyword">if</span> gp._defer != d &#123;</span><br><span class="line">            throw(<span class="string">"bad defer entry in panic"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//defer函数已经被执行，脱链</span></span><br><span class="line">        d._panic = <span class="literal">nil</span></span><br><span class="line">        d.fn = <span class="literal">nil</span></span><br><span class="line">        gp._defer = d.link</span><br><span class="line"> </span><br><span class="line">        <span class="comment">// trigger shrinkage to test stack copy. See stack_test.go:TestStackPanic</span></span><br><span class="line">        <span class="comment">//GC()</span></span><br><span class="line"> </span><br><span class="line">        pc := d.pc  <span class="comment">//call deferproc的下一条指令的地址，下一条指令为 test rax, rax，在defer实现机制一文中有详细说明</span></span><br><span class="line">        <span class="comment">//call deferproc指令执行前的栈顶指针</span></span><br><span class="line">        sp := unsafe.Pointer(d.sp) <span class="comment">// must be pointer so it gets adjusted during stack copy</span></span><br><span class="line">        freedefer(d)</span><br><span class="line">        <span class="keyword">if</span> p.recovered &#123;</span><br><span class="line">            <span class="comment">//defered函数调用recover成功捕获了panic会设置p.recovered = true</span></span><br><span class="line">            atomic.Xadd(&amp;runningPanicDefers, <span class="number">-1</span>)</span><br><span class="line"> </span><br><span class="line">            gp._panic = p.link</span><br><span class="line">            <span class="comment">// Aborted panics are marked but remain on the g.panic list.</span></span><br><span class="line">            <span class="comment">// Remove them from the list.</span></span><br><span class="line">            <span class="keyword">for</span> gp._panic != <span class="literal">nil</span> &amp;&amp; gp._panic.aborted &#123;</span><br><span class="line">                gp._panic = gp._panic.link</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> gp._panic == <span class="literal">nil</span> &#123; <span class="comment">// must be done with signal</span></span><br><span class="line">                gp.sig = <span class="number">0</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// Pass information about recovering frame to recovery.</span></span><br><span class="line">            gp.sigcode0 = <span class="keyword">uintptr</span>(sp)</span><br><span class="line">            gp.sigcode1 = pc</span><br><span class="line">            <span class="comment">//mcall函数永远不会返回，mcall函数的实现可以参考公众号内的其它文章，有详细分析</span></span><br><span class="line">            <span class="comment">//调用recovery函数跳转到pc位置继续执行</span></span><br><span class="line">            mcall(recovery)  </span><br><span class="line">            throw(<span class="string">"recovery failed"</span>) <span class="comment">// mcall should not return</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// ran out of deferred calls - old-school panic now</span></span><br><span class="line">    <span class="comment">// Because it is unsafe to call arbitrary user code after freezing</span></span><br><span class="line">    <span class="comment">// the world, we call preprintpanics to invoke all necessary Error</span></span><br><span class="line">    <span class="comment">// and String methods to prepare the panic strings before startpanic.</span></span><br><span class="line">    preprintpanics(gp._panic)</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//打印函数调用链，然后挂死程序</span></span><br><span class="line">    fatalpanic(gp._panic) <span class="comment">// should not return</span></span><br><span class="line">    *(*<span class="keyword">int</span>)(<span class="literal">nil</span>) = <span class="number">0</span>      <span class="comment">// not reached</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// runtime/panic.go : 578</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// The implementation of the predeclared function recover.</span></span><br><span class="line"><span class="comment">// Cannot split the stack because it needs to reliably</span></span><br><span class="line"><span class="comment">// find the stack segment of its caller.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// TODO(rsc): Once we commit to CopyStackAlways,</span></span><br><span class="line"><span class="comment">// this doesn't need to be nosplit.</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">gorecover</span><span class="params">(argp <span class="keyword">uintptr</span>)</span> <span class="title">interface</span></span>&#123;&#125; &#123;</span><br><span class="line">    <span class="comment">// Must be in a function running as part of a deferred call during the panic.</span></span><br><span class="line">    <span class="comment">// Must be called from the topmost function of the call</span></span><br><span class="line">    <span class="comment">// (the function used in the defer statement).</span></span><br><span class="line">    <span class="comment">// p.argp is the argument pointer of that topmost deferred function call.</span></span><br><span class="line">    <span class="comment">// Compare against argp reported by caller.</span></span><br><span class="line">    <span class="comment">// If they match, the caller is the one who can recover.</span></span><br><span class="line">    gp := getg()</span><br><span class="line">    p := gp._panic</span><br><span class="line">    <span class="comment">//条件argp == uintptr(p.argp)在判断panic和recover是否匹配，内层recover不能捕获外层的panic</span></span><br><span class="line">    <span class="comment">//比如本文开头的例2中m函数中的defer catch("m")不能捕获g函数中的panic</span></span><br><span class="line">    <span class="keyword">if</span> p != <span class="literal">nil</span> &amp;&amp; !p.recovered &amp;&amp; argp == <span class="keyword">uintptr</span>(p.argp) &#123;</span><br><span class="line">        p.recovered = <span class="literal">true</span>  <span class="comment">//通过设置p.recovered = true告诉gopanic函数panic已经被recover了</span></span><br><span class="line">        <span class="keyword">return</span> p.arg</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// runtime/panic.go : 634</span></span><br><span class="line"><span class="comment">// Unwind the stack after a deferred function calls recover</span></span><br><span class="line"><span class="comment">// after a panic. Then arrange to continue running as though</span></span><br><span class="line"><span class="comment">// the caller of the deferred function returned normally.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">recovery</span><span class="params">(gp *g)</span></span> &#123;</span><br><span class="line">    <span class="comment">// Info about defer passed in G struct.</span></span><br><span class="line">    sp := gp.sigcode0   <span class="comment">//call deferproc时的栈顶指针</span></span><br><span class="line">    pc := gp.sigcode1   <span class="comment">//call deferproc下一条指令的地址</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// d's arguments need to be in the stack.</span></span><br><span class="line">    <span class="keyword">if</span> sp != <span class="number">0</span> &amp;&amp; (sp &lt; gp.stack.lo || gp.stack.hi &lt; sp) &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"recover: "</span>, hex(sp), <span class="string">" not in ["</span>, hex(gp.stack.lo), <span class="string">", "</span>, hex(gp.stack.hi), <span class="string">"]\n"</span>)</span><br><span class="line">        throw(<span class="string">"bad recovery"</span>)</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Make the deferproc for this d return again,</span></span><br><span class="line">    <span class="comment">// this time returning 1. The calling function will</span></span><br><span class="line">    <span class="comment">// jump to the standard return epilogue.</span></span><br><span class="line">    gp.sched.sp = sp</span><br><span class="line">    gp.sched.pc = pc</span><br><span class="line">    gp.sched.lr = <span class="number">0</span></span><br><span class="line">    gp.sched.ret = <span class="number">1</span>     <span class="comment">//该值（1）会被gogo函数放入eax寄存器</span></span><br><span class="line">    gogo(&amp;gp.sched)   <span class="comment">//跳转到pc所指的指令处继续执行，gogo函数的实现请参考公众号内的其它文章，有详细分析</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>从代码可以看出，如果不考虑嵌套，主动 panic/recover 的流程比较清晰：遍历当前 goroutine 所注册的 defered 函数并通过 reflectcall 调用遍历到的函数，如果某个 defered 函数调用了recover（对应到runtime的gorecover函数）则使用 mcall(recovery)  恢复程序的正常流程，否则执行完所有的 defered 函数之后打印出 panic 的栈信息然后退出程序。这里需要说明一下为什么需要通过 reflectcall 来调用 defered 函数而不是直接调用 defered 函数。原因在于直接调用 defered 函数就得在当前栈帧中为它准备参数，而不同的 defered 函数的参数大小可能会有很大差异，比如有的defered函数没有参数而有些defered函数可能又需要成千上万字节的参数，然而gopanic 函数的栈帧大小固定而且很小，所以很有可能没有足够的空间来存放 defered 函数的参数，而reflectcall函数可以处理这种情况，具体是怎么处理的这里就不介绍了，有兴趣的话大家可以去看一下reflectcall函数的代码。</p>
<p>对于panic的嵌套，也就是defered函数再次发生了panic，这会导致gopanic函数再次被调用，也就是说gopanic函数会存在递归调用，其调用链为 gopanic()-&gt;reflectcall()-&gt;defered函数-&gt;gopanic() ，这时有两种情况：</p>
<p>defered函数通过defer再次注册了defered函数而且recover了最新的panic，则上面的调用链将原路从reflect call()返回到gopanic函数继续执行；</p>
<p>defered函数没有recover它自己的panic，则reflectcall()不会返回。要么第二次gopanic执行完所有defered函数之后退出程序，要么新发生的panic代替了前一次panic然后由外层的defered函数recover。</p>
<p>对于上述两种情况，大家可以结合前面代码中的注释以及例2和例3加以理解。</p>
<p>非法操作引起的panic</p>
<p>最常见的非法操作主要是非法访问内存，我们来看一个例子：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">   <span class="string">"fmt"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> p *<span class="keyword">int</span></span><br><span class="line"> </span><br><span class="line">    *p = <span class="number">100</span>  <span class="comment">// crash</span></span><br><span class="line"> </span><br><span class="line">    fmt.Println(<span class="string">"not reached"</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    f()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个程序运行时会发生panic，原因是f()函数企图向p所指的内存写入100，但指针变量p却是nil。来看看f函数的汇编代码片段：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0x0000000000487200 &lt;+0&gt;: mov   %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">0x0000000000487209 &lt;+9&gt;: cmp   0x10(%rcx),%rsp</span><br><span class="line">0x000000000048720d &lt;+13&gt;: jbe   0x4872a1 &lt;main.f+161&gt;</span><br><span class="line">0x0000000000487213 &lt;+19&gt;: sub   <span class="variable">$0x70</span>,%rsp</span><br><span class="line">0x0000000000487217 &lt;+23&gt;: mov   %rbp,0x68(%rsp)</span><br><span class="line">0x000000000048721c &lt;+28&gt;: lea   0x68(%rsp),%rbp</span><br><span class="line">0x0000000000487221 &lt;+33&gt;: movq   <span class="variable">$0x0</span>,0x30(%rsp)</span><br><span class="line">0x000000000048722a &lt;+42&gt;: xor   %eax,%eax</span><br><span class="line">0x000000000048722c &lt;+44&gt;: <span class="built_in">test</span>   %al,(%rax)</span><br><span class="line">0x000000000048722e &lt;+46&gt;: movq   <span class="variable">$0x64</span>,(%rax)    <span class="comment"># *p = 100</span></span><br><span class="line">0x0000000000487235 &lt;+53&gt;: xorps %xmm0,%xmm0</span><br><span class="line">0x0000000000487238 &lt;+56&gt;: movups %xmm0,0x40(%rsp)</span><br><span class="line">0x000000000048723d &lt;+61&gt;: lea   0x40(%rsp),%rax</span><br><span class="line">......</span><br></pre></td></tr></table></figure></p>
<p>通过汇编代码我们可以确定编译器并未插入对gopanic函数的调用，但这个程序运行起来发生panic时与在go代码中直接调用gopanic函数时的表现是一样的，都会输出panic时栈的信息，所以这种非法操作最终应该也会调用到gopanic函数，但具体是怎么调用到它的呢？我们可以使用调试工具dlv给gopanic下一个断点，等断下来之后使用bt可以看到其函数调用链为：</p>
<p>f()-&gt;runtime.sigpanic()-&gt;runtime.panicmem()-&gt;runtime.gopanic()<br>可以看到f()调用了runtime.sigpanic()函数，但从上面的汇编代码可以得知f()其实并没有直接调用runtime.sigpanic()函数，是不是有些奇怪？</p>
<p>事实上，当CPU在执行</p>
<p>0x000000000048722e &lt;+46&gt;: movq   $0x64,(%rax)    # *p = 100<br>这一条指令时，CPU会发生异常，异常发生后将依次执行如下流程：</p>
<p>CPU在内存中保存发生异常的指令的地址（这里是0x000000000048722e）。为了方便描述，我们称这个地址为异常返回地址；</p>
<p>陷入内核执行由操作系统在系统启动时提供的异常处理程序，该异常处理程序会负责把CPU的所有相关寄存器的值保存在内存之中；</p>
<p>向引起异常的当前线程发送一个SIGSEGV信号；</p>
<p>从内核返回，在返回过程中发现有信号需要处理；</p>
<p>从内核返回到用户态执行信号处理程序（go程序启动时向内核注册的信号处理函数），该信号处理程序将会把第1步中由CPU保存的异常返回地址修改为runtime.sigpanic函数的地址；</p>
<p>信号处理程序执行完成后再次进入内核；</p>
<p>从内核返回开始执行runtime.sigpanic函数；</p>
<p>上述整个流程与Linux系统的信号处理有关，了解即可，如果有兴趣可以参考相关的内核资料，这里我们只需要关注第5步和第7步。从该流程可以看出，当go程序发生异常之后之所以能够最终执行到gopanic函数，关键在于上述流程的第5步修改了异常之后的执行流程，而第5步中的信号处理程序是由go语言的runtime提供的，所以下面我们直接从信号处理程序开始大致看一下其流程。</p>
<p>SIGSEGV信号处理流程</p>
<p>对于SIGSEGV信号，信号处理程序的函数调用链为</p>
<p>内核返回-&gt; runtime.sigtramp() -&gt;runtime.sigtrampgo()-&gt;runtime.sighandler()-&gt;sigctxt.preparePanic()修改异常返回地址<br>这个调用链中的函数由大家自己去挖掘细节，这里只说两点：</p>
<p>runtime.sigtramp是go程序启动时向内核注册的信号处理函数，所以当线程收到SIGSEGV信号后内核会负责让CPU进入这个函数运行；</p>
<p>内核在返回用户态执行信号处理程序runtime.sigtramp()函数之前，内核会把异常返回地址等数据保存在信号处理程序的函数调用栈之中，等信号处理程序执行完成之后再次进入内核时，内核会把它之前保存在信号处理程序函数栈上的异常返回地址等数据拷贝回内核，然后再返回到用户态继续执行异常返回地址处的指令。这个流程给信号处理程序提供了一个可以修改CPU执行流程的机会，我们来看看sigctxt.preparePanic()函数是怎么修改异常返回地址的：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// preparePanic sets up the stack to look like a call to sigpanic.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(c *sigctxt)</span> <span class="title">preparePanic</span><span class="params">(sig <span class="keyword">uint32</span>, gp *g)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> GOOS == <span class="string">"darwin"</span> &#123;</span><br><span class="line">        ......</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//指针c所指的内存即执行信号处理程序之前由内核保存在栈上的数据</span></span><br><span class="line">    <span class="comment">//c.rip即为异常返回地址，也就是异常发生时CPU正在执行的指令的地址</span></span><br><span class="line">    pc := <span class="keyword">uintptr</span>(c.rip())</span><br><span class="line">    sp := <span class="keyword">uintptr</span>(c.rsp())</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> shouldPushSigpanic(gp, pc, *(*<span class="keyword">uintptr</span>)(unsafe.Pointer(sp))) &#123;</span><br><span class="line">        <span class="comment">// Make it look the like faulting PC called sigpanic.</span></span><br><span class="line">        <span class="keyword">if</span> sys.RegSize &gt; sys.PtrSize &#123;</span><br><span class="line">            sp -= sys.PtrSize</span><br><span class="line">            *(*<span class="keyword">uintptr</span>)(unsafe.Pointer(sp)) = <span class="number">0</span></span><br><span class="line">        &#125;</span><br><span class="line">        sp -= sys.PtrSize</span><br><span class="line">        *(*<span class="keyword">uintptr</span>)(unsafe.Pointer(sp)) = pc</span><br><span class="line">        c.set_rsp(<span class="keyword">uint64</span>(sp))</span><br><span class="line">    &#125;</span><br><span class="line">    c.set_rip(<span class="keyword">uint64</span>(funcPC(sigpanic)))  <span class="comment">//修改异常返回地址为sigpanic函数的地址</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个函数的最后一行把异常返回地址修改成了runtime.sigpanic函数的地址，等信号处理完成进入内核后再次返回用户态时CPU将会从runtime.sigpanic函数开始执行，最终执行到前面已经分析过的gopanic函数，这部分代码很清晰，大家有兴趣的话可以自己看看。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>How to Use Websockets in go</title>
    <url>/go/How%20to%20Use%20Websockets%20in%20go/</url>
    <content><![CDATA[<p><a href="https://yalantis.com/blog/how-to-build-websockets-in-go/" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>Sending a message and getting an instant response without refreshing the page is something we take for granted. But in the past, enabling real-time functionality was a real challenge for developers. The developer community has come a long way from HTTP long polling and AJAX and has finally found a solution for building truly real-time apps. </p>
<p>This solution comes in the form of WebSockets, which make it possible to open an interactive session between a user’s browser and a server. WebSockets allow a browser to send messages to a server and receive event-driven responses without having to poll the server for a reply.</p>
<p>For now, WebSockets are the number one solution for building real-time applications: online games, instant messengers, tracking apps, and so on. This guide explains how WebSockets operate and shows how we can build WebSocket applications in Go programming language. We also compare the most popular WebSocket libraries so you can choose the best one for your needs. </p>
<p>Read also: Best Practices for Speeding Up JSON Encoding and Decoding in Go</p>
<p>Network sockets vs WebSockets<br>Getting started with WebSockets in go, let’s draw the line between network sockets and WebSockets. </p>
<p>Network socket<br>A network socket, or simply a socket, serves as an internal endpoint for exchanging data between applications running on the same computer or on different computers on the same network. </p>
<p>Sockets are a key part of Unix and Windows-based operating systems, and they make it easier for developers to create network-enabled software. Instead of constructing network connections from scratch, app developers can include sockets in their programs. Since network sockets are used for a number of different network protocols (HTTP, FTP, etc.), several sockets can be used simultaneously.</p>
<p>Sockets are created and used with a set of function calls, which are sometimes referred to as a socket’s application programming interface (API). Thanks to function calls, sockets can be opened just like regular files. </p>
<p>There are several types of network sockets:</p>
<p>Datagram sockets (SOCK_DGRAM), also known as connectionless sockets, use the User Datagram Protocol (UDP). Datagram sockets support a bidirectional flow of messages and preserve record boundaries.</p>
<p>Stream sockets (SOCK_STREAM), also known as connection-oriented sockets, use the Transmission Control Protocol (TCP), Stream Control Transmission Protocol (SCTP), or Datagram Congestion Control Protocol (DCCP). These sockets provide a bidirectional, reliable, sequenced, and unduplicated flow of data with no record boundaries. </p>
<p>Raw sockets (or raw IP sockets) are typically available in routers and other networking equipment. These sockets are normally datagram-oriented, although their exact characteristics depend on the interface provided by the protocol. Raw sockets are not used by most applications. They’re provided to support the development of new communication protocols and to provide access to more esoteric facilities of existing protocols. </p>
<p>Socket communication </p>
<p>First, let’s figure out how to ensure that every socket is unique. If they’re not, you can’t establish a reliable communication channel. </p>
<p>Giving every process a unique PID helps to deal with the problem locally. But such an approach doesn’t work over a network. To create a unique socket, we recommend using the TCP/IP protocol. With TCP/IP, the IP addresses of the network layer are unique within a given network, and the protocol and port are unique among host applications.</p>
<p>TCP and UDP are two major protocols for communicating between hosts. Let’s see how your app can connect to TCP and UDP sockets. </p>
<p>Connecting to a TCP socket</p>
<p>To establish a TCP connection, a Go client uses the DialTCP function in the net package. DialTCP returns a TCPConn object. When a connection is established, the client and server begin exchanging data: the client sends a request to the server through a TCPConn, the server parses the request and sends a response, and the TCPConn receives the response from the server.<br><img src="https://img-blog.csdnimg.cn/img_convert/0f7312e570a0ed005eb75a4994939e89.png" alt><br>tcp socket</p>
<p>This connection remains valid until the client or server closes it. The functions for creating a connection are as follows:</p>
<p>Client side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line">  tcpAddr, err := net.ResolveTCPAddr(resolver, serverAddr)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">       <span class="comment">// handle error</span></span><br><span class="line">  &#125;</span><br><span class="line">  conn, err := net.DialTCP(network, <span class="literal">nil</span>, tcpAddr)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">          <span class="comment">// handle error</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// send message</span></span><br><span class="line">   _, err = conn.Write(&#123;message&#125;)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">       <span class="comment">// handle error</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// receive message</span></span><br><span class="line">  <span class="keyword">var</span> buf [&#123;buffSize&#125;]<span class="keyword">byte</span></span><br><span class="line">  _, err := conn.Read(buf[<span class="number">0</span>:])</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">       <span class="comment">// handle error</span></span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure></p>
<p>Server side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line">   tcpAddr, err := net.ResolveTCPAddr(resolver, serverAddr)</span><br><span class="line">       <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">           <span class="comment">// handle error</span></span><br><span class="line">       &#125;</span><br><span class="line">   </span><br><span class="line">       listener, err := net.ListenTCP(<span class="string">"tcp"</span>, tcpAddr)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// listen for an incoming connection</span></span><br><span class="line">    conn, err := listener.Accept()</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// send message</span></span><br><span class="line">    <span class="keyword">if</span> _, err := conn.Write(&#123;message&#125;); err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;    </span><br><span class="line">    <span class="comment">// receive message</span></span><br><span class="line">    buf := <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">512</span>)</span><br><span class="line">    n, err := conn.Read(buf[<span class="number">0</span>:])</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<p>Connecting to a UDP socket</p>
<p>In contrast to a TCP socket, with a UDP socket, the client just sends a datagram to the server. There’s no Accept function, since the server doesn’t need to accept a connection and just waits for datagrams to arrive.<br><img src="https://img-blog.csdnimg.cn/img_convert/1bf773f01b80ed9693a4707837ff909e.png" alt><br>udp socket</p>
<p>Other TCP functions have UDP counterparts; just replace TCP with UDP in the functions above.</p>
<p>Client side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line">    raddr, err := net.ResolveUDPAddr(<span class="string">"udp"</span>, address)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br><span class="line">       </span><br><span class="line">    conn, err := net.DialUDP(<span class="string">"udp"</span>, <span class="literal">nil</span>, raddr)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br><span class="line">        ....... </span><br><span class="line">    <span class="comment">// send message</span></span><br><span class="line">    buffer := <span class="built_in">make</span>([]<span class="keyword">byte</span>, maxBufferSize)</span><br><span class="line">    n, addr, err := conn.ReadFrom(buffer)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br><span class="line">         .......            </span><br><span class="line">    <span class="comment">// receive message</span></span><br><span class="line">    buffer := <span class="built_in">make</span>([]<span class="keyword">byte</span>, maxBufferSize)</span><br><span class="line">    n, err = conn.WriteTo(buffer[:n], addr)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// handle error</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<p>Server side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line">udpAddr, err := net.ResolveUDPAddr(resolver, serverAddr)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">conn, err := net.ListenUDP(<span class="string">"udp"</span>, udpAddr)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line">    .......</span><br><span class="line"><span class="comment">// send message</span></span><br><span class="line">buffer := <span class="built_in">make</span>([]<span class="keyword">byte</span>, maxBufferSize)</span><br><span class="line">n, addr, err := conn.ReadFromUDP(buffer)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line">     .......</span><br><span class="line"><span class="comment">// receive message</span></span><br><span class="line">buffer := <span class="built_in">make</span>([]<span class="keyword">byte</span>, maxBufferSize)</span><br><span class="line">n, err = conn.WriteToUDP(buffer[:n], addr)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>What WebSockets are<br>The WebSocket communication protocol provides a full-duplex communication channel over a single TCP connection. In contrast to HTTPs, WebSockets don’t require you to send a request in order to get a  response. They allow for bidirectional data flows, so you can just wait for the server to respond. It will send you a message when it’s available. </p>
<p>WebSockets are a good solution for services that require continuous data exchange – for instance, instant messengers, online games, and real-time trading systems. You can find complete information about the WebSocket protocol in the RFC 6455 specification.  </p>
<p>WebSocket connections are requested by browsers and are responded to by servers, after which a connection is established. This process is often called a handshake. The special kind of header in WebSockets requires only one handshake between a browser and server for establishing a connection that will remain active throughout its lifetime. </p>
<p>WebSockets solve many of the headaches of real-time web development and have several benefits over traditional HTTP:</p>
<p>The lightweight header reduces data transmission overhead.</p>
<p>Only one TCP connection is required for a single web client.</p>
<p>WebSocket servers can push data to web clients.<br><img src="https://img-blog.csdnimg.cn/img_convert/dd102e118e9cb3276e5d7ef43d5b5418.png" alt><br>how websockets work</p>
<p>The WebSocket protocol is relatively simple to implement. It uses the HTTP protocol for the initial handshake. After a successful handshake, a connection is established and the WebSocket essentially uses raw TCP to read/write data.</p>
<p>This is what a client request looks like:<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">GET /chat HTTP/1.1</span><br><span class="line">  Host: server.example.com</span><br><span class="line">  Upgrade: websocket</span><br><span class="line">  Connection: Upgrade</span><br><span class="line">  Sec-WebSocket-Key: x3JJHMbDL1EzLkh9GBhXDw==</span><br><span class="line">  Sec-WebSocket-Protocol: chat, superchat</span><br><span class="line">  Sec-WebSocket-Version: 13</span><br><span class="line">  Origin: http://example.com</span><br></pre></td></tr></table></figure></p>
<p>And here’s the server response:<br><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">HTTP/1.1 101 Switching Protocols</span><br><span class="line">Upgrade: websocket</span><br><span class="line">Connection: Upgrade</span><br><span class="line">Sec-WebSocket-Accept: HSmrc0sMlYUkAGmm5OPpG2HaGWk=</span><br><span class="line">Sec-WebSocket-Protocol: chat</span><br></pre></td></tr></table></figure></p>
<p>How to create WebSocket app in Go<br>To write a simple WebSocket echo server based on the net/http library, you need to:</p>
<p>Initiate a handshake</p>
<p>Receive data frames from the client</p>
<p>Send data frames to the client</p>
<p>Close the handshake</p>
<p>First, let’s create an HTTP handler with a WebSocket endpoint:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// HTTP server with WebSocket endpoint</span></span><br><span class="line">        <span class="function"><span class="keyword">func</span> <span class="title">Server</span><span class="params">()</span></span> &#123;</span><br><span class="line">        http.HandleFunc(<span class="string">"/"</span>, <span class="function"><span class="keyword">func</span><span class="params">(w http.ResponseWriter, r *http.Request)</span></span> &#123;</span><br><span class="line">            ws, err := NewHandler(w, r)</span><br><span class="line">            <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">                 <span class="comment">// handle error</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> err = ws.Handshake(); err != <span class="literal">nil</span> &#123;</span><br><span class="line">                <span class="comment">// handle error</span></span><br><span class="line">            &#125;</span><br><span class="line">        …</span><br></pre></td></tr></table></figure></p>
<p>Then initialize the WebSocket structure. </p>
<p>The initial handshake request always comes from the client. Once the server has defined a WebSocket request, it needs to reply with a handshake response. </p>
<p>Bear in mind that you can’t write the response using the http.ResponseWriter, since it will close the underlying TCP connection once you start sending the response. </p>
<p>So you need to use HTTP Hijacking. Hijacking allows you to take over the underlying TCP connection handler and bufio.Writer. This gives you the possibility to read and write data without closing the TCP connection.</p>
<p>// NewHandler initializes a new handler<br>        func NewHandler(w http.ResponseWriter, req <em>http.Request) (</em>WS, error) {<br>        hj, ok := w.(http.Hijacker)<br>        if !ok {<br>            // handle error<br>        }                  …..<br>}<br>To complete the handshake, the server must respond with the appropriate headers.</p>
<p>// Handshake creates a handshake header<br>    func (ws *WS) Handshake() error {</p>
<pre><code>  hash := func(key string) string {
      h := sha1.New()
      h.Write([]byte(key))
      h.Write([]byte(&quot;258EAFA5-E914-47DA-95CA-C5AB0DC85B11&quot;))

  return base64.StdEncoding.EncodeToString(h.Sum(nil))
  }(ws.header.Get(&quot;Sec-WebSocket-Key&quot;))
.....
</code></pre><p>}</p>
<p>“Sec-WebSocket-key” is generated randomly and is Base64-encoded. The server needs to append this key to a fixed string after accepting a request. Assume your have the x3JJHMbDL1EzLkh9GBhXDw== key. In this case, you can use SHA-1 to compute the binary value and use Base64 to encode it. You’ll get HSmrc0sMlYUkAGmm5OPpG2HaGWk=. Use this as the value of the Sec-WebSocket-Accept response header.</p>
<p>Transferring the data frame<br>When the handshake has been successfully completed, your app can read and write data from and to the client. The WebSocket specification defines a specific frame format that’s used between a client and server. Here is a bit pattern of the frame:<br><img src="https://img-blog.csdnimg.cn/img_convert/fa7f0b60b62ea96a80fd5da17c1a8b75.png" alt><br>The bit pattern of the frame</p>
<p>Use the following code to decode the client payload:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Recv receives data and returns a Frame</span></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="params">(ws *WS)</span> <span class="title">Recv</span><span class="params">()</span> <span class="params">(frame Frame, _ error)</span></span> &#123;</span><br><span class="line">        frame = Frame&#123;&#125;</span><br><span class="line">        head, err := ws.read(<span class="number">2</span>)</span><br><span class="line">        <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="comment">// handle error</span></span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure></p>
<p>In turn, these lines of code allow for encoding data:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Send sends a Frame</span></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="params">(ws *WS)</span> <span class="title">Send</span><span class="params">(fr Frame)</span> <span class="title">error</span></span> &#123;</span><br><span class="line">        <span class="comment">// make a slice of bytes of length 2</span></span><br><span class="line">        data := <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">2</span>)</span><br><span class="line">    </span><br><span class="line">        <span class="comment">// Save fragmentation &amp; opcode information in the first byte</span></span><br><span class="line">        data[<span class="number">0</span>] = <span class="number">0x80</span> | fr.Opcode</span><br><span class="line">        <span class="keyword">if</span> fr.IsFragment &#123;</span><br><span class="line">            data[<span class="number">0</span>] &amp;= <span class="number">0x7F</span></span><br><span class="line">        &#125;</span><br><span class="line">        .....</span><br></pre></td></tr></table></figure></p>
<p>Closing a handshake<br>A handshake is closed when one of the parties sends a close frame with a close status as the payload. Optionally, the party sending the close frame can send a close reason in the payload. If closing is initiated by the client, the server should send a corresponding close frame in response.<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Close sends a close frame and closes the TCP connection</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(ws *Ws)</span> <span class="title">Close</span><span class="params">()</span> <span class="title">error</span></span> &#123;</span><br><span class="line">    f := Frame&#123;&#125;</span><br><span class="line">    f.Opcode = <span class="number">8</span></span><br><span class="line">    f.Length = <span class="number">2</span></span><br><span class="line">    f.Payload = <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">2</span>)</span><br><span class="line">    binary.BigEndian.PutUint16(f.Payload, ws.status)</span><br><span class="line">    <span class="keyword">if</span> err := ws.Send(f); err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> err</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ws.conn.Close()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>List of WebSocket libraries<br>There are several third-party libraries that ease developers’ lives and greatly facilitate working with WebSockets. </p>
<p>STDLIB ( x/net/websocket )</p>
<p>This WebSocket library is part of the standard library. It implements a client and server for the WebSocket protocol, as described in the RFC 6455 specification. It doesn’t need to be installed and has good official documentation. But on the other hand, it still lacks some features that can be found in other WebSocket libraries. Golang WebSocket implementations in the /x/net/websocket package do not allow users to reuse I/O buffers between connections in a clear way. </p>
<p>Let’s check how the STDLIB package works. Here’s an example of code for performing basic functions like creating a connection and sending and receiving messages. </p>
<p>First of all, to install and use this library, you should add this line of code to your: </p>
<p>import “golang.org/x/net/websocket”</p>
<p>Client side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// create connection</span></span><br><span class="line"><span class="comment">// schema can be ws:// or wss://</span></span><br><span class="line"><span class="comment">// host, port – WebSocket server</span></span><br><span class="line">conn, err := websocket.Dial(<span class="string">"&#123;schema&#125;://&#123;host&#125;:&#123;port&#125;"</span>, <span class="string">""</span>, op.Origin)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125; </span><br><span class="line"><span class="keyword">defer</span> conn.Close()</span><br><span class="line">         .......</span><br><span class="line">  <span class="comment">// send message</span></span><br><span class="line">    <span class="keyword">if</span> err = websocket.JSON.Send(conn, &#123;message&#125;); err != <span class="literal">nil</span> &#123;</span><br><span class="line">     <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line">          .......</span><br><span class="line">    <span class="comment">// receive message</span></span><br><span class="line"><span class="comment">// messageType initializes some type of message</span></span><br><span class="line">message := messageType&#123;&#125;</span><br><span class="line"><span class="keyword">if</span> err := websocket.JSON.Receive(conn, &amp;message); err != <span class="literal">nil</span> &#123;</span><br><span class="line">      <span class="comment">// handle error</span></span><br><span class="line">&#125;  </span><br><span class="line">    .......</span><br></pre></td></tr></table></figure></p>
<p>Server side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Initialize WebSocket handler + server</span></span><br><span class="line">mux := http.NewServeMux()</span><br><span class="line">    mux.Handle(<span class="string">"/"</span>, websocket.Handler(<span class="function"><span class="keyword">func</span><span class="params">(conn *websocket.Conn)</span></span> &#123;</span><br><span class="line">        <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">            <span class="keyword">for</span> &#123;</span><br><span class="line">            </span><br><span class="line">                <span class="comment">// do something, receive, send, etc.</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        .......    </span><br><span class="line">    <span class="comment">// receive message</span></span><br><span class="line"><span class="comment">// messageType initializes some type of message</span></span><br><span class="line">message := messageType&#123;&#125;</span><br><span class="line"><span class="keyword">if</span> err := websocket.JSON.Receive(conn, &amp;message); err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line">    .......     </span><br><span class="line"><span class="comment">// send message</span></span><br><span class="line"><span class="keyword">if</span> err := websocket.JSON.Send(conn, message); err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;    </span><br><span class="line">    ........</span><br></pre></td></tr></table></figure></p>
<p>GORILLA</p>
<p>The WebSocket package in the Gorilla web toolkit boasts a complete and tested implementation of the WebSocket protocol as well as a stable package API. The WebSocket package is well-documented and easy to use. You can find documentation on the official Gorilla website.</p>
<p>Installation</p>
<p>go get github.com/gorilla/websocket<br>Examples of code<br>Client side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line">   <span class="comment">// schema – can be ws:// or wss://</span></span><br><span class="line">   <span class="comment">// host, port – WebSocket server  </span></span><br><span class="line">   u := url.URL&#123;</span><br><span class="line">       Scheme: &#123;schema&#125;,</span><br><span class="line">       Host:   &#123;host&#125;:&#123;port&#125;,</span><br><span class="line">       Path:   <span class="string">"/"</span>,</span><br><span class="line">   &#125;</span><br><span class="line">   c, _, err := websocket.DefaultDialer.Dial(u.String(), <span class="literal">nil</span>)</span><br><span class="line">   <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">       <span class="comment">// handle error</span></span><br><span class="line">   &#125;</span><br><span class="line">       .......    </span><br><span class="line">   <span class="comment">// send message</span></span><br><span class="line">   err := c.WriteMessage(websocket.TextMessage, &#123;message&#125;)</span><br><span class="line">   <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">       <span class="comment">// handle error</span></span><br><span class="line">   &#125;  </span><br><span class="line">       .......      </span><br><span class="line">   <span class="comment">// receive message</span></span><br><span class="line">   _, message, err := c.ReadMessage()</span><br><span class="line">   <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">       <span class="comment">// handle error</span></span><br><span class="line">   &#125;</span><br><span class="line">       .......</span><br></pre></td></tr></table></figure></p>
<p>Server side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line">  u := websocket.Upgrader&#123;&#125;</span><br><span class="line">  c, err := u.Upgrade(w, r, <span class="literal">nil</span>)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">      <span class="comment">// handle error</span></span><br><span class="line">  &#125;</span><br><span class="line">      .......    </span><br><span class="line">  <span class="comment">// receive message</span></span><br><span class="line">  messageType, message, err := c.ReadMessage()</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">      <span class="comment">// handle error</span></span><br><span class="line">  &#125;</span><br><span class="line">      .......       </span><br><span class="line">  <span class="comment">// send message</span></span><br><span class="line">  err = c.WriteMessage(messageType, &#123;message&#125;)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">      <span class="comment">// handle error</span></span><br><span class="line">  &#125;</span><br><span class="line">      .......</span><br></pre></td></tr></table></figure></p>
<p>Read also: What projects should you use Go for?</p>
<p>GOBWAS</p>
<p>This tiny WebSocket packahe has a powerful list of features, such as a zero-copy upgrade and a low-level API that allows for building custom packet handling logic. GOBWAS requires no intermediate allocations during I/O. It also boasts high-level wrappers and helpers around the API in the wsutil package, allowing developers to start fast without digging into the internals of the protocol. This library has a flexible API, but that comes at the cost of usability and clarity.</p>
<p>You can check the GoDoc website for the available documentation. The installation is provided by including the following line of code:</p>
<p>go get github.com/gobwas/ws</p>
<p>Client side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line"><span class="comment">// schema – can be ws or wss</span></span><br><span class="line"><span class="comment">// host, port – ws server</span></span><br><span class="line">conn, _, _, err := ws.DefaultDialer.Dial(ctx, &#123;schema&#125;:<span class="comment">//&#123;host&#125;:&#123;port&#125;)</span></span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line">    .......</span><br><span class="line"><span class="comment">// send message</span></span><br><span class="line">err = wsutil.WriteClientMessage(conn, ws.OpText, &#123;message&#125;)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">    .......</span><br><span class="line"><span class="comment">// receive message    </span></span><br><span class="line">msg, _, err := wsutil.ReadServerData(conn)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line">    .......</span><br></pre></td></tr></table></figure></p>
<p>Server side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line"> listener, err := net.Listen(<span class="string">"tcp"</span>, op.Port)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">     <span class="comment">// handle error</span></span><br><span class="line"> &#125;</span><br><span class="line"> conn, err := listener.Accept()</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">     <span class="comment">// handle error</span></span><br><span class="line"> &#125;</span><br><span class="line"> upgrader := ws.Upgrader&#123;&#125;</span><br><span class="line"> <span class="keyword">if</span> _, err = upgrader.Upgrade(conn); err != <span class="literal">nil</span> &#123;</span><br><span class="line">     <span class="comment">// handle error</span></span><br><span class="line"> &#125;</span><br><span class="line">     .......</span><br><span class="line"> <span class="comment">// receive message</span></span><br><span class="line"> <span class="keyword">for</span> &#123; </span><br><span class="line">      reader := wsutil.NewReader(conn, ws.StateServerSide)</span><br><span class="line">      _, err := reader.NextFrame()</span><br><span class="line">      <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">          <span class="comment">// handle error</span></span><br><span class="line">      &#125;</span><br><span class="line">      data, err := ioutil.ReadAll(reader)</span><br><span class="line">      <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">          <span class="comment">// handle error</span></span><br><span class="line">      &#125;</span><br><span class="line">         .......</span><br><span class="line"> &#125;   </span><br><span class="line">     .......</span><br><span class="line"> <span class="comment">// send message</span></span><br><span class="line"> msg := <span class="string">"new server message"</span></span><br><span class="line"> <span class="keyword">if</span> err := wsutil.WriteServerText(conn, &#123;message&#125;); err != <span class="literal">nil</span> &#123;</span><br><span class="line">     <span class="comment">// handle error</span></span><br><span class="line"> &#125;</span><br><span class="line">     .......</span><br></pre></td></tr></table></figure></p>
<p>GOWebsockets</p>
<p>This tool offers a wide range of easy-to-use features. It allows for concurrency control, data compression, and setting request headers. GoWebsockets supports proxies and subprotocols for emitting and receiving text and binary data. Developers can also enable or disable SSL verification.<br>You can find documentation for and examples of how to use GOWebsockets on the GoDoc website and on the project’s GitHub page. Install the package by adding the following line of code:</p>
<p>go get github.com/sacOO7/gowebsocket</p>
<p>Client side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line"><span class="comment">// schema – can be ws or wss</span></span><br><span class="line"><span class="comment">// host, port – ws server</span></span><br><span class="line">socket := gowebsocket.New(&#123;schema&#125;:<span class="comment">//&#123;host&#125;:&#123;port&#125;)</span></span><br><span class="line">socket.Connect()</span><br><span class="line">    .......  </span><br><span class="line"><span class="comment">// send message</span></span><br><span class="line">socket.SendText(&#123;message&#125;)</span><br><span class="line">or</span><br><span class="line">socket.SendBinary(&#123;message&#125;)</span><br><span class="line">    .......</span><br><span class="line"><span class="comment">// receive message</span></span><br><span class="line">socket.OnTextMessage = <span class="function"><span class="keyword">func</span><span class="params">(message <span class="keyword">string</span>, socket gowebsocket.Socket)</span></span> &#123;</span><br><span class="line">    <span class="comment">// hande received message</span></span><br><span class="line">&#125;;</span><br><span class="line">or</span><br><span class="line">socket.OnBinaryMessage = <span class="function"><span class="keyword">func</span><span class="params">(data [] <span class="keyword">byte</span>, socket gowebsocket.Socket)</span></span> &#123;</span><br><span class="line">    <span class="comment">// hande received message</span></span><br><span class="line">&#125;;  </span><br><span class="line">    .......</span><br></pre></td></tr></table></figure></p>
<p>Server side:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// init</span></span><br><span class="line"><span class="comment">// schema – can be ws or wss</span></span><br><span class="line"><span class="comment">// host, port – ws server</span></span><br><span class="line">conn, _, _, err := ws.DefaultDialer.Dial(ctx, &#123;schema&#125;:<span class="comment">//&#123;host&#125;:&#123;port&#125;)</span></span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line">    .......        </span><br><span class="line"><span class="comment">// send message</span></span><br><span class="line">err = wsutil.WriteClientMessage(conn, ws.OpText, &#123;message&#125;)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br><span class="line">    .......       </span><br><span class="line"><span class="comment">// receive message    </span></span><br><span class="line">msg, _, err := wsutil.ReadServerData(conn)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// handle error</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>Comparing existing solutions<br>We’ve described four of the most widely used WebSocket libraries for Golang. The table below contains a detailed comparison of these tools.<br><img src="https://img-blog.csdnimg.cn/img_convert/405ca386824aadd8950b00e1b0946c1b.png" alt><br>websocket libraries</p>
<p>To better analyze their performance, we also conducted a couple of benchmarks.</p>
<p>Read also: Monitoring the Performance of Your Go Application: Why and How You Should Do It</p>
<p>The results are the following:<br><img src="https://img-blog.csdnimg.cn/img_convert/d91495feb007d97effa0bdb6a4fe677a.png" alt><br>Benchmark tests</p>
<p>As you can see, GOBWAS has a significant advantage over others libraries. It has fewer allocations per operation and uses less memory and time per allocation. Plus, it has zero I/O allocation. Besides, GOBWAS has all the methods you need to create WebSocket client–server interactions and receive message fragments. You can also use it to easily work with TCP sockets. </p>
<p>If you really don’t like GOBWAS, you can use Gorilla. It’s quite simple and has almost all the same features. You can also use STDLIB, but it’s not as good in production because it lacks many necessary features and, as you can see in the benchmarks, offers weaker performance. GOWebsocket is about the same as STDLIB. But if you need to quickly build a prototype or MVP, it can be a reasonable choice.  </p>
<p>Besides these tools, there are also several alternative implementations that allow you to build powerful steaming solutions. Among them there are:</p>
<p>go-socket.io</p>
<p>Apache Thrift  </p>
<p>gRPC</p>
<p>Package rpc</p>
<p>The constant development of streaming technologies and the availability of well-documented tools such as WebSockets make it easy for developers to create truly real-time applications. Write us if you need advice on or help with creating a real-time app using WebSockets. We hope, this tutorial helped you a lot.</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go系统学习推荐书籍</title>
    <url>/go/go%E7%B3%BB%E7%BB%9F%E5%AD%A6%E4%B9%A0%E6%8E%A8%E8%8D%90%E4%B9%A6%E7%B1%8D/</url>
    <content><![CDATA[<p><a href="https://tonybai.com/2020/11/04/the-recommend-books-list-for-learning-go/" rel="external nofollow noopener noreferrer" target="_blank">Tony Bai</a></p>
<p>一个程序员的心路历程</p>
<ol>
<li>Go语言的发展现状<br>如果从2007年9月20日那个下午三个“程序员大佬”在谷歌总部的一间办公室里进行的一次有关设计一门新编程语言的讨论算起，那么Go语言已经度过了自己的13个年头了。</li>
</ol>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2d81729a32d793d6a63a5a0ed77b953f.png" alt></p>
<p>Robert Griesemer、Rob Pike和Ken Thompson</p>
<p>如果从2009年11月10日Go语言正式开源发布算起，Go语言也即将迎来自己的第11个生日。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a3f50e7cef18ba25e61ae414d65564a1.png" alt></p>
<p>2020年，Go联合创始人Rob Pike在专访中也认可了Go确实已成为云基础架构的语言。在Go即将迎来自己的11个生日的时候，Hacker News有人发起了“Go已超过10岁了，你觉得这门语言如何？”的提问，收到了广泛的关注和回答。国内媒体将这些问答整理后得到的结论是：“人生苦短，我要换Go”。</p>
<p>Stackoverflow官博11月2日发表的《Go语言有哪些优点？探讨导致Go语言日益流行的特征 》一文对Go语言的发展趋势描述的贴切：Go语言就像爬行的藤蔓，虽缓慢，但却逐渐占据了开发世界。它正以一种郁郁葱葱的并且在许多方面都很优越的编程能力覆盖着在它之前出现的所有事物。<br><img src="https://img-blog.csdnimg.cn/img_convert/c2b459705344a2ba7ed375c997b7a2c1.png" alt></p>
<p>不管你是否承认，Go在IT就业市场已经成为事实上的“香饽饽”之一，就像一贯不激进的慕课网也在今年双11打出了下面的专题：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d08c494d772be129938ee4eef4f1be47.png" alt></p>
<p>上车，任何时间都不晚！ 那么怎么才能踏上Go这一强大且稳健前行的车呢？和其他主流编程语言一样，上车的必经之路：看书！</p>
<ol start="2">
<li>市面上的Go书籍为何这么少<br>和C、C++、Java、Python等编程语言在市面上的书籍数量相比，Go流行于市面（大陆）上的图书似乎少了很多。其原因笔者觉得有如下几点：</li>
</ol>
<p>1) 年轻</p>
<p>我们来看看上述几门主流编程语言的诞生时间：</p>
<p>java 1995</p>
<p>c 1972</p>
<p>c++ 1983</p>
<p>python 1991</p>
<p>对于很多IT从业者来说，这些语言诞生的时候他们还没出生呢。而2009年末才正式发布的Go和“最年轻”的java之间还有14年的“年龄差”。</p>
<p>Go在国内真正开始快速流行起来大致在2015年第一届GopherChina大会(2015.4月)之后，当时的Go是1.4版本)。同一年下半年发布的Go 1.5实现自举并让GC延迟大幅下降，这引爆了Go在国内的流行。一批又一批程序员成为Gopher，在大厂、初创实践着Go语言。但知识和技能的沉淀和总结需要时间，相信再有5年，国内作者出版的Go语言相关书籍会像雨后春笋版出现在大家的书架上。</p>
<p>2）以品类代名词的身份占据的“领域”还少</p>
<p>提到Web，人们想到的是Java spring；提到深度学习、机器学习、人工智能，人们想到的是python和tensorflow；提到比特币，嵌入式，人们想到的是C；提到游戏，人们想到的是C++；提到前端，人们想到的是Javascript。这些语言在这些垂直领域早早以杀手级框架入场，使得它们成为了这一领域的“品类代名词”，因此与该垂直领域相关的技术书籍都会采用作为该领域“品类代名词”的编程语言编写书中示例等，这样的书也就会被归类为这类语言方面的书籍。</p>
<p>Go语言诞生晚，入场也较晚。Go虽然通过缓慢的“爬行”，覆盖了一些领域并占据优势地位，但还不能说已经成为了该领域的“品类代名词”，比如：云原生、API、微服务、区块链等，因此被垂直领域书籍关联的机会也不像上面那几门语言多。</p>
<p>同时，由于Go“自带电池”，基于Go标准库我们可以实现大部分功能特性，无需依赖过多框架。即便依赖框架，框架本身也不复杂，很少以“某某框架”为主题编写一本技术书籍，这方面远远无法媲美Java和Spring这对“黄金组合”。</p>
<p>3) 引进国外优秀作品需要时间</p>
<p>相对于国内，国外关于Go语言的作品要多不少，但引进国外图书资料需要时机以及时间(找译者翻译)。</p>
<ol start="3">
<li>系统学习Go语言的书籍列表TOP 5<br>笔者接触Go语言较早，Go语言相关的中外文书籍几乎都通读过一遍（经典好书读过可不止一遍哦）。Go语言比较简单，如果单单从系统掌握这门语言的角度来看，阅读下面基本书籍就足够了。如果你要学习某些垂直领域的Go应用和技巧，那么期待我后续对垂直领域Go书籍/资料的推荐吧^_^。</li>
</ol>
<p>这里参考“天下足球”TOP10栏目的方式推荐我心目中掌握Go语言必读的五大好书（每项满分为5分）！</p>
<p>第五名：<a href="https://go101.org/article/101.html" rel="external nofollow noopener noreferrer" target="_blank">The Way To Go</a> – Go语言百科全书</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/dee54cacbb43cb45f673f6f65a73122c.png" alt></p>
<p>《The Way To Go》是我早期学习Go语言时最喜欢翻看的一本书。该书成书于2012年3月，恰逢Go 1.0版本刚刚发布，作者承诺书中代码均可在Go 1.0版本上编译通过并运行。该书分为4个部分：</p>
<p>为什么学习Go以及Go环境安装入门</p>
<p>Go语言核心语法</p>
<p>Go高级用法（读写、错误处理、单元测试、并发编程、socket与web编程等)</p>
<p>Go应用(常见陷阱、语言应用模式、从性能考量的代码编写建议、现实中的Go应用等)</p>
<p>每部分的每个章节都很精彩，这本书也是目前见到的最全面详实的讲解Go语言的书籍了，我称之为Gopher们的第一本“Go百科全书”。</p>
<p>该书作者Ivo Balbaert想必大多数人都不曾耳闻。为了写本文，我特地研究了一下他的作品以及出版时间，发现这个技术作者是很会“抢先机”并且眼光独到。他总是能发现市面刚出现不久但却很有潜力的编程语言并在其他人了解该门语言之前，就编写出类似“The way to Go”这样的为早期语言接纳者提供的详实资料，包括Julia，Rust等。在很多人还不知道这些语言名字的时候，他就已经开始学习这些语言，并为这些语言编写出质量很高的“百科全书”式的书籍。</p>
<p>很遗憾，这本书没有中文版。这可能是由于本书出版太早，等国内出版社意识到要引进Go语言方面的书籍时，这本书使用的Go版本又太老了，虽然本书中绝大部分例子依然可以在今天最新的Go编译器下通过编译并运行起来。不过无闻在github上发起了这本书的中译版项目：<a href="https://github.com/Unknwon/the-way-to-go_ZH_CN，感兴趣的gopher可以去在线或下载阅读。" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/Unknwon/the-way-to-go_ZH_CN，感兴趣的gopher可以去在线或下载阅读。</a><br>此书虽棒，但毕竟年头“久远”，我只能委屈它一下了，将它列在第五位，下面是其各个指数的评分：</p>
<p>作者名气指数：3</p>
<p>关注度指数：3</p>
<p>内容实用指数：4</p>
<p>经典指数：4</p>
<p>总分：14</p>
<p>第四名：<a href="https://go101.org/article/101.html" rel="external nofollow noopener noreferrer" target="_blank">Go 101</a> – Go语言规范全方位解读</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/46abe81b412b306022771d99dfe92b47.png" alt></p>
<p>这是一本在国外人气和关注度比在国内高的中国人编写的英文书，当然也是有中文版的。</p>
<p>如果仅从书名中的101去判断，你很大可能会认为这仅仅是一本讲解Go入门基础的书，但这本书的内容可远远不止入门这么简单。这本书可大致分为三个部分：</p>
<p>Go语法基础</p>
<p>Go类型系统与运行时实现</p>
<p>以专题(topic)形式阐述的Go特性、技巧与实践模式</p>
<p>除了第一部分算是101范畴，其余两个部分都是Go语言的高级话题，也是要精通Go必须要掌握的“知识点”。并且，结合Go语言规范，作者对每个知识点的阐述都细致入微并结合大量示例辅助说明。我们知道有关C和C++语言，市面上有一些由语言作者或标准规范委员会成员编写的annotated或rationale书籍（语言参考手册或标准解读），Go 101这本书也可以理解为Go语言的标准解读或参考手册。</p>
<p>Go 101这本书是<a href="https://github.com/go101/go101" rel="external nofollow noopener noreferrer" target="_blank">开源电子书</a>，其作者也在国外一些支持自出版的服务商那里做了付费数字出版。这使得这本书相对于其他纸板书有着另外一个优势：与时俱进。在作者的不断努力下，该书的知识点更新基本保持与Go的演化同步，目前其内容已经覆盖了最新的Go 1.15版本。</p>
<p>该书作者为国内资深工程师老貘，他花费三年时间“呕心沥血”完成此书并免费奉献给Go社区，值得大家为其大大的点赞！<br>下面是本书推荐指数的评分：</p>
<p>作者名气指数：3</p>
<p>关注度指数：4</p>
<p>内容实用指数：4</p>
<p>经典指数：4</p>
<p>总分：15</p>
<p>第三名：<a href="https://book.douban.com/subject/26832468/" rel="external nofollow noopener noreferrer" target="_blank">Go语言学习笔记</a> – Go源码剖析与实现原理探索</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/92d1b23f701996ce280a7d93fe6fb366.png" alt></p>
<p>这是一本在国内影响力很大和关注度较高的作品。一来其作者雨痕老师是国内资深工程师，也是2015年第一届GopherChina大会讲师；二来，该作品的前期版本是以开源电子书的形式风险给国内Go社区的；三来，作者在Go源码剖析方便可谓之条理清晰，细致入微。</p>
<p>2016年《Go语言学习笔记》纸版书出版，该书覆盖了当时最新的Go 1.5版本，Go 1.5版本在Go语言演化历史中的分量极高，它不仅实现了Go自举，还让Go GC的延迟下降到绝大多数应用可以将其应用到生产的程度。本书整体上分为两大部分：</p>
<p>Go语言详解：以短平快、捞干的来的风格对Go语言语法做了说明，能用示例说明的，绝不用文字做过多修饰。</p>
<p>Go源码剖析：这是本书精华，也是最受Gopher关注的部分。这部分对Go运行时神秘的内存分配、垃圾回收、并发调度、channel和defer的实现原理、syn.Pool的实现原理做了细致的源码剖析与原理总结。</p>
<p>随着Go语言演化，其语言和运行时实现一直在变化，但Go 1.5版本的实现是后续版本的基础，因此这本书的剖析非常值得每位Gopher阅读。从雨痕老师的github上最新消息来看，他似乎在编写新版Go语言学习笔记，基于Go 1.12版本，剖析源码是枯燥繁琐的，期待新版Go学习笔记早日与Gopher们见面。<br>下面是本书各个指数的评分：</p>
<p>作者名气指数：4</p>
<p>关注度指数：4</p>
<p>内容实用指数：4</p>
<p>经典指数：4</p>
<p>总分：16</p>
<p>第二名：<a href="https://book.douban.com/subject/27015617/" rel="external nofollow noopener noreferrer" target="_blank">Go语言实战</a> – 实战系列(in action)经典之作，紧扣Go语言的精华</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/300c28b52bbcd5301b2c7cadbd20dde7.png" alt></p>
<p>Manning出版社出版的“实战系列(xx in action)”一直是程序员心中高质量和经典的代名词。在出版Go语言实战方面，该出版社也是丝毫不敢怠慢，邀请了Go社区知名的三名明星级作者联合撰写了该书的内容。这三位作者分别是：</p>
<p>威廉·肯尼迪 (William Kennedy) – 知名Go培训师，培训机构Ardan Labs的联合创始人，”Ultimate Go”培训的策划实施者。</p>
<p>布赖恩·克特森 (Brian Ketelsen) – 世界上最知名的Go技术大会 – GopherCon大会的联合发起人和组织者，GopherAcademy创立者，现微软Azure工程师</p>
<p>埃里克·圣马丁 (Erik St.Martin) – 世界上最知名的Go技术大会 – GopherCon大会的联合发起人和组织者</p>
<p>本书并不是大部头，而是薄薄的一本（中文版才200多页），因此你不要期望从本书得到百科全书一样的阅读感。本书的作者们显然也没有想将其写成面面俱到的作品，而是直击要点，即挑出Go语言和其他语言相比与众不同的特点进行着重讲解，这些特点构成了本书的结构框架：</p>
<p>入门：快速上手搭建、编写、运行一个go程序</p>
<p>语法：数组(作为一个类型而存在)、切片和map</p>
<p>Go类型系统的与众不同：方法、接口、嵌入类型</p>
<p>Go的拿手好戏：并发及并发模式</p>
<p>标准库常用包：log、marshal/unmarshal、io(Reader和Writer)</p>
<p>原生支持的测试</p>
<p>读完这本书，你就掌握了Go语言的精髓之处，这迎合了多数gopher的内心需求。本书中文版译者Googol Lee也是Go圈子里的资深gopher，翻译质量上乘。</p>
<p>下面对本书各个指数的评分：</p>
<p>作者名气指数：5</p>
<p>关注度指数：5</p>
<p>内容实用指数：4</p>
<p>经典指数：4</p>
<p>总分：18</p>
<p>第一名：<a href="https://img-blog.csdnimg.cn/img_convert/300c28b52bbcd5301b2c7cadbd20dde7.png" rel="external nofollow noopener noreferrer" target="_blank">Go程序设计语言</a> – 人手一本的Go语言“圣经”</p>
<p>如果说由Brian W. Kernighan和Dennis M. Ritchie联合编写的《The C Programming Language》(也称K&amp;R C)是C程序员(甚至是所有程序员)心目中的“圣经”的话，</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f1223e4509e71c02bef10bbf2b4b161e.png" alt></p>
<p>那么同样由Brian W. Kernighan(K)参与编写的<a href="https://book.douban.com/subject/26337545/" rel="external nofollow noopener noreferrer" target="_blank">The Go Programming Language</a>（也称tgpl）就是Go程序员心目中的“圣经”。<br><img src="https://img-blog.csdnimg.cn/img_convert/ebed51aa6c4dbe1eb3a272035b268e1b.png" alt><br>本书模仿并致敬“The C Programming Language”的经典结构，从一个”hello, world”示例开始带领大家开启Go语言之旅。第二章程序结构是Go语言这个“游乐园”的向导图，了解它之后，我们就会迫不及待地奔向各个“景点”细致参观。Go语言规范中的所有“景点”在本书中都被覆盖到了，并且由浅入深，循序渐进：从基础数据类型到复合数据类型、从函数、方法到接口、从创新的并发goroutine到传统的基于共享变量的并发，从包、工具链到测试，从反射到低级编程(unsafe包)。作者行文十分精炼，字字珠玑，这与《The C Programming Language》的风格保持了高度的一致。书中的示例在浅显易懂的同时，又极具实用性并突出Go语言的特点（比如：并发web爬虫、并发非阻塞缓存等）。</p>
<p>读完本书后，你会有一种爱不释手，马上还要从头再读一遍的感觉，也许这就是“圣经”的魅力！</p>
<p>本书出版于2015年10月26日，也是既当年中旬Go 1.5这个里程碑版本发布后，Go社区的又一重大历史事件！并且Brian W. Kernighan老爷子的影响力让更多程序员加入到Go阵营，这也或多或少促成了Go成为下一个年度，即2016年年度TIOBE最佳编程语言。能得到Brian W. Kernighan老爷子青睐的编程语言只有C和Go，这也是Go的幸运。当然了如果老爷子是被Rob Pike或Ken Thompson通过私人关系邀请写书的，那就另当别论了，当然这纯属臆测，别当真^_^。</p>
<p>这本书的另一名作者Alan A. A. Donovan也并非等闲之辈，他是Go核心开发团队的成员，专注于Go工具链方面的开发。</p>
<p>现在唯一遗憾的是Brian W. Kernighan老爷子年事已高，不知道Go加入泛型后老爷子是否还有精力更新这本圣经。</p>
<p>该书中文版由七牛团队翻译，总体质量是不错的。建议Gopher们人手购置一本圣经“供奉”起来！^_^</p>
<p>下面对本书各个指数的评分：</p>
<p>作者名气指数：5</p>
<p>关注度指数：5</p>
<p>内容实用指数：5</p>
<p>经典指数：5</p>
<p>总分：20</p>
<ol start="4">
<li>小结<br>Go书籍绝非“汗牛充栋”，预计Go增加泛型表达力增强后，市面上会有更多的技术书籍出炉。上面的某些经典也许还会出新版。而市面上Go书籍不多从另外一角度也可以理解成Go语言在国内还有巨大的发展空间与潜力。</li>
</ol>
<p>努力吧，Gopher们！</p>
<p>只有写书者，才能体会到写者的艰辛！Go专栏：<a href="https://www.imooc.com/read/87" rel="external nofollow noopener noreferrer" target="_blank">改善Go语言编程质量的50个有效实践</a>也是我努力了一年多才打磨雕琢出来的心血之作。自从上线后，收到大家的热烈关注和好评！现在恰逢双11慕课大促，欢迎有意愿在Go这条技术路线上进阶的朋友们订阅，在学习过程中欢迎随时反馈和交流！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c2f91cbacd5b6adc77947b8d495c6bf4.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go高效内存分配</title>
    <url>/go/go%E9%AB%98%E6%95%88%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D/</url>
    <content><![CDATA[<p><a href="https://segment.com/blog/allocation-efficiency-in-high-performance-go-services/" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<p>手动内存管理真的很坑爹(如C C++)，好在我们有强大的自动化系统能够管理内存分配和生命周期，从而解放我们的双手。</p>
<p>但是呢，如果你想通过调整JVM垃圾回收器参数或者是优化go代码的内存分配模式话来解决问题的话，这是远远不够的。自动化的内存管理帮我们规避了大部分的错误，但这只是故事的一半。我们必须要合理有效构建我们的软件，这样垃圾回收系统可以有效工作。</p>
<p>在构建高性能go服务Centrifuge时我们学习到的内存相关的东西，在这里进行分享。Centrifuge每秒钟可以处理成百上千的事件。Centrifuge是Segment公司基础设施的关键部分。一致性、行为可预测是必须的。整洁、高效和精确的使用内存是实现一致性的重要部分。</p>
<p>这篇文章，我们将介绍导致低效率和与内存分配相关的生产意外的常见模式，以及消除这些问题的实用方法。我们会专注于分配器的核心机制，为广大开发人员提供一种处理内存使用的方法。</p>
<h1 id="使用工具"><a href="#使用工具" class="headerlink" title="使用工具"></a><strong><strong>使用工具</strong></strong></h1><p>首先我们建议的是避免过早进行优化。Go提供了出色的分析工具，能够直接指向内存分配密集的代码部分。没有必要重新造轮子，我们直接参考Go官方这篇文章即可。它为使用pprof进行CPU和分配分析提供了可靠的demo。我们在Segment中用于查找生产Go代码中的瓶颈的工具就是它，学会使用pprof是基本要求。</p>
<p>另外，使用数据去推动你的优化。</p>
<p><strong># </strong>逃逸分析<strong> #</strong><br>Go能够自动管理内存分配。这可以防止一大类潜在错误，但是不能说完全不去了解分配的机制。</p>
<p>首先要记住一点：<strong>栈分配是很廉价的而堆分配代价是昂贵的。我们来看一下具体含义。</strong></p>
<p>Go在两个地方分配内存：用于动态分配的全局堆，以及用于每个goroutine的局部栈。Go偏向于在栈中分配—-大多数go程序的分配都是在栈上面的。栈分配很廉价，因为它只需要两个CPU指令：一个是分配入栈，另一个是栈内释放。</p>
<p>但是不幸的是，不是所有数据都能使用栈上分配的内存。栈分配要求可以在编译时确定变量的生存期和内存占用量。然而堆上的动态分配发生在运行时。malloc必须去找一块儿足够大的空闲内存来保存新值。然后垃圾收集器扫描堆以查找不再引用的对象。毫无疑问，它比堆栈分配使用的两条指令要贵得多。</p>
<p>编译器使用逃逸分析技术去选择堆或者栈。基本思想是在编译时期进行垃圾收集工作。编译器追踪代码域变量的作用范围。它使用追踪数据来检查哪些变量的生命周期是完全可知的。如果变量通过这些检查，则可以在栈上进行分配。如果没通过，也就是所说的逃逸，则必须在堆上分配。</p>
<p>go语言里没有明确说明逃逸分析规则。对于Go程序员来说，最直接去了解规则的方式就是去实验。通过构建时候加上go build -gcflags ‘-m’，可以看到逃逸分析结果。我们看一个例子。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">        x := <span class="number">42</span></span><br><span class="line">        fmt.Println(x)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build -gcflags <span class="string">'-m'</span> ./main.go</span><br><span class="line"><span class="comment"># command-line-arguments</span></span><br><span class="line">./main.go:7: x escapes to heap</span><br><span class="line">./main.go:7: main ... argument does not escape</span><br></pre></td></tr></table></figure>
<p>我们这里看到变量x“逃逸到堆上”，因为它是在运行时期动态在堆上分配的。这个例子可能有点困惑。我们肉眼看上去，显然x变量在main()方法上不会逃逸。编译器输出并没有解释为什么它会认为变量逃逸了。为了看到更多细节，再加上一个-m参数，可以看到更多输出<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build -gcflags <span class="string">'-m -m'</span> ./main.go</span><br><span class="line"><span class="comment"># command-line-arguments</span></span><br><span class="line">./main.go:5: cannot inline main: non-leaf <span class="keyword">function</span></span><br><span class="line">./main.go:7: x escapes to heap</span><br><span class="line">./main.go:7:         from ... argument (arg to ...) at ./main.go:7</span><br><span class="line">./main.go:7:         from *(... argument) (indirection) at ./main.go:7</span><br><span class="line">./main.go:7:         from ... argument (passed to call[argument content escapes]) at ./main.go:7</span><br><span class="line">./main.go:7: main ... argument does not escape</span><br></pre></td></tr></table></figure></p>
<p>这说明，x逃逸是因为它被传入一个方法参数里，这个方法参数自己逃逸了。后面可以看到更多这种情况。</p>
<p>规则可能看上去是随意的，经过工具的尝试，一些规律显现出来。这里列出了一些典型的导致逃逸的情况：</p>
<ol>
<li><strong>发送指针或者是带有指针的值到channel里。</strong>编译时期没有办法知道哪个goroutine会受到channel中的数据。因此编译器无法确定这个数据什么时候不再被引用到。</li>
<li><strong>在slice中存储指针或者是带有指针的值。</strong>这种情况的一个例子是[]*string。它总会导致slice中的内容逃逸。尽管切片底层的数组还是在堆上，但是引用的数据逃逸到堆上了。</li>
<li><strong>slice底层数组由于append操作超过了它的容量，它会重新分片内存</strong>。如果在编译时期知道切片的初始大小，则它会在栈上分配。如果切片的底层存储必须被扩展，数据在运行时才获取到。则它将在堆上分配。</li>
<li><strong>在接口类型上调用方法。</strong>对接口类型的方法调用是动态调用–接口的具体实现只有在运行时期才能确定。考虑一个接口类型为io.Reader的变量r。对r.Read(b)的调用将导致r的值和byte slice b的底层数组都逃逸，因此在堆上进行分配。<br>以我们的经验来讲，这四种情况是Go程序中最常见的动态分配情况。对于这些情况还是有一些解决方案的。接下来，我们将深入探讨如何解决生产软件中内存低效问题的一些具体示例。</li>
</ol>
<p><strong># 指针相关 #</strong><br>经验法则是：指针指向堆上分配的数据。 因此，减少程序中指针的数量会减少堆分配的数量。 这不是公理，但我们发现它是现实世界Go程序中的常见情况。</p>
<p>我们直觉上得出的一个常见的假设是这样的：“复制值代价是昂贵的，所以我会使用指针。”然而在许多情况下，复制值比使用指针的开销要便宜的多。你可能会问这是为什么。</p>
<ol>
<li>在解引用一个指针的时候，编译器会生成检查。它的目的是，如果指针是nil的话，通过运行panic()来避免内存损坏。这部分额外代码必须在运行时去运行。如果数据按值传递，它不会是nil。</li>
<li>指针通常具有较差的引用局部性。函数中使用的所有值都在并置在堆栈内存中。引用局部性是代码高效的一个重要方面。它极大增加了变量在CPU caches中变热的可能性，并降低了预取时候未命中风险。</li>
<li>复制缓存行中的对象大致相当于复制单个指针。 CPU在缓存层和主存在常量大小的缓存行上之间移动内存。 在x86上，cache行是64个字节。 此外，Go使用一种名为Duff`s devices的技术，使拷贝等常见内存操作非常高效。<br>指针应主要用于反映成员所有关系以及可变性。实际中，使用指针避免复制应该是不常见的。不要陷入过早优化陷阱。按值传递数据习惯是好的，只有在必要的时候才去使用指针传递数据。另外，值传递消除了nil从而增加了安全性。</li>
</ol>
<p>减少程序中指针的数量可以产生另一个有用的结果，因为垃圾收集器将跳过不包含指针的内存区域。例如，根本不扫描返回类型为[]byte 的切片的堆区域。对于不包含任何具有指针类型字段的结构类型数组，也同样适用。</p>
<p>减少指针不仅减少垃圾回收的工作量，还会生存出”cache友好“的代码。读取内存会将数据从主存移到CPU cache中。Caches是优先的，因此必须清掉一些数据来腾出空间。cache清掉的数据可能会和程序的其它部分相关。由此产生的cache抖动可能会导致不可预期行为和突然改变生产服务的行为。</p>
<p><strong>指针深入</strong><br>减少指针使用通常意需要味着深入研究用于构建程序的类型的源代码。我们的服务Centrifuge保留了一个失败操作队列，来作为重试循环缓冲区去进行重试，它包含一组如下所示的数据结构：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> retryQueue <span class="keyword">struct</span> &#123;</span><br><span class="line">    buckets       [][]retryItem <span class="comment">// each bucket represents a 1 second interval</span></span><br><span class="line">    currentTime   time.Time</span><br><span class="line">    currentOffset <span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">type</span> retryItem <span class="keyword">struct</span> &#123;</span><br><span class="line">    id   ksuid.KSUID <span class="comment">// ID of the item to retry</span></span><br><span class="line">    time time.Time   <span class="comment">// exact time at which the item has to be retried</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>数组buckets的外部大小是一个常量值，但是[]retryItem所包含的items会在运行时期改变。重试次数越多，这些slices就变越大。</p>
<p>深入来看一下retryItem细节，我们了解到KSUID是一个[20]byte的同名类型，不包含指针，因此被逃逸规则排除在外。currentOffset是一个int值，是一个固定大小的原始值，也可以排除。下面看一下，time.Time的实现：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Time <span class="keyword">struct</span> &#123;</span><br><span class="line">    sec  <span class="keyword">int64</span></span><br><span class="line">    nsec <span class="keyword">int32</span></span><br><span class="line">    loc  *Location <span class="comment">// pointer to the time zone structure</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>time.Time结构内部包含一个loc的指针。在retryItem内部使用它导致了在每次变量通过堆区域时候，GC都会去标记struct上的指针。</p>
<p>我们发现这是在不可预期情况下级联效应的典型情况。通常情况下操作失败是很少见的。只有小量的内存去存这个retries的变量。当失败操作激增，retry队列会每秒增加到上千个，这会大大增加垃圾回收器的工作量。</p>
<p>对于这种特殊使用场景，time.Time的time信息其实是不必要的。这些时间戳存在内存中，永远不会被序列化。可以重构这些数据结构以完全避免time类型出现。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> retryItem <span class="keyword">struct</span> &#123;</span><br><span class="line">    id   ksuid.KSUID</span><br><span class="line">    nsec <span class="keyword">uint32</span></span><br><span class="line">    sec  <span class="keyword">int64</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(item *retryItem)</span> <span class="title">time</span><span class="params">()</span> <span class="title">time</span>.<span class="title">Time</span></span> &#123;</span><br><span class="line">    <span class="keyword">return</span> time.Unix(item.sec, <span class="keyword">int64</span>(item.nsec))</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">makeRetryItem</span><span class="params">(id ksuid.KSUID, time time.Time)</span> <span class="title">retryItem</span></span> &#123;</span><br><span class="line">    <span class="keyword">return</span> retryItem&#123;</span><br><span class="line">        id:   id,</span><br><span class="line">        nsec: <span class="keyword">uint32</span>(time.Nanosecond()),</span><br><span class="line">        sec:  time.Unix(),</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>现在retryItem不包含任何指针。这样极大的减少了垃圾回收器的工作负载，编译器知道retryItem的整个足迹。</p>
<p><strong>请给我传切片(Slice)</strong><br>slice使用很容易会产生低效分配代码。除非编译器知道slice的大小，否则slice(和maps)的底层数组会分配到堆上。我们来看一下一些方法，让slice在栈上分配而不是在堆上。</p>
<p>Centrifuge集中使用了Mysql。整个程序的效率严重依赖了Mysql driver的效率。在使用pprof去分析了分配行为之后，我们发现Go MySQL driver代码序列化time.Time值的代价十分昂贵。</p>
<p>分析器显示大部分堆分配都在序列化time.Time的代码中。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/9934da48507d4581f83a65f4db0f4d4f.png" alt></p>
<p>相关代码在调用time.Time的Format这里，它返回了一个string。等会儿，我们不是在说slices么？好吧，根据Go官方文档，一个string其实就是个只读的bytes类型slices，加上一点额外的语言层面的支持。大多数分配规则都适用！</p>
<p>分析数据告诉我们大量分配，即12.38%都产生在运行的这个Format方法里。这个Format做了些什么？</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/9934da48507d4581f83a65f4db0f4d4f.png" alt></p>
<p>事实证明，有一种更加有效的方式来做同样的事情。虽然Format()方法方便容易，但是我们使用AppendFormat()在分配器上会更轻松。观察源码库，我们注意到所有内部的使用都是AppendFormat()而非Format()，这是一个重要提示，AppendFormat()的性能更高。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5b5b7511bf51b5445fee061c201b666e.png" alt></p>
<p>实际上，Format方法仅仅是包装了一下AppendFormat方法：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(t Time)</span> <span class="title">Format</span><span class="params">(layout <span class="keyword">string</span>)</span> <span class="title">string</span></span> &#123;</span><br><span class="line">          <span class="keyword">const</span> bufSize = <span class="number">64</span></span><br><span class="line">          <span class="keyword">var</span> b []<span class="keyword">byte</span></span><br><span class="line">          max := <span class="built_in">len</span>(layout) + <span class="number">10</span></span><br><span class="line">          <span class="keyword">if</span> max &lt; bufSize &#123;</span><br><span class="line">                  <span class="keyword">var</span> buf [bufSize]<span class="keyword">byte</span></span><br><span class="line">                  b = buf[:<span class="number">0</span>]</span><br><span class="line">          &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                  b = <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">0</span>, max)</span><br><span class="line">          &#125;</span><br><span class="line">          b = t.AppendFormat(b, layout)</span><br><span class="line">          <span class="keyword">return</span> <span class="keyword">string</span>(b)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>更重要的是，AppendFormat()给程序员提供更多分配控制。传递slice而不是像Format()自己在内部分配。相比Format，直接使用AppendFormat()可以使用固定大小的slice分配，因此内存分配会在栈空间上面。</p>
<p>可以看一下我们给Go MySQL driver提的这个PR</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e366b7b3992e86e8c382accbb48d8918.png" alt></p>
<p>首先注意到var a [64]byte是一个大小固定的数组。编译期间我们知道它的大小，以及它的作用域仅在这个方法里，所以我们知道它会被分配在栈空间里。</p>
<p>但是这个类型不能传给AppendFormat()，该方法只接受[]byte类型。使用a[:0]的表示法将固定大小的数组转换为由此数组所支持的b表示的切片类型。这样可以通过编译器检查，并且会在栈上面分配内存。</p>
<p>更关键的是，AppendFormat()，这个方法本身通过编译器栈分配检查。而之前版本Format()，编译器不能确定需要分配的内存大小，所以不满足栈上分配规则。</p>
<p>这个小的改动大大减少了这部分代码的堆上分配！类似于我们在MySQL驱动里使用的“附加模式”。在这个PR里，KSUID类型使用了Append()方法。在热路径代码中，KSUID使用Append()模式处理大小固定的buffer而不是String()方法，节省了类似的大量动态堆分配。 另外值得注意的是，strconv包使用了相同的append模式，用于将包含数字的字符串转换为数字类型。</p>
<h1 id="接口类型"><a href="#接口类型" class="headerlink" title="接口类型"></a>接口类型</h1><p>众所周知，接口类型上进行方法调用比struct类型上进行方法调用要昂贵的多。接口类型的方法调用通过动态调度执行。这严重限制了编译器确定代码在运行时执行方式的能力。到目前为止，我们已经在很大程度上讨论了类型固定的代码，以便编译器能够在编译时最好地理解它的行为。 接口类型抛弃了所有这些规则！</p>
<p>不幸的是接口类型在抽象层面非常有用 — 它可以让我们写出更加灵活的代码。程序里常用的热路径代码的相关实例就是标准库提供的hash包。hash包定义了一系列常规接口并提供了几个具体实现。我们看一个例子。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">        <span class="string">"fmt"</span></span><br><span class="line">        <span class="string">"hash/fnv"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">hashIt</span><span class="params">(in <span class="keyword">string</span>)</span> <span class="title">uint64</span></span> &#123;</span><br><span class="line">        h := fnv.New64a()</span><br><span class="line">        h.Write([]<span class="keyword">byte</span>(in))</span><br><span class="line">        out := h.Sum64()</span><br><span class="line">        <span class="keyword">return</span> out</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">        s := <span class="string">"hello"</span></span><br><span class="line">        fmt.Printf(<span class="string">"The FNV64a hash of '%v' is '%v'\n"</span>, s, hashIt(s))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>构建检查逃逸分析结果：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./foo1.go:9:17: inlining call to fnv.New64a</span><br><span class="line">./foo1.go:10:16: ([]byte)(<span class="keyword">in</span>) escapes to heap</span><br><span class="line">./foo1.go:9:17: hash.Hash64(&amp;fnv.s·2) escapes to heap</span><br><span class="line">./foo1.go:9:17: &amp;fnv.s·2 escapes to heap</span><br><span class="line">./foo1.go:9:17: moved to heap: fnv.s·2</span><br><span class="line">./foo1.go:8:24: hashIt <span class="keyword">in</span> does not escape</span><br><span class="line">./foo1.go:17:13: s escapes to heap</span><br><span class="line">./foo1.go:17:59: hashIt(s) escapes to heap</span><br><span class="line">./foo1.go:17:12: main ... argument does not escape</span><br></pre></td></tr></table></figure></p>
<p>也就是说，hash对象，输入字符串，以及代表输入的[]byte全都会逃逸到堆上。我们肉眼看上去显然不会逃逸，但是接口类型限制了编译器。不通过hash包的接口就没有办法安全地使用具体的实现。 那么效率相关的开发人员应该做些什么呢？</p>
<p>我们在构建Centrifuge的时候遇到了这个问题，Centrifuge在热代码路径对小字符串进行非加密hash。因此我们建立了fasthash库。构建它很直接，困难工作依旧在标准库里做。fasthash只是在没有使用堆分配的情况下重新打包了标准库。</p>
<p>直接来看一下fasthash版本的代码<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">        <span class="string">"fmt"</span></span><br><span class="line">        <span class="string">"github.com/segmentio/fasthash/fnv1a"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">hashIt</span><span class="params">(in <span class="keyword">string</span>)</span> <span class="title">uint64</span></span> &#123;</span><br><span class="line">        out := fnv1a.HashString64(in)</span><br><span class="line">        <span class="keyword">return</span> out</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">        s := <span class="string">"hello"</span></span><br><span class="line">        fmt.Printf(<span class="string">"The FNV64a hash of '%v' is '%v'\n"</span>, s, hashIt(s))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>看一下逃逸分析输出<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./foo2.go:9:24: hashIt <span class="keyword">in</span> does not escape</span><br><span class="line">./foo2.go:16:13: s escapes to heap</span><br><span class="line">./foo2.go:16:59: hashIt(s) escapes to heap</span><br><span class="line">./foo2.go:16:12: main ... argument does not escape</span><br></pre></td></tr></table></figure></p>
<p>唯一产生的逃逸就是因为fmt.Printf()方法的动态特性。尽管通常我们更喜欢是用标准库，但是在一些情况下需要进行权衡是否要提高分配效率。</p>
<p><strong>一个小窍门</strong><br>我们最后这个事情，不够实际但是很有趣。它有助我们理解编译器的逃逸分析机制。 在查看所涵盖优化的标准库时，我们遇到了一段相当奇怪的代码。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// noescape hides a pointer from escape analysis.  noescape is</span></span><br><span class="line"><span class="comment">// the identity function but escape analysis doesn't think the</span></span><br><span class="line"><span class="comment">// output depends on the input.  noescape is inlined and currently</span></span><br><span class="line"><span class="comment">// compiles down to zero instructions.</span></span><br><span class="line"><span class="comment">// USE CAREFULLY!</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">noescape</span><span class="params">(p unsafe.Pointer)</span> <span class="title">unsafe</span>.<span class="title">Pointer</span></span> &#123;</span><br><span class="line">    x := <span class="keyword">uintptr</span>(p)</span><br><span class="line">    <span class="keyword">return</span> unsafe.Pointer(x ^ <span class="number">0</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个方法会让传递的指针逃过编译器的逃逸分析检查。那么这意味着什么呢？我们来设置个实验看一下。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">package main</span><br><span class="line"> </span><br><span class="line">import (</span><br><span class="line">        <span class="string">"unsafe"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="built_in">type</span> Foo struct &#123;</span><br><span class="line">        S *string</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">func (f *Foo) String() string &#123;</span><br><span class="line">        <span class="built_in">return</span> *f.S</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="built_in">type</span> FooTrick struct &#123;</span><br><span class="line">        S unsafe.Pointer</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">func (f *FooTrick) String() string &#123;</span><br><span class="line">        <span class="built_in">return</span> *(*string)(f.S)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">func NewFoo(s string) Foo &#123;</span><br><span class="line">        <span class="built_in">return</span> Foo&#123;S: &amp;s&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">func NewFooTrick(s string) FooTrick &#123;</span><br><span class="line">        <span class="built_in">return</span> FooTrick&#123;S: noescape(unsafe.Pointer(&amp;s))&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">func noescape(p unsafe.Pointer) unsafe.Pointer &#123;</span><br><span class="line">        x := uintptr(p)</span><br><span class="line">        <span class="built_in">return</span> unsafe.Pointer(x ^ 0)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">func <span class="function"><span class="title">main</span></span>() &#123;</span><br><span class="line">        s := <span class="string">"hello"</span></span><br><span class="line">        f1 := NewFoo(s)</span><br><span class="line">        f2 := NewFooTrick(s)</span><br><span class="line">        s1 := f1.String()</span><br><span class="line">        s2 := f2.String()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个代码包含两个相同任务的实现：它们包含一个字符串，并使用String()方法返回所持有的字符串。但是，编译器的逃逸分析说明FooTrick版本根本没有逃逸。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./foo3.go:24:16: &amp;s escapes to heap</span><br><span class="line">./foo3.go:23:23: moved to heap: s</span><br><span class="line">./foo3.go:27:28: NewFooTrick s does not escape</span><br><span class="line">./foo3.go:28:45: NewFooTrick &amp;s does not escape</span><br><span class="line">./foo3.go:31:33: noescape p does not escape</span><br><span class="line">./foo3.go:38:14: main &amp;s does not escape</span><br><span class="line">./foo3.go:39:19: main &amp;s does not escape</span><br><span class="line">./foo3.go:40:17: main f1 does not escape</span><br><span class="line">./foo3.go:41:17: main f2 does not escape</span><br></pre></td></tr></table></figure></p>
<p>这两行是最相关的<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./foo3.go:24:16: &amp;s escapes to heap</span><br><span class="line">./foo3.go:23:23: moved to heap: s</span><br></pre></td></tr></table></figure></p>
<p>这是编译器认为NewFoo()<code></code>方法把拿了一个string类型的引用并把它存到了结构体里，导致了逃逸。但是NewFooTrick()方法并没有这样的输出。如果去掉noescape()，逃逸分析会把FooTrick结构体引用的数据移动到堆上。这里发生了什么?<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">noescape</span><span class="params">(p unsafe.Pointer)</span> <span class="title">unsafe</span>.<span class="title">Pointer</span></span> &#123;</span><br><span class="line">    x := <span class="keyword">uintptr</span>(p)</span><br><span class="line">    <span class="keyword">return</span> unsafe.Pointer(x ^ <span class="number">0</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>noescape()方法掩盖了输入参数和返回值直接的依赖关系。编译器不认为p会通过x逃逸，因为uintptr()会产生一个对编译器不透明的引用。内置的uintptr类型的名称会让人相信它是一个真正的指针类型，但是从编译器的视角来看，它只是一个恰好大到足以存储指针的整数。最后一行代码构造并返回了一个看似任意整数的unsafe.Pointer值。</p>
<p>一定要清楚，我们并不推荐使用这种技术。这也是为什么它引用的包叫做unsafe，并且注释里写着USE CAREFULLY!</p>
<p><strong>总结</strong><br>我们来总结一下关键点：</p>
<ol>
<li>不要过早优化！使用数据来驱动优化工作</li>
<li>栈分配廉价，堆分配昂贵</li>
<li>了解逃逸分析的规则能够让我们写出更高效的代码</li>
<li>使用指针几乎不会在栈上分配</li>
<li>性能关键的代码段中寻找提供分配控制的API</li>
<li>在热代码路径里谨慎地使用接口类型</li>
</ol>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>著名建筑师的作品」那些年，那些令人震撼的现代建筑！</title>
    <url>/%E5%85%B4%E8%B6%A3%E7%88%B1%E5%A5%BD/%E8%91%97%E5%90%8D%E5%BB%BA%E7%AD%91%E5%B8%88%E7%9A%84%E4%BD%9C%E5%93%81%E3%80%8D%E9%82%A3%E4%BA%9B%E5%B9%B4%EF%BC%8C%E9%82%A3%E4%BA%9B%E4%BB%A4%E4%BA%BA%E9%9C%87%E6%92%BC%E7%9A%84%E7%8E%B0%E4%BB%A3%E5%BB%BA%E7%AD%91%EF%BC%81/</url>
    <content><![CDATA[<h1 id="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"><a href="#「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！" class="headerlink" title="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></a>「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！</h1><p>以下内容转载自 <a href="https://www.toutiao.com/i6695523214673576452/" rel="external nofollow noopener noreferrer" target="_blank">https://www.toutiao.com/i6695523214673576452/</a></p>
<p>所谓“震撼”一定是内心受到了强烈的冲击，从而引起精神或情绪的剧烈起伏。这种感受远比“惊讶”、“感动”等来的强烈。</p>
<p>能让人感觉“震撼”的建筑一定是因为某些方面做到了当时的极致，让人完全意想不到，如无比的宏伟、奢华、令人脑洞大开、引领时代、……。脱离了“集权”的现代建筑一般不会靠宏伟来取胜，更多的是靠别出心裁、引领时代的理念。</p>
<p>能让人感觉“震撼”的建筑又一定是“<strong>意料之外、情理之中</strong>”的解决方案，而非“哗众取宠”、“昙花一现”的、仅仅让人感觉新奇的“奇奇怪怪”的建筑。</p>
<p>现代主义建筑早期的大师弗兰克·劳埃德·赖特（Frank Lloyd Wright,1867,6,8-1959,4,9）、瓦尔特·格罗皮乌斯（Walter Gropius,1883,5,18-1969,7,5）、密斯·凡·德·罗（Ludwig Mies van der Rohe，1886,3,27－1969,8,17）、 勒· 柯布西耶（Le Corbusier，1887,10,6-1965,8,27）以及阿尔瓦·阿尔托（Alvar Aalto,1898,2,3-1976,5,11）、路易斯·康（LouisI.Kahn，1901，2，20-1974，3，14）等都是建筑从古典过渡到现代的划时代人物，他们的许多带给我们“震撼”作品也是我们耳熟能详的。因此，他们以及同年代的其他著名建筑师的作品就不论述了。</p>
<hr>
<p><strong>美国耶鲁大学贝克特珍本图书馆（1963年竣工）</strong></p>
<p><strong>建筑师：SOM事务所戈登.邦夏（Gordon Bunshaft）</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/98" rel="external nofollow noopener noreferrer" target="_blank">经典重温——耶鲁大学贝内克珍本图书馆 - 建材U选</a>）</p>
<p><strong>透光石材的创新性应用+低调背后的奢华+简洁精致的细节</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/096cd731f421dab8d597931924dfc48f.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3361a1ce45a8c180636843404e6a5cd5.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/1e4c5d3364f29c3bc022665b09b94632.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>美国华盛顿国家美术馆东馆（1978年竣工）</strong></p>
<p><strong>建筑师：贝聿铭</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/101" rel="external nofollow noopener noreferrer" target="_blank">经典重温：40年后回头看——美国国家美术馆东馆 - 建材U选</a>）</p>
<p><strong>超前的美术馆理念+经典、极致的几何+完美的控制+丰富的空间</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/fae2f5936a96b0ac8b35b73de15ca54d.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0186465e3ecc29bd7453f376449ffc34.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/af43da58444f75f543d3cc946d20edd8.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/daa56a1c13a7fbef04e5aed70d043c0a.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><strong>法国巴黎卢浮宫广场扩建（1988年竣工）</strong></p>
<p><strong>建筑师：贝聿铭</strong></p>
<p><strong>古典与现代+极简与繁杂的碰撞与融合</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/10cea725915354eae30e01ebcd415219.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/12108b865c7cbbbfa1ab298ec0748205.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0038da1375a73620bd8f20e42ffa4a5b.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>德国慕尼黑1972年奥运会场馆（1972年竣工）</strong></p>
<p><strong>建筑师：弗雷·奥托(Frei Otto)+ 冈瑟·贝尼什（Gunther Behnisch）</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/127" rel="external nofollow noopener noreferrer" target="_blank">轻盈的索网结构 - 建材U选</a>）</p>
<p><strong>奇妙的轻型结构+现代材料的创新性应用</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c0d279f3da7a25d6eb94c352710b1ad7.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p>图片来自于谷歌地球</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c49ee424f398c38f5ace69ae495df2c9.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/757bb9dc88511225db33edcb243c1e91.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/de6a71aecd340772096ed05c67987526.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2b99a0634fe735b82fc76acaf787de91.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>法国巴黎蓬皮杜国家艺术和文化中心（1977年竣工）</strong></p>
<p><strong>建筑师：伦佐·皮亚诺（Renzo Piano）+理查德·罗杰斯（Richard George Rogers）</strong></p>
<p><strong>颠覆性的、反常规思维的划时代设计</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bcbcce91c66569eb7849811068982b47.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/b8a7f312d778673e0858c869e375ca3e.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/7705c89da4b055c176c1eefbc26e7eb8.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3ec15ec5179cc080e632226d304eb917.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>德国斯图加特新国立美术馆（1983年竣工）</strong></p>
<p><strong>建筑师：詹姆斯.斯特林（James Stirling）</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/60" rel="external nofollow noopener noreferrer" target="_blank">经典重温：30年后回头看——斯图加特新美术馆 - 建材U选</a>）</p>
<p><strong>古典主义与现代建筑的完美融合</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/1872161f82dc67c97e3886a55aa64876.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5b494aa7dd1ae48efa559e175d3034fb.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/344de567c124a90b86e88879ab0a4956.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5125693d9fe332914dd0c8141b8af46a.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/17d50f1ae2a5264c2aa9f57300cbdc68.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>西班牙毕尔巴鄂古根海姆博物馆（1997年竣工）</strong></p>
<p><strong>建筑师：弗兰克·盖里（Frank Owen Gehry）</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/97" rel="external nofollow noopener noreferrer" target="_blank">“奇奇怪怪”的建筑——弗兰克·盖里的几个设计作品 - 建材U选</a>）</p>
<p><strong>让人瞠目结舌的雕塑化建筑形式</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d19c2509e0142f9ea0021db4cdde1fce.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p>图片来自于谷歌地球</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/b9d5a45f2a30d67e28e283f9e58dee1d.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/40bd931ee6faf79732ecafe35d6bc610.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/ea251a20a93464881760f1eafdcf2399.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d7db55f57bf1a36689a80158bf20ac54.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/29b4078559fa1cec57f0770e72d096f6.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>日本仙台媒体中心（2000年竣工）</strong></p>
<p><strong>建筑师：伊东丰雄（Toyo Ito）</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/62" rel="external nofollow noopener noreferrer" target="_blank">结构的异化——仙台媒体中心 - 建材U选</a>）</p>
<p><strong>诗意的结构</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/6a081ef46cfd695490c4075cd823bce0.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/ed73cb9a9578a335635dd41c60fb7487.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5457cde01a35ad942bf85fa6b368db8b.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/82aac187513a6700a96d1e3e321c9bac.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>德国柏林消防警察局（2004年竣工）</strong></p>
<p><strong>建筑师：绍尔布鲁奇&amp;赫顿 （Sauerbruch &amp; hutton）</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/84" rel="external nofollow noopener noreferrer" target="_blank">色彩的盛宴 - 建材U选</a>，<a href="http://www.bml365.com/show/study/detail/63" rel="external nofollow noopener noreferrer" target="_blank">形形色色的印刷玻璃 - 建材U选</a>）</p>
<p><strong>将艳丽色彩发挥到极致的建筑</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/b05b4bfaa84e0e74232ec2ca3262a935.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/869e7789b6a59fe82172e03655bec335.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>德国慕尼黑安联体育场（2005年竣工）</strong></p>
<p><strong>建筑师：赫尔佐格以及德梅隆（Herzog&amp;De Meuron）</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/41" rel="external nofollow noopener noreferrer" target="_blank">建筑中的ETFE薄膜结构 - 建材U选</a>）</p>
<p><strong>材料及构造的创新性使用</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f4e514e394b4c69aef37fb81d37c0b35.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/ff78cd62c406f62195270a33ae76d104.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/9b11160df8b33a6a948cce9108f4fbdc.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/aadcd5b513c8dcb631ca7d43857286a2.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>中国杭州中国美术学院（2007年竣工）</strong></p>
<p><strong>建筑师：王澍</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/82" rel="external nofollow noopener noreferrer" target="_blank">“忘记建筑”——向艺术家学习！ - 建材U选</a>）</p>
<p><strong>跳出建筑师程式化设计思维+如艺术家般追求真实+传统人文精神</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/143d575be5d5f01aa3d7331919710d60.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p>图片来自于谷歌地球</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/8ea1c87f213b53e48267a2bacc0b91bd.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a6ba55c7d2f6054fbb406c77aa5b8232.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a3cd670b6535fdd51d1d3abe60a8a1e6.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/baa27b4e2cc7e99bf460e648791282ea.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/42a963dc473b94fc87425fe82c99e8a9.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c0347a7a3a4536729a503e708faa5328.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>2010中国上海世博会英国馆（2010年竣工）</strong></p>
<p><strong>建筑师：托马斯·赫斯维克（Thomas Heatherwick）工作室</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/52" rel="external nofollow noopener noreferrer" target="_blank">经历了时间考验的塑料——有机玻璃（PMMA） - 建材U选</a>）</p>
<p><strong>令人脑洞大开的设计</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c05de6d8600482a77b0643a605d9997d.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5f88082166a724bde65bf3a519565d3a.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c87f11a1dd6126d9b6ce9576ac39e917.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>新加坡南洋理工大学学习中心（2015年竣工）</strong></p>
<p><strong>建筑师：托马斯·赫斯维克（Thomas Heatherwick）工作室</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/113" rel="external nofollow noopener noreferrer" target="_blank">内行看门道：形式背后的逻辑 - 建材U选</a>）</p>
<p><strong>创新性的功能规划+应对特定地域气候的设计+特立独行的形式</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/27dae8360bea7688a3eb60901abeb8e2.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/180f6f98551d392d329a78de340e82b9.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/966305ab81fc99b4e8e3662dbd1b6964.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3e22a5a9093fe3538216f7ecd72cb700.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/155a6691aa3b20f0c904e32a89574452.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>中国上海复星艺术中心（2016年竣工）</strong></p>
<p><strong>建筑师：福斯特事务所（Foster + Partners）+托马斯·赫斯维克（Thomas Heatherwick）工作室</strong></p>
<p><strong>动感的建筑表皮</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/36850773142fd312c9eef8845f8b05c3.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d9315a6b2645e8d0259541cccdb3a915.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<hr>
<p><strong>新加坡parkroyal酒店（2013年竣工）+ oasia酒店（2016年竣工）</strong></p>
<p><strong>建筑师：新加坡WOHA 建筑设计事务所</strong></p>
<p>（参阅过往文章：<a href="http://www.bml365.com/show/study/detail/117" rel="external nofollow noopener noreferrer" target="_blank">绿化也能成为立面的主角？ - 建材U选</a>）</p>
<p><strong>立体绿化成为建筑的标志</strong></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2da14f5a8d4afb45b9c7978c758663f9.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/6ef4da1a818f3d8c17f1f1ee43a25b3a.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/78d7118dfe71e4af7be1921b93b56cc6.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bc2741e075835ec112b10b928fba5aaa.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/381a897b3cf9c12709b099d4e50e197f.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d26a3192ec0709806faf424c7df21819.png" alt="「著名建筑师的作品」那些年，那些令人“震撼”的现代建筑！"></p>
]]></content>
      <categories>
        <category>兴趣爱好</category>
        <category>旅游</category>
      </categories>
      <tags>
        <tag>兴趣爱好</tag>
        <tag>旅游</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part4：Table设计与实现</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part4/</url>
    <content><![CDATA[<p><a href="http://manistein.club/post/program/let-us-build-a-lua-interpreter/%E6%9E%84%E5%BB%BAlua%E8%A7%A3%E9%87%8A%E5%99%A8part4/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>   本篇我将对dummylua中Table的设计和实现进行介绍和说明。本文的目的旨在梳理清dummylua项目Table的数据结构和运作流程，该部分深度参考了lua-5.3.4的Table设计与实现，由于所有的细节是我自己根据理解重新实现，因此不会在所有的设计细节上和官方lua保持一致，但是遵循了基本的设计思路。<br>   Table是Lua语言中举足轻重的组成部分，掌握和理解它具有战略意义，这也是实现Lua虚拟机的基础所在，本文首先介绍dummylua项目Table的数据结构，然后在概念上介绍一些基本的操作流程，如创建、resize、查询、插入和迭代等操作。</p>
<h1 id="lua-table的基本数据结构"><a href="#lua-table的基本数据结构" class="headerlink" title="lua table的基本数据结构"></a>lua table的基本数据结构</h1><p>   现在我们来介绍一下table的基本数据结构，虽然我很反对在一开始就贴代码，但是对于数据结构而言，展示必要的代码反而有助于我们的理解，因此还是决定先把lua table的数据结构代码贴出来，然后再逐步进行解析。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> lua_Value &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gc</span>;</span></span><br><span class="line">    <span class="keyword">void</span>* p;</span><br><span class="line">    <span class="keyword">int</span> b;</span><br><span class="line">    lua_Integer i;</span><br><span class="line">    lua_Number n;</span><br><span class="line">    lua_CFunction f;</span><br><span class="line">&#125; Value;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">lua_TValue</span> &#123;</span></span><br><span class="line">    Value value_;</span><br><span class="line">    <span class="keyword">int</span> tt_;</span><br><span class="line">&#125; TValue;</span><br><span class="line"></span><br><span class="line"><span class="comment">// lua Table</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> TKey &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">        Value value_;</span><br><span class="line">        <span class="keyword">int</span> tt_;</span><br><span class="line">        <span class="keyword">int</span> next;</span><br><span class="line">    &#125; nk;</span><br><span class="line">    TValue tvk;</span><br><span class="line">&#125; TKey;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Node</span> &#123;</span></span><br><span class="line">    TKey key;</span><br><span class="line">    TValue value;</span><br><span class="line">&#125; Node;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Table</span> &#123;</span></span><br><span class="line">    CommonHeader;</span><br><span class="line">    TValue* <span class="built_in">array</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> arraysize;</span><br><span class="line">    Node* node;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> lsizenode; <span class="comment">// real hash size is 2 ^ lsizenode</span></span><br><span class="line">    Node* lastfree;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gclist</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>暂时忽略掉gc部分，我们可以看到Table的主要组成部分由一个TValue类型的数组array，以及一个Node类型的数组node。我们的array数组，主要是存放key为整数类型，值为TValue类型的变量。而我们的node数组，主要用于存放key为任意lua类型的变量，实际上node数组是作为hash表使用的。TValue变量主要由表示类型的int类型变量tt_，和表示值的Value类型变量value_组成，而如上所示，Value是一个union类型，它可以表示任意一种lua类型。<br>   我们的Node主要由两个部分组成，一个是TKey类型表示key，一个是TValue类型表示value值，TKey和TValue不同之处在于，它有个部分包含了next域，这个域用于处理hash冲突的情况。Node变量的key和Node数组的下标有着密切的关系，在后面我将详细介绍，现在只需要知道Table主要由两个部分组成，一个数组和一个hash表。现在假设一个刚初始化好的Table变量，它如图1所示：<br><img src="table_init_01.png" alt></p>
<p>图1展示了一个array的size为2，hash表大小为4的table实例，图中虚线方框部分不属于hash表的一部分，它是紧挨着hash表最后一个元素的一块内存，这块内存，并不允许直接通过lastfree指针对其进行修改，当我们的元素往hash表插入时发生了key冲突，那么要将冲突的其中一个元素转移到空余的node中，而这个空余的node，需要lastfree指针不断向左移动，直至找到第一个空余的node为止，然后将冲突中要被转移的元素存入这个node中。也就是说，未被使用的node只可能存在于lastfree指针的左边，lastfree指向的内存，以及右边的内存均已被使用，或者是不可修改的区域（虚线方框部分）。这种设计可以快速找到空闲的节点。<br>  接下来我们来看一个例子，如图2所示，我们的Table的hash表已经有两个元素，现在有一个新的元素要插入hash表，对该元素的key值进行hash运算后，得到的hash值，根据该hash值，新的Node应该插入的位置被判定到了红色Node处，此时两个Node发生了key冲突。<br><img src="node_key_confilt01.png" alt><br>  根据lua的处理规则，此时需要将冲突的其中一个Node（后续会介绍规则），放入空闲的节点中。而处理的方法则是lastfree指针不断向左移动，查找空闲节点，如图3所示：<br><img src="node_key_confilt02.png" alt><br>  lastfree指针找到空闲节点以后，将冲突的元素写入，如图4所示。此时我们可以发现，正如上述的那样，lastfree所指向的内存块，以及lastfree指针右边的内存块均不是空闲区域，我们要查找空闲节点需要让lastfree指针不断向左移动来快速查找，这样做的好处则是已经被使用的部分不会被遍历，当hash表很大时，能够很大程度上提高效率。<br><img src="node_key_confilt03.png" alt><br>上述流程主要是为了阐明lastfree的主要用途。现在，我们完成了对Table结构的简要介绍，接下来将讨论Table操作的各种流程。</p>
<h1 id="table的创建和初始化"><a href="#table的创建和初始化" class="headerlink" title="table的创建和初始化"></a>table的创建和初始化</h1><p>   现在我们来介绍table的创建和初始化流程，dummylua几乎原封不动地仿制了官方lua的设计与实现方式，在创建一个table实例时，我们是通过一个叫做luaH_new的接口来进行的，在完成创建后，table实例的结构如图5所示：<br><img src="luaH_new_01.png" alt><br>CommonHeader和gclist是gc相关的变量，table被创建时，每个变量都被赋予了初始值。我们可以看到，table实例在被刚创建出来的时候，并没有给array数组和hash表nodes开辟有效的内存空间，array被赋值为NULL，而nodes则被指向了一个被称之为dummynode的全局const变量，lua官方采取了这样的实现。为什么初始化后的nodes要指向这个变量？对于官方版本，有一种说法是，为了避免对nodes进行过多的NULL值判断[1]。<br>   然而我并不认同这种说法，如果是为了避免过多的NULL值判断，为什么array初始化后可以为NULL但nodes不行？针对这个问题，我认为原因可能是这样：正如上一节代码所示，表示hash表nodes大小的lsizenode值，是2的次幂，之所以要如此是为了强制保证nodes的大小一定是2的次幂，避免node的size被错误赋值导致hash表无法正常使用的后果（后面我将介绍为什么nodes的size必须是2的次幂），而初始值lsizenode被赋值为0，2^0的值是1，也就是说就算是table实例被创建出来后，nodes并无实际可用的内存空间，但是lsizenode指明了nodes的尺寸为1，出于逻辑上的严谨性，不论nodes是否可用，他都应该有一个节点，而如果nodes赋值为NULL，nodes和lsizenode的关系就会出现逻辑上的矛盾（lsizenode为0，说明了nodes的节点数是1，但是nodes却为NULL）。初始化后，让nodes指向一个全局常量dummynode，正好解决了这个逻辑上的矛盾，同时lua也提供了判断hash表nodes是否是dummynode的判断，以方便后续对hash表nodes的使用。如图6所示，这个dummynode是全局共享的。<br><img src="luaH_new_02.png" alt></p>
<h1 id="node节点的key与hash表下标之间的关系"><a href="#node节点的key与hash表下标之间的关系" class="headerlink" title="node节点的key与hash表下标之间的关系"></a>node节点的key与hash表下标之间的关系</h1><p>   到目前为止，我们已经遇到了一个不论如何都无法绕过去的问题，就是要阐述清楚，hash表中的node的key和hash表下标的关系。我们已经了解到，hash表其实是一个size为2^n的一维数组，那key和数组下标又存在怎样的联系呢？<br>   第一节，已经阐述了hash表中的Node节点的数据结构，Node节点的key可以是任意lua类型。那这key值是如何和hash表的下标对应起来的呢？首先要做的一步，就是对key进行hash运算，和官方的lua一样，dummylua提供了对各种类型进行hash运算的接口，将key的值转换成一个int型变量，如下表所示：<br><img src="20210408124811.png" alt><br>上表展示了的类型，是TKey类型变量中，类型标识tt_的值，可以理解为是个enum变量（实际上是宏定义），用来标记value_是什么类型的。表的另一列，则展示了对应的hash函数，他们的目标都只有一个，将不同类型的变量，转成一个int型变量（计算出的uint值，最终也会被转成int类型来使用）。于是，我们可以把这一切抽象成如图7所示的关系：<br><img src="key2index_describe.png" alt><br>在完成了对key的hash运算以后，我们需要根据key的hash值寻找该key应该位于hash表中的哪个下标，而计算的公式则是：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">index = hash_value &amp; ((<span class="number">2</span> ^ lsizenode) - <span class="number">1</span>)</span><br></pre></td></tr></table></figure></p>
<p>为什么要采取这样的方式？首先我们上面章节已经提到lua table的hash表大小必须是2的n次幂，这样设计的目的是，hash表size - 1得到的值，低位bit位全是1，让我们看一下2 ^ lsizenode - 1的二进制表示的表：<br><img src="20210408124927.png" alt><br>二进制低位全部是1，这样做的好处是，当我们的hash值超出hash表size边界的时候，会被自动回绕回去，而且效率非常高，我们来看一个例子，假设有一个字符串“table”，现在要将他和一个尺寸为8的hash表关联起来，则有：</p>
<ul>
<li>计算”table”这个字符串的hash值，假设得到01101011 00100100 10001101 001011002</li>
<li>table的hash表的lsizenode值为3，也就是size为8，于是有(2^lsizenode)-1 = 710 = 01112</li>
<li>计算”table”在hash表中的下标，于是有01101011 00100100 10001101 001011002 &amp; 01112，由于右边的值高位全是0，因此只需要截取”table”字符串hash值的低4位即可，于是有index = 11002 &amp; 01112 = 01002 = 410</li>
<li>于是key为”table”的node，将会被定位到hash[4]的位置上<br>经过这种操作，hash运算得到的int型数值，最终都会被映射到hash表尺寸范围内的下标中，不用担心超出边界的问题。hash运算本身具备离散性，因此key值可以相对均匀地分布在hash表中，这种设计使得lua table的插入和查找都可以保持很高的效率。</li>
</ul>
<h1 id="table元素的查找"><a href="#table元素的查找" class="headerlink" title="table元素的查找"></a>table元素的查找</h1><p>   在介绍完，hash表的下标和hash表中Node的key值的关系后，现在我们就可以来看看table元素的查找流程了。我们需要从被查找元素的Key值是int（也就是LUA_TNUMINT类型）和非int这两种情况来考察，当被查找的元素的key是int类型时，其查找流程如下所示：</p>
<ul>
<li>令被查找元素的key值为k，Table array数组的大小为arraysize</li>
<li>判断被查找元素的key值是否在数组范围内(即k &lt;= arraysize是否成立)</li>
<li><p>若key值在Table的array范围内，则返回array[k - 1]，流程终止</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (k - <span class="number">1</span> &lt; arraysize) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">array</span>[k - <span class="number">1</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 进入hash表查找流程</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>若key值不在Table的array范围内，计算key值在hash表中的位置，计算方式为index = k &amp; (2lsizenode-1)，然后以hash[index]节点为起点，查找key值与k相等的node，其伪代码如下所示：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 进入hash表查找流程</span></span><br><span class="line"><span class="comment">// k是被查询元素的key值</span></span><br><span class="line">Node* node = &amp;hash[index];</span><br><span class="line"><span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    <span class="keyword">if</span> (luaV_equalobject(node.key, k)) &#123;</span><br><span class="line">        <span class="keyword">return</span> node.value_;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (node.next == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> luaO_nilobject;</span><br><span class="line">    &#125;</span><br><span class="line">    node = node + node.next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>我们可以看到，如果在遍历hash表的过程中，找不到key值与k相等的node时，会返回luaO_nilobject，返回这个值，意味着查询失败。以上是被查询元素的key值是int型的情况，当被查询的key值不是int型时，它将遵循如下流程：</p>
<ul>
<li>计算被查询元素的key(记为k)的hash值，计为key_hash</li>
<li>计算key在hash表中的位置，计算方式为index = key_hash &amp; (2lsizenode-1)，然后以hash[index]节点为起点，查询key值与k相等的node，伪代码和key值为int类型时，hash表查询的方式一致。<br>现在我已经介绍了Table查询的规则了，其本质的逻辑就是试图寻找匹配的value值，如果找不到就返回luaO_nilobject变量。</li>
</ul>
<h1 id="table值的更新与插入"><a href="#table值的更新与插入" class="headerlink" title="table值的更新与插入"></a>table值的更新与插入</h1><p>  我们要更新Table，首先会根据上一节提到的方法，查找指定的元素，当返回的元素不是luaO_nilobject对象时，说明查找成功，此时只需要将要更新的值设置到这个TValue变量上，这种情况被称之为更新。图8的例子展示了，一个key值为3，value值为”haha”的元素，要写入table表中（table的array长度为4，hash表长度也为4，且均是NIL值），由于key值3在array的size范围内，因此将直接返回array中的第三个元素，并设置为值”haha”。<br><img src="table_array_update_01.png" alt><br>而图9的例子，则尝试更新一个key值为5的域，由于5超出了array的size，因此他会在hash表中查找位置（与图8不同的是，图9已经存在了一个key值为5的node），5的二进制值为01012，hash表的size为4，其二进制值为01002，则index = 5 &amp; (4 - 1) = 01012 &amp; 00112 = 00012 = 1。hash[1]这个Node的key值，与要操作的元素的key值相等，于是更新hash[1]这个Node的Value为指定的值。<br><img src="table_array_update_02.png" alt></p>
<p>  当我们查找指定key值的变量时，如果能找到则返回TValue类型变量的指针，而当找不到指定key值的变量时，则返回值为luaO_nilobject，此时需要创建新的key，并向value_域赋值，这种操作实际上是插入操作。创建key的流程如下所示：</p>
<ul>
<li>计要新建的key值为k，并计算k的hash值，记为k_hash</li>
<li>计算key应该落在hash表的哪个位置，计算方式为index = k_hash &amp; (2lsizenode-1)</li>
<li>如果hash[index]这个node的value值为nil，将node的key值设置为k的值，并返回value_对象指针，供调用者设置</li>
<li>如果hash[index]这个node的value值不为nil，需要分两种情况处理<ul>
<li>计算node key的hash值，如果经过定位运算后，index的值不在自己所处的位置上，那么lastfree不断左移，直至找到一个空闲的节点，将其移动到这里，修改链表关系，令其上一个与自己计算得到相同index值的节点的next域指向自己（如果存在的话）。新插入的key和value设置到hash[index]节点上</li>
<li>计算node key的hash值，如果经过定位运算后，index的值在自己所处的位置上，那么lastfree不断左移，直至找到一个空闲的节点，将自己的key和value值设置到这个节点上，并调整链表关系，将与自己计算得到相同index值的上一个节点的key的next指向自己的位置。<br>通过上述流程，我们大致知道了Table插入操作的大体流程，现在通过一个例子，加深我们的理解，假设一个array的size为4，hash表的size为4的table，所有域的值都是nil，如图10所示，现在要向table塞入一个key值为5，value值为”xixi”的元素，由于key值5超出了array的size范围，那么程序首先会尝试去hash表中查找，我们可以得到最终index的值为1，由于hash[1]这个Node的key值为nil，与要更新元素的key值不相等，因此此时触发了插入操作，由于hash[1]这个Node的key和value均是nil，因此可以将该元素直接设置到这里。<br><img src="table_hash_insert_01.png" alt><br>与此同时，一个key值为13，value值为”manistein”的元素也要对table进行赋值，经过之前阐述过的方式计算，得到index值为1，在这种情况下直接在hash表中进行查找，因为hash[1]的value域的值为”xixi”并不是nil，key值为5，与13并不相等，于是此时发生了hash碰撞，key值5经过转换运算，得到的hash表index的值为1，此时他就在这个位置上，因此key值为13的新元素需要被移走，lastfree指针，此时向左移动，并且将key值为13，value值为”manistein”的元素，赋值到lastfree指向的位置上（即hash[3]的位置上），并且将hash[1]的key的next指向lastfree指针所指的位置，如图11所示：<br><img src="table_hash_insert_02.png" alt><br>又再次，一个key值为7，value值为”wu”的元素要对table进行赋值，经过计算得到其对应的hash表index值为3，此时hash[3]已经被占用，此时需要计算，占据在这里的元素的key值，其真实对应的hash表index其实是1，因为hash[1]被占用才被移动到这里，因为这个元素计算得到的index与当前位置并不匹配，因此lastfree指针需要继续向左移动，并将key值为13的元素迁移到这里，并更新其前置节点的next域，最后将key值为7的元素，赋值到hash[3]的位置上，如图12所示：<br><img src="table_hash_insert_03.png" alt><br>本节花费和较大的篇幅，阐述了table域的更新和插入操作，以及插入可能出现的各种情况，不过目前为止，我们的操作并未触发任何resize操作，接下来的一节，将阐述resize操作的具体流程。</li>
</ul>
</li>
</ul>
<h1 id="table的resize操作"><a href="#table的resize操作" class="headerlink" title="table的resize操作"></a>table的resize操作</h1><p>  上一节，我们介绍了table值的更新和插入的操作，不过均是在hash表空间充足的情况下进行的。当hash表已满，且又有新的元素要插入hash表时，将触发table的resize操作。他的操作如下面步骤所示：</p>
<ul>
<li>计算table中，array和hash表中的所有value值不为nil的元素总数，记为total_element</li>
<li>因为新插入一个元素，因此total_element += 1</li>
<li>创建一个nums[32]的数组，nums[i]表示key值为int型，且key值在(2i-1, 2i]范围内(lua脚本里table的int型下标，如t[1]的key在(2-1, 20]这个区间)，并且value不为nil的元素个数，它本质是统计所有的int key，如果在一个一维数组中，各个元素的分布情况，它将作为后面确定arraysize的重要依据</li>
<li><p>统计array在nums数组中，不同区间的分布情况，其伪代码为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">int</span> j = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">int</span> twotoi = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">for</span> (; i &lt; <span class="number">32</span>; i ++, twotoi *= <span class="number">2</span>) &#123;</span><br><span class="line">    <span class="keyword">for</span> (; j &lt; twotoi; j ++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!is_nil(<span class="built_in">array</span>[j])) &#123;</span><br><span class="line">            nums[i] ++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">```c</span><br><span class="line">* 统计hash表元素在nums数组中，不同区间的分布情况，其伪代码为：</span><br><span class="line">```c</span><br><span class="line"><span class="comment">// ceillog2函数片段</span></span><br><span class="line"><span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (; i &lt; <span class="number">32</span>; i ++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">pow</span>(<span class="number">2</span>, i) &gt;= hash.key) &#123;</span><br><span class="line">        <span class="keyword">return</span> i;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> i;</span><br><span class="line"><span class="comment">// -----------------------------</span></span><br><span class="line"><span class="comment">// 计算hash在nums中的分布函数片段</span></span><br><span class="line"><span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (; i &lt; <span class="built_in">pow</span>(<span class="number">2</span>, lsizenode); i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (is_int(hash[i].key) &amp;&amp; !is_nil(hash[i].value_)) &#123;</span><br><span class="line">        <span class="keyword">int</span> k = ceillog2(hash[i].key); <span class="comment">// </span></span><br><span class="line">        nums[k] ++;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>判断新插入元素new_element的key值是否为int型，如果是则令nums[ceillog2(new_element.key)]++</p>
</li>
<li><p>完成数组nums的统计之后，根据nums，计算新的arraysize，在arraysize范围内，值不为nil的元素要超过arraysize的一半，其计算公式为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">int</span> asize = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (; i &lt; <span class="number">32</span>; i++) &#123;</span><br><span class="line">    asize += nums[i];</span><br><span class="line">    <span class="keyword">if</span> (asize &gt; <span class="built_in">pow</span>(<span class="number">2</span>, i) / <span class="number">2</span>) &#123;</span><br><span class="line">        arraysize = <span class="built_in">pow</span>(<span class="number">2</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>计算在arraysize范围内，有效的元素个数，记为array_used_num</p>
</li>
<li>当arraysize比原来大时，扩展原来的array到新的尺寸，并将hash表中，key值&lt;=arraysize的元素转移到array中，并将hash表大小调整为ceillog2(total_element - array_used_num)，同时对每个node进行rehash重新定位位置</li>
<li>当arraysize比原来小时，缩小原来的array到新的尺寸，并将array中，key值超过arraysize的元素转移到hash表中，此时hash表大小调整为ceillog2(total_element - array_used_num)，同时对每个node进行rehash计算，重新定位位置<br>现在我们来看一个例子，如图13所示，Table的array和hash表均已经占满，hash表中有3个key值为int类型的Node，此时要向Table插入一个key值为7，value值为”wu”的元素，因为7超出了array的大小，因此会尝试插入hash表。由于hash表中，没找到与之相等key的节点，因此要执行插入操作，而此时hash表已经满了，因此要进行resize操作。按照上面所说的流程，我们将array和hash表中，所有以int类型为key的元素，映射到一个一维数组中，并且用不同的颜色区分他们所处的区间，图13标记了数组下标区间和nums数组之间的关系，同一色块内的不为nil的元素，就是nums[i]的值，通过图13，我们现在更能清楚地理解nums数组，就是统计不同范围内的有效值。最后当arraysize为8的时候，array的利用率超过了一半，因此数组大小调整为8，hash表中的int key转移到了array中，我们的total_element一共是9个，其中7个在array中，因此hash表只剩下，total_element - array_used_num = 9 - 7 = 2，hash表的size调整为2，同时，Node重新计算hash值，重新分配了hash位置，最后得到图13最下部的结果。<br><img src="table_resize.png" alt><h1 id="table的迭代"><a href="#table的迭代" class="headerlink" title="table的迭代"></a>table的迭代</h1> 现在我们来看一下table的迭代机制，我们提供了luaH_next函数来进行迭代操作，这个函数的声明如下所示：<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaH_next</span><span class="params">(struct lua_State* L, struct Table* t, TValue* key)</span></span>;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>第一个参数是我们正在使用的lua虚拟机状态实例，第二个参数是表示我们要进行迭代的table，第三个参数传入的是一个key值。调用的结果是，将传入的key的下一个key和value压入栈中。当传入的key值是nil值时，将1和array数组的第一个值入栈。如图14所示，是传入key值为nil的情况：<br><img src="table_next_01.png" alt><br>当我们传入一个值不为nil，key为int类型，且在arraysize范围内的，并且不是array的最后一个元素的情况下，我们将会把当前array元素的下一个index和value值入栈，如图15所示：<br><img src="table_next_02.png" alt><br>当我们传入的key值，是array数组的最后一个元素的index时，我们会将nodes列表的第一个node的key和value入栈，如图16所示：<br><img src="table_next_03.png" alt><br>同样的，如果我们传入一个hash表中的key值时，他会将紧挨着自己的的下一个元素的key和value值入栈，如图17所示：<br><img src="table_next_04.png" alt><br>当我们能够查询到结果的时候，luaH_next函数将返回1，而当我们的传入的key是当前hash表的最后一个元素的key时，luaH_next函数将返回0。我们可以通过一个循环语句，调用luaH_next进行循环调用，最后实现对table的遍历。</p>
<h1 id="程序实现"><a href="#程序实现" class="headerlink" title="程序实现"></a>程序实现</h1><p>  本文用了巨量的篇幅，通过图文的形式，阐述了table的数据结构和基本运作流程，我相信如果读者有认真阅读，理解这些应该不是难事，在理解了原理，在文章内贴上代码已经显得非常累赘，因此，如果读者有意愿阅读源码，可以直接上dummylua项目的github站点上查看代码，或者直接阅读lua-5.3中的ltable.h和ltable.c内部的代码。</p>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>   到目前为止，我不仅完成了table设计实现篇，也完成了整个《构建Lua解释器》系列的第一部分的开发和blog编写，第一部分花费了我数月之久，仅仅是table这篇我就用了半个月的业余时间，不可不谓之艰难，原创相当不易，希望大家转载注明出处，由于本人水平有限，如过对文章内容有异议，希望大家加以斧正，你们可以在QQ185017593群里找到我，我是群主欢迎大家的到来。<br>   本系列的第二部分，我们将进入到Lua词法分析器、语法分析器以及虚拟机部分的开发，更激烈的战斗即将来临，敬请期待。</p>
<p>Reference<br>[1] <a href="http://lua-users.org/lists/lua-l/2012-09/msg00364.html" rel="external nofollow noopener noreferrer" target="_blank">why lua need a dummy node?</a></p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器之主动调度(20)</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/Go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E4%B9%8B%E4%B8%BB%E5%8A%A8%E8%B0%83%E5%BA%A6-20/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/zA7KY_25NGjip9pP38RIvg" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-24</p>
<p>本文是《Go语言调度器源代码情景分析》系列的第20篇，也是第五章《主动调度》的第1小节。</p>
<p>Goroutine的主动调度是指当前正在运行的goroutine通过直接调用runtime.Gosched()函数暂时放弃运行而发生的调度。</p>
<p>主动调度完全是用户代码自己控制的，我们根据代码就可以预见什么地方一定会发生调度。比如下面的程序，在main goroutine中创建了一个新的我们称之为g2的goroutine去执行start函数，g2在start函数的循环中反复调用Gosched()函数放弃自己的执行权，主动把CPU让给调度器去执行调度。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"runtime"</span></span><br><span class="line">    <span class="string">"sync"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="keyword">const</span> N = <span class="number">1</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"> </span><br><span class="line">    wg.Add(N)</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; N; i++ &#123;</span><br><span class="line">        <span class="keyword">go</span> start(&amp;wg)</span><br><span class="line">    &#125;</span><br><span class="line">    wg.Wait()</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">start</span><span class="params">(wg *sync.WaitGroup)</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">1000</span> * <span class="number">1000</span> * <span class="number">1000</span>; i++ &#123;</span><br><span class="line">        runtime.Gosched()</span><br><span class="line">    &#125;</span><br><span class="line">    wg.Done()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>下面我们就从这个程序开始分析主动调度是如何实现的。</p>
<p>首先从主动调度的入口函数Gosched()开始分析。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">262</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Gosched yields the processor, allowing other goroutines to run. It does not</span></span><br><span class="line"><span class="comment">// suspend the current goroutine, so execution resumes automatically.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Gosched</span><span class="params">()</span></span> &#123;</span><br><span class="line">    checkTimeouts() <span class="comment">//amd64 linux平台空函数</span></span><br><span class="line">   </span><br><span class="line">    <span class="comment">//切换到当前m的g0栈执行gosched_m函数</span></span><br><span class="line">    mcall(gosched_m)</span><br><span class="line">    <span class="comment">//再次被调度起来则从这里开始继续运行</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>因为我们需要关注程序运行起来之后g2 goroutine的状态，所以这里用gdb配合源代码一起来进行调试和分析，首先使用b proc.go:266在Gosched函数的mcall(gosched_m)这一行设置一个断点，然后运行程序，等程序被断下来之后，反汇编一下程序当前正在执行的函数<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(gdb) disass</span><br><span class="line">Dump of assembler code <span class="keyword">for</span> <span class="keyword">function</span> main.start:</span><br><span class="line">     0x000000000044fc90 &lt;+0&gt;:mov   %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">     0x000000000044fc99 &lt;+9&gt;:cmp   0x10(%rcx),%rsp</span><br><span class="line">     0x000000000044fc9d &lt;+13&gt;:jbe   0x44fcfa &lt;main.start+106&gt;</span><br><span class="line">     0x000000000044fc9f &lt;+15&gt;:sub   <span class="variable">$0x20</span>,%rsp</span><br><span class="line">     0x000000000044fca3 &lt;+19&gt;:mov   %rbp,0x18(%rsp)</span><br><span class="line">     0x000000000044fca8 &lt;+24&gt;:lea   0x18(%rsp),%rbp</span><br><span class="line">     0x000000000044fcad &lt;+29&gt;:xor   %eax,%eax</span><br><span class="line">     0x000000000044fcaf &lt;+31&gt;:jmp   0x44fcd0 &lt;main.start+64&gt;</span><br><span class="line">     0x000000000044fcb1 &lt;+33&gt;:mov   %rax,0x10(%rsp)</span><br><span class="line">     0x000000000044fcb6 &lt;+38&gt;:nop</span><br><span class="line">     0x000000000044fcb7 &lt;+39&gt;:nop</span><br><span class="line">=&gt; 0x000000000044fcb8 &lt;+40&gt;:lea   0x241e1(%rip),%rax        <span class="comment"># 0x473ea0</span></span><br><span class="line">     0x000000000044fcbf &lt;+47&gt;:mov   %rax,(%rsp)</span><br><span class="line">     0x000000000044fcc3 &lt;+51&gt;:callq 0x447380 &lt;runtime.mcall&gt;</span><br><span class="line">     0x000000000044fcc8 &lt;+56&gt;:mov   0x10(%rsp),%rax</span><br><span class="line">     0x000000000044fccd &lt;+61&gt;:inc   %rax</span><br><span class="line">     0x000000000044fcd0 &lt;+64&gt;:cmp   <span class="variable">$0x3b9aca00</span>,%rax</span><br><span class="line">     0x000000000044fcd6 &lt;+70&gt;:jl     0x44fcb1 &lt;main.start+33&gt;</span><br><span class="line">     0x000000000044fcd8 &lt;+72&gt;:nop</span><br><span class="line">    0x000000000044fcd9 &lt;+73&gt;:mov   0x28(%rsp),%rax</span><br><span class="line">     0x000000000044fcde &lt;+78&gt;:mov   %rax,(%rsp)</span><br><span class="line">     0x000000000044fce2 &lt;+82&gt;:movq   <span class="variable">$0xffffffffffffffff</span>,0x8(%rsp)</span><br><span class="line">     0x000000000044fceb &lt;+91&gt;:callq 0x44f8f0 &lt;sync.(*WaitGroup).Add&gt;</span><br><span class="line">     0x000000000044fcf0 &lt;+96&gt;:mov   0x18(%rsp),%rbp</span><br><span class="line">     0x000000000044fcf5 &lt;+101&gt;:add   <span class="variable">$0x20</span>,%rsp</span><br><span class="line">     0x000000000044fcf9 &lt;+105&gt;:retq   </span><br><span class="line">     0x000000000044fcfa &lt;+106&gt;:callq 0x447550 &lt;runtime.morestack_noctxt&gt;</span><br><span class="line">     0x000000000044fcff &lt;+111&gt;:jmp   0x44fc90 &lt;main.start&gt;</span><br></pre></td></tr></table></figure></p>
<p>可以看到当前正在执行的函数是main.start而不是runtime.Gosched，在整个start函数中都找不到Gosched函数的身影，原来它被编译器优化了。程序现在停在了0x000000000044fcb8 &lt;+40&gt;: lea 0x241e1(%rip),%rax 这一指令处，该指令下面的第二条callq指令在调用runtime.mcall，我们首先使用si 2来执行2条汇编指令让程序停在下面这条指令处：</p>
<p>=&gt; 0x000000000044fcc3 &lt;+51&gt;: callq 0x447380 &lt;runtime.mcall&gt;<br>然后使用i r rsp rbp rip记录一下CPU的rsp、rbp和rip寄存器的值备用：</p>
<p>(gdb) i r rsp rbp rip<br>rsp    0xc000031fb0     0xc000031fb0<br>rbp    0xc000031fc8     0xc000031fc8<br>rip     0x44fcc3             0x44fcc3 &lt;main.start+51&gt;<br>继续看0x000000000044fcc3位置的callq指令，它首先会把紧挨其后的下一条指令的地址0x000000000044fcc8放入g2的栈，然后跳转到mcall函数的第一条指令开始执行。回忆一下第二章我们详细分析过的mcall函数的执行流程，结合现在这个场景，mcall将依次完成下面几件事：</p>
<p>把上面call指令压栈的返回地址0x000000000044fcc8取出来保存在g2的sched.pc字段，把上面我们查看到的rsp(0xc000031fb0)和rbp(0xc000031fc8)分别保存在g2的sched.sp和sched.bp字段，这几个寄存器代表了g2的调度现场信息；</p>
<p>把保存在g0的sched.sp和sched.bp字段中的值分别恢复到CPU的rsp和rbp寄存器，这样完成从g2的栈到g0的栈的切换；</p>
<p>在g0栈执行gosched_m函数（gosched_m函数是runtime.Gosched函数调用mcall时传递给mcall的参数）。</p>
<p>继续看gosched_m函数<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2623</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Gosched continuation on g0.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">gosched_m</span><span class="params">(gp *g)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> trace.enabled &#123; <span class="comment">//traceback 不关注</span></span><br><span class="line">        traceGoSched()</span><br><span class="line">    &#125;</span><br><span class="line">    goschedImpl(gp)  <span class="comment">//我们这个场景：gp = g2</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>gosched_m函数只是简单的在调用goschedImpl：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2608</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">goschedImpl</span><span class="params">(gp *g)</span></span> &#123;</span><br><span class="line">    ......</span><br><span class="line">    casgstatus(gp, _Grunning, _Grunnable)</span><br><span class="line">    dropg() <span class="comment">//设置当前m.curg = nil, gp.m = nil</span></span><br><span class="line">    lock(&amp;sched.lock)</span><br><span class="line">    globrunqput(gp) <span class="comment">//把gp放入sched的全局运行队列runq</span></span><br><span class="line">    unlock(&amp;sched.lock)</span><br><span class="line"> </span><br><span class="line">    schedule() <span class="comment">//进入新一轮调度</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>goschedImpl函数有一个g指针类型的形参，我们这个场景传递给它的实参是g2，goschedImpl函数首先把g2的状态从_Grunning设置为_Grunnable，并通过dropg函数解除当前工作线程m和g2之间的关系（把m.curg设置成nil，把g2.m设置成nil），然后通过调用我们已经分析过的globrunqput函数把g2放入全局运行队列之中。</p>
<p>g2被挂入全局运行队列之后，g2以及其它一些相关部分的状态和关系如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5c3d766232ff4dbba2a447b72e96aeaa.png" alt></p>
<p>从上图我们可以清晰的看到，g2被挂在了sched的全局运行队列里面，该队列有一个head头指针指向队列中的第一个g对象，还有一个tail尾指针指向队列中的最后一个g对象，队列中各个g对象通过g的schedlink指针成员相互链接起在一起；g2的sched结构体成员中保存了调度所需的所有现场信息（比如栈寄存器sp和bp的值，pc指令寄存器的值等等），这样当g2下次被schedule函数调度时，gogo函数会负责把这些信息恢复到CPU的rsp, rbp和rip寄存器中，从而使g2又得以从0x44fcc8地址处开始在g2的栈中执行g2的代码。</p>
<p>把g2挂入全局运行队列之后，goschedImpl函数继续调用schedule()进入下一轮调度循环，至此g2通过自己主动调用Gosched()函数自愿放弃了执行权，达到了调度的目的。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析之八：系统调用</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90%E4%B9%8B%E5%85%AB%EF%BC%9A%E7%B3%BB%E7%BB%9F%E8%B0%83%E7%94%A8/</url>
    <content><![CDATA[<h1 id="原创-爱写程序的阿波张-源码游记-2019-04-24"><a href="#原创-爱写程序的阿波张-源码游记-2019-04-24" class="headerlink" title="原创 爱写程序的阿波张 源码游记 2019-04-24"></a>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/CXhc71UZtbdbAo15xbJ6_Q" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-04-24</h1><p>我们将在最后一章讨论有关系统调用方面的抢占调度，所以这里有必要对系统调用有个基本的了解。</p>
<p>系统调用是指使用类似函数调用的方式调用操作系统提供的API。</p>
<p>虽然从概念上来说系统调用和函数调用差不多，但本质上它们有很大的不同，操作系统的代码位于内核地址空间，而CPU在执行用户代码时特权等级很低，无权访问需要最高优先级才能访问的内核地址空间的代码和数据，所以不能通过简单的call指令直接调用操作系统提供的函数，而需要使用特殊的指令进入操作系统内核完成指定的功能。</p>
<p>另外，用户代码调用操作系统API也不是根据函数名直接调用，而是需要根据操作系统为每个API提供的一个整型编号来调用，AMD64 Linux平台约定在进行系统调用时使用rax寄存器存放系统调用编号，同时约定使用rdi, rsi, rdx, r10, r8和r9来传递前6个系统调用参数。</p>
<p>可能有读者会说，我们平时编程也没有用到系统调用啊？！其实并不是没有用到，而是我们没有感觉到它的存在，比如最简单的向屏幕输出字符串，打开文件，读写文件以及网络编程中的创建socket等等都使用了系统调用，我们没有感觉到系统调用的存在主要是因为我们使用的函数库或package把它们封装成了函数，我们只需要直接调用这些函数就可以了。比如有下面一段go代码：</p>
<p>package main</p>
<p>import (<br>“os”<br>)</p>
<p>func main() {<br>        fd, err := os.Open(“./syscall.go”)  // 将会使用系统调用打开文件<br>        ……<br>        fd.Close()  // 将会使用系统调用关闭文件<br>}<br>这里的os.Open()和fd.Close()函数最终都会通过系统调用进入操作系统内核完成相应的功能。以os.Open为例，它最终会执行下面这段汇编代码来通过openat系统调用打开文件：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mov    0x10(%rsp),%rdi  #第1个参数</span><br><span class="line">mov    0x18(%rsp),%rsi  #第2个参数</span><br><span class="line">mov    0x20(%rsp),%rdx #第3个参数</span><br><span class="line">mov    0x28(%rsp),%r10 #第4个参数</span><br><span class="line">mov    0x30(%rsp),%r8  #第5个参数</span><br><span class="line">mov    0x38(%rsp),%r9  #第6个参数</span><br><span class="line">mov    0x8(%rsp),%rax  #系统调用编号 rax = 267，表示调用openat系统调用</span><br><span class="line">syscall                           #系统调用指令，进入Linux内核</span><br></pre></td></tr></table></figure></p>
<p>这里，代码首先把6个参数以及openat这个系统调用的编号267保存在了对应的寄存器中，然后使用syscall指令进入内核执行打开文件的功能。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析之二：CPU寄存器</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90%E4%B9%8B%E4%BA%8C%EF%BC%9ACPU%E5%AF%84%E5%AD%98%E5%99%A8/</url>
    <content><![CDATA[<p>原创 张方波 <a href="https://mp.weixin.qq.com/s/wTfbFfBfTJNGsgIJ_71nAg" title="源码游记" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-04-17</p>
<p>寄存器是CPU内部的存储单元，用于存放从内存读取而来的数据（包括指令）和CPU运算的中间结果，之所以要使用寄存器来临时存放数据而不是直接操作内存，一是因为CPU的工作原理决定了有些操作运算只能在CPU内部进行，二是因为CPU读写寄存器的速度比读写内存的速度快得多。</p>
<p>为了便于交流和使用汇编语言进行编程，CPU厂商为每个寄存器都取了一个名字，比如AMD64 CPU中的rax, rbx, rcx, rdx等等，这样程序员就可以很方便的在汇编代码中使用寄存器的名字来进行编程，为了对寄存器的使用有个直观的感受，我们用个例子来简单的说明一下。</p>
<p>假设有如下go语言编写的一行代码：</p>
<p>c = a + b<br>在AMD64 Linux平台下，使用go编译器编译它可得到如下AT&amp;T格式的汇编代码（如果对汇编代码不熟悉的话可以直接看每一条指令后面的注释，不影响我们理解）：</p>
<p>mov   (%rsp),%rdx          //把变量a的值从内存中读取到寄存器rdx中<br>mov    0x8(%rsp),%rax   //把变量b的值从内存中读取到寄存器rax中<br>add   %rdx,%rax             //把寄存器rdx和rax中的值相加，并把结果放回rax寄存器中<br>mov   %rax,0x10(%rsp)  //把寄存器rax中的值写回变量c所在的内存<br>可以看到，上面的一行go语言代码被编译成了4条汇编指令，指令中出现的rax，rdx和rsp都是寄存器的名字（AT&amp;T格式的汇编代码中所有寄存器名字前面都有一个%符号），虽然这里只有4条指令，但也从一个侧面说明汇编代码其实比较简单，它所做的工作不外乎就是把数据在内存和寄存器中搬来搬去或做一些基础的数学和逻辑运算。</p>
<p>不同体系结构的CPU，其内部寄存器的数量、种类以及名称可能大不相同，这里我们只介绍目前使用最为广泛的AMD64这种体系结构的CPU，这种CPU共有20多个可以直接在汇编代码中使用的寄存器，其中有几个寄存器在操作系统代码中才会见到，而应用层代码一般只会用到如下分为三类的19个寄存器。</p>
<p>通用寄存器：rax, rbx, rcx, rdx, rsi, rdi, rbp, rsp, r8, r9, r10, r11, r12, r13, r14, r15寄存器。CPU对这16个通用寄存器的用途没有做特殊规定，程序员和编译器可以自定义其用途（下面会介绍，rsp/rbp寄存器其实是有特殊用途的）；</p>
<p>程序计数寄存器（PC寄存器，有时也叫IP寄存器）：rip寄存器。它用来存放下一条即将执行的指令的地址，这个寄存器决定了程序的执行流程；</p>
<p>段寄存器：fs和gs寄存器。一般用它来实现线程本地存储（TLS），比如AMD64 linux平台下go语言和pthread都使用fs寄存器来实现系统线程的TLS，在本章线程本地存储一节和第二章详细分析goroutine调度器的时候我们可以分别看到Linux平台下Pthread线程库和go是如何使用fs寄存器的。</p>
<p>上述这些寄存器除了fs和gs段寄存器是16位的，其它都是64位的，也就是8个字节，其中的16个通用寄存器还可以作为32/16/8位寄存器使用，只是使用时需要换一个名字，比如可以用eax这个名字来表示一个32位的寄存器，它使用的是rax寄存器的低32位。</p>
<p>为了便于查阅，下表列出这些64通用寄存器对应的32/16/8位寄存器的名字：</p>
<p>64位    32位    16位    8位<br>rax    eax    ax    al/ah<br>rbx    ebx    bx    bl/bh<br>rcx    ecx    cx    cl/ch<br>rdx    edx    dx    dl/dh<br>rsi    esi    si    -<br>rdi    edi    di    -<br>rbp    ebp    bp    -<br>rsp    esp    sp    -<br>r8~r15    r8d~r15d    r8w~r15w    r8b~r15b<br>通用寄存器的用法如前面我们所见，主要用于临时存放数据，后面的章节我们还会见到大量使用通用寄存器的例子，所以这里就不对其进行详细介绍了，但有三个比较特殊的寄存器值得在这里单独提出来做一下说明：</p>
<p>rip寄存器</p>
<p>rip寄存器里面存放的是CPU即将执行的下一条指令在内存中的地址。看如下汇编语言代码片段：</p>
<p>0x0000000000400770: add   %rdx,%rax<br>0x0000000000400773: mov   $0x0,%ecx<br>假设当前CPU正在执行第一条指令，这条指令在内存中的地址是0x0000000000400770，紧接它后面的下一条指令的地址是0x0000000000400773，所以此时rip寄存器里面存放的值是0x0000000000400773。</p>
<p>这里需要牢记的就是rip寄存器的值不是正在被CPU执行的指令在内存中的地址，而是紧挨这条正在被执行的指令后面那一条指令的地址。</p>
<p>读者可能会有疑问，在前面的两个汇编指令片段中并没有指令修改rip寄存器的值，是怎么做到让它一直指向下一条即将执行的指令的呢？其实修改rip寄存器的值是CPU自动控制的，不需要我们用指令去修改，当然CPU也提供了几条可以间接修改rip寄存器的指令，在汇编语言一节中我们会详细介绍CPU自动修改以及用指令修改rip寄存器值的两种方式。</p>
<p>rsp 栈顶寄存器和rbp栈基址寄存器</p>
<p>这两个寄存器都跟函数调用栈有关，其中rsp寄存器一般用来存放函数调用栈的栈顶地址，而rbp寄存器通常用来存放函数的栈帧起始地址，编译器一般使用这两个寄存器加一定偏移的方式来访问函数局部变量或函数参数，比如：</p>
<p>mov    0x8(%rsp),%rdx<br>这条指令把地址为 0x8(%rsp) 的内存中的值拷贝到rdx寄存器，这里的0x8(%rsp) 就利用了 rsp 寄存器加偏移 8 的方式来读取内存中的值。</p>
<p>寄存器的内容我们就先简单的介绍到这里，但这些并不是我们需要了解的有关寄存器的全部内容，有些内容需要等我们学习了汇编指令和函数调用栈之后才能更加深刻的理解，到时候我们再回头来继续介绍相关的知识。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析1</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%901/</url>
    <content><![CDATA[<p>原创 张方波  源码游记  2019-04-16<br>专栏简介</p>
<p>本专栏以精心设计的情景为线索，结合go语言最新1.12版源代码深入细致的分析了goroutine调度器实现原理。</p>
<p>适宜读者</p>
<p>go语言开发人员<br>对线程调度器工作原理感兴趣的工程师<br>对计算机底层运行原理感兴趣的工程师</p>
<p>专栏目标</p>
<p>笔者希望即使是从未接触过计算机底层原理的读者，通过对本专栏的认真学习，都可以完全掌握go语言调度器的实现原理及细节，从而可以充分利用go调度器的特性写出更加优秀的并发程序和解决一些与调度相关的疑难杂症；另外，读者还可以学到大量与程序运行相关的基础知识，比如汇编代码，CPU如何执行指令以及并发无锁编程等等非常重要的编程内功心法，有了这些基础理论，在今后的学习和工作中，读者就完全有能力对其它一些自己感兴趣的底层实现原理做深入的分析。</p>
<p>如何学习本专栏</p>
<p>编程是一门实践性很强的技术，所以建议读者在阅读本专栏时多注重实践练习，特别是在学习第一部分的汇编指令及函数调用栈等知识的时候，读者可以自己写一些小程序然后用调试工具单步执行来跟踪寄存器以及栈的变化，从而更好的理解这些内容。</p>
<p>专栏模块</p>
<p>本专栏主要分为两大部分：</p>
<p>第一部分包括第一章，主要介绍理解调度器工作原理所必须的预备知识，比如汇编指令，函数调用过程，操作系统线程调度原理以及线程本地存储等重要理论知识。这部分内容主要是为从未接触过底层知识的读者准备的，如果读者对这部分已经很熟了，则完全可以跳过这部分的阅读。</p>
<p>第二部分包括第2到第6章，主要分析goroutine调度器的实现，包括调度器的初始化，调度策略以及主动被动和抢占调度等，在分析过程中我们不仅会介绍调度器在干什么，更重要的是我们还会详细说明为什么要这么做，让读者知其然也知其所以然。</p>
<p>专栏章节</p>
<p>第一章 预备知识</p>
<ol>
<li>CPU寄存器与内存介绍</li>
<li>函数调用栈</li>
<li>汇编指令</li>
<li>函数调用过程</li>
<li>操作系统线程及调度</li>
<li>线程本地存储</li>
</ol>
<p>第二章 初始化和调度循环</p>
<ol start="7">
<li>调度器概述</li>
<li>程序启动和调度器初始化</li>
<li>创建main goroutine</li>
<li>启动main goroutine </li>
</ol>
<p>第三章 调度策略</p>
<ol start="11">
<li>goroutine的退出与调度循环</li>
<li>再探schedule函数与寻找可运行的goroutine</li>
<li>盗取goroutine与工作线程睡眠</li>
</ol>
<p>第四章 被动调度</p>
<ol start="14">
<li>goroutine进入运行队列</li>
<li>goroutine进入睡眠</li>
<li>唤醒睡眠中的goroutine与工作线程</li>
<li>创建工作线程</li>
</ol>
<p>第五章 主动调度</p>
<ol start="18">
<li>主动调度</li>
</ol>
<p>第六章 抢占调度</p>
<ol start="19">
<li>抢占运行时间过长的goroutine</li>
<li>goroutine进入系统调用以及从系统调用返回</li>
<li>抢占进入系统调用的goroutine</li>
</ol>
<p>作者介绍</p>
<p>张方波，15年软件开发经验，对计算机底层运行原理很感兴趣，做过应用层软件，也玩过操作系统内核，某公司前高级技术专家，现专注于K12在线教育，偶尔写写技术文章。</p>
<p>最后，如果您有任何建议或疑问，可以扫描下方微信二维码添加好友，我们一起进步！</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析之三：内存</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90%E4%B9%8B%E4%B8%89%EF%BC%9A%E5%86%85%E5%AD%98/</url>
    <content><![CDATA[<p>原创 张方波 <a href="https://mp.weixin.qq.com/s/iZxfTL34NFKsJNyLvREZPQ" title="源码游记" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-04-18</p>
<p>内存是计算机系统的存储设备，其主要作用是协助CPU在执行程序时存储数据和指令。</p>
<p>内存由大量内存单元组成，内存单元大小为1个字节（1字节包含8个二进制位）， 每个内存单元都有一个编号，更专业的说法是每一个内存单元都有一个地址，我们在编写汇编代码或编译器把用高级语言所写的程序编译成汇编指令时，如果要读写内存，就必须在指令中指定内存地址，这样CPU才知道它要存取哪个或哪些内存单元。</p>
<p>大家都知道，高级语言中有变量的概念，变量又有全局变量和函数局部变量之分，而不管是哪种变量（除了C语言中申明为register的变量），都需要保存在内存之中， 同时，绝大多数类型的变量都不会只占一个字节大小的内存，但是每个内存单元却只有一个字节大小，那么像这种大于一个字节的变量是如何保存在内存中的呢？聪明的你一定会想到，任何大于一个字节的变量都存储在相邻的几个内存单元中，事实也确实如此，比如go语言中的int64类型的变量在内存中就被存放在连续的8个内存单元之中，要读写该变量，只需在汇编指令中指定这些内存单元的起始地址以及读写的字节数即可。说到这里，问题又来了，既然一个int64类型的变量在内存中占8个字节，那么如何安排这8个字节来存储64位的整型数据呢？比如如何在内存中存储0x1122334455667788这个16进制表示的整型值，是把高位的0x11放在这8个内存单元中的第一个字节还是把低位的0x88放在第一个字节呢？其实两种方案都是可以的，不同的CPU采用的方案也可能不同，比如X86系列（包括AMD64）的CPU就会把低位的0x88放在起始位置，而PowerPC CPU则会把高位的0x11放在起始的第一个字节，这就是所谓的大小端存储模式：</p>
<p>大端存储模式：数据的高字节保存在内存的低地址中，低字节保存在内存的高地址中。</p>
<p>小端存储模式：数据的高字节保存在内存的高地址中，低字节保存在内存的低地址中。</p>
<p>需要注意的是大小端存储模式与CPU相关，而与内存无关，内存只管保存数据而不关心数据是什么以及怎么解释这些数据。下图是大小端存储模式的示意图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a37b03f237fdab1644c3376b50af4ed5.png" alt></p>
<p>下面再用上一节讨论寄存器时使用过的例子来帮助我们加深对内存的理解：</p>
<p>c = a + b  // go语言代码<br>mov   (%rsp),%rdx         #把变量a的值从内存中读取到寄存器rdx中<br>mov    0x8(%rsp),%rax   #把变量b的值从内存中读取到寄存器rax中<br>add   %rdx,%rax            #把寄存器rdx和rax中的值相加，并把结果放回rax寄存器中<br>mov   %rax,0x10(%rsp)  #把寄存器rax中的值写回变量c所在的内存<br>这里的4条指令有3条跟内存读写有关，指令中的rsp寄存器里面存放的是一个内存地址，现假设这个内存地址是X, 则第一条指令 mov    (%rsp),%rdx 表示把从地址为X开始的8个内存单元中的值读取到rdx寄存器中（因为rdx是一个64位寄存器，这就隐含了要一次读取连续的8个字节，指令中的地址只是起始地址，这个地址开始的8个字节是变量a所在的位置），第二条指令类似，只是起始地址为X + 0x8（变量b在内存中的地址），最后一条指令表示把rax寄存器中的值写入从地址为X + 0x10开始的8个内存单元中。下图直观的表示了上面4条指令的执行过程。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e333519f9dd49ddfd113314075ba768d.png" alt></p>
<p>对这个图做个简单的说明：</p>
<p>这里假定rsp寄存器的值是X</p>
<p>图中的内存部分，每一行有8个内存单元，它们的地址从右向左依次加一，即如果最右边的内存单元的地址为X的话，则同一行最左边的内存单元的地址为X+7。</p>
<p>灰色箭头表述数据流动方向</p>
<p>紫红色数字n表示上述代码片段中的第n条指令</p>
<p>最后，我们对内存部分做个简单的总结：</p>
<p>内存中的每个字节都有一个地址；</p>
<p>任何大于一个字节的变量在内存中都存储在相邻连续的的几个内存单元之中；</p>
<p>大端存储模式指数据的高字节保存在内存的低地址中，低字节保存在内存的高地址中；小端存储模式指数据的高字节保存在内存的高地址中，低字节保存在内存的低地址中。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析之九：操作系统线程及线程调度</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/go%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90%E4%B9%8B%E4%B9%9D%EF%BC%9A%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E7%BA%BF%E7%A8%8B%E5%8F%8A%E7%BA%BF%E7%A8%8B%E8%B0%83%E5%BA%A6/</url>
    <content><![CDATA[<h3 id="原创-爱写程序的阿波张-源码游记-2019-04-25"><a href="#原创-爱写程序的阿波张-源码游记-2019-04-25" class="headerlink" title="原创 爱写程序的阿波张 源码游记 2019-04-25"></a>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/OvGlI5VvvRdMRuJegNrOMg" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-04-25</h3><p><strong>本文是《go调度器源代码情景分析》系列 第一章 预备知识的第九小节。</strong></p>
<p>要深入理解goroutine的调度器，就需要对操作系统线程有个大致的了解，因为go的调度系统是建立在操作系统线程之上的，所以接下来我们对其做一个简单的介绍。</p>
<p>很难对线程下一个准确且易于理解的定义，特别是对于从未接触过多线程编程的读者来说，要搞懂什么是线程可能并不是很容易，所以下面我们抛开定义直接从一个C语言的程序开始来直观的看一下什么是线程。之所以使用C语言，是因为C语言中我们一般使用pthread线程库，而使用该线程库创建的用户态线程其实就是Linux操作系统内核所支持的线程，它与go语言中的工作线程是一样的，这些线程都由Linux内核负责管理和调度，然后go语言在操作系统线程之上又做了goroutine，实现了一个二级线程模型。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N (1000 * 1000 * 1000)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">volatile</span> <span class="keyword">int</span> g = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">start</span><span class="params">(<span class="keyword">void</span> *arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span> i;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">g++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">pthread_t</span> tid;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用pthread_create函数创建一个新线程执行start函数</span></span><br><span class="line">pthread_create(&amp;tid, <span class="literal">NULL</span>, start, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (;;) &#123;</span><br><span class="line">usleep(<span class="number">1000</span> * <span class="number">100</span> * <span class="number">5</span>);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"loop g: %d\n"</span>, g);</span><br><span class="line"><span class="keyword">if</span> (g == N) &#123;</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">pthread_join(tid, <span class="literal">NULL</span>); <span class="comment">// 等待子线程结束运行</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>该程序运行起来之后将会有2个线程，一个是操作系统把程序加载起来运行时创建的主线程，另一个是主线程调用pthread_create创建的start子线程，主线程在创建完子线程之后每隔500毫秒打印一下全局变量 g 的值直到 g 等于10亿，而start线程启动后就开始执行一个10亿次的对 g 自增加 1 的循环，这两个线程同时并发运行在系统中，操作系统负责对它们进行调度，我们无法精确预知某个线程在什么时候会运行。</p>
<p><strong>关于操作系统对线程的调度，有两个问题需要搞清楚：</strong></p>
<ol>
<li><p><strong>什么时候会发生调度</strong></p>
</li>
<li><p><strong>调度的时候会做哪些事情？</strong></p>
</li>
</ol>
<p>首先来看第一个问题，操作系统什么时候会发起调度呢？总体来说操作系统必须要得到CPU的控制权后才能发起调度，那么当用户程序在CPU上运行时如何才能让CPU去执行操作系统代码从而让内核获得控制权呢？一般说来在两种情况下会从执行用户程序代码转去执行操作系统代码：</p>
<p>用户程序使用系统调用进入操作系统内核；</p>
<p>硬件中断。硬件中断处理程序由操作系统提供，所以当硬件发生中断时，就会执行操作系统代码。硬件中断有个特别重要的时钟中断，这是操作系统能够发起抢占调度的基础。</p>
<p>操作系统会在执行操作系统代码路径上的某些点检查是否需要调度，所以操作系统对线程的调度也会相应的发生在上述两种情况之下。</p>
<p>下面来看一下在笔者的单核电脑上运行该程序的输出：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bobo@ubuntu:~/study/c$ gcc thread.c -o thread -lpthread</span><br><span class="line">bobo@ubuntu:~/study/c$ ./thread</span><br><span class="line">loop g: 98938361</span><br><span class="line">loop g: 198264794</span><br><span class="line">loop g: 297862478</span><br><span class="line">loop g: 396750048</span><br><span class="line">loop g: 489684941</span><br><span class="line">loop g: 584723988</span><br><span class="line">loop g: 679293257</span><br><span class="line">loop g: 777715939</span><br><span class="line">loop g: 876083765</span><br><span class="line">loop g: 974378774</span><br><span class="line">loop g: 1000000000</span><br></pre></td></tr></table></figure></p>
<p>从输出可以看出，主线程和start线程在轮流着运行，这是操作系统对它们进行了调度的结果，操作系统一会儿把start线程调度起来运行，一会儿又把主线程调度起来运行。</p>
<p>从程序的输出结果可以看到抢占调度的身影，因为主线程在start线程运行过程中得到了运行，而start线程执行的start函数根本没有系统调用，并且这个程序又运行在单核系统中，没有其它CPU来运行主线程，所以如果没有中断时发生的抢占调度，操作系统就无法获取到CPU的控制权，也就不可能发生线程调度。</p>
<p>接下来我们再来看看操作系统在调度线程时会做哪些事情。</p>
<p>如上所述，操作系统会把不同的线程调度到同一个CPU上运行，而每个线程运行时又都会使用CPU的寄存器，但每个CPU却只有一组寄存器，所以操作系统在把线程B调度到CPU上运行时需要首先把刚刚正在运行的线程A所使用到的寄存器的值全部保存在内存之中，然后再把保存在内存中的线程B的寄存器的值全部又放回CPU的寄存器，这样线程B就能恢复到之前运行的状态接着运行。</p>
<p>线程调度时操作系统需要保存和恢复的寄存器除了通用寄存器之外，还包括指令指针寄存器rip以及与栈相关的栈顶寄存器rsp和栈基址寄存器rbp，rip寄存器决定了线程下一条需要执行的指令，2个栈寄存器确定了线程执行时需要使用的栈内存。所以恢复CPU寄存器的值就相当于改变了CPU下一条需要执行的指令，同时也切换了函数调用栈，因此从调度器的角度来说，线程至少包含以下3个重要内容：</p>
<p>一组通用寄存器的值</p>
<p>将要执行的下一条指令的地址</p>
<p>栈</p>
<p>所以操作系统对线程的调度可以简单的理解为内核调度器对不同线程所使用的寄存器和栈的切换。</p>
<p>最后，我们对操作系统线程下一个简单且不准确的定义：操作系统线程是由内核负责调度且拥有自己私有的一组寄存器值和栈的执行流。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析之六：go汇编语言</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90%E4%B9%8B%E5%85%AD%EF%BC%9Ago%E6%B1%87%E7%BC%96%E8%AF%AD%E8%A8%80/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/dNzLMtn1X7dBvtkfX7Ifag" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-04-21</p>
<p>go语言runtime（包括调度器）源代码中有部分代码是用汇编语言编写的，不过这些汇编代码并非针对特定体系结构的汇编代码，而是go语言引入的一种伪汇编，它同样也需要经过汇编器转换成机器指令才能被CPU执行。需要注意的是，用go汇编语言编写的代码一旦经过汇编器转换成机器指令之后，再用调试工具反汇编出来的代码已经不是go语言汇编代码了，而是跟平台相关的汇编代码。</p>
<p>go汇编格式跟前面讨论过的AT&amp;T汇编基本上差不多，但也有些重要区别，本节就这些差异做一个简单说明。</p>
<p>寄存器</p>
<p>go汇编语言中使用的寄存器的名字与AMD64不太一样，下表显示了它们之间的对应关系：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/b82794380d4a3c60d206d2049dfcea42.png" alt></p>
<p>除了这些跟AMD64 CPU硬件寄存器一一对应的寄存器外，go汇编还引入了几个没有任何硬件寄存器与之对应的虚拟寄存器，这些寄存器一般用来存放内存地址，引入它们的主要目的是为了方便程序员和编译器用来定位内存中的代码和数据。</p>
<p>下面重点介绍在go汇编中常见的2个虚拟寄存器的使用方法：</p>
<p>FP虚拟寄存器：主要用来引用函数参数。go语言规定函数调用时参数都必须放在栈上，比如被调用函数使用 first_arg+0(FP)  来引用调用者传递进来的第一个参数，用second_arg+8(FP)来引用第二个参数 ，以此类推，这里的first_arg和second_arg仅仅是一个帮助我们阅读源代码的符号，对编译器来说无实际意义，+0和+8表示相对于FP寄存器的偏移量。我们用一个runtime中的函数片段作为例子来看看FP的使用。</p>
<p>go runtime中有一个叫gogo的函数，它接受一个gobuf类型的指针</p>
<p>// func gogo(buf *gobuf)<br>// restore state from Gobuf; longjmp<br>TEXT runtime·gogo(SB), NOSPLIT, $16-8<br>MOVQbuf+0(FP), BX// gobuf –&gt;bx<br>……<br>MOVQ buf+0(FP), BX这一条指令把调用者传递进来的指针buf放入BX寄存器中，可以看到，在gogo函数是通过buf+0(FP)这种方式获取到参数的。从被调用函数（此处为gogo函数）的角度来看，FP与函数栈帧之间的关系如下图，可以看出FP寄存器指向调用者的栈帧，而不是被调用函数的栈帧。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/089f5175585df78a61b81c4fbada8319.png" alt></p>
<p>SB虚拟寄存器：保存程序地址空间的起始地址。还记得在函数调用栈一节我们看过的进程在内存中的布局那张图吗，这个SB寄存器保存的值就是代码区的起始地址，它主要用来定位全局符号。go汇编中的函数定义、函数调用、全局变量定义以及对其引用会用到这个SB虚拟寄存器。对于这个虚拟寄存器，我们不用过多的关注，在代码中看到它时知道它是一个虚拟寄存器就行了。</p>
<p>操作码</p>
<p>AT&amp;T格式的寄存器操作码一般使用小写且寄存器的名字前面有个%符号，而go汇编使用大写而且寄存器名字前没有%符号，比如:</p>
<h1 id="AT-amp-T格式"><a href="#AT-amp-T格式" class="headerlink" title="AT&amp;T格式"></a>AT&amp;T格式</h1><p>mov %rbp,%rsp</p>
<h1 id="go汇编格式"><a href="#go汇编格式" class="headerlink" title="go汇编格式"></a>go汇编格式</h1><p>MOVQ BP,SP</p>
<p>操作数宽度（即操作数的位数）</p>
<p>AT&amp;T格式的汇编指令中如果有寄存器操作数，则根据寄存器的名字（比如rax, eax, ax, al分别代表64，32，16和8位寄存器）就可以确定操作数到底是多少位（8，16，32还是64位），所以不需要操作码后缀，如果没有寄存器操作数又是访存指令的话，则操作码需要加上后缀b、w、l或q来指定到底存取内存中的多少个字节。</p>
<p>而go汇编中，寄存器的名字没有位数之分，比如AX寄存器没有什么RAX, EAX之类的名字，指令中一律只能使用AX。所以如果指令中有操作数寄存器或是指令需要访问内存，则操作码都需要带上后缀B(8位)、W(16位)、D(32位)或Q(64位)。</p>
<p>函数定义</p>
<p>还是以go runtime中的gogo函数为例：</p>
<p>// func gogo(buf *gobuf)<br>// restore state from Gobuf; longjmp<br>TEXT runtime·gogo(SB), NOSPLIT, $16-8<br>……<br>下面对这个函数定义的第一行的各部分做个说明：</p>
<p>TEXT runtime·gogo(SB)：指明在代码区定义了一个名字叫gogo的全局函数（符号），该函数属于runtime包。</p>
<p>NOSPLIT：指示编译器不要在这个函数中插入检查栈是否溢出的代码。</p>
<p>$16-8：数字16说明此函数的栈帧大小为16字节，8说明此函数的参数和返回值一共需要占用8字节内存。因为这里的gogo函数没有返回值，只有一个指针参数，对于AMD64平台来说指针就是8字节。go语言中函数调用的参数和函数返回值都是放在栈上的，而且这部分栈内存是由调用者而非被调用函数负责预留，所以在函数定义时需要说明到底需要在调用者的栈帧中预留多少空间。</p>
<p>go汇编还有一些用法比较特别的地方，现在不讨论，等我们分析源代码遇到它们时再结合上下文做详细说明。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>Go 错误处理：用 panic 取代 err != nil 的模式</title>
    <url>/go/%E7%9F%A5%E8%AF%86%E7%82%B9/Go%20%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86%EF%BC%9A%E7%94%A8%20panic%20%E5%8F%96%E4%BB%A3%20err%20!=%20nil%20%E7%9A%84%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h2 id="Go-错误处理：用-panic-取代-err-nil-的模式"><a href="#Go-错误处理：用-panic-取代-err-nil-的模式" class="headerlink" title="Go 错误处理：用 panic 取代 err != nil 的模式"></a>Go 错误处理：用 panic 取代 err != nil 的模式</h2><p><a href="https://mp.weixin.qq.com/s/p77V3_LkREuXPVLdebmmmQ" rel="external nofollow noopener noreferrer" target="_blank">转载自煎鱼的blog</a></p>
<p>前段时间我分享了文章 《先睹为快，Go2 Error 的挣扎之路》后，和一位朋友进行了一次深度交流，他给我分享了他们项目组对于 Go 错误处理的方式调整。</p>
<p>简单来讲，就是在业务代码中使用 <code>panic</code> 的方式来替代 “永无止境” 的 <code>if err != nil</code>。</p>
<p><img src="1.png" alt="图片"></p>
<p>我们一起来看看是怎么做，又有什么优缺点，互相学习一轮。</p>
<h2 id="为什么想替换"><a href="#为什么想替换" class="headerlink" title="为什么想替换"></a>为什么想替换</h2><p>在 Go 语言中 <code>if err != nil</code> 写的太多，还要管方法声明各种，嫌麻烦又不方便：</p>
<figure class="highlight hsp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">err</span> := foo()</span><br><span class="line"><span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line">     <span class="comment">//do something..</span></span><br><span class="line">     <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">err</span> := foo()</span><br><span class="line"><span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line">     <span class="comment">//do something..</span></span><br><span class="line">     <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">err</span> := foo()</span><br><span class="line"><span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line">     <span class="comment">//do something..</span></span><br><span class="line">     <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">err</span> := foo()</span><br><span class="line"><span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line">     <span class="comment">//do something..</span></span><br><span class="line">     <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上述还是示例代码，比较直面。若是在工程实践，还得各种 package 跳来跳去加 <code>if err != nil</code>，讲更繁琐，要去关心整体的上下游。</p>
<p>其余更具体的就不赘述了，可以关注我的公众号翻看先前的文章。</p>
<h2 id="怎么替换-err-nil"><a href="#怎么替换-err-nil" class="headerlink" title="怎么替换 err != nil"></a>怎么替换 err != nil</h2><p>不想写 <code>if err != nil</code> 的代码，方式之一就是用 <code>panic</code> 来替代他。</p>
<p>示例代码如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GetFish</span><span class="params">(db *sql.DB, name <span class="keyword">string</span>)</span> []<span class="title">string</span></span> &#123;</span><br><span class="line"> rows, err := db.Query(<span class="string">"select name from users where `name` = ?"</span>, name)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="built_in">panic</span>(err)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">defer</span> rows.Close()</span><br><span class="line"></span><br><span class="line"> <span class="keyword">var</span> names []<span class="keyword">string</span></span><br><span class="line"> <span class="keyword">for</span> rows.Next() &#123;</span><br><span class="line">  <span class="keyword">var</span> name <span class="keyword">string</span></span><br><span class="line">  err := rows.Scan(&amp;name)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="built_in">panic</span>(err)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  names = <span class="built_in">append</span>(names, name)</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> err = rows.Err()</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="built_in">panic</span>(err)</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> names</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在上述业务代码中，我们通过 <code>panic</code> 的方式取代了 <code>return err</code> 的函数返回，自然其所关联的下游业务代码也就不需要编写 <code>if err != nil</code> 的代码：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> fish1 := GetFish(db, <span class="string">"煎鱼"</span>)</span><br><span class="line"> fish2 := GetFish(db, <span class="string">"咸鱼"</span>)</span><br><span class="line"> fish3 := GetFish(db, <span class="string">"摸鱼"</span>)</span><br><span class="line"> ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>同时在转换为使用 <code>panic</code> 模式的错误机制后，我们必须要在外层增加 <code>recover</code> 方法：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">AppRecovery</span><span class="params">()</span> <span class="title">gin</span>.<span class="title">HandlerFunc</span></span> &#123;</span><br><span class="line"> <span class="keyword">return</span> <span class="function"><span class="keyword">func</span><span class="params">(c *gin.Context)</span></span> &#123;</span><br><span class="line">  <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">   <span class="keyword">if</span> err := <span class="built_in">recover</span>(); err != <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> _, ok := err.(AppErr); ok &#123;</span><br><span class="line">     <span class="comment">// do something...</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">     <span class="built_in">panic</span>(err)</span><br><span class="line">    &#125;</span><br><span class="line">   &#125;</span><br><span class="line">  &#125;()</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>每次 <code>panic</code> 后根据其抛出的错误进行断言，识别是否定制的 <code>AppErr</code> 错误类型，若是则可以进行一系列的处理动作。</p>
<p>否则可继续向上 <code>panic</code> 抛出给顶级的 <code>Recovery</code> 方法进行处理。</p>
<p>这就是一个相对完整的 <code>panic</code> 错误链路处理了。</p>
<h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><ul>
<li><p>从优点上来讲：</p>
</li>
<li><ul>
<li>整体代码结构看起来更加的简洁，仅专注于实现逻辑即可。</li>
<li>不需要关注和编写冗杂的 <code>if err != nil</code> 的错误处理代码。</li>
</ul>
</li>
<li><p>从缺点上来讲：</p>
</li>
<li><ul>
<li>认知负担的增加，需要参加项目的每一个新老同学都清楚该模式，要做一个基本规范或培训。</li>
<li>存在一定的性能开销，每次 <code>panic</code> 都存在用户态的上下文切换。</li>
<li>存在一定的风险性，一旦 <code>panic</code> 没有 <code>recover</code> 住，就会导致事故。</li>
<li>Go 官方并不推荐，与 <code>panic</code> 本身的定义相违背，也就是 <code>panic</code> 与 <code>error</code> 的概念混淆。</li>
</ul>
</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在今天这篇文章给大家分享了如何使用 <code>panic</code> 的方式来处理 Go 的错误，其必然有利必有有弊，需要做一个权衡了。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>深入浅出Go Runtime</title>
    <url>/go/%E6%B7%B1%E5%85%A5%E6%B5%85%E5%87%BAGo%20Runtime/</url>
    <content><![CDATA[<p>以下内容转载自 <a href="https://mp.weixin.qq.com/s/ivO-USpxiyrL-9BzgE8Vcg" rel="external nofollow noopener noreferrer" target="_blank">yifhao</a></p>
<p>介绍</p>
<p>基于2019.02发布的go 1.12 linux amd64版本, 主要介绍了Runtime一些原理和实现的一些细节, 对大家容易不容易理解或者网络上很多错误的地方做一些梳理:</p>
<ol>
<li><p>Golang Runtime是个什么? Golang Runtime的发展历程, 每个版本的改进</p>
</li>
<li><p>Go调度: 协程结构体, 上下文切换, 调度队列, 大致调度流程, 同步执行流又不阻塞线程的网络实现等</p>
</li>
<li><p>Go内存: 内存结构, mspan结构, 全景图及分配策略等</p>
</li>
<li><p>Go GC: Golang GC停顿大致的一个发展历程, 三色标记实现的一些细节, 写屏障, 三色状态, 扫描及元信息, 1.12版本相对1.5版本的改进点, GC Pacer等</p>
</li>
<li><p>实践: 观察调度, GC信息, 一些优化的方式, 几点问题排查的思路, 几个有意思的问题排查</p>
</li>
<li><p>总结: 贯穿Runtime的思想总结</p>
</li>
</ol>
<p>这个是我今年8月份在深圳Gopher Meetup做的一个关于runtime的分享, 10月份在Go夜读(项目地址 <a href="https://reading.developerlearning.cn" rel="external nofollow noopener noreferrer" target="_blank">https://reading.developerlearning.cn</a>) 里也分享了一次</p>
<p>视频地址: <a href="https://www.bilibili.com/video/av73297683?seid=596154768710832594" rel="external nofollow noopener noreferrer" target="_blank">https://www.bilibili.com/video/av73297683?seid=596154768710832594</a></p>
<p>一些问题解答: <a href="https://github.com/developer-learning/night-reading-go/issues/492" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/developer-learning/night-reading-go/issues/492</a></p>
<p>两个分享都是用的精简版, 这里的把完整版的讲一下, 加一些注语, 注语位于PPT页的下面. (ppt地址 <a href="https://github.com/yifhao/share)" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/yifhao/share)</a>.</p>
<p><strong>序</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/956519af574e8fe5543e4af5c7a51044.png" alt><br><img src="https://img-blog.csdnimg.cn/img_convert/956519af574e8fe5543e4af5c7a51044.png" alt><br><img src="https://img-blog.csdnimg.cn/img_convert/26fe297b3e2896343891acf2dd9aeb89.png" alt></p>
<p>为什么去了解runtime呢?</p>
<ol>
<li><p>可以解决一些棘手的问题: 在写这个PPT的时候, 就有一位朋友在群里发了个pprof图, 说同事写的代码有问题, CPU利用率很高., 找不出来问题在哪, 我看了下pprof图, 说让他找找是不是有这样用select的, 一查的确是的. 平时也帮同事解决了一些和并发, 调度, GC有关的问题</p>
</li>
<li><p>好奇心: 大家写久了go, 惊叹于它的简洁, 高性能外, 必然对它是怎么实现的有很多好奇. 协程怎么实现, GC怎么能并发, 对象在内存里是怎么存在的? 等等</p>
</li>
<li><p>技术深度的一种 </p>
</li>
</ol>
<p><img src="https://img-blog.csdnimg.cn/img_convert/58384f3d3cf2d1e4e76a45b3d95888f7.png" alt><br>本次分享基于2019.02发布的go 1.12 linux amd64版本, 主要介绍了Runtime实现的一点细节. 水平和精力有限, 必然有问题存在, 有问题欢迎大家给我留言.</p>
<p><strong>Runtime简介及发展</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/2c6c8870f19cb6c0405c2ff9f5bb0830.png" alt></p>
<p>Runtime简介</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a026e1b872e602cf68223b2e3f7a41cc.png" alt></p>
<p>go的runtime代码在go sdk的runtime目录下. </p>
<p>主要有所述的4块功能. 提到runtime, 大家可能会想起java, python的runtime. </p>
<p>不过go和这两者不太一样, java, python的runtime是虚拟机, 而go的runtime和用户代码一起编译到一个可执行文件中. </p>
<p>用户代码和runtime代码除了代码组织上有界限外, 运行的时候并没有明显的界限. </p>
<p>如上所示, 一些常用的关键字被编译成runtime包下的一些函数调用.</p>
<p>Runtime版本历史</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/4adbdd78f912cd2ac031c3772551b1c7.png" alt></p>
<p>左边标粗的是一些更新比较大的版本. 右边的GC STW仅供参考.</p>
<p>调度</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/b0774cf0bc39a2f67d266716a3d0609c.png" alt></p>
<p>调度简述</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/1592157a45a76e3ab4342ceb2b13de9d.png" alt></p>
<p>goroutine实现</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/11c1555ec32bc00250b756fa297b9b5d.png" alt></p>
<p>我们去看调度的一个进化, 从进程到线程再到协程, 其实是一个不断共享, 不断减少切换成本的过程. go实现的协程为有栈协程, go协程的用法和线程的用法基本类似. 很多人会疑问, 协程到底是个什么东西? 用户态的调度感觉很陌生, 很抽象, 到底是个什么东西?</p>
<p>我觉得要理解调度, 要理解两个概念: 运行和阻塞. 特别是在协程中, 这两个概念不容易被正确理解. 我们理解概念时往往会代入自身感受, 觉得线程或协程运行就是像我们吭哧吭哧的处理事情, 线程或协程阻塞就是做事情时我们需要等待其他人, 然后就在这等着了. 要是其他人搞好了, 那我们就继续做当前的事. 其实主体对象搞错了. 正确的理解应该是我们处理事情时就像CPU, 而不是像线程或者协程. 假如我当前在写某个服务, 发现依赖别人的函数还没有ready, 那就把写服务这件事放一边. 点开企业微信, 我去和产品沟通一些问题了. 我和产品沟通了一会后, 检查一下, 发现别人已经把依赖的函数提交了, 然后我就最小化企业微信, 切到IDE, 继续写服务A了.</p>
<p>对操作系统有过一些了解, 知道linux下的线程其实是task_struct结构, 线程其实并不是真正运行的实体, 线程只是代表一个执行流和其状态. 真正运行驱动流程往前的其实是CPU. CPU在时钟的驱动下, 根据PC寄存器从程序中取指令和操作数, 从RAM中取数据, 进行计算, 处理, 跳转, 驱动执行流往前. CPU并不关注处理的是线程还是协程, 只需要设置PC寄存器, 设置栈指针等(这些称为上下文), 那么CPU就可以欢快的运行这个线程或者这个协程了.</p>
<p>线程的运行, 其实是被运行. 其阻塞, 其实是切换出调度队列, 不再去调度执行这个执行流. 其他执行流满足其条件, 便会把被移出调度队列的执行流重新放回调度队列.</p>
<p>协程同理, 协程其实也是一个数据结构, 记录了要运行什么函数, 运行到哪里了. go在用户态实现调度, 所以go要有代表协程这种执行流的结构体, 也要有保存和恢复上下文的函数, 运行队列. 理解了阻塞的真正含义, 也就知道能够比较容易理解, 为什么go的锁, channel这些不阻塞线程. 对于实现的同步执行流效果, 又不阻塞线程的网络, 接下来也会介绍.</p>
<p><strong>协程结构体和切换函数</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/b2603dc38e476009aed8ebd4ad7f39c3.png" alt></p>
<p>我们go一个func时一般这样写</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">go</span> func1(arg1 type1,arg2 type2)&#123;....&#125;(a1,a2)</span><br></pre></td></tr></table></figure>
<p>一个协程代表了一个执行流, 执行流有需要执行的函数(对应上面的func1), 有函数的入参(a1, a2), 有当前执行流的状态和进度(对应CPU的PC寄存器和SP寄存器), 当然也需要有保存状态的地方, 用于执行流恢复. 真正代表协程的是runtime.g结构体. 每个go func都会编译成runtime.newproc函数, 最终有一个runtime.g对象放入调度队列. 上面的func1函数的指针设置在runtime.g的startfunc字段, 参数会在newproc函数里拷贝到stack中, sched用于保存协程切换时的pc位置和栈位置. 协程切换出去和恢复回来需要保存上下文, 恢复上下文, 这些由以下两个汇编函数实现. 以上就能实现协程这种执行流, 并能进行切换和恢复. (下图中的struct和函数都做了精简)</p>
<p>GM模型及GPM模型</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/4c62580d46b6128aec8a1b5ab6a938a8.png" alt></p>
<p>有了协程的这种执行流形式, 那待运行的协程放在哪呢? 在Go1.0的时候:</p>
<ol>
<li><p>调度队列schedt是全局的, 对该队列的操作均需要竞争同一把锁, 导致伸缩性不好.</p>
</li>
<li><p>新生成的协程也会放入全局的队列, 大概率是被其他m(可以理解为底层线程的一个表示)运行了, 内存亲和性不好. 当前协程A新生成了协程B, 然后协程A比较大概率会结束或者阻塞, 这样m直接去执行协程B, 内存的亲和性也会好很多.</p>
</li>
<li><p>因为mcache与m绑定, 在一些应用中(比如文件操作或其他可能会阻塞线程的系统调用比较多), m的个数可能会远超过活跃的m个数, 导致比较大的内存浪费..</p>
</li>
</ol>
<p>那是不是可以给m分配一个队列, 把阻塞的m的mcache给执行go代码的m使用? Go 1.1及以后就是这样做的.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/edcbd581533fcda413571a0197d485d1.png" alt></p>
<p>在1.1中调度模型更改为GPM模型, 引入逻辑Process的概念, 表示执行Go代码所需要的资源, 同时也是执行Go代码的最大的并行度. 这个概念可能很多人不知道怎么理解. P涉及到几点, 队列和mcache, 还有P的个数的选取. 首先为什么把全局队列打散, 以及mcache为什么跟随P, 这个在GM模型那一页就讲的比较清楚了. 然后为什么P的个数默认是CPU核数: Go尽量提升性能, 那么在一个n核机器上, 如何能够最大利用CPU性能呢? 当然是同时有n个线程在并行运行中, 把CPU喂饱, 即所有核上一直都有代码在运行. 在go里面, 一个协程运行到阻塞系统调用, 那么这个协程和运行它的线程m, 自然是不再需要CPU的, 也不需要分配go层面的内存. 只有一直在并行运行的go代码才需要这些资源, 即同时有n个go协程在并行执行, 那么就能最大的利用CPU, 这个时候需要的P的个数就是CPU核数. (注意并行和并发的区别)</p>
<p><strong>协程状态及流转</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/4227ae99c3365fbe16d1ff3c2014afa2.png" alt></p>
<p>协程的状态其实和线程状态类似,状态转换和发生状态转换的时机如图所示. 还是需要注意: 协程只是一个执行流, 并不是运行实体.</p>
<p>调度</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/49d06747d9dd21fc6655610b555bd963.png" alt></p>
<p>并没有一个一直在运行调度的调度器实体. 当一个协程切换出去或新生成的m, go的运行时从stw中恢复等情况时, 那么接下来就需要发生调度. go的调度是通过线程(m)执行runtime.schedule函数来完成的.</p>
<p>sysmon协程</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/6c3d465fe72a90c5ad425f8a9af5255e.png" alt></p>
<p>在linux内核中有一些执行定时任务的线程, 比如定时写回脏页的pdflush, 定期回收内存的kswapd0, 以及每个cpu上都有一个负责负载均衡的migration线程等. </p>
<p>在go运行时中也有类似的协程, sysmon. 功能比较多: 定时从netpoll中获取ready的协程, 进行抢占, 定时GC,打印调度信息,归还内存等定时任务</p>
<p><strong>协作式抢占</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/908bd0d57f00f2598a41d2732642e58a.png" alt></p>
<p>go目前(1.12)还没有实现非协作的抢占. 基本流程是sysmon协程标记某个协程运行过久, 需要切换出去, 该协程在运行函数时会检查栈标记, 然后进行切换.</p>
<p><strong>同步执行流不阻塞线程的网络的实现</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/95ae519bbf6bf5efc176e8a280af655a.png" alt></p>
<p>go写后台最舒服的就是能够以同步写代码的方式操作网络, 但是网络操作不阻塞线程. </p>
<p>主要是结合了非阻塞的fd, epoll以及协程的切换和恢复. </p>
<p>linux提供了网络fd的非阻塞模式, 对于没有ready的非阻塞fd执行网络操作时, linux内核不阻塞线程, 会直接返回EAGAIN, 这个时候将协程状态设置为wait, 然后m去调度其他协程. </p>
<p>go在初始化一个网络fd的时候, 就会把这个fd使用epollctl加入到全局的epoll节点中. 同时放入epoll中的还有polldesc的指针.</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">netpollopen</span><span class="params">(fd <span class="keyword">uintptr</span>, pd *pollDesc)</span> <span class="title">int32</span></span> &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> ev epollevent</span><br><span class="line"></span><br><span class="line">ev.events = _EPOLLIN | _EPOLLOUT | _EPOLLRDHUP | _EPOLLET</span><br><span class="line"></span><br><span class="line">*(**pollDesc)(unsafe.Pointer(&amp;ev.data)) = pd</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span>-epollctl(epfd, _EPOLL_CTL_ADD, <span class="keyword">int32</span>(fd), &amp;ev)</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在sysmon中, schedule函数中, start the world中等情况下, 会执行netpoll 调用epollwait系统调用.</p>
<p>把ready的网络事件从epoll中取出来, 每个网络事件可以通过前面传入的polldesc获取到阻塞在其上的协程, 以此恢复协程为runnable.</p>
<p><strong>调度相关结构体</strong><br>图片<img src="https://img-blog.csdnimg.cn/img_convert/d662d7248662977ae088ee44c85d0283.png" alt><br>调度综述<br><img src="https://img-blog.csdnimg.cn/img_convert/53ccf374e84a0bc85b35670de7a9057e.png" alt></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/ac09c54cbe6c690efcad9643c1d9560b.png" alt></p>
<p>内存分配<br><img src="https://img-blog.csdnimg.cn/img_convert/12ba0029b77d03b23aced102d144f7ce.png" alt></p>
<p>内存分配简介<br><img src="https://img-blog.csdnimg.cn/img_convert/a4a6116ce29c3cc8aa8d2075a8a5b01a.png" alt></p>
<p>Go的分配采用了类似tcmalloc的结构. </p>
<p>特点: 使用一小块一小块的连续内存页, 进行分配某个范围大小的内存需求. </p>
<p>比如某个连续8KB专门用于分配17-24字节,以此减少内存碎片. 线程拥有一定的cache, 可用于无锁分配.</p>
<p>同时Go对于GC后回收的内存页, 并不是马上归还给操作系统, 而是会延迟归还, 用于满足未来的内存需求.</p>
<h1 id="内存空间结构"><a href="#内存空间结构" class="headerlink" title="内存空间结构"></a>内存空间结构</h1><p><img src="https://img-blog.csdnimg.cn/img_convert/4df72677ce42f969808fb09ad1d98a13.png" alt></p>
<p>在1.10以前go的堆地址空间是线性连续扩展的, 比如在1.10(linux amd64)中, 最大可扩展到512GB. </p>
<p>因为go在gc的时候会根据拿到的指针地址来判断是否位于go的heap的, 以及找到其对应的span, 其判断机制需要gc heap是连续的. </p>
<p>但是连续扩展有个问题, cgo中的代码(尤其是32位系统上)可能会占用未来会用于go heap的内存. 这样在扩展go heap时, mmap出现不连续的地址, 导致运行时throw. </p>
<p>在1.11中, 改用了稀疏索引的方式来管理整体的内存. 可以超过512G内存, 也可以允许内存空间扩展时不连续. </p>
<p>在全局的mheap struct中有个arenas二阶数组, 在linux amd64上,一阶只有一个slot, 二阶有4M个slot, 每个slot指向一个heapArena结构, 每个heapArena结构可以管理64M内存, 所以在新的版本中, go可以管理4M*64M=256TB内存, 即目前64位机器中48bit的寻址总线全部256TB内存.</p>
<p>span机制</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/7518d16f16322fd9389fd76dde496820.png" alt></p>
<p>前面提到了go的内存分配类似于tcmalloc, 采用了span机制来减少内存碎片. </p>
<p>每个span管理8KB整数倍的内存, 用于分配一定范围的内存需求.</p>
<p>内存分配全景</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0aab079a6e87f17d361c510bbd453ad2.png" alt></p>
<p>多层次的分配Cache, 每个P上有一个mcache, mcache会为每个size最多缓存一个span, 用于无锁分配. </p>
<p>全局每个size的span都有一个mcentral, 锁的粒度相对于全局的heap小很多, 每个mcentral可以看成是每个size的span的一个全局后备cache. </p>
<p>在gc完成后, 会把P中的span都flush到mcentral中, 用于清扫后再分配. P有需要span时, 从对应size的mcentral获取. 获取不到再上升到全局的heap.</p>
<p>几种特殊的分配器</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5fe4d7c202dbe016f2e075508b6953bc.png" alt></p>
<p>对于很小的对象分配, go做了个优化, 把小对象合并, 以移动指针的方式分配. </p>
<p>对于栈内存有stackcache分配, 也有多个层次的分配, 同时stack也有多个不同size. </p>
<p>用于分配stack的内存也是位于go gc heap, 用mspan管理, 不过这个span的状态和用于分配对象的mspan状态不太一样, 为mSpanManual. </p>
<p>我们可以思考一个问题, go的对象是分配在go gc heap中, 并由mcache, mspan, mcentral这些结构管理, 那么mcache, mspan, mcentral这些结构又是哪里管理和分配的呢? </p>
<p>肯定不是自己管理自己. 这些都是由特殊的分配fixalloc分配的, 每种类型有一个fixalloc, 大致原理就是通过mmap从进程空间获取一小块内存(百KB的样子), 然后用来分配这个固定大小的结构.</p>
<p>内存分配综合</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/75e74d9f0e5324b2ce6e45827ac55e26.png" alt></p>
<p>GC</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e34ea1c798ae43f5d84fc806769233df.png" alt></p>
<p>Golang GC简述</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e67a8b147beeb1d1f67c7ae46a125b76.png" alt></p>
<p>GC简介</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/7f0561422e53fe24a6bf04342a0ed3d8.png" alt></p>
<p>GC并不是个新事物, 使得GC大放光彩的是Java语言.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/777148d29cf6070538b3707861048bad.png" alt></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c74ce88343959558994d45e89ca48730.png" alt></p>
<p><strong>Golang GC发展</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/45b8e0d85166b17f92ad740083d03a57.png" alt></p>
<p>上面是几个比较重要的版本. 左图是根据twitter工程师的数据绘制的(堆比较大), 从1.4的百ms级别的停顿到1.8以后的小于1ms. </p>
<p>右图是我对线上服务(Go 1.11编译)测试的一个结果, 是一个批量拉取数据的服务, 大概3000qps, 服务中发起的rpc调用大概在2w/s. 可以看到大部分情况下GC停顿小于1ms, 偶尔超过一点点. </p>
<p>整体来说golang gc用起来是很舒心的, 几乎不用你关心.</p>
<p>三色标记</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bfcc7b369733004d64ba5648fd688491.png" alt></p>
<p>go采用的是并发三色标记清除法. </p>
<p>图展示的是一个简单的原理.</p>
<p>有几个问题可以思考一下: </p>
<p>并发情况下, 会不会漏标记对象? </p>
<p>对象的三色状态存放在哪? </p>
<p>如何根据一个对象来找到它引用的对象?</p>
<p>写屏障</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/8707802a49b492dd7c0df4072e114ab9.png" alt></p>
<p>GC最基本的就是正确性: 不漏标记对象, 程序还在用的对象都被清除了, 那程序就错误了. 有一点浮动垃圾是允许的. </p>
<p>在并发情况下, 如果没有一些措施来保障, 那可能会有什么问题呢? </p>
<p>看左边的代码和图示, 第2步标记完A对象, A又没有引用对象, 那A变成黑色对象. </p>
<p>在第3步的时候, muator(程序)运行, 把对象C从B转到了A, </p>
<p>第4步, GC继续标记, 扫描B, 此时B没有引用对象, 变成了黑色对象. 我们会发现C对象被漏标记了.</p>
<p>如何解决这个问题? go使用了写屏障, 这里的写屏障是指由编译器生成的一小段代码. 在gc时对指针操作前执行的一小段代码, 和CPU中维护内存一致性的写屏障不太一样哈. 所以有了写屏障后, 第3步, A.obj=C时, 会把C加入写屏障buf. 最终还是会被扫描的.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/780e50f5a9e13248827f09141f183068.png" alt></p>
<p>这里感受一下写屏障具体生成的代码. </p>
<p>我们可以看到在写入指针slot时, 对写屏障是否开启做了判断, 如果开启了, 会跳转到写屏障函数, 执行加入写屏障buf的逻辑. </p>
<p>1.8中写屏障由Dijkstra写屏障改成了混合式写屏障, 使得GC停顿达到了1ms以下.</p>
<p>三色状态</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c32de8adbf70398a57a1f1e7e1bab613.png" alt></p>
<p>并没有这样一个集合把不同状态对象放到对应集合中. 只是一个逻辑上的意义.</p>
<p><strong>扫描和元信息</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/558f821c6f9666c7802727cd16cb2140.png" alt></p>
<p>gc拿到一个指针, 如何把这个指针指向的对象其引用的子对象都加到扫描队列呢? 而且go还允许内部指针, 似乎更麻烦了. </p>
<p>我们分析一下, 要知道对象引用的子对象, 从对象开始到对象结尾, 把对象那一块内存上是指针的放到扫描队列就好了. </p>
<p>那我们是不是得知道对象有多大, 从哪开始到哪结束, 同时要知道内存上的8个字节, 哪里是指针, 哪里是普通的数据. </p>
<p>首先go的对象是mspan管理的, 我们如果能知道对象属于哪个mspan, 就知道对象多大, 从哪开始, 到哪结束了. </p>
<p>前面我们讲到了areans结构, 可以通过指针加上一定的偏移量, 就知道属于哪个heap arean 64M块. 再通过对64M求余, 结合spans数组, 即可知道属于哪个mspan了.</p>
<p>结合heapArean的bitmap和每8个字节在heapArean中的偏移, 就可知道对象每8个字节是指针还是普通数据(这里的bitmap是在分配对象时根据type信息就设置了, type信息来源于编译器生成)</p>
<p>GC流程</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/231f18b9ed9d11d58c7fd255062d72b4.png" alt></p>
<p>1.5和1.12的GC大致流程相同. </p>
<p>上图是golang官方的ppt里的图, 下图是我根据1.12源码绘制的. </p>
<p>从最坏可能会有百ms的gc停顿到能够稳定在1ms以下, 这之间GC做了很多改进. </p>
<p>右边是我根据官方issues整理的一些比较重要的改进. 1.6的分布式检测, 1.7将栈收缩放到了并发扫描阶段, 1.8的混合写屏障, 1.12更改了mark termination检测算法, mcache flush移除出mark termination等等…</p>
<p><strong>Golang GC Pacer</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/e5e478f0a2cf8907dfaa919fd952c7c9.png" alt></p>
<p>大家对并发GC除了怎么保证不漏指针有疑问外, 可能还会疑问, 并发GC如何保证能够跟得上应用程序的分配速度? 会不会分配太快了, GC完全跟不上, 然后OOM?</p>
<p>这个就是Golang GC Pacer的作用. </p>
<p>Go的GC是一种比例GC, 下一次GC结束时的堆大小和上一次GC存活堆大小成比例. 由GOGC控制, 默认100, 即2倍的关系, 200就是3倍, 以此类推. </p>
<p>假如上一次GC完成时, 存活对象1000M, 默认GOGC 100, 那么下次GC会在比较接近但小于2000M的时候(比如1900M)开始, 争取在堆大小达到2000M的时候结束. </p>
<p>这之间留有一定的裕度, 会计算待扫描对象大小(根据历史数据计算)与可分配的裕度的比例, 应用程序分配内存根据该比例进行辅助GC, 如果应用程序分配太快了, 导致credit不够, 那么会被阻塞, 直到后台的mark跟上来了,该比例会随着GC进行不断调整. </p>
<p>GC结束后, 会根据这一次GC的情况来进行负反馈计算, 计算下一次GC开始的阈值. 如何保证按时完成GC呢? </p>
<p>GC完了后, 所有的mspan都需要sweep, 类似于GC的比例, 从GC结束到下一次GC开始之间有一定的堆分配裕度, 会根据还有多少的内存需要清扫, 来计算分配内存时需要清扫的span数这样的一个比例.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/400be7421e780d12accd26c964490a59.png" alt></p>
<p>实践与总结</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/7d5f9ac0b362f9be8905acfde9243a45.png" alt><br><strong><br>观察调度</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/066a6cfd0386d6035ec459f9ca97052c.png" alt><br>观察一下调度, 加一些请求. </p>
<p>我们可以看到虽然有1000个连接, 但是go只用了几个线程就能处理了, 表明go的网络的确是由epoll管理的. </p>
<p>runqueue表示的是全局队列待运行协程数量, 后面的数字表示每个P上的待运行协程数. </p>
<p>可以看到待处理的任务并没有增加, 表示虽然请求很多, 但完全能hold住. </p>
<p>同时可以看到, 不同P上有的时候可能任务不均衡, 但是一会后, 任务又均衡了, 表示go的work stealing是有效的.</p>
<p>观察GC</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/cd6c3bb5603431e170d4972427166904.png" alt></p>
<p>其中一些数据的含义, 在分享的时候没有怎么解释, 不过网上的解释几乎没有能完全解释正确. </p>
<p>我这里敲一下. 其实一般关注堆大小和两个stw的wall time即可. </p>
<p>gc 8913(第8913次gc) @2163.341s(在程序运行的第2163s) 1%(gc所有work消耗的历史累计CPU比例, 所以其实这个数据没太大意义) 0.13(第一个stw的wall time)+14(并发mark的wall time)+0.20(第二个stw的wall time) ms clock, 1.1(第一个stw消耗的CPU时间)+21(用户程序辅助扫描消耗的cpu时间)/22(分配用于mark的P消耗的cpu时间)/0(空闲的P用于mark的cpu时间)+1.6ms(第2个stw的cpu时间) cpu, 147(gc开始时的堆大小)-&gt;149(gc结束的堆大小)-&gt;75MB(gc结束时的存活堆大小), 151 MB goal(本次gc预计结束的堆大小), 8P(8个P)</p>
<p>优化<br><img src="https://img-blog.csdnimg.cn/img_convert/cd6c3bb5603431e170d4972427166904.png" alt></p>
<p>个人建议, 没事不要总想着优化, 好好curd就好.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3e93f123ae3cd4d7b9bfe01e02792df4.png" alt></p>
<p>当然还是有一些优化方法的..</p>
<p>一点实践</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/97dc441d76392a14371a601e2ca678fd.png" alt></p>
<p>我们将pprof的开启集成到模板中, 并自动选择端口, 并集成了gops工具, 方便查询runtime信息, 同时在浏览器上可直接点击生成火焰图, pprof图, 非常的方便, 也不需要使用者关心.</p>
<p>问题排查的一点思路</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/4980c5c2d3e4536ec05f71d2bcb7419a.png" alt><br>一次有意思的问题排查</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2640da9265d8f2fdeb0188a1d367fbdd.png" alt></p>
<p>负载, 依赖服务都很正常, CPU利用率也不高, 请求也不多, 就是有很多超时.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c74b27eb841a5f8bafad3d4431d4a61b.png" alt></p>
<p>该服务在线上打印了debug日志, 因为早期的服务模板开启了gctrace, 框架把stdout重定向到一个文件了. 而输出gctrace时本来是到console的, 输出到文件了, 而磁盘跟不上, 导致gctrace日志被阻塞了.</p>
<p>这里更正一下ppt中的内容, 并不是因为gc没完成而导致其他协程不能运行, 而是后续gc无法开启, 导致实质上的stw. 打印gc trace日志时, 已经start the world了, 其他协程可以开始运行了. 但是在打印gctrace日志时, 还保持着开启gc需要的锁, 所以, 打印gc trace日志一直没完成, 而gc又比较频繁, 比如0.1s一次, 这样会导致下一次gc开始时无法获取锁, 每一个进入gc检查的p阻塞, 实际上就造成了stw.</p>
<p><strong>Runtime的一点个人总结</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/24bb1ec7fccaeff492163f58aa715eb5.png" alt></p>
<p>并行, 纵向多层次, 横向多个class, 缓存, 缓冲, 均衡.</p>
<p>参考文档</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/6209335ec65ade263338bbfca40f37eb.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part11：Upvalue</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part11/</url>
    <content><![CDATA[<p><a href="http://manistein.club/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="构建Lua解释器Part11：Upvalue"><a href="#构建Lua解释器Part11：Upvalue" class="headerlink" title="构建Lua解释器Part11：Upvalue"></a>构建Lua解释器Part11：Upvalue</h1><h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>​    本章，我将深入探讨lua的upvalue机制。在写这篇博客之前，我已经自己动手实现了这个机制，并且将其提交到了github仓库<a href="https://github.com/Manistein/dummylua-tutorial" rel="external nofollow noopener noreferrer" target="_blank">dummylua</a>上了。为了专注于upvalue机制本身的讨论，本章不会展示大量的代码细节，尽量在抽象层面来论述。</p>
<h1 id="什么是Upvalue？"><a href="#什么是Upvalue？" class="headerlink" title="什么是Upvalue？"></a>什么是Upvalue？</h1><p>​    upvalue是让lua来模拟实现，类似c语言中，静态变量机制的一种机制。一个带上upvalue的函数，我们称之为闭包(closure)。一个c函数，带上upvalue的结构，我们称之为c闭包。而一个lua函数，带上upvalue，我们称之为lua闭包。我们可以看一下官方对于upvalue相关内容的阐述[1]。</p>
<blockquote>
<p>While the registry implements global values, the upvalue mechanism implements an equivalent of C static variables, which are visible only inside a particular function. Every time you create a new C function in Lua, you can associate with it any number of upvalues; each upvalue can hold a single Lua value. Later, when the function is called, it has free access to any of its upvalues, using pseudo-indices.</p>
<p>We call this association of a C function with its upvalues a closure. Remember that, in Lua code, a closure is a function that uses local variables from an outer function. A C closure is a C approximation to a Lua closure. One interesting fact about closures is that you can create different closures using the same function code, but with different upvalues.</p>
</blockquote>
<p>简单的文字，并不能给我们具象化的展示，这里通过几个例子，来直观感受一下，什么是upvalue：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> upval = <span class="number">1</span></span><br><span class="line"><span class="keyword">local</span> upval2 = <span class="number">2</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span><span class="params">()</span></span></span><br><span class="line">	<span class="keyword">local</span> locvar = <span class="number">3</span></span><br><span class="line">	<span class="built_in">print</span>(upval)</span><br><span class="line"></span><br><span class="line">	<span class="keyword">local</span> <span class="function"><span class="keyword">function</span> <span class="title">aaa</span><span class="params">()</span></span></span><br><span class="line">		<span class="built_in">print</span>(upval+upval2+locvar)</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line">	aaa()</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>在上面这个例子中，test函数的外层，一共有两个变量，upval和upval2。test函数内，有一行打印upval的代码，因为有这个操作，test函数需要引用到外层函数的upval这个变量，因此upval是test函数的一个upvavle。而上面这个例子，在test函数内，又定义了一个aaa函数，在aaa函数内，因为使用了upval、upval2和locvar几个变量，因此aaa函数的upvalue有upval、upval2和locvar。由于aaa是test的内层定义的函数，test是aaa的外层函数，因此test函数的upvalue也包括了，upval、upval2。现在，只是给大家一个直观的概念，其实很多读者对此已经耳熟能详了，在后面的章节内容中，我会更细致地论述这个机制的原理。</p>
<h1 id="Lua函数探索"><a href="#Lua函数探索" class="headerlink" title="Lua函数探索"></a>Lua函数探索</h1><p>​    要彻底搞明白upvalue的概念，首先，我们要捋清楚lua函数的概念，前面也提到过，带upvalue的函数就是closure（实际上每个lua函数都至少包含一个upvalue）。从类型上来看，lua一共有3种函数，分别是light c function、c closure和lua closure。关于第一种，light c function的概念，操作和调用流程，在<a href="http://manistein.club/post/program/let-us-build-a-lua-interpreter/构建lua解释器part1/" rel="external nofollow noopener noreferrer" target="_blank">第一章</a>有着非常详细的阐述。至于第二种c closure，它有自己独立的数据结构，包含一个函数指针，以及一个upvalue列表，c closure实例受gc管控，c closure的创建流程，和运行流程，可以luabase.c文件里的luaB_openbase函数，以及<a href="http://manistein.club/post/program/let-us-build-a-lua-interpreter/构建lua解释器part5/" rel="external nofollow noopener noreferrer" target="_blank">第5章</a>调用函数的流程里找到。现在，我将集中篇幅，来探讨lua closure。</p>
<h4 id="Chunk的概念"><a href="#Chunk的概念" class="headerlink" title="Chunk的概念"></a>Chunk的概念</h4><p>​    在开始讨论lua函数之前，我们不妨来复习一下chunk的概念，实际上在第五章，我已经论述过chunk的概念，这里为了内容完整性，我决定重新再论述一次。chunk是什么？在《Programming In Lua》里的解释是[2]：</p>
<blockquote>
<p>Each piece of code that Lua executes, such as a file or a single line in interactive mode, is a chunk. More specifically, a chunk is simply a sequence of statements.</p>
</blockquote>
<p>chunk本质是一个文件里的所有代码，或是交互模式下的一个字符串，其实质就是statement流的集合。</p>
<h4 id="Lua的函数层次"><a href="#Lua的函数层次" class="headerlink" title="Lua的函数层次"></a>Lua的函数层次</h4><p>​    现在，我要来论述一下Lua的函数层次了。前面已经探讨了chunk的概念，那么什么是lua的函数层次，它和chunk又有什么关系呢？现在假设，我们有个脚本test.lua，代码如下所示：</p>
<figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">1 </span> -- test.lua</span><br><span class="line"><span class="symbol">2 </span> local a = <span class="number">1</span></span><br><span class="line"><span class="symbol">3 </span> local b = <span class="number">2</span></span><br><span class="line"><span class="symbol">4 </span> function xxx()</span><br><span class="line"><span class="symbol">5 </span> 		<span class="keyword">print</span>(a + b)</span><br><span class="line"><span class="symbol">6 </span> <span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>chunk是什么？chunk就是test.lua文件内的所有代码(第1~第6行)，它和函数层次有什么关系呢？实际上，一个chunk（上面例子中的第1~第6行代码）实际上就是一个lua函数，只是它没有函数名，不需要通过function关键字来修饰，这个chunk所代表的函数，被称之为top-level function，也是level 1函数。而xxx函数是在top-level function里定义的，因此它是level 2函数(top-level function是level1)，如果函数xxx内，如果还有定义其他函数，那么这个函数的level就是3，以此类推。实际上，level的级别，代表了函数定义嵌套的层次。最关键的还是top-level function和chunk的关系。函数的层次关系，对于后续理解upvalue机制，非常重要。<br><img src="function-level.png" alt="image">图1<br>图1展示了上面例子中，函数的层次关系。除去top-level function，其他层次的函数，在定义上实际上可以有多重表示含义，比如我们的xxx函数的定义，其实质可以替换为如下所示的形式：</p>
<figure class="highlight fortran"><table><tr><td class="code"><pre><span class="line">xxx = <span class="function"><span class="keyword">function</span><span class="params">()</span></span></span><br><span class="line">	<span class="built_in">print</span>(a + b)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>因为在lua中，function是first-class类型，因此函数本身就是一种变量类型，因此这样的语法是成立且合理的。</p>
<h4 id="Lua-Closure的数据结构"><a href="#Lua-Closure的数据结构" class="headerlink" title="Lua Closure的数据结构"></a>Lua Closure的数据结构</h4><p>​    我在<a href="http://manistein.club/post/program/let-us-build-a-lua-interpreter/构建lua解释器part8/" rel="external nofollow noopener noreferrer" target="_blank">第8章</a>，详细论述了函数体编译的流程，对lua函数的数据结构也有非常详细的论述，因此过多的细节，在这里不再赘述，只在抽象层面复习一下它的数据结构。我们前面提过，什么是闭包(Closure)了，不管是c函数还是lua函数，只要带有upvalue的就是闭包，实际上，每个lua函数，至少带有一个upvalue，我将在后面的内容中，着重阐述这一点。先来回顾一下LClosure的数据结构：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CommonHeader struct GCObject* next; lu_byte tt_; lu_byte marked</span></span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="comment">// Closure</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ClosureHeader CommonHeader; int nupvalues; struct GCObject* gclist</span></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LClosure</span> &#123;</span></span><br><span class="line">    ClosureHeader;</span><br><span class="line">    Proto* p;</span><br><span class="line">    UpVal* upvals[<span class="number">1</span>];</span><br><span class="line">&#125; LClosure;</span><br></pre></td></tr></table></figure>
<p>CommonHeader是需要被gc的类型的公共头部，LClosure还包含了一个变量，nupvalues。这个变量记录着Upvals列表的实际大小。Proto结构，前面章节也详细论述过，它是存放lua函数编译结果的结构体。我们现在通过图2，来感受一下：<br><img src="upvalue-diagram.png" alt="image">图2<br>如图2所示，xxx函数的LClosure，包含两个主要的部分，一个是proto部分，还有一个是upvalue部分，xxx函数，就是整个函数编译的结果，upvalue列表指向了外层函数的变量a和b。现在，我们还没法直观感受到，xxx函数，在虚拟机层面，如何引用到自己的upvalue，还是通过上面的例子来感知一下，当xxx函数完成编译后，虚拟机指令会存在proto结构的code列表中，我们可以通过图3来表示：<br><img src="upvalue-diagram-vm-code-1.png" alt="image">图3<br>我们现在通过执行列表中的指令，来感受一下，xxx函数是如何使用自己的upvalue的。先看第一步：<br><img src="upvalue-diagram-vm-code-2.png" alt="image">图4<br>此时执行的指令是使用红色标记的指令，它实际表达的含义，可以归纳为：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">R(A) := UpValue[<span class="string">B</span>][<span class="symbol">RK( C )</span>]</span><br></pre></td></tr></table></figure>
<p>这里的UpValue的含义，就是LClosure实例中，upval列表。B代表，取UpValue列表里的哪个值，RK[C]的含义，之前的章节也有提到过，当C的值&lt;256的时候，它需要到栈上查找。当C的值&gt;=256的时候，它直接到Proto结构变量，k中查找，取出k[C-256]的变量，在本例中是k[0]。而此时，代表我们寄存器的值R(A)中，A的值为0，这代表了，我们要将k[0]，要存储的位置，R(A)的位置，就是base+A的位置，因为A为0，因此，存放目标位置，就是base所指向的位置。于是，我们的推导就变成了：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">R(A) := UpValue[<span class="string">B</span>][<span class="symbol">RK[C</span>]]</span><br><span class="line">==&gt;</span><br><span class="line">R(0) := UpValue[<span class="string">0</span>][<span class="symbol">k[0</span>]]</span><br><span class="line">==&gt;</span><br><span class="line">R(0) := UpValue[<span class="string">0</span>][<span class="symbol">"print"</span>]</span><br><span class="line">==&gt;</span><br><span class="line">base := _ENV["print"]</span><br><span class="line">==&gt;</span><br><span class="line">base := _G["print"]</span><br></pre></td></tr></table></figure>
<p>任何一个lua函数，都至少有一个upvalue，而这个upvalue就是以_ENV为upvalue名称的table，它默认指向了_G。接下来，虚拟机就执行第二和第三个步骤，它直接将UpValue[1]和UpValue[2]的值（分别是top-level函数中，变量a和变量b），分别赋值到了base+1，和base+2的位置上，于是我们得到了图5的结果：<br><img src="upvalue-diagram-vm-code-3.png" alt="image">图5<br>接下来执行OP_ADD指令，得到图6的结果：<br><img src="upvalue-diagram-vm-code-4.png" alt="image">图6<br>最后就是执行OP_CALL指令，调用base位置的函数print，最后输出3。<br>    这里稍微用了一点篇幅，来论述xxx函数如何调用upvalue的流程，需要注意的是，xxx函数在完成编译以后，所有的指令都是int型变量，因此指令是不可能带有任何非数值类型的信息的，因此，虚拟机只能根据不同的指令，去到不同的位置（常量表k、upvalue表等）根据指令中的数值信息，去里面找到对应的值。这也是为什么，我们的proto结构，需要一个code列表去存储指令信息，用一个k列表去存储常量，lua closure实例，需要有一个upval列表去存储upvalue，目的是在虚拟机运行的过程中，虚拟机随时可以根据指令中包含的索引，到这些列表中，找到他们的值，最后再进行逻辑处理。如果读者需要了解整个编译过程，可以回顾第5~第8章的内容。</p>
<h1 id="Upvalue的生成"><a href="#Upvalue的生成" class="headerlink" title="Upvalue的生成"></a>Upvalue的生成</h1><p>​    章节前面的内容，我们已经了解到了upvalue的定义。本节，就是要在概念层面，论述清楚，upvalue是如何生成的。</p>
<h4 id="首个upvalue"><a href="#首个upvalue" class="headerlink" title="首个upvalue"></a>首个upvalue</h4><p>​    每个lua函数，都至少拥有一个upvalue，这个upvalue就是位于第0个位置的那个upvalue，这个upvalue的名称是_ENV，我们现在通过一个例子来观察一下：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- test.lua</span></span><br><span class="line"><span class="keyword">local</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">local</span> b = <span class="number">2</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">xxx</span><span class="params">()</span></span></span><br><span class="line">	<span class="keyword">local</span> c = <span class="number">3</span></span><br><span class="line">	<span class="keyword">local</span> d = <span class="number">4</span></span><br><span class="line">	<span class="built_in">print</span>(a + b)</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">yyy</span><span class="params">()</span></span></span><br><span class="line">		<span class="built_in">print</span>(c + d)</span><br><span class="line">	<span class="keyword">end</span> </span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>上面这个例子的upvalue关系图，可以通过图7来展示：<br><img src="upvalue-relationship.png" alt="image">图7<br>我们可以看到，每个lua函数，都有一个upvalue列表，并且他们首个upvalue，都是一个名为_ENV的upvalue，内层lua函数的_ENV指向外层lua函数的_ENV，而最外层的top-level函数，则将值指向了全局表_G。为什么lua要用这种组织方式？将_ENV作为每个lua函数的第0个upvalue呢？我认为，这是为了效率，同时也能是的逻辑更为清晰，lua函数去查找一个变量的方式，如下所示：</p>
<ul>
<li>lua函数查找一个变量v，首先会在自己的local变量中查找，如果找到就直接获取它的值，找不到则进入下一步</li>
<li>查找upvalue列表，有没有一个名为v的upvalue，有则获取它的值，没有则进入下一步</li>
<li>到_ENV里去查找一个名为v的值</li>
</ul>
<p>在虚拟机运行层面，查找一个变量要简单的多。每个函数，只要在自己lclosure实例中，包含的变量集合（local列表，upvalue列表，_ENV）中查找即可，不需要直接到外层函数去查找，逻辑清晰。我们现在还是以上面的例子为例，yyy函数的逻辑很简单，就是去打印c+d的值，首先yyy函数要获取的是一个名为print的函数，它首先会尝试去查找，是否有一个名为print的local变量，因为没找到，所以去upvalue列表中查找。由于在yyy函数的upvalue列表中，找不到一个名为print的变量，所以就去_ENV[“print”]中查找，因为_ENV默认指向_G所以，从全局表_G中获取了print函数，并压入lua栈中。接下来就是以同样的方式，去获取c和d的值，因为c和d是外层函数xxx的local变量，因此他们被当做upvalue，存在了yyy函数的upvalue列表中了，最后通过print函数，打印了c+d的值。</p>
<h4 id="upvalue生成过程"><a href="#upvalue生成过程" class="headerlink" title="upvalue生成过程"></a>upvalue生成过程</h4><p>​    通过前面的内容，其实我们已经可以了解到，upvalue是什么？它们存放的位置在哪里，以及怎么去获取他们的值。本节的主要任务，就是要搞清楚，upvalue具体是怎么生成的。实际上，查阅了源码后，不难发现，upvalue实际上是在编译时确定（位置信息，和外层函数的关联等），在运行时生成，什么意思呢？意思就是，在编译脚本阶段，就要确定lua函数的upvalue有哪些，在哪里。在虚拟机运行阶段，再去生成对应的upvalue。实际上，用来表示upvalue的有两个数据结构，一个是编译时期，存储upvalue信息的Upvaldesc（这个结构并不存储upvalue的实际值，只是用来标记upvalue的位置信息），还有一个是在运行期，实际存储upvalue值的UpVal结构。它们的结构定义如下所示：</p>
<figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Upvaldesc</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> in_stack; 	<span class="comment">// 本函数的upvalue，是否指向外层函数的栈(不是则指向外层函数的某个upvalue值)</span></span><br><span class="line">    <span class="keyword">int</span> idx;  		<span class="comment">// upvalue在外层函数中的位置(栈的位置或upval列表中的位置，根据in_stack确定)</span></span><br><span class="line">    TString* name;  <span class="comment">// upvalue的名称</span></span><br><span class="line">&#125; Upvaldesc;</span><br><span class="line"></span><br><span class="line"><span class="comment">// luafunc.h</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">UpVal</span> &#123;</span></span><br><span class="line">	TValue* v;  <span class="comment">// 指向外层函数的local变量(open upvalue)，或者指向自己(upvalue关闭时)</span></span><br><span class="line">	<span class="keyword">int</span> refcount; <span class="comment">// UpVal实例，被引用的次数</span></span><br><span class="line">	<span class="keyword">union</span> &#123;</span><br><span class="line">		<span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">			<span class="class"><span class="keyword">struct</span> <span class="title">UpVal</span>* <span class="title">next</span>;</span> <span class="comment">// next open upvalue</span></span><br><span class="line">			<span class="keyword">int</span> touched;</span><br><span class="line">		&#125; <span class="built_in">open</span>;</span><br><span class="line">		TValue value;		<span class="comment">// its value (when closed)</span></span><br><span class="line">	&#125; u;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>Upvaldesc的每一个字段，都很清晰，也比较容易理解，只是UpVal结构就要复杂得多了，我会慢慢来解释，我相信通过这篇文章，读者能够彻底搞清楚upvalue到底是什么。写到这里，我们不得不回顾一下，Proto的数据结构了：</p>
<figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Proto</span> &#123;</span></span><br><span class="line">    CommonHeader;</span><br><span class="line">	<span class="keyword">int</span>* <span class="built_in">line</span>;</span><br><span class="line">	<span class="keyword">int</span> sizeline;</span><br><span class="line">    <span class="keyword">int</span> is_vararg;</span><br><span class="line">    <span class="keyword">int</span> nparam;</span><br><span class="line">    Instruction* code;  <span class="comment">// array of opcodes</span></span><br><span class="line">    <span class="keyword">int</span> sizecode;</span><br><span class="line">    TValue* k;</span><br><span class="line">    <span class="keyword">int</span> sizek;</span><br><span class="line">    LocVar* locvars;</span><br><span class="line">    <span class="keyword">int</span> sizelocvar;</span><br><span class="line">    Upvaldesc* upvalues;  <span class="comment">// upvaluedesc列表</span></span><br><span class="line">    <span class="keyword">int</span> sizeupvalues;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Proto</span>** <span class="title">p</span>;</span></span><br><span class="line">    <span class="keyword">int</span> sizep;</span><br><span class="line">    TString* source;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gclist</span>;</span></span><br><span class="line">	<span class="keyword">int</span> maxstacksize;</span><br><span class="line">&#125; Proto;</span><br></pre></td></tr></table></figure>
<p>由于前面章节，已经详细介绍过Proto的结构了，之类不再赘述，本章只关注Proto和Upvalue之间的关系。我们都知道，lua脚本的编译信息，会被存储到Proto结构实例之中，当一个lua函数的某个变量，不是local变量时，我们希望获取它的值，实际上就是要查找这个变量的位置，如果local列表中找不到，则进入到如下流程：</p>
<ul>
<li>到自己的upvaldesc列表中，根据变量名查找，是否存在某个upvalue存在，如果存在则使用它，否则进入下一步</li>
<li>到外层函数，查找local变量，如果找到，那么将它作为自己的upvalue，否则查找它的upvaldesc表，找到就将其生成为自己的upvalue，否则进入外层函数，重复这一步</li>
<li>如果一直到top-level函数都找不到，那么表示这个upvalue不存在，此时需要去_ENV中查找</li>
</ul>
<p>这里，我们需要通过一个例子，来理解这个流程，需要注意的是，这个过程，是在lua脚本的编译阶段进行的：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- test.lua</span></span><br><span class="line"><span class="keyword">local</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">local</span> b = <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">xxx</span><span class="params">()</span></span></span><br><span class="line">	<span class="keyword">local</span> c = <span class="number">3</span></span><br><span class="line">	<span class="built_in">print</span>(a)</span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">yyy</span><span class="params">()</span></span></span><br><span class="line">		<span class="built_in">print</span>(a + b + c)</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>上面这个例子，我们可以通过图8来展示：<br><img src="find-upvalue.png" alt="image">图8<br>当我们的解释器，对这个脚本进行编译时，首先会从top-level function开始。本章我们只关注upvalue的编译流程和结果，所以中间会略过大量的细节。在top-level function中，变量a和b均是它的local变量。按照它们定义的顺序，在虚拟机运行期间，a会在栈中第0个位置，而b则在第1个位置。对脚本的编译开始之后，编译器首先会对top-level函数，会往自己的upvaldesc列表的第0个位置，填充_ENV的信息，得到图9的结果：<br><img src="find-upvalue-2.png" alt="image">图9<br>我们可以观察到，top-level函数的第一个upvalue信息，已经被填充了，这里可以观察到，in_stack的值为1，idx的值为0，name为_ENV，由于top-level函数，已经是最外层的函数了，因此它没有外层函数，因此这里可以忽略掉in_stack和idx所表示的值，编译器会在脚本完成编译时，将_G赋值到UpVal[0]的位置，这个后面会论述。接下来，则会对xxx函数进行编译，在xxx函数中，c是它的local变量，因此变量c将在xxx函数栈中的第0个位置。我们也可以观察到，xxx函数引用了外层函数的一个变量a，编译器首先会为xxx函数，填充第0个upvalue的编译信息，然后再生成它的第1个upvalue a的信息，得到图10的结果：<br><img src="find-upvalue-3.png" alt="image">图10<br>我们可以观察一下，xxx函数的upvalue描述信息，第0个仍然是_ENV，它的in_stack的值为0，表示它引用的是外层函数的upvalue值，idx为0，表示该值位于lclosure-&gt;upval[0]的位置，name为_ENV表示它的名称是_ENV。它的第1个upvalue的信息中，in_stack的值为1，表示，这个upvalue的值，在外层函数的栈上，idx为0，表示upvalue的值是外层函数栈上，第0个位置的值，在这里指代外层函数local变量a的值1，name的值为a，表示它引用外层函数的变量名为a。再往下，就开始编译函数yyy了。由于在yyy函数内，a、b和c的值，均不是它的local变量，因此这里触发生成upvalue的逻辑。<br>    yyy函数首先要处理的变量是a，由于a不是它的local变量，所以去它的upvaluedesc列表中查找，由于upvaluedesc列表中，没有名为a的upvalue，因此要去外层函数找，在开始找之前，它会将_ENV的信息，填写到upvaluedesc列表的第0个位置上。接下来，yyy函数，会去它的外层函数xxx中，查找名为a的local变量，很显然xxx没有定义名为a的local变量，所以它就去xxx的upvalue列表里查找，最后它在第1个upvalue那里找到这个值，于是得到图11的结果：<br><img src="find-upvalue-4.png" alt="image">图11<br>我们可以看到，yyy函数生成_ENV的upvalue的方式，和xxx函数是一致的，因此这里不再赘述，而生成upvalue a，则在外层函数xxx的upvaluedesc列表中找到了，因此这个变量不在栈上，所以in_stack=0，并且idx=1（a在xxx函数upvaluedesc列表上的位置）。接下来，则进入到了，生成upvalue b的流程之中了。同样的，yyy函数内部没有定义过变量b，所以要到外层函数xxx中查找，因为xxx没有定义b这个变量，自己的upvaluedesc列表中也没有，所以，此时要到xxx函数的外层函数去查找，并且在top-level函数中，找到了一个local变量b，此时xxx函数首先要将其生成为自己的upvalue，于是得到图12的结果：<br><img src="find-upvalue-5.png" alt="image">图12<br>name为b，in_stack=1，表示upvalue b，在top-level函数的栈上，前面也说过了，在运行阶段，变量b会在top-level函数栈上的第1个位置（第0个位置是a），所以idx的值，此时为1。接下来，轮到yyy去生成这个upvalue了，因为upvalue b，位于xxx函数，upvaluedesc列表中的第2个位置，因此，它得到如图13的结果：<br><img src="find-upvalue-6.png" alt="image">图13<br>这个生成过程，需要读者自己再去梳理领会一下。接下来，就到了upvalue c的生成流程了，首先yyy函数内找不到c的定义，因此去yyy函数的外层函数，xxx函数中查找，xxx函数中，真的有一个变量名为c的local变量，因此，直接生成图14的结果：<br><img src="find-upvalue-7.png" alt="image">图14<br>因为c是xxx函数的第1个local变量，因此在运行阶段，它在xxx函数栈上的第0个位置，所以，upvalue c在yyy函数upvaluedesc列表中的值为，in_stack=1（它是外层函数的一个局部变量），idx=0（在外层函数栈上的第0个位置），名称name为c。当编译器完成这个脚本的编译时，会给top-level函数的第0个upvalue赋值_G，得到图15的结果：<br><img src="find-upvalue-8.png" alt="image">图15<br>    前面的论述，主要在集中论述脚本编译期间，upvalue的位置信息生成。现在我们来在概念层面，观察一下，运行期间如何生成虚拟机运行过程中，真实被用到的upvalue值。还是用上面的例子，假设前面的top-level函数开始执行，首先会执行两个赋值操作，于是得到图16的结果：<br><img src="find-upvalue-9.png" alt="image">图16<br>两个local变量的定义和初始化操作，虚拟机会将它们放入栈中，接下来往下执行，我们前面也提到过，在lua中，函数是first-class类型，因此它本身也是一种变量类型，所以前面的例子，其实质等价于如下形式：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- test.lua</span></span><br><span class="line"><span class="keyword">local</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">local</span> b = <span class="number">2</span></span><br><span class="line"></span><br><span class="line">xxx = <span class="function"><span class="keyword">function</span><span class="params">()</span></span></span><br><span class="line">	<span class="keyword">local</span> c = <span class="number">3</span></span><br><span class="line">	<span class="built_in">print</span>(a)</span><br><span class="line">	yyy = <span class="function"><span class="keyword">function</span><span class="params">()</span></span></span><br><span class="line">		<span class="built_in">print</span>(a + b + c)</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>所以逻辑接着往下执行，是对一个名为xxx的变量，赋值一个函数变量。要完成这个操作，首先要创建一个函数实例，并填充它的upvalue值信息，最后再赋值给变量xxx，于是得到图17的结果：<br><img src="find-upvalue-10.png" alt="image">图17<br>我们可以看到，此时，已经开始往xxx函数的upval列表填充信息了，首先它的第一个变量是_ENV，这里不包含任何upvalue名称，而是直接将upval的指针，指向了外层函数的第一个upval。回顾一下图7，他们实际上是同一个UpVal* 实例。接下来，它会创建一个UpVal实例，并且upval-&gt;v指向了外层函数的local变量a，然后是local变量b。执行到这里，这个逻辑流程就结束了。可能有读者会问，那yyy函数没做处理吗？答案是是的，因为我们没有调用xxx函数，因此yyy变量的赋值操作，也就不会进行，并且该LClosure实例也不会被创建，xxx函数会被创建，是因为它是在top-level函数里定义的，大家可以回顾一下上面替换成等式的例子。如果，我们在top-level函数里，调用xxx函数，那么，xxx函数里的逻辑也会被创建，yyy函数被创建的逻辑，也会被执行到，于是得到图18的结果。<br><img src="find-upvalue-11.png" alt="image">图18<br>xxx函数被调时，它会被压入栈中，同时局部变量也会被压入栈中。这里有一个非常有意思的现象，就是一个函数的upvalue，一直溯源下去，最终的源头，要么是某个外层函数的local变量，要么就是最外层函数的_ENV。<br>    本节，我精心挑选了一个例子，基本把upvalue生成的几种情况都涵盖到了。这里需要注意的是，上面示例图里的LClosure图集，只是为了展示一个关系层次，并不是在编译阶段，就会创建LClosure实例，编译阶段，只会生成Proto实例，并将编译结果存到这个结构中。只有执行脚本逻辑阶段，才会将LClosure实例创建。</p>
<h1 id="Open-Upvalue和Closed-Upvalue"><a href="#Open-Upvalue和Closed-Upvalue" class="headerlink" title="Open Upvalue和Closed Upvalue"></a>Open Upvalue和Closed Upvalue</h1><p>​    我们已经完成了upvalue生成流程的讨论了，现在来关注一下open upvalue和closed upvalue。可能很多读者，对这两个概念非常陌生，但是理解它，对于解释一些和upvalue有关的现象，是非常有帮助的。open upvalue和closed upvalue这两个概念，是针对一个函数的upvalue是外层函数local变量的情况。关于upvalue的使用，最难理解的也是这个部分。接下来，我将通过两个例子来说明这个概念，先看例1：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> var1 = <span class="number">1</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">aaa</span><span class="params">()</span></span></span><br><span class="line">	<span class="keyword">local</span> var2 = <span class="number">1</span></span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">bbb</span><span class="params">()</span></span></span><br><span class="line">		var1 = var1 + <span class="number">1</span></span><br><span class="line">		var2 = var2 + <span class="number">1</span></span><br><span class="line">		<span class="built_in">print</span>(<span class="string">"bbb:"</span>, var1, var2)</span><br><span class="line">	<span class="keyword">end</span> </span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">ccc</span><span class="params">()</span></span></span><br><span class="line">		var1 = var1 + <span class="number">1</span></span><br><span class="line">		var2 = var2 + <span class="number">1</span></span><br><span class="line">		<span class="built_in">print</span>(<span class="string">"ccc:"</span>, var1, var2)</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">	bbb()</span><br><span class="line">	<span class="built_in">print</span>(<span class="string">"hahaha"</span>)</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">aaa()</span><br><span class="line">bbb()</span><br><span class="line">bbb()</span><br><span class="line">ccc()</span><br><span class="line">ccc()</span><br></pre></td></tr></table></figure>
<p>例1<br>例1的输出结果如下所示：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">bbb:</span>	<span class="number">2</span>	<span class="number">2</span></span><br><span class="line"><span class="string">hahaha</span></span><br><span class="line"><span class="attr">bbb:</span>	<span class="number">3</span>	<span class="number">3</span></span><br><span class="line"><span class="attr">bbb:</span>	<span class="number">4</span>	<span class="number">4</span></span><br><span class="line"><span class="attr">ccc:</span>	<span class="number">5</span>	<span class="number">5</span></span><br><span class="line"><span class="attr">ccc:</span>	<span class="number">6</span>	<span class="number">6</span></span><br></pre></td></tr></table></figure>
<p>这个例子中，函数bbb和函数ccc是在aaa里定义的，也就是说，只有调用执行了函数aaa，bbb和ccc两个函数才会被创建。我们现在来看一下，执行这段脚本时的情况，图19是该脚本完成编译时的状态：<br><img src="open-closed-upvalue-1.png" alt="image">图19<br>这里的upvalue生成，前面已经花费了相当篇幅去论述了，因此这里留给读者自己去推导，这里不再赘述。接下来，我们就进入到了执行环节，先从top-level函数的第一行代码开始，于是得到图20的结果：<br><img src="open-closed-upvalue-2.png" alt="image">图20<br>接下来，就进入到了，创建函数aaa的流程，得到图21的结果：<br><img src="open-closed-upvalue-3.png" alt="image">图21<br>创建函数aaa，的时期，并不会执行函数aaa内的逻辑，但是会为其创建一个函数实例，并且完成UpVal列表的填充。接下来，就会执行函数aaa()，得到图22的结果：<br><img src="open-closed-upvalue-4.png" alt="image">图22<br>这里我们可以看到，执行aaa函数之后，它的内部逻辑会创建函数bbb和函数ccc（创建LClosure实例），并且初始化它们的upvalue列表。这里需要注意的是，函数bbb和函数ccc的第2个（var2 upvalue）UpVal<em> 指针指向了同一个UpVal</em> 实例。图22中所示的，包含的值，指向LuaStack中的两个Upvalue，此时是Open Upvalue。当我们的逻辑继续往下走，执行到调用第一个bbb函数的时候，此时结果如图23所示：<br><img src="open-closed-upvalue-5.png" alt="image">图23<br>此时，aaa函数已经执行完毕，因此它的局部变量需要从栈中清退，而在执行aaa函数的过程中，已经将函数对象（LClosure实例），bbb和ccc创建，他们的var2变量，共享同一个UpVal<em>实例。在aaa函数执行完毕时，他们的UpVal</em>实例，会进行close操作，什么意思呢，就是原来的upval-&gt;v指向栈的某个位置，现在这个关联将被破除，并且upval-&gt;v的值，赋值为upval-&gt;u.value的地址，同时，upval-&gt;v原来指向的值，会被赋值到upval-&gt;u.value上。为什么此时，填入upval-&gt;u.value内的值是2呢？因为aaa函数内调用了一次bbb函数，因此，此时我们的控制台会输出如下结果：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">bbb:</span>	<span class="number">2</span>	<span class="number">2</span></span><br><span class="line"><span class="string">hahaha</span></span><br></pre></td></tr></table></figure>
<p>而接下来，要执行的函数，就是图23中，黑色箭头所指的函数调用，此时调用的函数是bbb函数。这个函数，会分别将var1和var2的值加1，最后再打印他们，我们可以看到，var1仍然在栈上，因此var1的值会变成3，而var2的值，指向了一个UpVal* 实例，而其原来的值是2，自增1后，就变成了3，此时得到的输出结果为：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">bbb:</span>	<span class="number">2</span>	<span class="number">2</span></span><br><span class="line"><span class="string">hahaha</span></span><br><span class="line"><span class="attr">bbb:</span>	<span class="number">3</span>	<span class="number">3</span></span><br></pre></td></tr></table></figure>
<p>而与结果相对应的图，如图24所示：<br><img src="open-closed-upvalue-6.png" alt="image">图24<br>接下来，执行第二个bbb函数的调用，var1的值仍然在栈上，var2的值，仍然在那个UpVal*实例里，于是得到如下的结果：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">bbb:</span>	<span class="number">2</span>	<span class="number">2</span></span><br><span class="line"><span class="string">hahaha</span></span><br><span class="line"><span class="attr">bbb:</span>	<span class="number">3</span>	<span class="number">3</span></span><br><span class="line"><span class="attr">bbb:</span>	<span class="number">4</span>	<span class="number">4</span></span><br></pre></td></tr></table></figure>
<p><img src="open-closed-upvalue-7.png" alt="image">图25<br>接下来，则是执行第一个ccc函数，通过观察ccc函数的upvalue列表，我们可以看到，不论是var1还是var2，他们引用的都是同一个UpVal*实例，因此bbb函数的执行结果，会影响到ccc。第一个ccc函数执行完以后，得到如下的结果：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">bbb:</span>	<span class="number">2</span>	<span class="number">2</span></span><br><span class="line"><span class="string">hahaha</span></span><br><span class="line"><span class="attr">bbb:</span>	<span class="number">3</span>	<span class="number">3</span></span><br><span class="line"><span class="attr">bbb:</span>	<span class="number">4</span>	<span class="number">4</span></span><br><span class="line"><span class="attr">ccc:</span>	<span class="number">5</span>	<span class="number">5</span></span><br></pre></td></tr></table></figure>
<p>执行完第二个ccc函数得到的结果，则如下所示：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">bbb:</span>	<span class="number">2</span>	<span class="number">2</span></span><br><span class="line"><span class="string">hahaha</span></span><br><span class="line"><span class="attr">bbb:</span>	<span class="number">3</span>	<span class="number">3</span></span><br><span class="line"><span class="attr">bbb:</span>	<span class="number">4</span>	<span class="number">4</span></span><br><span class="line"><span class="attr">ccc:</span>	<span class="number">5</span>	<span class="number">5</span></span><br><span class="line"><span class="attr">ccc:</span>	<span class="number">6</span>	<span class="number">6</span></span><br></pre></td></tr></table></figure>
<p>到现在为止，我就完成了例1的论述了，读者通过这个例子，可以理解到，在运行阶段，不同的函数，引用到了同一个Upvalue，那他们upvalue列表里对应的实例，实际上是共享的。读者可以通过多次阅读上面的例子，来感受一下open upvalue变为closed upvalue的流程。接下来，我们要看的是例2。<br>    例1展示的是，两个同级函数，引用了同一个外层函数local变量，并且经历从open upvalue到closed upvalue转变的流程。我们现在要看的是第二个例子：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> var1 = <span class="number">1</span></span><br><span class="line"><span class="keyword">local</span> <span class="function"><span class="keyword">function</span> <span class="title">aaa</span><span class="params">()</span></span></span><br><span class="line">	<span class="keyword">local</span> var2 = <span class="number">1</span></span><br><span class="line">	<span class="keyword">return</span> <span class="function"><span class="keyword">function</span><span class="params">()</span></span></span><br><span class="line">		var1 = var1 + <span class="number">1</span></span><br><span class="line">		var2 = var2 + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">		<span class="built_in">print</span>(var1, var2)</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">local</span> f1 = aaa()</span><br><span class="line"><span class="keyword">local</span> f2 = aaa()</span><br><span class="line"></span><br><span class="line">f1()</span><br><span class="line">f2()</span><br></pre></td></tr></table></figure>
<p>例2<br>现在我们通过图文的方式，来展现这个逻辑流程，首先执行脚本的第一行逻辑，得到图26的结果：<br><img src="open-closed-upvalue-8.png" alt="image">图26<br>接下来，就会执行函数aaa的创建逻辑，此时我们会创建一个LClosure实例，并存放在local变量aaa中，得到图27的结果：<br><img src="open-closed-upvalue-9.png" alt="image">图27<br>程序执行流继续往下走，开始执行local f1 = aaa()函数的逻辑了。首先程序要进入到aaa函数的内部，执行它的逻辑，先来看一下执行了第一个赋值操作之后的结果：<br><img src="open-closed-upvalue-10.png" alt="image">图28<br>接下来，aaa函数，会往下执行逻辑，它会创建一个匿名的function实例（LClosure实例），得到图29结果：<br><img src="open-closed-upvalue-11.png" alt="image">图29<br>我们可以看到，橙色线框的部分，就是被创建出来的匿名函数实例，此时，aaa函数要结束了，因此aaa函数的栈信息会被清退，此时匿名函数中的var2，引用的是外层函数aaa的local变量，在aaa函数完成调用之前，它是open upvalue，而aaa函数结束时，它要转变成closed upvalue，于是得到图30的结果：<br><img src="open-closed-upvalue-12.png" alt="image">图30<br>我们可以看到，var2的upvalue中，upval-&gt;v指针，从之前指向栈上，专向了指向自身的结构之中，并且将原来栈上的值，赋值给了它。与此同时，局部变量f1也被压入栈中，它的值，就是从aaa函数中创建并返回的函数实例（LClosure实例）。接下来，黑色箭头会继续往下走，此时会重复上面的逻辑，于是得到图31的结果：<br><img src="open-closed-upvalue-13.png" alt="image">图31<br>我们可以观察到，aaa函数，又重新创建了一个新的函数实例，并且赋值到local变量f2中，f1和f2的var1 upvalue，引用了同一个UpVal<em>实例，而var2这个upvalue，则是每个都独立一份，这里需要和例1的情况进行区别，例1的upvalue var2是共享的，因为调用aaa函数时，在同一次调用时创建的，而例2的情况则是，分两次调用创建，因此UpVal</em>实例是各自独立一份。接下来，例2的代码会继续往下执行，分别执行了f1和f2两个函数，于是得到如下的输出。</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">2</span>	<span class="number">2</span></span><br><span class="line"><span class="number">3</span>	<span class="number">2</span></span><br></pre></td></tr></table></figure>
<p>因为var1是共享的，所以f2执行的时候，输出3，但是var2是f1和f2各自独立有一个UpVal*实例的，因此，f1和f2调用的结果都是2。</p>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>​    到这里，我就完成了整个upvalue的论述了，后面还有几个重要章节，它们分别是weektable、coroutine和require机制，我将在接下来的几个月内完成这个系列。</p>
<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><p>[1] <a href="https://www.lua.org/pil/27.3.3.html" rel="external nofollow noopener noreferrer" target="_blank">Programming in Lua 27.3.3 - Upvalues</a><br>[2] <a href="https://www.lua.org/pil/1.1.html" rel="external nofollow noopener noreferrer" target="_blank">Programming in Lua 1.1 - Chunks</a></p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>深入理解Lua虚拟机</title>
    <url>/lua/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3Lua%E8%99%9A%E6%8B%9F%E6%9C%BA/</url>
    <content><![CDATA[<blockquote>
<p>本文从一个简单示例入手，详细讲解 Lua 字节码文件的存储结构及各字段含义，进而引出 Lua 虚拟机指令集和运行时的核心数据结构 Lua State，最后解释 Lua 虚拟机的 47 条指令如何在 Lua State 上运作的。</p>
</blockquote>
<p>为了达到较高的执行效率，lua 代码并不是直接被 Lua 解释器解释执行，而是会先编译为字节码，然后再交给 lua 虚拟机去执行。lua 代码称为 chunk，编译成的字节码则称为二进制 chunk（Binary chunk）。lua.exe、wlua.exe 解释器可直接执行 lua 代码（解释器内部会先将其编译成字节码），也可执行使用 luac.exe 将 lua 代码预编译（Precompiled）为字节码。使用预编译的字节码并不会加快脚本执行的速度，但可以加快脚本加载的速度，并在一定程度上保护源代码。luac.exe 可作为编译器，把 lua 代码编译成字节码，同时可作为反编译器，分析字节码的内容。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">luac.exe -v  // 显示luac的版本号</span><br><span class="line">luac.exe Hello.lua  //</span><br><span class="line">在当前目录下，编译得到Hello.lua的二进制chunk文件luac.out（默认含调试符号）</span><br><span class="line"></span><br><span class="line">luac.exe -o Hello.out Hello1.lua Hello2.lua //</span><br><span class="line">在当前目录下，编译得到Hello1.lua和Hello2.lua的二进制chunk文件Hello.out（默认含调试符号）</span><br><span class="line"></span><br><span class="line">luac.exe -s -o d:\\Hello.out Hello.lua  //</span><br><span class="line">编译得到Hello.lua的二进制chunk文件d:\\Hello.out（去掉调试符号）</span><br><span class="line"></span><br><span class="line">luac.exe -p Hello1.lua Hello2.lua  //</span><br><span class="line">对Hello1.lua和Hello2.lua只进行语法检测（注：只会检查语法规则，不会检查变量、函数等是否定义和实现，函数参数返回值是否合法）</span><br></pre></td></tr></table></figure></p>
<p>lua 编译器以函数为单位对源代码进行编译，每个函数会被编译成一个称之为原型（Prototype）的结构，原型主要包含 6 部分内容：函数基本信息（basic info，含参数数量、局部变量数量等信息）、字节码（bytecodes）、常量（constants）表、upvalue（闭包捕获的非局部变量）表、调试信息（debug info）、子函数原型列表（sub functions）。</p>
<p>原型结构使用这种嵌套递归结构，来描述函数中定义的子函数：<br><img src="https://p1-tt.byteimg.com/origin/pgc-image/bcd6ce0147114cebbcd9f0c0d7adee03?from=pc" alt><br>注：lua 允许开发者可将语句写到文件的全局范围中，这是因为 lua 在编译时会将整个文件放到一个称之为 main 函数中，并以它为起点进行编译。</p>
<p>Hello.lua 源代码如下：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span> (<span class="string">"hello"</span>)</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span><span class="params">(a, b)</span></span></span><br><span class="line">    <span class="keyword">return</span> a+b</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p>
<p>编译得到的 Hello.out 的二进制为：<br><img src="https://p6-tt.byteimg.com/origin/pgc-image/b19a7fd07e744d719eeedfc69949ab65?from=pc" alt><br>二进制 chunk（Binary chunk）的格式并没有标准化，也没有任何官方文档对其进行说明，一切以 lua 官方实现的源代码为准。其设计并没有考虑跨平台，对于需要超过一个字节表示的数据，必须要考虑大小端（Endianness）问题。</p>
<p>lua 官方实现的做法比较简单：编译 lua 脚本时，直接按照本机的大小端方式生成二进制 chunk 文件，当加载二进制 chunk 文件时，会探测被加载文件的大小端方式，如果和本机不匹配，就拒绝加载。二进制 chunk 格式设计也没有考虑不同 lua 版本之间的兼容问题，当加载二进制 chunk 文件时，会检测其版本号，如果和当前 lua 版本不匹配，就拒绝加载。另外，二进制 chunk 格式设计也没有被刻意设计得很紧凑。在某些情况下，一段 lua 代码编译成二进制 chunk 后，甚至会被文本形式的源代码还要大。预编译成二进制 chunk 主要是为了提升加载速度，因此这也不是很大的问题。</p>
<h1 id="头部字段："><a href="#头部字段：" class="headerlink" title="头部字段："></a>头部字段：</h1><p><img src="https://p1-tt.byteimg.com/origin/pgc-image/32fc87dd7ed64bd6bf9bf5d7f32a749b?from=pc" alt></p>
<h1 id="嵌套的函数原型："><a href="#嵌套的函数原型：" class="headerlink" title="嵌套的函数原型："></a>嵌套的函数原型：</h1><p><img src="https://p3-tt.byteimg.com/origin/pgc-image/c948d406090448f88eef97949d9d78cb?from=pc" alt></p>
<blockquote>
<p>注 1：二进制 chunk 中的字符串分为三种情况：</p>
</blockquote>
<ol>
<li><p>①NULL 字符串用 0x00 表示；</p>
</li>
<li><p>② 长度小于等于 253（0xFD）的字符串，先用 1 个 byte 存储字符串长度+1 的数值，然后是字节数组；</p>
</li>
<li><p>③ 长度大于等于 254（0xFE）的字符串，第一个字节是 0xFF，后面跟一个 8 字节 size_t 类型存储字符串长度+1 的数值，然后是字节数组。</p>
</li>
<li><p>注 2：常量 tag 对应表</p>
</li>
</ol>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/2fb11a263eca496f93df861745b11a73?from=pc" alt><br>查看二进制 chunk 中的所有函数（精简模式）：</p>
<blockquote>
<p>luac.exe -l Hello.lua</p>
</blockquote>
<blockquote>
<p>luac.exe -l Hello.out</p>
</blockquote>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/cbb9d9e96d784d89a0dcb31d7fa790fe?from=pc" alt></p>
<blockquote>
<p>注 1：每个函数信息包括两个部分：前面两行是函数的基本信息，后面是函数的指令列表。<br>注 2：函数的基本信息包括：函数名称、函数的起始行列号、函数包含的指令数量、函数地址。函数的参数 params 个数（0+表示函数为不固定参数）、寄存器 slots 数量、upvalue 数量、局部变量 locals 数量、常量 constants 数量、子函数 functions 数量。<br>注 3：指令列表里的每一条指令包含指令序号、对应代码行号、操作码和操作数。分号后为 luac 生成的注释，以便于我们理解指令。<br>注 4：整个文件内容被放置到了 main 函数中，并以它作为嵌套起点。</p>
</blockquote>
<p>查看二进制 chunk 中的所有函数（详细模式）：</p>
<blockquote>
<p>luac.exe -l -l Hello.lua 注：参数为 2 个-l<br>luac.exe -l -l Hello.out 注：详细模式下，luac 会把常量表、局部变量表和 upvalue 表的信息也打印出来<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">main &lt;Test2.lua:0,0&gt; (6 instructions at 0046e528)</span><br><span class="line">0+ params, 2 slots, 1 upvalue, 0 locals, 3 constants, 1 <span class="keyword">function</span></span><br><span class="line">        序号    代码行    指令</span><br><span class="line">        1       [1]     GETTABUP        0 0 -1  ; _ENV <span class="string">"print"</span>   //GETTABUP A B C  //将upvalues表索引为B:0的upvalue(即：_ENV)中key为常量表索引为C:-1的(即<span class="built_in">print</span>)，放到寄存器索引为A:0的地方</span><br><span class="line">        2       [1]     LOADK           1 -2    ; <span class="string">"hello"</span>  //LOADK A Bx  //将常量表索引为Bx:-2的hello加载到寄存器索引为A:1的地方</span><br><span class="line">        3       [1]     CALL            0 2 1    ; //CALL A B C  //调用寄存器索引为A:0的函数，参数个数为B:2减1(即1个)，C:1表示无返回值</span><br><span class="line">        4       [5]     CLOSURE         0 0     ; 0046e728      //CLOSURE A Bx  //将子函数原型列表索引为Bx:0的函数地址，放到寄存器索引为A:0的地方</span><br><span class="line">        5       [3]     SETTABUP        0 -3 0  ; _ENV <span class="string">"add"</span>   //SETTABUP A B C  //将upvalues表索引为A:0的upvalue(即：_ENV)中key为常量表索引为B:-3(即add)，设置为寄存器索引为C:0指向的值</span><br><span class="line">        6       [5]     RETURN          0 1        ; //RETURN A B   //B:1表示无返回值</span><br><span class="line">constants (3) <span class="keyword">for</span> 0046e528:</span><br><span class="line">        序号    常量名</span><br><span class="line">        1       <span class="string">"print"</span></span><br><span class="line">        2       <span class="string">"hello"</span></span><br><span class="line">        3       <span class="string">"add"</span></span><br><span class="line">locals (0) <span class="keyword">for</span> 0046e528:</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0046e528:</span><br><span class="line">        序号    upvalue名    是否为直接外围函数的局部变量    在外围函数调用帧的索引</span><br><span class="line">        0       _ENV        1                               0</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> &lt;Test2.lua:3,5&gt; (3 instructions at 0046e728)</span><br><span class="line">2 params, 3 slots, 0 upvalues, 2 locals, 0 constants, 0 <span class="built_in">functions</span></span><br><span class="line">        序号    代码行    指令</span><br><span class="line">        1       [4]     ADD             2 0 1    ; //ADD A B C  //将寄存器索引为0、1的两个数相加得到的结果放到寄存器索引为2的地方</span><br><span class="line">        2       [4]     RETURN          2 2        ; //RETURN A B //B:2表示有一个返回值  A:2表示返回值在寄存器索引为2的地方</span><br><span class="line">        3       [5]     RETURN          0 1        ; //RETURN A B //B:1表示无返回值</span><br><span class="line">constants (0) <span class="keyword">for</span> 0046e728:</span><br><span class="line">locals (2) <span class="keyword">for</span> 0046e728:</span><br><span class="line">    寄存器索引    起始指令序号  终止指令序号  -1得到实际指令序号</span><br><span class="line">        0       a       1       4        ; a变量的指令范围为[0, 3]，起始为0表示为传入的参数变量</span><br><span class="line">        1       b       1       4        ; b变量的指令范围为[0, 3]</span><br><span class="line">upvalues (0) <span class="keyword">for</span> 0046e728:</span><br></pre></td></tr></table></figure></p>
</blockquote>
<blockquote>
<p>luac.exe -l - // 从标准设备读入脚本，输完后按回车，然后按 Ctrl+Z 并回车，会打印出输入内容对应的二进制 chunk 内容 注：进入输入模式后可按 Ctrl+C 强制退出<br>luac.exe -l – // 使用上次输入，打印出二进制 chunk 内容<br>luac.exe -l -l – // 使用上次输入，详细模式下打印出二进制 chunk 内容（参数为 2 个-l）</p>
</blockquote>
<h1 id="Stack-Based-VM-vs-Rigister-Based-VM"><a href="#Stack-Based-VM-vs-Rigister-Based-VM" class="headerlink" title="Stack Based VM vs Rigister Based VM"></a>Stack Based VM vs Rigister Based VM</h1><p>高级编程语言的虚拟机是利用软件技术对硬件进行的模拟和抽象。按照实现方式，可分为两类：基于栈（Stack Based）和基于寄存器（Rigister Based）。Java、.NET CLR、Python、Ruby、Lua5.0 之前的版本的虚拟机都是基于栈的虚拟机；从 5.0 版本开始，Lua 的虚拟机改成了基于寄存器的虚拟机。</p>
<p>一个简单的加法赋值运算：a=b+c</p>
<p>基于栈的虚拟机，会转化成如下指令:<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">push b; // 将变量b的值压入stack</span><br><span class="line">push c; // 将变量c的值压入stack</span><br><span class="line">add; // 将stack顶部的两个值弹出后相加，然后将结果压入stack顶</span><br><span class="line">mov a; // 将stack顶部结果放到a中</span><br></pre></td></tr></table></figure></p>
<p>所有的指令执行，都是基于一个操作数栈的。你想要执行任何指令时，对不起，得先入栈，然后算完了再给我出栈。总的来说，就是抽象出了一个高度可移植的操作数栈，所有代码都会被编译成字节码，然后字节码就是在玩这个栈。好处是实现简单，移植性强。坏处是指令条数比较多，数据转移次数比较多，因为每一次入栈出栈都牵涉数据的转移。</p>
<p>基于寄存器的虚拟机，会转化成如下指令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">add a b c; // 将b与c对应的寄存器的值相加，将结果保存在a对应的寄存器中</span><br></pre></td></tr></table></figure></p>
<p>没有操作数栈这一概念，但是会有许多的虚拟寄存器。这类虚拟寄存器有别于 CPU 的寄存器，因为 CPU 寄存器往往是定址的(比如 DX 本身就是能存东西)，而寄存器式的虚拟机中的寄存器通常有两层含义：</p>
<p>(1)寄存器别名(比如 lua 里的 RA、RB、RC、RBx 等)，它们往往只是起到一个地址映射的功能，它会根据指令中跟操作数相关的字段计算出操作数实际的内存地址，从而取出操作数进行计算；</p>
<p>(2)实际寄存器，有点类似操作数栈，也是一个全局的运行时栈，只不过这个栈是跟函数走的，一个函数对应一个栈帧，栈帧里每个 slot 就是一个寄存器，第 1 步中通过别名映射后的地址就是每个 slot 的地址。</p>
<p>好处是指令条数少，数据转移次数少。坏处是单挑指令长度较长。具体来看，lua 里的实际寄存器数组是用 TValue 结构的栈来模拟的，这个栈也是 lua 和 C 进行交互的虚拟栈。</p>
<h1 id="lua-指令集"><a href="#lua-指令集" class="headerlink" title="lua 指令集"></a>lua 指令集</h1><p>Lua 虚拟机的指令集为定长（Fixed-width）指令集，每条指令占 4 个字节（32bits），其中操作码（OpCode）占 6bits，操作数（Operand）使用剩余的 26bits。Lua5.3 版本共有 47 条指令，按功能可分为 6 大类：常量加载指令、运算符相关指令、循环和跳转指令、函数调用相关指令、表操作指令和 Upvalue 操作指令。</p>
<p>按编码模式分为 4 类：iABC（39）、iABx（3）、iAsBx（4）、iAx（1）</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/b3bedb46c642448bb763f5007bc52e8c?from=pc" alt><br>4 种模式中，只有 iAsBx 下的 sBx 操作数会被解释成有符号整数，其他情况下操作数均被解释为无符号整数。操作数 A 主要用来表示目标寄存器索引，其他操作数按表示信息可分为 4 种类型：OpArgN、OpArgU、OpArgR、OpArgK：</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/41326c37a2764da1bbb244964c8a6cbe?from=pc" alt></p>
<h1 id="Lua-栈索引"><a href="#Lua-栈索引" class="headerlink" title="Lua 栈索引"></a>Lua 栈索引</h1><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/ad961bbec5824743949352c6b18db38d?from=pc" alt><br>注 1：绝对索引是从 1 开始由栈底到栈顶依次增长的；</p>
<p>注 2：相对索引是从-1 开始由栈顶到栈底依次递减的（在 lua API 函数内部会将相对索引转换为绝对索引）；</p>
<p>注 3：上图栈的容量为 7，栈顶绝对索引为 5，有效索引范围为：[1,5]，可接受索引范围为：[1, 7]；</p>
<p>注 4：Lua 虚拟机指令里寄存器索引是从 0 开始的，而 Lua API 里的栈索引是从 1 开始的，因此当需要把寄存器索引当成栈索引使用时，要进行+1。</p>
<h1 id="Lua-State"><a href="#Lua-State" class="headerlink" title="Lua State"></a>Lua State</h1><p><img src="https://p6-tt.byteimg.com/origin/pgc-image/8e26c797ef854ec392cd5119a50ea68f?from=pc" alt></p>
<h1 id="指令表"><a href="#指令表" class="headerlink" title="指令表"></a>指令表</h1><p>下面是 Lua 的 47 条指令详细说明：</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/c5a115b426cd4fa4ad2c9a3ebbb7c3b4?from=pc" alt><br>B:1 C A:3 MOVE</p>
<p>把源寄存器（索引由 B 指定）里的值移动到目标寄存器（索引有 A 指定），常用于局部变量赋值和参数传递。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/33fc9f47fed047dfa706cd0783dbe0c3?from=pc" alt><br>公式：R(A) := R(B)</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/038ce33628b848a2b821a3d48badbb70?from=pc" alt><br>Bx:2 A:4 LOADK</p>
<p>给单个寄存器（索引由 A 指定）设置成常量（其在常量表的索引由 Bx 指定），将常量表里的某个常量加载到指定寄存器。</p>
<p>在 lua 中，数值型、字符串型等局部变量赋初始值 （数字和字符串会放到常量表中）：</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/1a78929fa797476ab89a676a0d951ea1?from=pc" alt><br>公式：R(A) := Kst(Bx)</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/3fbbe826ecfb4bbc9244c4faa1c55274?from=pc" alt><br>Bx A:4 LOADKX</p>
<p>Ax:585028 EXTRAARG</p>
<p>LOADK 使用 Bx（18bits，最大无符号整数为 262143）表示常量表索引。当将 lua 作数据描述语言使用时，常量表可能会超过这个限制，为了应对这种情况，lua 提供了 LOADKX 指令。LOADKX 指令需要和 EXTRAAG 指令搭配使用，用后者的 Ax（26bits）操作数来指定常量索引。</p>
<p>公式：R(A) := Kst(Ax)</p>
<h1 id="指令名称类型操作码BCA"><a href="#指令名称类型操作码BCA" class="headerlink" title="指令名称类型操作码BCA"></a>指令名称类型操作码BCA</h1><p>LOADBOOLiABC0x03OpArgUOpArgU目标寄存器 idx</p>
<p>B:0 C:1 A:2 LOADBOOL</p>
<p>给单个寄存器（索引由 A 指定）设置布尔值（布尔值由 B 指定），如果寄存器 C 为非 0 则跳过下一条指令。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/5d5be65bc16640e883d2dd3e0b72fa46?from=pc" alt><br>公式：</p>
<p>R(A) := (bool)B</p>
<p>if(C) pc++</p>
<p>指令名称类型操作码BCA<br>LOADNILiABC0x04OpArgUOpArgN目标寄存器 idx</p>
<p>B:4 C A:0 LOADNIL</p>
<p>将序号[A,A+B]连续 B+1 个寄存器设置成 nil 值，用于给连续 n 个寄存器放置 nil 值。在 lua 中，局部变量的默认初始值为 nil，LOADNIL 指令常用于给连续 n 个局部变量设置初始值。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/e9b2057255ec469689e0cb60235dbddb?from=pc" alt><br>公式：R(A), R(A+1), … ,R(A+B) := nil</p>
<p>指令名称类型操作码BCA<br>GETUPVALiABC0x05OpArgUOpArgN目标寄存器 idx</p>
<p>B:1 C A:3 GETUPVAL</p>
<p>把当前闭包的某个 Upvalue 值（索引由 B 指定）拷贝到目标寄存器（索引由 A 指定）中 。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/e8e6687f1d99407a8e62d61df2e22e39?from=pc" alt><br>公式：R(A) := Upvalue[B]</p>
<p>指令名称类型操作码BCA<br>GETTABUPiABC0x06OpArgUOpArgK目标寄存器 idx</p>
<p>B:0 C:0x002 A:3 GETTABUP</p>
<p>把当前闭包的某个 Upvalue 值（索引由 B 指定）拷贝到目标寄存器（索引由 A 指定）中，与 GETUPVAL 不同的是，Upvalue 从表里取值（键由 C 指定，为寄存器或常量表索引）。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/9e026d965cd04e9ebc6d56816b342542?from=pc" alt><br>R(A) := Upvalue[B][rk(c)]</p>
<p>指令名称类型操作码BCA<br>GETTABLEiABC0x07OpArgROpArgK目标寄存器 idx</p>
<p>B:0 C:0x002 A:3 GETTABLE</p>
<p>把表中某个值拷贝到目标寄存器（索引由 A 指定）中，表所在寄存器索引由 B 指定，键由 C（为寄存器或常量表索引）指定。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/07b7c5b03d6e456ba467b9f54bd3a0d9?from=pc" alt><br>公式：R(A) := R[B][rk(c)]</p>
<p>指令名称类型操作码BCA<br>SETTABUPiABC0x08OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x002 C:0x003 A:0 SETTABUP</p>
<p>设置当前闭包的某个 Upvalue 值（索引由 A 指定）为寄存器或常量表的某个值（索引由 C 指定），与 SETUPVAL 不同的是，Upvalue 从表里取值（键由 B 指定，为寄存器或常量表索引）。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/4849926dd2c8460693c83b13cd4daa8b?from=pc" alt><br>Upvalue[A][rk(b)] := RK(C)</p>
<p>指令名称类型操作码BCA<br>SETUPVALiABC0x09OpArgUOpArgN目标寄存器 idx</p>
<p>B:0 C A:3 SETUPVAL</p>
<p>设置当前闭包的某个 Upvalue 值（索引由 B 指定）为寄存器的某个值（索引由 A 指定）。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/c0dbc0195e8e45de81fc697399d2f6ff?from=pc" alt><br>公式：Upvalue[B] := R(A)</p>
<p>指令名称类型操作码BCA<br>SETTABLEiABC0x0AOpArgKOpArgK目标寄存器 idx</p>
<p>B:0x002 C:0x003 A:1 SETTABLE</p>
<p>给寄存器中的表（索引由 A 指定）的某个键进行赋值，键和值分别由 B 和 C 指定（为寄存器或常量表索引）。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/bab05d984ee744ba93b9146e52f061ab?from=pc" alt><br>公式：R(A)[RK(B)] := RK(C)</p>
<h1 id="指令名称类型操作码BCA-1"><a href="#指令名称类型操作码BCA-1" class="headerlink" title="指令名称类型操作码BCA"></a>指令名称类型操作码BCA</h1><p>NEWTABLEiABC0x0BOpArgUOpArgU目标寄存器 idx</p>
<p>B:0 C:2 A:4 NEWTABLE</p>
<p>创建空表，并将其放入指定寄存器（索引有 A 指定），表的初始数组容量和哈希表容量分别有 B 和 C 指定。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/2ad1d863d7d9431f905059dc840fc962?from=pc" alt><br>公式：R(A) := {} (size = B, C)</p>
<p>指令名称类型操作码BCASELFiABC0x0COpArgROpArgK目标寄存器 idx</p>
<p>B:1 C:0x100 A:2 SELF</p>
<p>把寄存器中对象（索引由 B 指定）和常量表中方法（索引由 C 指定）拷贝到相邻的两个目标寄存器中，起始目标寄存器的索引由 A 指定。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/b5f4389ccf1b421a88801be161733e35?from=pc" alt><br>公式：</p>
<p>R(A+1) := R(B)</p>
<p>R(A) := R(B)[RK(C)]</p>
<p>指令名称类型操作码BCAADDiABC0x0DOpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 ADD</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行相加，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/cabba38804fc4af4b91ef3e6120cd23b?from=pc" alt><br>公式：R(A) := RK(B) + RK(C)</p>
<p>指令名称类型操作码BCASUBiABC0x0EOpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 SUB</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行相减，并将结果放入另一个寄存器中（索引由 A 指定）</p>
<p>公式：</p>
<p>R(A) := RK(B) - RK(C)</p>
<p>指令名称类型操作码BCAMULiABC0x0FOpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 MUL</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行相乘，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := RK(B) * RK(C)</p>
<p>指令名称类型操作码BCAMODiABC0x10OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 MOD</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行求摸运算，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := RK(B) % RK(C)</p>
<p>指令名称类型操作码BCAPOWiABC0x11OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 POW</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行求幂运算，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := RK(B) ^ RK(C)</p>
<p>指令名称类型操作码BCADIViABC0x12OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 DIV</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行相除，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := RK(B) / RK(C)</p>
<p>指令名称类型操作码BCAIDIViABC0x13OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 IDIV</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行相整除，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := RK(B) // RK(C)</p>
<p>指令名称类型操作码BCABANDiABC0x14OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 BAND</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行求与操作，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := RK(B) &amp; RK(C)</p>
<p>指令名称类型操作码BCABORiABC0x15OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 BOR</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行求或操作，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := RK(B) | RK(C)</p>
<p>指令名称类型操作码BCABXORiABC0x16OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 BXOR</p>
<p>对两个寄存器或常量值（索引由 B 和 C 指定）进行求异或操作，并将结果放入另一个寄存器中（索引由 A 指定）</p>
<p>公式：R(A) := RK(B) ~ RK(C)</p>
<p>指令名称类型操作码BCASHLiABC0x17OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 SHL</p>
<p>索引由 B 指定的寄存器或常量值进行左移位操作（移动位数的索引由 C 指定的寄存器或常量值），并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := RK(B) &lt;&lt; RK(C)</p>
<p>指令名称类型操作码BCASHRiABC0x18OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:4 SHR</p>
<p>索引由 B 指定的寄存器或常量值进行右移位操作（移动位数的索引由 C 指定的寄存器或常量值），并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := RK(B) &gt;&gt; RK(C)</p>
<p>指令名称类型操作码BCAUNMiABC0x19OpArgROpArgN目标寄存器 idx</p>
<p>B:1 C A:3 UNM</p>
<p>对寄存器（索引由 B 指定）进行取负数操作，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p>公式：R(A) := - R(B)</p>
<p>指令名称类型操作码BCABNOTiABC0x1AOpArgROpArgN目标寄存器 idx</p>
<p>B:1 C A:3 BNOT</p>
<p>对寄存器（索引由 B 指定）进行取反操作，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/58f952d6c394479a8c68cdbaac48c2d8?from=pc" alt><br>公式：R(A) := ~ R(B)</p>
<p>指令名称类型操作码BCANOTiABC0x1BOpArgROpArgN目标寄存器 idx</p>
<p>B:1 C A:3 NOT</p>
<p>对寄存器（索引由 B 指定）进行求非操作，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/b58892f4aabb4bfa9c9ea970d44726cd?from=pc" alt><br>公式：R(A) := not R(B)</p>
<p>指令名称类型操作码BCALENiABC0x1COpArgROpArgN目标寄存器 idx</p>
<p>B:1 C A:3 LEN</p>
<p>对寄存器（索引由 B 指定）进行求长度操作，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/691864b1d5094529a3a0ed10dd6e7879?from=pc" alt><br>公式：R(A) := length of R(B)</p>
<p>指令名称类型操作码BCA<br>CONCATiABC0x1DOpArgROpArgR目标寄存器 idx</p>
<p>B:2 C:4 A:1 CONCAT</p>
<p>将连续 n 个寄存器（起始索引和终止索引由 B 和 C 指定）里的值进行拼接，并将结果放入另一个寄存器中（索引由 A 指定）。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/64ff9c30cc434a13abe4eed37cd7b846?from=pc" alt><br>公式：R(A) := R(B) .. … .. R(C)</p>
<p>指令名称类型操作码sBxAJMPiAsBx0x1EOpArgR目标寄存器 idx</p>
<p>sBx:-1 A JMP</p>
<p>当 sBx 不为 0 时，进行无条件跳转，执行 pc = pc + sBx（sBx 为-1，表示将当前指令再执行一次 注：这将是一个死循环）</p>
<p>sBx:0 A:0x001 JMP；</p>
<p>当 sBx 为 0 时（继续执行后面指令，不跳转），用于闭合处于开启状态的 Upvalue（即：把即将销毁的局部变量的值复制出来，并更新到某个 Upvalue 中）。</p>
<p>当前闭包的某个 Upvalue 值的索引由 A 指定：</p>
<p>指令名称类型操作码BCAEQiABC0x1FOpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:1 EQ</p>
<p>寄存器或常量表（索引由 B 指定）是否等于寄存器或常量表（索引由 C 指定），若结果等于操作数 A，则跳过下一条指令。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/3f63cacef167451aa073da240fcb0e24?from=pc" alt><br>公式：if ((RK(B) == RK(C)) pc++</p>
<p>指令名称类型操作码BCALTiABC0x20OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:1 LT</p>
<p>寄存器或常量表（索引由 B 指定）是否小于寄存器或常量表（索引由 C 指定），若结果等于操作数 A，则跳过下一条指令。</p>
<p>公式：if ((RK(B) &lt; RK(C)) pc++</p>
<p>指令名称类型操作码BCALEiABC0x21OpArgKOpArgK目标寄存器 idx</p>
<p>B:0x001 C:0x100 A:1 LE</p>
<p>寄存器或常量表（索引由 B 指定）是否小于等于寄存器或常量表（索引由 C 指定），若结果等于操作数 A，则跳过下一条指令。</p>
<p>公式：if ((RK(B) &lt;= RK(C)) pc++</p>
<p>指令名称类型操作码BCATESTiABC0x22OpArgNOpArgU目标寄存器 idx</p>
<p>B C:0 A:1 TEST</p>
<p>判断寄存器（索引由 A 指定）中的值转换为 bool 值后，是否和操作数 C 表示的 bool 值一致，若结果不一致，则跳过下一条指令。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/3a4bbb1f035b46a9a2c38866b2009982?from=pc" alt><br>公式：</p>
<p>if not (R(A) &lt;=&gt; C) pc++</p>
<p>注：&lt;=&gt;表示按 bool 值比较</p>
<p>指令名称类型操作码BCA<br>TESTSETiABC0x23OpArgROpArgU目标寄存器 idx</p>
<p>B:3 C:0 A:1 TESTSET</p>
<p>判断寄存器（索引由 B 指定）中的值转换为 bool 值后，是否和操作数 C 表示的 bool 值一致，若结果一致，将寄存器（索引由 B 指定）中的值复制到寄存器中（索引由 A 指定），否则跳过下一条指令。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/2b34822a093f42d78d2c6436f0f5debb?from=pc" alt><br>公式：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (R(B) \&lt;=\&gt; C)</span><br><span class="line">   R(A) := R(B)</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">   pc++</span><br></pre></td></tr></table></figure></p>
<p>注：&lt;=&gt;表示按 bool 值比较</p>
<p>指令名称类型操作码BCACALLiABC0x24OpArgUOpArgU目标寄存器 idx</p>
<p>B:5 C:4 A:0 CALL</p>
<p>被调用函数位于寄存器中（索引由 A 指定），传递给被调用函数的参数值也在寄存器中，紧挨着被调用函数，参数个数为操作数 B 指定。</p>
<p>① B==0，接受其他函数全部返回来的参数</p>
<p>② B&gt;0，参数个数为 B-1</p>
<p>函数调用结束后，原先存放函数和参数值的寄存器会被返回值占据，具体多少个返回值由操作数 C 指定。</p>
<p>① C==0，将返回值全部返回给接收者</p>
<p>② C==1，无返回值</p>
<p>③ C&gt;1，返回值的数量为 C-1</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/7f766e23711746bda75fe1cc76ba4aed?from=pc" alt><br>公式：R(A), … ,</p>
<p>指令名称类型操作码BCA<br>TAILCALLiABC0x25OpArgUOpArgU目标寄存器 idx</p>
<p>函数调用一般通过调用栈来实现。用这种方法，每调用一个函数都会产生一个调用帧。</p>
<p>如果调用层次太深（如递归），容易导致栈溢出。尾递归优化则可以让我们发挥递归函数调用威力的同时，避免调用栈溢出。利用这种优化，被调函数可以重用主调函数的调用帧，因此可有效缓解调用栈溢出症状。不过该优化只适合某些特定情况。</p>
<p>如：return f(args) 会被编译器优化成 TAILCALL 指令，公式：return R(A)(R(A+1), … , R(A+B-1))</p>
<p>指令名称类型操作码BCA<br>RETURNiABC0x26OpArgUOpArgN目标寄存器 idx</p>
<p>B:4 C A:2 RETURN</p>
<p>把存放在连续多个寄存器里的值返回给父函数，其中第一个寄存器的索引由操作数 A 指定，寄存器数量由操作数 B 指定，操作数 C 没有使用，需要将返回值推入栈顶：</p>
<p>① B==1，不需要返回任何值</p>
<p>② B &gt; 1，需要返回 B-1 个值；这些值已经在寄存器中了，只用再将它们复制到栈顶即可</p>
<p>③ B==0，一部分返回值已经在栈顶了，只需将另一部分也推入栈顶即可</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/916820bd14cd4f7fb7ba7c1fa65f6b9b?from=pc" alt><br>公式：return R(A),…,R(A+B-2)</p>
<p>指令名称类型操作码sBxAFORLOOPiAsBx0x27OpArgR目标寄存器 idx</p>
<p>数值 for 循环：用于按一定步长遍历某个范围内的数值 如：for i=1,100,2 do f() end // 初始值为 1，步长为 2，上限为 100</p>
<p>该指令先给 i 加上步长，然后判断 i 是否在范围之内。若已经超出范围，则循环结束；若为超出范围，则将数值拷贝给用户定义的局部变量，然后跳转到循环体内部开始执行具体的代码块。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/f0501b686d90434797c15e6e09102948?from=pc" alt><br>公式：</p>
<p>R(A) += R(A+2)<br>if R(A) &lt;?= R(A+1)<br>    pc+=sBx<br>    R(A+3)=R(A)<br>注：当步长为正数时&lt;?=为&lt;=</p>
<p>当步长为负数时&lt;?=为&gt;=</p>
<p>指令名称类型操作码sBxAFORPREPiAsBx0x28OpArgR目标寄存器 idx</p>
<p>数值 for 循环：用于按一定步长遍历某个范围内的数值 如：for i=1,100,2 do f() end // 初始值为 1，步长为 2，上限为 100。</p>
<p>该指令的目的是在循环之前预先将 i 减去步长（得到-1），然后跳转到 FORLOOP 指令正式开始循环：</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/1196cd6635154accbcd594f83a6d843a?from=pc" alt><br>公式：</p>
<p>R(A)-=R(A+2)</p>
<p>pc+=sBx</p>
<p>指令名称类型操作码BCA<br>TFORCALLiABC0x29OpArgNOpArgU目标寄存器 idx</p>
<p>通用 for 循环：for k,v in pairs(t) do print(k,v) end</p>
<p>编译器使用的第一个特殊变量(generator):f 存放的是迭代器，其他两个特殊变量(state):s、(control):var 来调用迭代器，把结果保存在用户定义的变量 k、v 中。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/bd8e1ebd5a12455a8903ae4c26c01589?from=pc" alt><br>公式：R(A+3),…,R(A+2+C) := R(A)(R(A+1),R(A+2))</p>
<p>指令名称类型操作码sBxATFORLOOPiAsBx0x2AOpArgR目标寄存器 idx</p>
<p>通用 for 循环：for k,v in pairs(t) do print(k,v) end</p>
<p>若迭代器返回的第一个值（变量 k）不是 nil，则把该值拷贝到(control):var，然后跳转到循环体；若为 nil，则循环结束。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/0cb91e33d94e4dcabc6e8a1adcf459ab?from=pc" alt><br>公式：</p>
<p>if R(A+1) ~= nil</p>
<p>R(A)=R(A+1)</p>
<p>pc+=sBx</p>
<p>指令名称类型操作码BCA<br>SETLISTiABC0x2BOpArgUOpArgU目标寄存器 idx</p>
<p>SETTABLE 是通用指令，每次只处理一个键值对，具体操作交给表去处理，并不关心实际写入的是表的 hash 部分还是数组部分。SETLIST 则是专门给数组准备的，用于按索引批量设置数组元素。其中数组位于寄存器中，索引由操作数 A 指定；需要写入数组的一系列值也在寄存器中，紧挨着数组，数量由操作数 B 指定；数组起始索引则由操作数 C 指定。</p>
<p>因为 C 操作数只有 9bits，所以直接用它表示数组索引显然不够用。这里解决办法是让 C 操作数保存批次数，然后用批次数乘上批大小（FPF，默认为 50）就可以算出数组的起始索引。因此，C 操作数能表示的最大索引为 25600（50*512），当数组长度大于 25600 时，SETLIST 指令后会跟一条 EXTRAARG 指令，用其 Ax 操作数来保存批次数。</p>
<p>综上，C&gt;0，表示的是批次数+1，否则，真正批次数存放在后续的 EXTRAARG 指令中。</p>
<p>操作数 B 为 0 时，当表构造器的最后一个元素是函数调用或者 vararg 表达式时，Lua 会把它们产生的所有值都收集起来供 SETLIST 使用。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/164a6a250fcc46a8918355777ffb0848?from=pc" alt><br>公式：</p>
<p>R(A)[(C-1)*FPF+i] := R(A+i)</p>
<p>1 &lt;= i &lt;= B</p>
<p>指令名称类型操作码BxACLOSUREiABx0x2COpArgU目标寄存器 idx</p>
<p>把当前 Lua 函数的子函数原型实例化为闭包，放入由操作数 A 指定的寄存器中子函数原型来自于当前函数原型的子函数原型表，索引由操作数 Bx 指定。</p>
<p>下图为将 prototypes 表中索引为 1 的 g 子函数，放入索引为 4 的寄存器中：</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/a209d81fdebc4dbcace002a601920443?from=pc" alt><br>公式：R(A) := closure(KPROTO[Bx])</p>
<p>指令名称类型操作码BCA<br>VARARGiABC0x2DOpArgUOpArgN目标寄存器 idx</p>
<p>把传递给当前函数的变长参数加载到连续多个寄存器中。</p>
<p>其中第一个寄存器的索引由操作数 A 指定，寄存器数量由操作数 B 指定，操作数 C 没有使用，操作数 B 若大于 1，表示把 B-1 个 vararg 参数复制到寄存器中，否则只能等于 0。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/14bea5e82eca482e93f1c624fea3467c?from=pc" alt><br>公式：R(A),R(A+1),…R(A+B-2)=vararg</p>
<p>指令名称类型操作码AxEXTRAARGiAx0x2EOpArgU</p>
<p>Ax:67108864 EXTRAARG</p>
<p>Ax 有 26bits，用来指定常量索引，可存放最大无符号整数为 67108864，可满足大部分情况的需要了。</p>
<p>参考</p>
<ul>
<li>《自己动手实现 Lua》源代码</li>
<li>Lua 设计与实现–虚拟机篇</li>
<li>Lua 5.3 Bytecode Reference</li>
<li>Lua 源码解析<br>作者：nicochen，腾讯 IEG 游戏开发工程师</li>
</ul>
]]></content>
      <categories>
        <category>lua</category>
        <category>lua知识点</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>lua知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>Golang 中的垃圾回收（二)</title>
    <url>/go/Golang%20%E4%B8%AD%E7%9A%84%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6/Golang%20%E4%B8%AD%E7%9A%84%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6(%E4%BA%8C%EF%BC%89/</url>
    <content><![CDATA[<p><a href="https://juejin.cn/post/6844903892212269064" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>第一部分，我花了时间去描述了golang 垃圾收集器的行为，并说明了程序运行时候收集器所产生的延迟。我分享了怎么样去产生以及去解释GC traces。并展示了堆内内存如何改变，并且解释了GC不同阶段对延迟代价的影响。<br>最后的结论是，如果你减少了堆的压力，你就会减少延迟代价并提高了程序性能。我也指出了，降低收集速率，推迟收集，设法增加两次收集的时间间隔并不是很好的策略。即使是收集速率很快，但是以一致的速度进行，也能保持程序以最佳性能运行。<br>这部分里，我会带你通过一个实际的web应用并向你展示怎么样生成GC traces和程序状态。然后我会解释这些工具的输出内容，这样你可以找到方法去提高应用程序的性能。<br><strong>运行应用</strong><br>看一下go training的web应用程序<br>图1.1<br><img src="https://user-gold-cdn.xitu.io/2019/7/18/16c03b402f24c06a?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br><a href="https://github.com/ardanlabs/gotraining/tree/master/topics/go/profiling/project" rel="external nofollow noopener noreferrer" target="_blank">github.com/ardanlabs/g…</a><br>图一是应用程序的样子。这个程序从不同的地方下载三个rss 订阅集合，并且允许用户去进行搜索。build之后，启动程序<br><strong>L1</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build</span><br><span class="line">$ GOGC=off ./project &gt; /dev/null</span><br></pre></td></tr></table></figure></p>
<p>复制代码L1中我们设置了GOGC变量为off，也就是关闭垃圾回收。日志重定向到/dev/null。随着程序运行，我们可以向server中发送请求了。<br><strong>L2</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hey -m POST -c 100 -n 10000 <span class="string">"http://localhost:5000/search?</span></span><br><span class="line"><span class="string">term=topic&amp;cnn=on&amp;bbc=on&amp;nyt=on"</span></span><br></pre></td></tr></table></figure></p>
<p>复制代码L2展示了通过hey使用100个连接发送了10k的请求。所有请求发送到服务端，这个过程结果产生如下结果<br>图1.2<br><img src="https://user-gold-cdn.xitu.io/2019/7/18/16c03b4245f5e90e?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.2给出了关闭GC处理requests的可视化的过程。10k请求的处理大概花费了4188ms，也就是服务端每秒处理大概2387次请求。<br>打开GC<br>如果开启GC的话会怎么样<br><strong>L3</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ GODEBUG=gctrace=1 ./project &gt; /dev/null</span><br></pre></td></tr></table></figure></p>
<p>复制代码L3中GOGC移除了，使用了GODEBUG变量，我们可以看到GC traces。<br>GODEBUG设置之后runtime就会在每次收集的时候生成GC trace。现在我们再次跑同样10k的请求。所有请求发送到服务端，我们可以看到GC traces和hey提供的信息。<br><strong>L4</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ GODEBUG=gctrace=1 ./project &gt; /dev/null</span><br><span class="line">gc 3 @3.182s 0%: 0.015+0.59+0.096 ms clock, 0.19+0.10/1.3/3.0+1.1 ms cpu, 4-&gt;4-&gt;2 MB, 5 MB goal, 12 P</span><br><span class="line">.</span><br><span class="line">.</span><br><span class="line">.</span><br><span class="line">gc 2553 @8.452s 14%: 0.004+0.33+0.051 ms clock, 0.056+0.12/0.56/0.94+0.61 ms cpu, 4-&gt;4-&gt;2 MB, 5 MB goal, 12 P</span><br></pre></td></tr></table></figure></p>
<p>复制代码L4中展示了从程序运行开始的第三次和最后一次的GC trace。我没展示前两次的collection因为在这些收集发生之后，负载信息才会通过服务发送。最后一次collection表明了处理请求一共产生了2551次收集（没算前两个）<br>下面是我对每次trace进行了拆分<br><strong>L5</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gc 2553 @8.452s 14%: 0.004+0.33+0.051 ms clock, 0.056+0.12/0.56/0.94+0.61 ms cpu, 4-&gt;4-&gt;2 MB, 5 MB goal, 12 P</span><br><span class="line"></span><br><span class="line">gc 2553     : The 2553 GC runs since the program started</span><br><span class="line">@8.452s     : Eight seconds since the program started</span><br><span class="line">14%         : Fourteen percent of the available CPU so far has been spent <span class="keyword">in</span> GC</span><br><span class="line"></span><br><span class="line">// wall-clock</span><br><span class="line">0.004ms     : STW        : Write-Barrier - Wait <span class="keyword">for</span> all Ps to reach a GC safe-point.</span><br><span class="line">0.33ms      : Concurrent : Marking</span><br><span class="line">0.051ms     : STW        : Mark Term     - Write Barrier off and clean up.</span><br><span class="line"></span><br><span class="line">// CPU time</span><br><span class="line">0.056ms     : STW        : Write-Barrier</span><br><span class="line">0.12ms      : Concurrent : Mark - Assist Time (GC performed <span class="keyword">in</span> line with allocation)</span><br><span class="line">0.56ms      : Concurrent : Mark - Background GC time</span><br><span class="line">0.94ms      : Concurrent : Mark - Idle GC time</span><br><span class="line">0.61ms      : STW        : Mark Term</span><br><span class="line"></span><br><span class="line">4MB         : Heap memory <span class="keyword">in</span>-use before the Marking started</span><br><span class="line">4MB         : Heap memory <span class="keyword">in</span>-use after the Marking finished</span><br><span class="line">2MB         : Heap memory marked as live after the Marking finished</span><br><span class="line">5MB         : Collection goal <span class="keyword">for</span> heap memory <span class="keyword">in</span>-use after Marking finished</span><br><span class="line"></span><br><span class="line">// Threads</span><br><span class="line">12P         : Number of logical processors or threads used to run Goroutines.</span><br></pre></td></tr></table></figure></p>
<p>复制代码L5展示了最后一次collection的实际数字。多亏hey,下面是我们看到的运行时的性能结果。<br>L6<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Requests            : 10,000</span><br><span class="line">------------------------------------------------------</span><br><span class="line">Requests/sec        : 1,882 r/s   - Hey</span><br><span class="line">Total Duration      : 5,311ms     - Hey</span><br><span class="line">Percent Time <span class="keyword">in</span> GC  : 14%         - GC Trace</span><br><span class="line">Total Collections   : 2,551       - GC Trace</span><br><span class="line">------------------------------------------------------</span><br><span class="line">Total GC Duration   : 744.54ms    - (5,311ms * .14)</span><br><span class="line">Average Pace of GC  : ~2.08ms     - (5,311ms / 2,551)</span><br><span class="line">Requests/Collection : ~3.98 r/gc  - (10,000 / 2,511)</span><br></pre></td></tr></table></figure></p>
<p>L6给出了结果。下面给出了过程的可视化，来看一下发生了什么。<br>图1.3<br><img src="https://user-gold-cdn.xitu.io/2019/7/18/16c03b55867b98a6?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.3给出了可视化的发生过程。开启回收器之后，相同10k的请求，它必须去处理大概~2.5k次collection。每次collection平均处理速度大概是2ms，处理这些collection增加了额外大概1.1秒的延迟。<br>图1.4<br><img src="https://user-gold-cdn.xitu.io/2019/7/18/16c03b5e9bd0a6a3?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.4对比了两种情况下应用程序的运行情况。<br>减少分配<br>我们最好是可以看看堆内存的分配情况，来看看是否有可以去掉的non-productive的分配<br><strong>L7</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">go tool pprof http://localhost:5000/debug/pprof/allocs</span><br></pre></td></tr></table></figure></p>
<p>复制代码L7使用了pprof去调用/debug/pprof/allocs接口，然后取从运行程序里拉取内存分配信息。这个接口的存在是因为用了下面的代码。<br>L8<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> _ <span class="string">"net/http/pprof"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    http.ListenAndServe(<span class="string">"localhost:5000"</span>, http.DefaultServeMux)</span><br><span class="line">&#125;()</span><br></pre></td></tr></table></figure></p>
<p>导入net/http/pprof去绑定系统的默认服务。然后使用http.ListenAndServer携带http.DefaultServerMux去开启服务。<br><strong>L9</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(pprof) top 6 -cum</span><br><span class="line">Showing nodes accounting <span class="keyword">for</span> 0.56GB, 5.84% of 9.56GB total</span><br><span class="line">Dropped 80 nodes (cum &lt;= 0.05GB)</span><br><span class="line">Showing top 6 nodes out of 51</span><br><span class="line">      flat  flat%   sum%        cum   cum%</span><br><span class="line">         0     0%     0%     4.96GB 51.90%  net/http.(*conn).serve</span><br><span class="line">    0.49GB  5.11%  5.11%     4.93GB 51.55%  project/service.handler</span><br><span class="line">         0     0%  5.11%     4.93GB 51.55%  net/http.(*ServeMux).ServeHTTP</span><br><span class="line">         0     0%  5.11%     4.93GB 51.55%  net/http.HandlerFunc.ServeHTTP</span><br><span class="line">         0     0%  5.11%     4.93GB 51.55%  net/http.serverHandler.ServeHTTP</span><br><span class="line">    0.07GB  0.73%  5.84%     4.55GB 47.63%  project/search.rssSearch</span><br></pre></td></tr></table></figure></p>
<p>L9的底下出现了rssSearch的方法。这个方法分配了4.55GB的内存。接下来使用list去看一下这个方法的内部细节<br>L10<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(pprof) list rssSearch</span><br><span class="line">Total: 9.56GB</span><br><span class="line">ROUTINE ======================== project/search.rssSearch <span class="keyword">in</span> project/search/rss.go</span><br><span class="line">   71.53MB     4.55GB (flat, cum) 47.63% of Total</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">         .          .    117:	// Capture the data we need <span class="keyword">for</span> our results <span class="keyword">if</span> we find ...</span><br><span class="line">         .          .    118:	<span class="keyword">for</span> _, item := range d.Channel.Items &#123;</span><br><span class="line">         .     4.48GB    119:		<span class="keyword">if</span> strings.Contains(strings.ToLower(item.Description), strings.ToLower(term)) &#123;</span><br><span class="line">   48.53MB    48.53MB    120:			results = append(results, Result&#123;</span><br><span class="line">         .          .    121:				Engine:  engine,</span><br><span class="line">         .          .    122:				Title:   item.Title,</span><br><span class="line">         .          .    123:				Link:    item.Link,</span><br><span class="line">         .          .    124:				Content: item.Description,</span><br><span class="line">         .          .    125:			&#125;)</span><br></pre></td></tr></table></figure></p>
<p>L10列出了代码，第119行出现了大量的内存分配<br>L11<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.     4.48GB    119:		<span class="keyword">if</span> strings.Contains(strings.ToLower(item.Description), strings.ToLower(term)) &#123;</span><br></pre></td></tr></table></figure></p>
<p>L11 给出了出现问题的代码。整个函数分配了4.55GB的数据，但这一行就分配了4.48GB。接下来看一下这一行代码做了什么，然后看看有哪里可以优化的地方。<br><strong>L12</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">117 // Capture the data we need <span class="keyword">for</span> our results <span class="keyword">if</span> we find the search term.</span><br><span class="line">118 <span class="keyword">for</span> _, item := range d.Channel.Items &#123;</span><br><span class="line">119     <span class="keyword">if</span> strings.Contains(strings.ToLower(item.Description), strings.ToLower(term)) &#123;</span><br><span class="line">120         results = append(results, Result&#123;</span><br><span class="line">121             Engine:  engine,</span><br><span class="line">122             Title:   item.Title,</span><br><span class="line">123             Link:    item.Link,</span><br><span class="line">124             Content: item.Description,</span><br><span class="line">125        &#125;)</span><br><span class="line">126    &#125;</span><br><span class="line">127 &#125;</span><br></pre></td></tr></table></figure></p>
<p>复制代码L12列出了tight loop里的代码。调用strings.ToLower会产生内存分配，因为需要创造新的strings，而且它们需要分配到堆内存上。在循环里调用strings.ToLower根本没有必要，因为完全可以在循环外面进行处理。<br>119行代码可以做一下调整来去掉不必要的内存分配。<br><strong>L13</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Before the code change.</span></span><br><span class="line"><span class="keyword">if</span> strings.Contains(strings.ToLower(item.Description), strings.ToLower(term)) &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">// After the code change.</span></span><br><span class="line"><span class="keyword">if</span> strings.Contains(item.Description, term) &#123;</span><br></pre></td></tr></table></figure></p>
<p><strong>注意：在把item塞进缓存之前已经把item.Description进行lower处理了，这部分代码没列出来。新塞进来item会每15分钟缓存一次。把term进行lower处理直接在循环外面进行就可以了</strong><br>L13里给出了我们是怎么把strings.ToLower从tight loop中拿掉的。重新编译项目然后运行，然后再次拿10k个requests去请求。<br><strong>L14</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build</span><br><span class="line">$ GODEBUG=gctrace=1 ./project &gt; /dev/null</span><br><span class="line">gc 3 @6.156s 0%: 0.011+0.72+0.068 ms clock, 0.13+0.21/1.5/3.2+0.82 ms cpu, 4-&gt;4-&gt;2 MB, 5 MB goal, 12 P</span><br><span class="line">.</span><br><span class="line">.</span><br><span class="line">.</span><br><span class="line">gc 1404 @8.808s 7%: 0.005+0.54+0.059 ms clock, 0.060+0.47/0.79/0.25+0.71 ms cpu, 4-&gt;5-&gt;2 MB, 5 MB goal, 12 P</span><br></pre></td></tr></table></figure></p>
<p>L14中，在调整了代码之后，相同10K请求下，现在只有1402次的collections次数了。<br>L15<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">With Extra Allocations              Without Extra Allocations</span><br><span class="line">======================================================================</span><br><span class="line">Requests            : 10,000        Requests            : 10,000</span><br><span class="line">----------------------------------------------------------------------</span><br><span class="line">Requests/sec        : 1,882 r/s     Requests/sec        : 3,631 r/s</span><br><span class="line">Total Duration      : 5,311ms       Total Duration      : 2,753 ms</span><br><span class="line">Percent Time <span class="keyword">in</span> GC  : 14%           Percent Time <span class="keyword">in</span> GC  : 7%</span><br><span class="line">Total Collections   : 2,551         Total Collections   : 1,402</span><br><span class="line">----------------------------------------------------------------------</span><br><span class="line">Total GC Duration   : 744.54ms      Total GC Duration   : 192.71 ms</span><br><span class="line">Average Pace of GC  : ~2.08ms       Average Pace of GC  : ~1.96ms</span><br><span class="line">Requests/Collection : ~3.98 r/gc    Requests/Collection : 7.13 r/gc</span><br></pre></td></tr></table></figure></p>
<p>L15展示了和上一次结果的对比。下面提供更加清晰的可视化图例来看看发生了什么。<br>图1.5<br><img src="https://user-gold-cdn.xitu.io/2019/7/18/16c03b6d4417e1c5?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.5展示了对比图。这一次收集器运行少了1149次（1402 vs 2551）在相同10k请求情况下。GC的时间比率从14%降到了7%。collection节省了%74的时间，这让应用程序处理速度提高了48%。<br>图1.6<br><img src="https://user-gold-cdn.xitu.io/2019/7/18/16c03b62033c44ef?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.6展示了4中不同情况的对比图。我加上了优化版本关闭gc的情况<br><strong>学习到什么</strong><br>在上一篇文章中提到，提升收集器的效率在于减少堆内存的压力。记住，压力可以定义为：在给定时间内，应用程序在可用堆内存上的分配有多快。当压力减少，收集器产生的延时就会减少。这些延迟会拖慢你的应用程序。<br>L16<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">With Extra Allocations              Without Extra Allocations</span><br><span class="line">======================================================================</span><br><span class="line">Requests            : 10,000        Requests            : 10,000</span><br><span class="line">----------------------------------------------------------------------</span><br><span class="line">Requests/sec        : 1,882 r/s     Requests/sec        : 3,631 r/s</span><br><span class="line">Total Duration      : 5,311ms       Total Duration      : 2,753 ms</span><br><span class="line">Percent Time <span class="keyword">in</span> GC  : 14%           Percent Time <span class="keyword">in</span> GC  : 7%</span><br><span class="line">Total Collections   : 2,551         Total Collections   : 1,402</span><br><span class="line">----------------------------------------------------------------------</span><br><span class="line">Total GC Duration   : 744.54ms      Total GC Duration   : 192.71 ms</span><br><span class="line">Average Pace of GC  : ~2.08ms       Average Pace of GC  : ~1.96ms</span><br><span class="line">Requests/Collection : ~3.98 r/gc    Requests/Collection : 7.13 r/gc</span><br></pre></td></tr></table></figure></p>
<p>L16是上面两个带GC版本的程序运行对比。很明显移除了4.48GB的不必要分配使程序运行更快。有趣的事情是，两个版本里，每次垃圾收集的平均速度时间几乎是相同的，大概都是在2ms左右。我们改变的事情是在于每次收集之间做了更多的work处理。应用程序从3.98r/gc提升到7.13r/gc(请求次数/gc)。工作量提高了79.1%。<br>在两次收集之间处理更多work会帮助减少收集的次数，可以看到gc次数从2551减少到了1402，大概45%的降低。应用程序的gc时间减少了74%，从745ms减少到193ms，也就是总时间的14%减少到7%。如果你运行关闭gc的优化版本代码，性能差异只有13%，应用程序处理时间从2753ms降到2398ms。<br><strong>结论</strong><br>如果你花时间专注于减少分配，作为go开发者的你也就是在提高gc的效率。你无法写出0分配的程序，因此区分生产性的分配（利于程序运行）和非生产性的内存分配(损害性能)是很重要的事情。之后你就可以完全相信gc去帮你处理内存管理的事情了。<br>gc是一个好的折衷方式。我花一点时间去进行gc处理，得到的是我不需要再去关系内存的管理了。go会让开发者更有效率而且应用程序依旧可以足够快的运行，这一切都归功于golang的garbage collector<br>原文链接：<a href="https://www.ardanlabs.com/blog/2019/05/garbage-collection-in-go-part2-gctraces.html" rel="external nofollow noopener noreferrer" target="_blank">www.ardanlabs.com/blog/2019/0…</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go内存详解</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go内存详解</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器之创建main goroutine(13)</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/Go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E4%B9%8B%E5%88%9B%E5%BB%BAmain-goroutine-13/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/FF8YU8nXP9TKt0qvSuzmtw" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a></p>
<p>本文是《Go语言调度器源代码情景分析》系列的第13篇，也是第二章的第3小节。</p>
<p>上一节我们分析了调度器的初始化，这一节我们来看程序中的第一个goroutine是如何创建的。</p>
<p>创建main goroutine<br>接上一节，schedinit完成调度系统初始化后，返回到rt0_go函数中开始调用newproc() 创建一个新的goroutine用于执行mainPC所对应的runtime·main函数，看下面的代码：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">197</span></span><br><span class="line"></span><br><span class="line"># create a <span class="built_in">new</span> goroutine to start program</span><br><span class="line">MOVQ  $runtime·mainPC(SB), AX # entry，mainPC是runtime.main</span><br><span class="line"># newproc的第二个参数入栈，也就是新的goroutine需要执行的函数</span><br><span class="line">PUSHQ  AX          # AX = &amp;funcval&#123;runtime·main&#125;,</span><br><span class="line"> </span><br><span class="line"># newproc的第一个参数入栈，该参数表示runtime.main函数需要的参数大小，因为runtime.main没有参数，所以这里是<span class="number">0</span></span><br><span class="line">PUSHQ  $<span class="number">0</span></span><br><span class="line">CALL  runtime·newproc(SB) # 创建main goroutine</span><br><span class="line">POPQ  AX</span><br><span class="line">POPQ  AX</span><br><span class="line"> </span><br><span class="line"># start this M</span><br><span class="line">CALL  runtime·mstart(SB)  # 主线程进入调度循环，运行刚刚创建的goroutine</span><br><span class="line"> </span><br><span class="line"># 上面的mstart永远不应该返回的，如果返回了，一定是代码逻辑有问题，直接abort</span><br><span class="line">CALL  runtime·abort(SB)<span class="comment">// mstart should never return</span></span><br><span class="line">RET</span><br><span class="line"> </span><br><span class="line">DATA  runtime·mainPC+<span class="number">0</span>(SB)/<span class="number">8</span>,$runtime·main(SB)</span><br><span class="line">GLOB  Lruntime·mainPC(SB),RODATA,$<span class="number">8</span></span><br></pre></td></tr></table></figure></p>
<p>在后面的分析过程中我们会看到这个runtime.main最终会调用我们写的main.main函数，在分析runtime·main之前我们先把重点放在newproc这个函数上。</p>
<p>newproc函数用于创建新的goroutine，它有两个参数，先说第二个参数fn，新创建出来的goroutine将从fn这个函数开始执行，而这个fn函数可能也会有参数，newproc的第一个参数正是fn函数的参数以字节为单位的大小。比如有如下go代码片段：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">start</span><span class="params">(a, b, c <span class="keyword">int64</span>)</span></span> &#123;</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">go</span> start(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>编译器在编译上面的go语句时，就会把其替换为对newproc函数的调用，编译后的代码逻辑上等同于下面的伪代码<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    push <span class="number">0x3</span></span><br><span class="line">    push <span class="number">0x2</span></span><br><span class="line">    push <span class="number">0x1</span></span><br><span class="line">    runtime.newproc(<span class="number">24</span>, start)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>编译器编译时首先会用几条指令把start函数需要用到的3个参数压栈，然后调用newproc函数。因为start函数的3个int64类型的参数共占24个字节，所以传递给newproc的第一个参数是24，表示start函数需要24字节大小的参数。</p>
<p>那为什么需要传递fn函数的参数大小给newproc函数呢？原因就在于newproc函数将创建一个新的goroutine来执行fn函数，而这个新创建的goroutine与当前这个goroutine会使用不同的栈，因此就需要在创建goroutine的时候把fn需要用到的参数先从当前goroutine的栈上拷贝到新的goroutine的栈上之后才能让其开始执行，而newproc函数本身并不知道需要拷贝多少数据到新创建的goroutine的栈上去，所以需要用参数的方式指定拷贝多少数据。</p>
<p>了解完这些背景知识之后，下面我们开始分析newproc的代码。newproc函数是对newproc1的一个包装，这里最重要的准备工作有两个，一个是获取fn函数第一个参数的地址（代码中的argp），另一个是使用systemstack函数切换到g0栈，当然，对于我们这个初始化场景来说现在本来就在g0栈，所以不需要切换，然而这个函数是通用的，在用户的goroutine中也会创建goroutine，这时就需要进行栈的切换。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">3232</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Create a new g running fn with siz bytes of arguments.</span></span><br><span class="line"><span class="comment">// Put it on the queue of g's waiting to run.</span></span><br><span class="line"><span class="comment">// The compiler turns a go statement into a call to this.</span></span><br><span class="line"><span class="comment">// Cannot split the stack because it assumes that the arguments</span></span><br><span class="line"><span class="comment">// are available sequentially after &amp;fn; they would not be</span></span><br><span class="line"><span class="comment">// copied if a stack split occurred.</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">newproc</span><span class="params">(siz <span class="keyword">int32</span>, fn *funcval)</span></span> &#123;</span><br><span class="line">    <span class="comment">//函数调用参数入栈顺序是从右向左，而且栈是从高地址向低地址增长的</span></span><br><span class="line">    <span class="comment">//注意：argp指向fn函数的第一个参数，而不是newproc函数的参数</span></span><br><span class="line">    <span class="comment">//参数fn在栈上的地址+8的位置存放的是fn函数的第一个参数</span></span><br><span class="line">    argp := add(unsafe.Pointer(&amp;fn), sys.PtrSize)</span><br><span class="line">    gp := getg()  <span class="comment">//获取正在运行的g，初始化时是m0.g0</span></span><br><span class="line">   </span><br><span class="line">    <span class="comment">//getcallerpc()返回一个地址，也就是调用newproc时由call指令压栈的函数返回地址，</span></span><br><span class="line">    <span class="comment">//对于我们现在这个场景来说，pc就是CALLruntime·newproc(SB)指令后面的POPQ AX这条指令的地址</span></span><br><span class="line">    pc := getcallerpc()</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//systemstack的作用是切换到g0栈执行作为参数的函数</span></span><br><span class="line">    <span class="comment">//我们这个场景现在本身就在g0栈，因此什么也不做，直接调用作为参数的函数</span></span><br><span class="line">    systemstack(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        newproc1(fn, (*<span class="keyword">uint8</span>)(argp), siz, gp, pc)</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br><span class="line">newproc1函数的第一个参数fn是新创建的goroutine需要执行的函数，注意这个fn的类型是funcval结构体类型，其定义如下：</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> funcval <span class="keyword">struct</span> &#123;</span><br><span class="line">    fn <span class="keyword">uintptr</span></span><br><span class="line">    <span class="comment">// variable-size, fn-specific data here</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>newproc1的第二个参数argp是fn函数的第一个参数的地址，第三个参数是fn函数的参数以字节为单位的大小，后面两个参数我们不用关心。这里需要注意的是，newproc1是在g0的栈上执行的。该函数很长也很重要，所以我们分段来看。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">3248</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Create a new g running fn with narg bytes of arguments starting</span></span><br><span class="line"><span class="comment">// at argp. callerpc is the address of the go statement that created</span></span><br><span class="line"><span class="comment">// this. The new g is put on the queue of g's waiting to run.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">newproc1</span><span class="params">(fn *funcval, argp *<span class="keyword">uint8</span>, narg <span class="keyword">int32</span>, callergp *g, callerpc <span class="keyword">uintptr</span>)</span></span> &#123;</span><br><span class="line">    <span class="comment">//因为已经切换到g0栈，所以无论什么场景都有 _g_ = g0，当然这个g0是指当前工作线程的g0</span></span><br><span class="line">    <span class="comment">//对于我们这个场景来说，当前工作线程是主线程，所以这里的g0 = m0.g0</span></span><br><span class="line">    _g_ := getg() </span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    _p_ := _g_.m.p.ptr() <span class="comment">//初始化时_p_ = g0.m.p，从前面的分析可以知道其实就是allp[0]</span></span><br><span class="line">    newg := gfget(_p_) <span class="comment">//从p的本地缓冲里获取一个没有使用的g，初始化时没有，返回nil</span></span><br><span class="line">    <span class="keyword">if</span> newg == <span class="literal">nil</span> &#123;</span><br><span class="line">         <span class="comment">//new一个g结构体对象，然后从堆上为其分配栈，并设置g的stack成员和两个stackgard成员</span></span><br><span class="line">        newg = malg(_StackMin)</span><br><span class="line">        casgstatus(newg, _Gidle, _Gdead) <span class="comment">//初始化g的状态为_Gdead</span></span><br><span class="line">         <span class="comment">//放入全局变量allgs切片中</span></span><br><span class="line">        allgadd(newg) <span class="comment">// publishes with a g-&gt;status of Gdead so GC scanner doesn't look at uninitialized stack.</span></span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">    ......</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//调整g的栈顶置针，无需关注</span></span><br><span class="line">    totalSize := <span class="number">4</span>*sys.RegSize + <span class="keyword">uintptr</span>(siz) + sys.MinFrameSize <span class="comment">// extra space in case of reads slightly beyond frame</span></span><br><span class="line">    totalSize += -totalSize &amp; (sys.SpAlign - <span class="number">1</span>)                  <span class="comment">// align to spAlign</span></span><br><span class="line">    sp := newg.stack.hi - totalSize</span><br><span class="line">    spArg := sp</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">   </span><br><span class="line">    <span class="keyword">if</span> narg &gt; <span class="number">0</span> &#123;</span><br><span class="line">         <span class="comment">//把参数从执行newproc函数的栈（初始化时是g0栈）拷贝到新g的栈</span></span><br><span class="line">        memmove(unsafe.Pointer(spArg), unsafe.Pointer(argp), <span class="keyword">uintptr</span>(narg))</span><br><span class="line">        <span class="comment">// ......</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p>
<p>这段代码主要从堆上分配一个g结构体对象并为这个newg分配一个大小为2048字节的栈，并设置好newg的stack成员，然后把newg需要执行的函数的参数从执行newproc函数的栈（初始化时是g0栈）拷贝到newg的栈，完成这些事情之后newg的状态如下图所示：</p>
<p>我们可以看到，经过前面的代码之后，程序中多了一个我们称之为newg的g结构体对象，该对象也已经获得了从堆上分配而来的2k大小的栈空间，newg的stack.hi和stack.lo分别指向了其栈空间的起止位置。</p>
<p>接下来我们继续分析newproc1函数。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">3314</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//把newg.sched结构体成员的所有成员设置为0</span></span><br><span class="line">    memclrNoHeapPointers(unsafe.Pointer(&amp;newg.sched), unsafe.Sizeof(newg.sched))</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//设置newg的sched成员，调度器需要依靠这些字段才能把goroutine调度到CPU上运行。</span></span><br><span class="line">    newg.sched.sp = sp  <span class="comment">//newg的栈顶</span></span><br><span class="line">    newg.stktopsp = sp</span><br><span class="line">    <span class="comment">//newg.sched.pc表示当newg被调度起来运行时从这个地址开始执行指令</span></span><br><span class="line">    <span class="comment">//把pc设置成了goexit这个函数偏移1（sys.PCQuantum等于1）的位置，</span></span><br><span class="line">    <span class="comment">//至于为什么要这么做需要等到分析完gostartcallfn函数才知道</span></span><br><span class="line">    newg.sched.pc = funcPC(goexit) + sys.PCQuantum <span class="comment">// +PCQuantum so that previous instruction is in same function</span></span><br><span class="line">    newg.sched.g = guintptr(unsafe.Pointer(newg))</span><br><span class="line"> </span><br><span class="line">    gostartcallfn(&amp;newg.sched, fn) <span class="comment">//调整sched成员和newg的栈</span></span><br><span class="line">这段代码首先对newg的sched成员进行了初始化，该成员包含了调度器代码在调度goroutine到CPU运行时所必须的一些信息，其中sched的sp成员表示newg被调度起来运行时应该使用的栈的栈顶，sched的pc成员表示当newg被调度起来运行时从这个地址开始执行指令，然而从上面的代码可以看到，<span class="built_in">new</span>.sched.pc被设置成了goexit函数的第二条指令的地址而不是fn.fn，这是为什么呢？要回答这个问题，必须深入到gostartcallfn函数中做进一步分析。</span><br><span class="line"></span><br><span class="line"><span class="comment">// adjust Gobuf as if it executed a call to fn</span></span><br><span class="line"><span class="comment">// and then did an immediate gosave.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">gostartcallfn</span><span class="params">(gobuf *gobuf, fv *funcval)</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> fn unsafe.Pointer</span><br><span class="line">    <span class="keyword">if</span> fv != <span class="literal">nil</span> &#123;</span><br><span class="line">        fn = unsafe.Pointer(fv.fn) <span class="comment">//fn: gorotine的入口地址，初始化时对应的是runtime.main</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        fn = unsafe.Pointer(funcPC(nilfunc))</span><br><span class="line">    &#125;</span><br><span class="line">    gostartcall(gobuf, fn, unsafe.Pointer(fv))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>gostartcallfn首先从参数fv中提取出函数地址（初始化时是runtime.main），然后继续调用gostartcall函数。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// adjust Gobuf as if it executed a call to fn with context ctxt</span></span><br><span class="line"><span class="comment">// and then did an immediate gosave.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">gostartcall</span><span class="params">(buf *gobuf, fn, ctxt unsafe.Pointer)</span></span> &#123;</span><br><span class="line">    sp := buf.sp <span class="comment">//newg的栈顶，目前newg栈上只有fn函数的参数，sp指向的是fn的第一参数</span></span><br><span class="line">    <span class="keyword">if</span> sys.RegSize &gt; sys.PtrSize &#123;</span><br><span class="line">        sp -= sys.PtrSize</span><br><span class="line">        *(*<span class="keyword">uintptr</span>)(unsafe.Pointer(sp)) = <span class="number">0</span></span><br><span class="line">    &#125;</span><br><span class="line">    sp -= sys.PtrSize <span class="comment">//为返回地址预留空间，</span></span><br><span class="line">    <span class="comment">//这里在伪装fn是被goexit函数调用的，使得fn执行完后返回到goexit继续执行，从而完成清理工作</span></span><br><span class="line">    *(*<span class="keyword">uintptr</span>)(unsafe.Pointer(sp)) = buf.pc <span class="comment">//在栈上放入goexit+1的地址</span></span><br><span class="line">    buf.sp = sp <span class="comment">//重新设置newg的栈顶寄存器</span></span><br><span class="line">    <span class="comment">//这里才真正让newg的ip寄存器指向fn函数，注意，这里只是在设置newg的一些信息，newg还未执行，</span></span><br><span class="line">    <span class="comment">//等到newg被调度起来运行时，调度器会把buf.pc放入cpu的IP寄存器，</span></span><br><span class="line">    <span class="comment">//从而使newg得以在cpu上真正的运行起来</span></span><br><span class="line">    buf.pc = <span class="keyword">uintptr</span>(fn) </span><br><span class="line">    buf.ctxt = ctxt</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>gostartcall函数的主要作用有两个：</p>
<p>调整newg的栈空间，把goexit函数的第二条指令的地址入栈，伪造成goexit函数调用了fn，从而使fn执行完成后执行ret指令时返回到goexit继续执行完成最后的清理工作；</p>
<p>重新设置newg.buf.pc 为需要执行的函数的地址，即fn，我们这个场景为runtime.main函数的地址。</p>
<p>调整完成newg的栈和sched成员之后，返回到newproc1函数，我们继续往下看，<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">    newg.gopc = callerpc  <span class="comment">//主要用于traceback</span></span><br><span class="line">    newg.ancestors = saveAncestors(callergp)</span><br><span class="line">    <span class="comment">//设置newg的startpc为fn.fn，该成员主要用于函数调用栈的traceback和栈收缩</span></span><br><span class="line">    <span class="comment">//newg真正从哪里开始执行并不依赖于这个成员，而是sched.pc</span></span><br><span class="line">    newg.startpc = fn.fn  </span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//设置g的状态为_Grunnable，表示这个g代表的goroutine可以运行了</span></span><br><span class="line">    casgstatus(newg, _Gdead, _Grunnable)</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//把newg放入_p_的运行队列，初始化的时候一定是p的本地运行队列，其它时候可能因为本地队列满了而放入全局队列</span></span><br><span class="line">    runqput(_p_, newg, <span class="literal">true</span>)</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>newproc1函数最后这点代码比较直观，首先设置了几个与调度无关的成员变量，然后修改newg的状态为_Grunnable并把其放入了运行队列，到此程序中第一个真正意义上的goroutine已经创建完成。</p>
<p>这时newg也就是main goroutine的状态如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e56967d45a7cc5c2f6cd0d0d2338956e.png" alt></p>
<p>这个图看起来比较复杂，因为表示指针的箭头实在是太多了，这里对其稍作一下解释。</p>
<p>首先，main goroutine对应的newg结构体对象的sched成员已经完成了初始化，图中只显示了pc和sp成员，pc成员指向了runtime.main函数的第一条指令，sp成员指向了newg的栈顶内存单元，该内存单元保存了runtime.main函数执行完成之后的返回地址，也就是runtime.goexit函数的第二条指令，预期runtime.main函数执行完返回之后就会去执行runtime.exit函数的CALL runtime.goexit1(SB)这条指令；</p>
<p>其次，newg已经放入与当前主线程绑定的p结构体对象的本地运行队列，因为它是第一个真正意义上的goroutine，还没有其它goroutine，所以它被放在了本地运行队列的头部；</p>
<p>最后，newg的m成员为nil，因为它还没有被调度起来运行，也就没有跟任何m进行绑定。</p>
<p>这一节我们分析了程序中第一个goroutine也就是main goroutine的创建，下一节我们继续分析它是怎么被主工作线程调度到CPU上去执行的。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>Golang 中的垃圾回收（三)</title>
    <url>/go/Golang%20%E4%B8%AD%E7%9A%84%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6/Golang%20%E4%B8%AD%E7%9A%84%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6(%E4%B8%89%EF%BC%89/</url>
    <content><![CDATA[<p><a href="https://juejin.cn/post/6844903902924521485" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>通过前两节的说明，我们得出这样一个结论：如果降低堆内存的分配压力就会相应的减少延迟，从而提升程序性能。这一节来讲一下，给一种类型的工作负载，GC的pacing算法是怎么来确定最佳回收速率的。<br><strong>并发代码实例</strong><br>本节给出的代码在这里可以找到：<br><a href="https://github.com/ardanlabs/gotraining/tree/master/topics/go/profiling/trace" rel="external nofollow noopener noreferrer" target="_blank">github.com/ardanlabs/g…</a><br>程序是做了这样一件事情，给一个特定topic，要确定它在文档集中出现的频率。程序包含了不用版本的寻找算法，它们使用了不同的并发模式。这里我们只看freq，freqConcurrent和freqNumCPU这三种版本的算法。<br>首先看freq，它是非并发顺序执行的程序版本，代码如下。<br>L1<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">01</span> <span class="function"><span class="keyword">func</span> <span class="title">freq</span><span class="params">(topic <span class="keyword">string</span>, docs []<span class="keyword">string</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="number">02</span>     <span class="keyword">var</span> found <span class="keyword">int</span></span><br><span class="line"><span class="number">03</span></span><br><span class="line"><span class="number">04</span>     <span class="keyword">for</span> _, doc := <span class="keyword">range</span> docs &#123;</span><br><span class="line"><span class="number">05</span>         file := fmt.Sprintf(<span class="string">"%s.xml"</span>, doc[:<span class="number">8</span>])</span><br><span class="line"><span class="number">06</span>         f, err := os.OpenFile(file, os.O_RDONLY, <span class="number">0</span>)</span><br><span class="line"><span class="number">07</span>         <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="number">08</span>             log.Printf(<span class="string">"Opening Document [%s] : ERROR : %v"</span>, doc, err)</span><br><span class="line"><span class="number">09</span>             <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line"><span class="number">10</span>         &#125;</span><br><span class="line"><span class="number">11</span>         <span class="keyword">defer</span> f.Close()</span><br><span class="line"><span class="number">12</span></span><br><span class="line"><span class="number">13</span>         data, err := ioutil.ReadAll(f)</span><br><span class="line"><span class="number">14</span>         <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="number">15</span>             log.Printf(<span class="string">"Reading Document [%s] : ERROR : %v"</span>, doc, err)</span><br><span class="line"><span class="number">16</span>             <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line"><span class="number">17</span>         &#125;</span><br><span class="line"><span class="number">18</span></span><br><span class="line"><span class="number">19</span>         <span class="keyword">var</span> d document</span><br><span class="line"><span class="number">20</span>         <span class="keyword">if</span> err := xml.Unmarshal(data, &amp;d); err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="number">21</span>             log.Printf(<span class="string">"Decoding Document [%s] : ERROR : %v"</span>, doc, err)</span><br><span class="line"><span class="number">22</span>             <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line"><span class="number">23</span>         &#125;</span><br><span class="line"><span class="number">24</span></span><br><span class="line"><span class="number">25</span>         <span class="keyword">for</span> _, item := <span class="keyword">range</span> d.Channel.Items &#123;</span><br><span class="line"><span class="number">26</span>             <span class="keyword">if</span> strings.Contains(item.Title, topic) &#123;</span><br><span class="line"><span class="number">27</span>                 found++</span><br><span class="line"><span class="number">28</span>                 <span class="keyword">continue</span></span><br><span class="line"><span class="number">29</span>             &#125;</span><br><span class="line"><span class="number">30</span></span><br><span class="line"><span class="number">31</span>             <span class="keyword">if</span> strings.Contains(item.Description, topic) &#123;</span><br><span class="line"><span class="number">32</span>                 found++</span><br><span class="line"><span class="number">33</span>             &#125;</span><br><span class="line"><span class="number">34</span>        &#125;</span><br><span class="line"><span class="number">35</span>     &#125;</span><br><span class="line"><span class="number">36</span></span><br><span class="line"><span class="number">37</span>     <span class="keyword">return</span> found</span><br><span class="line"><span class="number">38</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>非并发版本代码会去遍历文件集合，并执行以下4中操作：打开，读文件，解码和search，每次只处理一个文件。<br>运行freq，得到如下信息。<br><strong>L2</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ time ./trace</span><br><span class="line">2019/07/02 13:40:49 Searching 4000 files, found president 28000 <span class="built_in">times</span>.</span><br><span class="line">./trace  2.54s user 0.12s system 105% cpu 2.512 total</span><br></pre></td></tr></table></figure></p>
<p>复制代码可以看到程序处理4000个文件花费了大约2.5s的时间。如果能够看到gc的实际情况就更好了，你可以使用trace包来生成trace信息。<br>L3<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">03 import <span class="string">"runtime/trace"</span></span><br><span class="line">04</span><br><span class="line">05 func <span class="function"><span class="title">main</span></span>() &#123;</span><br><span class="line">06     trace.Start(os.Stdout)</span><br><span class="line">07     defer trace.Stop()</span><br></pre></td></tr></table></figure></p>
<p>L3中引入了runtime/trace包。<br>重新编译运行代码，不要忘记把标准输出重定向到文件里。<br><strong>L4</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build</span><br><span class="line">$ time ./trace &gt; t.out</span><br><span class="line">Searching 4000 files, found president 28000 <span class="built_in">times</span>.</span><br><span class="line">./trace &gt; t.out  2.67s user 0.13s system 106% cpu 2.626 total</span><br></pre></td></tr></table></figure></p>
<p>复制代码正如我们预料的，运行时间增加了大概100ms。trace获取了每一次的方法调用的执行时间，达到微秒的级别。t.out包含了trace的数据。<br>我们可以使用下面的命令来查看trace信息。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go tool trace t.out</span><br></pre></td></tr></table></figure></p>
<p>运行命令后会自动开一个浏览器窗口，如下图所示<br>图1.1<br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42db227eca9ef?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.1给了9个链接，现在我们只关心View trace。点击链接，看到如下图示：<br>图1.2<br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42dc577dd6c8f?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2是程序运行的trace图。这部分我会主要关注垃圾回收器相关的东西，图中的Heap和GC部分。<br>图1.3<br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42dc7f97319d8?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.3更近一些去看前200ms的trace信息。注意Heap信息(绿色和橘黄色区域)，以及GC（底下蓝色的竖线）。Heap信息告诉了你两件事情。橘色区域是当前时间正在使用的堆空间大小。绿色部分代表,触发下次回收，in-used堆内存空间。也就是说，每次橘色区域达到顶峰的时候，便开始进行垃圾回收。蓝色竖线代表了垃圾回收。<br>这个版本的程序运行中，堆中in-use的内存保持在大约4MB。要看一次单独垃圾回收的统计信息，可以使用选择工具框住蓝线。<br>图1.4<br>!()[<a href="https://user-gold-cdn.xitu.io/2019/7/30/16c42dd4681fa5d2?imageView2/0/w/1280/h/960/format/webp/ignore-error/1]" rel="external nofollow noopener noreferrer" target="_blank">https://user-gold-cdn.xitu.io/2019/7/30/16c42dd4681fa5d2?imageView2/0/w/1280/h/960/format/webp/ignore-error/1]</a><br>图1.4格里的数字，代表了选中区域的时间量。图中大概是316ms，当选中了所有蓝线，会出现下面统计信息<br>图1.5<br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42dd7b4bc96d5?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图5中代表了图表中所有蓝线都在标记点15.911ms到2.596s之间。一共发生了232次回收，占用了64.524ms，平均每次回收时间是287.121微秒。了解到程序运行时间是2.626s，也就是说，垃圾回收占用了总时间的2%，基本上垃圾回收的代价对程序运行来说是微不足道的。<br>下面来看一下使用算法的并发版，我们希望可以加速程序运行。<br><strong>L6</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">01 func freqConcurrent(topic string, docs []string) int &#123;</span><br><span class="line">02     var found int32</span><br><span class="line">03</span><br><span class="line">04     g := len(docs)</span><br><span class="line">05     var wg sync.WaitGroup</span><br><span class="line">06     wg.Add(g)</span><br><span class="line">07</span><br><span class="line">08     <span class="keyword">for</span> _, doc := range docs &#123;</span><br><span class="line">09         go func(doc string) &#123;</span><br><span class="line">10             var lFound int32</span><br><span class="line">11             defer <span class="function"><span class="title">func</span></span>() &#123;</span><br><span class="line">12                 atomic.AddInt32(&amp;found, lFound)</span><br><span class="line">13                 wg.Done()</span><br><span class="line">14             &#125;()</span><br><span class="line">15</span><br><span class="line">16             file := fmt.Sprintf(<span class="string">"%s.xml"</span>, doc[:8])</span><br><span class="line">17             f, err := os.OpenFile(file, os.O_RDONLY, 0)</span><br><span class="line">18             <span class="keyword">if</span> err != nil &#123;</span><br><span class="line">19                 log.Printf(<span class="string">"Opening Document [%s] : ERROR : %v"</span>, doc, err)</span><br><span class="line">20                 <span class="built_in">return</span></span><br><span class="line">21             &#125;</span><br><span class="line">22             defer f.Close()</span><br><span class="line">23</span><br><span class="line">24             data, err := ioutil.ReadAll(f)</span><br><span class="line">25             <span class="keyword">if</span> err != nil &#123;</span><br><span class="line">26                 log.Printf(<span class="string">"Reading Document [%s] : ERROR : %v"</span>, doc, err)</span><br><span class="line">27                 <span class="built_in">return</span></span><br><span class="line">28             &#125;</span><br><span class="line">29</span><br><span class="line">30             var d document</span><br><span class="line">31             <span class="keyword">if</span> err := xml.Unmarshal(data, &amp;d); err != nil &#123;</span><br><span class="line">32                 log.Printf(<span class="string">"Decoding Document [%s] : ERROR : %v"</span>, doc, err)</span><br><span class="line">33                 <span class="built_in">return</span></span><br><span class="line">34             &#125;</span><br><span class="line">35</span><br><span class="line">36             <span class="keyword">for</span> _, item := range d.Channel.Items &#123;</span><br><span class="line">37                 <span class="keyword">if</span> strings.Contains(item.Title, topic) &#123;</span><br><span class="line">38                     lFound++</span><br><span class="line">39                     <span class="built_in">continue</span></span><br><span class="line">40                 &#125;</span><br><span class="line">41</span><br><span class="line">42                 <span class="keyword">if</span> strings.Contains(item.Description, topic) &#123;</span><br><span class="line">43                     lFound++</span><br><span class="line">44                 &#125;</span><br><span class="line">45             &#125;</span><br><span class="line">46         &#125;(doc)</span><br><span class="line">47     &#125;</span><br><span class="line">48</span><br><span class="line">49     wg.Wait()</span><br><span class="line">50     <span class="built_in">return</span> int(found)</span><br><span class="line">51 &#125;</span><br></pre></td></tr></table></figure></p>
<p>L6给出了freq算法的一个并发版。这个程序版本使用了扇出模式。每个一个docs列出的文件，都去创建一个goroutine去出里。如果有4000个文档，那么就要去创建4000个goroutine。这个版本的好处是，最简单的方式使用了并发。每个goroutine处理一个文件。我们可以使用WaitGroup和原子操作来保持计数器的同步处理。<br>在程序运行早期，所有goroutines都会分配时间去运行，也就是说程序会快速消耗大量的内存。在12行found增加这里，也会产生cache一致性的问题。由于每个虚拟内核共享这个变量的相同cache行，这会导致程序内存抖动，如果文件数或者内核数增加话，这种问题会更加严重。<br>我们重新编译代码然后再次运行<br><strong>L7</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build</span><br><span class="line">$ time ./trace &gt; t.out</span><br><span class="line">Searching 4000 files, found president 28000 <span class="built_in">times</span>.</span><br><span class="line">./trace &gt; t.out  6.49s user 2.46s system 941% cpu 0.951 total</span><br></pre></td></tr></table></figure></p>
<p>L7中可以看到，程序现在处理4000个文件的时间是951ms，大概是64%的性能提升，来看一下trace信息。<br>图1.6<br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42ddad4b371ac?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>程序开始的时候，图中会有很多密集的线。这是因为所有goroutines都被创建了，他们运行并且尝试进行堆内存分配。只要第一个4MB内存分配了之后，就会开始GC。在GC期间，每个Goroutine都会去运行，并且当它们在堆上请求内存时，大多数都会进入等待状态。GC结束时，至少有9个goroutines还在继续运行，并且堆内存涨到了大约26MB。<br><strong>图1.7</strong><br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42ddd30ae3c07?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.7中你可以看到，第一次GC的时候，有一大堆处于Running或者是Runnable状态的goroutines。第一次GC结束之后，后面的GC时间就很快了，而且GC不再像上一个版本代码那样有规律。<br>如果你选择了图表上所有的回收，你会得到如下统计信息<br>图1.8<br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42ddf470af79f?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.8给出了，从4.828ms到906.939ms内。一共有23次垃圾回收，占用时间284.447ms，平均收集时间为12.367ms。程序运行时间是951ms，这意味着垃圾回收占用了总时间的34%。<br>可以对比出程序性能和GC时间的不同。运行更多goroutines确实让程序运行速度提升了64%。但是代价就是需要更多的机器资源，如果在同一时间，一下子达到了200MB in-use堆内存这个峰值的话就会很糟糕。<br>在并发版本的基础上，下面的并发版本，我们来更有效率更合理的来利用资源。<br><strong>L8</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">01</span> <span class="function"><span class="keyword">func</span> <span class="title">freqNumCPU</span><span class="params">(topic <span class="keyword">string</span>, docs []<span class="keyword">string</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="number">02</span>     <span class="keyword">var</span> found <span class="keyword">int32</span></span><br><span class="line"><span class="number">03</span></span><br><span class="line"><span class="number">04</span>     g := runtime.NumCPU()</span><br><span class="line"><span class="number">05</span>     <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"><span class="number">06</span>     wg.Add(g)</span><br><span class="line"><span class="number">07</span></span><br><span class="line"><span class="number">08</span>     ch := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">string</span>, g)</span><br><span class="line"><span class="number">09</span></span><br><span class="line"><span class="number">10</span>     <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; g; i++ &#123;</span><br><span class="line"><span class="number">11</span>         <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="number">12</span>             <span class="keyword">var</span> lFound <span class="keyword">int32</span></span><br><span class="line"><span class="number">13</span>             <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="number">14</span>                 atomic.AddInt32(&amp;found, lFound)</span><br><span class="line"><span class="number">15</span>                 wg.Done()</span><br><span class="line"><span class="number">16</span>             &#125;()</span><br><span class="line"><span class="number">17</span></span><br><span class="line"><span class="number">18</span>             <span class="keyword">for</span> doc := <span class="keyword">range</span> ch &#123;</span><br><span class="line"><span class="number">19</span>                 file := fmt.Sprintf(<span class="string">"%s.xml"</span>, doc[:<span class="number">8</span>])</span><br><span class="line"><span class="number">20</span>                 f, err := os.OpenFile(file, os.O_RDONLY, <span class="number">0</span>)</span><br><span class="line"><span class="number">21</span>                 <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="number">22</span>                     log.Printf(<span class="string">"Opening Document [%s] : ERROR : %v"</span>, doc, err)</span><br><span class="line"><span class="number">23</span>                     <span class="keyword">return</span></span><br><span class="line"><span class="number">24</span>                 &#125;</span><br><span class="line"><span class="number">25</span></span><br><span class="line"><span class="number">26</span>                 data, err := ioutil.ReadAll(f)</span><br><span class="line"><span class="number">27</span>                 <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="number">28</span>                     f.Close()</span><br><span class="line"><span class="number">29</span>                     log.Printf(<span class="string">"Reading Document [%s] : ERROR : %v"</span>, doc, err)</span><br><span class="line"><span class="number">23</span>                     <span class="keyword">return</span></span><br><span class="line"><span class="number">24</span>                 &#125;</span><br><span class="line"><span class="number">25</span>                 f.Close()</span><br><span class="line"><span class="number">26</span></span><br><span class="line"><span class="number">27</span>                 <span class="keyword">var</span> d document</span><br><span class="line"><span class="number">28</span>                 <span class="keyword">if</span> err := xml.Unmarshal(data, &amp;d); err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="number">29</span>                     log.Printf(<span class="string">"Decoding Document [%s] : ERROR : %v"</span>, doc, err)</span><br><span class="line"><span class="number">30</span>                     <span class="keyword">return</span></span><br><span class="line"><span class="number">31</span>                 &#125;</span><br><span class="line"><span class="number">32</span></span><br><span class="line"><span class="number">33</span>                 <span class="keyword">for</span> _, item := <span class="keyword">range</span> d.Channel.Items &#123;</span><br><span class="line"><span class="number">34</span>                     <span class="keyword">if</span> strings.Contains(item.Title, topic) &#123;</span><br><span class="line"><span class="number">35</span>                         lFound++</span><br><span class="line"><span class="number">36</span>                         <span class="keyword">continue</span></span><br><span class="line"><span class="number">37</span>                     &#125;</span><br><span class="line"><span class="number">38</span></span><br><span class="line"><span class="number">39</span>                     <span class="keyword">if</span> strings.Contains(item.Description, topic) &#123;</span><br><span class="line"><span class="number">40</span>                         lFound++</span><br><span class="line"><span class="number">41</span>                     &#125;</span><br><span class="line"><span class="number">42</span>                 &#125;</span><br><span class="line"><span class="number">43</span>             &#125;</span><br><span class="line"><span class="number">44</span>         &#125;()</span><br><span class="line"><span class="number">45</span>     &#125;</span><br><span class="line"><span class="number">46</span></span><br><span class="line"><span class="number">47</span>     <span class="keyword">for</span> _, doc := <span class="keyword">range</span> docs &#123;</span><br><span class="line"><span class="number">48</span>         ch &lt;- doc</span><br><span class="line"><span class="number">49</span>     &#125;</span><br><span class="line"><span class="number">50</span>     <span class="built_in">close</span>(ch)</span><br><span class="line"><span class="number">51</span></span><br><span class="line"><span class="number">52</span>     wg.Wait()</span><br><span class="line"><span class="number">53</span>     <span class="keyword">return</span> <span class="keyword">int</span>(found)</span><br><span class="line"><span class="number">54</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>L8中给出了freqNumCPU版本的代码。这个设计模式的核心就是使用了池化模式。我们使用基于逻辑处理器数的goroutines池子，去处理所有的文件。如果是12个逻辑处理器，那么就是12个goroutines。这种方式的好处是，它会从开始到结束持续地让资源被使用。在任何时间，只有12个goroutines去申请内存。这也解决了由于cache一致性导致的内存抖动。<br>重新编译程序，再次运行。<br><strong>L9</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build</span><br><span class="line">$ time ./trace &gt; t.out</span><br><span class="line">Searching 4000 files, found president 28000 <span class="built_in">times</span>.</span><br><span class="line">./trace &gt; t.out  6.22s user 0.64s system 909% cpu 0.754 total</span><br></pre></td></tr></table></figure></p>
<p>可以看到L9中，同样4000个文件，花费了754ms。相比上个程序块了200ms，下面看下trace<br><strong>图1.9</strong><br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42de26a1f57e0?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.9里，可以看到全部CPU容量都被使用，仔细观察可以看到，这个版本代码和顺序版本类似，GC比较有规律。<br>图1.10<br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42de4ef405a8a?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图10给出了前20ms的程序trace图。12个goroutines运行，垃圾回收相对顺序版本时间更长一些。内存使用量维持在4MB之内。<br>框住所有垃圾回收，可以看到下面信息。<br>图1.11<br><img src="https://user-gold-cdn.xitu.io/2019/7/30/16c42de7f4a4a965?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.11，给出了3.055ms到719.928ms之间。产生了467次垃圾回收，占用177.709ms，平均回收时间380.535微秒。程序运行时间754ms，也就是说垃圾回收占了25%的全部时间。相比上一个版本有9%的提升。<br>如果文件数或者是虚拟核数更多的话，池化版本似乎会有更好的扩展，代码复杂性的代价是值得的。<br><strong>结论</strong><br>我们主要关注的就是不同版本代码的GC情况。总的内存分配其实每个版本都是一样的，不同只是如何去分配。<br>当只有一个goroutine，4MB的内存基本够用了。当程序一次性把所有work都扔过来，GC会让触发GC的in-used堆内存增加，减少GC次数但同时增加了GC时间。当程序控制每次并发处理file的数量的时候，GC会采取使用保持小堆的方式，增加了GC次数但是减少了每次GC时间。每种方式GC都会尽力让其对程序产生的影响最小。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">| Algorithm  | Program | GC Time  | % Of GC | <span class="comment"># of GC’s | Avg GC   | Max Heap |</span></span><br><span class="line">|------------|---------|----------|---------|-----------|----------|----------|</span><br><span class="line">| freq       | 2626 ms |  64.5 ms |     ~2% |       232 |   278 μs |    4 meg |</span><br><span class="line">| concurrent |  951 ms | 284.4 ms |    ~34% |        23 |  12.3 ms |  200 meg |</span><br><span class="line">| numCPU     |  754 ms | 177.7 ms |    ~25% |       467 | 380.5 μs |    4 meg |</span><br></pre></td></tr></table></figure></p>
<p>复制代码freqNumCPU 版本会更好的处理cache一致性上的问题，这很有用。<br>综上，程序运行时候我们还是可以把一堆work扔过去处理。例如一个web服务使用50k的goroutines，本质上就是类似于第一种并发算法的扇出模式。GC会考虑workload然后找到最优的步调去进行。但是一些情况下也需要考虑相应的代价。<br>原文链接：<a href="https://www.ardanlabs.com/blog/2019/07/garbage-collection-in-go-part3-gcpacing.html" rel="external nofollow noopener noreferrer" target="_blank">www.ardanlabs.com/blog/2019/0…</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go内存详解</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go内存详解</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析之十：线程本地存储</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/Go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90%E4%B9%8B%E5%8D%81%EF%BC%9A%E7%BA%BF%E7%A8%8B%E6%9C%AC%E5%9C%B0%E5%AD%98%E5%82%A8/</url>
    <content><![CDATA[<p>原创 <strong>爱写程序的阿波张</strong> <a href="https://mp.weixin.qq.com/s/-tiXJpH0IrJw-RH4x5SRdQ" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-04-27</p>
<p><strong>本文是《go调度器源代码情景分析》系列 第一章 预备知识的第十小节，也是预备知识的最后一小节。</strong></p>
<p>线程本地存储又叫线程局部存储，其英文为Thread Local Storage，简称TLS，看似一个很高大上的东西，其实就是线程私有的全局变量而已。</p>
<p>有过多线程编程的读者一定知道，普通的全局变量在多线程中是共享的，一个线程对其进行了修改，所有线程都可以看到这个修改，而线程私有的全局变量与普通全局变量不同，线程私有全局变量是线程的私有财产，每个线程都有自己的一份副本，某个线程对其所做的修改只会修改到自己的副本，并不会修改到其它线程的副本。</p>
<p>下面用例子来说明一下多线程共享全局变量以及线程私有全局变量之间的差异，并对gcc的线程本地存储做一个简单的分析。</p>
<p>首先来看普通的全局变量<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> g = <span class="number">0</span>;  <span class="comment">// 1，定义全局变量g并赋初值0</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">start</span><span class="params">(<span class="keyword">void</span> *arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"start, g[%p] : %d\n"</span>, &amp;g, g); <span class="comment">// 4，子线程中打印全局变量g的地址和值</span></span><br><span class="line"></span><br><span class="line">g++; <span class="comment">// 5，修改全局变量</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">pthread_t</span> tid;</span><br><span class="line"></span><br><span class="line">g = <span class="number">100</span>;  <span class="comment">// 2，主线程给全局变量g赋值为100</span></span><br><span class="line"></span><br><span class="line">pthread_create(&amp;tid, <span class="literal">NULL</span>, start, <span class="literal">NULL</span>); <span class="comment">// 3， 创建子线程执行start()函数</span></span><br><span class="line">pthread_join(tid, <span class="literal">NULL</span>); <span class="comment">// 6，等待子线程运行结束</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"main, g[%p] : %d\n"</span>, &amp;g, g); <span class="comment">// 7，打印全局变量g的地址和值</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>简单解释一下，这个程序在注释1的地方定义了一个全局变量g并设置其初值为0，程序运行后主线程首先把g修改成了100（注释2），然后创建了一个子线程执行start()函数（注释3），start()函数先打印出g的值（注释4）确定在子线程中可以看到主线程对g的修改，然后修改g的值（注释5）后线程结束运行，主线程在注释6处等待子线程结束后，在注释7处打印g的值确定子线程对g的修改同样可以影响到主线程对g的读取。</p>
<p>编译并运行程序：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bobo@ubuntu:~/study/c$ gcc thread.c -o thread -lpthread</span><br><span class="line">bobo@ubuntu:~/study/c$ ./thread</span><br><span class="line">start, g[0x601064] : 100</span><br><span class="line">main, g[0x601064] : 101</span><br></pre></td></tr></table></figure></p>
<p>从输出结果可以看出，全局变量g在两个线程中的地址都是一样的，任何一个线程都可以读取到另一个线程对全局变量g的修改，这实现了全局变量g的多个线程中的共享。</p>
<p>了解了普通的全局变量之后我们再来看通过线程本地存储(TLS)实现的线程私有全局变量。这个程序与上面的程序几乎完全一样，唯一的差别就是在定义全局变量 g 时增加了 __thread 关键字，这样g就变成了线程私有全局变量了。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">__thread <span class="keyword">int</span> g = <span class="number">0</span>;  <span class="comment">// 1，这里增加了__thread关键字，把g定义成私有的全局变量，每个线程都有一个g变量</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">start</span><span class="params">(<span class="keyword">void</span> *arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"start, g[%p] : %d\n"</span>, &amp;g, g); <span class="comment">// 4，打印本线程私有全局变量g的地址和值</span></span><br><span class="line"></span><br><span class="line">g++; <span class="comment">// 5，修改本线程私有全局变量g的值</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">pthread_t</span> tid;</span><br><span class="line"></span><br><span class="line">g = <span class="number">100</span>;  <span class="comment">// 2，主线程给私有全局变量赋值为100</span></span><br><span class="line"></span><br><span class="line">pthread_create(&amp;tid, <span class="literal">NULL</span>, start, <span class="literal">NULL</span>); <span class="comment">// 3，创建子线程执行start()函数</span></span><br><span class="line">pthread_join(tid, <span class="literal">NULL</span>);  <span class="comment">// 6，等待子线程运行结束</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"main, g[%p] : %d\n"</span>, &amp;g, g); <span class="comment">// 7，打印主线程的私有全局变量g的地址和值</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>运行程序看一下效果：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bobo@ubuntu:~/study/c$ gcc -g thread.c -o thread -lpthread</span><br><span class="line">bobo@ubuntu:~/study/c$ ./thread</span><br><span class="line">start, g[0x7f0181b046fc] : 0</span><br><span class="line">main, g[0x7f01823076fc] : 100</span><br></pre></td></tr></table></figure></p>
<p>从输出结果可以看出：首先，全局变量g在两个线程中的地址是不一样的；其次main函数对全局变量 g 赋的值并未影响到子线程中 g 的值，而子线程对g都做了修改，同样也没有影响到主线程中 g 的值，这个结果正是我们所期望的，这说明，每个线程都有一个自己私有的全局变量g。</p>
<p>这看起来很神奇，明明2个线程都是用的同一个全局变量名来访问变量但却像在访问不同的变量一样。</p>
<p>下面我们就来分析一下gcc到底使用了什么黑魔法实现了这个特性。对于像这种由编译器实现的特性，我们怎么开始研究呢？最快最直接的方法就是使用调试工具来调试程序的运行，这里我们使用gdb来调试。</p>
<p>bobo@ubuntu:~/study/c$ gdb ./thread<br>首先在源代码的第20行（对应到源代码中的 g = 100）处下一个断点，然后运行程序，程序停在了断点处，反汇编一下main函数：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(gdb) b thread.c:20</span><br><span class="line">Breakpoint 1 at 0x400793: file thread.c, line 20.</span><br><span class="line">(gdb) r</span><br><span class="line">Starting program: /home/bobo/study/c/thread</span><br><span class="line"></span><br><span class="line">Breakpoint 1, at thread.c:20</span><br><span class="line">20g = 100;</span><br><span class="line">(gdb) disass</span><br><span class="line">Dump of assembler code <span class="keyword">for</span> <span class="keyword">function</span> main:</span><br><span class="line">  0x0000000000400775 &lt;+0&gt;:push   %rbp</span><br><span class="line">  0x0000000000400776 &lt;+1&gt;:mov   %rsp,%rbp</span><br><span class="line">  0x0000000000400779 &lt;+4&gt;:sub   <span class="variable">$0x20</span>,%rsp</span><br><span class="line">  0x000000000040077d &lt;+8&gt;:mov   %edi,-0x14(%rbp)</span><br><span class="line">  0x0000000000400780 &lt;+11&gt;:mov   %rsi,-0x20(%rbp)</span><br><span class="line">  0x0000000000400784 &lt;+15&gt;:mov   %fs:0x28,%rax</span><br><span class="line">  0x000000000040078d &lt;+24&gt;:mov   %rax,-0x8(%rbp)</span><br><span class="line">  0x0000000000400791 &lt;+28&gt;:xor   %eax,%eax</span><br><span class="line">=&gt; 0x0000000000400793 &lt;+30&gt;:movl   <span class="variable">$0x64</span>,%fs:0xfffffffffffffffc</span><br><span class="line">  0x000000000040079f &lt;+42&gt;:lea   -0x10(%rbp),%rax</span><br><span class="line">  0x00000000004007a3 &lt;+46&gt;:mov   <span class="variable">$0x0</span>,%ecx</span><br><span class="line">  0x00000000004007a8 &lt;+51&gt;:mov   <span class="variable">$0x400736</span>,%edx</span><br><span class="line">  0x00000000004007ad &lt;+56&gt;:mov   <span class="variable">$0x0</span>,%esi</span><br><span class="line">  0x00000000004007b2 &lt;+61&gt;:mov   %rax,%rdi</span><br><span class="line">  0x00000000004007b5 &lt;+64&gt;:callq 0x4005e0 &lt;pthread_create@plt&gt;</span><br><span class="line">  0x00000000004007ba &lt;+69&gt;:mov   -0x10(%rbp),%rax</span><br><span class="line">  0x00000000004007be &lt;+73&gt;:mov   <span class="variable">$0x0</span>,%esi</span><br><span class="line">  0x00000000004007c3 &lt;+78&gt;:mov   %rax,%rdi</span><br><span class="line">  0x00000000004007c6 &lt;+81&gt;:callq 0x400620 &lt;pthread_join@plt&gt;</span><br><span class="line">  0x00000000004007cb &lt;+86&gt;:mov   %fs:0xfffffffffffffffc,%eax</span><br><span class="line">  0x00000000004007d3 &lt;+94&gt;:mov   %eax,%esi</span><br><span class="line">  0x00000000004007d5 &lt;+96&gt;:mov   <span class="variable">$0x4008df</span>,%edi</span><br><span class="line">  0x00000000004007da &lt;+101&gt;:mov   <span class="variable">$0x0</span>,%eax</span><br><span class="line">  0x00000000004007df &lt;+106&gt;:callq 0x400600 &lt;<span class="built_in">printf</span>@plt&gt;</span><br><span class="line">  ......</span><br></pre></td></tr></table></figure></p>
<p>程序停在了g = 100这一行，看一下汇编指令，</p>
<p>=&gt; 0x0000000000400793 &lt;+30&gt;:movl   $0x64,%fs:0xfffffffffffffffc<br>这句汇编指令的意思是把常量100(0x64)复制到地址为%fs:0xfffffffffffffffc的内存中，可以看出全局变量g的地址为%fs:0xfffffffffffffffc，fs是段寄存器，0xfffffffffffffffc是有符号数-4，所以全局变量g的地址为：</p>
<p>fs段基址 - 4<br>前面我们在讲段寄存器时说过段基址就是段的起始地址，为了验证g的地址确实是fs段基址 - 4，我们需要知道fs段基址是多少，虽然我们可以用gdb命令查看fs寄存器的值，但fs寄存器里面存放的是段选择子（segment selector）而不是该段的起始地址，为了拿到这个基地址，我们需要加一点代码来获取它，修改后的代码如下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;asm/prctl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/prctl.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">__thread <span class="keyword">int</span> g = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">print_fs_base</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">       <span class="keyword">unsigned</span> <span class="keyword">long</span> addr;</span><br><span class="line">       <span class="keyword">int</span> ret = arch_prctl(ARCH_GET_FS, &amp;addr);  <span class="comment">//获取fs段基地址</span></span><br><span class="line">       <span class="keyword">if</span> (ret &lt; <span class="number">0</span>) &#123;</span><br><span class="line">               perror(<span class="string">"error"</span>);</span><br><span class="line">               <span class="keyword">return</span>;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">       <span class="built_in">printf</span>(<span class="string">"fs base addr: %p\n"</span>, (<span class="keyword">void</span> *)addr); <span class="comment">//打印fs段基址</span></span><br><span class="line"></span><br><span class="line">       <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">start</span><span class="params">(<span class="keyword">void</span> *arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   print_fs_base(); <span class="comment">//子线程打印fs段基地址</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"start, g[%p] : %d\n"</span>, &amp;g, g);</span><br><span class="line"></span><br><span class="line">g++;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">pthread_t</span> tid;</span><br><span class="line"></span><br><span class="line">g = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">pthread_create(&amp;tid, <span class="literal">NULL</span>, start, <span class="literal">NULL</span>);</span><br><span class="line">pthread_join(tid, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">   print_fs_base(); <span class="comment">//main线程打印fs段基址</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"main, g[%p] : %d\n"</span>, &amp;g, g);</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>代码中主线程和子线程都分别调用了print_fs_base()函数用于打印fs段基地址，运行程序看一下：</p>
<p>fs base addr: 0x7f36757c8700<br>start, g[0x7f36757c86fc] : 0<br>fs base addr: 0x7f3675fcb700<br>main, g[0x7f3675fcb6fc] : 100<br>可以看到：</p>
<p>子线程fs段基地址为0x7f36757c8700，g的地址为0x7f36757c86fc，它正好是基地址-4</p>
<p>主线程fs段基地址为0x7f3675fcb700，g的地址为0x7f3675fcb6fc，它也是基地址-4</p>
<p>由此可以得出，gcc编译器（其实还有线程库以及内核的支持）使用了CPU的fs段寄存器来实现线程本地存储，不同的线程中fs段基地址是不一样的，这样看似同一个全局变量但在不同线程中却拥有不同的内存地址，实现了线程私有的全局变量。</p>
<p>这里我们简要的分析了AMD64 Linux平台下gcc对线程本地存储的实现，后面的章节我们还会看到go的runtime是如何利用线程本地存储来把正在运行的goroutine和工作线程关联在一起的。</p>
<p>到此，第一部分预备知识的主要内容就已经全部介绍完毕。我们从汇编指令开始，一起讨论了寄存器，内存，栈，函数调用过程，操作系统内核对线程的调度以及线程本地存储等内容，相信读者已经很好的掌握了这些基础知识，接下来就让我们一起来撩开goroutine调度器的神秘面纱吧！</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析之五：汇编指令</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90%E4%B9%8B%E4%BA%94%EF%BC%9A%E6%B1%87%E7%BC%96%E6%8C%87%E4%BB%A4/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/fuox6st_iXg_rpklxWXXRA" title="源码游记" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-04-20</p>
<p>汇编语言是每位后端程序员都应该掌握的一门语言，因为学会了汇编语言，不管是对我们调试程序还是研究与理解计算机底层的一些运行原理都具有非常重要的作用，所以建议有兴趣的读者可以多花点时间把它学好。</p>
<p>与高级编程语言一样，汇编语言也是一门完整的计算机编程语言，它所涉及的知识内容也很多，好在我们的主要目标是通过对本小节的学习而有能力去读懂汇编代码，而不是要用汇编语言去写代码，所以本节并不会全面介绍汇编语言，而只会选取汇编语言的一个子集–汇编指令出来做介绍。不过，虽然这里的介绍做了精简，但读者大可放心，熟练运用这些知识就足以应付本书将要分析的goroutine调度器中的汇编代码了。</p>
<p>说到汇编指令，不得不提一下机器指令，二进制格式的机器指令才是CPU能够理解的语言，因为它是二进制格式的，非常便于CPU的解析和执行，但并不利于人类阅读和交流，所以才有了跟机器指令一一对应的汇编指令，汇编指令使用符号来表示机器指令，下面的例子非常直观的说明了这两种指令之间的差异：</p>
<p>0x40054d : add   %rdx,%rax // 汇编指令</p>
<p>(gdb) x/3xb 0x40054d<br>0x40054d : 0x48    0x01    0xd0 // 机器指令<br>(gdb)<br>同样是把rdx和rax寄存器中的值相加，汇编指令为：add    %rdx,%rax，而机器指令却是三个数字：0x48 0x01 0xd0，显然，汇编指令对人类来说更加友好，它更加易记易读和易写。</p>
<p>汇编指令格式</p>
<p>因为不同的CPU所支持的机器指令不一样，所以其汇编指令也不同，即使是相同的CPU，不同的汇编工具和平台所使用的汇编指令格式也有些差别，由于本书主要专注于AMD64 Linux平台下的go调度器，因此下面我们只介绍该平台下所使用的AT&amp;T格式的汇编指令，AT&amp;T汇编指令的基本格式为：</p>
<p>操作码  [操作数]<br>可以看到每一条汇编指令通常都由两部分组成：</p>
<p>操作码：操作码指示CPU执行什么操作，比如是执行加法，减法还是读写内存。每条指令都必须要有操作码。</p>
<p>操作数：操作数是操作的对象，比如加法操作需要两个加数，这两个加数就是这条指令的操作数。操作数的个数一般是0个，1个或2个。</p>
<p>来看几个汇编指令的例子</p>
<p>add   %rdx,%rax<br>这条指令的操作码是add，表示执行加法操作，它有两个操作数，rdx和rax。如果一条指令有两个操作数，那么第一个操作数叫做源操作数，第二个操作数叫做目的操作数，顾名思义，目的操作数表示这条指令执行完后结果应该保存的地方。所以上面这条指令表示对rax和rdx寄存器里面的值求和，并把结果保存在rax寄存器中。其实这条指令的第二个操作数rax寄存器既是源操作数也是目的操作数，因为rax既是加法操作的两个加数之一，又得存放加法操作的结果。这条指令执行完后rax寄存器的值发生了改变，指令执行前的值被覆盖而丢失了，如果rax寄存器之前的值还有用，那么就得先用指令把它保存到其它寄存器或内存之中。</p>
<p>再来看一个只有一个操作数的例子：</p>
<p>callq  0x400526<br>这条指令的操作码是callq，表示调用函数，操作数是0x400526，它是被调用函数的地址。</p>
<p>最后来看一条没有操作数的指令：</p>
<p>retq<br>这条指令只有操作码retq，表示从被调用函数返回到调用函数继续执行。</p>
<p>为了更好的理解AT&amp;T格式的汇编指令，这里先对其格式做一个简要的说明：</p>
<ol>
<li><p>AT&amp;T格式的汇编指令中，寄存器名需要加%作为前缀，前面我们已经见过；</p>
</li>
<li><p>有2个操作数的指令中，第一个操作数是源操作数，第二个是目的操作数，刚才也讨论过，不过那条指令中的源和目的不是那么清晰，来看一个直白的，mov    %eax,%esi，这条指令表示把eax寄存器中的值拷贝给esi，这条指令中源和目的就很清楚了；</p>
</li>
<li><p>立即操作数需要加上$符号做前缀，如  “mov $0x1 %rdi” 这条指令中第一个操作数不是寄存器，也不是内存地址，而是直接写在指令中的一个常数，这种操作数叫做立即操作数。这条指令表示把数值0x1放入rdi寄存器中。</p>
</li>
<li><p>寄存器间接寻址的格式为  offset(%register)，如果offset为0，则可以略去偏移不写直接写成(%register)。何为间接寻址呢？其实就是指指令中的寄存器并不是真正的源操作数或目的操作数，寄存器的值是一个内存地址，这个地址对应的内存才是真正的源或目的操作数，比如 mov    %rax, (%rsp)这条指令，第二个操作数(%rsp)中的寄存器的名字用括号括起来了，表示间接寻址，rsp的值是一个内存地址，这条指令的真实意图是把rax寄存器中的值赋值给rsp寄存器的值（内存地址）对应的内存，rsp寄存器本身的值不会被修改，作为比较，我们看一下 mov    %rax, %rsp 这条指令 ，这里第二个操作数仅仅少了个括号，变成了直接寻址，意思完全不一样了，这条指令的意思是把rax的值赋给rsp，这样rsp寄存器的值被修改为跟rax寄存器一样的值了。下面的2张图展示了这两种寻址方式的不同：</p>
</li>
</ol>
<p><img src="https://img-blog.csdnimg.cn/img_convert/36df2f42c6dcb08a666deb558071ead7.png" alt></p>
<p>执行mov %rax, %rsp这条指令之前，rsp寄存器的值是x，rax寄存器的值是y，执行指令之后，rax寄存器的值被复制给了rsp寄存器，所以rsp寄存器的值变成了y，可以看出，采用直接寻址方式时，目的操作数rsp寄存器的值在指令执行之前和指令执行之后发生了变化，源操作数没有变化。再看看间接寻址方式的示意图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/69dfcf2834e4d7da1c8564720b828b16.png" alt></p>
<p>执行mov %rax, （%rsp）这条指令之前，rax寄存器的值是y，rsp寄存器的值是X，它是一个内存地址，如上图所示，我们用了一个红色箭头从rsp寄存器指向了地址为X的内存；执行指令之后，rsp寄存器的值并没有发生变化，而rsp所指的内存中的值却发生了改变，因为这条指令的目的操作数采用了间接寻址方式(%rsp)，指令执行的结果是rax寄存器中的值被复制到了rsp寄存器存放的地址所对应的8个内存单元中。另外需要注意的是指令中出现的内存地址仅仅是起始地址，具体要操作以这个地址为起始地址的连续几个内存单元要根据具体的指令而定，比如上图中的mov %rax,(%rsp)，因为源操作数是一个64位的寄存器，所以这条指令会复制rax存放的8个字节到地址为X, X+1, X+2, X+3, X+4, X+5, X+6, X+7这8个内存单元中去。</p>
<p>间接寻址格式offset(%register)中前面的offset表示偏移，如-0x8(%rbp)，-0x8就是偏移量，整个表示rbp寄存器里面保存的地址值先减去8（因为偏移是负8）得到的地址对应的内存。</p>
<ol start="5">
<li>与内存相关的一些指令的操作码会加上b, w, l和q字母分别表示操作的内存是1，2，4还是8个字节，比如指令 movl   $0x0,-0x8(%rbp) ，这条指令操作码movl的后缀字母l说明我们要把从-0x8(%rbp) 这个地址开始的4个内存单元赋值为0。可能有读者会问，那如果我要操作3个，或5个内存单元呢？很遗憾的是cpu没有提供相应的单条指令，我们只能通过多条指令组合起来达到目的。</li>
</ol>
<p>常用指令详解</p>
<p>x86-64汇编指令上千条，这里不会去详细讲解每一条，读者如果有兴趣可以参考汇编语言相关教程。我们在这里着重关注几条非常常见或是能帮助我们理解程序运行机制的指令。</p>
<p>mov指令</p>
<p>mov 源操作数 目的操作数<br>该指令复制源操作数到目的操作数。例：</p>
<p>mov %rsp,%rbp       # 直接寻址，把rsp的值拷贝给rbp，相当于 rbp = rsp<br>mov -0x8(%rbp),%edx # 源操作数间接寻址，目的操作数直接寻址。从内存中读取4个字节到edx寄存器<br>mov %rsi,-0x8(%rbp) # 源操作数直接寻址，目的操作数间接寻址。把rsi寄存器中的8字节值写入内存<br>add/sub指令</p>
<p>add 源操作数 目的操作数<br>sub 源操作数 目的操作数<br>加减运算指令。例：</p>
<p>sub $0x350,%rsp  # 源操作数是立即操作数，目的操作数直接寻址。rsp = rsp - 0x350<br>add %rdx,%rax    # 直接寻址。rax = rax + rdx<br>addl $0x1,-0x8(%rbp) # 源操作数是立即操作数，目的操作数间接寻址。内存中的值加1（addl后缀字母l表示操作内存中的4个字节）<br>call/ret指令</p>
<p>call 目标地址<br>ret<br>call指令执行函数调用。CPU执行call指令时首先会把rip寄存器中的值入栈，然后设置rip值为目标地址，又因为rip寄存器决定了下一条需要执行的指令，所以当CPU执行完当前call指令后就会跳转到目标地址去执行。</p>
<p>ret指令从被调用函数返回调用函数，它的实现原理是把call指令入栈的返回地址弹出给rip寄存器。</p>
<p>下面用例子对这两条指令的原理加以说明。</p>
<p>#调用函数片段<br>0x0000000000400559 : callq 0x400526 <sum><br>0x000000000040055e : mov   %eax,-0x4(%rbp)</sum></p>
<p>#被调用函数片段<br>0x0000000000400526 : push   %rbp<br>……<br>0x000000000040053f : retq<br>上面代码片段中，调用函数使用callq  0x400526指令调用0x400526处的函数，0x400526是被调用函数的第一条指令所在的地址。被调用函数在0x40053f处执行retq指令返回调用函数继续执行0x40055e地址处的指令。注意这两条指令会涉及入栈和出栈操作，所以会影响rsp寄存器的值。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/77895100f734c9fe5e498ad03243d650.png" alt></p>
<p>从上图可以看到call指令执行之初rip寄存器的值是紧跟call后面那一条指令的地址，即0x40055e，但当call指令完成后但还未开始执行下一条指令之前，rip寄存器的值变成了call指令的操作数，即被调用函数的地址0x400526，这样CPU就会跳转到被调用函数去执行了。</p>
<p>同时还需要注意的是这里的call指令执行时把call指令后面那一条指令的地址 0x40055e PUSH到了栈上，所以一条call指令修改了3个地方的值：rip寄存器、rsp和栈。</p>
<p>下面我们再看看从被调用函数返回调用函数时执行的ret指令，其示意图如下：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/dc9af703de1688f7bf4ccce3cfc12191.png" alt></p>
<p>可以看到ret指令执行的操作跟call指令执行的操作完全相反，ret指令开始执行时rip寄存器的值是紧跟ret指令后面的那个地址，也就是0x400540，但ret指令执行过程中会把之前call指令PUSH到栈上的返回地址 0x40055e POP给rip寄存器，这样，当ret执行完成后就会从被调用函数返回到调用函数的call指令的下一条指令继续执行。这里同样要注意的是retq指令也会修改rsp寄存器的值。</p>
<p>jmp/je/jle/jg/jge等等j开头的指令</p>
<p>这些都属于跳转指令，操作码后面直接跟要跳转到的地址或存有地址的寄存器，这些指令与高级编程语言中的 goto 和 if 等语句对应。用法示例：</p>
<p>jmp    0x4005f2<br>jle    0x4005ee<br>jl     0x4005b8<br>push/pop指令</p>
<p>push 源操作数<br>pop 目的操作数<br>专用于函数调用栈的入栈出栈指令，这两个指令都会自动修改rsp寄存器。</p>
<p>push入栈时rsp寄存器的值先减去8把栈位置留出来，然后把操作数复制到rsp所指位置。push指令相当于：</p>
<p>sub $8,%rsp<br>mov 源操作数,(%rsp)<br><img src="https://img-blog.csdnimg.cn/img_convert/3c09f37c2412b0ac0f7d13925b73dad2.png" alt></p>
<p>push指令需要重点注意rsp寄存器的变化。</p>
<p>pop出栈时先把rsp寄存器所指位置的数据复制到目的操作数中，然后rsp寄存器的值加8。pop指令相当于：</p>
<p>mov (%rsp),目的操作数<br>add $8,%rsp<br><img src="https://img-blog.csdnimg.cn/img_convert/335a7dc27f39599b710bff5c7ac4dc12.png" alt></p>
<p>同样，pop指令也需要重点注意rsp寄存器的变化。</p>
<p>leave指令</p>
<p>leave指令没有操作数，它一般放在函数的尾部ret指令之前，用于调整rsp和rbp，这条指令相当于如下两条指令：</p>
<p>mov %rbp,%rsp<br>pop %rbp</p>
<p>AMD64汇编我们就介绍这么多，下一节我们将介绍go runtime中使用的go汇编语言，它与这里介绍的AMD64汇编类似，但有一些差别。理解了本节的内容，go汇编也就很容易理解了。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析之四：函数调用栈</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90%E4%B9%8B%E5%9B%9B%EF%BC%9A%E5%87%BD%E6%95%B0%E8%B0%83%E7%94%A8%E6%A0%88/</url>
    <content><![CDATA[<p>原创 阿波张 <a href="https://mp.weixin.qq.com/s/EY1acVrkUnmFQ0E1IwLzUQ" title="源码游记" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-04-19</p>
<p>什么是栈</p>
<p>栈是一种“后进先出”的数据结构，它相当于一个容器，当需要往容器里面添加元素时只能放在最上面的一个元素之上，需要取出元素时也只能从最上面开始取，通常我们称添加元素为入栈(push)，取出元素为出栈(pop)。</p>
<p>不知道读者是否有快餐店吃饭的经历，快餐店一般都有一摞干净的盘子让顾客取用，这就好比一个栈，我们取盘子时通常都是拿走最上面一个(pop)，当盘子被取走剩得不多时，服务员又会拿一些干净的盘子放在原有盘子的上面(push)，取盘子和放盘子这一端用栈的术语来说叫栈顶，另一端叫栈底。</p>
<p>下面用图演示一下栈的push和pop。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/c2f68414069dc1b87bbba233bfe136a2.png" alt></p>
<p>进程在内存中的布局</p>
<p>严格说来这里讲的是进程在虚拟地址空间中的布局，但这并不影响我们的讨论，所以这里我们就不做区分，笼统的称之为进程在内存中的布局。</p>
<p>操作系统把磁盘上的可执行文件加载到内存运行之前，会做很多工作，其中很重要的一件事情就是把可执行文件中的代码，数据放在内存中合适的位置，并分配和初始化程序运行过程中所必须的堆栈，所有准备工作完成后操作系统才会调度程序起来运行。来看一下程序运行时在内存中的布局图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/dcf6b34ce88cbf4c9ba8ce54fe318cbe.png" alt></p>
<p>进程在内存中的布局主要分为4个区域：代码区，数据区，堆和栈。在详细讨论栈之前，先来简单介绍一下其它区域。</p>
<p>代码区，包括能被CPU执行的机器代码（指令）和只读数据比如字符串常量，程序一旦加载完成代码区的大小就不会再变化了。</p>
<p>数据区，包括程序的全局变量和静态变量（c语言有静态变量，而go没有），与代码区一样，程序加载完毕后数据区的大小也不会发生改变。</p>
<p>堆，程序运行时动态分配的内存都位于堆中，这部分内存由内存分配器负责管理。该区域的大小会随着程序的运行而变化，即当我们向堆请求分配内存但分配器发现堆中的内存不足时，它会向操作系统内核申请向高地址方向扩展堆的大小，而当我们释放内存把它归还给堆时如果内存分配器发现剩余空闲内存太多则又会向操作系统请求向低地址方向收缩堆的大小。从这个内存申请和释放流程可以看出，我们从堆上分配的内存用完之后必须归还给堆，否则内存分配器可能会反复向操作系统申请扩展堆的大小从而导致堆内存越用越多，最后出现内存不足，这就是所谓的内存泄漏。值的一提的是传统的c/c++代码就必须小心处理内存的分配和释放，而在go语言中，有垃圾回收器帮助我们，所以程序员只管申请内存，而不用管内存的释放，这大大降低了程序员的心智负担，这不光是提高了程序员的生产力，更重要的是还会减少很多bug的产生。</p>
<p>函数调用栈</p>
<p>函数调用栈简称栈，在程序运行过程中，不管是函数的执行还是函数调用，栈都起着非常重要的作用，它主要被用来：</p>
<p>保存函数的局部变量；</p>
<p>向被调用函数传递参数；</p>
<p>返回函数的返回值；</p>
<p>保存函数的返回地址。返回地址是指从被调用函数返回后调用者应该继续执行的指令地址，在汇编指令一节介绍call指令时我们将会对返回地址做更加详细的说明。</p>
<p>每个函数在执行过程中都需要使用一块栈内存用来保存上述这些值，我们称这块栈内存为某函数的栈帧(stack frame)。当发生函数调用时，因为调用者还没有执行完，其栈内存中保存的数据还有用，所以被调用函数不能覆盖调用者的栈帧，只能把被调用函数的栈帧“push”到栈上，等被调函数执行完成后再把其栈帧从栈上“pop”出去，这样，栈的大小就会随函数调用层级的增加而生长，随函数的返回而缩小，也就是说函数调用层级越深，消耗的栈空间就越大。栈的生长和收缩都是自动的，由编译器插入的代码自动完成，因此位于栈内存中的函数局部变量所使用的内存随函数的调用而分配，随函数的返回而自动释放，所以程序员不管是使用有垃圾回收还是没有垃圾回收的高级编程语言都不需要自己释放局部变量所使用的内存，这一点与堆上分配的内存截然不同。</p>
<p>另外，AMD64 Linux平台下，栈是从高地址向低地址方向生长的，为什么栈会采用这种看起来比较反常的生长方向呢，具体原因无从考究，不过根据前面那张进程的内存布局图可以猜测，当初这么设计的计算机科学家是希望尽量利用内存地址空间，才采用了堆和栈相向生长的方式，因为程序运行之前无法确定堆和栈谁会消耗更多的内存，如果栈也跟堆一样向高地址方向生长的话，栈底的位置不好确定，离堆太近则堆内存可能不够用，离堆太远栈又可能不够用，于是乎就采用了现在这种相向生长的方式。</p>
<p>AMD64 CPU提供了2个与栈相关的寄存器：</p>
<p>rsp寄存器，始终指向函数调用栈栈顶</p>
<p>rbp寄存器，一般用来指向函数栈帧的起始位置</p>
<p>下面用两个图例来说明一下函数调用栈以及rsp/rbp与栈之间的关系。</p>
<p>假设现在有如下函数调用链且正在执行函数C()：</p>
<p>A()-&gt;B()-&gt;C()<br>则函数ABC的栈帧以及rsp/rbp的状态大致如下图所示（注意，栈从高地址向低地址方向生长）：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/95d7ac62614da2aaedc42277388d4f74.png" alt></p>
<p>对于上图，有几点需要说明一下：</p>
<p>调用函数时，参数和返回值都是存放在调用者的栈帧之中，而不是在被调函数之中；</p>
<p>目前正在执行C函数，且函数调用链为A()-&gt;B()-&gt;C()，所以以栈帧为单位来看的话，C函数的栈帧目前位于栈顶；</p>
<p>CPU硬件寄存器rsp指向整个栈的栈顶，当然它也指向C函数的栈帧的栈顶，而rbp寄存器指向的是C函数栈帧的起始位置；</p>
<p>虽然图中ABC三个函数的栈帧看起来都差不多大，但事实上在真实的程序中，每个函数的栈帧大小可能都不同，因为不同的函数局部变量的个数以及所占内存的大小都不尽相同；</p>
<p>有些编译器比如gcc会把参数和返回值放在寄存器中而不是栈中，go语言中函数的参数和返回值都是放在栈上的；</p>
<p>随着程序的运行，如果C、B两个函数都执行完成并返回到了A函数继续执行，则栈状态如下图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/30eafa3ef7a07229b12eac9524ddc48e.png" alt></p>
<p>因为C、B两个函数都已经执行完成并返回到了A函数之中，所以C、B两个函数的栈帧就已经被POP出栈了，也就是说它们所消耗的栈内存被自动回收了。因为现在正在执行A函数，所以寄存器rbp和rsp指向的是A函数的栈中的相应位置。如果A函数又继续调用了D函数的话，则栈又变成下面这个样子：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a452ab3aa80bcba6e773413c48df6abf.png" alt></p>
<p>可以看到，现在D函数的栈帧其实使用的是之前调用B、C两个函数所使用的栈内存，这没有问题，因为B和C函数已经执行完了，现在D函数重用了这块内存，这也是为什么在C语言中绝对不要返回函数局部变量的地址，因为同一个地址的栈内存会被重用，这就会造成意外的bug，而go语言中没有这个限制，因为go语言的编译器比较智能，当它发现程序返回了某个局部变量的地址，编译器会把这个变量放到堆上去，而不会放在栈上。同样，这里我们还是需要注意rbp和rsp这两个寄存器现在指向了D函数的栈帧。从上面的分析我们可以看出，寄存器rbp和rsp始终指向正在执行的函数的栈帧。</p>
<p>最后，我们再来看一个递归函数的例子，假设有如下go语言代码片段：</p>
<p>func f(n int) {<br>   if n &lt;= 0 { //递归结束条件 n &lt;= 0<br>       return<br>  }<br>   ……<br>   f(n - 1) //递归调用f函数自己<br>   ……<br>}<br>函数f是一个递归函数，f函数会一直递归的调用自己直到参数 n 小于等于0为止，如果我们在其它某个函数里调用了f(10)，而且现在正在执行f(8)的话，则其栈状态如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f395cf6fd8a3a2bc36d8e59d93c3c968.png" alt></p>
<p>从上图可以看出，即使是同一个函数，每次调用都会产生一个不同的栈帧，因此对于递归函数，每递归一次都会消耗一定的栈内存，如果递归层数太多就有导致栈溢出的风险，这也是为什么我们在实际的开发过程中应该尽量避免使用递归函数的原因之一，另外一个原因是递归函数执行效率比较低，因为它要反复调用函数，而调用函数有较大的性能开销。</p>
<p>本节我们简要的介绍了栈的基本概念及它在程序运行过程中的重要作用，但遗留了一些细节问题，比如每个函数的栈帧是怎么分配的，局部变量和参数又是如何保存在栈中的，又是谁把返回地址放在了栈上等等，这些内容我们会在函数调用过程一节加以详细介绍。这里为什么不把细节跟概念放在一起讨论呢，主要是因为我们首先要对栈有个大致的了解，才能更好的理解下一节即将讲述的有关汇编语言相关的知识，而没有汇编语言作为基础，我们又不能很好的理解栈的这些细节问题，所以我们决定把基本概念和用途与细节分开介绍。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>非main goroutine的退出及调度循环（15）</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/%E9%9D%9Emain-goroutine%E7%9A%84%E9%80%80%E5%87%BA%E5%8F%8A%E8%B0%83%E5%BA%A6%E5%BE%AA%E7%8E%AF%EF%BC%8815%EF%BC%89/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/XttP9q7-PO7VXhskaBzGqA" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-12</p>
<p>本文是《Go语言调度器源代码情景分析》系列的第15篇，也是第二章的第5小节。</p>
<p>上一节我们说过main goroutine退出时会直接执行exit系统调用退出整个进程，而非main goroutine退出时则会进入goexit函数完成最后的清理工作，本小节我们首先就来验证一下非main goroutine执行完成后是否真的会去执行goexit，然后再对非main goroutine的退出流程做个梳理。这一节我们需要重点理解以下内容：</p>
<p>非main goroutine是如何返回到goexit函数的；</p>
<p>mcall函数如何从用户goroutine切换到g0继续执行；</p>
<p>调度循环。</p>
<p>非main goroutine会返回到goexit吗</p>
<p>首先来看一段代码：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">)</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">g2</span><span class="params">(n <span class="keyword">int</span>, ch <span class="keyword">chan</span> <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">    ch &lt;- n * n</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    ch := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">int</span>)</span><br><span class="line">    <span class="keyword">go</span> g2(<span class="number">100</span>, ch)</span><br><span class="line">    fmt.Println(&lt;-ch)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个程序比较简单，main goroutine启动后在main函数中创建了一个goroutine执行g2函数，我们称它为g2 goroutine，下面我们就用这个g2的退出来验证一下非main goroutine退出时是否真的会返回到goexit继续执行。</p>
<p>怎么验证呢？比较简单的办法就是用gdb来调试，在gdb中首先使用backtrace命令查看g2函数是被谁调用的，然后单步执行看它能否返回到goexit继续执行。下面是gdb调试过程：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(gdb) b main.g2       // 在main.g2函数入口处下断点</span><br><span class="line">Breakpoint 1 at 0x4869c0: file /home/bobo/study/go/goexit.go, line 7.</span><br><span class="line">(gdb) r</span><br><span class="line">Starting program: /home/bobo/study/go/goexit </span><br><span class="line">Thread 1 <span class="string">"goexit"</span> hit Breakpoint 1 at /home/bobo/study/go/goexit.go:7</span><br><span class="line">(gdb) bt       //查看函数调用链，看起来g2真的是被runtime.goexit调用的</span><br><span class="line"><span class="comment">#0 main.g2 (n=100, ch=0xc000052060) at /home/bobo/study/go/goexit.go:7</span></span><br><span class="line"><span class="comment">#1 0x0000000000450ad1 in runtime.goexit () at /usr/local/go/src/runtime/asm_amd64.s:1337</span></span><br><span class="line">(gdb) disass     //反汇编找ret的地址，这是为了在ret处下断点</span><br><span class="line">Dump of assembler code <span class="keyword">for</span> <span class="keyword">function</span> main.g2:</span><br><span class="line">=&gt; 0x00000000004869c0 &lt;+0&gt;:mov   %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">  0x00000000004869c9 &lt;+9&gt;:cmp   0x10(%rcx),%rsp</span><br><span class="line">  0x00000000004869cd &lt;+13&gt;:jbe   0x486a0d &lt;main.g2+77&gt;</span><br><span class="line">  0x00000000004869cf &lt;+15&gt;:sub   <span class="variable">$0x20</span>,%rsp</span><br><span class="line">  0x00000000004869d3 &lt;+19&gt;:mov   %rbp,0x18(%rsp)</span><br><span class="line">  0x00000000004869d8 &lt;+24&gt;:lea   0x18(%rsp),%rbp</span><br><span class="line">  0x00000000004869dd &lt;+29&gt;:mov   0x28(%rsp),%rax</span><br><span class="line">  0x00000000004869e2 &lt;+34&gt;:imul   %rax,%rax</span><br><span class="line">  0x00000000004869e6 &lt;+38&gt;:mov   %rax,0x10(%rsp)</span><br><span class="line">  0x00000000004869eb &lt;+43&gt;:mov   0x30(%rsp),%rax</span><br><span class="line">  0x00000000004869f0 &lt;+48&gt;:mov   %rax,(%rsp)</span><br><span class="line">  0x00000000004869f4 &lt;+52&gt;:lea   0x10(%rsp),%rax</span><br><span class="line">  0x00000000004869f9 &lt;+57&gt;:mov   %rax,0x8(%rsp)</span><br><span class="line">  0x00000000004869fe &lt;+62&gt;:callq 0x4046a0 &lt;runtime.chansend1&gt;</span><br><span class="line">  0x0000000000486a03 &lt;+67&gt;:mov   0x18(%rsp),%rbp</span><br><span class="line">  0x0000000000486a08 &lt;+72&gt;:add   <span class="variable">$0x20</span>,%rsp</span><br><span class="line">  0x0000000000486a0c &lt;+76&gt;:retq   </span><br><span class="line">  0x0000000000486a0d &lt;+77&gt;:callq 0x44ece0 &lt;runtime.morestack_noctxt&gt;</span><br><span class="line">  0x0000000000486a12 &lt;+82&gt;:jmp   0x4869c0 &lt;main.g2&gt;</span><br><span class="line">End of assembler dump.</span><br><span class="line">(gdb) b *0x0000000000486a0c             //在retq指令位置下断点</span><br><span class="line">Breakpoint 2 at 0x486a0c: file /home/bobo/study/go/goexit.go, line 9.</span><br><span class="line">(gdb) c</span><br><span class="line">Continuing.</span><br><span class="line"> </span><br><span class="line">Thread 1 <span class="string">"goexit"</span> hit Breakpoint 2 at /home/bobo/study/go/goexit.go:9</span><br><span class="line">(gdb) disass             //程序停在了ret指令处</span><br><span class="line">Dump of assembler code <span class="keyword">for</span> <span class="keyword">function</span> main.g2:</span><br><span class="line">  0x00000000004869c0 &lt;+0&gt;:mov   %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">  0x00000000004869c9 &lt;+9&gt;:cmp   0x10(%rcx),%rsp</span><br><span class="line">  0x00000000004869cd &lt;+13&gt;:jbe   0x486a0d &lt;main.g2+77&gt;</span><br><span class="line">  0x00000000004869cf &lt;+15&gt;:sub   <span class="variable">$0x20</span>,%rsp</span><br><span class="line">  0x00000000004869d3 &lt;+19&gt;:mov   %rbp,0x18(%rsp)</span><br><span class="line">  0x00000000004869d8 &lt;+24&gt;:lea   0x18(%rsp),%rbp</span><br><span class="line">  0x00000000004869dd &lt;+29&gt;:mov   0x28(%rsp),%rax</span><br><span class="line">  0x00000000004869e2 &lt;+34&gt;:imul   %rax,%rax</span><br><span class="line">  0x00000000004869e6 &lt;+38&gt;:mov   %rax,0x10(%rsp)</span><br><span class="line">  0x00000000004869eb &lt;+43&gt;:mov   0x30(%rsp),%rax</span><br><span class="line">  0x00000000004869f0 &lt;+48&gt;:mov   %rax,(%rsp)</span><br><span class="line">  0x00000000004869f4 &lt;+52&gt;:lea   0x10(%rsp),%rax</span><br><span class="line">  0x00000000004869f9 &lt;+57&gt;:mov   %rax,0x8(%rsp)</span><br><span class="line">  0x00000000004869fe &lt;+62&gt;:callq 0x4046a0 &lt;runtime.chansend1&gt;</span><br><span class="line">  0x0000000000486a03 &lt;+67&gt;:mov   0x18(%rsp),%rbp</span><br><span class="line">  0x0000000000486a08 &lt;+72&gt;:add   <span class="variable">$0x20</span>,%rsp</span><br><span class="line">=&gt; 0x0000000000486a0c &lt;+76&gt;:retq   </span><br><span class="line">  0x0000000000486a0d &lt;+77&gt;:callq 0x44ece0 &lt;runtime.morestack_noctxt&gt;</span><br><span class="line">  0x0000000000486a12 &lt;+82&gt;:jmp   0x4869c0 &lt;main.g2&gt;</span><br><span class="line">End of assembler dump.</span><br><span class="line">(gdb) si         //单步执行一条指令</span><br><span class="line">runtime.goexit () at /usr/<span class="built_in">local</span>/go/src/runtime/asm_amd64.s:1338</span><br><span class="line">1338CALLruntime·goexit1(SB)// does not <span class="built_in">return</span></span><br><span class="line">(gdb) disass           //可以看出来g2已经返回到了goexit函数中</span><br><span class="line">Dump of assembler code <span class="keyword">for</span> <span class="keyword">function</span> runtime.goexit:</span><br><span class="line">  0x0000000000450ad0 &lt;+0&gt;:nop</span><br><span class="line">=&gt; 0x0000000000450ad1 &lt;+1&gt;:callq 0x42faf0 &lt;runtime.goexit1&gt;</span><br><span class="line">  0x0000000000450ad6 &lt;+6&gt;:nop</span><br></pre></td></tr></table></figure></p>
<p>使用gdb调试时，首先我们在g2函数入口处下了一个断点，程序暂停后通过查看函数调用栈发现g2函数确实是被goexit调用的，然后再一次使用断点让程序暂停在g2返回之前的最后一条指令retq处，最后单步执行这条指令，可以看到程序从g2函数返回到了goexit函数的第二条指令的位置，这个位置正是当初在创建goroutine时设置好的返回地址。可以看到，虽然g2函数并不是被goexit函数直接调用的，但它执行完成之后却返回到了goexit函数中！</p>
<p>至此，我们已经证实非main goroutine退出时确实会返回到goexit函数继续执行，下面我们就沿着这条线继续分析非main goroutine的退出流程。</p>
<p>非main goroutine的退出流程</p>
<p>首先来看goexit函数<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">1334</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// The top-most function running on a goroutine</span></span><br><span class="line"><span class="comment">// returns to goexit+PCQuantum.</span></span><br><span class="line">TEXT runtime·goexit(SB),NOSPLIT,$<span class="number">0</span><span class="number">-0</span></span><br><span class="line">    BYTE  $<span class="number">0x90</span>  <span class="comment">// NOP</span></span><br><span class="line">    CALL  runtime·goexit1(SB)  <span class="comment">// does not return</span></span><br><span class="line">    <span class="comment">// traceback from goexit1 must hit code range of goexit</span></span><br><span class="line">    BYTE  $<span class="number">0x90</span>  <span class="comment">// NOP</span></span><br><span class="line">从前面的分析我们已经看到，非main goroutine返回时直接返回到了goexit的第二条指令：CALL runtime·goexit1(SB)，该指令继续调用goexit1函数。</span><br><span class="line"></span><br><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2652</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Finishes execution of the current goroutine.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">goexit1</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> raceenabled &#123;  <span class="comment">//与竞态检查有关，不关注</span></span><br><span class="line">        racegoend()</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> trace.enabled &#123; <span class="comment">//与backtrace有关，不关注</span></span><br><span class="line">        traceGoEnd()</span><br><span class="line">    &#125;</span><br><span class="line">    mcall(goexit0)</span><br><span class="line">&#125;</span><br><span class="line">goexit1函数通过调用mcall从当前运行的g2 goroutine切换到g0，然后在g0栈上调用和执行goexit0这个函数。</span><br><span class="line"></span><br><span class="line">runtime/asm_amd64.s : <span class="number">270</span></span><br><span class="line"></span><br><span class="line"># <span class="function"><span class="keyword">func</span> <span class="title">mcall</span><span class="params">(fn <span class="keyword">func</span>(*g)</span>)</span></span><br><span class="line"># Switch to m-&gt;g0<span class="string">'s stack, call fn(g).</span></span><br><span class="line"><span class="string"># Fn must never return. It should gogo(&amp;g-&gt;sched)</span></span><br><span class="line"><span class="string"># to keep running g.</span></span><br><span class="line"><span class="string"># mcall的参数是一个指向funcval对象的指针</span></span><br><span class="line"><span class="string">TEXT runtime·mcall(SB), NOSPLIT, $0-8</span></span><br><span class="line"><span class="string">    #取出参数的值放入DI寄存器，它是funcval对象的指针，此场景中fn.fn是goexit0的地址</span></span><br><span class="line"><span class="string">    MOVQ  fn+0(FP), DI</span></span><br><span class="line"><span class="string"> </span></span><br><span class="line"><span class="string">    get_tls(CX)</span></span><br><span class="line"><span class="string">    MOVQ  g(CX), AX # AX = g，本场景g 是 g2</span></span><br><span class="line"><span class="string"> </span></span><br><span class="line"><span class="string">    #mcall返回地址放入BX</span></span><br><span class="line"><span class="string">    MOVQ  0(SP), BX# caller'</span>s PC</span><br><span class="line"> </span><br><span class="line">    #保存g2的调度信息，因为我们要从当前正在运行的g2切换到g0</span><br><span class="line">    MOVQ  BX, (g_sched+gobuf_pc)(AX)   #g.sched.pc = BX，保存g2的rip</span><br><span class="line">    LEAQ  fn+<span class="number">0</span>(FP), BX # caller<span class="string">'s SP  </span></span><br><span class="line"><span class="string">    MOVQ  BX, (g_sched+gobuf_sp)(AX)  #g.sched.sp = BX，保存g2的rsp</span></span><br><span class="line"><span class="string">    MOVQ  AX, (g_sched+gobuf_g)(AX)   #g.sched.g = g</span></span><br><span class="line"><span class="string">    MOVQ  BP, (g_sched+gobuf_bp)(AX)  #g.sched.bp = BP，保存g2的rbp</span></span><br><span class="line"><span class="string"> </span></span><br><span class="line"><span class="string">    # switch to m-&gt;g0 &amp; its stack, call fn</span></span><br><span class="line"><span class="string">    #下面三条指令主要目的是找到g0的指针</span></span><br><span class="line"><span class="string">    MOVQ  g(CX), BX         #BX = g</span></span><br><span class="line"><span class="string">    MOVQ  g_m(BX), BX    #BX = g.m</span></span><br><span class="line"><span class="string">    MOVQ  m_g0(BX), SI   #SI = g.m.g0</span></span><br><span class="line"><span class="string"> </span></span><br><span class="line"><span class="string">    #此刻，SI = g0， AX = g，所以这里在判断g 是否是 g0，如果g == g0则一定是哪里代码写错了</span></span><br><span class="line"><span class="string">    CMPQ  SI, AX# if g == m-&gt;g0 call badmcall</span></span><br><span class="line"><span class="string">    JNE  3(PC)</span></span><br><span class="line"><span class="string">    MOVQ  $runtime·badmcall(SB), AX</span></span><br><span class="line"><span class="string">    JMP  AX</span></span><br><span class="line"><span class="string"> </span></span><br><span class="line"><span class="string">    #把g0的地址设置到线程本地存储之中</span></span><br><span class="line"><span class="string">    MOVQ  SI, g(CX)</span></span><br><span class="line"><span class="string"> </span></span><br><span class="line"><span class="string">    #恢复g0的栈顶指针到CPU的rsp积存，这一条指令完成了栈的切换，从g的栈切换到了g0的栈</span></span><br><span class="line"><span class="string">    MOVQ  (g_sched+gobuf_sp)(SI), SP# rsp = g0-&gt;sched.sp</span></span><br><span class="line"><span class="string"> </span></span><br><span class="line"><span class="string">    #AX = g</span></span><br><span class="line"><span class="string">    PUSHQ  AX   #fn的参数g入栈 </span></span><br><span class="line"><span class="string">    MOVQ  DI, DX   #DI是结构体funcval实例对象的指针，它的第一个成员才是goexit0的地址</span></span><br><span class="line"><span class="string">    MOVQ  0(DI), DI   #读取第一个成员到DI寄存器</span></span><br><span class="line"><span class="string">    CALL  DI   #调用goexit0(g)</span></span><br><span class="line"><span class="string">    POPQ  AX</span></span><br><span class="line"><span class="string">    MOVQ  $runtime·badmcall2(SB), AX</span></span><br><span class="line"><span class="string">    JMP  AX</span></span><br><span class="line"><span class="string">    RET</span></span><br><span class="line"><span class="string">mcall的参数是一个函数，在Go语言的实现中，函数变量并不是一个直接指向函数代码的指针，而是一个指向funcval结构体对象的指针，funcval结构体对象的第一个成员fn才是真正指向函数代码的指针。</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">type funcval struct &#123;</span></span><br><span class="line"><span class="string">    fn uintptr</span></span><br><span class="line"><span class="string">    // variable-size, fn-specific data here</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure></p>
<p>也就是说，在我们这个场景中mcall函数的fn参数的fn成员中存放的才是goexit0函数的第一条指令的地址。</p>
<p>mcall函数主要有两个功能：</p>
<p>首先从当前运行的g(我们这个场景是g2)切换到g0，这一步包括保存当前g的调度信息，把g0设置到tls中，修改CPU的rsp寄存器使其指向g0的栈；</p>
<p>以当前运行的g(我们这个场景是g2)为参数调用fn函数(此处为goexit0)。</p>
<p>从mcall的功能我们可以看出，mcall做的事情跟gogo函数完全相反，gogo函数实现了从g0切换到某个goroutine去运行，而mcall实现了从某个goroutine切换到g0来运行，因此，mcall和gogo的代码非常相似，然而mcall和gogo在做切换时有个重要的区别：gogo函数在从g0切换到其它goroutine时首先切换了栈，然后通过跳转指令从runtime代码切换到了用户goroutine的代码，而mcall函数在从其它goroutine切换回g0时只切换了栈，并未使用跳转指令跳转到runtime代码去执行。为什么会有这个差别呢？原因在于在从g0切换到其它goroutine之前执行的是runtime的代码而且使用的是g0栈，所以切换时需要首先切换栈然后再从runtime代码跳转某个goroutine的代码去执行（切换栈和跳转指令不能颠倒，因为跳转之后执行的就是用户的goroutine代码了，没有机会切换栈了），然而从某个goroutine切换回g0时，goroutine使用的是call指令来调用mcall函数，mcall函数本身就是runtime的代码，所以call指令其实已经完成了从goroutine代码到runtime代码的跳转，因此mcall函数自身的代码就不需要再跳转了，只需要把栈切换到g0栈即可。</p>
<p>因为mcall跟gogo非常相似，前面我们对gogo的每一条指令已经做过详细的分析，所以这里就不再详细解释mcall的每一条指令了，但笔者在上面所展示的mcall代码中做了一些注释（注释中的g表示当前正在运行的goroutine，我们这个场景g就是g2），这里大家可以结合gogo的代码以及mcall的代码和注释来加深对g0与其它goroutine之间的切换的理解。</p>
<p>从g2栈切换到g0栈之后，下面开始在g0栈执行goexit0函数，该函数完成最后的清理工作：</p>
<p>把g的状态从_Grunning变更为_Gdead；</p>
<p>然后把g的一些字段清空成0值；</p>
<p>调用dropg函数解除g和m之间的关系，其实就是设置g-&gt;m = nil, m-&gt;currg = nil；</p>
<p>把g放入p的freeg队列缓存起来供下次创建g时快速获取而不用从内存分配。freeg就是g的一个对象池；</p>
<p>调用schedule函数再次进行调度；<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2662</span></span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"></span><br><span class="line"><span class="comment">// goexit continuation on g0.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">goexit0</span><span class="params">(gp *g)</span></span> &#123;</span><br><span class="line">    _g_ := getg()  <span class="comment">//g0</span></span><br><span class="line"> </span><br><span class="line">    casgstatus(gp, _Grunning, _Gdead) <span class="comment">//g马上退出，所以设置其状态为_Gdead</span></span><br><span class="line">    <span class="keyword">if</span> isSystemGoroutine(gp, <span class="literal">false</span>) &#123;</span><br><span class="line">        atomic.Xadd(&amp;sched.ngsys, <span class="number">-1</span>)</span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">   <span class="comment">//清空g保存的一些信息</span></span><br><span class="line">    gp.m = <span class="literal">nil</span></span><br><span class="line">    locked := gp.lockedm != <span class="number">0</span></span><br><span class="line">    gp.lockedm = <span class="number">0</span></span><br><span class="line">    _g_.m.lockedg = <span class="number">0</span></span><br><span class="line">    gp.paniconfault = <span class="literal">false</span></span><br><span class="line">    gp._defer = <span class="literal">nil</span> <span class="comment">// should be true already but just in case.</span></span><br><span class="line">    gp._panic = <span class="literal">nil</span> <span class="comment">// non-nil for Goexit during panic. points at stack-allocated data.</span></span><br><span class="line">    gp.writebuf = <span class="literal">nil</span></span><br><span class="line">    gp.waitreason = <span class="number">0</span></span><br><span class="line">    gp.param = <span class="literal">nil</span></span><br><span class="line">    gp.labels = <span class="literal">nil</span></span><br><span class="line">    gp.timer = <span class="literal">nil</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Note that gp's stack scan is now "valid" because it has no</span></span><br><span class="line">    <span class="comment">// stack.</span></span><br><span class="line">    gp.gcscanvalid = <span class="literal">true</span></span><br><span class="line">   </span><br><span class="line">    <span class="comment">//g-&gt;m = nil, m-&gt;currg = nil 解绑g和m之关系</span></span><br><span class="line">    dropg()</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">   </span><br><span class="line">    gfput(_g_.m.p.ptr(), gp) <span class="comment">//g放入p的freeg队列，方便下次重用，免得再去申请内存，提高效率</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//下面再次调用schedule</span></span><br><span class="line">    schedule()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>到此为止g2的生命周期就结束了，工作线程再次调用了schedule函数进入新一轮的调度循环。</p>
<p>调度循环</p>
<p>我们说过，任何goroutine被调度起来运行都是通过schedule()-&gt;execute()-&gt;gogo()这个函数调用链完成的，而且这个调用链中的函数一直没有返回。以我们刚刚讨论过的g2 goroutine为例，从g2开始被调度起来运行到退出是沿着下面这条路径进行的</p>
<p>schedule()-&gt;execute()-&gt;gogo()-&gt;g2()-&gt;goexit()-&gt;goexit1()-&gt;mcall()-&gt;goexit0()-&gt;schedule()<br>可以看出，一轮调度是从调用schedule函数开始的，然后经过一系列代码的执行到最后又再次通过调用schedule函数来进行新一轮的调度，从一轮调度到新一轮调度的这一过程我们称之为一个调度循环，这里说的调度循环是指某一个工作线程的调度循环，而同一个Go程序中可能存在多个工作线程，每个工作线程都有自己的调度循环，也就是说每个工作线程都在进行着自己的调度循环。</p>
<p>从前面的代码分析可以得知，上面调度循环中的每一个函数调用都没有返回，虽然g2()-&gt;goexit()-&gt;goexit1()-&gt;mcall()这几个函数是在g2的栈空间执行的，但剩下的函数都是在g0的栈空间执行的，那么问题就来了，在一个复杂的程序中，调度可能会进行无数次循环，也就是说会进行无数次没有返回的函数调用，大家都知道，每调用一次函数都会消耗一定的栈空间，而如果一直这样无返回的调用下去无论g0有多少栈空间终究是会耗尽的，那么这里是不是有问题？其实没有问题，关键点就在于，每次执行mcall切换到g0栈时都是切换到g0.sched.sp所指的固定位置，这之所以行得通，正是因为从schedule函数开始之后的一系列函数永远都不会返回，所以重用这些函数上一轮调度时所使用过的栈内存是没有问题的。</p>
<p>每个工作线程的执行流程和调度循环都一样，如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/61038477913f3d941762171b0ff1b92a.png" alt></p>
<p>总结</p>
<p>我们用上图来总结一下工作线程的执行流程：</p>
<p>初始化，调用mstart函数；</p>
<p>调用mstart1函数，在该函数中调用save函数设置g0.sched.sp和g0.sched.pc等调度信息，其中g0.sched.sp指向mstart函数栈帧的栈顶；</p>
<p>依次调用schedule-&gt;execute-&gt;gogo函数执行调度；</p>
<p>运行用户的goroutine代码；</p>
<p>用户goroutine代码执行过程中调用runtime中的某些函数，然后这些函数调用mcall切换到g0.sched.sp所指的栈并最终再次调用schedule函数进入新一轮调度，之后工作线程一直循环执行着3～5这一调度循环直到进程退出为止。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>第三章 goroutine调度策略（16）</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/%E7%AC%AC%E4%B8%89%E7%AB%A0-Goroutine%E8%B0%83%E5%BA%A6%E7%AD%96%E7%95%A5%EF%BC%8816%EF%BC%89/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/2objs5JrlnKnwFbF4a2z2g" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-14</p>
<p>本文是《Go语言调度器源代码情景分析》系列的第16篇，也是第三章《Goroutine调度策略》的第1小节。</p>
<p>在调度器概述一节我们提到过，所谓的goroutine调度，是指程序代码按照一定的算法在适当的时候挑选出合适的goroutine并放到CPU上去运行的过程。这句话揭示了调度系统需要解决的三大核心问题：</p>
<p>调度时机：什么时候会发生调度？</p>
<p>调度策略：使用什么策略来挑选下一个进入运行的goroutine？</p>
<p>切换机制：如何把挑选出来的goroutine放到CPU上运行？</p>
<p>对这三大问题的解决构成了调度器的所有工作，因而我们对调度器的分析也必将围绕着它们所展开。</p>
<p>第二章我们已经详细的分析了调度器的初始化以及goroutine的切换机制，本章将重点讨论调度器如何挑选下一个goroutine出来运行的策略问题，而剩下的与调度时机相关的内容我们将在第4～6章进行全面的分析。</p>
<p>再探schedule函数</p>
<p>在讨论main goroutine的调度时我们已经见过schedule函数，因为当时我们的主要关注点在于main goroutine是如何被调度到CPU上运行的，所以并未对schedule函数如何挑选下一个goroutine出来运行做深入的分析，现在是重新回到schedule函数详细分析其调度策略的时候了。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2467</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// One round of scheduler: find a runnable goroutine and execute it.</span></span><br><span class="line"><span class="comment">// Never returns.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">schedule</span><span class="params">()</span></span> &#123;</span><br><span class="line">    _g_ := getg()   <span class="comment">//_g_ = m.g0</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">var</span> gp *g</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">   </span><br><span class="line">    <span class="keyword">if</span> gp == <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="comment">// Check the global runnable queue once in a while to ensure fairness.</span></span><br><span class="line">    <span class="comment">// Otherwise two goroutines can completely occupy the local runqueue</span></span><br><span class="line">    <span class="comment">// by constantly respawning each other.</span></span><br><span class="line">       <span class="comment">//为了保证调度的公平性，每个工作线程每进行61次调度就需要优先从全局运行队列中获取goroutine出来运行，</span></span><br><span class="line">       <span class="comment">//因为如果只调度本地运行队列中的goroutine，则全局运行队列中的goroutine有可能得不到运行</span></span><br><span class="line">        <span class="keyword">if</span> _g_.m.p.ptr().schedtick%<span class="number">61</span> == <span class="number">0</span> &amp;&amp; sched.runqsize &gt; <span class="number">0</span> &#123;</span><br><span class="line">            lock(&amp;sched.lock) <span class="comment">//所有工作线程都能访问全局运行队列，所以需要加锁</span></span><br><span class="line">            gp = globrunqget(_g_.m.p.ptr(), <span class="number">1</span>) <span class="comment">//从全局运行队列中获取1个goroutine</span></span><br><span class="line">            unlock(&amp;sched.lock)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> gp == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">//从与m关联的p的本地运行队列中获取goroutine</span></span><br><span class="line">        gp, inheritTime = runqget(_g_.m.p.ptr())</span><br><span class="line">        <span class="keyword">if</span> gp != <span class="literal">nil</span> &amp;&amp; _g_.m.spinning &#123;</span><br><span class="line">            throw(<span class="string">"schedule: spinning with local work"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> gp == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">//如果从本地运行队列和全局运行队列都没有找到需要运行的goroutine，</span></span><br><span class="line">        <span class="comment">//则调用findrunnable函数从其它工作线程的运行队列中偷取，如果偷取不到，则当前工作线程进入睡眠，</span></span><br><span class="line">        <span class="comment">//直到获取到需要运行的goroutine之后findrunnable函数才会返回。</span></span><br><span class="line">        gp, inheritTime = findrunnable() <span class="comment">// blocks until work is available</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//当前运行的是runtime的代码，函数调用栈使用的是g0的栈空间</span></span><br><span class="line">    <span class="comment">//调用execte切换到gp的代码和栈空间去运行</span></span><br><span class="line">    execute(gp, inheritTime)  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>schedule函数分三步分别从各运行队列中寻找可运行的goroutine：</p>
<p>第一步，从全局运行队列中寻找goroutine。为了保证调度的公平性，每个工作线程每经过61次调度就需要优先尝试从全局运行队列中找出一个goroutine来运行，这样才能保证位于全局运行队列中的goroutine得到调度的机会。全局运行队列是所有工作线程都可以访问的，所以在访问它之前需要加锁。</p>
<p>第二步，从工作线程本地运行队列中寻找goroutine。如果不需要或不能从全局运行队列中获取到goroutine则从本地运行队列中获取。</p>
<p>第三步，从其它工作线程的运行队列中偷取goroutine。如果上一步也没有找到需要运行的goroutine，则调用findrunnable从其他工作线程的运行队列中偷取goroutine，findrunnable函数在偷取之前会再次尝试从全局运行队列和当前线程的本地运行队列中查找需要运行的goroutine。</p>
<p>下面我们先来看如何从全局运行队列中获取goroutine。</p>
<p>从全局运行队列中获取goroutine</p>
<p>从全局运行队列中获取可运行的goroutine是通过globrunqget函数来完成的，该函数的第一个参数是与当前工作线程绑定的p，第二个参数max表示最多可以从全局队列中拿多少个g到当前工作线程的本地运行队列中来。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">4663</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Try get a batch of G's from the global runnable queue.</span></span><br><span class="line"><span class="comment">// Sched must be locked.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">globrunqget</span><span class="params">(_p_ *p, max <span class="keyword">int32</span>)</span> *<span class="title">g</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> sched.runqsize == <span class="number">0</span> &#123;  <span class="comment">//全局运行队列为空</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//根据p的数量平分全局运行队列中的goroutines</span></span><br><span class="line">    n := sched.runqsize / gomaxprocs + <span class="number">1</span></span><br><span class="line">    <span class="keyword">if</span> n &gt; sched.runqsize &#123; <span class="comment">//上面计算n的方法可能导致n大于全局运行队列中的goroutine数量</span></span><br><span class="line">        n = sched.runqsize</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> max &gt; <span class="number">0</span> &amp;&amp; n &gt; max &#123;</span><br><span class="line">        n = max   <span class="comment">//最多取max个goroutine</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> n &gt; <span class="keyword">int32</span>(<span class="built_in">len</span>(_p_.runq)) / <span class="number">2</span> &#123;</span><br><span class="line">        n = <span class="keyword">int32</span>(<span class="built_in">len</span>(_p_.runq)) / <span class="number">2</span>  <span class="comment">//最多只能取本地队列容量的一半</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    sched.runqsize -= n</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//直接通过函数返回gp，其它的goroutines通过runqput放入本地运行队列</span></span><br><span class="line">    gp := sched.runq.pop()  <span class="comment">//pop从全局运行队列的队列头取</span></span><br><span class="line">    n--</span><br><span class="line">    <span class="keyword">for</span> ; n &gt; <span class="number">0</span>; n-- &#123;</span><br><span class="line">        gp1 := sched.runq.pop()  <span class="comment">//从全局运行队列中取出一个goroutine</span></span><br><span class="line">        runqput(_p_, gp1, <span class="literal">false</span>)  <span class="comment">//放入本地运行队列</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> gp</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>globrunqget函数首先会根据全局运行队列中goroutine的数量，函数参数max以及_p_的本地队列的容量计算出到底应该拿多少个goroutine，然后把第一个g结构体对象通过返回值的方式返回给调用函数，其它的则通过runqput函数放入当前工作线程的本地运行队列。这段代码值得一提的是，计算应该从全局运行队列中拿走多少个goroutine时根据p的数量（gomaxprocs）做了负载均衡。</p>
<p>如果没有从全局运行队列中获取到goroutine，那么接下来就在工作线程的本地运行队列中寻找需要运行的goroutine。</p>
<p>从工作线程本地运行队列中获取goroutine</p>
<p>从代码上来看，工作线程的本地运行队列其实分为两个部分，一部分是由p的runq、runqhead和runqtail这三个成员组成的一个无锁循环队列，该队列最多可包含256个goroutine；另一部分是p的runnext成员，它是一个指向g结构体对象的指针，它最多只包含一个goroutine。</p>
<p>从本地运行队列中寻找goroutine是通过runqget函数完成的，寻找时，代码首先查看runnext成员是否为空，如果不为空则返回runnext所指的goroutine，并把runnext成员清零，如果runnext为空，则继续从循环队列中查找goroutine。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">4825</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Get g from local runnable queue.</span></span><br><span class="line"><span class="comment">// If inheritTime is true, gp should inherit the remaining time in the</span></span><br><span class="line"><span class="comment">// current time slice. Otherwise, it should start a new time slice.</span></span><br><span class="line"><span class="comment">// Executed only by the owner P.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">runqget</span><span class="params">(_p_ *p)</span> <span class="params">(gp *g, inheritTime <span class="keyword">bool</span>)</span></span> &#123;</span><br><span class="line">    <span class="comment">// If there's a runnext, it's the next G to run.</span></span><br><span class="line">    <span class="comment">//从runnext成员中获取goroutine</span></span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        <span class="comment">//查看runnext成员是否为空，不为空则返回该goroutine</span></span><br><span class="line">        next := _p_.runnext   </span><br><span class="line">        <span class="keyword">if</span> next == <span class="number">0</span> &#123;</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> _p_.runnext.cas(next, <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> next.ptr(), <span class="literal">true</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//从循环队列中获取goroutine</span></span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        h := atomic.LoadAcq(&amp;_p_.runqhead) <span class="comment">// load-acquire, synchronize with other consumers</span></span><br><span class="line">        t := _p_.runqtail</span><br><span class="line">        <span class="keyword">if</span> t == h &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">nil</span>, <span class="literal">false</span></span><br><span class="line">        &#125;</span><br><span class="line">        gp := _p_.runq[h%<span class="keyword">uint32</span>(<span class="built_in">len</span>(_p_.runq))].ptr()</span><br><span class="line">        <span class="keyword">if</span> atomic.CasRel(&amp;_p_.runqhead, h, h+<span class="number">1</span>) &#123; <span class="comment">// cas-release, commits consume</span></span><br><span class="line">            <span class="keyword">return</span> gp, <span class="literal">false</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里首先需要注意的是不管是从runnext还是从循环队列中拿取goroutine都使用了cas操作，这里的cas操作是必需的，因为可能有其他工作线程此时此刻也正在访问这两个成员，从这里偷取可运行的goroutine。</p>
<p>其次，代码中对runqhead的操作使用了atomic.LoadAcq和atomic.CasRel，它们分别提供了load-acquire和cas-release语义。</p>
<p>对于atomic.LoadAcq来说，其语义主要包含如下几条：</p>
<p>原子读取，也就是说不管代码运行在哪种平台，保证在读取过程中不会有其它线程对该变量进行写入；</p>
<p>位于atomic.LoadAcq之后的代码，对内存的读取和写入必须在atomic.LoadAcq读取完成后才能执行，编译器和CPU都不能打乱这个顺序；</p>
<p>当前线程执行atomic.LoadAcq时可以读取到其它线程最近一次通过atomic.CasRel对同一个变量写入的值，与此同时，位于atomic.LoadAcq之后的代码，不管读取哪个内存地址中的值，都可以读取到其它线程中位于atomic.CasRel（对同一个变量操作）之前的代码最近一次对内存的写入。</p>
<p>对于atomic.CasRel来说，其语义主要包含如下几条：</p>
<p>原子的执行比较并交换的操作；</p>
<p>位于atomic.CasRel之前的代码，对内存的读取和写入必须在atomic.CasRel对内存的写入之前完成，编译器和CPU都不能打乱这个顺序；</p>
<p>线程执行atomic.CasRel完成后其它线程通过atomic.LoadAcq读取同一个变量可以读到最新的值，与此同时，位于atomic.CasRel之前的代码对内存写入的值，可以被其它线程中位于atomic.LoadAcq（对同一个变量操作）之后的代码读取到。</p>
<p>因为可能有多个线程会并发的修改和读取runqhead，以及需要依靠runqhead的值来读取runq数组的元素，所以需要使用atomic.LoadAcq和atomic.CasRel来保证上述语义。</p>
<p>我们可能会问，为什么读取p的runqtail成员不需要使用atomic.LoadAcq或atomic.load？因为runqtail不会被其它线程修改，只会被当前工作线程修改，此时没有人修改它，所以也就不需要使用原子相关的操作。</p>
<p>最后，由p的runq、runqhead和runqtail这三个成员组成的这个无锁循环队列非常精妙，我们会在后面的章节对这个循环队列进行分析。</p>
<p>CAS操作与ABA问题</p>
<p>我们知道使用cas操作需要特别注意ABA的问题，那么runqget函数这两个使用cas的地方会不会有问题呢？答案是这两个地方都不会有ABA的问题。原因分析如下：</p>
<p>首先来看对runnext的cas操作。只有跟_p_绑定的当前工作线程才会去修改runnext为一个非0值，其它线程只会把runnext的值从一个非0值修改为0值，然而跟_p_绑定的当前工作线程正在此处执行代码，所以在当前工作线程读取到值A之后，不可能有线程修改其值为B(0)之后再修改回A。</p>
<p>再来看对runq的cas操作。当前工作线程操作的是_p_的本地队列，只有跟_p_绑定在一起的当前工作线程才会因为往该队列里面添加goroutine而去修改runqtail，而其它工作线程不会往该队列里面添加goroutine，也就不会去修改runqtail，它们只会修改runqhead，所以，当我们这个工作线程从runqhead读取到值A之后，其它工作线程也就不可能修改runqhead的值为B之后再第二次把它修改为值A（因为runqtail在这段时间之内不可能被修改，runqhead的值也就无法越过runqtail再回绕到A值），也就是说，代码从逻辑上已经杜绝了引发ABA的条件。</p>
<p>到此，我们已经分析完工作线程从全局运行队列和本地运行队列获取goroutine的代码，由于篇幅的限制，我们下一节再来分析从其它工作线程的运行队列偷取goroutine的流程。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go深入理解defer（上）defer基础</title>
    <url>/go/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3defer/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3defer%EF%BC%88%E4%B8%8A%EF%BC%89defer%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/QmeQTONUuWlr_sRNP8b5Tw" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-06-11</p>
<p>深入理解 defer 分上下两篇文章，本文为上篇，主要介绍如下内容：</p>
<p>为什么需要 defer；</p>
<p>defer 语法及语义；</p>
<p>defer 使用要点；</p>
<p>defer 语句中的函数到底是在 return 语句之后被调用还是 return 语句之前被调用。</p>
<p>为什么需要 defer</p>
<p>先来看一段没有使用 defer 的代码：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">    r := getResource()  <span class="comment">//0，获取资源</span></span><br><span class="line">    ......</span><br><span class="line">    <span class="keyword">if</span> ... &#123;</span><br><span class="line">        r.release()  <span class="comment">//1，释放资源</span></span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">    <span class="keyword">if</span> ... &#123;</span><br><span class="line">        r.release()  <span class="comment">//2，释放资源</span></span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">    <span class="keyword">if</span> ... &#123;</span><br><span class="line">        r.release()  <span class="comment">//3，释放资源</span></span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">    r.release()  <span class="comment">//4，释放资源</span></span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>f() 函数首先通过调用 getResource()  获取了某种资源（比如打开文件，加锁等），然后进行了一些我们不太关心的操作，但这些操作可能会导致 f() 函数提前返回，为了避免资源泄露，所以每个 return 之前都调用了 r.release() 函数对资源进行释放。这段代码看起来并不糟糕，但有两个小问题：代码臃肿和可维护性比较差。臃肿倒是其次，主要问题在于代码的可维护性差，因为随着开发和维护的进行，修改代码在所难免，一旦对 f() 函数进行修改添加某个提前返回的分支，就很有可能在提前 return 时忘记调用 r.release() 释放资源，从而导致资源泄漏。</p>
<p>那么我们如何改善上述两个问题呢？一个不错的方案就是通过 defer 调用 r.release() 来释放资源：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">     r := getResource()  <span class="comment">//0，获取资源</span></span><br><span class="line">     <span class="keyword">defer</span> r.release()  <span class="comment">//1，注册延迟调用函数，f()函数返回时才会调用r.release函数释放资源</span></span><br><span class="line">     ......</span><br><span class="line">     <span class="keyword">if</span> ... &#123;</span><br><span class="line">         <span class="keyword">return</span></span><br><span class="line">     &#125;</span><br><span class="line">     ......</span><br><span class="line">     <span class="keyword">if</span> ... &#123;</span><br><span class="line">         <span class="keyword">return</span></span><br><span class="line">     &#125;</span><br><span class="line">     ......</span><br><span class="line">     <span class="keyword">if</span> ... &#123;</span><br><span class="line">         <span class="keyword">return</span></span><br><span class="line">     &#125;</span><br><span class="line">     ......</span><br><span class="line">     <span class="keyword">return</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到通过使用 defer 调用 r.release()，我们不需要在每个 return 之前都去手动调用 r.release() 函数，代码确实精简了一点，重要的是不管以后加多少提前 return 的代码，都不会出现资源泄露的问题，因为不管在什么地方 return ，r.release() 函数始终都会被调用。</p>
<p>defer 语法及语义</p>
<p>defer语法很简单，直接在普通写法的函数调用之前加 defer 关键字即可：</p>
<p>defer xxx(arg0, arg1, arg2, ……)<br>defer 表示对紧跟其后的 xxx() 函数延迟到 defer 语句所在的当前函数返回时再进行调用。比如前文代码中注释 1 处的 defer r.release() 表示等 f() 函数返回时再调用 r.release() 。下文我们称 defer 语句中的函数叫 defer函数。</p>
<p>defer 使用要点</p>
<p>对 defer 的使用需要注意如下几个要点：</p>
<p>延迟对函数进行调用；</p>
<p>即时对函数的参数进行求值；</p>
<p>根据 defer 顺序反序调用；</p>
<p>下面我们用例子来简单的看一下这几个要点。</p>
<p>defer 函数延迟调用<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">     <span class="keyword">defer</span> fmt.Println(<span class="string">"defer"</span>)</span><br><span class="line">     fmt.Println(<span class="string">"begin"</span>)</span><br><span class="line">     fmt.Println(<span class="string">"end"</span>)</span><br><span class="line">     <span class="keyword">return</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这段代码首先会输出 begin 字符串，然后是 end ，最后才输出 defer 字符串。</p>
<p>defer 函数参数即时求值<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">g</span><span class="params">(i <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">   fmt.Println(<span class="string">"g i:"</span>, i)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">   i := <span class="number">100</span></span><br><span class="line">   <span class="keyword">defer</span> g(i)  <span class="comment">//1</span></span><br><span class="line">   fmt.Println(<span class="string">"begin i:"</span>, i)</span><br><span class="line">   i = <span class="number">200</span></span><br><span class="line">   fmt.Println(<span class="string">"end i:"</span>, i)</span><br><span class="line">   <span class="keyword">return</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这段代码首先输出 begin i: 100，然后输出 end i: 200，最后输出 g i: 100 ，可以看到 g() 函数虽然在f函数返回时才被调用，但传递给 g() 函数的参数还是100，因为代码 1 处的 defer g(i) 这条语句执行时 i 的值是100。也就是说 defer 函数会被延迟调用，但传递给 defer 函数的参数会在 defer 语句处就被准备好。</p>
<p>反序调用<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span></span> &#123;</span><br><span class="line">     <span class="keyword">defer</span> fmt.Println(<span class="string">"defer01"</span>)</span><br><span class="line">     fmt.Println(<span class="string">"begin"</span>)</span><br><span class="line">     <span class="keyword">defer</span> fmt.Println(<span class="string">"defer02"</span>)</span><br><span class="line">     fmt.Println(<span class="string">"----"</span>)</span><br><span class="line">     <span class="keyword">defer</span> fmt.Println(<span class="string">"defer03"</span>)</span><br><span class="line">     fmt.Println(<span class="string">"end"</span>)</span><br><span class="line">     <span class="keyword">return</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这段程序的输出如下：</p>
<h2 id="begin"><a href="#begin" class="headerlink" title="begin"></a>begin</h2><p>end<br>defer03<br>defer02<br>defer01<br>可以看出f函数返回时，第一个 defer 函数最后被执行，而最后一个 defer 函数却第一个被执行。</p>
<p>defer 函数的执行与 return 语句之间的关系</p>
<p>到目前为止，defer 看起来都还比较好理解。下面我们开始把问题复杂化<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> g = <span class="number">100</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span> <span class="params">(r <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        g = <span class="number">200</span></span><br><span class="line">    &#125;()</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"f: g = %d\n"</span>, g)</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> g</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    i := f()</span><br><span class="line">    fmt.Printf(<span class="string">"main: i = %d, g = %d\n"</span>, i, g)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出：</p>
<p>$ ./defer<br>f: g = 100<br>main: i = 100, g = 200<br>这个输出还是比较容易理解，f() 函数在执行 return g 之前 g 的值还是100，所以 main() 函数获得的 f() 函数的返回值是100，因为 g 已经被 defer 函数修改成了200，所以在 main 中输出的 g 的值为200，看起来 defer 函数在 return g 之后才运行。下面稍微修改一下上面的程序：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> g = <span class="number">100</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">()</span> <span class="params">(r <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">    r = g</span><br><span class="line">    <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        r = <span class="number">200</span></span><br><span class="line">    &#125;()</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"f: r = %d\n"</span>, r)</span><br><span class="line"> </span><br><span class="line">    r = <span class="number">0</span></span><br><span class="line">    <span class="keyword">return</span> r</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    i := f()</span><br><span class="line">    fmt.Printf(<span class="string">"main: i = %d, g = %d\n"</span>, i, g)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>输出：</p>
<p>$ ./defer<br>f: r = 100<br>main: i = 200, g = 100<br>从这个输出可以看出，defer 函数修改了 f() 函数的返回值，从这里看起来 defer 函数的执行发生在 return r 之前，然而上一个例子我们得出的结论是 defer 函数在 return 语句之后才被调用执行，这两个结论很矛盾，到底是怎么回事呢？</p>
<p>仅仅从go语言的角度来说确实不太好理解，我们需要深入到汇编来分析一下。</p>
<p>老套路，使用 gdb 反汇编一下 f() 函数：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0x0000000000488a30 &lt;+0&gt;: mov  %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">0x0000000000488a39 &lt;+9&gt;: cmp  0x10(%rcx),%rsp</span><br><span class="line">0x0000000000488a3d &lt;+13&gt;: jbe  0x488b33 &lt;main.f+259&gt;</span><br><span class="line">0x0000000000488a43 &lt;+19&gt;: sub  <span class="variable">$0x68</span>,%rsp</span><br><span class="line">0x0000000000488a47 &lt;+23&gt;: mov  %rbp,0x60(%rsp)</span><br><span class="line">0x0000000000488a4c &lt;+28&gt;: lea   0x60(%rsp),%rbp</span><br><span class="line">0x0000000000488a51 &lt;+33&gt;: movq  <span class="variable">$0x0</span>,0x70(%rsp) <span class="comment"># 初始化返回值r为0</span></span><br><span class="line">0x0000000000488a5a &lt;+42&gt;: mov  0xbd66f(%rip),%rax        <span class="comment"># 0x5460d0 &lt;main.g&gt;</span></span><br><span class="line">0x0000000000488a61 &lt;+49&gt;: mov  %rax,0x70(%rsp)  <span class="comment"># r = g</span></span><br><span class="line">0x0000000000488a66 &lt;+54&gt;: movl   <span class="variable">$0x8</span>,(%rsp)</span><br><span class="line">0x0000000000488a6d &lt;+61&gt;: lea  0x384a4(%rip),%rax        <span class="comment"># 0x4c0f18</span></span><br><span class="line">0x0000000000488a74 &lt;+68&gt;: mov  %rax,0x8(%rsp)</span><br><span class="line">0x0000000000488a79 &lt;+73&gt;: lea  0x70(%rsp),%rax</span><br><span class="line">0x0000000000488a7e &lt;+78&gt;: mov  %rax,0x10(%rsp)</span><br><span class="line">0x0000000000488a83 &lt;+83&gt;: callq  0x426c00 &lt;runtime.deferproc&gt;</span><br><span class="line">0x0000000000488a88 &lt;+88&gt;: <span class="built_in">test</span>  %eax,%eax</span><br><span class="line">0x0000000000488a8a &lt;+90&gt;: jne  0x488b23 &lt;main.f+243&gt;</span><br><span class="line">0x0000000000488a90 &lt;+96&gt;: mov  0x70(%rsp),%rax</span><br><span class="line">0x0000000000488a95 &lt;+101&gt;: mov  %rax,(%rsp)</span><br><span class="line">0x0000000000488a99 &lt;+105&gt;: callq  0x408950 &lt;runtime.convT64&gt;</span><br><span class="line">0x0000000000488a9e &lt;+110&gt;: mov  0x8(%rsp),%rax</span><br><span class="line">0x0000000000488aa3 &lt;+115&gt;: xorps  %xmm0,%xmm0</span><br><span class="line">0x0000000000488aa6 &lt;+118&gt;: movups  %xmm0,0x50(%rsp)</span><br><span class="line">0x0000000000488aab &lt;+123&gt;: lea  0x101ee(%rip),%rcx        <span class="comment"># 0x498ca0</span></span><br><span class="line">0x0000000000488ab2 &lt;+130&gt;: mov  %rcx,0x50(%rsp)</span><br><span class="line">0x0000000000488ab7 &lt;+135&gt;: mov   %rax,0x58(%rsp)</span><br><span class="line">0x0000000000488abc &lt;+140&gt;: nop</span><br><span class="line">0x0000000000488abd &lt;+141&gt;: mov  0xd0d2c(%rip),%rax <span class="comment"># 0x5597f0 &lt;os.Stdout&gt;</span></span><br><span class="line">0x0000000000488ac4 &lt;+148&gt;: lea  0x495f5(%rip),%rcx <span class="comment"># 0x4d20c0 &lt;go.itab.*os.File,io.Writer&gt;</span></span><br><span class="line">0x0000000000488acb &lt;+155&gt;: mov   %rcx,(%rsp)</span><br><span class="line">0x0000000000488acf &lt;+159&gt;: mov  %rax,0x8(%rsp)</span><br><span class="line">0x0000000000488ad4 &lt;+164&gt;: lea   0x31ddb(%rip),%rax        <span class="comment"># 0x4ba8b6</span></span><br><span class="line">0x0000000000488adb &lt;+171&gt;: mov  %rax,0x10(%rsp)</span><br><span class="line">0x0000000000488ae0 &lt;+176&gt;: movq   <span class="variable">$0xa</span>,0x18(%rsp)</span><br><span class="line">0x0000000000488ae9 &lt;+185&gt;: lea  0x50(%rsp),%rax</span><br><span class="line">0x0000000000488aee &lt;+190&gt;: mov  %rax,0x20(%rsp)</span><br><span class="line">0x0000000000488af3 &lt;+195&gt;: movq  <span class="variable">$0x1</span>,0x28(%rsp)</span><br><span class="line">0x0000000000488afc &lt;+204&gt;: movq  <span class="variable">$0x1</span>,0x30(%rsp)</span><br><span class="line">0x0000000000488b05 &lt;+213&gt;: callq  0x480b20 &lt;fmt.Fprintf&gt;</span><br><span class="line">0x0000000000488b0a &lt;+218&gt;: movq  <span class="variable">$0x0</span>,0x70(%rsp) <span class="comment"># r = 0</span></span><br><span class="line"><span class="comment"># ---- 下面5条指令对应着go代码中的 return r</span></span><br><span class="line">0x0000000000488b13 &lt;+227&gt;: nop</span><br><span class="line">0x0000000000488b14 &lt;+228&gt;: callq  0x427490 &lt;runtime.deferreturn&gt;</span><br><span class="line">0x0000000000488b19 &lt;+233&gt;: mov  0x60(%rsp),%rbp</span><br><span class="line">0x0000000000488b1e &lt;+238&gt;: add  <span class="variable">$0x68</span>,%rsp</span><br><span class="line">0x0000000000488b22 &lt;+242&gt;: retq   </span><br><span class="line"><span class="comment"># ---------------------------</span></span><br><span class="line">0x0000000000488b23 &lt;+243&gt;: nop</span><br><span class="line">0x0000000000488b24 &lt;+244&gt;: callq  0x427490 &lt;runtime.deferreturn&gt;</span><br><span class="line">0x0000000000488b29 &lt;+249&gt;: mov  0x60(%rsp),%rbp</span><br><span class="line">0x0000000000488b2e &lt;+254&gt;: add  <span class="variable">$0x68</span>,%rsp</span><br><span class="line">0x0000000000488b32 &lt;+258&gt;: retq   </span><br><span class="line">0x0000000000488b33 &lt;+259&gt;: callq  0x44f300 &lt;runtime.morestack_noctxt&gt;</span><br><span class="line">0x0000000000488b38 &lt;+264&gt;: jmpq  0x488a30 &lt;main.f&gt;</span><br></pre></td></tr></table></figure></p>
<p>f() 函数本来很简单，但里面使用了闭包和 Printf，所以汇编代码看起来比较复杂，这里我们只挑重点出来说。f() 函数最后 2 条语句被编译器翻译成了如下6条汇编指令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0x0000000000488b0a &lt;+218&gt;: movq   <span class="variable">$0x0</span>,0x70(%rsp) <span class="comment"># r = 0</span></span><br><span class="line"><span class="comment"># ---- 下面5条指令对应着go代码中的 return r</span></span><br><span class="line">0x0000000000488b13 &lt;+227&gt;: nop</span><br><span class="line">0x0000000000488b14 &lt;+228&gt;: callq  0x427490 &lt;runtime.deferreturn&gt;  <span class="comment"># deferreturn会调用defer注册的函数</span></span><br><span class="line">0x0000000000488b19 &lt;+233&gt;: mov  0x60(%rsp),%rbp   <span class="comment"># 调整栈</span></span><br><span class="line">0x0000000000488b1e &lt;+238&gt;: add  <span class="variable">$0x68</span>,%rsp  <span class="comment"># 调整栈</span></span><br><span class="line">0x0000000000488b22 &lt;+242&gt;: retq   <span class="comment"># 从f()函数返回</span></span><br><span class="line"><span class="comment"># ---------------------------</span></span><br></pre></td></tr></table></figure></p>
<p>这6条指令中的第一条指令对应到的go语句是 r = 0，因为 r = 0 之后的下一行语句是 return r ，所以这条指令相当于把 f() 函数的返回值保存到了栈上，然后第三条指令调用了 runtime.deferreturn 函数，该函数会去调用我们在 f() 函数开始处使用 defer 注册的函数修改 r 的值为200，所以我们在main函数拿到的返回值是200，后面三条指令完成函数调用栈的调整及返回。</p>
<p>从这几条指令可以得出，准确的说，defer 函数的执行既不是在 return 之后也不是在 return 之前，而是一条go语言的 return 语句包含了对 defer 函数的调用，即 return 会被翻译成如下几条伪指令</p>
<p>保存返回值到栈上<br>调用defer函数<br>调整函数栈<br>retq指令返回<br>到此我们已经知道，前面说的矛盾其实并非矛盾，只是从Go语言层面来理解不好理解而已，一旦我们深入到汇编层面，一切都会显得那么自然，正所谓汇编之下了无秘密。</p>
<p>总结</p>
<p>defer 主要用于简化编程（以及实现 panic/recover ，后面会专门写一篇相关文章来介绍）</p>
<p>defer 实现了函数的延迟调用；</p>
<p>defer 使用要点：延迟调用，即时求值和反序调用；</p>
<p>go 语言的 return 会被编译器翻译成多条指令，其中包括保存返回值，调用defer注册的函数以及实现函数返回。</p>
<p>本文我们主要从使用的角度介绍了defer 的基础知识，下一篇文章我们将会深入 runtime.deferproc 和 runtime.deferreturn 这两个函数分析 defer 的实现机制。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>理解golang调度之一 ：操作系统调度</title>
    <url>/go/%E7%90%86%E8%A7%A3go%E8%B0%83%E5%BA%A6/%E7%90%86%E8%A7%A3golang%E8%B0%83%E5%BA%A6%E4%B9%8B%E4%B8%80%20%EF%BC%9A%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E8%B0%83%E5%BA%A6/</url>
    <content><![CDATA[<p><a href="https://juejin.im/post/6844903846284787719" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a><strong>简介</strong></h2><p>golang调度器的设计行为能够使你的多线程go程序更有效率、性能更好，这要归功于golang调度器对于操作系统调度器的支持。对于一个golang开发者来说，同时深刻理解操作系统调度和golang调度器工作原理，能够让你的golang程序设计和开发走到正确道路上。</p>
<h2 id="操作系统调度器"><a href="#操作系统调度器" class="headerlink" title="操作系统调度器"></a><strong>操作系统调度器</strong></h2><p>操作系统调度器十分复杂，它必须要考虑到底层的硬件结构，包括但不限于处理器数和内核数，cpu cache和NUMA。如果没有这些东西，调度器就没办法尽可能有效的工作。<br>程序其实就是一系列按顺序执行的机器指令。为了能让其正常干活，操作系统使用了线程的概念。线程会处理和执行分配给它的一系列的机器指令。线程会一直执行这些机器指令，直到没有指令再去执行了。这也是为什么把线程称作”a path of execution”。<br>每个运行程序都会创建一个进程，每个进程都会有一个初始线程。线程能够创建更多的线程。这些不同的线程独立运行并且调度行为是线程级别决定的，而不是在进程级别。线程能够并发的执行(单独内核上每个线程会轮询占用一段cpu时间),而不是并行执行(在不同内核上同时执行)。线程同时会维持它自己的状态，并且能够在本地安全、独立地执行他自己的指令。这也说明了为什么线程是cpu调度的最小单位。<br>操作系统调度器，它负责确保在有线程能够运行的时候内核不会空闲下来。它会制造一种假象——所有能够跑的线程此时都在同时执行。为此，调度器需要优先执行高优先级的线程，但是它也必须保证低优先级的线程不会饿死。调度器也必须尽可能将调度延时压倒最少，。<br>好在许多算法的应用使得调度器更加高效。下面解释一些重要的概念。</p>
<h2 id="执行指令"><a href="#执行指令" class="headerlink" title="执行指令"></a><strong>执行指令</strong></h2><p>程序计数器(PC)，有时候也叫做指令指针(IP)，能够让你找到下一个要执行的指令。大部分的处理器里，PC指向下一个指令。<br><img src="https://user-gold-cdn.xitu.io/2019/5/17/16ac61a45164982f?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>如果你曾经注意到go程序的追踪栈，你会注意到这些每一行末尾的16进制数字。例如Listing 1里的+0x39和+0x72<br>Listing 1<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">goroutine 1 [running]:</span><br><span class="line">   main.example(0xc000042748, 0x2, 0x4, 0x106abae, 0x5, 0xa)</span><br><span class="line">       stack_trace/example1/example1.go:13 +0x39                 &lt;- LOOK HERE</span><br><span class="line">   main.main()</span><br><span class="line">       stack_trace/example1/example1.go:8 +0x72                  &lt;- LOOK HERE</span><br></pre></td></tr></table></figure></p>
<p>这些数字代表了PC值，也就是从各自函数开始的偏移量。+0x39 PC偏移量代表了程序在还未panic的时候，线程在example方法执行的下一条指令。+0x72 PC偏移量代表如果example函数回到main函数里，main里的下一条指令。指向指令的前一个指针告诉了你现正在执行什么指令<br>看一下导致Listing 1 panic的程序<br><strong>Listing 2</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">07</span> <span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="number">08</span>     example(<span class="built_in">make</span>([]<span class="keyword">string</span>, <span class="number">2</span>, <span class="number">4</span>), <span class="string">"hello"</span>, <span class="number">10</span>)</span><br><span class="line"><span class="number">09</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="number">12</span> <span class="function"><span class="keyword">func</span> <span class="title">example</span><span class="params">(slice []<span class="keyword">string</span>, str <span class="keyword">string</span>, i <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line"><span class="number">13</span>    <span class="built_in">panic</span>(<span class="string">"Want stack trace"</span>)</span><br><span class="line"><span class="number">14</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>十六进制数+0x39代表了PC偏移量，在example函数里也就是距离函数开头57(10进制)bytes的位置。下面的Listing 3里，你可以通过二进制文件看到example函数的objdump。找到最下面的第12条指令，注意到是它上面一行的指令导致了panic<br><strong>Listing 3</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go tool objdump -S -s <span class="string">"main.example"</span> ./example1</span><br><span class="line">TEXT main.example(SB) stack_trace/example1/example1.go</span><br><span class="line">func example(slice []string, str string, i int) &#123;</span><br><span class="line">  0x104dfa0		65488b0c2530000000	MOVQ GS:0x30, CX</span><br><span class="line">  0x104dfa9		483b6110		CMPQ 0x10(CX), SP</span><br><span class="line">  0x104dfad		762c			JBE 0x104dfdb</span><br><span class="line">  0x104dfaf		4883ec18		SUBQ <span class="variable">$0x18</span>, SP</span><br><span class="line">  0x104dfb3		48896c2410		MOVQ BP, 0x10(SP)</span><br><span class="line">  0x104dfb8		488d6c2410		LEAQ 0x10(SP), BP</span><br><span class="line">	panic(<span class="string">"Want stack trace"</span>)</span><br><span class="line">  0x104dfbd		488d059ca20000	LEAQ runtime.types+41504(SB), AX</span><br><span class="line">  0x104dfc4		48890424		MOVQ AX, 0(SP)</span><br><span class="line">  0x104dfc8		488d05a1870200	LEAQ main.statictmp_0(SB), AX</span><br><span class="line">  0x104dfcf		4889442408		MOVQ AX, 0x8(SP)</span><br><span class="line">  0x104dfd4		e8c735fdff		CALL runtime.gopanic(SB)</span><br><span class="line">  0x104dfd9		0f0b			UD2              &lt;--- LOOK HERE PC(+0x39)</span><br></pre></td></tr></table></figure></p>
<p>注意: PC始终是下一个指令，不是当前指令。Listing 3很好的说明了amd64下面，go线程是如何执行指令序列的。</p>
<h2 id="线程状态"><a href="#线程状态" class="headerlink" title="线程状态"></a><strong>线程状态</strong></h2><p>另一个重要概念就是“线程状态”，线程状态说明了调度器该如何处理此时的线程。线程有三个状态:等待、可运行、执行中。<br><strong>等待(Waiting)：</strong><br>此时意味着线程停止并且等待被唤醒。可能发生的原因有，等待硬件(硬盘、网络)，操作系统(系统调用) 或者是同步调用(atomic,mutexes)。这些情况是导致性能问题的根源<br><strong>可运行(Runnable)：</strong><br>此时线程想要占用内核上的cpu时间来执行分配给线程的指令。如果你有许多线程想要cpu时间，线程必须要等一段时间才能取到cpu时间。随着更多线程争用cpu时间，线程分配的cpu时间会更短。这种情况下的调度延时也会造成性能问题。<br><strong>执行中(Executing):</strong><br>此时线程已经置于内核中，并且正在执行它的机器指令。应用程序的相关内容正在被处理。这种状态是我们所希望的</p>
<h2 id="工作类型"><a href="#工作类型" class="headerlink" title="工作类型"></a><strong>工作类型</strong></h2><p>线程有两种工作类型。第一种叫CPU密集型，第二种叫IO密集型<br><strong>CPU密集型(cpu-bound):</strong><br>这种工作下，线程永远不会被置换到等待(waiting)状态。这种一般是进行持续性的cpu计算工作。比如计算Pi这种的就是cpu密集型工作<br><strong>IO密集型(io-bound)</strong><br>这种工作会让线程进入到等待(waiting)状态。这种情况线程会持续的请求资源（比如网络资源）或者是对操作系统进行系统调用。线程需要访问数据库的情况就是IO密集型工作。同步事件(例如mutexes、atomic)，类似需要线程等待的情况y我也归为此类。<br><strong>上下文切换(Context Switch)</strong><br>如果你的程序运行在Linux、Mac或者是Windows上面，你的调度器则是抢占式的。这意味着，第一、调度器不会预先知道此时此刻会运行哪个线程。线程优先级加上事务(例如接受网络数据)让调度器无法确定哪个时间执行哪个线程。<br>第二、永远不能按照历史经验去看，之前跑出来的代码其实不能保证每次都按你所想去执行。如果你的代码1000次都是按照同样方式执行，你会以为下次也保证按照一样方式执行。如果你的程序需要确定性的话，你一定要控制线程的同步和编排。<br>在内核上切换线程的物理行为叫做上下文切换(context switch)。上下文切换发生情形如下，调度器从内核换下正在执行的线程，替换上可执行的线程。线程是从运行队列中取出，并设置成执行中(Executing)的状态。从内核上取下来的线程会置成可运行状态,或者是等待状态。<br>上下文切换的代价是昂贵的，因为需要花时间去交换线程，从内核上拿下来再放上去。上下文切换的延时受到很多因素影响，但是通常情况下，它会有1000–1500纳秒的延时。考虑到硬件上每个内核上平均每纳秒执行12个指令，一次上下文切换会花费你12k–18k个指令延时。这本质上来说，你的程序在上下文切换过程中失去了执行大量指令的机会。<br>如果你的程序集中于IO密集型(cpu-bound)的工作，上下文切换会相对有利。一旦一个线程进入到等待(waiting)状态。另一个处于可运行(Runnable)状态的线程会取代它的位置。这会使得内核始终是处于工作状态。这是调度器调度的一个重要层面，如果有事做(有线程处于可运行状态)就不允许内核闲下来。<br>如果你的程序集中于cpu密集型(cpu-bound)的工作，那么上下文切换会是性能的噩梦。因为线程要一直做事情，上下文切换会停止正在处理的工作。这种情况和IO密集型形工作成鲜明对比。</p>
<h2 id="少即是多-Less-Is-More"><a href="#少即是多-Less-Is-More" class="headerlink" title="少即是多(Less Is More)"></a><strong>少即是多(Less Is More)</strong></h2><p>在早期时候，处理器仅仅只有一个内核，调度器并不十分复杂。因为你有一个单独的处理器，一个单独的内核，所以任何时间只能跑一个线程。处理方式是定义一个调度期(scheduler period) 然后尝试在一个调度期内去执行所有可运行(Runnable)的线程。这样没问题:把调度期按照需要执行的线程数量去分每一小段。<br>举例，如果你定义了你的调度期是10ms 并且你有两个线程，那每个线程会分到5ms。5个线程的话，每个线程就是2ms。但是如果你有100个线程会怎么样？每个线程时间片是10us(微秒), 这样就会无法工作，因为你需要大量时间去进行上下文切换(context switches)。<br>在另外一个场景，如果最小的时间切片是2ms 并且你有100个线程，调度期需要增加到2000ms也就是2s。要是如果你有1000个线程呢，现在调度期需要20s，也就是你要花20s才能跑完所有的线程如果每个线程都能跑满它的时间切片。<br>上面场景都是显而易见的事情。调度器在做决定的时候还要考虑到更多的因素。你控制了应用程序里的线程数量，当有更多线程的时候，并且是IO密集(IO-Bound)工作，就会有更多的混乱和不确定行为发生，调度和执行就花费更多时间。<br>这也是为什么说游戏规则就是“少即是多(Less is More)”，可运行线程越少意味着调度时间越少，线程得到的时间越多。更多的线程就意味着每个线程获得的时间就越少，分配的时间内做的事情也就越少。</p>
<h2 id="找到平衡点"><a href="#找到平衡点" class="headerlink" title="找到平衡点"></a><strong>找到平衡点</strong></h2><p>你需要在内核数和你的线程数量两者间，找到一个能够让你的程序获得最好吞吐量的平衡点。想要去找到这样的平衡点，线程池是一个很好的选择。<br>使用go之前，原作者在NT系统上使用C++和c#。在那个操作系统里，使用IOCP(IO Completion Ports) 线程池对于写多线程软件十分重要。作为一个工程师，你需要计算出你要用多少个线程池，以及每个线程池的最大线程数，从而在确定了内核数的系统里最大化你的吞吐量。<br>当写web服务时候，你需要和数据库通信。3是一个魔法数字，每个内核设置3个线程似乎在NT上有最好的吞吐量。换句话说，每内核3线程能够最小化上下文切换的延时，最大化在内核上的执行时间。当你创建一个IOPC线程池，我知道我可以在主机上设置每个内核1–3个线程数量。<br>如果我使用2个线程每个内核，完成工作的时间会变长，因为本来需要有工作去做的内核会有空闲时间。如果我每个内核用4个线程，也会花更长时间，因为我需要花更多时间进行上下文切换。平衡数字3，不管是什么原因，似乎在NT上都是一个神奇的数字。<br>当你的服务需要处理许多不同类型的工作会如何呢。那会有不同并且不一致的延迟。可能它会产生许多需要去处理的不同系统级别的事件。这种情况，你不可能去找到一个魔法数字，能让你在所有时间所有不同的工作情况下都有优秀的性能。当你使用线程池的时候，找到一个合适的配置会十分复杂。</p>
<h2 id="缓存行-Cache-Lines"><a href="#缓存行-Cache-Lines" class="headerlink" title="缓存行(Cache Lines)"></a><strong>缓存行(Cache Lines)</strong></h2><p>从主存访问数据有很高的延迟（大概100~300个时钟周期），因此处理器和内核会有缓存，能够让线程访问到更近的数据。从缓存访问数据的延迟非常低(大概3~40个时钟周期) 根据不同的缓存访问方式。衡量性能的一个方面就是，处理器通过减少数据访问延时而获取数据的效率。编写多线程的应用程序需要考虑到机器的缓存系统。<br><img src="https://user-gold-cdn.xitu.io/2019/5/17/16ac61a997fe6ead?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>处理器和主存使用缓存行(cache lines)进行数据交换。一个缓存行是一个64 byte的内存块，它在内存和缓存系统之间进行交换。每个内核会分配它自己需要的cache副本。这也是为什么多线程中的内存突变会造成严重的性能问题。<br>当多线程并行运行，正在访问相同数据，甚至是相邻的数据单元，他们会访问相同的缓存行。任何内核上运行的任何线程能够从相同的缓存行获取各自的拷贝。<br><img src="https://user-gold-cdn.xitu.io/2019/5/17/16ac61ad13385da9?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>如果内核上面的线程修改它的cache行副本，在硬件的操作下，同一cache行的所有其他副本都会被标记为无效。当一个线程尝试读写无效cache行，需要重新访问主存去获取新的cache行副本(大约要100~300个时钟周期)<br>也许在2核的处理器上这不是大问题，但是如果是一个32核处理器并行跑32个线程，并且同时访问和修改一个相同的cache行呢？由于处理器到处理器之间的通信延迟增加，情况会更糟。程序内存会发生颠簸，性能变得很差，而且很可能你也不知道问题的所在。<br>这就是cache的一致性问题（ cache-coherency problem ）或者是说是共享失败（false sharing）。当编写改变共享状态的多线程应用时，cache系统必须要考虑在内。</p>
<h2 id="调度决策场景"><a href="#调度决策场景" class="headerlink" title="调度决策场景"></a><strong>调度决策场景</strong></h2><p>思考一下下面的调度场景。<br>应用程序启动，主线程已经在core1上启动。当线程正在执行，它为了访问数据需要去检索cache行。主线程现在为了某些并发处理创建一个新的线程。那么问题来了。<br>一旦线程创建好，并且准备要运行了，那么调度器是否应该:</p>
<ol>
<li>从core1上换下主线程？这样做有助于提高性能，因为这个新线程需要的相同数据被缓存的可能性非常大。但是主线程并没有得到它的全部时间片。</li>
<li>线程是否要一直等待直到main主线程完成它的时间后core1可用？线程并没有在运行，但是一旦运行它获取数据的延时将会消除。</li>
<li>线程等待下一个可用的core？这意味着所选择的core的cache行会经历冲刷、检索、复制，从而导致延迟。但是线程会更快的启动，并且主线程会完成它的时间片。</li>
</ol>
<p>以上都是调度器在做决定时需要考虑到的事情。<br><strong>结论</strong><br>这是第一部分，为你提供了一些多线程编程时要考虑到线程和OS调度器的一些理解。这同时也是golang调度器需要考虑的事情。下面一部分，会讲Go调度器的一些相关知识。</p>
<p>原文链接:<a href="https://www.ardanlabs.com/blog/2018/08/scheduling-in-go-part1.html" rel="external nofollow noopener noreferrer" target="_blank">www.ardanlabs.com/blog/2018/0…</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go调度</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go调度</tag>
      </tags>
  </entry>
  <entry>
    <title>先睹为快，Go2 Error 的挣扎之路</title>
    <url>/go/%E7%9F%A5%E8%AF%86%E7%82%B9/%E5%85%88%E7%9D%B9%E4%B8%BA%E5%BF%AB%EF%BC%8CGo2%20Error%20%E7%9A%84%E6%8C%A3%E6%89%8E%E4%B9%8B%E8%B7%AF/</url>
    <content><![CDATA[<h2 id="先睹为快，Go2-Error-的挣扎之路"><a href="#先睹为快，Go2-Error-的挣扎之路" class="headerlink" title="先睹为快，Go2 Error 的挣扎之路"></a>先睹为快，Go2 Error 的挣扎之路</h2><p><a href="https://mp.weixin.qq.com/s/XILveKzh07BOQnqxYDKQsA" rel="external nofollow noopener noreferrer" target="_blank">转载自煎鱼的blog</a></p>
<p>自从 Go 语言在国内火热以来，除去泛型，其次最具槽点的就是 Go 对错误的处理方式，一句经典的 <code>if err != nil</code> 暗号就能认出你是一个 Go 语言爱好者。</p>
<p><img src="1.png" alt="图片">err 里藏的是什么</p>
<p>自然，大家对 Go error 的关注度更是高涨，Go team 也是，因此在 Go 2 Draft Designs 中正式提到了 error handling（错误处理）的相关草案，希望能够在未来正式的解决这个问题。</p>
<p>在今天这篇文章中，我们将一同跟踪 Go2 error，看看他是怎么 “挣扎” 的，能不能破局？</p>
<h2 id="为什么要吐槽-Go1"><a href="#为什么要吐槽-Go1" class="headerlink" title="为什么要吐槽 Go1"></a>为什么要吐槽 Go1</h2><p>要吐槽 Go1 error，就得先知道为什么大家到底是在喷 Error 哪里处理的不好。在 Go 语言中，error 其实本质上只是个 Error 的 <code>interface</code>：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">type <span class="builtin-name">error</span><span class="built_in"> interface </span>&#123;</span><br><span class="line">    <span class="builtin-name">Error</span>() string</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>实际的应用场景如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> x, err := foo()</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="comment">// handle error</span></span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>单纯的看这个例子似乎没什么问题，但工程大了后呢？</p>
<p>显然 <code>if err != nil</code> 的逻辑是会堆积在工程代码中，Go 代码里的 <code>if err != nil</code> 甚至会达到工程代码量的 30% 以上：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> x, err := foo()</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="comment">// handle error</span></span><br><span class="line"> &#125;</span><br><span class="line"> y, err := foo()</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="comment">// handle error</span></span><br><span class="line"> &#125;</span><br><span class="line"> z, err := foo()</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="comment">// handle error</span></span><br><span class="line"> &#125;</span><br><span class="line"> s, err := foo()</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="comment">// handle error</span></span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>暴力的对比一下，就发现四行函数调用，十二行错误，还要苦练且精通 IDE 的快速折叠功能，还是比较麻烦的。</p>
<p>另外既然是错误处理，那肯定不单单是一个 <code>return err</code> 了。在工程实践中，项目代码都是层层嵌套的，如果直接写成：</p>
<figure class="highlight hsp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line"> <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在实际工程中肯定是不行。你怎么知道具体是哪里抛出来的错误信息，实际出错时只能瞎猜。大家又想出了 PlanB，那就是加各种描述信息：</p>
<figure class="highlight hsp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line"> logger.Errorf(<span class="string">"煎鱼报错 err：%v"</span>, <span class="keyword">err</span>)</span><br><span class="line"> <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虽然看上去人模人样的，在实际出错时，也会遇到新的问题，因为你要去查这个错误是从哪里抛出来的，没有调用堆栈，单纯几句错误描述是难以定位的。</p>
<p>这时候就会发展成<strong>到处打错误日志</strong>：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> err := bar()</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  logger.Errorf(<span class="string">"bar err：%v"</span>, err)</span><br><span class="line"> &#125;</span><br><span class="line"> ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">bar</span><span class="params">()</span> <span class="title">error</span></span> &#123;</span><br><span class="line"> _, err := foo()</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  logger.Errorf(<span class="string">"foo err：%v"</span>, err)</span><br><span class="line">  <span class="keyword">return</span> err</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">foo</span><span class="params">()</span> <span class="params">([]<span class="keyword">byte</span>, error)</span></span> &#123;</span><br><span class="line"> s, err := json.Marshal(<span class="string">"hello world."</span>)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  logger.Errorf(<span class="string">"json.Marshal err：%v"</span>, err)</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">nil</span>, err</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> s, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虽然到处打了日志，就会变成错误日志非常多，一旦出问题，人肉可能短时间内识别不出来。</p>
<p>最常见的就是到 IDE 上 <code>ctrl + f</code> 搜索是在哪出错。同时在实际应用中我们会自定义一些错误类型，在 Go 则需要各种判断和处理：</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="keyword">err</span> := <span class="keyword">dec</span>.<span class="keyword">Decode</span>(&amp;val); <span class="keyword">err</span> != nil &#123;</span><br><span class="line">    <span class="keyword">if</span> serr, ok := <span class="keyword">err</span>.(*json.SyntaxError); ok &#123;</span><br><span class="line">       ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>你得判断不等于 <code>nil</code>，还得对自定义的错误类型进行断言，整体来讲比较繁琐。</p>
<p>汇总来讲，Go1 错误处理的问题至少有：</p>
<ul>
<li>在工程实践中，<code>if err != nil</code> 写的烦，代码中一大堆错误处理的判断，占了相当的比例，不够优雅。</li>
<li>在排查问题时，Go 的 <code>err</code> 并没有其他堆栈信息，只能自己增加描述信息，层层叠加，打一大堆日志，排查很麻烦。</li>
<li>在验证和测试错误时，要自定义错误（各种判断和断言）或者被迫用字符串校验。</li>
</ul>
<h2 id="Go1-13-的挽尊"><a href="#Go1-13-的挽尊" class="headerlink" title="Go1.13 的挽尊"></a>Go1.13 的挽尊</h2><p>在 2019 年 09 月，Go1.13 正式发布。其中两个比较大的两个关注点分别是包依赖管理 Go modules 的转正，以及错误处理 errors 标准库的改进：</p>
<p><img src="2.png" alt="图片2">Error wrapping</p>
<p>在本次改进中，errors 标准库引入了 Wrapping Error 的概念，并增加了 Is/As/Unwarp 三个方法，用于对所返回的错误进行二次处理和识别。</p>
<p>同时也是将 Go2 error 预规划中没有破坏 Go1 兼容性的相关功能提前实现了。</p>
<p>简单来讲，Go1.13 后 Go 的 error 就可以嵌套了，并提供了三个配套的方法。例子：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">func</span> <span class="selector-tag">main</span>() &#123;</span><br><span class="line"> <span class="attribute">e </span>:= errors.<span class="built_in">New</span>(<span class="string">"脑子进煎鱼了"</span>)</span><br><span class="line"> w := fmt.<span class="built_in">Errorf</span>(<span class="string">"快抓住：%w"</span>, e)</span><br><span class="line"> fmt.<span class="built_in">Println</span>(w)</span><br><span class="line"> fmt.<span class="built_in">Println</span>(errors.Unwrap(w))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>输出结果：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">$ <span class="keyword">go</span> run main.<span class="keyword">go</span></span><br><span class="line">快抓住：脑子进煎鱼了</span><br><span class="line">脑子进煎鱼了</span><br></pre></td></tr></table></figure>
<p>在上述代码中，变量 <code>w</code> 就是一个嵌套一层的 error。最外层是 “快抓住：”，此处调用 <code>%w</code> 意味着 Wrapping Error 的嵌套生成。因此最终输出了 “快抓住：脑子进煎鱼了”。</p>
<p>需要注意的是，Go 并没有提供 <code>Warp</code> 方法，而是直接扩展了 <code>fmt.Errorf</code> 方法。而下方的输出由于直接调用了 <code>errors.Unwarp</code> 方法，因此将 “取” 出一层嵌套，最终直接输出 “脑子进煎鱼了”。</p>
<p>对 Wrapping Error 有了基本理解后，我们简单介绍一下三个配套方法：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Is</span><span class="params">(err, target error)</span> <span class="title">bool</span></span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">As</span><span class="params">(err error, target <span class="keyword">interface</span>&#123;&#125;)</span> <span class="title">bool</span></span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Unwrap</span><span class="params">(err error)</span> <span class="title">error</span></span></span><br></pre></td></tr></table></figure>
<h3 id="errors-Is"><a href="#errors-Is" class="headerlink" title="errors.Is"></a>errors.Is</h3><p>方法签名：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Is</span><span class="params">(err, target error)</span> <span class="title">bool</span></span></span><br></pre></td></tr></table></figure>
<p>方法例子：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> <span class="keyword">if</span> _, err := os.Open(<span class="string">"non-existing"</span>); err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> errors.Is(err, os.ErrNotExist) &#123;</span><br><span class="line">   fmt.Println(<span class="string">"file does not exist"</span>)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">   fmt.Println(err)</span><br><span class="line">  &#125;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>errors.Is</code> 方法的作用是判断所传入的 err 和 target 是否同一类型，如果是则返回 true。</p>
<h3 id="errors-As"><a href="#errors-As" class="headerlink" title="errors.As"></a>errors.As</h3><p>方法签名：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">As</span><span class="params">(err error, target <span class="keyword">interface</span>&#123;&#125;)</span> <span class="title">bool</span></span></span><br></pre></td></tr></table></figure>
<p>方法例子：</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line"> <span class="keyword">if</span> _, <span class="keyword">err</span> := os.<span class="keyword">Open</span>(<span class="string">"non-existing"</span>); <span class="keyword">err</span> != nil &#123;</span><br><span class="line">  <span class="keyword">var</span> pathError *os.PathError</span><br><span class="line">  <span class="keyword">if</span> errors.<span class="keyword">As</span>(<span class="keyword">err</span>, &amp;pathError) &#123;</span><br><span class="line">   fmt.Println(<span class="string">"Failed at path:"</span>, pathError.Path)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">   fmt.Println(<span class="keyword">err</span>)</span><br><span class="line">  &#125;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>errors.As</code> 方法的作用是从 err 错误链中识别和 target 相同的类型，如果可以赋值，则返回 true。</p>
<h3 id="errors-Unwarp"><a href="#errors-Unwarp" class="headerlink" title="errors.Unwarp"></a>errors.Unwarp</h3><p>方法签名：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Unwrap</span><span class="params">(err error)</span> <span class="title">error</span></span></span><br></pre></td></tr></table></figure>
<p>方法例子：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">func</span> <span class="selector-tag">main</span>() &#123;</span><br><span class="line"> <span class="attribute">e </span>:= errors.<span class="built_in">New</span>(<span class="string">"脑子进煎鱼了"</span>)</span><br><span class="line"> w := fmt.<span class="built_in">Errorf</span>(<span class="string">"快抓住：%w"</span>, e)</span><br><span class="line"> fmt.<span class="built_in">Println</span>(w)</span><br><span class="line"> fmt.<span class="built_in">Println</span>(errors.Unwrap(w))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>该方法的作用是将嵌套的 error 解析出来，若存在多级嵌套则需要调用多次 Unwarp 方法。</p>
<h2 id="民间自救-pkg-errors"><a href="#民间自救-pkg-errors" class="headerlink" title="民间自救 pkg/errors"></a>民间自救 pkg/errors</h2><p>Go1 的 error 处理固然存在许多问题，因此在 Go1.13 前，早已有 “民间” 发现没有上下文调试信息在实际工程应用中存在严重的体感问题。</p>
<p>因此 <code>github.com/pkg/errors</code> 在 2016 年诞生了，该库也已经受到了极大的关注。</p>
<p>官方例子如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> stackTracer <span class="keyword">interface</span> &#123;</span><br><span class="line">    StackTrace() errors.StackTrace</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">err, ok := errors.Cause(fn()).(stackTracer)</span><br><span class="line"><span class="keyword">if</span> !ok &#123;</span><br><span class="line">    <span class="built_in">panic</span>(<span class="string">"oops, err does not implement stackTracer"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">st := err.StackTrace()</span><br><span class="line">fmt.Printf(<span class="string">"%+v"</span>, st[<span class="number">0</span>:<span class="number">2</span>]) <span class="comment">// top two frames</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Example output:</span></span><br><span class="line"><span class="comment">// github.com/pkg/errors_test.fn</span></span><br><span class="line"><span class="comment">// /home/dfc/src/github.com/pkg/errors/example_test.go:47</span></span><br><span class="line"><span class="comment">// github.com/pkg/errors_test.Example_stackTrace</span></span><br><span class="line"><span class="comment">// /home/dfc/src/github.com/pkg/errors/example_test.go:127</span></span><br></pre></td></tr></table></figure>
<p>简单来讲，就是对 Go1 error 的上下文处理进行了优化和处理，例如类型断言、调用堆栈等。若有兴趣的小伙伴可以自行到 <code>github.com/pkg/errors</code> 进行学习。</p>
<p>另外你可能会发现 Go1.13 新增的 Wrapping Error 体系与 <code>pkg/errors</code> 有些相像。</p>
<p>你并没有体会错，Go team 接纳了相关的意见，对 Go1 进行了调整，但调用堆栈这块因综合原因暂时没有纳入。</p>
<h2 id="Go2-error-要解决什么问题"><a href="#Go2-error-要解决什么问题" class="headerlink" title="Go2 error 要解决什么问题"></a>Go2 error 要解决什么问题</h2><p>在前面我们聊了 Go1 error 的许多问题，以及 Go1.13 和 <code>pkg/errors</code> 的自救和融合。你可能会疑惑，那…Go2 error 还有出场的机会吗？即使 Go1 做了这些事情，Go1 error 还有问题吗？</p>
<p>并没有解决，<code>if err != nil</code> 依旧一把梭，目前社区声音依然认为 Go 语言的错误处理要改进。</p>
<h2 id="Go2-error-proposal"><a href="#Go2-error-proposal" class="headerlink" title="Go2 error proposal"></a>Go2 error proposal</h2><p>在 2018 年 8 月，官方正式公布了 Go 2 Draft Designs，其中包含泛型和错误处理机制改进的初步草案：</p>
<p><img src="3.png" alt="图片">Go2 Draft Designs</p>
<p>注：Go1.13 正式将一些不破坏 Go1 兼容性的 Error 特性加入到了 main branch，也就是前面提到的 Wrapping Error。</p>
<h3 id="错误处理（Error-Handling）"><a href="#错误处理（Error-Handling）" class="headerlink" title="错误处理（Error Handling）"></a>错误处理（Error Handling）</h3><p>第一个要解决的问题就是大量 <code>if err != nil</code> 的问题，针对此提出了 Go2 error handling 的草案设计。</p>
<p>简单例子：</p>
<figure class="highlight hsp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line"> <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>优化后的方案如下：</p>
<figure class="highlight smali"><table><tr><td class="code"><pre><span class="line">func CopyFile(src, dst string) error &#123;</span><br><span class="line"> handle err &#123;</span><br><span class="line"> <span class="built_in"> return </span>fmt.Errorf(<span class="string">"copy %s %s: %v"</span>, src, dst, err)</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> r<span class="keyword"> :</span>=<span class="built_in"> check </span>os.Open(src)</span><br><span class="line"> defer r.Close()</span><br><span class="line"></span><br><span class="line"> w<span class="keyword"> :</span>=<span class="built_in"> check </span>os.Create(dst)</span><br><span class="line"> handle err &#123;</span><br><span class="line">  w.Close()</span><br><span class="line">  os.Remove(dst) // (only<span class="built_in"> if </span>a<span class="built_in"> check </span>fails)</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in"> check </span>io.Copy(w, r)</span><br><span class="line"><span class="built_in"> check </span>w.Close()</span><br><span class="line"><span class="built_in"> return </span>nil</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>主函数：</p>
<figure class="highlight smali"><table><tr><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line"> handle err &#123;</span><br><span class="line">  log.Fatal(err)</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> hex<span class="keyword"> :</span>=<span class="built_in"> check </span>ioutil.ReadAll(os.Stdin)</span><br><span class="line"> data<span class="keyword"> :</span>=<span class="built_in"> check </span>parseHexdump(string(hex))</span><br><span class="line"> os.Stdout.Write(data)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>该提案引入了两种新的语法形式，首先是 <code>check</code> 关键字，其可以选中一个表达式 <code>check f(x, y, z)</code> 或 <code>check err</code>，其将会标识这是一个显式的错误检查。</p>
<p>其次引入了 <code>handle</code> 关键字，用于定义错误处理程序流转，逐级上抛，依此类推，直到处理程序执行 <code>return</code> 语句，才正式结束。</p>
<h3 id="错误值打印（Error-Printing）"><a href="#错误值打印（Error-Printing）" class="headerlink" title="错误值打印（Error Printing）"></a>错误值打印（Error Printing）</h3><p>第二个要解决的问题是错误值（Error Values）、错误检查（Error Inspection）的问题，其引申出错误值打印（Error Printing）的问题，也可以认为是错误格式化的不便利。</p>
<p>官方针对此提出了提出了 Error Values 和 Error Printing 的草案设计。</p>
<p>简单例子如下：</p>
<figure class="highlight hsp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line"> <span class="keyword">return</span> fmt.Errorf(<span class="string">"write users database: %v"</span>, <span class="keyword">err</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>优化后的方案如下：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">package errors</span><br><span class="line"></span><br><span class="line">type Wrapper<span class="built_in"> interface </span>&#123;</span><br><span class="line"> Unwrap() error</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func Is(err, target error) bool</span><br><span class="line">func As(type E)(err error) (e E, ok bool)</span><br></pre></td></tr></table></figure>
<p>该提案增加了错误链的 Wrapping Error 概念，并同时增加 <code>errors.Is</code> 和 <code>errors.As</code> 的方法，与前面说到的 Go1.13 的改进一致，不再赘述。</p>
<p>需要留意的是，Go1.13 并没有实现 <code>%+v</code> 输出调用堆栈的需求，因为此举会破坏 Go1 兼容性和产生一些性能问题，大概会在 Go2 加入。</p>
<h2 id="try-catch-不香吗"><a href="#try-catch-不香吗" class="headerlink" title="try-catch 不香吗"></a>try-catch 不香吗</h2><p>社区中另外一股声音就是直指 Go 语言反人类不用 <code>try-catch</code> 的机制，在社区内也产生了大量的探讨，具体可以看看相关的提案 Proposal: A built-in Go error check function, “try”。</p>
<p>目前该提案已被拒绝，具体可参见 go/issues/32437#issuecomment-512035919 和 Why does Go not have exceptions。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在这篇文章中，我们介绍了目前 Go1 Error 的现状，概括了大家对 Go 语言错误处理的常见问题和意见。</p>
<p>同时还介绍了在这几年间，Go team 针对 Go2、Go1.13 Error 的持续优化和探索。</p>
<p>欢迎评论区留言：<strong>现在 Go2 error proposal 你是否认可？为什么？</strong></p>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul>
<li>全成大佬的Golang error 的突围</li>
<li>为什么 Go 语言的 Error Handling 是一个败笔</li>
<li>Go语言(golang)新发布的1.13中的Error Wrapping深度分析</li>
</ul>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>必须要学的 Go 进程诊断工具 gops</title>
    <url>/go/%E7%9F%A5%E8%AF%86%E7%82%B9/%E5%BF%85%E9%A1%BB%E8%A6%81%E5%AD%A6%E7%9A%84%20Go%20%E8%BF%9B%E7%A8%8B%E8%AF%8A%E6%96%AD%E5%B7%A5%E5%85%B7%20gops/</url>
    <content><![CDATA[<p><a href="https://mp.weixin.qq.com/s?__biz=MzUxMDI4MDc1NA==&amp;mid=2247484471&amp;idx=1&amp;sn=f61bd753ddc80e475b71a846bba799fa&amp;chksm=f904116ace73987c48312fe6c01dfab3c7705d8e3b172986ccf1efe00d4bff630a366cfb766f&amp;scene=178&amp;cur_album_id=1515516076481101825#rd" rel="external nofollow noopener noreferrer" target="_blank">转载自煎鱼的blog</a></p>
<h2 id="必须要学的-Go-进程诊断工具-gops"><a href="#必须要学的-Go-进程诊断工具-gops" class="headerlink" title="必须要学的 Go 进程诊断工具 gops"></a>必须要学的 Go 进程诊断工具 gops</h2><p>在类 Unix 系统中，我们常常会使用 ps 命令来查看系统当前所运行的进程信息，该命令为我们提供了较大的帮助，能够快速的定位到某些进程的运行情况和状态。</p>
<p>而在 Go 语言中，也有类似的命令工具，那就是 <strong>gops</strong>[1]（Go Process Status）。</p>
<p>gops 是由 Google 官方出品的一个命令行工具，与 ps 命令的功能类似，能够查看并诊断当前系统中 Go 程序的运行状态及内部情况，在一些使用场景中具有较大的存在意义，属于常用工具。</p>
<p>在本文中我们将对 gops 进行全面的使用和介绍。</p>
<p><strong>基本使用</strong></p>
<p>我们先创建一个示例项目，然后在项目根目录执行下述模块安装命令：</p>
<figure class="highlight vim"><table><tr><td class="code"><pre><span class="line">$ <span class="keyword">go</span> <span class="built_in">get</span> -<span class="keyword">u</span> github.<span class="keyword">com</span>/google/gops</span><br></pre></td></tr></table></figure>
<p>写入如下启动代码：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> (</span><br><span class="line"> ...</span><br><span class="line"> <span class="string">"github.com/google/gops/agent"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> <span class="comment">// 创建并监听 gops agent，gops 命令会通过连接 agent 来读取进程信息</span></span><br><span class="line"> <span class="comment">// 若需要远程访问，可配置 agent.Options&#123;Addr: "0.0.0.0:6060"&#125;，否则默认仅允许本地访问</span></span><br><span class="line"> <span class="keyword">if</span> err := agent.Listen(agent.Options&#123;&#125;); err != <span class="literal">nil</span> &#123;</span><br><span class="line">  log.Fatalf(<span class="string">"agent.Listen err: %v"</span>, err)</span><br><span class="line"> &#125;</span><br><span class="line"> </span><br><span class="line"> http.HandleFunc(<span class="string">"/hello"</span>, <span class="function"><span class="keyword">func</span><span class="params">(w http.ResponseWriter, r *http.Request)</span></span> &#123;</span><br><span class="line">  _, _ = w.Write([]<span class="keyword">byte</span>(<span class="string">`Go语言编程之旅`</span>))</span><br><span class="line"> &#125;)</span><br><span class="line"> _ := http.ListenAndServe(<span class="string">":6060"</span>, http.DefaultServeMux)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在完成示例启动代码的写入后，我们启动该程序，并在命令行执行 <code>gops</code> 命令进行查看：</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="number">3739</span>  <span class="number">3725</span>  main  * go1<span class="number">.14</span>   <span class="regexp">/private/</span>var<span class="regexp">/folders/</span>jm<span class="regexp">/.../</span>b001<span class="regexp">/exe/</span>main</span><br><span class="line"><span class="number">3725</span>  <span class="number">71093</span> go      go1<span class="number">.14</span>   <span class="regexp">/usr/</span>local<span class="regexp">/Cellar/</span>go<span class="regexp">/1.14/</span>libexec<span class="regexp">/bin/</span>go</span><br><span class="line"><span class="number">62357</span> <span class="number">46131</span> go      go1<span class="number">.14</span>   <span class="regexp">/usr/</span>local<span class="regexp">/Cellar/</span>go<span class="regexp">/1.14/</span>libexec<span class="regexp">/bin/</span>go</span><br><span class="line"><span class="number">3872</span>  <span class="number">3742</span>  gops    go1<span class="number">.14</span>   <span class="regexp">/Users/</span>eddycjy<span class="regexp">/go/</span>bin/gops</span><br><span class="line"><span class="number">62379</span> <span class="number">62357</span> main    go1<span class="number">.14</span>   <span class="regexp">/private/</span>var<span class="regexp">/folders/</span>jm<span class="regexp">/.../</span>b001<span class="regexp">/exe/</span>main</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<p>在上述输出中，你很快就发现有一点不一样，那就是为什么某一行的输出结果中会包含一个 <code>*</code> 符号，如下：</p>
<figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">3739 </span> <span class="number">3725</span>  main  * go1.<span class="number">14</span>   /private/var/folders/jm/.../b001/exe/main</span><br></pre></td></tr></table></figure>
<p>这实际上代表着该 Go 进程，包含了 <code>agent</code>，因此它可以启用更强大的诊断功能，包括当前堆栈跟踪，Go版本，内存统计信息等等。</p>
<p>在最后也有一个 main 的 Go 进程，它不包含 <code>*</code> 符号，这意味着它是一个普通的 Go 程序，也就是没有植入 <code>agent</code>，只能使用最基本的功能。</p>
<h2 id="常规命令"><a href="#常规命令" class="headerlink" title="常规命令"></a>常规命令</h2><p>gops 工具包含了大量的分析命令，我们可以通过 <code>gops help</code> 进行查看：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">$ gops help</span><br><span class="line">gops is a<span class="built_in"> tool </span><span class="keyword">to</span> list <span class="keyword">and</span> diagnose Go processes.</span><br><span class="line"></span><br><span class="line">Usage:</span><br><span class="line">  gops &lt;cmd&gt; &lt;pid|addr&gt; <span class="built_in">..</span>.</span><br><span class="line">  gops &lt;pid&gt; # displays process info</span><br><span class="line">  gops help  # displays this help message</span><br><span class="line"></span><br><span class="line">Commands:</span><br><span class="line">  stack      Prints the stack trace.</span><br><span class="line">  gc         Runs the garbage collector <span class="keyword">and</span> blocks until successful.</span><br><span class="line">  setgc      Sets the garbage collection target percentage.</span><br><span class="line">  memstats   Prints the allocation <span class="keyword">and</span> garbage collection stats.</span><br><span class="line">  version    Prints the Go version used <span class="keyword">to</span> build the program.</span><br><span class="line">  stats      Prints runtime stats.</span><br><span class="line">  trace      Runs the runtime tracer <span class="keyword">for</span> 5 secs <span class="keyword">and</span> launches <span class="string">"go tool trace"</span>.</span><br><span class="line">  pprof-heap Reads the heap<span class="built_in"> profile </span><span class="keyword">and</span> launches <span class="string">"go tool pprof"</span>.</span><br><span class="line">  pprof-cpu  Reads the CPU<span class="built_in"> profile </span><span class="keyword">and</span> launches <span class="string">"go tool pprof"</span>.</span><br></pre></td></tr></table></figure>
<p>在接下来的小节中，我们将针对几个常用的分析功能进行概要分析。</p>
<h3 id="查看指定进程信息"><a href="#查看指定进程信息" class="headerlink" title="查看指定进程信息"></a>查看指定进程信息</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="string">$</span> <span class="string">gops</span> <span class="string">&lt;pid&gt;</span></span><br><span class="line"><span class="attr">parent PID:</span> <span class="number">3725</span></span><br><span class="line"><span class="attr">threads:</span> <span class="number">7</span></span><br><span class="line"><span class="attr">memory usage:</span> <span class="number">0.042</span><span class="string">%</span></span><br><span class="line"><span class="attr">cpu usage:</span> <span class="number">0.003</span><span class="string">%</span></span><br><span class="line"><span class="attr">username:</span> <span class="string">eddycjy</span></span><br><span class="line"><span class="string">cmd+args:</span> <span class="string">/var/folders/jm/pk20jr_s74x49kqmyt87n2800000gn/T/go-build943691423/b001/exe/main</span></span><br><span class="line"><span class="attr">elapsed time:</span> <span class="number">10</span><span class="string">:56</span></span><br><span class="line"><span class="attr">local/remote:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:59369</span> <span class="string">&lt;-&gt;</span> <span class="string">:0</span> <span class="string">(LISTEN)</span></span><br><span class="line"><span class="attr">local/remote:</span> <span class="string">*:6060</span> <span class="string">&lt;-&gt;</span> <span class="string">:0</span> <span class="string">(LISTEN)</span></span><br></pre></td></tr></table></figure>
<p>获取 Go 进程的概要信息，包括父级PID、线程数、内存/CPU使用率、运行者的账户名、进程的启动命令行参数、启动后所经过的时间以及 gops 的 agent 监听信息（若无植入 agent，则没有这项信息）。</p>
<h3 id="查看调用栈信息"><a href="#查看调用栈信息" class="headerlink" title="查看调用栈信息"></a>查看调用栈信息</h3><figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line">$ gops stack <span class="number">3739</span></span><br><span class="line">goroutine <span class="number">19</span> [running]:</span><br><span class="line">runtime/pprof.writeGoroutineStacks(<span class="number">0x1385aa0</span>, <span class="number">0xc000132038</span>, <span class="number">0x30</span>, <span class="number">0xd0</span>)</span><br><span class="line"> ...</span><br><span class="line"> <span class="regexp">/Users/</span>eddycjy<span class="regexp">/go/</span>src<span class="regexp">/github.com/</span>google<span class="regexp">/gops/</span>agent/agent.<span class="string">go:</span><span class="number">185</span> +<span class="number">0x1af</span></span><br><span class="line">github.com<span class="regexp">/google/</span>gops/agent.listen()</span><br><span class="line"> <span class="regexp">/Users/</span>eddycjy<span class="regexp">/go/</span>src<span class="regexp">/github.com/</span>google<span class="regexp">/gops/</span>agent/agent.<span class="string">go:</span><span class="number">133</span> +<span class="number">0x2bf</span></span><br><span class="line">created by github.com<span class="regexp">/google/</span>gops/agent.Listen</span><br><span class="line"> <span class="regexp">/Users/</span>eddycjy<span class="regexp">/go/</span>src<span class="regexp">/github.com/</span>google<span class="regexp">/gops/</span>agent/agent.<span class="string">go:</span><span class="number">111</span> +<span class="number">0x36b</span></span><br><span class="line"></span><br><span class="line">goroutine <span class="number">1</span> [IO wait]:</span><br><span class="line">internal/poll.runtime_pollWait(<span class="number">0x2f55e38</span>, <span class="number">0x72</span>, <span class="number">0x0</span>)</span><br><span class="line"> <span class="regexp">/usr/</span>local<span class="regexp">/Cellar/</span>go<span class="regexp">/1.14/</span>libexec<span class="regexp">/src/</span>runtime/netpoll.<span class="string">go:</span><span class="number">203</span> +<span class="number">0x55</span></span><br><span class="line"> ...</span><br></pre></td></tr></table></figure>
<p>获取对应进程的代码调用堆栈信息，可用于分析调用链路。</p>
<h3 id="查看内存使用情况"><a href="#查看内存使用情况" class="headerlink" title="查看内存使用情况"></a>查看内存使用情况</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="string">$</span> <span class="string">gops</span> <span class="string">memstats</span> <span class="number">3739</span></span><br><span class="line"><span class="attr">alloc:</span> <span class="number">1.</span><span class="string">15MB</span> <span class="string">(1205272</span> <span class="string">bytes)</span></span><br><span class="line"><span class="attr">total-alloc:</span> <span class="number">1.</span><span class="string">15MB</span> <span class="string">(1205272</span> <span class="string">bytes)</span></span><br><span class="line"><span class="attr">sys:</span> <span class="number">69.</span><span class="string">45MB</span> <span class="string">(72827136</span> <span class="string">bytes)</span></span><br><span class="line"><span class="attr">lookups:</span> <span class="number">0</span></span><br><span class="line"><span class="attr">mallocs:</span> <span class="number">644</span></span><br><span class="line"><span class="attr">frees:</span> <span class="number">12</span></span><br><span class="line"><span class="attr">heap-alloc:</span> <span class="number">1.</span><span class="string">15MB</span> <span class="string">(1205272</span> <span class="string">bytes)</span></span><br><span class="line"><span class="attr">heap-sys:</span> <span class="number">63.</span><span class="string">66MB</span> <span class="string">(66748416</span> <span class="string">bytes)</span></span><br><span class="line"><span class="attr">heap-idle:</span> <span class="number">62.</span><span class="string">05MB</span> <span class="string">(65060864</span> <span class="string">bytes)</span></span><br><span class="line"><span class="attr">heap-in-use:</span> <span class="number">1.</span><span class="string">61MB</span> <span class="string">(1687552</span> <span class="string">bytes)</span></span><br><span class="line"><span class="attr">heap-released:</span> <span class="number">62.</span><span class="string">02MB</span> <span class="string">(65028096</span> <span class="string">bytes)</span></span><br><span class="line"><span class="attr">heap-objects:</span> <span class="number">632</span></span><br><span class="line"><span class="string">...</span></span><br></pre></td></tr></table></figure>
<p>获取 Go 在运行时的当前内存使用情况，主要是 <strong>runtime.MemStats</strong>[2] 的相关字段信息。</p>
<h3 id="查看运行时信息"><a href="#查看运行时信息" class="headerlink" title="查看运行时信息"></a>查看运行时信息</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="string">$</span> <span class="string">gops</span> <span class="string">stats</span> <span class="number">3739</span></span><br><span class="line"><span class="attr">goroutines:</span> <span class="number">2</span></span><br><span class="line"><span class="attr">OS threads:</span> <span class="number">8</span></span><br><span class="line"><span class="attr">GOMAXPROCS:</span> <span class="number">4</span></span><br><span class="line"><span class="attr">num CPU:</span> <span class="number">4</span></span><br></pre></td></tr></table></figure>
<p>获取 Go 运行时的基本信息，包括当前的 Goroutine 数量、系统线程、GOMAXPROCS 数值以及当前系统的 CPU 核数。</p>
<h3 id="查看-trace-信息"><a href="#查看-trace-信息" class="headerlink" title="查看 trace 信息"></a>查看 trace 信息</h3><figure class="highlight gams"><table><tr><td class="code"><pre><span class="line"><span class="symbol">$</span> gops trace <span class="number">3739</span></span><br><span class="line"><span class="function"><span class="title">Tracing</span></span> now, will take <span class="number">5</span> secs...</span><br><span class="line">Trace dump saved to: /var/folders/jm/pk20jr_s74x49kqmyt87n2800000gn/T/trace092133110</span><br><span class="line"><span class="function"><span class="title">Parsing</span></span> trace...</span><br><span class="line"><span class="function"><span class="title">Splitting</span></span> trace...</span><br><span class="line">Opening browser. Trace viewer is listening on http:<span class="comment">//127.0.0.1:53811</span></span><br></pre></td></tr></table></figure>
<p>与 <code>go tool trace</code> 作用基本一致。</p>
<h3 id="查看-profile-信息"><a href="#查看-profile-信息" class="headerlink" title="查看 profile 信息"></a>查看 profile 信息</h3><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">$ gops pprof-cpu 3739</span><br><span class="line">Profiling CPU now, will take 30 secs<span class="built_in">..</span>.</span><br><span class="line">Profile dump saved <span class="keyword">to</span>: /var/folders/jm/pk20jr_s74x49kqmyt87n2800000gn/T/profile563685966</span><br><span class="line">Binary file saved <span class="keyword">to</span>: /var/folders/jm/pk20jr_s74x49kqmyt87n2800000gn/T/binary265411413</span><br><span class="line">File: binary265411413</span><br><span class="line">Type: cpu</span><br><span class="line"><span class="built_in">..</span>.</span><br><span class="line">(pprof) </span><br><span class="line"></span><br><span class="line">$ gops pprof-heap 3739</span><br><span class="line">Profile dump saved <span class="keyword">to</span>: /var/folders/jm/pk20jr_s74x49kqmyt87n2800000gn/T/profile967076057</span><br><span class="line">Binary file saved <span class="keyword">to</span>: /var/folders/jm/pk20jr_s74x49kqmyt87n2800000gn/T/binary904879716</span><br><span class="line">File: binary904879716</span><br><span class="line">Type: inuse_space</span><br><span class="line"><span class="built_in">..</span>.</span><br><span class="line">(pprof)</span><br></pre></td></tr></table></figure>
<p>与 <code>go tool pprof</code> 作用基本一致。</p>
<h2 id="你怎么知道我是谁"><a href="#你怎么知道我是谁" class="headerlink" title="你怎么知道我是谁"></a>你怎么知道我是谁</h2><p>在学习了 gops 的使用后，我们突然发现一个问题，那就是 gops 是怎么知道哪些进程是与 Go 相关的进程？</p>
<p>如果是植入了 <code>agent</code> 的应用程序还好说，可以理解为埋入了识别点。但实际情况是，没有植入 <code>agent</code> 的 Go 程序也被识别到了，说明 gops 本身并不是这么实现的，考虑植入<code>agent</code> 应当只是用于诊断信息的拓展使用，并不是一个识别点，那么 gops 到底是怎么发现哪些进程是 Go 相关的呢？</p>
<p>我们回归问题的前置需求，假设我们想知道哪些进程与 Go 相关，那么第一步我们要先知道我们当前系统中都运行了哪些进程，这些记录在哪里有？</p>
<p>认真思考一下，答案也就呼之欲出了，假设是 Linux 相关的系统下，其会将进程所有的相关信息都按照约定的数据结构写入 <code>/proc</code> 目录下，因此我们有充分的怀疑认为 gops 就是从 <code>/proc</code> 目录下读取到相关信息的，源代码如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">PidsWithContext</span><span class="params">(ctx context.Context)</span> <span class="params">([]<span class="keyword">int32</span>, error)</span></span> &#123;</span><br><span class="line"> <span class="keyword">var</span> ret []<span class="keyword">int32</span></span><br><span class="line"></span><br><span class="line"> d, err := os.Open(common.HostProc())</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">nil</span>, err</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">defer</span> d.Close()</span><br><span class="line"></span><br><span class="line"> fnames, err := d.Readdirnames(<span class="number">-1</span>)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">nil</span>, err</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">for</span> _, fname := <span class="keyword">range</span> fnames &#123;</span><br><span class="line">  pid, err := strconv.ParseInt(fname, <span class="number">10</span>, <span class="number">32</span>)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="keyword">continue</span></span><br><span class="line">  &#125;</span><br><span class="line">  ret = <span class="built_in">append</span>(ret, <span class="keyword">int32</span>(pid))</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> ret, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// common.HostProc</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">HostProc</span><span class="params">(combineWith ...<span class="keyword">string</span>)</span> <span class="title">string</span></span> &#123;</span><br><span class="line"> <span class="keyword">return</span> GetEnv(<span class="string">"HOST_PROC"</span>, <span class="string">"/proc"</span>, combineWith...)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在上述代码中，该方法通过调用 <code>os.Open</code> 方法打开了 <code>proc</code> 目录，并利用 <code>Readdirnames</code> 方法对该目录进行了扫描，最终获取到了所有需要 pid，最终完成其使命，返回了所有 pid。</p>
<p>在确定了 gops 是通过扫描 <code>/proc</code> 目录得到的进程信息后，我们又遇到了一个新的疑问点，那就是 gops 是怎么确定这个进程是 Go 进程，又怎么知道它的具体版本信息的呢，源代码如下：</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">func isGo(<span class="keyword">pr</span> ps.Process) (path, <span class="keyword">version</span> string, agent, ok bool, <span class="keyword">err</span> <span class="keyword">error</span>) &#123;</span><br><span class="line"> ...</span><br><span class="line"> path, _ = <span class="keyword">pr</span>.Path()</span><br><span class="line"> <span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line">  <span class="keyword">return</span></span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">var</span> versionInfo goversion.<span class="keyword">Version</span></span><br><span class="line"> versionInfo, <span class="keyword">err</span> = goversion.ReadExe(path)</span><br><span class="line"> <span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line">  <span class="keyword">return</span></span><br><span class="line"> &#125;</span><br><span class="line"> ok = true</span><br><span class="line"> <span class="keyword">version</span> = versionInfo.Release</span><br><span class="line"> pidfile, <span class="keyword">err</span> := internal.PIDFile(<span class="keyword">pr</span>.Pid())</span><br><span class="line"> <span class="keyword">if</span> <span class="keyword">err</span> == nil &#123;</span><br><span class="line">  _, <span class="keyword">err</span> := os.Stat(pidfile)</span><br><span class="line">  agent = <span class="keyword">err</span> == nil</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">return</span> path, <span class="keyword">version</span>, agent, ok, nil</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们可以看到该方法的主要作用是根据扫描 <code>/proc</code> 目录所得到的二进制文件地址中查找相关的标识，用于判断其是否 Go 程序，如果是 Go 程序，那么它将会返回该进程的 pid、二进制文件的名称以及二进制文件的完整存储路径，判断的标识如下：</p>
<figure class="highlight nix"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="attr">name</span> == <span class="string">"runtime.main"</span> || <span class="attr">name</span> == <span class="string">"main.main"</span> &#123;</span><br><span class="line">    <span class="attr">isGo</span> = <span class="literal">true</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> <span class="attr">name</span> == <span class="string">"runtime.buildVersion"</span> &#123;</span><br><span class="line">    <span class="attr">isGo</span> = <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>而关于所编译的 Go 语言的版本，Go 编译器会在二进制文件中打入 <code>runtime.buildVersion</code>标识，这个标识能够快速我们快速识别它的编译信息，而 gops 也正正是利用了这一点。</p>
<p>我们可以利用 gdb 来进行查看 Go 所编译的二进制文件的版本信息，如下：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">$ <span class="builtin-name">export</span> <span class="attribute">GOFLAGS</span>=<span class="string">"-ldflags=-compressdwarf=false"</span> &amp;&amp; go build .</span><br><span class="line"></span><br><span class="line">$ gdb awesomeProject </span><br><span class="line"><span class="built_in">..</span>.</span><br><span class="line">(gdb) p <span class="string">'runtime.buildVersion'</span></span><br><span class="line"><span class="variable">$1</span> = 0x131bbb0 <span class="string">"go1.14"</span></span><br></pre></td></tr></table></figure>
<p>在上述输出中，我们先对示例项目进行了编译，然后利用 gdb 中查看了 <code>runtime.buildVersion</code> 变量，最终可得知编译这个 Go 程序的版本是 Go1.14。</p>
<p>但在编译时，有一点需要注意，就是我们在编译时指定了 <code>export GOFLAGS=&quot;-ldflags=-compressdwarf=false&quot;</code> 参数。</p>
<p>如果不进行指定的话，就会出现 <code>Reading symbols from awesomeProject...(no debugging symbols found)...done.</code> 的相关报错，会将会影响部分功能使用。</p>
<p>这是因为在 Go1.11 版本开始，进行了调试信息的压缩，目的是为了减小所编译的二进制文件大小，但 Mac 上的 gdb 无法理解压缩的 DWARF，因此会产生问题。</p>
<p>需要进行指定在调试时不进行 DWARF 的压缩，便于 Mac 上的 gdb 使用。</p>
<h2 id="需要注意的一点"><a href="#需要注意的一点" class="headerlink" title="需要注意的一点"></a>需要注意的一点</h2><p>假设我们在一些特殊场景下希望对 Go 所编译的二进制文件进行压缩，那么在最后我们常常会使用到 upx 工具来减少其整体大小，命令如下：</p>
<figure class="highlight elixir"><table><tr><td class="code"><pre><span class="line"><span class="variable">$ </span>upx awesomeProject</span><br></pre></td></tr></table></figure>
<p>这时候我们再重新运行所编译的 awesomeProject 文件，这时候需要思考的是，gops 能不能识别到它是一个 Go 程序呢？</p>
<p>答案是不行的，经过 upx 压缩后的二进制文件将无法被识别为 Go 程序，并且在我所使用的 gops v0.3.7版本中，由于这类加壳进程的存在，执行 <code>gops</code> 命令直接出现了空指针调用的恐慌（panic），显然，这是一个 BUG，大家在实际环境中需要多加留意，如果要使用 gops 则尽量不要使用 upx 进行压缩。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在本文中我们针对 Google 官方出品的 gops 进行了基本使用和原理性的部分剖析。</p>
<p>如果你仔细研读了，就会发现其实 gops 几乎包含了大部分 Go 剖析工具的功能，是名副其实的进程诊断工具。</p>
<p>gops 集成了大量 Go 业界中常用的分析链，在排查问题上也会非常的方便，不需要一个个单独找特定工具在哪里，只需要使用 gops 即可，而更深层次的使用可以根据实际情况进行更一步的了解。</p>
<h3 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h3><p>[1]gops: <em><a href="https://github.com/google/gops" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/google/gops</a></em>[2]runtime.MemStats: <em><a href="https://golang.org/pkg/runtime/#MemStats" rel="external nofollow noopener noreferrer" target="_blank">https://golang.org/pkg/runtime/#MemStats</a></em></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>详解 Go 程序的启动流程，你知道 g0，m0 是什么吗？</title>
    <url>/go/%E7%9F%A5%E8%AF%86%E7%82%B9/%E8%AF%A6%E8%A7%A3%20Go%20%E7%A8%8B%E5%BA%8F%E7%9A%84%E5%90%AF%E5%8A%A8%E6%B5%81%E7%A8%8B%EF%BC%8C%E4%BD%A0%E7%9F%A5%E9%81%93%20g0%EF%BC%8Cm0%20%E6%98%AF%E4%BB%80%E4%B9%88%E5%90%97%EF%BC%9F/</url>
    <content><![CDATA[<p><a href="https://mp.weixin.qq.com/s/YK-TD3bZGEgqC0j-8U6VkQ" rel="external nofollow noopener noreferrer" target="_blank">转载自煎鱼的blog</a></p>
<h2 id="详解-Go-程序的启动流程，你知道-g0，m0-是什么吗？"><a href="#详解-Go-程序的启动流程，你知道-g0，m0-是什么吗？" class="headerlink" title="详解 Go 程序的启动流程，你知道 g0，m0 是什么吗？"></a>详解 Go 程序的启动流程，你知道 g0，m0 是什么吗？</h2><p>大家好，我是煎鱼。</p>
<p>自古应用程序均从 Hello World 开始，你我所写的 Go 语言亦然：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> fmt.Println(<span class="string">"hello world."</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这段程序的输出结果为 <code>hello world.</code>，就是这么的简单又直接。但这时候又不禁思考了起来，这个 <code>hello world.</code> 是怎么输出来，经历了什么过程。</p>
<p>真是非常的好奇，今天我们就一起来探一探 Go 程序的启动流程。其中涉及到 Go Runtime 的调度器启动，g0，m0 又是什么？</p>
<p>车门焊死，正式开始吸鱼之路。</p>
<h2 id="Go-引导阶段"><a href="#Go-引导阶段" class="headerlink" title="Go 引导阶段"></a>Go 引导阶段</h2><h3 id="查找入口"><a href="#查找入口" class="headerlink" title="查找入口"></a>查找入口</h3><p>首先编译上文提到的示例程序：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">$ GOFLAGS=<span class="string">"-ldflags=-compressdwarf=false"</span> <span class="keyword">go</span> build</span><br></pre></td></tr></table></figure>
<p>在命令中指定了 GOFLAGS 参数，这是因为在 Go1.11 起，为了减少二进制文件大小，调试信息会被压缩。导致在 MacOS 上使用 gdb 时无法理解压缩的 DWARF 的含义是什么（而我恰恰就是用的 MacOS）。</p>
<p>因此需要在本次调试中将其关闭，再使用 gdb 进行调试，以此达到观察的目的：</p>
<figure class="highlight applescript"><table><tr><td class="code"><pre><span class="line">$ gdb awesomeProject </span><br><span class="line">(gdb) info files</span><br><span class="line">Symbols <span class="keyword">from</span> <span class="string">"/Users/eddycjy/go-application/awesomeProject/awesomeProject"</span>.</span><br><span class="line">Local exec <span class="built_in">file</span>:</span><br><span class="line"> `/Users/eddycjy/go-<span class="built_in">application</span>/awesomeProject/awesomeProject', <span class="built_in">file</span> type mach-o-x86<span class="number">-64.</span></span><br><span class="line"> Entry point: <span class="number">0x1063c80</span></span><br><span class="line"> <span class="number">0x0000000001001000</span> - <span class="number">0x00000000010a6aca</span> <span class="keyword">is</span> .<span class="built_in">text</span></span><br><span class="line"> ...</span><br><span class="line">(gdb) b *<span class="number">0x1063c80</span></span><br><span class="line">Breakpoint <span class="number">1</span> <span class="keyword">at</span> <span class="number">0x1063c80</span>: <span class="built_in">file</span> /usr/<span class="keyword">local</span>/Cellar/go/<span class="number">1.15</span>/libexec/src/runtime/rt0_darwin_amd64.s, line <span class="number">8.</span></span><br></pre></td></tr></table></figure>
<p>通过 Entry point 的调试，可看到真正的程序入口在 runtime 包中，不同的计算机架构指向不同。例如：</p>
<ul>
<li>MacOS 在 <code>src/runtime/rt0_darwin_amd64.s</code>。</li>
<li>Linux 在 <code>src/runtime/rt0_linux_amd64.s</code>。</li>
</ul>
<p>其最终指向了 rt0_darwin_amd64.s 文件，这个文件名称非常的直观：</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line">Breakpoint <span class="number">1</span> at <span class="number">0</span>x1063c80: <span class="keyword">file</span> <span class="regexp">/usr/</span>local<span class="regexp">/Cellar/g</span>o<span class="regexp">/1.15/</span>libexec<span class="regexp">/src/</span><span class="keyword">runtime</span><span class="regexp">/rt0_darwin_amd64.s, line 8.</span></span><br></pre></td></tr></table></figure>
<p>rt0 代表 runtime0 的缩写，指代运行时的创世，超级奶爸：</p>
<ul>
<li>darwin 代表目标操作系统（GOOS）。</li>
<li>amd64 代表目标操作系统架构（GOHOSTARCH）。</li>
</ul>
<p>同时 Go 语言还支持更多的目标系统架构，例如：AMD64、AMR、MIPS、WASM 等：</p>
<p><img src="1.png" alt="图片">源码目录</p>
<p>若有兴趣可到 <code>src/runtime</code> 目录下进一步查看，这里就不一一介绍了。</p>
<h3 id="入口方法"><a href="#入口方法" class="headerlink" title="入口方法"></a>入口方法</h3><p>在 rt0_linux_amd64.s 文件中，可发现 <code>_rt0_amd64_darwin</code> JMP 跳转到了 <code>_rt0_amd64</code> 方法：</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><span class="line">TEXT _rt0_amd64_darwin(<span class="keyword">SB),NOSPLIT,$-8</span></span><br><span class="line"><span class="keyword"> </span><span class="keyword">JMP </span>_rt0_amd64(<span class="keyword">SB)</span></span><br><span class="line"><span class="keyword">...</span></span><br></pre></td></tr></table></figure>
<p>紧接着又跳转到 <code>runtime·rt0_go</code> 方法：</p>
<figure class="highlight x86asm"><table><tr><td class="code"><pre><span class="line">TEXT _rt0_amd64(SB),<span class="built_in">NOSPLIT</span>,$-<span class="number">8</span></span><br><span class="line"> <span class="keyword">MOVQ</span> <span class="number">0</span>(<span class="built_in">SP</span>), <span class="built_in">DI</span> // argc</span><br><span class="line"> LEAQ <span class="number">8</span>(<span class="built_in">SP</span>), <span class="built_in">SI</span> // argv</span><br><span class="line"> <span class="keyword">JMP</span> runtime·rt0_go(SB)</span><br></pre></td></tr></table></figure>
<p>该方法将程序输入的 argc 和 argv 从内存移动到寄存器中。</p>
<p>栈指针（SP）的前两个值分别是 argc 和 argv，其对应参数的数量和具体各参数的值。</p>
<h3 id="开启主线"><a href="#开启主线" class="headerlink" title="开启主线"></a>开启主线</h3><p>程序参数准备就绪后，正式初始化的方法落在 <code>runtime·rt0_go</code> 方法中：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">TEXT</span> runtime·rt0_go(<span class="built_in">SB</span>),NOSPLIT,<span class="number">$0</span></span><br><span class="line"> ...</span><br><span class="line"> CALL runtime·check(<span class="built_in">SB</span>)</span><br><span class="line"> <span class="keyword">MOVL </span><span class="number">16</span>(<span class="built_in">SP</span>), AX  // copy argc</span><br><span class="line"> <span class="keyword">MOVL </span>AX, <span class="number">0</span>(<span class="built_in">SP</span>)</span><br><span class="line"> <span class="keyword">MOVQ </span><span class="number">24</span>(<span class="built_in">SP</span>), AX  // copy argv</span><br><span class="line"> <span class="keyword">MOVQ </span>AX, <span class="number">8</span>(<span class="built_in">SP</span>)</span><br><span class="line"> CALL runtime·args(<span class="built_in">SB</span>)</span><br><span class="line"> CALL runtime·osinit(<span class="built_in">SB</span>)</span><br><span class="line"> CALL runtime·schedinit(<span class="built_in">SB</span>)</span><br><span class="line"></span><br><span class="line"> // create a new goroutine to start program</span><br><span class="line"> <span class="keyword">MOVQ </span>$runtime·mainPC(<span class="built_in">SB</span>), AX  // <span class="meta">entry</span></span><br><span class="line"> <span class="keyword">PUSHQ </span>AX</span><br><span class="line"> <span class="keyword">PUSHQ </span><span class="number">$0</span>   // arg size</span><br><span class="line"> CALL runtime·newproc(<span class="built_in">SB</span>)</span><br><span class="line"> <span class="keyword">POPQ </span>AX</span><br><span class="line"> <span class="keyword">POPQ </span>AX</span><br><span class="line"></span><br><span class="line"> // start this M</span><br><span class="line"> CALL runtime·mstart(<span class="built_in">SB</span>)</span><br><span class="line"> ...</span><br></pre></td></tr></table></figure>
<ul>
<li>runtime.check：运行时类型检查，主要是校验编译器的翻译工作是否正确，是否有 “坑”。基本代码均为检查 <code>int8</code> 在 <code>unsafe.Sizeof</code> 方法下是否等于 1 这类动作。</li>
<li>runtime.args：系统参数传递，主要是将系统参数转换传递给程序使用。</li>
<li>runtime.osinit：系统基本参数设置，主要是获取 CPU 核心数和内存物理页大小。</li>
<li>runtime.schedinit：进行各种运行时组件的初始化，包含调度器、内存分配器、堆、栈、GC 等一大堆初始化工作。会进行 p 的初始化，并将 m0 和某一个 p 进行绑定。</li>
<li>runtime.main：主要工作是运行 main goroutine，虽然在<code>runtime·rt0_go</code> 中指向的是<code>$runtime·mainPC</code>，但实质指向的是 <code>runtime.main</code>。</li>
<li>runtime.newproc：创建一个新的 goroutine，且绑定 <code>runtime.main</code> 方法（也就是应用程序中的入口 main 方法）。并将其放入 m0 绑定的p的本地队列中去，以便后续调度。</li>
<li>runtime.mstart：启动 m，调度器开始进行循环调度。</li>
</ul>
<p>在 <code>runtime·rt0_go</code> 方法中，其主要是完成各类运行时的检查，系统参数设置和获取，并进行大量的 Go 基础组件初始化。</p>
<p>初始化完毕后进行主协程（main goroutine）的运行，并放入等待队列（GMP 模型），最后调度器开始进行循环调度。</p>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>根据上述源码剖析，可以得出如下 Go 应用程序引导的流程图：</p>
<p><img src="2.png" alt="图片">Go 程序引导过程</p>
<p>在 Go 语言中，实际的运行入口并不是用户日常所写的 <code>main func</code>，更不是 <code>runtime.main</code> 方法，而是从 <code>rt0_*_amd64.s</code> 开始，最终再一路 JMP 到 <code>runtime·rt0_go</code> 里去，再在该方法里完成一系列 Go 自身所需要完成的绝大部分初始化动作。</p>
<p>其中整体包括：</p>
<ul>
<li>运行时类型检查、系统参数传递、CPU 核数获取及设置、运行时组件的初始化（调度器、内存分配器、堆、栈、GC 等）。</li>
<li>运行 main goroutine。</li>
<li>运行相应的 GMP 等大量缺省行为。</li>
<li>涉及到调度器相关的大量知识。</li>
</ul>
<p>后续将会继续剖析将进一步剖析 <code>runtime·rt0_go</code> 里的爱与恨，尤其像是 <code>runtime.main</code>、<code>runtime.schedinit</code> 等调度方法，都有非常大的学习价值，有兴趣的小伙伴可以持续关注。</p>
<h2 id="Go-调度器初始化"><a href="#Go-调度器初始化" class="headerlink" title="Go 调度器初始化"></a>Go 调度器初始化</h2><p>知道了 Go 程序是怎么引导起来的之后，我们需要了解 Go Runtime 中调度器是怎么流转的。</p>
<h3 id="runtime-mstart"><a href="#runtime-mstart" class="headerlink" title="runtime.mstart"></a>runtime.mstart</h3><p>这里主要关注 <code>runtime.mstart</code> 方法：</p>
<figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">func mstart() &#123;</span><br><span class="line"> <span class="comment">// 获取 g0</span></span><br><span class="line"> <span class="variable">_g_</span> := getg()</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 确定栈边界</span></span><br><span class="line"> osStack := <span class="variable">_g_</span>.stack.lo == <span class="number">0</span></span><br><span class="line"> <span class="keyword">if</span> osStack &#123;</span><br><span class="line">  <span class="built_in">size</span> := <span class="variable">_g_</span>.stack.hi</span><br><span class="line">  <span class="keyword">if</span> <span class="built_in">size</span> == <span class="number">0</span> &#123;</span><br><span class="line">   <span class="built_in">size</span> = <span class="number">8192</span> * sys.StackGuardMultiplier</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="variable">_g_</span>.stack.hi = uintptr(noescape(unsafe.Pointer(&amp;<span class="built_in">size</span>)))</span><br><span class="line">  <span class="variable">_g_</span>.stack.lo = <span class="variable">_g_</span>.stack.hi - <span class="built_in">size</span> + <span class="number">1024</span></span><br><span class="line"> &#125;</span><br><span class="line"> <span class="variable">_g_</span>.stackguard0 = <span class="variable">_g_</span>.stack.lo + <span class="variable">_StackGuard</span></span><br><span class="line"> <span class="variable">_g_</span>.stackguard1 = <span class="variable">_g_</span>.stackguard0</span><br><span class="line">  </span><br><span class="line">  <span class="comment">// 启动 m，进行调度器循环调度</span></span><br><span class="line"> mstart1()</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 退出线程</span></span><br><span class="line"> <span class="keyword">if</span> mStackIsSystemAllocated() &#123;</span><br><span class="line">  osStack = <span class="literal">true</span></span><br><span class="line"> &#125;</span><br><span class="line"> mexit(osStack)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>调用 <code>getg</code> 方法获取 GMP 模型中的 g，此处获取的是 g0。</li>
<li>通过检查 g 的执行栈 <code>_g_.stack</code> 的边界（堆栈的边界正好是 lo, hi）来确定是否为系统栈。若是，则根据系统栈初始化 g 执行栈的边界。</li>
<li>调用 <code>mstart1</code> 方法启动系统线程 m，进行调度器循环调度。</li>
<li>调用 <code>mexit</code> 方法退出系统线程 m。</li>
</ul>
<h3 id="runtime-mstart1"><a href="#runtime-mstart1" class="headerlink" title="runtime.mstart1"></a>runtime.mstart1</h3><p>这么看来其实质逻辑在 <code>mstart1</code> 方法，我们继续往下剖析：</p>
<figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">func mstart1() &#123;</span><br><span class="line"> <span class="comment">// 获取 g，并判断是否为 g0</span></span><br><span class="line"> <span class="variable">_g_</span> := getg()</span><br><span class="line"> <span class="keyword">if</span> <span class="variable">_g_</span> != <span class="variable">_g_</span>.m.g0 &#123;</span><br><span class="line">  <span class="keyword">throw</span>(<span class="string">"bad runtime·mstart"</span>)</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 初始化 m 并记录调用方 pc、sp</span></span><br><span class="line"> save(getcallerpc(), getcallersp())</span><br><span class="line"> asminit()</span><br><span class="line"> minit()</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 设置信号 handler</span></span><br><span class="line"> <span class="keyword">if</span> <span class="variable">_g_</span>.m == &amp;m0 &#123;</span><br><span class="line">  mstartm0()</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="comment">// 运行启动函数</span></span><br><span class="line"> <span class="keyword">if</span> fn := <span class="variable">_g_</span>.m.mstartfn; fn != <span class="literal">nil</span> &#123;</span><br><span class="line">  fn()</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">if</span> <span class="variable">_g_</span>.m != &amp;m0 &#123;</span><br><span class="line">  acquirep(<span class="variable">_g_</span>.m.nextp.ptr())</span><br><span class="line">  <span class="variable">_g_</span>.m.nextp = <span class="number">0</span></span><br><span class="line"> &#125;</span><br><span class="line"> schedule()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>调用 <code>getg</code> 方法获取 g。并且通过前面绑定的 <code>_g_.m.g0</code> 判断所获取的 g 是否 g0。若不是，则直接抛出致命错误。因为调度器仅在 g0 上运行。</li>
<li>调用 <code>minit</code> 方法初始化 m，并记录调用方的 PC、SP，便于后续 schedule 阶段时的复用。</li>
<li>若确定当前的 g 所绑定的 m 是 m0，则调用 <code>mstartm0</code> 方法，设置信号 handler。该动作必须在 <code>minit</code> 方法之后，这样 <code>minit</code> 方法可以提前准备好线程，以便能够处理信号。</li>
<li>若当前 g 所绑定的 m 有启动函数，则运行。否则跳过。</li>
<li>若当前 g 所绑定的 m 不是 m0，则需要调用 <code>acquirep</code> 方法获取并绑定 p，也就是 m 与 p 绑定。</li>
<li>调用 <code>schedule</code> 方法进行正式调度。</li>
</ul>
<p>忙活了一大圈，终于进入到开题的主菜了，原来潜伏的很深的 <code>schedule</code> 方法才是真正做调度的方法，其他都是前置处理和准备数据。</p>
<p>由于篇幅问题，<code>schedule</code> 方法会放到下篇再继续剖析，我们先聚焦本篇的一些细节点。</p>
<h2 id="问题深剖"><a href="#问题深剖" class="headerlink" title="问题深剖"></a>问题深剖</h2><p>不过到这里篇幅也已经比较长了，积累了不少问题。我们针对在 Runtime 中出镜率最高的两个元素进行剖析：</p>
<ol>
<li><code>m0</code> 是什么，作用是？</li>
<li><code>g0</code> 是什么，作用是？</li>
</ol>
<h3 id="m0"><a href="#m0" class="headerlink" title="m0"></a>m0</h3><p>m0 是 Go Runtime 所创建的第一个系统线程，一个 Go 进程只有一个 m0，也叫主线程。</p>
<p>从多个方面来看：</p>
<ul>
<li>数据结构：m0 和其他创建的 m 没有任何区别。</li>
<li>创建过程：m0 是进程在启动时应该汇编直接复制给 m0 的，其他后续的 m 则都是 Go Runtime 内自行创建的。</li>
<li>变量声明：m0 和常规 m 一样，m0 的定义就是 <code>var m0 m</code>，没什么特别之处。</li>
</ul>
<h3 id="g0"><a href="#g0" class="headerlink" title="g0"></a>g0</h3><p>g 一般分为三种，分别是：</p>
<ul>
<li>执行用户任务的叫做 g。</li>
<li>执行 <code>runtime.main</code> 的 main goroutine。</li>
<li>执行调度任务的叫 g0。。</li>
</ul>
<p>g0 比较特殊，每一个 m 都只有一个 g0（仅此只有一个 g0），且每个 m 都只会绑定一个 g0。在 g0 的赋值上也是通过汇编赋值的，其余后续所创建的都是常规的 g。</p>
<p>从多个方面来看：</p>
<ul>
<li>数据结构：g0 和其他创建的 g 在数据结构上是一样的，但是存在栈的差别。在 g0 上的栈分配的是系统栈，在 Linux 上栈大小默认固定 8MB，不能扩缩容。而常规的 g 起始只有 2KB，可扩容。</li>
<li>运行状态：g0 和常规的 g 不一样，没有那么多种运行状态，也不会被调度程序抢占，调度本身就是在 g0 上运行的。</li>
<li>变量声明：g0 和常规 g，g0 的定义就是 <code>var g0 g</code>，没什么特别之处。</li>
</ul>
<h3 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h3><p>在本章节中我们讲解了 Go 调度器初始化的一个过程，分别涉及：</p>
<ul>
<li>runtime.mstart。</li>
<li>runtime.mstart1。</li>
</ul>
<p>基于此也了解到了在调度器初始化过程中，需要准备什么，初始化什么。另外针对调度过程中最常提到的 m0、g0 的概念我们进行了梳理和说明。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在今天这篇文章中，我们详细的介绍了 Go 语言的引导启动过程中的所有流程和初始化动作。</p>
<p>同时针对调度器的初始化进行了初步分析，详细介绍了 m0、g0 的用途和区别。在下一篇文章中我们将进一步对真正调度的 <code>schedule</code> 方法进行详解，这块也是个硬骨头了。</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>go 汇编入门 如何学习Golang？万字详文教你Go语言入门</title>
    <url>/go/go%20%E6%B1%87%E7%BC%96%E5%85%A5%E9%97%A8%20%E5%A6%82%E4%BD%95%E5%AD%A6%E4%B9%A0Golang%EF%BC%9F%E4%B8%87%E5%AD%97%E8%AF%A6%E6%96%87%E6%95%99%E4%BD%A0Go%E8%AF%AD%E8%A8%80%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<p>作者：<a href="https://www.toutiao.com/i6882641627349778955/" rel="external nofollow noopener noreferrer" target="_blank">ivansli</a>，腾讯开发工程师</p>
<blockquote>
<p>在深入学习 Golang 的 runtime 和标准库实现的时候发现，如果对 Golang 汇编没有一定了解的话，很难深入了解其底层实现机制。在这里整理总结了一份基础的 Golang 汇编入门知识，通过学习之后能够对其底层实现有一定的认识。</p>
</blockquote>
<p><strong>0. 为什么写本文</strong><br>平时业务中一直使用 PHP 编写代码，但是一直对 Golang 比较感兴趣，闲暇、周末之余会看一些 Go 底层源码。</p>
<p>近日在分析 go 的某些特性底层功能实现时发现：有些又跟 runtime 运行时有关，而要掌握这一部分的话，有一道坎是绕不过去的，那就是 Go 汇编。索性就查阅了很多大佬们写的资料，在阅读之余整理总结了一下，并在这里分享给大家。</p>
<blockquote>
<p>本文使用 Go 版本为 go1.14.1</p>
</blockquote>
<p><strong>1. 为什么需要汇编</strong><br>众所周知，在计算机的世界里，只有 2 种类型。那就是：0 和 1。</p>
<p>计算机工作是由一系列的机器指令进行驱动的，这些指令又是一组二进制数字，其对应计算机的高低电平。而这些机器指令的集合就是机器语言，这些机器语言在最底层是与硬件一一对应的。</p>
<p>显而易见，这样的机器指令有一个致命的缺点：可阅读性太差（恐怕也只有天才和疯子才有能力把控得了）。</p>
<p>为了解决可读性的问题以及代码编辑的需求，于是就诞生了最接近机器的语言：汇编语言（在我看来，汇编语言更像一种助记符，这些人们容易记住的每一条助记符都映射着一条不容易记住的由 0、1 组成的机器指令。你觉得像不像域名与 IP 地址的关系呢？）。</p>
<p><strong>1.1 程序的编译过程</strong><br>以 C 语言为例来说，从 hello.c 的源码文件到 hello 可执行文件，经过编译器处理，大致分为几个阶段：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/351f46cd5a8c9f66b08d47311c21c2f4.png" alt></p>
<p>编译器在不同的阶段会做不同的事情，但是有一步是可以确定的，那就是：源码会被编译成汇编，最后才是二进制。</p>
<p><strong>2. 程序与进程</strong><br>源码经过编译之后，得到一个二进制的可执行文件。文件这两个字也就表明，目前得到的这个文件跟其他文件对比，除了是具有一定的格式（Linux 中是 ELF 格式，即：可运行可链接。executable linkable formate）的二进制组成，并没什么区别。</p>
<p>在 Linux 中文件类型大致分为 7 种：</p>
<blockquote>
<p>b: 块设备文件<br>c：字符设备文件<br>d：目录<br>-：普通文件<br>l：链接<br>s：socket<br>p：管道</p>
</blockquote>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0aeffd9454c7ba82b7752fd904984417.png" alt></p>
<p>通过上面可以看到，可执行文件 main 与源码文件 main.go，都是同一种类型，属于普通文件。（当然了，在 Unix 中有一句很经典的话：一切皆文件）。</p>
<p>那么，问题来了：</p>
<ol>
<li>什么是程序？</li>
<li>什么是进程？<br><strong>2.1 程序</strong><br>维基百科告诉我们：程序是指一组指示计算机或其他具有消息处理能力设备每一步动作的指令，通常用某种程序设计语言编写，运行于某种目标体系结构上。</li>
</ol>
<p>从某个层面来看，可以把程序分为静态程序、动态程序： 静态程序：单纯的指具有一定格式的可执行二进制文件。 动态程序：则是静态可执行程序文件被加载到内存之后的一种运行时模型（又称为进程）。</p>
<p>2.2 进程<br>首先，要知道的是，进程是分配系统资源的最小单位，线程(带有时间片的函数)是系统调度的最小单位。进程包含线程，线程所属于进程。</p>
<p>创建进程一般使用 fork 方法(通常会有个拉起程序，先 fork 自身生成一个子进程。然后，在该子进程中通过 exec 函数把对应程序加载进来，进而启动目标进程。当然，实际上要复杂得多)，而创建线程则是使用 pthread 线程库。</p>
<p>以 32 位 Linux 操作系统为例，进程经典的虚拟内存结构模型如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/8c4329c6a59628861531d0ef4ba58f8d.png" alt></p>
<p>其中，有两处结构是静态程序所不具有的，那就是运行时堆(heap)与运行时栈(stack)。</p>
<p>运行时堆从低地址向高地址增长，申请的内存空间需要程序员自己或者由 GC 释放。 运行时栈从高地址向低地址增长，内存空间在当前栈桢调用结束之后自动释放(并不是清除其所占用内存中数据，而是通过栈顶指针 SP 的移动，来标识哪些内存是正在使用的)。</p>
<p><strong>3. Go 汇编</strong><br>对于 Go 编译器而言，其输出的结果是一种抽象可移植的汇编代码，这种汇编（Go 的汇编是基于 Plan9 的汇编）并不对应某种真实的硬件架构。Go 的汇编器会使用这种伪汇编，再为目标硬件生成具体的机器指令。</p>
<p>伪汇编这一个额外层可以带来很多好处，最主要的一点是方便将 Go 移植到新的架构上。</p>
<p>相关的信息可以参考 Rob Pike 的 The Design of the Go Assembler。</p>
<blockquote>
<p>要了解 Go 的汇编器最重要的是要知道 Go 的汇编器不是对底层机器的直接表示，即 Go 的汇编器没有直&gt;接使用目标机器的汇编指令。Go 汇编器所用的指令，一部分与目标机器的指令一一对应，而另外一部分则不是。这是因为编译器套件不需要汇编器直接参与常规的编译过程。<br>相反，编译器使用了一种半抽象的指令集，并且部分指令是在代码生成后才被选择的。汇编器基于这种半抽象的形式工作，所以虽然你看到的是一条 MOV 指令，但是工具链针对对这条指令实际生成可能完全不是一个移动指令，也许会是清除或者加载。也有可能精确的对应目标平台上同名的指令。概括来说，特定于机器的指令会&gt;以他们的本尊出现， 然而对于一些通用的操作，如内存的移动以及子程序的调用以及返回通常都做了抽象。细节因架构不同而不一样，我们对这样的不精确性表示歉意，情况并不明确。<br>汇编器程序的工作是对这样半抽象指令集进行解析并将其转变为可以输入到链接器的指令。<br>The most important thing to know about Go’s assembler is that it is not a direct representation of the underlying machine. Some of the details map precisely to the machine, but some do not. This is because the compiler suite needs no assembler pass in the usual pipeline. Instead, the compiler operates on a kind of semi-abstract instruction set, and instruction selection occurs partly after code generation. The assembler works on the semi-abstract form, so when you see an instruction like MOV what the toolchain actually generates for that operation might not be a move instruction at all, perhaps a clear or load.<br>Or it might correspond exactly to the machine instruction with that name. In general, machine-specific operations tend to appear as themselves, while more general concepts like memory move and subroutine call and return are more abstract. The details vary with architecture, and we apologize for the imprecision; the situation is not well-defined.<br>The assembler program is a way to parse a description of that semi-abstract instruction set and turn it into instructions to be input to the linker.</p>
</blockquote>
<p>Go 汇编使用的是caller-save模式，被调用函数的入参参数、返回值都由调用者维护、准备。因此，当需要调用一个函数时，需要先将这些工作准备好，才调用下一个函数，另外这些都需要进行内存对齐，对齐的大小是 sizeof(uintptr)。</p>
<p><strong>3.1 几个概念</strong><br>在深入了解 Go 汇编之前，需要知道的几个概念：</p>
<ul>
<li>栈：进程、线程、goroutine 都有自己的调用栈，先进后出（FILO）</li>
<li>栈帧：可以理解是函数调用时，在栈上为函数所分配的内存区域</li>
<li>调用者：caller，比如：A 函数调用了 B 函数，那么 A 就是调用者</li>
<li>被调者：callee，比如：A 函数调用了 B 函数，那么 B 就是被调者<br><strong>3.2 Go 的核心寄存器</strong><br>go 汇编中有 4 个核心的伪寄存器，这 4 个寄存器是编译器用来维护上下文、特殊标识等作用的：</li>
</ul>
<p>ble data-draft-node=”block” data-draft-type=”table” data-size=”normal” data-row-style=”normal”&gt;</p>
<ul>
<li>FP: 使用如 symbol+offset(FP)的方式，引用 callee 函数的入参参数。例如 arg0+0(FP)，arg1+8(FP)，使用 FP 必须加 symbol ，否则无法通过编译(从汇编层面来看，symbol 没有什么用，加 symbol 主要是为了提升代码可读性)。 另外，需要注意的是：往往在编写 go 汇编代码时，要站在 callee 的角度来看(FP)，在 callee 看来，(FP)指向的是 caller 调用 callee 时传递的第一个参数的位置。 假如当前的 callee 函数是 add，在 add 的代码中引用 FP，该 FP 指向的位置不在 callee 的 stack frame 之内。而是在 caller 的 stack frame 上，指向调用 add 函数时传递的第一个参数的位置，经常在 callee 中用symbol+offset(FP)来获取入参的参数值。</li>
<li>SB: 全局静态基指针，一般用在声明函数、全局变量中。</li>
<li>SP: 该寄存器也是最具有迷惑性的寄存器，因为会有伪 SP 寄存器和硬件 SP 寄存器之分。 plan9 的这个伪 SP 寄存器指向当前栈帧第一个局部变量的结束位置(为什么说是结束位置，可以看下面寄存器内存布局图)，使用形如 symbol+offset(SP) 的方式，引用函数的局部变量。offset 的合法取值是 [-framesize, 0)，注意是个左闭右开的区间。假如局部变量都是 8 字节，那么第一个局部变量就可以用 localvar0-8(SP) 来表示。与硬件寄存器 SP 是两个不同的东西，在栈帧 size 为 0 的情况下，伪寄存器 SP 和硬件寄存器 SP 指向同一位置。 手写汇编代码时，如果是 symbol+offset(SP)形式，则表示伪寄存器 SP。如果是 offset(SP)则表示硬件寄存器 SP。 务必注意：对于编译输出(go tool compile -S / go tool objdump)的代码来讲，所有的 SP 都是硬件 SP 寄存器，无论是否带 symbol（这一点非常具有迷惑性，需要慢慢理解。往往在分析编译输出的汇编时，看到的就是硬件 SP 寄存器）。</li>
<li>PC: 实际上就是在体系结构的知识中常见的 pc 寄存器，在 x86 平台下对应 ip 寄存器，amd64 上则是 rip。除了个别跳转之外，手写 plan9 汇编代码时，很少用到 PC 寄存器。<br>通过上面的讲解，想必已经对 4 个核心寄存器的区别有了一定的认识（或者是更加的迷惑、一头雾水）。那么，需要留意的是：如果是在分析编译输出的汇编代码时，要重点看 SP、SB 寄存器（FP 寄存器在这里是看不到的）。如果是，在手写汇编代码，那么要重点看 FP、SP 寄存器。</li>
</ul>
<p><strong>3.2.1 伪寄存器的内存模型</strong><br>下图描述了栈桢与各个寄存器的内存关系模型，值得注意的是要站在 callee 的角度来看。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/b2399a142cdc75db09daf31a1baab176.png" alt></p>
<p>有一点需要注意的是，return addr 也是在 caller 的栈上的，不过往栈上插 return addr 的过程是由 CALL 指令完成的（在分析汇编时，是看不到关于 addr 相关空间信息的。在分配栈空间时，addr 所占用空间大小不包含在栈帧大小内）。</p>
<p>在 AMD64 环境，伪 PC 寄存器其实是 IP 指令计数器寄存器的别名。伪 FP 寄存器对应的是 caller 函数的帧指针，一般用来访问 callee 函数的入参参数和返回值。伪 SP 栈指针对应的是当前 callee 函数栈帧的底部（不包括参数和返回值部分），一般用于定位局部变量。伪 SP 是一个比较特殊的寄存器，因为还存在一个同名的 SP 真寄存器，真 SP 寄存器对应的是栈的顶部。</p>
<p>在编写 Go 汇编时，当需要区分伪寄存器和真寄存器的时候只需要记住一点：伪寄存器一般需要一个标识符和偏移量为前缀，如果没有标识符前缀则是真寄存器。比如(SP)、+8(SP)没有标识符前缀为真 SP 寄存器，而 a(SP)、b+8(SP)有标识符为前缀表示伪寄存器。</p>
<p><strong>3.2.2 几点说明</strong><br>我们这里对容易混淆的几点简单进行说明：</p>
<ul>
<li>伪 SP 和硬件 SP 不是一回事，在手写汇编代码时，伪 SP 和硬件 SP 的区分方法是看该 SP 前是否有 symbol。如果有 symbol，那么即为伪寄存器，如果没有，那么说明是硬件 SP 寄存器。</li>
<li>伪 SP 和 FP 的相对位置是会变的，所以不应该尝试用伪 SP 寄存器去找那些用 FP+offset 来引用的值，例如函数的入参和返回值。</li>
<li>官方文档中说的伪 SP 指向 stack 的 top，可能是有问题的。其指向的局部变量位置实际上是整个栈的栈底（除 caller BP 之外），所以说 bottom 更合适一些。</li>
<li>在 go tool objdump/go tool compile -S 输出的代码中，是没有伪 SP 和 FP 寄存器的，我们上面说的区分伪 SP 和硬件 SP 寄存器的方法，对于上述两个命令的输出结果是没法使用的。在编译和反汇编的结果中，只有真实的 SP 寄存器。<br><strong>3.2.3 IA64 和 plan9 的对应关系</strong><br>在 plan9 汇编里还可以直接使用的 amd64 的通用寄存器，应用代码层面会用到的通用寄存器主要是: rax, rbx, rcx, rdx, rdi, rsi, r8~r15 这些寄存器，虽然 rbp 和 rsp 也可以用，不过 bp 和 sp 会被用来管理栈顶和栈底，最好不要拿来进行运算。</li>
</ul>
<p>plan9 中使用寄存器不需要带 r 或 e 的前缀，例如 rax，只要写 AX 即可: MOVQ $101, AX = mov rax, 101</p>
<p>下面是通用通用寄存器的名字在 IA64 和 plan9 中的对应关系:</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/16352d172b940ecdec747feffb9922ed.png" alt></p>
<p><strong>3.3 常用操作指令</strong><br>下面列出了常用的几个汇编指令（指令后缀Q 说明是 64 位上的汇编指令）</p>
<p>ble data-draft-node=”block” data-draft-type=”table” data-size=”normal” data-row-style=”normal”&gt;</p>
<p><strong>4. 汇编分析</strong><br>说了那么多，it is code show time。</p>
<p><strong>4.1 如何输出 Go 汇编</strong><br>对于写好的 go 源码，生成对应的 Go 汇编，大概有下面几种</p>
<ul>
<li><p>方法 1 先使用 go build -gcflags “-N -l” main.go 生成对应的可执行二进制文件 再使用 go tool objdump -s “main.“ main 反编译获取对应的汇编<br>反编译时 “main.“ 表示只输出 main 包中相关的汇编 “main.main” 则表示只输出 main 包中 main 方法相关的汇编</p>
</li>
<li><p>方法 2 使用 go tool compile -S -N -l main.go 这种方式直接输出汇编</p>
</li>
<li>方法 3 使用go build -gcflags=”-N -l -S” main.go 直接输出汇编<br>注意： 在使用这些命令时，加上对应的 flag，否则某些逻辑会被编译器优化掉，而看不到对应完整的汇编代码</li>
</ul>
<blockquote>
<p>-l 禁止内联 -N 编译时，禁止优化 -S 输出汇编代码</p>
</blockquote>
<p><strong>4.2 Go 汇编示例</strong><br>go 示例代码<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">add</span><span class="params">(a, b <span class="keyword">int</span>)</span> <span class="title">int</span></span>&#123;</span><br><span class="line">        sum := <span class="number">0</span> <span class="comment">// 不设置该局部变量sum，add栈空间大小会是0</span></span><br><span class="line">        sum = a+b</span><br><span class="line">        <span class="keyword">return</span> sum</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="built_in">println</span>(add(<span class="number">1</span>,<span class="number">2</span>))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>编译 go 源代码，输出汇编<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">go tool compile -N -l -S main.go</span><br></pre></td></tr></table></figure></p>
<p>截取主要汇编如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="string">""</span>.add STEXT nosplit size=60 args=0x18 locals=0x10</span><br><span class="line">        0x0000 00000 (main.go:3)        TEXT    <span class="string">""</span>.add(SB), NOSPLIT, <span class="variable">$16</span>-24</span><br><span class="line">        0x0000 00000 (main.go:3)        SUBQ    <span class="variable">$16</span>, SP  ;;生成add栈空间</span><br><span class="line">        0x0004 00004 (main.go:3)        MOVQ    BP, 8(SP)</span><br><span class="line">        0x0009 00009 (main.go:3)        LEAQ    8(SP), BP</span><br><span class="line">    ;; ...omitted FUNCDATA stuff...</span><br><span class="line">        0x000e 00014 (main.go:3)        MOVQ    <span class="variable">$0</span>, <span class="string">""</span>.~r2+40(SP) ;;初始化返回值</span><br><span class="line">        0x0017 00023 (main.go:4)        MOVQ    <span class="variable">$0</span>, <span class="string">""</span>.sum(SP) ;;局部变量sum赋为0</span><br><span class="line">        0x001f 00031 (main.go:5)        MOVQ    <span class="string">""</span>.a+24(SP), AX  ;;取参数a</span><br><span class="line">        0x0024 00036 (main.go:5)        ADDQ    <span class="string">""</span>.b+32(SP), AX ;;等价于AX=a+b</span><br><span class="line">        0x0029 00041 (main.go:5)        MOVQ    AX, <span class="string">""</span>.sum(SP)  ;;赋值局部变量sum</span><br><span class="line">        0x002d 00045 (main.go:6)        MOVQ    AX, <span class="string">""</span>.~r2+40(SP) ;;设置返回值</span><br><span class="line">        0x0032 00050 (main.go:6)        MOVQ    8(SP), BP</span><br><span class="line">        0x0037 00055 (main.go:6)        ADDQ    <span class="variable">$16</span>, SP ;;清除add栈空间</span><br><span class="line">        0x003b 00059 (main.go:6)        RET</span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line"><span class="string">""</span>.main STEXT size=107 args=0x0 locals=0x28</span><br><span class="line">        0x0000 00000 (main.go:9)        TEXT    <span class="string">""</span>.main(SB), <span class="variable">$40</span>-0</span><br><span class="line">    ......</span><br><span class="line">        0x000f 00015 (main.go:9)        SUBQ    <span class="variable">$40</span>, SP ;; 生成main栈空间</span><br><span class="line">        0x0013 00019 (main.go:9)        MOVQ    BP, 32(SP)</span><br><span class="line">        0x0018 00024 (main.go:9)        LEAQ    32(SP), BP</span><br><span class="line">    ;; ...omitted FUNCDATA stuff...</span><br><span class="line">        0x001d 00029 (main.go:10)       MOVQ    <span class="variable">$1</span>, (SP) ;;add入参：1</span><br><span class="line">        0x0025 00037 (main.go:10)       MOVQ    <span class="variable">$2</span>, 8(SP) ;;add入参：2</span><br><span class="line">        0x002e 00046 (main.go:10)       CALL    <span class="string">""</span>.add(SB) ;;调用add函数</span><br><span class="line">        0x0033 00051 (main.go:10)       MOVQ    16(SP), AX</span><br><span class="line">        0x0038 00056 (main.go:10)       MOVQ    AX, <span class="string">""</span>..autotmp_0+24(SP)</span><br><span class="line">        0x003d 00061 (main.go:10)       CALL    runtime.printlock(SB)</span><br><span class="line">        0x0042 00066 (main.go:10)       MOVQ    <span class="string">""</span>..autotmp_0+24(SP), AX</span><br><span class="line">        0x0047 00071 (main.go:10)       MOVQ    AX, (SP)</span><br><span class="line">        0x004b 00075 (main.go:10)       CALL    runtime.printint(SB)</span><br><span class="line">        0x0050 00080 (main.go:10)       CALL    runtime.printnl(SB)</span><br><span class="line">        0x0055 00085 (main.go:10)       CALL    runtime.printunlock(SB)</span><br><span class="line">        0x005a 00090 (main.go:11)       MOVQ    32(SP), BP</span><br><span class="line">        0x005f 00095 (main.go:11)       ADDQ    <span class="variable">$40</span>, SP ;;清除main栈空间</span><br><span class="line">        0x0063 00099 (main.go:11)       RET</span><br><span class="line">    ......</span><br></pre></td></tr></table></figure></p>
<p>这里列举了一个简单的 int 类型加法示例，实际开发中会遇到各种参数类型，要复杂的多，这里只是抛砖引玉 :)</p>
<p><strong>4.3 Go 汇编解析</strong><br>针对 4.2 输出汇编，对重要核心代码进行分析。</p>
<p><strong>4.3.1 add 函数汇编解析</strong><br>TEXT “”.add(SB), NOSPLIT|ABIInternal, $16-24<br>TEXT “”.add TEXT 指令声明了 “”.add 是 .text 代码段的一部分，并表明跟在这个声明后的是函数的函数体。在链接期，””这个空字符会被替换为当前的包名: 也就是说，””.add 在链接到二进制文件后会变成 main.add</p>
<p>(SB) SB 是一个虚拟的伪寄存器，保存静态基地址(static-base) 指针，即我们程序地址空间的开始地址。 “”.add(SB) 表明我们的符号位于某个固定的相对地址空间起始处的偏移位置 (最终是由链接器计算得到的)。换句话来讲，它有一个直接的绝对地址: 是一个全局的函数符号。</p>
<p>NOSPLIT: 向编译器表明不应该插入 stack-split 的用来检查栈需要扩张的前导指令。 在我们 add 函数的这种情况下，编译器自己帮我们插入了这个标记: 它足够聪明地意识到，由于 add 没有任何局部变量且没有它自己的栈帧，所以一定不会超出当前的栈。不然，每次调用函数时，在这里执行栈检查就是完全浪费 CPU 时间了。</p>
<p>$0-16</p>
<p>如何学习Golang？万字详文教你Go语言入门</p>
<p>24 指定了调用方传入的参数+返回值大小（24 字节=入参 a、b 大小8字节*2+返回值8字节）</p>
<blockquote>
<p>通常来讲，帧大小后一般都跟随着一个参数大小，用减号分隔。(这不是一个减法操作，只是一种特殊的语法) 帧大小 $24-8 意味着这个函数有 24 个字节的帧以及 8 个字节的参数，位于调用者的帧上。如果 NOSPLIT 没有在 TEXT 中指定，则必须提供参数大小。对于 Go 原型的汇编函数，go vet 会检查参数大小是否正确。<br>In the general case, the frame size is followed by an argument size, separated by a minus sign. (It’s not a subtraction, just idiosyncratic syntax.) The frame size $24-8 states that the function has a 24-byte frame and is called with 8 bytes of argument, which live on the caller’s frame. If NOSPLIT is not specified for the TEXT, the argument size must be provided. For assembly functions with Go prototypes, go vet will check that the argument size is correct.</p>
</blockquote>
<ul>
<li>SUBQ $16, SP SP 为栈顶指针，该语句等价于 SP-=16（由于栈空间是向下增长的，所以开辟栈空间时为减操作），表示生成 16 字节大小的栈空间。</li>
<li>MOVQ $0, “”.~r2+40(SP) 此时的 SP 为 add 函数栈的栈顶指针，40(SP)的位置则是 add 返回值的位置，该位置位于 main 函数栈空间内。 该语句设置返回值类型的 0 值，即初始化返回值，防止得到脏数据（返回值类型为 int，int 的 0 值为 0）。</li>
<li>MOVQ “”.a+24(SP), AX 从 main 函数栈空间获取入参 a 的值，存到寄存器 AX</li>
<li>ADDQ “”.b+32(SP), AX 从 main 函数栈空间获取入参 b 的值，与寄存器 AX 中存储的 a 值相加，结果存到 AX。 相当于 AX=a+b</li>
<li>MOVQ AX, “”.~r2+40(SP) 把 a+b 的结果放到 main 函数栈中, add(a+b)返回值所在的位置</li>
<li>ADDQ $16, SP 归还 add 函数占用的栈空间<br><strong>4.3.2 函数栈桢结构模型</strong><br>根据 4.2 对应汇编绘制的函数栈桢结构模型</li>
</ul>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0d2f80e2e898363a2437551c1ac4c861.png" alt></p>
<p>还记得前面提到的，Go 汇编使用的是caller-save模式，被调用函数的参数、返回值、栈位置都需要由调用者维护、准备吗？</p>
<p>在函数栈桢结构中可以看到，add()函数的入参以及返回值都由调用者 main()函数维护。 也正是因为如此，GO 有了其他语言不具有的，支持多个返回值的特性。</p>
<p><strong>4.4 Go 汇编语法</strong><br>这里重点讲一下函数声明、变量声明。</p>
<p><strong>4.4.1 函数声明</strong><br>来看一个典型的 Go 汇编函数定义<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// func add(a, b int) int</span></span><br><span class="line"><span class="comment">// 该add函数声明定义在同一个 package name 下的任意 .go文件中</span></span><br><span class="line"><span class="comment">// 只有函数头，没有实现</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// add函数的Go汇编实现</span></span><br><span class="line"><span class="comment">// pkgname 默认是  ""</span></span><br><span class="line">TEXT pkgname·add(SB), NOSPLIT, $<span class="number">16</span><span class="number">-24</span></span><br><span class="line">    MOVQ a+<span class="number">0</span>(FP), AX</span><br><span class="line">    ADDQ b+<span class="number">8</span>(FP), AX</span><br><span class="line">    MOVQ AX, ret+<span class="number">16</span>(FP)</span><br><span class="line">    RET</span><br></pre></td></tr></table></figure></p>
<p>Go 汇编实现为什么是 TEXT 开头？ 仔细观察上面的进程内存布局图就会发现，我们的代码在是存储在.text 段中的，这里也就是一种约定俗成的起名方式。实际上在 plan9 中 TEXT 是一个指令，用来定义一个函数。</p>
<p>定义中的 pkgname 是可以省略的，(非想写也可以写上，不过写上 pkgname 的话，在重命名 package 之后还需要改代码，默认为””) 编译器会在链接器自动加上所属的包名称。</p>
<p>中点 · 比较特殊，是一个 unicode 的中点，该点在 mac 下的输入方法是 option+shift+9。在程序被链接之后，所有的中点·都会被替换为句号.，比如你的方法是runtime·main，在编译之后的程序里的符号则是runtime.main。</p>
<p>简单总结一下, Go 汇编实现函数声明，格式为:<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"> 静态基地址(static-base) 指针</span><br><span class="line">    |</span><br><span class="line">                  |         add函数入参+返回值总大小</span><br><span class="line">                  |               |</span><br><span class="line">TEXT pkgname·add(SB),NOSPLIT,$16-24</span><br><span class="line">      |      |                |</span><br><span class="line">函数所属包名  函数名          add函数栈帧大小</span><br></pre></td></tr></table></figure></p>
<p>函数栈帧大小：局部变量+可能需要的额外调用函数的参数空间的总大小，不包括调用其它函数时的 ret address 的大小。<br>(SB): SB 是一个虚拟寄存器，保存了静态基地址(static-base) 指针，即我们程序地址空间的开始地址。 “”.add(SB) 表明我们的符号位于某个固定的相对地址空间起始处的偏移位置 (最终是由链接器计算得到的)。换句话来讲，它有一个直接的绝对地址: 是一个全局的函数符号。<br>NOSPLIT: 向编译器表明，不应该插入 stack-split 的用来检查栈需要扩张的前导指令。在我们 add 函数的这种情况下，编译器自己帮我们插入了这个标记: 它足够聪明地意识到，add 不会超出当前的栈，因此没必要调用函数时在这里执行栈检查。<br><strong>4.4.2 变量声明</strong><br>汇编里的全局变量，一般是存储在.rodata或者.data段中。对应到 Go 代码，就是已初始化过的全局的 const、var 变量/常量。</p>
<p>使用 DATA 结合 GLOBL 来定义一个变量。</p>
<p>DATA 的用法为:</p>
<p>DATA symbol+offset(SB)/width, value<br>大多数参数都是字面意思，不过这个 offset 需要注意：其含义是该值相对于符号 symbol 的偏移，而不是相对于全局某个地址的偏移。</p>
<p>GLOBL 汇编指令用于定义名为 symbol 的全局变量，变量对应的内存宽度为 width，内存宽度部分必须用常量初始化。</p>
<p>GLOBL ·symbol(SB), width<br>下面是定义了多个变量的例子:<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">DATA ·age+0(SB)/4, <span class="variable">$8</span>  ;; 数值8为 4字节</span><br><span class="line">GLOBL ·age(SB), RODATA, <span class="variable">$4</span></span><br><span class="line"> </span><br><span class="line">DATA ·pi+0(SB)/8, <span class="variable">$3</span>.1415926 ;; 数值3.1415926为float64, 8字节</span><br><span class="line">GLOBL ·pi(SB), RODATA, <span class="variable">$8</span></span><br><span class="line"> </span><br><span class="line">DATA ·year+0(SB)/4, <span class="variable">$2020</span> ;; 数值2020为 4字节</span><br><span class="line">GLOBL ·year(SB), RODATA, <span class="variable">$4</span></span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">;; 变量hello 使用2个DATA来定义</span><br><span class="line">DATA ·hello+0(SB)/8, $<span class="string">"hello my"</span> ;; `hello my` 共8个字节</span><br><span class="line">DATA ·hello+8(SB)/8, $<span class="string">"   world"</span> ;; `   world` 共8个字节(3个空格)</span><br><span class="line">GLOBL ·hello(SB), RODATA, <span class="variable">$16</span> ;; `hello my   world`  共16个字节</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">DATA ·hello&lt;&gt;+0(SB)/8, $<span class="string">"hello my"</span> ;; `hello my` 共8个字节</span><br><span class="line">DATA ·hello&lt;&gt;+8(SB)/8, $<span class="string">"   world"</span> ;; `   world` 共8个字节(3个空格)</span><br><span class="line">GLOBL ·hello&lt;&gt;(SB), RODATA, <span class="variable">$16</span> ;; `hello my   world`  共16个字节</span><br></pre></td></tr></table></figure></p>
<p>大部分都比较好理解，不过这里引入了新的标记&lt;&gt;，这个跟在符号名之后，表示该全局变量只在当前文件中生效，类似于 C 语言中的 static。如果在另外文件中引用该变量的话，会报 relocation target not found 的错误。</p>
<p><strong>5. 手写汇编实现功能</strong><br>在 Go 源码中会看到一些汇编写的代码，这些代码跟其他 go 代码一起组成了整个 go 的底层功能实现。 下面，我们通过一个简单的 Go 汇编代码示例来实现两数相加功能。</p>
<p><strong>5.1 使用 Go 汇编实现 add 函数</strong><br><img src="https://img-blog.csdnimg.cn/img_convert/52a01fecf15cac8046c2c165cc60d8ea.png" alt></p>
<p>Go 代码<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">add</span><span class="params">(a, b <span class="keyword">int64</span>)</span> <span class="title">int64</span></span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="built_in">println</span>(add(<span class="number">2</span>,<span class="number">3</span>))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>Go 源码中 add()函数只有函数签名，没有具体的实现（使用 GO 汇编实现）</p>
<p>使用 Go 汇编实现的 add()函数<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">TEXT ·add(SB), $<span class="number">0</span><span class="number">-24</span> ;; add栈空间为<span class="number">0</span>，入参+返回值大小=<span class="number">24</span>字节</span><br><span class="line">        MOVQ x+<span class="number">0</span>(FP), AX ;; 从main中取参数：<span class="number">2</span></span><br><span class="line">        ADDQ y+<span class="number">8</span>(FP), AX ;; 从main中取参数：<span class="number">3</span></span><br><span class="line"> </span><br><span class="line">        MOVQ AX, ret+<span class="number">16</span>(FP) ;; 保存结果到返回值</span><br><span class="line"> </span><br><span class="line">        RET</span><br></pre></td></tr></table></figure></p>
<p>把 Go 源码与 Go 汇编编译到一起(我这里，这两个文件在同一个目录)</p>
<p>go build -gcflags “-N -l” .<br>我这里目录为 demo1，所以得到可执行程序 demo1，运行得到结果：5</p>
<p><strong>5.2 反编译可执行程序</strong><br>对 5.1 中得到的可执行程序 demo1 使用 objdump 进行反编译，获取汇编代码<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">go tool objdump -s <span class="string">"main\."</span> demo1</span><br></pre></td></tr></table></figure></p>
<p>得到汇编</p>
<p>……<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">TEXT main.main(SB) /root/go/src/demo1/main.go</span><br><span class="line">  main.go:5   0x4581d0     64488b0c25f8ffffff      MOVQ FS:0xfffffff8, CX</span><br><span class="line">  main.go:5   0x4581d9     483b6110                CMPQ 0x10(CX), SP</span><br><span class="line">  main.go:5   0x4581dd     7655                    JBE 0x458234</span><br><span class="line">  main.go:5   0x4581df     4883ec28                SUBQ <span class="variable">$0x28</span>, SP ;;生成main栈桢</span><br><span class="line">  main.go:5   0x4581e3     48896c2420              MOVQ BP, 0x20(SP)</span><br><span class="line">  main.go:5   0x4581e8     488d6c2420              LEAQ 0x20(SP), BP</span><br><span class="line">  main.go:6   0x4581ed     48c7042402000000        MOVQ <span class="variable">$0x2</span>, 0(SP) ;;参数值 2</span><br><span class="line">  main.go:6   0x4581f5     48c744240803000000      MOVQ <span class="variable">$0x3</span>, 0x8(SP) ;;参数值 3</span><br><span class="line">  main.go:6   0x4581fe     e83d000000              CALL main.add(SB);;call add</span><br><span class="line">  main.go:6   0x458203     488b442410              MOVQ 0x10(SP), AX</span><br><span class="line">  main.go:6   0x458208     4889442418              MOVQ AX, 0x18(SP)</span><br><span class="line">  main.go:6   0x45820d     e8fe2dfdff              CALL runtime.printlock(SB)</span><br><span class="line">  main.go:6   0x458212     488b442418              MOVQ 0x18(SP), AX</span><br><span class="line">  main.go:6   0x458217     48890424                MOVQ AX, 0(SP)</span><br><span class="line">  main.go:6   0x45821b     e87035fdff              CALL runtime.printint(SB)</span><br><span class="line">  main.go:6   0x458220     e87b30fdff              CALL runtime.printnl(SB)</span><br><span class="line">  main.go:6   0x458225     e8662efdff              CALL runtime.printunlock(SB)</span><br><span class="line">  main.go:7   0x45822a     488b6c2420              MOVQ 0x20(SP), BP</span><br><span class="line">  main.go:7   0x45822f     4883c428                ADDQ <span class="variable">$0x28</span>, SP</span><br><span class="line">  main.go:7   0x458233     c3                      RET</span><br><span class="line">  main.go:5   0x458234     e89797ffff              CALL runtime.morestack_noctxt(SB)</span><br><span class="line">  main.go:5   0x458239     eb95                    JMP main.main(SB)</span><br></pre></td></tr></table></figure></p>
<p>;; 反编译得到的汇编与add_amd64.s文件中的汇编大致操作一致<br>TEXT main.add(SB) /root/go/src/demo1/add_amd64.s<br>  add_amd64.s:2   0x458240    488b442408    MOVQ 0x8(SP), AX ;; 获取第一个参数<br>  add_amd64.s:3   0x458245    4803442410    ADDQ 0x10(SP), AX ;;参数a+参数b<br>  add_amd64.s:5   0x45824a    4889442418    MOVQ AX, 0x18(SP) ;;保存计算结果<br>  add_amd64.s:7   0x45824f    c3            RET<br>通过上面操作，可知：</p>
<ol>
<li>(FP)伪寄存器，只有在编写 Go 汇编代码时使用。FP 伪寄存器指向 caller 传递给 callee 的第一个参数</li>
<li>使用 go tool compile / go tool objdump 得到的汇编中看不到(FP)寄存器的踪影<br><strong>6. Go 调试工具</strong><br>这里推荐 2 个 Go 代码调试工具。</li>
</ol>
<p><strong>6.1 gdb 调试 Go 代码</strong><br>测试代码<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">type</span> Ier <span class="keyword">interface</span>&#123;</span><br><span class="line">        add(a, b <span class="keyword">int</span>) <span class="keyword">int</span></span><br><span class="line">        sub(a, b <span class="keyword">int</span>) <span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">type</span> data <span class="keyword">struct</span>&#123;</span><br><span class="line">        a, b <span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(*data)</span> <span class="title">add</span><span class="params">(a, b <span class="keyword">int</span>)</span> <span class="title">int</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a+b</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(*data)</span> <span class="title">sub</span><span class="params">(a, b <span class="keyword">int</span>)</span> <span class="title">int</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a-b</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">var</span> t Ier = &amp;data&#123;<span class="number">3</span>,<span class="number">4</span>&#125;</span><br><span class="line"> </span><br><span class="line">        <span class="built_in">println</span>(t.add(<span class="number">1</span>,<span class="number">2</span>))</span><br><span class="line">        <span class="built_in">println</span>(t.sub(<span class="number">3</span>,<span class="number">2</span>))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>编译 go build -gcflags “-N -l” -o main</p>
<p>使用 GDB 调试<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&gt; gdb main</span><br><span class="line"> </span><br><span class="line">GNU gdb (GDB) Red Hat Enterprise Linux 7.6.1-80.el7</span><br><span class="line">Copyright (C) 2013 Free Software Foundation, Inc.</span><br><span class="line">License GPLv3+: GNU GPL version 3 or later http://gnu.org/licenses/gpl.html</span><br><span class="line">This is free software: you are free to change and redistribute it.</span><br><span class="line">There is NO WARRANTY, to the extent permitted by law.  Type <span class="string">"show copying"</span></span><br><span class="line">and <span class="string">"show warranty"</span> <span class="keyword">for</span> details.</span><br><span class="line">This GDB was configured as <span class="string">"x86_64-redhat-linux-gnu"</span>.</span><br><span class="line">For bug reporting instructions, please see:</span><br><span class="line">&lt;http://www.gnu.org/software/gdb/bugs/&gt;...</span><br><span class="line">Reading symbols from /root/go/src/interface/main...done.</span><br><span class="line">Loading Go Runtime support.</span><br><span class="line">(gdb) list   // 显示源码</span><br><span class="line">14      func (*data) add(a, b int) int&#123;</span><br><span class="line">15              <span class="built_in">return</span> a+b</span><br><span class="line">16      &#125;</span><br><span class="line">17</span><br><span class="line">18      func (*data) sub(a, b int) int&#123;</span><br><span class="line">19              <span class="built_in">return</span> a-b</span><br><span class="line">20      &#125;</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23      func <span class="function"><span class="title">main</span></span>()&#123;</span><br><span class="line">(gdb) list</span><br><span class="line">24              var t Ier = &amp;data&#123;3,4&#125;</span><br><span class="line">25</span><br><span class="line">26              println(t.add(1,2))</span><br><span class="line">27              println(t.sub(3,2))</span><br><span class="line">28      &#125;</span><br><span class="line">29</span><br><span class="line">(gdb) b 26  // 在源码26行处设置断点</span><br><span class="line">Breakpoint 1 at 0x45827c: file /root/go/src/interface/main.go, line 26.</span><br><span class="line">(gdb) r</span><br><span class="line">Starting program: /root/go/src/interface/main</span><br><span class="line"> </span><br><span class="line">Breakpoint 1, main.main () at /root/go/src/interface/main.go:26</span><br><span class="line">26              println(t.add(1,2))</span><br><span class="line">(gdb) info locals  // 显示变量</span><br><span class="line">t = &#123;tab = 0x487020 &lt;data,main.Ier&gt;, data = 0xc000096000&#125;</span><br><span class="line">(gdb) ptype t  // 打印t的结构</span><br><span class="line"><span class="built_in">type</span> = struct runtime.iface &#123;</span><br><span class="line">    runtime.itab *tab;</span><br><span class="line">    void *data;</span><br><span class="line">&#125;</span><br><span class="line">(gdb) p *t.tab.inter  // 打印t.tab.inter指针指向的数据</span><br><span class="line"><span class="variable">$2</span> = &#123;typ = &#123;size = 16, ptrdata = 16, <span class="built_in">hash</span> = 2491815843, tflag = 7 <span class="string">'\a'</span>, align = 8 <span class="string">'\b'</span>, fieldAlign = 8 <span class="string">'\b'</span>,</span><br><span class="line">    kind = 20 <span class="string">'\024'</span>, equal = &#123;void (void *, void *, bool *)&#125; 0x466ec0,</span><br><span class="line">    gcdata = 0x484351 <span class="string">"\002\003\004\005\006\a\b\t\n\f\r\016\017\020\022\025\026\030\033\034\036\037\"&amp;(,-5&lt;BUXx\216\231\330\335\377"</span>, str = 6568, ptrToThis = 23808&#125;, pkgpath = &#123;bytes = 0x4592b4 <span class="string">""</span>&#125;, mhdr =  []runtime.imethod = &#123;&#123;name = 277,</span><br><span class="line">      ityp = 48608&#125;, &#123;name = 649, ityp = 48608&#125;&#125;&#125;</span><br><span class="line">(gdb) disass  // 显示汇编</span><br><span class="line">Dump of assembler code <span class="keyword">for</span> <span class="keyword">function</span> main.main:</span><br><span class="line">   0x0000000000458210 &lt;+0&gt;:     mov    %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">   0x0000000000458219 &lt;+9&gt;:     cmp    0x10(%rcx),%rsp</span><br><span class="line">   0x000000000045821d &lt;+13&gt;:    jbe    0x458324 &lt;main.main+276&gt;</span><br><span class="line">   0x0000000000458223 &lt;+19&gt;:    sub    <span class="variable">$0x50</span>,%rsp</span><br><span class="line">   0x0000000000458227 &lt;+23&gt;:    mov    %rbp,0x48(%rsp)</span><br><span class="line">   0x000000000045822c &lt;+28&gt;:    lea    0x48(%rsp),%rbp</span><br><span class="line">   0x0000000000458231 &lt;+33&gt;:    lea    0x10dc8(%rip),%rax        <span class="comment"># 0x469000</span></span><br><span class="line">   0x0000000000458238 &lt;+40&gt;:    mov    %rax,(%rsp)</span><br><span class="line">   0x000000000045823c &lt;+44&gt;:    callq  0x40a5c0 &lt;runtime.newobject&gt;</span><br></pre></td></tr></table></figure></p>
<p>常用的 gdb 调试命令</p>
<ul>
<li>run</li>
<li>continue</li>
<li>break</li>
<li>backtrace 与 frame</li>
<li>info break、locals</li>
<li>list 命令</li>
<li>print 和 ptype 命令</li>
<li>disass<br>除了 gdb，另外推荐一款 gdb 的增强版调试工具 cgdb<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">https://cgdb.github.io/</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>效果如下图所示，分两个窗口：上面显示源代码，下面是具体的命令行调试界面(跟 gdb 一样)：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/1eecccc9252d545891d7cc13a44c8055.png" alt></p>
<p><strong>6.2 delve 调试代码</strong><br>delve 项目地址<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">https://github.com/go-delve/delve</span><br></pre></td></tr></table></figure></p>
<p>带图形化界面的 dlv 项目地址<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">https://github.com/aarzilli/gdlv</span><br></pre></td></tr></table></figure></p>
<p>dlv 的安装使用，这里不再做过多讲解，感兴趣的可以尝试一下。</p>
<p>gdb 作为调试工具自是不用多说，比较老牌、强大，可以支持多种语言。<br>delve 则是使用 go 语言开发的，用来调试 go 的工具，功能也是十分强大，打印结果可以显示 gdb 支持不了的东西，这里不再做过多讲解，有兴趣的可以查阅相关资料。<br><strong>7. 总结</strong><br>对于 Go 汇编基础大致需要熟悉下面几个方面：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f89d13a5bd7ea6d8a88eea0044aef6ff.png" alt></p>
<p>通过上面的例子相信已经让你对 Go 的汇编有了一定的理解。当然，对于大部分业务开发人员来说，只要看的懂即可。如果想进一步的了解，可以阅读相关的资料或者书籍。</p>
<p>最后想说的是：鉴于个人能力有限，在阅读过程中你可能会发现存在的一些问题或者缺陷，欢迎各位大佬指正。如果感兴趣的话，也可以一起私下交流。</p>
<ol start="8">
<li>参考资料</li>
</ol>
<p>在整理的过程中，部分参考、引用下面链接地址内容。有一些写的还是不错的，感兴趣的同学可以阅读。</p>
<p>[1] <a href="https://github.com/cch123/golang-notes/blob/master/assembly.md" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/cch123/golang-notes/blob/master/assembly.md</a> plan9 assembly</p>
<p>[2] <a href="https://segmentfault.com/a/1190000019753885" rel="external nofollow noopener noreferrer" target="_blank">https://segmentfault.com/a/1190000019753885</a> 汇编入门</p>
<p>[3] <a href="https://www.davidwong.fr/goasm/" rel="external nofollow noopener noreferrer" target="_blank">https://www.davidwong.fr/goasm/</a> Go Assembly by Example</p>
<p>[4] <a href="https://juejin.im/post/6844904005630443533#heading-3" rel="external nofollow noopener noreferrer" target="_blank">https://juejin.im/post/6844904005630443533#heading-3</a></p>
<p>[5] <a href="https://github.com/go-internals-cn/go-internals/blob/master/chapter1_assembly_primer/README.md" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/go-internals-cn/go-internals/blob/master/chapter1_assembly_primer/README.md</a></p>
<p>[6] <a href="https://lrita.github.io/2017/12/12/golang-asm/" rel="external nofollow noopener noreferrer" target="_blank">https://lrita.github.io/2017/12/12/golang-asm/</a></p>
<p>[7] <a href="https://chai2010.cn/advanced-go-programming-book/ch3-asm/ch3-01-basic.html" rel="external nofollow noopener noreferrer" target="_blank">https://chai2010.cn/advanced-go-programming-book/ch3-asm/ch3-01-basic.html</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part2：Garbage Collection基础架构</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part2/</url>
    <content><![CDATA[<p><a href="http://manistein.club/post/program/let-us-build-a-lua-interpreter/%E6%9E%84%E5%BB%BAlua%E8%A7%A3%E9%87%8A%E5%99%A8part2/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog
</a></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>   上一篇我已经设计和实现了Lua解释器最基础的基本数据结构，实现了纯C函数在栈中的调用流程，以及保护模式调用函数等内容，今天要介绍的则是Lua解释器的GC基础架构。Garbage Collection机制，可能是让人们听之不寒而栗的机制，都说GC很复杂，需要花费大量时间去消化和理解，那为什么要在本系列的第2篇就早早引入？做这个决定并非一时冲动之举，而是经过思考后而决定的。之前在阅读了云风大神翻译的《编程之魂》第7章[1]，这篇关于对Lua设计者的访谈记录中，作者有这么说过：</p>
<blockquote>
<p>Roberto：Lua 从第一天开始，就一直使用垃圾收集器。我想说，对于一种解释型语言来讲，垃圾收集器可以比引用计数更加紧凑和健壮，更不用说它没有把垃圾丢得到处都是。考虑到解释型语言通常已经有自描述数据（通过给值加上标签之类的东西），一个简单的标记清除（mark-and-sweep）收集器实现起来极其简单，而且几乎对解释器其余的部分不会产生什么影响。</p>
</blockquote>
<p>从作者的话语中，可以大约了解到，GC机制不是后来加进去的，而是在设计之初，就通盘考虑进去的。而且mark and sweep算法并不是一个很难的算法，甚至可以说的上是比较简单的，它的难在于和lua本身的其他部分结合。事实上gc机制可以单独剥离，在不和lua那些复杂的数据结构（如TString和Table等）结合的情况下，单独设计出来，并且测试，一套基于Incremental Mark and Sweep算法的Garbage Collection基础架构也只有短短300多行，只包含基础功能和设计的gc机制，有利于我阐述解释和说明，因此在这个阶段引入gc机制是最好不过的了，后面在设计和实现其他功能模块的时候，会逐步在今天建立的gc框架下不断完善相关细节。今天所做的则是对搭建这样一套基础gc框架的梳理总结，项目源码地址在这里，欢迎大家star。</p>
<p>   本篇将从传统的最简单的Naive Mark and Sweep算法开始介绍，然后介绍其改进版Incremental Mark and Sweep算法，最后介绍gc基础架构的代码实现和测试用例。</p>
<h1 id="Naive-Mark-and-Sweep算法"><a href="#Naive-Mark-and-Sweep算法" class="headerlink" title="Naive Mark and Sweep算法"></a>Naive Mark and Sweep算法</h1><h2 id="原理探索"><a href="#原理探索" class="headerlink" title="原理探索"></a>原理探索</h2><p>   现在我们进入到Mark and Sweep最初原型的探索阶段，在了解该算法的阶段，我参阅了很多资料，其中言简意赅的则是维基百科的Tracing garbage collection词条[2]。在开始阐述具体算法流程之前，首先我们要先梳理若干个点：</p>
<ul>
<li>所有新创建的对象，都要被放入一个单向链表中(我们姑且称之为allgc链表)，新创建的对象实例，直接放置于列表头部。</li>
<li>所有被创建的实例，均分为可达和不可达状态，可达意味着它正在被使用，或者有潜在被使用的可能，那么global变量，在栈中的变量以及在寄存器中的变量以及被这些变量引用的变量则被视为可达的[3]。</li>
<li><p>GC则是以全局变量，栈和寄存器作为起点开始标记的。<br>为了方便探索算法的本质，我们就以栈作为标记的起点，来探索Mark and Sweep算法：</p>
</li>
<li><p>GC开始前，新创建的对象均放置于allgc链表中</p>
</li>
<li>GC开始，进入标记阶段，从起始点（如栈）开始，标记所有被关联到的对象</li>
<li><p>标记结束后，进入清除阶段，所有未被标记的实例将被释放，而被标记的对象则清除标记，本次gc结束<br>流程很简单，不过需要注意的是，以上步骤只能一步到位执行完，如果步骤可拆分，那么在标记阶段结束之后，新创建的实例，在本轮gc中将无法继续被标记，因此在清除阶段，这个新创建而可能会被使用到的对象将被清除掉，导致不可挽回的bug。</p>
<p> 虽然算法本身并不复杂，这里还是希望通过一个图文举例说明，以使我们的表述更加直观：</p>
</li>
<li><p>如图1所示，在gc开始之前，我们现在创建5个object，他们全部被放在allgc列表中，其中object3被object1引用到，object4被global table引用到，object1、object2、object4和object5均被入栈。<br><img src="naive mark and sweep 01.png" alt></p>
</li>
<li>在GC开始之前，object5和object4被出栈，于是得到如图2的结果：<br><img src="naive mark and sweep 02.png" alt></li>
<li>GC开始，进入标记阶段，以stack和global table作为起始点，被标记的对象用蓝色边框表示，其中object1和object2直接被引用，则它们需要被标记，object3虽然既不在stack中，也不在global table中，但是它被object1引用，因此也会被标记，而object4因为被global table引用，所以也会被标记，只有object5既不在stack中，也不在global table中，也没有在被标记的object引用它，因此它不会被标记，这一切都展示在图3中：<br><img src="naive mark and sweep 03.png" alt></li>
<li>在标记阶段结束后，则进入清除阶段，Mark and Sweep算法会遍历allgc列表，如果链表中的object被标记，则清除标记，否则释放它，在本例中，object5被清除。最终我们得到如图4所示的结果：<br><img src="naive mark and sweep 04.png" alt><br>现在通过wikipedia上的一张gif图，动态展示这个流程<br><img src="330px-Animation_of_the_Naive_Mark_and_Sweep_Garbage_Collector_Algorithm.gif" alt><br><a href="https://en.wikipedia.org/wiki/Tracing_garbage_collection" rel="external nofollow noopener noreferrer" target="_blank">https://en.wikipedia.org/wiki/Tracing_garbage_collection</a></li>
</ul>
<h2 id="优点与缺点"><a href="#优点与缺点" class="headerlink" title="优点与缺点"></a>优点与缺点</h2><p>   我们已经详细探讨了Mark and Sweep基本算法的原理，他的优点和缺点都很明显。优点是足够简单，易于理解，缺点则是它必须一步到位执行完，本质就是stop the world的gc机制，至于为什么需要stop the world，前面也有提到过，这里不再赘述。总的来说，如果一门语言，要作为独立的编程语言，这种算法显得too young，too simple，有时还naive。为了解决这个问题，一种新的改进版的Mark and Sweep机制应运而生，他就是Incremental Mark and Sweep机制。</p>
<h1 id="Incremental-Mark-and-Sweep算法"><a href="#Incremental-Mark-and-Sweep算法" class="headerlink" title="Incremental Mark and Sweep算法"></a>Incremental Mark and Sweep算法</h1><p>  Naive Mark and Sweep算法的劣势在于，一步到位的执行，会在瞬时极大影响程序运行的性能，为了解决这个问题，就必须要允许gc能够分n次执行，每次只执行其中的一小部分，将gc的性能开销均摊掉。基于这样的现实需求，传统的Mark and Sweep算法，只对对象标记表示不可清除，不标记表示可清除这种方式就显得不合时宜了，如果gc要分步骤执行，我们需要一种方式去记录标记的进度，需要一种方式灵活处理不断变化的对象之间的引用关系，为此，Incremental Mark and Sweep算法增加了几种颜色，以及一些新的列表来处理这样的情况，本节首先会介绍颜色的概念，然后再通过伪代码展示gc处理的过程，最后再通过图文例子，直观展示这样一个流程是怎样运作的。<br>  首先我们要介绍的是几种颜色，在Incremental Mark and Sweep算法中，每一轮gc处理中一共有3种颜色参与处理，分别是白色，灰色和黑色。每个新创建的对象，将被标记为白色，而几乎所有的对象，gc的过程中大致要经历被标记和扫描两个阶段，标记阶段会将对象从白色标记为灰色，并放入一个专门放灰色对象的单向链表–gray列表中，这相当于记录了当前gc扫描的进度。然后就是gc对gray链表中的对象，一个一个进行扫描，扫描的过程就是将原本被标记为灰色的对象，标记为黑色，并遍历其所有引用的对象，将它们标记为灰色，并放入gray列表中，以等待下一次扫描。如此循环往复，当gray链表为空时，意味着本轮gc标记阶段结束，此时仍然为白色的实例，意味着它已经不可达，需要被清理，而黑色的则意味对象可达，不能被清除。<br>  这里需要注意的一点是，虽然多数对象，经历标记阶段的时候，是从白色标记为灰色，但对于那种不可能引用其他对象的数据类型（如lua的string类型）是被直接被标记为黑色的。此外lua5.3中的gc，白色有两种，一种是白色，还有一种是另一种白（otherwhite），这两种白色用于不同gc轮回之间的乒乓切换，比如，如果当前gc轮是white0作为白色标记，那么在扫描阶段结束后，新创建的对象就会以white1标记，这样在清除阶段的时候，就只清除被标记为white0的白色对象，而下一轮gc则刚好反过来。<br>  在介绍完gc的基本颜色以后，我们现在来看看gc过程的伪代码：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">function <span class="title">single_step</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    <span class="title">switch</span><span class="params">(gcstate)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">case</span> restart: &#123;</span><br><span class="line">            white2gray(mainthread);      <span class="comment">// mainthread是lua的主线程变量，他本质是一个lua_State类型的变量，</span></span><br><span class="line">                                         <span class="comment">// gc的起始点stack也包含在这个结构里</span></span><br><span class="line">            push mainthread to gray;</span><br><span class="line">            white2gray(global_table);</span><br><span class="line">            push global_table to gray;</span><br><span class="line">            gcstate = propagate;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> propagate: &#123;</span><br><span class="line">            traverse_bytes = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (gray is empty) &#123;</span><br><span class="line">                    gcstate = atomic;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                object = pop from gray;</span><br><span class="line">                gray2black(object);</span><br><span class="line">                </span><br><span class="line">                foreach (ref in object) &#123;</span><br><span class="line">                    white2gray(ref);</span><br><span class="line">                    push ref to gray;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                traverse_bytes += object sizes;</span><br><span class="line">                <span class="keyword">if</span> (traverse_bytes &gt;= per <span class="built_in">step</span> <span class="built_in">max</span> traverse bytes) &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; </span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> atomic: &#123;</span><br><span class="line">            foreach(object in grayagain) &#123;</span><br><span class="line">                gray2black(object);</span><br><span class="line">                foreach(ref in object) &#123;</span><br><span class="line">                    white2gray(ref);</span><br><span class="line">                    push ref to gray;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">while</span> (gray is <span class="keyword">not</span> empty) &#123;</span><br><span class="line">                object = pop from gray;</span><br><span class="line">                gray2black(object);</span><br><span class="line">                </span><br><span class="line">                foreach(ref in object) &#123;</span><br><span class="line">                    white2gray(ref);</span><br><span class="line">                    push ref to gray;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            change white to otherwhite;         <span class="comment">// 将白色切换为另一种白</span></span><br><span class="line">            sweepgco = head of allgc;</span><br><span class="line">            gcstate = sweepgc;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> sweep: &#123;</span><br><span class="line">            count = <span class="number">0</span>;</span><br><span class="line">            foreach(sweepgco is exist) &#123;</span><br><span class="line">                next_gc_object = sweepgco-&gt;next;</span><br><span class="line">                <span class="keyword">if</span> (sweepgco is dead) &#123;          <span class="comment">// 只有白色为本次gc标记的白色，才算是dead，otherwhite是下次gc用的白色</span></span><br><span class="line">                    <span class="built_in">free</span> sweepgco </span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="built_in">set</span> sweepgco to white;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                sweepgco = next_gc_object;</span><br><span class="line">                count ++;</span><br><span class="line">                <span class="keyword">if</span> (count &gt;= per <span class="built_in">step</span> foreach count) &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (sweepgco is <span class="keyword">not</span> exist) &#123;</span><br><span class="line">                gcstate = restart;</span><br><span class="line">            &#125; </span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>: <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="built_in">end</span></span><br></pre></td></tr></table></figure></p>
<p>上面这段伪代码，是每次执行gc时，会执行的主要流程，这里先展示伪代码的目的，是为了让大家先建立直观的感觉，然后再辅以文字说明，能够达到更好的效果。<br>   从上述伪代码，我们可以知道，整个gc执行的过程中，大致要经历几个阶段（实际编码时，状态并不止这些，但是为了捋清gc机制的主要流程，这些步骤基本是最核心的步骤了），他们分别是restart阶段、propagate阶段、atomic阶段和sweep阶段，现在分别对这些不同的阶段进行说明：</p>
<ul>
<li>restart阶段：在lua中，包含stack变量的mainthread和global_table都是要被gc关注的对象，我们已经讨论过，gc的起始点是globals、stack和register，register在lua的gc中，并没有特别的意义，因为所有的操作都是在stack上进行的。因为mainthread和global_table包含gc起始点，因此他们要先push入gray列表中。并且标记他们为灰色，这些操作结束后，就进入到了propagate阶段。</li>
<li><p>propagate阶段：从伪代码我们可以看到，propagate阶段所做的事情，就是不断从gray列表中pop出object，然后把它从灰色变为黑色，再遍历它所有引用的对象，并将其push到gray列表中。同时我们也要注意到，每次将一个从gray列表中pop出来的object，标记为黑色后，需要累计这个object的字节大小，当这一次gc，propagate(也就是扫描)的对象累计超过一定的字节数时，本轮gc会被暂停，等待下一次gc开始后，继续propagate gray列表中的对象。到这里，我们可以初步感受到，什么是步进式地进行gc处理，其实就是每次处理一部分，将gc处理的瞬时开销，均摊开来。当我们的gray列表为空时，意味着所有的gray列表均已propagate完毕，然后进入atomic阶段。</p>
</li>
<li><p>atomic阶段：和propagate阶段不同，atomic阶段是需要被原子执行的，也就是说进入到这个阶段，就得不中断地重头执行到阶段结束。之所以要这么做，需要花费一些功夫来进行解释和说明。<br><strong> 首先，因为我们的gc在propagate阶段是可以被中断的，也就是说，在中断的过程中，可能会有新的对象被创建，并且被已经被标记为黑色的对象引用，这种gc算法，是不能将黑色的对象，直接引用白色的对象的，因为黑色的对象已经标记和扫描完毕，本轮gc不会再进行扫描，这样被其引用的白色对象也不会被标记和扫描到，到了sweep阶段，因为新创建的对象未被标记和扫描，因此会被当做不可达的对象被清除掉，造成不可挽回的后果。
</strong> 为此，我们需要为这种情况下，为新创建的对象设置barrier。barrier分为两种，一种是向前设置barrier，也就是直接将新创建的对象设置为灰色，并放入gray列表；还有一种则是向后设置barrier，这种是将黑色对象设置为灰色，然后放入grayagain列表。向前barrier的情况，适用于已被标记为黑色的对象类型，为不会频繁改变引用关系的数据类型，如lua的proto结构。而向后barrier的情况，适合被标记为黑色的对象类型，为会出现频繁改变引用关系情况的数据类型，如lua的table结构，也就是说，在两次调用gc功能的过程中，table中的同一个key，可能被赋值多个value，如果把这些value对象均标记为灰色，并放入gray列表，那么将会造成许多无谓的标记和扫描操作，因为这些value很可能不再被引用，需要被回收，因此，只要把已经被标记为黑色的table，重新设置为灰色，是避开这个性能问题的良好方式。<br><strong> 而如果我们直接把从黑色重新标记为灰色的table对象，放入gray列表的话，如上所述，table的key和value的引用关系变化频繁，这个table很可能在黑色和灰色之间来回切换，进行很多重复的扫描，为了提高效率，则将他放在grayagain列表中，在atomic阶段，一次性标记和扫描完。
</strong> 为什么要原子执行atomic阶段？如果atomic阶段不能原子执行，那么就和propagate阶段没有区别了，table从黑色被标记为灰色，放入grayagain列表也失去了意义，因为如果不能原子执行，那么该table对象很可能前脚刚在grayagain列表中pop出来，由灰色变黑色，后脚又有新的对象被它引用，又将它从黑色变为灰色，因此避免不了一些table实例，在黑色和灰色之间来回切换，反复标记和扫描，浪费性能，很可能导致gc在标记和扫描阶段所处的时间过长，甚至无法进入sweep阶段，因此刚脆在atomic阶段设置为不可中断执行，一次完成所有的标记和扫描操作。这样，一个能够被频繁改变引用关系的table对象，最多在progapate阶段的时候被标记和扫描一次，在atomic阶段又被扫描一次，一共两次。<br>** 上述几个点，详细阐述了我们为什么需要有一个grayagain列表，为什么atomic阶段必须要原子执行，到现在为止我们梳理了来龙去脉，在atomic阶段结束以后，标记对象的白色会切换成另一种白，同时我们就进入到了sweep阶段。</p>
</li>
<li>sweep阶段：sweep阶段则很简单，每次我们执行这个流程，取出若干个对象，如果它已经是本轮gc要被清除的白色，那么它会被清除，如果不是，则标记为另一种白，以供下一次gc使用，当本轮gc涉及到的所有的对象清理和重置完毕后，进入下一轮gc。<br> 整段伪代码和上述的文字，已经对Incremental Mark and Sweep算法进行了较为彻底的解释和说明了，为此在本小节的最后，附上维基百科词条上找的图片，动态演示这个过程：<br><a href="Animation_of_tri-color_garbage_collection.gif"></a><br><a href="https://en.wikipedia.org/wiki/Tracing_garbage_collection" rel="external nofollow noopener noreferrer" target="_blank">wiki</a></li>
</ul>
<h1 id="Garbage-Collection基础架构逻辑实现"><a href="#Garbage-Collection基础架构逻辑实现" class="headerlink" title="Garbage Collection基础架构逻辑实现"></a>Garbage Collection基础架构逻辑实现</h1><p>   终于到了实现环节了，我们已经介绍了gc机制的原理，现在先从实现层面来看待gc接口的使用。对于外部而言，我们最希望的还是在创建对象以后，仅仅通过调用一个接口，就能使得gc运作，而不用关心它内部的逻辑实现，因此我们在使用层面可能类似如下代码所示的情况：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> j = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (; j &lt; <span class="number">500000000</span>; j ++) &#123;</span><br><span class="line">     TValue* o = luaL_index2addr(L, (j % ELEMENTNUM) + <span class="number">1</span>);</span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gco</span> = <span class="title">luaC_newobj</span>(<span class="title">L</span>, <span class="title">LUA_TSTRING</span>, <span class="title">sizeof</span>(<span class="title">TString</span>));</span></span><br><span class="line">     o-&gt;value_.gc = gco;</span><br><span class="line">     o-&gt;tt_ = LUA_TSTRING;</span><br><span class="line">     luaC_checkgc(L);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>正如上面的代码所示，我们所有的gc操作，都将在luaC_checkgc里完成。不过现在，我还不想展开其内部的所有细节，在开始论述细节之前，我们首先得了解gc的基本数据结构。<br>        现在我们在vm目录下，新增luagc.h和luagc.c两个文件，现在目录变成下面展示的那样：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">+ bin/</span><br><span class="line">+ clib/</span><br><span class="line">+ common/</span><br><span class="line">+ compiler/</span><br><span class="line">~ vm/</span><br><span class="line">    luado.h</span><br><span class="line">    luado.c</span><br><span class="line">    luagc.h</span><br><span class="line">    luagc.c</span><br><span class="line">main.c</span><br></pre></td></tr></table></figure></p>
<p>在lua中，受gc管理的数据类型主要有string、table、function、proto和lua_State，它们都需要有一个公共头部，由于lua是纯C实现的，因此，不能设置一个基类供这些类型继承，取而代之的是，通过一个宏来定义gc对象类型的公共部分，我将其定义在luaobject.h中，并以CommonHeader命名：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CommonHeader struct GCObject* next; lu_byte tt_; lu_byte marked</span></span><br><span class="line">``` </span><br><span class="line">这个CommonHeader一共有<span class="number">3</span>个字段，他们分别的意义是：</span><br><span class="line"></span><br><span class="line">* next：在allgc链表中，指定下一个gc对象的指针</span><br><span class="line">* tt_：记录gc对象的类型，不同的gc对象在propagate阶段有不同的处理逻辑</span><br><span class="line">* marked：用来标记gc对象颜色用的</span><br><span class="line">上一章节，我们定义了代表任何lua类型的Value类型，现在我们需要它也能表示gc类型，由于它是一个<span class="keyword">union</span>类型，而gc的字段必须是整合在一起的，因此我们还需要定义一个结构GCObject，然后让Value类型包含这个GCObject：</span><br><span class="line">```c</span><br><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span> &#123;</span></span><br><span class="line">    CommonHeader;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> lua_Value &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gc</span>;</span></span><br><span class="line">    <span class="keyword">void</span>* p;</span><br><span class="line">    <span class="keyword">int</span> b;</span><br><span class="line">    lua_Integer i;</span><br><span class="line">    lua_Number n;</span><br><span class="line">    lua_CFunction f;</span><br><span class="line">&#125; Value;</span><br></pre></td></tr></table></figure></p>
<p>现在TValue也能表示被gc管理的对象类型了，到目前为止，具体的类型我们只实现了lua_State，现在需要在lua_State的头部加上这个CommonHeader的宏，限于篇幅，以及后面我们还需要往lua_State内添加其他gc相关的字段，这里就不展示代码了。<br>   在我们编写解释器的过程中，经常会遇到Value类型和具体的类型（如lua_State）相互转换的情况，为了达到这个目的，我们需要设计一个union类型GCUnion来表示任何，需要被GC管理的数据类型，而它的定义如下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastate.h</span></span><br><span class="line"><span class="comment">// GCUnion</span></span><br><span class="line"><span class="keyword">union</span> GCUnion &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span> <span class="title">gc</span>;</span></span><br><span class="line">    lua_State th;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>有了这个结构，我们就可以实现具体类型和Value类型的互转了，具体逻辑实现如下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> obj2gco(o) (&amp;cast(union GCUnion*, o)-&gt;gc)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> gco2th(o)  check_exp((o)-&gt;tt_ == LUA_TTHREAD, &amp;cast(union GCUnion*, o)-&gt;th)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> gcvalue(o) ((o)-&gt;value_.gc)</span></span><br></pre></td></tr></table></figure></p>
<p>具体的数据类型，将调用obj2gco将自己转成Value的GCObject域的指针，而Value要转成具体类型，如lua_State，则通过gco2th这样的接口来转化，不论谁转谁，首先都要转成GCUnion类型的指针，再做进一步转换。<br>        现在要开始介绍gc颜色处理的具体实现了，首先我们要定义几个位操作的宏：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.h</span></span><br><span class="line"><span class="comment">// Bit operation</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> bitmask(b) (1&lt;&lt;b)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> bit2mask(b1, b2) (bitmask(b1) | bitmask(b2))</span></span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> resetbits(x, m) ((x) &amp;= cast(lu_byte, ~(m)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> setbits(x, m) ((x) |= (m))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> testbits(x, m) ((x) &amp; (m))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> resetbit(x, b) resetbits(x, bitmask(b)) </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> l_setbit(x, b) setbits(x, bitmask(b))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> testbit(x, b) testbits(x, bitmask(b))</span></span><br></pre></td></tr></table></figure></p>
<p>这些宏，有进行位移的操作，有进行与、或和异或操作，后面我们在进行颜色操作的时候，会频繁用到这些宏，逻辑都很直观，有程序基础的人看懂都不是问题。接下来，我们定义和颜色相关的宏：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//luagc.h</span></span><br><span class="line"><span class="comment">// Color</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> WHITE0BIT       0</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> WHITE1BIT       1</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BLACKBIT        2</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> WHITEBITS bit2mask(WHITE0BIT, WHITE1BIT)</span></span><br></pre></td></tr></table></figure></p>
<p>我们可以看到，白色有两个宏定义，黑色只有一个宏定义，而灰色则一个宏定义都没有，这是因为在lua官方的实现版本中，白色有两种，在不同的gc轮之间执行乒乓交换，而既不是白色也不是黑色则被视为是灰色，因此没有对灰色进行直接定义。最下面的WHITEBITS则是非常关键的存在，因为它是我们切换白色、判断对象是否dead以及标记对象为白色的重要参数，根据bit2mask的定义，WHITEBITS实际相当于被这样定义：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> WHITEBITS ((1&lt;&lt;0) | (1&lt;&lt;1))</span></span><br></pre></td></tr></table></figure></p>
<p>也就是说，WHITEBITS最后相当于二进制值112，现在我们可以来看一下标记一个对象为白色的接口是怎样的：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> luaC_white(g) (g-&gt;currentwhite &amp; WHITEBITS)</span></span><br></pre></td></tr></table></figure></p>
<p>g-&gt;currentwhite在初始化阶段，被赋值为012值，由于WHITEBITS是112，012 &amp; 112还是012能够获得当前白色的值。而切换不同白的宏定义，则是这样的：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> otherwhite(g) (g-&gt;currentwhite ^ WHITEBITS)</span></span><br></pre></td></tr></table></figure></p>
<p>这里是一个异或操作，也就是说如果g-&gt;currentwhite的值是012的话，通过otherwhite计算，则是012 ^ 112 = 102，如果g-&gt;currentwhite的值是102的话，结果正好相反。从这里的逻辑我们可以看出，white的值只有两种，要么是012，要么是102。<br>        现在我们来看看颜色相关的判断：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> iswhite(o) testbits((o)-&gt;marked, WHITEBITS)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> isgray(o)  (!testbits((o)-&gt;marked, bitmask(BLACKBIT) | WHITEBITS))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> isblack(o) testbit((o)-&gt;marked, bitmask(BLACKBIT))</span></span><br></pre></td></tr></table></figure></p>
<p>这段逻辑没什么好说的，都很直观，需要注意的是，灰色的值是0。接下来要看的是颜色间的互转宏：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> white2gray(o) resetbits((o)-&gt;marked, WHITEBITS)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> gray2black(o) l_setbit((o)-&gt;marked, BLACKBIT)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> black2gray(o) resetbit((o)-&gt;marked, BLACKBIT)</span></span><br></pre></td></tr></table></figure></p>
<p>从上可以看到，从白色转到灰色，需要把白色标记位清掉，因为灰色的值是0，黑色转灰色也是要把黑色标记位清掉，而灰色转黑色，则相当于把值设置为1002。</p>
<p>  在迅速过一篇，基本的定义以后，我们要看看最终要的数据结构global_State和lua_State到底新增了什么字段：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastate.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span> &#123;</span></span><br><span class="line">     CommonHeader;       <span class="comment">// gc header, all gcobject should have the commonheader</span></span><br><span class="line">     StkId <span class="built_in">stack</span>;</span><br><span class="line">     StkId stack_last;</span><br><span class="line">     StkId top;</span><br><span class="line">     <span class="keyword">int</span> stack_size;</span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">lua_longjmp</span>* <span class="title">errorjmp</span>;</span></span><br><span class="line">     <span class="keyword">int</span> status;</span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">previous</span>;</span></span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span> <span class="title">base_ci</span>;</span></span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">ci</span>;</span></span><br><span class="line">     <span class="keyword">int</span> nci;</span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">l_G</span>;</span></span><br><span class="line">     <span class="keyword">ptrdiff_t</span> errorfunc;</span><br><span class="line">     <span class="keyword">int</span> ncalls;</span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gclist</span>;</span></span><br><span class="line"> &#125; lua_State;</span><br><span class="line"> </span><br><span class="line"> <span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span> &#123;</span></span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">mainthread</span>;</span></span><br><span class="line">     lua_Alloc frealloc;</span><br><span class="line">     <span class="keyword">void</span>* ud; </span><br><span class="line">     lua_CFunction panic;</span><br><span class="line">  </span><br><span class="line">     <span class="comment">//gc fields</span></span><br><span class="line">     lu_byte gcstate;</span><br><span class="line">     lu_byte currentwhite;</span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">allgc</span>;</span>         <span class="comment">// gc root set</span></span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>** <span class="title">sweepgc</span>;</span></span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gray</span>;</span></span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">grayagain</span>;</span></span><br><span class="line">     lu_mem totalbytes;</span><br><span class="line">     l_mem GCdebt;                   <span class="comment">// GCdebt will be negative</span></span><br><span class="line">     lu_mem GCmemtrav;               <span class="comment">// per gc step traverse memory bytes </span></span><br><span class="line">     lu_mem GCestimate;              <span class="comment">// after finish a gc cycle,it records total memory bytes (totalbytes + GCdebt)</span></span><br><span class="line">     <span class="keyword">int</span> GCstepmul;</span><br><span class="line"> &#125; global_State;</span><br></pre></td></tr></table></figure></p>
<p>lua_State新增的字段很少，只有头部加了CommonHeader以及尾部加了一个GCObject指针的字段–gclist，这个gclist的作用是，当lua_State对象在gray链表时，它指向gray链表中的下一个灰色对象的地址，当lua_State对象在grayagain链表时，它指向grayagain链表中，下一个灰色对象的地址，其实就是相当于gray或者grayagain链表中，数据对象的next指针。<br>   相比于lua_State结构，我们的global_State结构新增的，与gc相关的字段则要多得多，现在一个一个对其进行解释：</p>
<ul>
<li>gcstate：标记gc当前处于哪个阶段，我们gc过程一共要经历几个阶段，分别是GCSpause、GCSpropagate、GCSatomic、GCSinsideatomic、GCSsweepgc和GCSsweepend，和我上面展示的伪代码有些出入，但是大体流程是相同的，后面我们会详细讨论。</li>
<li>currentwhite：当前gc是哪种白色，102与012中的一种，在gc的atomic阶段结束时，会从一种切换为另一种。</li>
<li>allgc：这是一个单向链表，所有新建的gc对象，都要放入到这个链表中，放入的方式是直接放在链表的头部。</li>
<li>sweepgc：这个变量用于记录当前sweep的进度。</li>
<li>gray：gc对象，首次从白色被标记为灰色的时候，会被放入这个列表，放入这个列表的gc对象，是准备被propagate的对象。</li>
<li>grayagain：前文已经提到了grayagain的作用了，table对象，从黑色变回灰色时，会放入这个链表中，作用是避免table反复在黑色和灰色之间来回切换重复扫描。</li>
<li>totalbytes：记录开辟内存字节大小的变量之一，真实的大小是totalbytes+GCdebt。</li>
<li>GCdebt：这是一个可以为负数的变量，主要用于控制gc触发的时机。当GCdebt&gt;0时，才能触发gc流程。</li>
<li>GCmemtrav：每次进行gc操作时，所遍历的对象字节大小之和，单位是byte，当其值大于单步执行的内存上限时，gc终止，上节的伪代码有这方面的展示，如果遗忘可以回顾一下。</li>
<li>GCestimate：在sweep阶段结束时，会被重新计算，本质是totalbytes+GCdebt，它的作用是，在本轮gc结束时，将自身扩充两倍大小，然后让真实大小减去扩充后的自己得到差debt，然后totalbytes会等于扩充后的自己，而GCdebt则会被负数debt赋值，就是是说下一次执行gc流程，要在有|debt|个bytes内存被开辟后，才会开始。目的是避免gc太过频繁。</li>
<li>GCstepmul：一个和GC单次处理多少字节相关的参数。<br> gc对象，在创建的开始，就已经被gc机制所监管，而我们将通过一个通用的创建gc的函数，来处理这个gc机制监管的开始流程，该函数定义在luagc.c中：<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c</span></span><br><span class="line"><span class="function">struct GCObject* <span class="title">luaC_newobj</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> tt_, <span class="keyword">size_t</span> <span class="built_in">size</span>)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">obj</span> = (<span class="title">struct</span> <span class="title">GCObject</span>*)<span class="title">luaM_realloc</span>(<span class="title">L</span>, <span class="title">NULL</span>, 0, <span class="title">size</span>);</span></span><br><span class="line">    obj-&gt;marked = luaC_white(g);</span><br><span class="line">    obj-&gt;next = g-&gt;allgc;</span><br><span class="line">    obj-&gt;tt_ = tt_;</span><br><span class="line">    g-&gt;allgc = obj;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>不难发现，这个函数先创建指定字节大小的堆内存以后，将其转成GCObject指针对象，在标记为白色后，放入allgc链表的头部。所有创建出来的对象，都会被放入allgc链表，因此所有的对象都能被跟踪到。在这个创建过程中，我们开辟内存的函数luaM_realloc函数也有一些改动，变为：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luamem.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> luaM_free(L, ptr, osize) luaM_realloc(L, ptr, osize, 0)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// luamem.c</span></span><br><span class="line"><span class="function"><span class="keyword">void</span>* <span class="title">luaM_realloc</span><span class="params">(struct lua_State* L, <span class="keyword">void</span>* ptr, <span class="keyword">size_t</span> osize, <span class="keyword">size_t</span> nsize)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    <span class="keyword">int</span> oldsize = ptr ? osize : <span class="number">0</span>;</span><br><span class="line">        </span><br><span class="line">    <span class="keyword">void</span>* ret = (*g-&gt;frealloc)(g-&gt;ud, ptr, oldsize, nsize);</span><br><span class="line">    <span class="keyword">if</span> (ret == <span class="literal">NULL</span> &amp;&amp; nsize &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        luaD_throw(L, LUA_ERRMEM);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    g-&gt;GCdebt = g-&gt;GCdebt - oldsize + nsize;</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里新增了对GCdebt进行统计的逻辑，当osize为0，且nsize &gt; 0时，说明这是一个创建新内存块，那么GCdebt就要加上这个新创建对象的内存大小。如果osize&gt;0且nsize为0时，说明程序要释放一个对象，那么GCdebt要减去这个内存的大小。如果这个对象调整了内存大小，GCdebt也能精确统计到实际的内存大小。<br>  一个对象被创建以后，我们就需要在合适的时机去进行gc检查和处理，这里将提供一个luaC_checkgc的宏来完成这个任务，事实上我们在创建完一个对象以后，都可以调用这个宏，因为这个宏所代表的逻辑，有对gc触发时机的管理流程，外部无需关注，这个宏定义在luagc.h中：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"> <span class="comment">// luagc.h</span></span><br><span class="line"> <span class="comment">// try trigger gc</span></span><br><span class="line"> <span class="meta">#<span class="meta-keyword">define</span> luaC_condgc(pre, L, pos) <span class="meta-keyword">if</span> (G(L)-&gt;GCdebt &gt; 0) &#123; pre; luaC_step(L); pos; &#125; </span></span><br><span class="line"> <span class="meta">#<span class="meta-keyword">define</span> luaC_checkgc(L) luaC_condgc((void)0, L, (void)0)</span></span><br><span class="line">我们可以看到，只有当GCdebt的值大于<span class="number">0</span>的时候，才能触发gc运作机制，而gc运作的机制均是在luaC_step函数里执行：</span><br><span class="line"></span><br><span class="line"> <span class="comment">// luagc.c</span></span><br><span class="line"> <span class="function"><span class="keyword">void</span> <span class="title">luaC_step</span><span class="params">(struct lua_State*L)</span> </span>&#123;</span><br><span class="line">     <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">     l_mem debt = get_debt(L);</span><br><span class="line">     <span class="keyword">do</span> &#123;</span><br><span class="line">         l_mem work = singlestep(L);</span><br><span class="line">         debt -= work;</span><br><span class="line">     &#125;<span class="keyword">while</span>(debt &gt; -GCSTEPSIZE &amp;&amp; G(L)-&gt;gcstate != GCSpause);</span><br><span class="line"> </span><br><span class="line">     <span class="keyword">if</span> (G(L)-&gt;gcstate == GCSpause) &#123;</span><br><span class="line">         setpause(L);</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="keyword">else</span> &#123;</span><br><span class="line">         debt = debt / g-&gt;GCstepmul * STEPMULADJ;</span><br><span class="line">         setdebt(L, debt);</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure></p>
<p>这个函数，首先获取了debt的字节大小，所有的gc流程都在singlestep函数里进行处理，这里需要注意的是，当lua解释器能被处理的内存字节总大小，小于debt+GCSTEPSIZE时，那么gc将是一步到位执行完毕（gc的初始状态就是GCSpause，在上述的逻辑中，如果while条件判断内，gcstate等于GCSpause的话，说明本轮gc已经彻底完结，即将进入下一个gc轮回中）。当创建的gc对象足够多时，每次触发luaC_step函数，只会处理至多debt+GCSTEPSIZE个字节的数据，GCSTEPSIZE我设置为1k，通常运行过程中，单次执行gc操作也差不多是处理1kb的数据。当程序满足跳出while循环的条件后，如果一轮gc已经完整执行完毕，那么需要重新设置totalbytes和GCdebt变量的大小，这些操作在setpause函数内执行：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// when memory is twice bigger than current estimate, it will trigger gc</span></span><br><span class="line"><span class="comment">// again</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">setpause</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    l_mem estimate = g-&gt;GCestimate / GCPAUSE;</span><br><span class="line">    estimate = (estimate * g-&gt;GCstepmul) &gt;= MAX_LMEM ? MAX_LMEM : estimate * g-&gt;GCstepmul;</span><br><span class="line"></span><br><span class="line">    l_mem debt = g-&gt;GCestimate - estimate;</span><br><span class="line">    setdebt(L, debt);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>在sweep阶段结束时，我们会重新计算GCestimate的值，其实质是totalbytes+GCdebt，代表当前lua解释器内存的真实大小。GCestimate首先除以一个GCPAUSE常量，然后再乘以GCstepmul变量，以获取新的估算值，这里我参照lua53的标准，将GCstepmul设置为GCPAUSE的两倍，也就是说GCestimate被扩充为当前内存实际大小的两倍。在计算实际大小和扩充大小的差值以后，将会进行设置操作：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c                                                                                                                                                        X </span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">setdebt</span><span class="params">(struct lua_State* L, l_mem debt)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    lu_mem totalbytes = gettotalbytes(g);</span><br><span class="line"></span><br><span class="line">    g-&gt;totalbytes = totalbytes - debt;</span><br><span class="line">    g-&gt;GCdebt = debt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里的处理很简单，将差值的绝对值累加到totalbytes上，并把GCdebt设置为负数，这里相当于内存”负债”，因为GCdebt小于0，gc操作不会被触发，但是真实的内存大小是totalbytes+GCdebt并没有改变，这样做的目的是，当新创建的内存大小，足以还清GCdebt所欠下的”债务”时（GCdebt&gt;0），才会触发新的gc流程。这种操作时为了避免gc过度频繁地被调用。同样的，在luaC_step内处理够足够多的对象以后（这里是1kb左右），我们会将这1kb左右的内存，累加到totalbytes上，而GCdebt则等于负的1kb，产生”负债”，当lua虚拟机有新的1kb的内存被开辟时，gc逻辑流程才会被再次触发。         上文我们介绍的gc的触发流程控制，接下来来看看我们的gc处理流程函数signlestep。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c                                                                                                                                                        X </span></span><br><span class="line"><span class="function"><span class="keyword">static</span> lu_mem <span class="title">singlestep</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    <span class="keyword">switch</span>(g-&gt;gcstate) &#123;</span><br><span class="line">        <span class="keyword">case</span> GCSpause: &#123;</span><br><span class="line">            g-&gt;GCmemtrav = <span class="number">0</span>;</span><br><span class="line">            restart_collection(L);     <span class="comment">// markroot阶段，将lua_State标记为灰色，并push到gray列表中</span></span><br><span class="line">            g-&gt;gcstate = GCSpropagate;</span><br><span class="line">            <span class="keyword">return</span> g-&gt;GCmemtrav;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> GCSpropagate:&#123;</span><br><span class="line">            g-&gt;GCmemtrav = <span class="number">0</span>;</span><br><span class="line">            propagatemark(L);         <span class="comment">// 从gray列表中，pop出一个对象，并标记为黑色，同时扫描其关联对象，设置为灰色并放入gray列表。</span></span><br><span class="line">            <span class="keyword">if</span> (g-&gt;gray == <span class="literal">NULL</span>) &#123;</span><br><span class="line">                g-&gt;gcstate = GCSatomic;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> g-&gt;GCmemtrav;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> GCSatomic:&#123;</span><br><span class="line">            g-&gt;GCmemtrav = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">if</span> (g-&gt;gray) &#123;</span><br><span class="line">                propagateall(L); <span class="comment">// 一次性将gray列表中所有的对象标记和扫描</span></span><br><span class="line">            &#125;</span><br><span class="line">            atomic(L);           <span class="comment">// 一次性将grayagain链表中所有的对象标记和扫描</span></span><br><span class="line">            entersweep(L);</span><br><span class="line">            g-&gt;GCestimate = gettotalbytes(g);</span><br><span class="line">            <span class="keyword">return</span> g-&gt;GCmemtrav;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> GCSsweepallgc: &#123;</span><br><span class="line">            g-&gt;GCmemtrav = <span class="number">0</span>;</span><br><span class="line">            sweepstep(L);</span><br><span class="line">            <span class="keyword">return</span> g-&gt;GCmemtrav;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> GCSsweepend: &#123;</span><br><span class="line">            g-&gt;GCmemtrav = <span class="number">0</span>;</span><br><span class="line">            g-&gt;gcstate = GCSpause;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:<span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> g-&gt;GCmemtrav;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure></p>
<p>逻辑代码和伪代码很相似，如果能看懂上面展示的伪代码，这段逻辑应该不难理解。不过这里对部分逻辑做了注释，完整的项目代码可以在这里找到。</p>
<pre><code>到目前为止，重要的内容基本写完了，在文章的最后，我觉得有必要将测试用例贴出来，让大家直观感受一下最终的效果：
</code></pre><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// p2_test.c                                                                                                                                                      X </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"../clib/luaaux.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"../vm/luagc.h"</span> </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"p2_test.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line">            </span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ELEMENTNUM 5</span></span><br><span class="line">        </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">p2_test_main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">   <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">L</span> = <span class="title">luaL_newstate</span>();</span></span><br><span class="line">            </span><br><span class="line">   <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">   <span class="keyword">for</span> (; i &lt; ELEMENTNUM; i ++) &#123;</span><br><span class="line">       luaL_pushnil(L);</span><br><span class="line">   &#125;        </span><br><span class="line">        </span><br><span class="line">   <span class="keyword">int</span> start_time = time(<span class="literal">NULL</span>);</span><br><span class="line">   <span class="keyword">int</span> end_time = time(<span class="literal">NULL</span>);</span><br><span class="line">   <span class="keyword">size_t</span> max_bytes = <span class="number">0</span>; </span><br><span class="line">   <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">   <span class="keyword">int</span> j = <span class="number">0</span>;</span><br><span class="line">   <span class="keyword">for</span> (; j &lt; <span class="number">500000000</span>; j ++) &#123;</span><br><span class="line">        TValue* o = luaL_index2addr(L, (j % ELEMENTNUM) + <span class="number">1</span>);</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gco</span> = <span class="title">luaC_newobj</span>(<span class="title">L</span>, <span class="title">LUA_TSTRING</span>, <span class="title">sizeof</span>(<span class="title">TString</span>));</span></span><br><span class="line">        o-&gt;value_.gc = gco;</span><br><span class="line">        o-&gt;tt_ = LUA_TSTRING;</span><br><span class="line">        luaC_checkgc(L);</span><br><span class="line">            </span><br><span class="line">        <span class="keyword">if</span> ((g-&gt;totalbytes + g-&gt;GCdebt) &gt; max_bytes) &#123;</span><br><span class="line">            max_bytes = g-&gt;totalbytes + g-&gt;GCdebt;</span><br><span class="line">        &#125; </span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> (j % <span class="number">1000</span> == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"timestamp:%d totalbytes:%f kb \n"</span>, (<span class="keyword">int</span>)time(<span class="literal">NULL</span>), (<span class="keyword">float</span>)(g-&gt;totalbytes + g-&gt;GCdebt) / <span class="number">1024.0f</span>);</span><br><span class="line">        &#125;   </span><br><span class="line">   &#125;    </span><br><span class="line">   end_time = time(<span class="literal">NULL</span>);</span><br><span class="line">   <span class="built_in">printf</span>(<span class="string">"finish test start_time:%d end_time:%d max_bytes:%f kb \n"</span>, start_time, end_time, (<span class="keyword">float</span>)max_bytes / <span class="number">1024.0f</span>);</span><br><span class="line"> </span><br><span class="line">   luaL_close(L);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面的测试代码，创建了5亿次gc对象，新创建的对象，不断在适当的stack位置替换旧的gc对象，如图7所示，内存大小的峰值大约是1.7k，这说明gc在不断进行，回收不可达的gc对象：<br><img src="gc_test_result.png" alt></p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>   到这里，gc相关的东西也基本完成了，通过这篇blog我们可以了解传统Mark and Sweep Garbage Collection算法的原理，以及其优势和劣势，同时我们也细致探索了其改进型Incremental Mark and Sweep算法。本文刻意简化了代码说明部分，希望让有兴趣了解更多细节的读者，下载源码结合blog深入研究，这样比我在这里说能获取的有效信息，将会多得多。GC相关的文章是个很难写的部分，本篇我也花了巨量的时间和精力，不过现在总算是交了答卷，下一章我们将进入探索lua string的设计和实现上。</p>
<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><p>[1] 这是一篇关于Lua设计者的访谈记录，出版社中文翻译由云风负责审校，由于云风对翻译并不满意，因此他重新写了<a href="https://blog.codingnow.com/2010/06/masterminds_of_programming_7_lua.html" rel="external nofollow noopener noreferrer" target="_blank">一篇</a><br>[2] 参考内容位于<a href="https://en.wikipedia.org/wiki/Tracing_garbage_collection" rel="external nofollow noopener noreferrer" target="_blank">Tracing garbage collection</a>的Naive Mark and Sweep章节<br>[3] 参考<a href="http://wiki.c2.com/?MarkAndSweep" rel="external nofollow noopener noreferrer" target="_blank">Mark and Sweep</a> 其中有一段话这么阐述”The second pass traverses all pointers, starting at the accessible roots of a program (conventionally, globals, the stack, and registers) and for each object traversed it marks the object.“，即globals，stack和register是作为gc开始的起点，会被使用的变量基本在这上面，或者与在这上面的变量关联。</p>
<p>let us build a lua interpreter </p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part3：String设计与实现</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part3/</url>
    <content><![CDATA[<p><a href="http://manistein.club/post/program/let-us-build-a-lua-interpreter/%E6%9E%84%E5%BB%BAlua%E8%A7%A3%E9%87%8A%E5%99%A8part3/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>   上一章节我介绍了dummylua项目的gc的设计原理和实现方式，该部分我基本是仿造了官方lua-5.3.4的做法。本章节，我将为大家带来dummylua项目TString的设计实现讲解。和以往任何一篇一样，本篇我也是基本遵循lua-5.3的设计思想，但由于实现是我凭理解自己手动实现，因此不会在所有的细节上和官方lua保持一致。<br>   任何一门语言都要支持字符串，lua也一样，本章将从lua string的数据结构开始阐述，然后深入其他细节进行阐述，并试图揭露这些设计的历史背景和原因，由于本人能力有限，如有不当之处想要指明，或是想和我一起讨论，欢迎加Q群与我讨论(QQ185017593)，我是群主，欢迎大家的到来。</p>
<h1 id="Lua字符串结构"><a href="#Lua字符串结构" class="headerlink" title="Lua字符串结构"></a>Lua字符串结构</h1><p>   在论述Lua字符串结构之前，首先我们要捋清楚c语言字符串和lua字符串的关系，我们都知道，lua代码是需要通过c来编译解析生成opcode，最后交给由c写的解释器负责执行这些指令，因此lua脚本首先要经过c的解析，才能转换成lua所能使用的内部结构，比如下面一段lua代码：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> str = <span class="string">"hello world"</span></span><br><span class="line"><span class="built_in">print</span>(str)</span><br></pre></td></tr></table></figure></p>
<p>很直观，str是一个lua字符串，当lua解释器运行这个脚本时，需要创建一个”hello world”的字符串。然而实际上，这段脚本，lua解释器并非新建一个c的char<em>变量或者是char数组去存放并使用这个”hello world”，而是需要创建lua内部能够使用的字符串类型，其步骤是，”hello world”这个str首先只能被c读取，然后存入c的char</em>或char[SIZE]对应的buffer中，再以buffer为内容创建对应的lua字符串，以备其（指的是包含“hello world”的lua字符串）能够在虚拟机中被使用。对此，我们后续只需要关注，lua如何通过char*变量创建lua字符串即可，后文中，我们统一将lua字符串称为TString类型的字符串。</p>
<p>   从lua-5.2.1开始，字符串就分为长字符串和短字符串了[1]，其中短字符串会进行充分的hash运算，并且进行内部化处理，借以提高lua虚拟机对字符串操作的效率,和节约内存（尽可能少去malloc新内存）。而长字符串则不进行充分的hash运算，且不进行内部化，关于为何要这样做，后续小节我会详细进行讨论。dummylua也是遵循这种区分长短字符串的方式进行设计和实现的。<br>   在开始深入探讨其他部分之前，我先来介绍一下dummylua字符串的结构，我并不想在一开始就贴上大段的代码，这里只展示一个概念图，因为大段的代码只会过早暴露太多的设计细节，使得我们讨论的问题变得索然无味，现在我们来看看图1的结构：<br><img src="string_struct01.png" alt><br>上图给我们展示了一块连续的内存块，这里包含了String的Header和String的Body部分，Header包含了String的类型（长字符串或短字符串）、hash值、长度等信息。不同类型的字符串包含的头部字段也是不同的，虽然他们内部字段不同，但是Header的size都是一致的，而String的Body部分，则是字符串内容的本体所在，真正放字符串内容的地方，相当于char*。在lua中，String Body短于或等于40字节的是短字符串，长于40字节的是长字符串。<br>   lua在c语言层面，对字符串求size，本质是需要求出String Header和String Body的总size大小（单位是byte）。<br>   这一小节，我对dummylua的TString类型，在宏观上进行了介绍，我们暂时不用太过关注Header包含哪些部分，目前只需要关心TString包含GC的CommonHeader，其中有一个字段是表示类型的tt_变量，如图2所示，后面我们继续深入探索其他部分。<br><img src="string_struct02.png" alt></p>
<h1 id="字符串的哈希运算"><a href="#字符串的哈希运算" class="headerlink" title="字符串的哈希运算"></a>字符串的哈希运算</h1><p>   上一节我介绍了TString的概念结构，本节要探讨的是lua字符串的hash运算。lua的hash运算，主要针对body部分，header并不参与hash计算。计算完的hash值将被记录在String Header内，如图3所示：<br><img src="string_struct03.png" alt><br>我们都知道，hash运算的目的，是为每一个字符串或文档内容生成相同长度不同值的字符串或者是数值，输入的文件内容，字符串只要有细微的差别，都能做到hash值的极大不同，hash code通常是用来识别不同文档或字符串用的。关于hash更多的介绍，与在网络安全领域的应用，可以参考我另一篇blog，<a href="https://manistein.github.io/blog/post/network/%E7%BD%91%E7%BB%9C%E5%AE%89%E5%85%A8%E5%88%9D%E6%8E%A2/" rel="external nofollow noopener noreferrer" target="_blank">《网络安全初探》</a>，这里只讨论hash算法本身，lua-5.3的hash运算如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="title">luaS_hash</span><span class="params">(struct lua_State* L, <span class="keyword">const</span> <span class="keyword">char</span>* str, <span class="keyword">unsigned</span> <span class="keyword">int</span> l, <span class="keyword">unsigned</span> <span class="keyword">int</span> h)</span> </span>&#123;</span><br><span class="line">    h = h ^ l;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="built_in">step</span> = (l &gt;&gt; <span class="number">5</span>) + <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; l; i = i + <span class="built_in">step</span>) &#123;</span><br><span class="line">        h ^= (h &lt;&lt; <span class="number">5</span>) + (h &gt;&gt; <span class="number">2</span>) + cast(lu_byte, str[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> h;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上面的参数中，str表示String Body部分，l表示他的长度，而h则是外部传入的一个参与hash运算的种子，这个种子在lua虚拟机创建时就确定了，创建的那一刻具有随机性，创建后不再改变，其代码如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="title">makeseed</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">char</span> buff[<span class="number">4</span> * <span class="keyword">sizeof</span>(<span class="keyword">size_t</span>)];</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> h = time(<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">int</span> p = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    addbuff(buff, L, p);</span><br><span class="line">    addbuff(buff, &amp;h, p);</span><br><span class="line">    addbuff(buff, luaO_nilobject, p);</span><br><span class="line">    addbuff(buff, &amp;lua_newstate, p);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> luaS_hash(L, buff, p, h);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到，这个种子，是由lua_State实例的地址，当前时间值，luaO_nilobject变量的地址和lua_newstate函数地址决定的，具有极强的随机性，外界无法捉摸。现在我们返回luaS_hash来观察其hash运算。首先该函数对传入的hash种子，对字符串的长度进行了xor运算。然后是小于32字节的每个字符都会参与hash运算，大于等于32字节的，则会至少跳过一个字节的方式进行hash运算，其计算方式也很简单(l &gt;&gt; 5) + 1。<br>        至于hash算法为何喜欢使用xor来运算，这个和xor运算的性质有关系，它能够使二进制值0和1进行相对平均的分布，我们通过一个例子来对xor、and和or来进行简单的比较：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">A B AND</span><br><span class="line"><span class="number">0</span> <span class="number">0</span>  <span class="number">0</span></span><br><span class="line"><span class="number">0</span> <span class="number">1</span>  <span class="number">0</span></span><br><span class="line"><span class="number">1</span> <span class="number">0</span>  <span class="number">0</span></span><br><span class="line"><span class="number">1</span> <span class="number">1</span>  <span class="number">1</span></span><br><span class="line"></span><br><span class="line">A B OR</span><br><span class="line"><span class="number">0</span> <span class="number">0</span>  <span class="number">0</span></span><br><span class="line"><span class="number">0</span> <span class="number">1</span>  <span class="number">1</span></span><br><span class="line"><span class="number">1</span> <span class="number">0</span>  <span class="number">1</span></span><br><span class="line"><span class="number">1</span> <span class="number">1</span>  <span class="number">1</span></span><br><span class="line"></span><br><span class="line">A B XOR</span><br><span class="line"><span class="number">0</span> <span class="number">0</span>  <span class="number">0</span></span><br><span class="line"><span class="number">0</span> <span class="number">1</span>  <span class="number">1</span></span><br><span class="line"><span class="number">1</span> <span class="number">0</span>  <span class="number">1</span></span><br><span class="line"><span class="number">1</span> <span class="number">1</span>  <span class="number">0</span></span><br></pre></td></tr></table></figure></p>
<p>我们发现通过XOR运算，结果中0和1的数量是相当的，有研究表明，or运算会产生3/4的1，and会产生3/4的0，只有xor能做到均匀分布[2]，这个性质正好符合hash运算需要进行均匀散列运算的要求，因此它大量运用在hash算法里。我们计算完的hash值，最终会存入String Header的hash字段中。<br>   本节我们探讨了lua hash算法的实现细节，但留下了一个疑惑，就是为什么要生成一个hash种子？这个问题留在后面来解答。</p>
<h1 id="短字符串与内部化"><a href="#短字符串与内部化" class="headerlink" title="短字符串与内部化"></a>短字符串与内部化</h1><p>  文章一开始我们就在讨论字符串内部化了，但到底什么是字符串内部化？它的作用是什么？如何使用它？<br>   字符串内部化的本质是将字符串缓存起来，所有拥有相同内容的字符串共享一个副本，其步骤是将字符串计算String Body的hash值后，放入一个hash map中，然后以后有要用到相同的字符串时，则直接返回该字符串的指针，以做到字符串共享，提升lua的运行效率。这种方式只适用于短字符串，也就是我们前面说的String Body小于40字节的字符串。要理解字符串内部化，首先我们要熟悉字符串内部化实现的基础。我们的lua虚拟机在创建以后，会生成一个size是2的n次幂的数组，这个数组将作为我们短字符串的hash表，我们将他称之为strt（意为string table），为什么数组能做hash表？我一会会解释。首先我们来看看，一个lua虚拟机初始化好以后，这个hash table的存在如图4所示：<br><img src="string_table.png" alt><br>现在我来解释一下上图的含义，strt是一个TString*类型的一维数组，他的长度是2的n次幂，每个hash表的slot，指向一个字符串单向链表（链表next指针是包含在String Header中的），字符串内部化的本质，就是为每个字符串创建唯一的实例，当要创建新的字符串时，首先在hash表中找，找到对象时则返回该指针，找不到则新建并插入。详细的步骤如下所示：</p>
<ul>
<li>已知c层字符串char* str，要为str创建一个lua TString对象，首先计算str的hash值</li>
<li>在完成hash值计算以后，查找String Body和str相同的TString对象，可能在strt的哪个slot中，方式是slot_idx = hash &amp; (strt-&gt;size - 1)。因为全局字符串表strt的size是2的n次幂，所以hash &amp; strt-&gt;size - 1必定是0 ~ strt-&gt;size - 1之间的值（如4-1=3，二进制表示0112，任何正整数和它进行&amp;运算区间都是在0~3之间，这也是为什么strt的size必须是2的n次幂的原因），不会超出strt的有效范围。</li>
<li>找到对应的slot以后，取出对应slot引用的TString链表，遍历链表，如果找到String Body和str匹配的lua TString对象，则返回其指针，否则直接插入该链表的头部。<br><img src="new_string01.gif" alt><br>图5展示的是，要创建内容为“World”的lua TString实例，先计算World的hash值，再计算出“World”可能能在Slot 1的位置的位置找到，然后查找该Slot的TString链表，最后遍历链表找到内容与之匹配的TString实例，最后返回指针。</li>
</ul>
<p><img src="new_string02.gif" alt><br>图6展示的是，要创建内容为“GTA”的lua TString实例，先计算“GTA”的hash值，再计算出“GTA”可能在的Slot位置，并遍历查找对应的TString链表，最后没找到，则新创建一个lua TString实例，并插入该Slot位置的TString链表首部。</p>
<p>   前面我们展示了创建字符串的两个流程，查找和新建。而如果全局字符串表strt如果是固定的，那么TString链表将会很长，最终查找效率会退化成O(n)，这显然不是我们愿意见到的，因此，当字符串数量到达一定数量时，strt就会拓展为原来的两倍，并且所有的字符串都会重新做mod操作（即hash &amp; (new_size - 1)）分配到新的Slot中，组建新的TString链表。而在什么情况下会触发resize呢？一种是已经存在的lua TString数量&gt;=strt-&gt;size的时候会触发，还有一种则是lua TString数量&lt;=strt-&gt;size / 2时，需要缩小为原来的一半。这里有一个特点，即TString的数量，始终不能超过strt的size。这样做有个好处，则是每个Slot尽可能保证只有一个lua TString实例，即便偶尔有hash碰撞，组成的TString链表也不会太长，使得查找效率保持在O(1)。</p>
<p>   我们先来看看拓展的情况，如图7所示，此时新建一个全新的短字符串s = “Red Dead Redemption”，我们先计算它的hash值，不论它放到哪个位置，当前的TString实例已经超过了全局字符串表strt的当前总长，因此此时，strt需要扩充为原来的两倍，并且所有的TString实例将会被重新分配到新的Slot中，如图8所示。<br><a href="strt_resize01.png"></a><br><a href="strt_resize02.png"></a><br>所有已存的TString实例安排好以后，将会为”Red Dead Redemption”创建一个新的TString实例，并计算出其所在的Slot，并关联起来：<img src="strt_resize03.png" alt></p>
<p>   缩小的情况和扩展时类似，但是要保证TString的数量要小于或等于当前strt size的一半，这样每个TString对象要重新排列，查找对应的Slot，方式则是new_slot_idx = hash &amp; (strt-&gt;size / 2 - 1)。然后就可以执行缩小操作了。</p>
<p>   这里对删除字符串做一个简要说明，删除一个lua短字符串，首先要对String Body做hash值运算，并且通过hash &amp; strt-&gt;size的运算定位到其在哪个Slot中，最后遍历TString链表找到指定的TString实例，最后将其移除。lua字符串的删除行为，一般是由gc发起的。</p>
<p>   本节我们通过图文，对短字符串的内部化进行了详细的讨论，了解到了内部化的本质和特征，以及它为何能够提升lua的运作效率，下一节我们将讨论lua长字符串的一些内容。</p>
<h1 id="长字符串与惰性哈希"><a href="#长字符串与惰性哈希" class="headerlink" title="长字符串与惰性哈希"></a>长字符串与惰性哈希</h1><p>  和短字符串不同，lua的长字符串不会进行内部化，与全局字符串列表没有任何关系，因此对于长字符串，lua虚拟机是直接创建它的，因为和strt没有关联，所以在创建阶段，它甚至不需要进行hash值运算，这样的结果就是，对于长字符串而言，哪怕String Body完全一致，lua虚拟机也会为其创建独立的TString实例。长字符串的hash运算，并不是充分的hash运算，并且只有在需要的时候才进行，借以提高lua虚拟机的运行效率。</p>
<h1 id="Lua-5-2-0的Hash-Dos攻击"><a href="#Lua-5-2-0的Hash-Dos攻击" class="headerlink" title="Lua-5.2.0的Hash Dos攻击"></a>Lua-5.2.0的Hash Dos攻击</h1><p>  Lua为什么要将长字符串和短字符串区分开来，并且只对短字符串做内部化处理？其实在lua-5.2.0这个版本中，lua其实是不分长字符串和短字符串的，所有的字符串都会做内部化处理，并且留下了一个非常致命的Hash Dos攻击漏洞，并在社区上被提出来了[3]，在深入这个问题之前，我们不妨查看一下lua-5.2.0创建字符串的代码<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lua-5.2.0 lstring.c</span></span><br><span class="line"><span class="function">TString *<span class="title">luaS_newlstr</span> <span class="params">(lua_State *L, <span class="keyword">const</span> <span class="keyword">char</span> *str, <span class="keyword">size_t</span> l)</span> </span>&#123;</span><br><span class="line">   GCObject *o; </span><br><span class="line">   <span class="keyword">unsigned</span> <span class="keyword">int</span> h = cast(<span class="keyword">unsigned</span> <span class="keyword">int</span>, l);  <span class="comment">/* seed */</span></span><br><span class="line">   <span class="keyword">size_t</span> <span class="built_in">step</span> = (l&gt;&gt;<span class="number">5</span>)+<span class="number">1</span>;  <span class="comment">/* if string is too long, don't hash all its chars */</span></span><br><span class="line">   <span class="keyword">size_t</span> l1; </span><br><span class="line">   <span class="keyword">for</span> (l1=l; l1&gt;=<span class="built_in">step</span>; l1-=<span class="built_in">step</span>)  <span class="comment">/* compute hash */</span></span><br><span class="line">     h = h ^ ((h&lt;&lt;<span class="number">5</span>)+(h&gt;&gt;<span class="number">2</span>)+cast(<span class="keyword">unsigned</span> <span class="keyword">char</span>, str[l1<span class="number">-1</span>]));</span><br><span class="line">   <span class="keyword">for</span> (o = G(L)-&gt;strt.hash[lmod(h, G(L)-&gt;strt.<span class="built_in">size</span>)];</span><br><span class="line">        o != <span class="literal">NULL</span>;</span><br><span class="line">        o = gch(o)-&gt;next) &#123;</span><br><span class="line">     TString *ts = rawgco2ts(o);</span><br><span class="line">     <span class="keyword">if</span> (h == ts-&gt;tsv.hash &amp;&amp;</span><br><span class="line">         ts-&gt;tsv.len == l &amp;&amp;</span><br><span class="line">         (<span class="built_in">memcmp</span>(str, getstr(ts), l * <span class="keyword">sizeof</span>(<span class="keyword">char</span>)) == <span class="number">0</span>)) &#123;</span><br><span class="line">       <span class="keyword">if</span> (isdead(G(L), o))  <span class="comment">/* string is dead (but was not collected yet)? */</span></span><br><span class="line">         changewhite(o);  <span class="comment">/* resurrect it */</span></span><br><span class="line">       <span class="keyword">return</span> ts; </span><br><span class="line">     &#125;   </span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> newlstr(L, str, l, h);  <span class="comment">/* not found; create a new string */</span></span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure></p>
<p>上面这段代码，我们可以了解到，在lua-5.2.0的版本创建字符串的几个特点：</p>
<ul>
<li>字符串不论长短，一律实行内部化。</li>
<li>小于32字节(2^5=32)的字符串将进行充分的hash运算，大于的则不充分。这点是由于内部化的目的是为了让相同内容的字符串共享一个副本，尽可能减少malloc和free的操作，既希望减少内存，又希望提升性能。但是长字符串的hash运算将会造成性能上的问题，因此这里采取了长字符串不充分hash的策略，字符串越长，hash越不充分。</li>
<li>hash运算，只依赖于算法，以及传入的c字符串的内容已和长度，也就是说，客户端有办法知道构造什么样的字符串，能够生成相同的hash值。</li>
<li>内部化的结果，就是需要频繁的查询，但是我们也看到了，经过hash运算后，我们能定位到一个具有相同内容的lua字符串，可能在strt hash表中的位置，并且将存在这个位置的TString链表取出，并遍历查找是否已经有内存实例存在。遍历查找的过程，先对比hash值，再对比长度，如果都匹配上了，需要对String Body和要新创建的字符串进行字符串匹配（这个过程很耗），之所以要进行hash值比较和长度比较，目的是尽可能避免直接进行字符串匹配，造成性能上的极大损耗。<br>上述几点，则是lua-5.2.0关于lua字符串创建的几个特点所在，目前光看这几点，难以让我们对Hash Dos攻击产生直观的感受。我们不妨先来研究一下，客户端如何构造相同hash值的不同字符串。<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="title">hash</span><span class="params">(<span class="keyword">int</span> l, <span class="keyword">const</span> <span class="keyword">char</span>* str)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> h = (<span class="keyword">unsigned</span> <span class="keyword">int</span>)l;  <span class="comment">/* seed */</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="built_in">step</span> = (l&gt;&gt;<span class="number">5</span>)+<span class="number">1</span>;  <span class="comment">/* if string is too long, don't hash all its chars */</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> l1; </span><br><span class="line">    <span class="keyword">for</span> (l1=l; l1&gt;=<span class="built_in">step</span>; l1-=<span class="built_in">step</span>)  <span class="comment">/* compute hash */</span></span><br><span class="line">        h = h ^ ((h&lt;&lt;<span class="number">5</span>)+(h&gt;&gt;<span class="number">2</span>)+(<span class="keyword">unsigned</span> <span class="keyword">char</span>)str[l1<span class="number">-1</span>]);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> h;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123; </span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* str1 = <span class="string">"helloworld, hello world, hello world, hello world"</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* str2 = <span class="string">"hxlLoMorld, hello world, hello world, hello world"</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* str3 = <span class="string">"asdfasdfasdfasdfasdfasdfasdfasdfasdfasdfasdfasdfa"</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"str1 len = %ld, str2 len = %ld, str3 len = %ld\n"</span>, \</span><br><span class="line">        <span class="built_in">strlen</span>(str1), <span class="built_in">strlen</span>(str2), <span class="built_in">strlen</span>(str3));</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"str1 hash = %u, str2 hash = %u, str3 hash = %u"</span>,\</span><br><span class="line">        hash(<span class="built_in">strlen</span>(str1), str1), hash(<span class="built_in">strlen</span>(str2), str2), hash(<span class="built_in">strlen</span>(str3), str3));</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>上述代码我截取了hash计算的部分，str1、str2和str3均是不同的字符串，而str1和str2则是高度相似，但是却不完全相同，通过提取出来hash算法，我们得到如下的结果：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">str1 len = <span class="number">49</span>, str2 len = <span class="number">49</span>, str3 len = <span class="number">49</span></span><br><span class="line">str1 hash = <span class="number">2931300550</span>, str2 hash = <span class="number">2931300550</span>, str3 hash = <span class="number">3850065186</span></span><br></pre></td></tr></table></figure></p>
<p>我们可以发现，str1和str2的hash值相同，长度相同，但是内容不同，由于lua-5.2.0的长字符串hash运算并不充分，使得我们有机会构造hash值相同、长度相同、但是值不同的字符串，如上所示的代码，由于字符串长49字节，偶数位字符并不参与hash运算，因此我们可以修改这些字符为其他字符，使其在保持hash值相同的情况下，改变内容。换句话说，只要我的字符足够长，我们完全有可能构建几百万甚至上千万个符合这种条件的不同字符串。</p>
<p>   现在问题变得非常有意思了，似乎距离我们的目标更为接近了，但是对于攻击而言，我们仍然缺乏直观的感受，我们需要更丰满的例子来阐述这个问题。现在假设我们有这样一个服务器程序：</p>
<ul>
<li>通过c层，监听连接，并接受来自客户端经过序列化的请求。</li>
<li>c层网络模块，在处理好整包以后，将序列化好的数据包传给一个c层回调函数。</li>
<li>c层回调函数，先对数据包进行反序列化，得到通用结构后，将参数压入lua栈中，然后调用lua层回调函数处理业务。<br>这个c层的callback函数的，伪代码如下所示：<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">network_cb</span><span class="params">(struct lua_State* L, <span class="keyword">const</span> <span class="keyword">char</span>* package)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">request</span> <span class="title">req</span> = <span class="title">pb</span>.<span class="title">decode</span>(<span class="title">package</span>);</span></span><br><span class="line">    </span><br><span class="line">    luaL_pushstring(L, req.command_str);    <span class="comment">//将c字符串转成lua字符串，以便lua虚拟机能够正常运行</span></span><br><span class="line">    luaL_pushstring(L, req.json_param_str);</span><br><span class="line">    luaL_pcall(L, <span class="number">2</span>, <span class="number">0</span>); <span class="comment">// lua_State已经注册了一个lua层回调函数，c层先把命令字符串和参数字符串转成lua字符串后，交给lua层处理。</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>上述代码，假设lua_State的栈顶已经压入一个lua函数，用于处理请求逻辑，客户端发送的数据包，收齐之后会传给这个network_cb函数，并且在c层进行反序列化操作，然后将请求命令和参数压入栈中，让lua虚拟机处理。在这个层面，回调层并不关心req.command_str和req.json_param_str具体有什么内容，这里要做的事情就是将字符串传给lua层，让其处理，在将字符串压栈的过程中，也就是在调用luaL_pushstring函数时，lua框架会根据c字符串的内容，创建lua TString实例，并压如lua_State的栈顶。也就是说不论command_str或json_param_str是否合法，这一层都不会检查到，会直接创建出lua字符串交给lua虚拟机处理。如果我们使用的是lua-5.2.0，那么现在就给攻击者留下了可乘之机，攻击者完全可以将command_str或json_param_str设置为一个很长的字符串，并且可能构造出几万，十几万，几十万甚至几百万个hash值相同，长度相同但是内容不同的字符串，传过来，每个请求每个字符串都会进行一次内部化处理，由于这些海量的字符串的hash值相同，因此他们在全局字符串表strt的hash表中，只用到一个slot，并且全部在TString链表中线性排列，于是查询效率退化为O(n)，更严重的是，在字符串内部化的查找阶段（新的不合法的请求到来时，也会进行一次内部化处理），这些字符串的hash值相同，并且长度相同，根据它的对比规则，最终会落到比较字符串这种效率极低操作上，在海量不合法的字符串面前还要进行内部化操作，会极大损耗服务器的性能，甚至导致瘫痪，最终使得DOS攻击成功。</p>
<p>  为了解决这个问题，从lua-5.2.1开始，官方lua将字符串拆分成长字符串和短字符串，其中String Body大于40字节的是长字符串，长字符串不参与内部化处理；而小于等于40字节的则是短字符串，短字符串基本会充分进行hash运算，不同的字符串，基本会有不同的hash值，极小可能会发生hash冲突进而导致字符串插入链表，同时它也加入了随机生成的hash种子参与，客户端无法知道这个hash种子的具体数值，无法有效构建出正确的hash值验证攻击的有效性。这样执行上面代码时，如果command_str是长字符串，两次不同的请求，即便是command_str的内容相同，都会创建两个独立的lua TString实例，这样虽然在内存上的使用更多了，但是也更安全了，算是彻底解决了Hash Dos攻击的问题了。</p>
<p>  本节我通过lua-5.2.0中一种可能的Hash Dos攻击的途径，解释了为什么现在版本的lua需要区分长字符串和短字符串，并且只对短字符串进行内部化处理的原因，到目前位置，所有概念上的前置知识已经全部介绍完毕了，下一节我将介绍lua TString的代码实现。</p>
<h1 id="程序实现"><a href="#程序实现" class="headerlink" title="程序实现"></a>程序实现</h1><p>   终于进入到程序实现的环节，我们先介绍lua字符串TString的数据结构：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.c</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">TString</span> &#123;</span></span><br><span class="line">    CommonHeader;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> hash;          <span class="comment">// string hash value</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// if TString is long string type, then extra = 1 means it has been hash, </span></span><br><span class="line">    <span class="comment">// extra = 0 means it has not hash yet. if TString is short string type,</span></span><br><span class="line">    <span class="comment">// then extra = 0 means it can be reclaim by gc, or if extra is not 0,</span></span><br><span class="line">    <span class="comment">// gc can not reclaim it.</span></span><br><span class="line">    <span class="keyword">unsigned</span> short extra;       </span><br><span class="line">    <span class="keyword">unsigned</span> short shrlen;</span><br><span class="line">    <span class="keyword">union</span> &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">hnext</span>;</span> <span class="comment">// only for short string, if two different string encounter hash collision, then chain them</span></span><br><span class="line">        <span class="keyword">size_t</span> lnglen;</span><br><span class="line">    &#125; u;</span><br><span class="line">    <span class="keyword">char</span> data[<span class="number">0</span>];</span><br><span class="line">&#125; TString;</span><br></pre></td></tr></table></figure></p>
<p>该数据结构展示了TString的全貌，现在我来对每一个字段作解读：</p>
<ul>
<li>CommonHeader：这是所有GC对象的公共头部，包含指向下一个GC对象实例地址的GCObject* next指针，表示类型的int型变量tt_，以及表示标记状态的marked变量。如果你没有看过GC部分或者已经印象模糊，可以回顾上一章的关于GC部分的内容。</li>
<li>hash：String Body经过hash计算后所得到的值，将存放在这里。</li>
<li>extra：特殊的变量，不同的类型有不同的含义。</li>
<li>TString为长字符串时：当extra=0表示该字符串未进行hash运算；当extra=1时表示该字符串已经进行过hash运算。</li>
<li>TString为短字符串时：当extra=0时表示它需要被gc托管；当extra=1时表示该字符串不会被gc回收。</li>
<li>shrlen：仅对短字符串有效，表示短字符串的长度，既String Body的长度是多少。</li>
<li>u：这是一个union结构，当TString为短字符串时，hnext域有效，当全局字符串表，有其他字符串hash冲突时，会将这些冲突的TString实例链接成单向链表，hnext的作用则是指向下一个对象在哪个位置。当TString为长字符串时，lnglen域生效，并且表示长字符串的长度也就是String Body的长度是多少。</li>
<li><p>data：这个是用来标记String Body起始位置用的，配合shrlen或lnglen，我们能够找到String Body的结束位置在哪里。<br>我已经对lua字符串类型TString做过概念上的介绍，即其分为长字符串和短字符串，并且详细介绍了为何需要这样区分，这里只需要对不同的字段做说明，相信已经能够给读者比较清晰的概念呈现了。</p>
<p>在了解了TString的数据结构后，接下来来介绍全局字符串列表的数据结构和初始化流程，全局字符串列表的数据结构如下所示：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastate.h</span></span><br><span class="line"><span class="comment">// only for short string</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">stringtable</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>** <span class="title">hash</span>;</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> nuse;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="built_in">size</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>这是实际上是一个TString*类型的一维数组，其中nuse表示当前有多少个经过内部化处理的短字符串，而size表示这个stringtable结构内部的hash数组的大小有多大。上述内容非常容易理解，接下来我们来看看，这个全局字符串列表存在哪个位置，以及它的初始化流程是怎样的，事实上，我们的global_State包含了一个stringtable类型的变量，它叫做strt变量：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastate.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">mainthread</span>;</span></span><br><span class="line">    lua_Alloc frealloc;</span><br><span class="line">    <span class="keyword">void</span>* ud; </span><br><span class="line">    lua_CFunction panic;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">stringtable</span> <span class="title">strt</span>;</span></span><br><span class="line">    TString* strcache[STRCACHE_M][STRCACHE_N];</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> seed;              <span class="comment">// hash seed, just for string hash</span></span><br><span class="line">    TString* memerrmsg;</span><br><span class="line">    TValue l_registry;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//gc fields</span></span><br><span class="line">    lu_byte gcstate;</span><br><span class="line">    lu_byte currentwhite;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">allgc</span>;</span>         <span class="comment">// gc root set</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>** <span class="title">sweepgc</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gray</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">grayagain</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">fixgc</span>;</span>         <span class="comment">// objects can not collect by gc</span></span><br><span class="line">    lu_mem totalbytes;</span><br><span class="line">    l_mem GCdebt;                   <span class="comment">// GCdebt will be negative</span></span><br><span class="line">    lu_mem GCmemtrav;               <span class="comment">// per gc step traverse memory bytes </span></span><br><span class="line">    lu_mem GCestimate;              <span class="comment">// after finish a gc cycle,it records total memory bytes (totalbytes + GCdebt)</span></span><br><span class="line">    <span class="keyword">int</span> GCstepmul;</span><br><span class="line">&#125; global_State;</span><br></pre></td></tr></table></figure></p>
<p>当我们的lua虚拟机被创建时，会对这个stringtable变量strt进行初始化操作：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastring.c</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaS_init</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    g-&gt;strt.nuse = <span class="number">0</span>;</span><br><span class="line">    g-&gt;strt.<span class="built_in">size</span> = <span class="number">0</span>;</span><br><span class="line">    g-&gt;strt.hash = <span class="literal">NULL</span>;</span><br><span class="line">    luaS_resize(L, MINSTRTABLESIZE);</span><br><span class="line">    g-&gt;memerrmsg = luaS_newlstr(L, MEMERRMSG, <span class="built_in">strlen</span>(MEMERRMSG)); </span><br><span class="line">    luaC_fix(L, obj2gco(g-&gt;memerrmsg));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// strcache table can not hold the string objects which will be sweep soon</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; STRCACHE_M; i ++) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; STRCACHE_N; j ++)</span><br><span class="line">            g-&gt;strcache[i][j] = g-&gt;memerrmsg;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个初始化操作，主要做了几件事情，第一件是初始化stringtable类型变量strt，并且通过luaS_resize函数为其分配最小内存，MINSTRINGTABLESIZE，这个变量在dummylua中被设置为128，也就是说strt的最小hash表长度为128。另外还创建了一个不可被gc回收的字符串memerrmsg，最后初始化了一个长短字符串都可以共用的字符串缓存strcache（这个稍后讨论）。我们可以注意到，这里调整strt大小的函数是luaS_resize，它的实现如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastring.c</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// attention please, luaS_resize is use for resize stringtable type,</span></span><br><span class="line"><span class="comment">// which is only use by short string. The second argument usually must </span></span><br><span class="line"><span class="comment">// be 2 ^ n</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaS_resize</span><span class="params">(struct lua_State* L, <span class="keyword">unsigned</span> <span class="keyword">int</span> nsize)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> osize = g-&gt;strt.<span class="built_in">size</span>;</span><br><span class="line">    <span class="keyword">if</span> (nsize &gt; osize) &#123;</span><br><span class="line">        luaM_reallocvector(L, g-&gt;strt.hash, osize, nsize, TString*);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = osize; i &lt; nsize; i ++) &#123;</span><br><span class="line">            g-&gt;strt.hash[i] = <span class="literal">NULL</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// all TString value will be rehash by nsize</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; g-&gt;strt.<span class="built_in">size</span>; i ++) &#123;</span><br><span class="line">       <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">g</span>-&gt;<span class="title">strt</span>.<span class="title">hash</span>[<span class="title">i</span>];</span></span><br><span class="line">       g-&gt;strt.hash[i] = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">while</span>(ts) &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">old_next</span> = <span class="title">ts</span>-&gt;<span class="title">u</span>.<span class="title">hnext</span>;</span></span><br><span class="line">            <span class="keyword">unsigned</span> <span class="keyword">int</span> hash = lmod(ts-&gt;hash, nsize);</span><br><span class="line">            ts-&gt;u.hnext = g-&gt;strt.hash[hash];</span><br><span class="line">            g-&gt;strt.hash[hash] = ts;</span><br><span class="line">            ts = old_next;</span><br><span class="line">       &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// shrink string hash table </span></span><br><span class="line">    <span class="keyword">if</span> (nsize &lt; osize) &#123;</span><br><span class="line">        lua_assert(g-&gt;strt.hash[nsize] == <span class="literal">NULL</span> &amp;&amp; g-&gt;strt.hash[osize - <span class="number">1</span>] == <span class="literal">NULL</span>);</span><br><span class="line">        luaM_reallocvector(L, g-&gt;strt.hash, osize, nsize, TString*);</span><br><span class="line">    &#125;</span><br><span class="line">    g-&gt;strt.<span class="built_in">size</span> = nsize;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> g-&gt;strt.<span class="built_in">size</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们可以看到，要进行大小调整，需要传入一个nsize值，而这个nsize值必须是2的n次幂。它内部的处理也非常有意思，处理流程如下所示：</p>
<ul>
<li>当新建尺寸nsize大于原来strt hash表的大小时，首先需要开辟一个nsize大小的TString*数组，然后原有的TString变量会重新定位到新的hash表中的slot中（slot_idx=luastring-&gt;hash &amp; nsize - 1）。</li>
<li><p>当新建尺寸nsize小于原来strt hash表的大小时，首先strt-&gt;hash表内的所有TString实例，会根据nsize重新排列，定位<em> 到合适的slot中（slot_idx=luastring-&gt;hash &amp; nsize - 1），并且开辟一块nszie大小的TString</em>数组，再将strt-&gt;hash中的值拷贝到该数组中，最后strt-&gt;hash将引用这块内存，以达到缩小的目的。<br>结合之前画的那些图，这段逻辑并不难理解。</p>
<p>在完成了全局字符串表创建和初始化流程以后，我们现在将目光关注于如何创建一个字符串实例，这里我们先不考虑长字符串和短字符串的的情况，而关注与一个luastring实例是如何被创建的，这个流程对长短字符串都适用。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastring.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> struct TString* <span class="title">createstrobj</span><span class="params">(struct lua_State* L, <span class="keyword">const</span> <span class="keyword">char</span>* str, <span class="keyword">int</span> tag, <span class="keyword">unsigned</span> <span class="keyword">int</span> l, <span class="keyword">unsigned</span> <span class="keyword">int</span> hash)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">size_t</span> total_size = sizelstring(l);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">o</span> = <span class="title">luaC_newobj</span>(<span class="title">L</span>, <span class="title">tag</span>, <span class="title">total_size</span>);</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">gco2ts</span>(<span class="title">o</span>);</span></span><br><span class="line">    <span class="built_in">memcpy</span>(getstr(ts), str, l * <span class="keyword">sizeof</span>(<span class="keyword">char</span>));</span><br><span class="line">    getstr(ts)[l] = <span class="string">'\0'</span>;</span><br><span class="line">    ts-&gt;extra = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (tag == LUA_SHRSTR) &#123;</span><br><span class="line">        ts-&gt;shrlen = cast(lu_byte, l);</span><br><span class="line">        ts-&gt;hash = hash;</span><br><span class="line">        ts-&gt;u.hnext = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (tag == LUA_LNGSTR) &#123;</span><br><span class="line">        ts-&gt;hash = <span class="number">0</span>;</span><br><span class="line">        ts-&gt;u.lnglen = l;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        lua_assert(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> ts;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>我们的lua字符串实例，是需要被gc托管的对象，因此它是一个gcobject，要创建一个gcobject，我们必须知道TString实例的具体大小，它的大小就是String Header的字节加上String Body字节的总大小，它由sizelstring函数来计算。String Body的结尾需要加上特殊字符‘\0’，这样做的目的是能够和c语言的字符串兼容，使之在c层操作更为方便，接下里则是针对不同的TString类型，作不同的初始化操作。</p>
<p>   lua字符串被创建前，我们还需要判断字符串是长字符串还是短字符串，不同类型的字符串，会做不同的处理，比如短字符串会进行内部化操作，而长字符串则是直接创建lua字符串实例。对于短字符串而言，在开始内部化处理之前，还需要进行hash值计算，由于文章的前半部分已经介绍了，这里就不再赘述，我们先看看我们常用的lua字符串创建函数：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastring.c</span></span><br><span class="line"><span class="comment">// only short strings can be interal</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> struct TString* <span class="title">internalstr</span><span class="params">(struct lua_State* L, <span class="keyword">const</span> <span class="keyword">char</span>* str, <span class="keyword">unsigned</span> <span class="keyword">int</span> l)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">stringtable</span>* <span class="title">tb</span> = &amp;<span class="title">g</span>-&gt;<span class="title">strt</span>;</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> h = luaS_hash(L, str, l, g-&gt;seed); </span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>** <span class="title">list</span> = &amp;<span class="title">tb</span>-&gt;<span class="title">hash</span>[<span class="title">lmod</span>(<span class="title">h</span>, <span class="title">tb</span>-&gt;<span class="title">size</span>)];</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (struct TString* ts = *<span class="built_in">list</span>; ts; ts = ts-&gt;u.hnext) &#123;</span><br><span class="line">        <span class="keyword">if</span> (ts-&gt;shrlen == l &amp;&amp; (<span class="built_in">memcmp</span>(getstr(ts), str, l * <span class="keyword">sizeof</span>(<span class="keyword">char</span>)) == <span class="number">0</span>)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (isdead(g, ts)) &#123;</span><br><span class="line">                changewhite(ts);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> ts;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (tb-&gt;nuse &gt;= tb-&gt;<span class="built_in">size</span> &amp;&amp; tb-&gt;<span class="built_in">size</span> &lt; INT_MAX / <span class="number">2</span>) &#123;</span><br><span class="line">        luaS_resize(L, tb-&gt;<span class="built_in">size</span> * <span class="number">2</span>);</span><br><span class="line">        <span class="built_in">list</span> = &amp;tb-&gt;hash[lmod(h, tb-&gt;<span class="built_in">size</span>)];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">createstrobj</span>(<span class="title">L</span>, <span class="title">str</span>, <span class="title">LUA_SHRSTR</span>, <span class="title">l</span>, <span class="title">h</span>);</span></span><br><span class="line">    ts-&gt;u.hnext = *<span class="built_in">list</span>;</span><br><span class="line">    *<span class="built_in">list</span> = ts;</span><br><span class="line">    tb-&gt;nuse++;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ts;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">struct TString* <span class="title">luaS_createlongstr</span><span class="params">(struct lua_State* L, <span class="keyword">const</span> <span class="keyword">char</span>* str, <span class="keyword">size_t</span> l)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> createstrobj(L, str, LUA_LNGSTR, l, G(L)-&gt;seed);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">struct TString* <span class="title">luaS_newlstr</span><span class="params">(struct lua_State* L, <span class="keyword">const</span> <span class="keyword">char</span>* str, <span class="keyword">unsigned</span> <span class="keyword">int</span> l)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (l &lt;= MAXSHORTSTR) &#123;</span><br><span class="line">        <span class="keyword">return</span> internalstr(L, str, l); </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> luaS_createlongstr(L, str, l); </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们可以看到，luaS_createlongstr函数，会先判断，要创建的字符串是长字符串还是短字符串，如果是短字符串则进行内部化处理，如果是长字符串，则直接创建TString实例。字符串内部化的逻辑处理也很简单：</p>
<ul>
<li>首先计算要创建的字符串的hash值</li>
<li>根据hash值，计算该字符串应该被放在strt-&gt;hash表中的哪个位置(slot_idx = hash &amp; strt-&gt;size - 1)</li>
<li>取出该位置的TString*链表，逐个实例进行比较，如果找到同内容的TString实例，则不创建新的TString实例，将该指针返回；如果找不到，则创建新的TString实例，并插入链表头部（如之前展示的gif图所示）<br> 接下来我们来看看字符串比较的逻辑：<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastring.c</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaS_eqshrstr</span><span class="params">(struct lua_State* L, struct TString* a, struct TString* b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (a == b) || (a-&gt;shrlen == b-&gt;shrlen &amp;&amp; <span class="built_in">strcmp</span>(getstr(a), getstr(b)) == <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaS_eqlngstr</span><span class="params">(struct lua_State* L, struct TString* a, struct TString* b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (a == b) || (a-&gt;u.lnglen == b-&gt;u.lnglen &amp;&amp; <span class="built_in">strcmp</span>(getstr(a), getstr(b)) == <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>不论是长字符串还是短字符串，他们的比较方式都一致，首先比较字符串实例的地址，地址相同自然相等。长度不同自然不相等，当地址不同，长度不同时，才会触发字符串比较的逻辑，这里的getstr函数，会将String Body的地址返回，方便我们使用TString对象的字符串值部分。</p>
<p>  也许细心的读者会发现，在global_State的结构中，还有一个strcache的二维数组，该数组将作为字符串缓存存在。我们创建lua字符串还有另一个函数，则是luaS_new函数，它的定义如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastring.c</span></span><br><span class="line"><span class="function">struct TString* <span class="title">luaS_new</span><span class="params">(struct lua_State* L, <span class="keyword">const</span> <span class="keyword">char</span>* str, <span class="keyword">unsigned</span> <span class="keyword">int</span> l)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> hash = point2uint(str); </span><br><span class="line">    <span class="keyword">int</span> i = hash % STRCACHE_M;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; STRCACHE_N; j ++) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">G</span>(<span class="title">L</span>)-&gt;<span class="title">strcache</span>[<span class="title">i</span>][<span class="title">j</span>];</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">strcmp</span>(getstr(ts), str) == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> ts;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = STRCACHE_N - <span class="number">1</span>; j &gt; <span class="number">0</span>; j--) &#123;</span><br><span class="line">        G(L)-&gt;strcache[i][j] = G(L)-&gt;strcache[i][j - <span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    G(L)-&gt;strcache[i][<span class="number">0</span>] = luaS_newlstr(L, str, l);</span><br><span class="line">    <span class="keyword">return</span> G(L)-&gt;strcache[i][<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个函数首先会对c层字符串str的地址进行hash运算，然后通过取膜规则，在strcache查找指定的TString实例，如果找到就返回，如果找不到，则丢弃一个，且创建一个新的TString实例并写入strcache中，这种方式对已经保存在c层且需要频繁转换为TString的c层字符串非常有效率上的帮助。strcache内的字符串缓存保存一个gc周期，gc进入sweep阶段前清空。</p>
<p>   我们最后需要讨论的部分，就是将现有的TString模块整合到gc机制中，本章将不会对gc作深入的探讨，如果读者想深入研究可以看我的上一篇blog，《Garbage Collection基础架构》，本章只关注TString如何整合到现有的GC机制中。阅读过我上篇blog的读者应该可以回想起，dummylua的gc机制，目前包含几个阶段，分别是restart、propagate、atomic和sweep这几个阶段，restart是gc开始前的准备阶段，propagate则是传播、扫描阶段，atomic则是一次性传播、扫描阶段，sweep则是清理阶段。在propagate阶段，对TString实例进行传播扫描时，由于TString不可能包含子节点，因此可以在propagate阶段直接标记为黑色，其逻辑如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">reallymarkobject</span><span class="params">(struct lua_State* L, struct GCObject* gco)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    white2gray(gco);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">switch</span>(gco-&gt;tt_) &#123;</span><br><span class="line">        <span class="keyword">case</span> LUA_TTHREAD:&#123;</span><br><span class="line">            linkgclist(gco2th(gco), g-&gt;gray);            </span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_TTABLE:&#123;</span><br><span class="line">            linkgclist(gco2tbl(gco), g-&gt;gray);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_SHRSTR:&#123; </span><br><span class="line">            gray2black(gco);</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">gco2ts</span>(<span class="title">gco</span>);</span></span><br><span class="line">            g-&gt;GCmemtrav += sizelstring(ts-&gt;shrlen);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_LNGSTR: &#123;</span><br><span class="line">            gray2black(gco);</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">gco2ts</span>(<span class="title">gco</span>);</span></span><br><span class="line">            g-&gt;GCmemtrav += sizelstring(ts-&gt;u.lnglen);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:<span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里这样做的好处就是，TString实例不放入gray列表，再去做无谓的扫描，从而一定程度提升性能。除了在传播、扫描时，被标记的阶段需要做处理，我们还需要在atomic阶段结束时，将strcache清空，以避免在sweep阶段，将字符串实例删除后，在strcache留下野指针。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">atomic</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    g-&gt;gray = g-&gt;grayagain;</span><br><span class="line">    g-&gt;grayagain = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    g-&gt;gcstate = GCSinsideatomic;</span><br><span class="line">    propagateall(L);</span><br><span class="line">    g-&gt;currentwhite = cast(lu_byte, otherwhite(g));</span><br><span class="line"></span><br><span class="line">    luaS_clearcache(L);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里的luaS_clearcache做的就是情况缓存的操作。在gc的sweep阶段，回收短字符串时，还要记得将strt内对应的短字符串指针清空，避免留下野指针：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luagc.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> lu_mem <span class="title">freeobj</span><span class="params">(struct lua_State* L, struct GCObject* gco)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">switch</span>(gco-&gt;tt_) &#123;</span><br><span class="line">        <span class="keyword">case</span> LUA_SHRSTR: &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">gco2ts</span>(<span class="title">gco</span>);</span></span><br><span class="line">            luaS_remove(L, ts);</span><br><span class="line">            lu_mem sz = sizelstring(ts-&gt;shrlen);</span><br><span class="line">            luaM_free(L, ts, sz); </span><br><span class="line">            <span class="keyword">return</span> sz; </span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_LNGSTR: &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">ts</span> = <span class="title">gco2ts</span>(<span class="title">gco</span>);</span></span><br><span class="line">            lu_mem sz = sizelstring(ts-&gt;u.lnglen);</span><br><span class="line">            luaM_free(L, ts, sz);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_TTABLE: &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">Table</span>* <span class="title">tbl</span> = <span class="title">gco2tbl</span>(<span class="title">gco</span>);</span></span><br><span class="line">            lu_mem sz = <span class="keyword">sizeof</span>(struct Table) + tbl-&gt;arraysize * <span class="keyword">sizeof</span>(TValue) + twoto(tbl-&gt;lsizenode) * <span class="keyword">sizeof</span>(Node);</span><br><span class="line">            luaH_free(L, tbl);</span><br><span class="line">            <span class="keyword">return</span> sz;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:&#123;</span><br><span class="line">            lua_assert(<span class="number">0</span>);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>如果你对这个部分感到疑惑，我建议先回顾一下本系列的gc部分。到目前为止，有关TString的内容就全部结束了。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>   TString模块，早在去年12月份就完成开发测试了，不过一直迟迟没能抽出时间来写blog，如今我也花费了不算小的篇幅来阐述dummylua项目字符串相关的内容，这个部分对于阅读官方源码字符串部分，同样具有借鉴意义。虽然和其他部分相比，这篇并不算特别复杂，但是也耗费了我整整3天满打满算的时间，原创不易，希望大家转载能够注明出处。下一篇将进入到lua Table的的设计实现的论述上，敬请期待。</p>
<p>Reference<br>[1] <a href="https://storage.googleapis.com/google-code-archive-downloads/v2/code.google.com/luadec/%E4%BA%91%E9%A3%8E-lua%E6%BA%90%E7%A0%81%E6%AC%A3%E8%B5%8F-lua-5.2.pdf" rel="external nofollow noopener noreferrer" target="_blank">Lua源码欣赏 3.1.1 Hash Dos “lua 5.2.1</a>为了解决这个问题，把长字符串独立出来。大量文本处理中的输入的字符串不再通过哈希内部 化进入全局字符串表。同时，使用了一个随机种子用于哈希值的计算，使攻击者无法轻易构造出拥有相同哈 希值的不同字符串”<br>[2] <a href="https://stackoverflow.com/questions/2334218/why-are-xor-often-used-in-java-hashcode-but-another-bitwise-operators-are-used" rel="external nofollow noopener noreferrer" target="_blank">Why are XOR often used in java hashCode() but another bitwise operators are used rarely?</a><br>[3] <a href="http://lua-users.org/lists/lua-l/2012-01/msg00497.html" rel="external nofollow noopener noreferrer" target="_blank">Lua Hash Dos Attack</a></p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part6：脚本运行基础架构的设计与实现</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part6/</url>
    <content><![CDATA[<p><a href="http://manistein.club/post/program/let-us-build-a-lua-interpreter/%E6%9E%84%E5%BB%BAlua%E8%A7%A3%E9%87%8A%E5%99%A8part6/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>​    构建Lua解释器Part5，对Lua解释器进行了整体介绍，并且以一个hello world程序为例子，给读者一个初步的概念。通过那一篇，我们知道了编译器至少要包括词法分析其和语法分析器，而本篇，我将集中时间和精力，用来介绍和讲解Lua词法分析器的设计与实现，实际上，它是对Part5词法分析器部分的一个补充。本文所指的词法分析器，是参照Lua-5.3这个版本的源码，并且亲自动手实现和测试过，它也已经被整合到<a href="https://github.com/Manistein/dummylua-tutorial" rel="external nofollow noopener noreferrer" target="_blank">dummylua</a>这个工程中，欢迎大家star。由于整个词法分析是我自己重新实现，因此不会在所有的细节上和官方lua保持一致，最后由于本人水平有限，如有写的不正确的地方，欢迎大家批评指正。此外，我已经建了一个qq群(QQ:185017593)，有兴趣参与技术讨论的同学可以加进来。</p>
<h1 id="词法分析器简介"><a href="#词法分析器简介" class="headerlink" title="词法分析器简介"></a>词法分析器简介</h1><p>​    首先我要对词法分析器作一个简短的介绍，所谓的词法分析器，其实就是对输入的内容(文本、字符串等)进行词法分析的模块。英文维基百科对词法分析的解释如下[1]：</p>
<blockquote>
<p>In computer science, lexical analysis, lexing or tokenization is the process of converting a sequence of characters (such as in a computer program or web page) into a sequence of tokens (strings with an assigned and thus identified meaning). A program that performs lexical analysis may be termed a lexer, tokenizer,[1] or scanner, although scanner is also a term for the first stage of a lexer. A lexer is generally combined with a parser, which together analyze the syntax of programming languages, web pages, and so forth.</p>
</blockquote>
<p>总而言之，词法分析就是将一串字符，转化为一串token，而词法分析器就是执行这一过程的逻辑模块。<br>    在编译器中，什么是token？token是一门语言中，能够被识别的最小单元。现在来看一个例子，假设我么有一个文件，其内部的内容如下所示：</p>
<figure class="highlight applescript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">name</span>  <span class="string">"hello"</span>  <span class="number">2020.</span></span><br></pre></td></tr></table></figure>
<p>现在要对这个文本进行词法分析，首先我们要做的是，将文本字符加载到词法分析器的缓存中。在完成文本加载以后，我们需要从中，一个一个地获取有效token，而获取token的操作，是通过词法分析器里的一个函数来实现，我们可以假设它叫next函数。我们通过调用next函数若干次，得到以下结果</p>
<ul>
<li>我们通过调用next函数，词法分析器返回第一个有效被识别的token，这个token就是name，它是一个标识符，能够表示变量。</li>
<li>然后我们再次调用next函数时，能够获取第二个token，这个token是一个字符串“hello”。</li>
<li>当我们第三次调用next函数时，则获取了第三个token，这个token是个数值。</li>
<li>当我们第四次调用next函数时，获取一个ASCII符号”.”</li>
<li>当我们第五次调用next函数时，获取文本文书标志EOF</li>
</ul>
<p>从上面的例子中，我们可以看到一些有效的信息。首先是，词法分析需要和文本或者字符串打交道，如果我们的代码是存放在文本中的，那么词法分析器首先要将文本中的代码，加载到内存中。被加载到内存的文本内容，实际上就是一个字符序列。词法分析器需要对这个字符序列进行进一步的提取工作。我们一次只能获取一个有效的token，获取这个token的函数由词法分析器提供，比如例子里的next函数。其次是，我们所称的token，其实能够表示的内容非常丰富，它可以表示标志符，可以表示字符串，可以表是ASCII字符，可以是表示文本结束的EOF标识。正因为token能够表示的内容相当丰富，因此我们需要对token进行分类。实际上，我们一个token，既要表明它是什么类型的，还要表明自己包含的内容是什么，它的结构，在逻辑上如下所示：</p>
<figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line">Token</span><br><span class="line"><span class="code">+--------+</span>--------+</span><br><span class="line">|  Type  |  Data  |</span><br><span class="line"><span class="code">+--------+</span>--------+</span><br></pre></td></tr></table></figure>
<p>有些token，只是说明类型是不够的，它还需要存储token的内容，比如我们的标识符，需要将组成标识符的内容的字符串存入token的data域中，接下来，我们通过&lt; Type, Data &gt;的方式来表示一个token，那么我们持续调用next函数，并将其打印，则获得如下内容（Type使用lua中使用的定义）：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="params">&lt;TK_NAME, "name"&gt;</span>    <span class="comment">// TK_NAME表示它是标识符类型</span></span><br><span class="line"><span class="params">&lt;TK_STRING, "hello"&gt;</span></span><br><span class="line"><span class="params">&lt;TK_INT, <span class="number">2020</span>&gt;</span></span><br><span class="line"><span class="params">&lt;., null&gt;</span></span><br><span class="line"><span class="params">&lt;TK_EOS, null&gt;</span></span><br></pre></td></tr></table></figure>
<p>从上面的例子中，我们可以感知，token能够表示的内容丰富，有些token通过type就能表示，有些还需要存储其内容在data域，以供词法分析器使用。<br>    本节对词法分析器的简介，就到此结束，后面将展开lua词法分析器的设计与实现的论述。</p>
<h1 id="词法分析器基本数据结构"><a href="#词法分析器基本数据结构" class="headerlink" title="词法分析器基本数据结构"></a>词法分析器基本数据结构</h1><p>​    本节开始介绍<a href="https://github.com/Manistein/dummylua-tutorial" rel="external nofollow noopener noreferrer" target="_blank">dummylua</a>词法分析器的基本数据结构，dummylua的词法分析器，基本参照lua-5.3.5的设计与实现。首先，我来介绍一下lua的Token数据结构：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> Seminfo &#123;</span><br><span class="line">    lua_Number r;</span><br><span class="line">    lua_Integer i;</span><br><span class="line">    TString* s; </span><br><span class="line">&#125; Seminfo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Token</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> token;          <span class="comment">// token enum value</span></span><br><span class="line">    Seminfo seminfo;    <span class="comment">// token info</span></span><br><span class="line">&#125; Token;</span><br></pre></td></tr></table></figure>
<p>Token结构中，包含一个int变量token，还有一个Seminfo结构的变量seminfo。其中token字段代表Token实例的类型，而seminfo则用来保存token对应类型的实际值。<br>    lua的token类型，一部分是直接使用ASCII值，一部分是定义在一个枚举类型中。我们现在来看一下，token一共有哪些类型，我们现在来看一下分类：</p>
<ul>
<li>EOF：文件结束符，表示文件结束，意为End Of File，使用单独的枚举值TK_EOS</li>
<li>算数：+,-,*,/,%,^,~,&amp;,|,&lt;&lt;,&gt;&gt;。由于&lt;&lt;和&gt;&gt;无法使用单独的字符来表示token的类型，因此他们使用单独的枚举值，TK_SHL和TK_SHR</li>
<li>括号：(,),[,],{,}</li>
<li>赋值：=</li>
<li>比较：&gt;,&lt;,&gt;=,&lt;=,~=,==。由于&gt;=,&lt;=,~=,==无法单独使用，因此他们要使用单独的枚举值，TK_GREATEQ,TK_LESSEQ,TK_NOTEQUAL,TK_EQUAL</li>
<li>分隔：,和;</li>
<li>字符串：’string’和”string”，字符串类型使用TK_STRING</li>
<li>连接符：..，因为单个字符无法表示，因此它也是用单独的枚举值TK_CONCAT</li>
<li>数字：数值分为浮点数和整数，浮点数使用TK_FLOAT，而整数使用TK_INT</li>
<li>标识符：就是我们常说的identity，通常用来表示变量，这种类型在lua中，统称为TK_NAME</li>
<li>保留字：local,nil,true,false,end,then,if,elseif,not,and,or,function等，每个保留字，都是一个token类型，比如local是TK_LOCAL类型，而NIL则是TK_NIL，依次类推</li>
</ul>
<p>此外，词法分析器，遇到空格，换行符\r\n、\n\r，制表符\t、\v等，是直接跳过，直至获取下一个不需要跳过的字符为止。在dummylua中，对Token的类型定义主要分为两个部分，第一部分是直接通过ASCII值来表示，比如’&gt;‘, ‘&lt;‘, ‘.‘和’,‘等，还有一部分通过枚举值来定义，我们现在来看一下枚举值的定义有哪些：</p>
<figure class="highlight autohotkey"><table><tr><td class="code"><pre><span class="line">// lualexer.h</span><br><span class="line">// <span class="number">1</span>~<span class="number">256</span> should reserve for ASCII character token</span><br><span class="line">enum RESERVED &#123;</span><br><span class="line">	<span class="comment">/* terminal token donated by reserved word */</span></span><br><span class="line">	TK_LOCAL = FIRST_REVERSED,</span><br><span class="line"><span class="built_in">	TK_NIL,</span></span><br><span class="line"><span class="built_in">	TK_TRUE,</span></span><br><span class="line"><span class="built_in">	TK_FALSE,</span></span><br><span class="line"><span class="built_in">	TK_END,</span></span><br><span class="line"><span class="built_in">	TK_THEN,</span></span><br><span class="line"><span class="built_in">	TK_IF,</span></span><br><span class="line"><span class="built_in">	TK_ELSEIF,</span></span><br><span class="line"><span class="built_in">	TK_NOT,</span></span><br><span class="line"><span class="built_in">	TK_AND,</span></span><br><span class="line"><span class="built_in">	TK_OR,</span></span><br><span class="line"><span class="built_in">	TK_FUNCTION,</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">/* other token */</span></span><br><span class="line"><span class="built_in">	TK_STRING,</span></span><br><span class="line"><span class="built_in">	TK_NAME,</span></span><br><span class="line"><span class="built_in">	TK_FLOAT,</span></span><br><span class="line"><span class="built_in">	TK_INT,</span></span><br><span class="line"><span class="built_in">	TK_NOTEQUAL,</span></span><br><span class="line"><span class="built_in">	TK_EQUAL,</span></span><br><span class="line"><span class="built_in">	TK_GREATEREQUAL,</span></span><br><span class="line"><span class="built_in">	TK_LESSEQUAL,</span></span><br><span class="line"><span class="built_in">	TK_SHL,</span></span><br><span class="line"><span class="built_in">	TK_SHR,</span></span><br><span class="line"><span class="built_in">	TK_MOD,</span></span><br><span class="line"><span class="built_in">	TK_DOT,</span></span><br><span class="line"><span class="built_in">	TK_VARARG,</span></span><br><span class="line"><span class="built_in">	TK_CONCAT,</span></span><br><span class="line"><span class="built_in">	TK_EOS,</span></span><br><span class="line">&#125;<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>FIRST_REVERSED的值是257，为什么取257开始呢？由于我们有很多token类型（主要是单个字符就能表示的token）是直接通过ASCII值来表示，为了避免和ASCII的值冲突，因此这里直接从257开始。在众多的类型中，只有几种需要保存值到token实例中，他们分别是TK_NAME、TK_FLOAT、TK_INT和TK_STRING，于是我们的Seminfo结构就派上用场了。Seminfo结构是一个union类型，它包含三个域，一个是lua_Number类型，用于存放浮点型数据，一个是lua_Integer类型，用于存放整型数据，一个是TString用于存放标识符和字符串的值。<br>    现在我们对token结构有了一个初步的认识，接下来要介绍则是词法分析器里要用到的最重要的数据结构，它就是LexState结构，其定义如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LexState</span> &#123;</span></span><br><span class="line">	Zio* zio;				<span class="comment">// 负责从文件中读取、缓存字符，并提供字符的模块</span></span><br><span class="line">    <span class="keyword">int</span> current;			<span class="comment">// 从zio实例中，获取的当前需要使用的字符</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">MBuffer</span>* <span class="title">buff</span>;</span>	<span class="comment">// 保留字本身以及TK_STRING、TK_NAME、TK_FLOAT和TK_INT的值，</span></span><br><span class="line">							<span class="comment">// 由于不止一个字符组成，因此token在被完全识别之前，读取出来</span></span><br><span class="line">							<span class="comment">// 的字符，应当存在buff结构中，当词法分析器攒够一个完整的token</span></span><br><span class="line">							<span class="comment">// 时，则将其拷贝到Seminfo.s（TK_NAME、TK_STRING类型和保留字）</span></span><br><span class="line">							<span class="comment">// Seminfo.r（TK_FLOAT类型，string转换成浮点型数值）或Seminfo.i</span></span><br><span class="line">							<span class="comment">// （TK_INT类型，string转换成整型数值）中</span></span><br><span class="line">	Token t;				<span class="comment">// current token</span></span><br><span class="line">	Token lookahead;        <span class="comment">// 提前获取的token，如果它存在（不为TK_EOS），那么词法分析器调用</span></span><br><span class="line">							<span class="comment">// next函数时，它的值直接被获取。</span></span><br><span class="line">	<span class="keyword">int</span> linenumber;			<span class="comment">// 代码的行号</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">Dyndata</span>* <span class="title">dyd</span>;</span>    <span class="comment">// 语法分析过程中，存放local变量信息的结构</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">FuncState</span>* <span class="title">fs</span>;</span>   <span class="comment">// 语法分析器数据实例</span></span><br><span class="line">	lua_State* L;			<span class="comment">// lua vm实例</span></span><br><span class="line">	TString* source;        <span class="comment">// 正在进行编译的源码文件名称</span></span><br><span class="line">	TString* env;           <span class="comment">// 一般是_ENV</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">Table</span>* <span class="title">h</span>;</span>       	<span class="comment">// 常量缓存表，用于缓存lua代码中的常量，加快编译时的常量查找</span></span><br><span class="line">&#125; LexState;</span><br></pre></td></tr></table></figure>
<p>当我们的编译模块，要对一个文本里的代码进行编译时，首先会创建一个LexState的数据实例。词法分析器的工作，首先是要将代码文件，加载到内存中，被加载到内存中的代码，是一个字符串。词法分析器要做的第二个工作，就是将被加载到内存中的字符串里的字符，一个一个获取出来，并组成合适的token，如果不能组成，则抛出异常。<br>    正如前面所说，词法分析器第一个工作，就是要将代码加载到内存中，作为官方lua-5.3的仿制品，dummylua的词法分析器和官方lua一样，采用一个叫做Zio的结构，负责存放从磁盘中加载出来的代码，其结构如下所示：</p>
<figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luazio.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">char</span>* (*lua_Reader)(struct lua_State* L, <span class="keyword">void</span>* data, <span class="keyword">size_t</span>* <span class="built_in">size</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LoadF</span> &#123;</span></span><br><span class="line">    FILE* f;</span><br><span class="line">    <span class="keyword">char</span> buff[BUFSIZE]; <span class="comment">// read the file stream into buff</span></span><br><span class="line">	<span class="keyword">int</span> n;		       <span class="comment">// how many char you have read</span></span><br><span class="line">&#125; LoadF;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Zio</span> &#123;</span></span><br><span class="line">	lua_Reader reader;		<span class="comment">// read buffer to p</span></span><br><span class="line">	<span class="keyword">int</span> n;					<span class="comment">// the number of unused bytes</span></span><br><span class="line">	<span class="keyword">char</span>* p;				<span class="comment">// the pointer to buffer</span></span><br><span class="line">	<span class="keyword">void</span>* data;				<span class="comment">// structure which holds FILE handler</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">L</span>;</span></span><br><span class="line">&#125; Zio;</span><br></pre></td></tr></table></figure>
<p>与官方lua一样，dummylua的Zio结构，并没有限制使用者，用哪种方式来加载代码到内存中。而具体操作的函数，则是函数指针reader指向的函数，我们只要自定义的函数，符合这个签名，就能够被词法分析器调用，另外Zio的data指针，是作为reader函数的重要参数存在的，它同样可以由用户自己定义。不过lua提供了一个默认的LoadF结构，以及一个getF函数，用于将文件里的代码，加载到内存中，我将在接下来的内容中详细讨论。<br>    现在我们抛开具体的代码实现，通过一个实例，将整个流程串联起来，如图1所示，我们现在要将一个文件里的字节流读出，并识别里面的token。</p>
<p><img src="zio.png" alt="image">图1<br>识别token的流程，也是需要将源码文件里的字符，一个一个获取出来，第一个被获取的字符，将决定它进入哪个token类型的处理分支之中，事实上lua是通过一个叫做zget的宏，获取字符的，这个宏如下所示：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luazio.h</span></span><br><span class="line">#<span class="function"><span class="title">define</span> zget(z) (((z)-&gt;</span><span class="function"><span class="title">n</span>--) &gt; 0 ? (*(z)-&gt;</span>p++) : luaZ_fill(z))</span><br></pre></td></tr></table></figure>
<p>传入这个宏的，是一个Zio结构的数据实例。事实上，我们识别token的逻辑是在一个叫做llex的函数内进行的，这个函数会不断读取新的字符，并且判断应该生成哪个token，下面的伪代码展示了这一点：</p>
<figure class="highlight mel"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line">static <span class="keyword">int</span> llex(LexState* <span class="keyword">ls</span>, Seminfo* s) &#123;</span><br><span class="line">	...</span><br><span class="line">	<span class="keyword">ls</span>-&gt;current = zget(<span class="keyword">ls</span>-&gt;z);</span><br><span class="line">	<span class="keyword">switch</span>(<span class="keyword">ls</span>-&gt;current) &#123;</span><br><span class="line">	...</span><br><span class="line">	<span class="keyword">case</span> <span class="string">'\''</span>: <span class="keyword">case</span> <span class="string">'\"'</span>: &#123;</span><br><span class="line">		<span class="keyword">return</span> readstring(<span class="keyword">ls</span>, s);</span><br><span class="line">	&#125; <span class="keyword">break</span>;</span><br><span class="line">	<span class="keyword">case</span> <span class="string">'0'</span>: <span class="keyword">case</span> <span class="string">'1'</span>: <span class="keyword">case</span> <span class="string">'2'</span>: <span class="keyword">case</span> <span class="string">'3'</span>: <span class="keyword">case</span> <span class="string">'4'</span>:</span><br><span class="line">	<span class="keyword">case</span> <span class="string">'5'</span>: <span class="keyword">case</span> <span class="string">'6'</span>: <span class="keyword">case</span> <span class="string">'7'</span>: <span class="keyword">case</span> <span class="string">'8'</span>: <span class="keyword">case</span> <span class="string">'9'</span>: &#123;</span><br><span class="line">		<span class="keyword">return</span> readnumber(<span class="keyword">ls</span>, s);</span><br><span class="line">	&#125; <span class="keyword">break</span>;</span><br><span class="line">	...</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从上面的伪代码我们可以看到，词法分析器识别一个token，需要不断从源码文件中，一个一个读取字符，然后判断它可能是哪种类型的token，最后作相应的处理，比如readstring和readnumber操作，就是把有效字符串识和数值别出来，最后存储在LexState结构的Token类型变量t中。readstring函数和readnumber函数内部也会多次调用zget函数，不断获取新的字符，最后生成token。<br>    回到图1的例子，在词法分析器数据结构实例LexState完成初始化的伊始，我们刚完成初始化的Zio结构实例，会关联一个已经打开的文件，这个文件就是LoadF结构中的FILE<em>指针f指定，正如图所示，LoadF类型变量的n值为0，这意味着我们未读取buff中任何一个字符。而buff此时也未存储任何一个文件源码中的字符。现在回头来看看Zio数据实例本身，其void</em>指针，data指向了我们刚刚讨论过的LoadF类型实例，Zio的变量n表示，LoadF结构的buff中，还剩下多少个未读取的字符，char*指针p，应当指向LoadF结构中buff的某个位置，但是现在还是初始化状态，因此它是NULL，至于Zio中的reader函数，主要用于处理从文件中读取字符到LoadF结构的buff中的情况。<br>    回顾一下zget这个宏，当我们第一次调用zget这个宏的时候，它会首先调用一个叫做luaZ_fill的函数来处理，我们先忽略它的具体实现，通过一个情景展示来观察它的逻辑流程。这段逻辑的本质是，因为没有未被读取的字符，因此需要重新到文件里加载。以图1为例，假设BUFFSIZ的值为8，现在我们来看看它的执行步骤：</p>
<ul>
<li>从文件中读取8个字符，到LoadF结构实例中的buff中，此时LoadF中的n值仍然是。</li>
<li>Zio结构中的n值被赋值为8，指针p被赋值为buff的地址，如图2所示：<img src="zio02.png" alt="image">图2</li>
<li>返回p所指向的字符</li>
<li>p指针自增1，移动到下一个地址，结果如图3所示：<img src="zio03.png" alt="image">图3</li>
</ul>
<p>在这个阶段之后，每次我们调用zget这个宏，就会返回Zio的变量p所指向的字符，并且自增，同时Zio的变量n自减1，LoadF的n自增1。当Zio的变量n为0时，此时如果再调用zget宏，那么说明LoadF中的buff的字符已经被取完了，因此需要重新从磁盘获取BUFFSIZ个字符，得到图4的结果。<br><img src="zio04.png" alt="image">图4<br>然后和前面概述的一样，返回p所指向的字符，并且p指针自增，LoadF的n值加1，Zio的n值减1。<br>    现在我们来看一下luaZ_fill函数的实现：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luazio.c</span></span><br><span class="line">int luaZ_fill(Zio* z) &#123;</span><br><span class="line">	int c = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">	size_t read_size = <span class="number">0</span>;</span><br><span class="line">	<span class="function"><span class="title">z</span>-&gt;</span><span class="function"><span class="title">p</span> = (void*)z-&gt;</span><span class="function"><span class="title">reader</span>(z-&gt;</span>L, <span class="function"><span class="title">z</span>-&gt;</span><span class="keyword">data</span>, &amp;read_size);</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">if</span> (read_size &gt; <span class="number">0</span>) &#123;</span><br><span class="line">		<span class="function"><span class="title">z</span>-&gt;</span>n = (int)read_size;</span><br><span class="line">		<span class="function"><span class="title">c</span> = (int)(*z-&gt;</span>p);</span><br><span class="line"></span><br><span class="line">		<span class="function"><span class="title">z</span>-&gt;</span>p++;</span><br><span class="line">		<span class="function"><span class="title">z</span>-&gt;</span>n--;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> &#123;</span><br><span class="line">		c = EOF;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	return c;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>而这里的reader函数，在是在luaaux.c文件里，名称为getF函数：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaaux.c</span></span><br><span class="line">static char* getF(struct lua_State* L, void* <span class="keyword">data</span>, size_t* sz) &#123;</span><br><span class="line">	LoadF* lf = (LoadF*)<span class="keyword">data</span>;</span><br><span class="line">	<span class="function"><span class="title">if</span> (lf-&gt;</span>n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">		*<span class="function"><span class="title">sz</span> = lf-&gt;</span>n;</span><br><span class="line">		<span class="function"><span class="title">lf</span>-&gt;</span>n = <span class="number">0</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> &#123;</span><br><span class="line">		*<span class="function"><span class="title">sz</span> = fread(lf-&gt;</span><span class="function"><span class="title">buff</span>, sizeof(char), BUFSIZE, lf-&gt;</span>f);</span><br><span class="line">		<span class="function"><span class="title">lf</span>-&gt;</span>n = <span class="number">0</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="title">return</span> lf-&gt;</span>buff;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里的逻辑非常清晰，每当Zio结构里，字符被取完的时候，就需要调用luaZ_fill函数，该函数会通过reader函数，获取新的字符缓存，以及缓存的字符数量，然后返回p指针所指向的字符，最后p指针向前移动一位，n变量减1。这里的reader函数，实际上就是getF函数。结合上面描述的流程，要读懂这两段代码并不难。<br>    我们之所以要用到Zio这样的结构，主要的原因是词法分析器，需要从源码文件中，一个一个获取字符，如果每次都要走io，那么效率将会非常低，但是如果每次都将所有的代码加载进来，并且缓存，如果代码中，某个block有词法错误，整个词法分析的流程就会中断，如果文本较大，且位于开头的token识别起来就有问题，那么花费时间在io处理上就是极大的浪费，因此这里采用的策略就是，一部分一部分地加载到代码缓存中。<br>    到目前为止，我就完成了lua词法分析器的基本数据结构的说明了，接下来将会对lua词法分析器的常用接口，初始化流程，和token识别流程进行说明。</p>
<h1 id="词法分析器的接口"><a href="#词法分析器的接口" class="headerlink" title="词法分析器的接口"></a>词法分析器的接口</h1><p>​    在了解了词法分析器的数据结构以后，我们现在来看看，词法分析器一共有哪些主要的接口，现在将接口展示如下所示：</p>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 模块初始化</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaX_init</span>(<span class="params"><span class="keyword">struct</span> lua_State* L</span>)</span>;</span><br><span class="line"><span class="comment">// 词法分析器实例初始化</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaX_setinput</span>(<span class="params"><span class="keyword">struct</span> lua_State* L, LexState* ls, Zio* z, <span class="keyword">struct</span> MBuffer* buffer, </span></span></span><br><span class="line"><span class="function"><span class="params">	<span class="keyword">struct</span> Dyndata* dyd, TString* source, TString* env</span>)</span>;</span><br><span class="line"><span class="comment">// 提前获取下一个token的信息，并暂存</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaX_lookahead</span>(<span class="params"><span class="keyword">struct</span> lua_State* L, LexState* ls</span>)</span>;</span><br><span class="line"><span class="comment">// 获取下一个token，如果有lookahead暂存的token，就直接获取，否则通过</span></span><br><span class="line"><span class="comment">// llex函数获取下一个token</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaX_next</span>(<span class="params"><span class="keyword">struct</span> lua_State* L, LexState* ls</span>)</span>;</span><br><span class="line"><span class="comment">// 抛出词法分析错误</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaX_syntaxerror</span>(<span class="params"><span class="keyword">struct</span> lua_State* L, LexState* ls, <span class="keyword">const</span> <span class="keyword">char</span>* error_text</span>)</span>;</span><br></pre></td></tr></table></figure>
<p>接口非常简洁，无非就是初始化和获取下一个token的接口。</p>
<h1 id="初始化流程"><a href="#初始化流程" class="headerlink" title="初始化流程"></a>初始化流程</h1><p>​    在完成了lua词法分析器的讨论以后，我们现在来看一下词法分析器的初始化操作。词法分析器首要要做的初始化处理，即是对保留字进行内部化处理，由于保留字全部是短字符串，因此TString实例的extra字段不为0，并且值为对应token类型的枚举值。这样做的目的是，由于我们经过内部化的短字符串会被缓存起来，因此当我们识别到保留字的token的时候，会从缓存中直接获取字符串TString实例，通过这个TString实例的extra字段，我们可以直接获得其token类别的枚举值，方便我们在语法分析时的处理。<br>​    我们现在来看一下，lua词法分析器的初始化逻辑：</p>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line"><span class="comment">// the sequence must be the same as enum RESERVED</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span>* luaX_tokens[] = &#123;</span><br><span class="line">	<span class="string">"local"</span>, <span class="string">"nil"</span>, <span class="string">"true"</span>, <span class="string">"false"</span>, <span class="string">"end"</span>, <span class="string">"then"</span>, <span class="string">"if"</span>, <span class="string">"elseif"</span>, <span class="string">"not"</span>, <span class="string">"and"</span>, <span class="string">"or"</span>, <span class="string">"function"</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaX_init</span>(<span class="params"><span class="keyword">struct</span> lua_State* L</span>)</span> &#123;</span><br><span class="line">	TString* env = luaS_newliteral(L, LUA_ENV);</span><br><span class="line">	luaC_fix(L, obj2gco(env));</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; NUM_RESERVED; i++) &#123;</span><br><span class="line">		TString* reserved = luaS_newliteral(L, luaX_tokens[i]);</span><br><span class="line">		luaC_fix(L, obj2gco(reserved));</span><br><span class="line">		reserved-&gt;extra = i + FIRST_REVERSED;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们可以看到，初始化阶段，会创建保留字的字符串，由于保留字的字符数均少于40字节，因此他们属于短字符串，并且能够内部化。init函数对这些字符串，进行了脱离gc管理的操作，其本质就是从allgc列表中，移到fixgc列表中，避免这些保留字字符串被gc掉。回顾一下Part3，我们可以知道，当TString为短字符串时，extra字段不为0，则表示不能被gc，并且这个值现在被赋值为保留字类型的枚举值。我们的词法分析器初始化操作，主要是在创建lua_State实例的函数lua_newstate里调用的。<br>    我们在加载一段lua代码，并且开始编译的时候，需要创建一个LexState的结构，并且初始化它，初始化它的操作，在luaY_parser函数中，这个函数主要用来编译lua代码的：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line">LClosure* luaY_parser(<span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span></span>* L, Zio* zio, MBuffer* buffer, Dyndata* dyd, <span class="keyword">const</span> <span class="built_in">char</span>* name) &#123;</span><br><span class="line">	FuncState fs;</span><br><span class="line">	LexState ls;</span><br><span class="line">	luaX_setinput(L, &amp;ls, zio, buffer, dyd, luaS_newliteral(L, name), luaS_newliteral(L, LUA_ENV));</span><br><span class="line">	ls.current = zget(ls.zio);</span><br><span class="line">	</span><br><span class="line">	LClosure* closure = luaF_newLclosure(L, <span class="number">1</span>);</span><br><span class="line">	closure-&gt;p = fs.p = luaF_newproto(L);</span><br><span class="line"></span><br><span class="line">	setlclvalue(L-&gt;top, closure);</span><br><span class="line">	increase_top(L);</span><br><span class="line">	ptrdiff_t save_top = savestack(L, L-&gt;top);</span><br><span class="line"></span><br><span class="line">	ls.h = luaH_new(L);</span><br><span class="line">	setgco(L-&gt;top, obj2gco(ls.h));</span><br><span class="line">	increase_top(L);</span><br><span class="line"></span><br><span class="line">	mainfunc(L, &amp;ls, &amp;fs);</span><br><span class="line">	L-&gt;top = restorestack(L, save_top);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> closure;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面调用luaX_setinput函数，则是对词法分析器实例进行初始化操作：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line">void luaX_setinput(struct lua_State* L, </span><br><span class="line">	LexState* ls, Zio* z, struct MBuffer* buffer, struct Dyndata* dyd, TString* source, TString* env) &#123;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>L = L;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>source = source;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>env = env;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>current = <span class="number">0</span>;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>buff = buffer;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>dyd = dyd;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>env = env;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>fs = NULL;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>linenumber = <span class="number">1</span>;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>t.token = <span class="number">0</span>;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>t.seminfo.i = <span class="number">0</span>;</span><br><span class="line">	<span class="function"><span class="title">ls</span>-&gt;</span>zio = z;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里的初始化操作很简单，主要是做一些赋值操作，前面介绍数据结构的时候，有对LexState结构进行说明，这里不再赘述。</p>
<h1 id="Token识别流程"><a href="#Token识别流程" class="headerlink" title="Token识别流程"></a>Token识别流程</h1><p>​    词法分析器，将在语法分析器内被使用，语法分析器要调用一个新的token，需要调用luaX_next来实现，我们现在来看看luaX_next函数的定义：</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line">int luaX_next(struct lua_State* <span class="keyword">L</span>, LexState* <span class="keyword">ls</span>) &#123;</span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;lookahead.<span class="keyword">token</span> != TK_EOS) &#123;</span><br><span class="line">		<span class="keyword">ls</span>-&gt;t.<span class="keyword">token</span> = <span class="keyword">ls</span>-&gt;lookahead.<span class="keyword">token</span>;</span><br><span class="line">		<span class="keyword">ls</span>-&gt;lookahead.<span class="keyword">token</span> = TK_EOS;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">ls</span>-&gt;t.<span class="keyword">token</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">ls</span>-&gt;t.<span class="keyword">token</span> = llex(<span class="keyword">ls</span>, &amp;<span class="keyword">ls</span>-&gt;t.seminfo);</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">ls</span>-&gt;t.<span class="keyword">token</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们可以看到，如果lookhead里有存token的信息，那么就直接返回给LexState的token变量t，否则直接调用llex函数来识别新的token：</p>
<figure class="highlight mel"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.h</span></span><br><span class="line">#define next(<span class="keyword">ls</span>) (<span class="keyword">ls</span>-&gt;current = zget(<span class="keyword">ls</span>-&gt;zio))</span><br><span class="line"></span><br><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line">static <span class="keyword">int</span> llex(LexState* <span class="keyword">ls</span>, Seminfo* seminfo) &#123;</span><br><span class="line">	<span class="keyword">for</span> (;;) &#123;</span><br><span class="line">		luaZ_resetbuffer(<span class="keyword">ls</span>);</span><br><span class="line">		<span class="keyword">switch</span> (<span class="keyword">ls</span>-&gt;current)</span><br><span class="line">		&#123;</span><br><span class="line">		<span class="comment">// new line</span></span><br><span class="line">		<span class="keyword">case</span> <span class="string">'\n'</span>: <span class="keyword">case</span> <span class="string">'\r'</span>: &#123;</span><br><span class="line">			inclinenumber(<span class="keyword">ls</span>);</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="comment">// skip spaces</span></span><br><span class="line">		<span class="keyword">case</span> <span class="string">' '</span>: <span class="keyword">case</span> <span class="string">'\t'</span>: <span class="keyword">case</span> <span class="string">'\v'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'-'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="comment">// this line is comment</span></span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'-'</span>) &#123;</span><br><span class="line">				<span class="keyword">while</span> (!currIsNewLine(<span class="keyword">ls</span>) &amp;&amp; <span class="keyword">ls</span>-&gt;current != EOF)</span><br><span class="line">					next(<span class="keyword">ls</span>);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'-'</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> EOF:&#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> TK_EOS;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'+'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'+'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'*'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'*'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'/'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'/'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'~'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="comment">// not equal</span></span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'='</span>) &#123;</span><br><span class="line">				next(<span class="keyword">ls</span>);</span><br><span class="line">				<span class="keyword">return</span> TK_NOTEQUAL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'~'</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'%'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> TK_MOD;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'.'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">if</span> (isdigit(<span class="keyword">ls</span>-&gt;current)) &#123;</span><br><span class="line">				<span class="keyword">return</span> str2number(<span class="keyword">ls</span>, true);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'.'</span>) &#123;</span><br><span class="line">				next(<span class="keyword">ls</span>);</span><br><span class="line">				<span class="comment">// the '...' means vararg </span></span><br><span class="line">				<span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'.'</span>) &#123;</span><br><span class="line">					next(<span class="keyword">ls</span>);</span><br><span class="line">					<span class="keyword">return</span> TK_VARARG;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="comment">// the '..' means concat</span></span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="keyword">return</span> TK_CONCAT;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'.'</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'"'</span>: <span class="keyword">case</span> <span class="string">'\''</span>: &#123; <span class="comment">// process string</span></span><br><span class="line">			<span class="keyword">return</span> read_string(<span class="keyword">ls</span>, <span class="keyword">ls</span>-&gt;current, &amp;<span class="keyword">ls</span>-&gt;t.seminfo);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'('</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'('</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">')'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">')'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'['</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'['</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">']'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">']'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'&#123;'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'&#123;'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'&#125;'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'&#125;'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'&gt;'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'='</span>) &#123;</span><br><span class="line">				next(<span class="keyword">ls</span>);</span><br><span class="line">				<span class="keyword">return</span> TK_GREATEREQUAL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'&gt;'</span>) &#123;</span><br><span class="line">				next(<span class="keyword">ls</span>);</span><br><span class="line">				<span class="keyword">return</span> TK_SHR;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'&gt;'</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'&lt;'</span>:&#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'='</span>) &#123;</span><br><span class="line">				next(<span class="keyword">ls</span>);</span><br><span class="line">				<span class="keyword">return</span> TK_LESSEQUAL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'&lt;'</span>)</span><br><span class="line">			&#123;</span><br><span class="line">				next(<span class="keyword">ls</span>);</span><br><span class="line">				<span class="keyword">return</span> TK_SHL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'&lt;'</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'='</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'='</span>) &#123;</span><br><span class="line">				next(<span class="keyword">ls</span>);</span><br><span class="line">				<span class="keyword">return</span> TK_EQUAL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'='</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">','</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">','</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'0'</span>: <span class="keyword">case</span> <span class="string">'1'</span>: <span class="keyword">case</span> <span class="string">'2'</span>: <span class="keyword">case</span> <span class="string">'3'</span>: <span class="keyword">case</span> <span class="string">'4'</span>:</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'5'</span>: <span class="keyword">case</span> <span class="string">'6'</span>: <span class="keyword">case</span> <span class="string">'7'</span>: <span class="keyword">case</span> <span class="string">'8'</span>: <span class="keyword">case</span> <span class="string">'9'</span>: &#123;</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'0'</span>) &#123;</span><br><span class="line">				save_and_next(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>, <span class="keyword">ls</span>-&gt;current);</span><br><span class="line">				<span class="keyword">if</span> (<span class="keyword">ls</span>-&gt;current == <span class="string">'x'</span> || <span class="keyword">ls</span>-&gt;current == <span class="string">'X'</span>) &#123;</span><br><span class="line">					save_and_next(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>, <span class="keyword">ls</span>-&gt;current);</span><br><span class="line">					<span class="keyword">return</span> str2hex(<span class="keyword">ls</span>);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="keyword">return</span> str2number(<span class="keyword">ls</span>, false);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">return</span> str2number(<span class="keyword">ls</span>, false);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">default</span>: &#123;</span><br><span class="line">			<span class="comment">// TK_NAME or reserved name</span></span><br><span class="line">			<span class="keyword">if</span> (isalpha(<span class="keyword">ls</span>-&gt;current) || <span class="keyword">ls</span>-&gt;current == <span class="string">'_'</span>) &#123;</span><br><span class="line">				<span class="keyword">while</span> (isalpha(<span class="keyword">ls</span>-&gt;current) || <span class="keyword">ls</span>-&gt;current == <span class="string">'_'</span> || isdigit(<span class="keyword">ls</span>-&gt;current)) &#123;</span><br><span class="line">					save_and_next(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>, <span class="keyword">ls</span>-&gt;current);</span><br><span class="line">				&#125;</span><br><span class="line">				save(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>, <span class="string">'\0'</span>);</span><br><span class="line">				</span><br><span class="line">				TString* s = luaS_newlstr(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>-&gt;buff-&gt;buffer, strlen(<span class="keyword">ls</span>-&gt;buff-&gt;buffer));</span><br><span class="line">				<span class="keyword">if</span> (s-&gt;extra &gt; <span class="number">0</span>) &#123;</span><br><span class="line">					<span class="keyword">return</span> s-&gt;extra;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="keyword">ls</span>-&gt;t.seminfo.s = s;</span><br><span class="line">					<span class="keyword">return</span> TK_NAME;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123; <span class="comment">// single char</span></span><br><span class="line">				<span class="keyword">int</span> c = <span class="keyword">ls</span>-&gt;current;</span><br><span class="line">				next(<span class="keyword">ls</span>);</span><br><span class="line">				<span class="keyword">return</span> c;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> TK_EOS;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从上面的代码中，我们不难看出，对于单个ASCII字符就能表示的token，llex函数基本上是直接返回的，对于那些需要多个字符组成的token，llex函数，则是返回在枚举类型里定义的枚举值，比如TK_LESSEQUAL，TK_GREATEREQ等。这些都比较简单，不过需要进行特殊处理的类型，则是TK_INT，TK_FLOAT，TK_STRING和TK_NAME。<br>    如果token首字母是0~9，那么判定分支则走入识别数值的分支之中，紧接着判断第二个字母是否是x或者是X，如果是，那么判定它是十六进制的数值，我们通过str2hex函数来进行处理：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line"><span class="number">1</span>  #<span class="function"><span class="title">define</span> save_and_next(L, ls, c) save(L, ls, c); ls-&gt;</span>current = next(ls)</span><br><span class="line"><span class="number">2</span> #<span class="function"><span class="title">define</span> currIsNewLine(ls) (ls-&gt;</span><span class="function"><span class="title">current</span> == '\n' || ls-&gt;</span>current == <span class="string">'\r'</span>)</span><br><span class="line"></span><br><span class="line"><span class="number">3</span>  static int str2hex(LexState* ls) &#123;</span><br><span class="line"><span class="number">4</span>  	int num_part_count = <span class="number">0</span>;</span><br><span class="line"><span class="number">5</span>  	<span class="function"><span class="title">while</span> (is_hex_digit(ls-&gt;</span>current)) &#123;</span><br><span class="line"><span class="number">6</span>  		<span class="function"><span class="title">save_and_next</span>(ls-&gt;</span>L, <span class="function"><span class="title">ls</span>, ls-&gt;</span>current);</span><br><span class="line"><span class="number">7</span>  		num_part_count++;</span><br><span class="line"><span class="number">8</span>  	&#125;</span><br><span class="line"><span class="number">9</span>  	<span class="function"><span class="title">save</span>(ls-&gt;</span>L, ls, <span class="string">'\0'</span>);</span><br><span class="line"><span class="number">10</span></span><br><span class="line"><span class="number">11</span>  <span class="keyword">if</span> (num_part_count &lt;= <span class="number">0</span>) &#123;</span><br><span class="line"><span class="number">12</span>		LUA_ERROR(<span class="function"><span class="title">ls</span>-&gt;</span>L, <span class="string">"malformed number near '0x'"</span>);</span><br><span class="line"><span class="number">13</span>		<span class="function"><span class="title">luaD_throw</span>(ls-&gt;</span>L, LUA_ERRLEXER);</span><br><span class="line"><span class="number">14</span>	&#125;</span><br><span class="line"><span class="number">15</span></span><br><span class="line"><span class="number">16</span>	<span class="function"><span class="title">ls</span>-&gt;</span><span class="function"><span class="title">t</span>.seminfo.i = strtoll(ls-&gt;</span><span class="function"><span class="title">buff</span>-&gt;</span>buffer, NULL, <span class="number">0</span>);</span><br><span class="line"><span class="number">17</span>	return TK_INT;</span><br><span class="line"><span class="number">18</span> &#125;</span><br></pre></td></tr></table></figure>
<p>上述代码的save操作，则是将ls-&gt;current所存储的字符，存入到LexState结构MBuffer类型的buff缓存中。词法分析器会不断调next(内部调用了zget宏)函数，并判断它是不是16进制的字符，如果是，则存储到buff缓存中，直到第一个不是16进制字符为止，我们则需将buff缓存里存储的字符，通过strtoll函数，转化为一个整型变量，并存储在ls-&gt;t这个表示当前token的变量之中，同时它的类型被设置为TK_INT。<br>    如果token首字母是0~9，并且紧随其后的第二个字符不是’x‘或者’X‘时，那么我们则进入到识别整型数值或者浮点型数值的逻辑之中了，此时调用str2number函数来进行操作：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line">static int str2number(LexState* ls, bool has_dot) &#123;</span><br><span class="line">	<span class="keyword">if</span> (has_dot) &#123;</span><br><span class="line">		<span class="function"><span class="title">save</span>(ls-&gt;</span>L, ls, <span class="string">'0'</span>);</span><br><span class="line">		<span class="function"><span class="title">save</span>(ls-&gt;</span>L, ls, <span class="string">'.'</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="title">while</span> (isdigit(ls-&gt;</span><span class="function"><span class="title">current</span>) || ls-&gt;</span>current == <span class="string">'.'</span>) &#123;</span><br><span class="line">		<span class="function"><span class="title">if</span> (ls-&gt;</span>current == <span class="string">'.'</span>) &#123;</span><br><span class="line">			<span class="keyword">if</span> (has_dot) &#123;</span><br><span class="line">				LUA_ERROR(<span class="function"><span class="title">ls</span>-&gt;</span>L, <span class="string">"unknow number"</span>);</span><br><span class="line">				<span class="function"><span class="title">luaD_throw</span>(ls-&gt;</span>L, LUA_ERRLEXER);</span><br><span class="line">			&#125;</span><br><span class="line">			has_dot = <span class="literal">true</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="function"><span class="title">save_and_next</span>(ls-&gt;</span>L, <span class="function"><span class="title">ls</span>, ls-&gt;</span>current);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="title">save</span>(ls-&gt;</span>L, ls, <span class="string">'\0'</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (has_dot) &#123;</span><br><span class="line">		<span class="function"><span class="title">ls</span>-&gt;</span><span class="function"><span class="title">t</span>.seminfo.r = atof(ls-&gt;</span><span class="function"><span class="title">buff</span>-&gt;</span>buffer);</span><br><span class="line">		return TK_FLOAT;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> &#123;</span><br><span class="line">		<span class="function"><span class="title">ls</span>-&gt;</span><span class="function"><span class="title">t</span>.seminfo.i = atoll(ls-&gt;</span><span class="function"><span class="title">buff</span>-&gt;</span>buffer);</span><br><span class="line">		return TK_INT;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里同样会不断将字符存储到MBuffer类型的buff变量中，直至有一个字符不是0~9的字符或者’.‘为止，而后会进入到讲buff中的字符串转为数值的操作。函数会判断buff中是否包含’.‘，如果有且只有1个，那么转化为数值的操作则是将字符串转化为浮点型数据，如果没有，则进入到将字符串转化为整型数据的操作。<br>    识别字符串则简单得多，如果首字母是’\‘‘或者’\“‘，那么则进入到字符串识别流程之中，这些操作则由read_string函数来执行：</p>
<figure class="highlight mel"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line">static <span class="keyword">int</span> read_string(LexState* <span class="keyword">ls</span>, <span class="keyword">int</span> delimiter, Seminfo* seminfo) &#123;</span><br><span class="line">	next(<span class="keyword">ls</span>);</span><br><span class="line">	<span class="keyword">while</span> (<span class="keyword">ls</span>-&gt;current != delimiter) &#123;</span><br><span class="line">		<span class="keyword">int</span> c = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">switch</span> (<span class="keyword">ls</span>-&gt;current)</span><br><span class="line">		&#123;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'\n'</span>: <span class="keyword">case</span> <span class="string">'\r'</span>: <span class="keyword">case</span> EOF: &#123;</span><br><span class="line">			LUA_ERROR(<span class="keyword">ls</span>-&gt;L, <span class="string">"uncomplete string"</span>);</span><br><span class="line">			luaD_throw(<span class="keyword">ls</span>-&gt;L, LUA_ERRLEXER);</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'\\'</span>: &#123;</span><br><span class="line">			next(<span class="keyword">ls</span>);</span><br><span class="line">			<span class="keyword">switch</span> (<span class="keyword">ls</span>-&gt;current)</span><br><span class="line">			&#123;</span><br><span class="line">			<span class="keyword">case</span> <span class="string">'t'</span>:&#123; c = <span class="string">'\t'</span>; goto save_escape_sequence; &#125;</span><br><span class="line">			<span class="keyword">case</span> <span class="string">'v'</span>:&#123; c = <span class="string">'\v'</span>; goto save_escape_sequence; &#125;</span><br><span class="line">			<span class="keyword">case</span> <span class="string">'a'</span>:&#123; c = <span class="string">'\a'</span>; goto save_escape_sequence; &#125;</span><br><span class="line">			<span class="keyword">case</span> <span class="string">'b'</span>:&#123; c = <span class="string">'\b'</span>; goto save_escape_sequence; &#125;</span><br><span class="line">			<span class="keyword">case</span> <span class="string">'f'</span>:&#123; c = <span class="string">'\f'</span>; goto save_escape_sequence; &#125;</span><br><span class="line">			<span class="keyword">case</span> <span class="string">'n'</span>:&#123; c = <span class="string">'\n'</span>; goto save_escape_sequence; &#125;</span><br><span class="line">			<span class="keyword">case</span> <span class="string">'r'</span>: &#123;</span><br><span class="line">				c = <span class="string">'\r'</span>;</span><br><span class="line">			save_escape_sequence:</span><br><span class="line">				save_and_next(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>, c);</span><br><span class="line">			&#125; <span class="keyword">break</span>;</span><br><span class="line">			<span class="keyword">default</span>: &#123;</span><br><span class="line">				save(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>, <span class="string">'\\'</span>);</span><br><span class="line">				save_and_next(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>, <span class="keyword">ls</span>-&gt;current);</span><br><span class="line">			&#125; <span class="keyword">break</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">default</span>: &#123;</span><br><span class="line">			save_and_next(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>, <span class="keyword">ls</span>-&gt;current);</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	save(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>, <span class="string">'\0'</span>);</span><br><span class="line">	next(<span class="keyword">ls</span>);</span><br><span class="line"></span><br><span class="line">	seminfo-&gt;s = luaS_newliteral(<span class="keyword">ls</span>-&gt;L, <span class="keyword">ls</span>-&gt;buff-&gt;buffer);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> TK_STRING;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>整个逻辑也很简单，只要没有遇到delimiter字符（’\‘‘或者’\“‘），除了一些转义字符会做特殊处理（两个字符合成一个字符），其他的字符都会直接存到MBuffer类型的buff数组中，直至遇到delimiter字符，此时会根据buff中的字符串，去生成一个TString类型的字符串，并存到token的seminfo变量中，这里需要注意的是，delimiter字符本身不存入buff中。<br>    识别标识符(identify)也是简单的多，其开头必须是alphabet字符，或者是’_‘，然后接下来的字符，只要是alphabet、下划线或者数字的其中一种，它都会被存储到MBuffer类型的buff变量中，直至条件不成立，此时会将buff传入luaS_newlstr函数中去生成一个TString类型的字符串，如果字符串是个保留字，那么返回extra字段的值，这表示它的token类型的值，由于保留字是特定的，因此我们只需要extra所代表的值（token类型的枚举值）即可。如果字符串不是保留字，那么新生成的TString字符串则会被保存到seminfo变量中，并且token类型为TK_NAME。</p>
<h1 id="一个测试用例"><a href="#一个测试用例" class="headerlink" title="一个测试用例"></a>一个测试用例</h1><p>​    在文章的最后，我通过一个测试用例来展现词法分析器的分析结果，对part06.lua脚本中的代码，进行解析，获得的打印，则在下方展示。测试代码在p6_test.c中。</p>
<figure class="highlight maxima"><table><tr><td class="code"><pre><span class="line">-- part06.lua</span><br><span class="line"><span class="built_in">local</span> function print_test()</span><br><span class="line">	<span class="built_in">local</span> str = <span class="string">"hello world"</span></span><br><span class="line">	<span class="built_in">print</span>(<span class="string">"hello world"</span>)</span><br><span class="line">end</span><br><span class="line"></span><br><span class="line">print_test()</span><br><span class="line"></span><br><span class="line">	 <span class="built_in">local</span> number = <span class="number">0.123</span></span><br><span class="line">	 <span class="built_in">local</span> number2 = .<span class="number">456</span></span><br><span class="line"><span class="built_in">local</span> tbl = &#123;&#125;</span><br><span class="line">tbl[<span class="string">"key"</span>] = <span class="string">"value"</span> .. <span class="string">"value2"</span></span><br><span class="line"></span><br><span class="line">function print_r(...)</span><br><span class="line">	<span class="built_in">return</span> ...</span><br><span class="line">end</span><br><span class="line"></span><br><span class="line">tbl.<span class="built_in">key</span></span><br><span class="line"></span><br><span class="line">-- This <span class="built_in">is</span> comment</span><br><span class="line">tbl.<span class="built_in">sum</span> = <span class="number">100</span> + <span class="number">200.0</span> - <span class="number">10</span> * <span class="number">12</span> / <span class="number">13</span> <span class="symbol">%</span> (<span class="number">1</span>+<span class="number">2</span>)</span><br><span class="line"><span class="keyword">if</span> tbl.<span class="built_in">sum</span> ~= <span class="number">100</span> <span class="keyword">then</span></span><br><span class="line">	tbl.<span class="built_in">sum</span> = tbl.<span class="built_in">sum</span> &lt;&lt; <span class="number">2</span></span><br><span class="line"><span class="keyword">elseif</span> tbl.<span class="built_in">sum</span> == <span class="number">200</span> <span class="keyword">then</span></span><br><span class="line">	tbl.<span class="built_in">sum</span> = tbl.<span class="built_in">sum</span> &gt;&gt; <span class="number">2</span></span><br><span class="line"><span class="keyword">elseif</span> tbl.<span class="built_in">sum</span> &gt; <span class="number">1</span> <span class="keyword">then</span> </span><br><span class="line"><span class="keyword">elseif</span> tbl.<span class="built_in">sum</span> &lt; <span class="number">2</span> <span class="keyword">then</span></span><br><span class="line"><span class="keyword">elseif</span> tbl.<span class="built_in">sum</span> &gt;= <span class="number">3</span> <span class="keyword">then</span> </span><br><span class="line"><span class="keyword">elseif</span> tbl.<span class="built_in">sum</span> &lt;= <span class="number">4</span> <span class="keyword">then</span> </span><br><span class="line">	tbl.<span class="built_in">sum</span> = nil</span><br><span class="line">end</span><br><span class="line"></span><br><span class="line">tbl.<span class="literal">true</span> = <span class="literal">true</span></span><br><span class="line">tbl.<span class="literal">false</span> = <span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">local</span> a, b = <span class="number">11</span>, <span class="number">22</span></span><br></pre></td></tr></table></figure>
<p>保留字会在开头加上”RESERVED:“的开头，而单个ASCII字符就能展示的token，则是直接打印，其他的会打印枚举定义。</p>
<figure class="highlight excel"><table><tr><td class="code"><pre><span class="line">?</span><br><span class="line">REVERS<span class="symbol">ED:</span> local</span><br><span class="line">REVERS<span class="symbol">ED:</span> function</span><br><span class="line">TK_NAME print_test</span><br><span class="line">(</span><br><span class="line">)</span><br><span class="line">REVERS<span class="symbol">ED:</span> local</span><br><span class="line">TK_NAME str</span><br><span class="line">=</span><br><span class="line">TK_STRING hello world</span><br><span class="line">TK_NAME print</span><br><span class="line">(</span><br><span class="line">TK_STRING hello world</span><br><span class="line">)</span><br><span class="line">REVERS<span class="symbol">ED:</span> end</span><br><span class="line">TK_NAME print_test</span><br><span class="line">(</span><br><span class="line">)</span><br><span class="line">REVERS<span class="symbol">ED:</span> local</span><br><span class="line">TK_NAME number</span><br><span class="line">=</span><br><span class="line">TK_FLOAT <span class="number">0.123000</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> local</span><br><span class="line">TK_NAME number2</span><br><span class="line">=</span><br><span class="line">TK_FLOAT <span class="number">0.456000</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> local</span><br><span class="line">TK_NAME tbl</span><br><span class="line">=</span><br><span class="line">&#123;</span><br><span class="line">&#125;</span><br><span class="line">TK_NAME tbl</span><br><span class="line">[</span><br><span class="line">TK_STRING key</span><br><span class="line">]</span><br><span class="line">=</span><br><span class="line">TK_STRING <span class="built_in">value</span></span><br><span class="line">TK_CONCAT ..</span><br><span class="line">TK_STRING value2</span><br><span class="line">REVERS<span class="symbol">ED:</span> function</span><br><span class="line">TK_NAME print_r</span><br><span class="line">(</span><br><span class="line">TK_VARARG ...</span><br><span class="line">)</span><br><span class="line">TK_NAME return</span><br><span class="line">TK_VARARG ...</span><br><span class="line">REVERS<span class="symbol">ED:</span> end</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME key</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">=</span><br><span class="line">TK_INT <span class="number">100</span></span><br><span class="line">+</span><br><span class="line">TK_FLOAT <span class="number">200.000000</span></span><br><span class="line">-</span><br><span class="line">TK_INT <span class="number">10</span></span><br><span class="line">*</span><br><span class="line">TK_INT <span class="number">12</span></span><br><span class="line">/</span><br><span class="line">TK_INT <span class="number">13</span></span><br><span class="line">TK_MOD %</span><br><span class="line">(</span><br><span class="line">TK_INT <span class="number">1</span></span><br><span class="line">+</span><br><span class="line">TK_INT <span class="number">2</span></span><br><span class="line">)</span><br><span class="line">REVERS<span class="symbol">ED:</span> <span class="built_in">if</span></span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">TK_NOEQUAL ~=</span><br><span class="line">TK_INT <span class="number">100</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> then</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">=</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">TK_SHL &lt;&lt;</span><br><span class="line">TK_INT <span class="number">2</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> elseif</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">TK_EQUAL ==</span><br><span class="line">TK_INT <span class="number">200</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> then</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">=</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">TK_SHR &gt;&gt;</span><br><span class="line">TK_INT <span class="number">2</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> elseif</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">&gt;</span><br><span class="line">TK_INT <span class="number">1</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> then</span><br><span class="line">REVERS<span class="symbol">ED:</span> elseif</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">&lt;</span><br><span class="line">TK_INT <span class="number">2</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> then</span><br><span class="line">REVERS<span class="symbol">ED:</span> elseif</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">TK_GREATEREQUAL &gt;=</span><br><span class="line">TK_INT <span class="number">3</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> then</span><br><span class="line">REVERS<span class="symbol">ED:</span> elseif</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">TK_LESSEQUAL &lt;=</span><br><span class="line">TK_INT <span class="number">4</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> then</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">TK_NAME <span class="built_in">sum</span></span><br><span class="line">=</span><br><span class="line">REVERS<span class="symbol">ED:</span> nil</span><br><span class="line">REVERS<span class="symbol">ED:</span> end</span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">REVERS<span class="symbol">ED:</span> <span class="built_in">true</span></span><br><span class="line">=</span><br><span class="line">REVERS<span class="symbol">ED:</span> <span class="built_in">true</span></span><br><span class="line">TK_NAME tbl</span><br><span class="line">.</span><br><span class="line">REVERS<span class="symbol">ED:</span> <span class="built_in">false</span></span><br><span class="line">=</span><br><span class="line">REVERS<span class="symbol">ED:</span> <span class="built_in">false</span></span><br><span class="line">REVERS<span class="symbol">ED:</span> local</span><br><span class="line">TK_NAME a</span><br><span class="line">,</span><br><span class="line">TK_NAME b</span><br><span class="line">=</span><br><span class="line">TK_INT <span class="number">11</span></span><br><span class="line">,</span><br><span class="line">TK_INT <span class="number">22</span></span><br><span class="line">total linenumber = <span class="number">35</span>请按任意键继续. . .</span><br></pre></td></tr></table></figure>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>​    本章节，我用一些篇幅讨论了词法分析器的设计与实现，实际上part5已经有对词法分析器进行一些必要的概述了，我这里是对part5进行一些补充，目的是为了能够让读者对lua的词法分析器有更深刻的认识。下一篇开始，我将开始论述lua语法分析器的设计与实现。</p>
<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><p>[1] <a href="https://en.wikipedia.org/wiki/Lexical_analysis" rel="external nofollow noopener noreferrer" target="_blank">Lexical analysis</a></p>
<ul>
<li><a href="https://github.com/halogenica/beautifulhugo" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/halogenica/beautifulhugo</a>)</li>
</ul>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>Golang 中的垃圾回收（一）</title>
    <url>/go/Golang%20%E4%B8%AD%E7%9A%84%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6/Golang%20%E4%B8%AD%E7%9A%84%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6(%E4%B8%80%EF%BC%89/</url>
    <content><![CDATA[<p><a href="https://juejin.cn/post/6844903888177332237" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a><br>垃圾回收器负责追踪堆内存的分配，释放掉不需要的空间，追踪那些还在使用的分配空间。不同编程语言对这个机制的实现都很复杂，但是开发人员开发软件时候并不需要了解垃圾回收太细节的东西就能进行构建。另外，不同发布版本编程语言的VM和runtime也总是在改变和进化。对于应用开发人员来说，重要的是保持一个良好的work模型，了解编程语言里垃圾回收器的行为并且它们是怎么样支持这种行为的。<br>对于go 1.12版本来说，go语言使用了非分代，并发的三色标记和清扫的回收器。如果想了解如何进行标记和清扫的工作，请参考这篇文章。golang的垃圾回收器的实现每个版本都在更新和进化。因此一旦下个版本发布，讲任何细节的实现都不再准确。<br>总而言之，这篇文章不会去讲实际的实现细节。我会为你分享回收器的一些行为并且去解释怎样面对这些行为，不考虑实现细节以及未来的改变。这将会使你成为一个更好的golang开发者<br><strong>堆不是一个容器</strong><br>我不会把堆看做是一个可以存储或者是释放值的容器。理解这件事情很重要，内存里并没有明确定义了“堆”的一个分界线。任何应用程序预留的内存空间，在堆内存分配上是可用的。给定任何堆内存分配空间，它实际在虚拟内存还是物理内存上的存储位置和我们的模型并没有关联。理解这件事情会帮助你更好的理解垃圾回收模型的工作方式。<br><strong>回收器行为</strong><br>当回收开始，回收器会完成三个阶段的工作。这其中两个阶段会产生Stop The World(STW) 延迟，并且另一个阶段也会产生延迟，并且会导致降低应用程序的吞吐量。这三个阶段是：</p>
<ul>
<li>Mark Setup - STW</li>
<li>Marking - Concurrent</li>
<li>Mark Termination -STW</li>
</ul>
<p>下面看各个阶段的详细说明。<br><strong>Mark Setup -STW</strong><br>当回收开始，首先要做的肯定是开启写屏障(Write Barrier)。写屏障的目的是在collector和应用程序goroutines并发时候，允许回收器去维持数据在堆中的完整性。<br>为了开启写屏障，每个运行中的应用程序goroutine必须要停下来。这个活动通常非常快，平均在10~30微妙之间。前提是在你的应用程序goroutines行为合理的情况下。<br><strong>注意：为了更好的理解下面的调度图解，最好先看过之前写的<a href="https://juejin.im/post/6844903846284787719" rel="external nofollow noopener noreferrer" target="_blank">golang调度文章</a></strong><br>图1.1<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d3e1ecf8363?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt></p>
<p>图1给出了4个应用程序goroutines，在开始垃圾回收之前它们都在运行中。为了进行回收，4个goroutines中每一个都必须被停下来，这么做的唯一方式就是让回收器去检查并等待goroutine去做<strong>方法调用</strong>。方法调用确保了goroutines在安全的点停下来。如果其中一个goroutine没有进行方法调用但是其它的做了方法调用，会发生什么？<br>图1.2<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d3fa74cf3af?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt></p>
<p>图1.2给出了一个问题的实际案例。如果P4的goroutine不停下来的话，垃圾回收就无法启动。但是P4正在执行一个<a href="https://github.com/golang/go/issues/10958" rel="external nofollow noopener noreferrer" target="_blank">tight loop</a>去做一些math处理，这导致回收根本无法开始。<br>L1：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">01</span> <span class="function"><span class="keyword">func</span> <span class="title">add</span><span class="params">(numbers []<span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="number">02</span>     <span class="keyword">var</span> v <span class="keyword">int</span></span><br><span class="line"><span class="number">03</span>     <span class="keyword">for</span> _, n := <span class="keyword">range</span> numbers &#123;</span><br><span class="line"><span class="number">04</span>         v += n</span><br><span class="line"><span class="number">05</span>     &#125;</span><br><span class="line"><span class="number">06</span>     <span class="keyword">return</span> v</span><br><span class="line"><span class="number">07</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>L1给出了P4 goroutine正在执行的代码。取决于slice的大小，goroutine可能会执行很长很长的时间，导致了根本没有机会停下来。这种代码会阻止垃圾回收开始。更糟糕的是，其他的P无法为其他goroutine服务，因为collector处于等待状态。所以goroutine在合理的实际范围内进行方法调用是至关重要的。<br><strong>注意：这部分是golang团队将在1.14版本中要改进的内容，通过加入调度器的<a href="https://github.com/golang/go/issues/24543" rel="external nofollow noopener noreferrer" target="_blank">抢占式调度技术</a></strong><br><strong>Marking -Concurrent</strong><br>一旦写屏障开启，回收器就会开始进入都标记阶段。回收器第一件做的事情就是拿走25%的可用CPU给自己使用。collector使用Goroutines去进行回收工作，也就是它会从应用程序抢过来对应数量的P和M。这意味着，4个线程的go程序里，会有一个P被拿去处理回收工作。<br>图1.3<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d41d27cf087?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.3给出了collector是怎样拿走P1的在进行回收工作的时候。现在回收器可以开始Marking过程了。标记阶段就是标记处理堆内存中的in-use的值。它会去检查栈中所有存在的goroutines，去找到指向堆内存的根指针。之后回收器必须从根指针开始，遍历堆内存的树图。当P1上进行处理Marking工作，应用程序可以在P2、P3和P4上继续并发执行。这意味着回收器减少了当前CPU容量的25%。<br>我希望事情到此就结束了，但是并不是。如果P1上GC的goroutine在in-use的堆内存达到上限时候没完成Marking会怎么样？如果其他3个应用程序的goroutine导致了collector无法按时完成工作会怎么样？如果发生这种情况，新的内存分配就必须慢下来，尤其是在对应的goroutine上。<br>如果回收器确定它必须要减慢内存分配速度，它就会招募应用程序的goroutines去协助（Assist）进行标记工作。这叫做Mark Assist。任何应用程序goroutine被置入Mark Assist的时间和它将要在堆内存中加的数据量是成比例的。Mark Assist的一个正面功能就是它帮助提高了回收速度。<br>图1.4<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d437122ffae?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.4展示了，之前P3上应用程序运行的goroutine，现在正在进行Mark Assist来帮助进行回收工作。希望其他goroutines不要参与其中。应用程序有分配内存压力的时候会看到大部分正在运行的goroutines在垃圾回收的时候去处理小量的Mark Assist工作。<br>Mark Termination -STW<br>一旦标记工作完成，下一个过程就是Mark Termination。这个时候写屏障关闭，各种清理工作会进行，并且计算出下一次的回收目标。在标记过程中那些发现自己处理tight loop的goroutines也会导致Mark Termination STW的延时增加。<br>图1.5<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d451aecffc6?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.5展示了，Mark Termination阶段完成，所有Goroutines都会停止。这个活动通常会在60~90微妙内完成。这个阶段完成可以没有STW，但是通过STW，会使得代码更加简单，并且增加的代码复杂度并不值得这点小增益。<br>一旦回收完成了，每个P可以再次被应用程序goroutines去使用，程序又回到了全力运行的状态。<br>图1.6<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d464c32f21d?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.6展示了回收完成后，全部可用的P现在正在处理应用程序的工作。<br>Sweeping - Concurrent<br>在回收完成之后，会有另外一个活动，叫做清扫（Sweeping）。Sweeping就是清理内存中有值但是没有被标记为in-use的堆内存。这个活动发生在当应用程序goroutines尝试去在堆中分配新的值的时候。Sweeping延迟增加到了堆内存分配的开销中，并且和任何垃圾回收的延迟都没有关联。<br>下面是在我的机器上进行trace的样本，我的机器上有12个hardware thread去执行goroutines。<br>图1.7<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d483c6140de?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.7展示了trace的部分快照。你可以看到在回收中（注意上面蓝色GC行），12个P中的3个被拿去处理GC。你可以看到goroutine 2450，1978和2696在这时间正在进行Mark Assist而不是它自己的程序work。在回收的最后，只有一个P去处理GC并且最终进行STW(Mark Termination)工作.<br>在回收完成后，程序又回到了全力运行的状态。你可以看到在这些goroutine下面的许多玫瑰色的竖线。<br>图1.8<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d4a89ac193a?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.8展示了那些玫瑰色的线代表了goroutine进行Sweeping工作而不是它自己的程序工作的时候。这些时刻goroutine会尝试在堆中分配新的值。<br>图1.9<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d4be7b8df1c?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.9展示了一个goroutine在Sweeping活动最后的追踪数据。runtime.mallocgc的调用会去在堆中分配新的值。runtime.(*mcache).nextFree调用会导致Sweeping。一旦堆中不再有分配的内存需要回收，nextFree就不会再看见。<br>上面描述的回收行为仅仅发生在当回收已经启动并正在处理的过程中。在确定什么时候开始回收中，GC配置选项扮演了重要的角色。<br><strong>GC percentage</strong><br>runtime中有一个配置选项叫做 GC Percentage，默认值是100。这个值代表了下一次回收开始之前，有多少新的堆内存可以分配。GC Percentage设置为100意味着，基于回收完成之后被标记为生存的堆内存数量，下一次回收的开始必须在有100%以上的新内存分配到堆内存时启动。<br>作为例子，想象回收完成了并标记了2MB的in-use堆内存。<br>注意：图表中的堆内存不代表实际情况。go中的堆内存通常都是凌乱的碎片化的，你不会有图表中那种清晰的区分。这些图表提供了一个方便的可视化的堆内存模型来方便理解。<br>图1.10<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d4d71289068?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.10展示了，在上一次的回收完成后，有2MB的in-use堆内存。由于GC Percentage设置了100%，下一次回收启动需要在堆内存增加了2MB或者更多内存时候或者之前启动。<br>图1.11<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d4e88deedc1?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.11展示了2MB或者更多内存处于in-use。这会触发回收。一种方式去看到这些行为的方法，就是为每次GC生成一个GC trace。<br>L2<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">GODEBUG=gctrace=1 ./app</span><br><span class="line"></span><br><span class="line">gc 1405 @6.068s 11%: 0.058+1.2+0.083 ms clock, 0.70+2.5/1.5/0+0.99 ms cpu, 7-&gt;11-&gt;6 MB, 10 MB goal, 12 P</span><br><span class="line"></span><br><span class="line">gc 1406 @6.070s 11%: 0.051+1.8+0.076 ms clock, 0.61+2.0/2.5/0+0.91 ms cpu, 8-&gt;11-&gt;6 MB, 13 MB goal, 12 P</span><br><span class="line"></span><br><span class="line">gc 1407 @6.073s 11%: 0.052+1.8+0.20 ms clock, 0.62+1.5/2.2/0+2.4 ms cpu, 8-&gt;14-&gt;8 MB, 13 MB goal, 12 P</span><br></pre></td></tr></table></figure></p>
<p>L2展示了如何使用GODEBUG变量去生成GC trace。下面的L3展示了程序生成的gc traces。<br>L3<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gc 1405 @6.068s 11%: 0.058+1.2+0.083 ms clock, 0.70+2.5/1.5/0+0.99 ms cpu, 7-&gt;11-&gt;6 MB, 10 MB goal, 12 P</span><br><span class="line"></span><br><span class="line">// General</span><br><span class="line">gc 1404     : The 1404 GC run since the program started</span><br><span class="line">@6.068s     : Six seconds since the program started</span><br><span class="line">11%         : Eleven percent of the available CPU so far has been spent <span class="keyword">in</span> GC</span><br><span class="line"></span><br><span class="line">// Wall-Clock</span><br><span class="line">0.058ms     : STW        : Mark Start       - Write Barrier on</span><br><span class="line">1.2ms       : Concurrent : Marking</span><br><span class="line">0.083ms     : STW        : Mark Termination - Write Barrier off and clean up</span><br><span class="line"></span><br><span class="line">// CPU Time</span><br><span class="line">0.70ms      : STW        : Mark Start</span><br><span class="line">2.5ms       : Concurrent : Mark - Assist Time (GC performed <span class="keyword">in</span> line with allocation)</span><br><span class="line">1.5ms       : Concurrent : Mark - Background GC time</span><br><span class="line">0ms         : Concurrent : Mark - Idle GC time</span><br><span class="line">0.99ms      : STW        : Mark Term</span><br><span class="line"></span><br><span class="line">// Memory</span><br><span class="line">7MB         : Heap memory <span class="keyword">in</span>-use before the Marking started</span><br><span class="line">11MB        : Heap memory <span class="keyword">in</span>-use after the Marking finished</span><br><span class="line">6MB         : Heap memory marked as live after the Marking finished</span><br><span class="line">10MB        : Collection goal <span class="keyword">for</span> heap memory <span class="keyword">in</span>-use after Marking finished</span><br><span class="line"></span><br><span class="line">// Threads</span><br><span class="line">12P         : Number of logical processors or threads used to run Goroutines</span><br></pre></td></tr></table></figure></p>
<p>L3展示了GC中的实际数值和它的含义。我最后会讲到这些值，但是现在注意1405 GC trace的内存片段。<br>图1.12<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d5188f65a4c?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>L4<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">// Memory</span><br><span class="line">7MB         : Heap memory <span class="keyword">in</span>-use before the Marking started</span><br><span class="line">11MB        : Heap memory <span class="keyword">in</span>-use after the Marking finished</span><br><span class="line">6MB         : Heap memory marked as live after the Marking finished</span><br><span class="line">10MB        : Collection goal <span class="keyword">for</span> heap memory <span class="keyword">in</span>-use after Marking finished</span><br></pre></td></tr></table></figure></p>
<p>复制代码GC trace行给出了如下的信息：Marking Work开始之前堆内存中in-use大小是7MB。当Marking Work完成后，堆内存中in-use的大小是11MB。这意味着回收中额外增加了4MB的内存分配。Marking Work完成之后堆内存中存活空间的大小是6MB。这意味着下次回收开始之前，in-use堆内存可以增加到12MB（100%*生存堆内存大小=6MB）<br>你可以看到回收器超过了它设定的目标1MB，Marking Work完成之后的in-use堆内存是11MB而不是10MB。但是没关系，因为目标是根据当前in-use的堆内存计算得到的，也就是堆内存中标记为生存的空间，当回收进行的时候会有额外随时间计算增加的内存分配。在这个案例里，应用程序做了一些事情，导致在Marking之后，需要比预期更多去使用的堆内存。<br>如果你看下一个GC Trace 行（1406），你开会看到在2ms内事情是如何改变的。<br>图1.13<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d531474711f?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>L5<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gc 1406 @6.070s 11%: 0.051+1.8+0.076 ms clock, 0.61+2.0/2.5/0+0.91 ms cpu, 8-&gt;11-&gt;6 MB, 13 MB goal, 12 P</span><br><span class="line"></span><br><span class="line">// Memory</span><br><span class="line">8MB         : Heap memory <span class="keyword">in</span>-use before the Marking started</span><br><span class="line">11MB        : Heap memory <span class="keyword">in</span>-use after the Marking finished</span><br><span class="line">6MB         : Heap memory marked as live after the Marking finished</span><br><span class="line">13MB        : Collection goal <span class="keyword">for</span> heap memory <span class="keyword">in</span>-use after Marking finished</span><br></pre></td></tr></table></figure></p>
<p>复制代码L5展示了在之前的回收工作开始之后（6.068s vs 6.070s）这个回收工作开始了2ms的状态，尽管in-use的堆内存在允许的12MB中仅仅达到8MB。需要注意到，如果回收器决定最好要早一点开始进行回收的话，它就会那么做。这个案例下，它可能提前开始回收了，因为应用程序的分配压力很大并且collector想要降低在这次回收工作中Mark Assist的延迟。<br>还有两个事情要注意，回收器在它设定的目标内完成了。在Marking 完成之后in-use的堆内存空间是11MB而不是13MB，少了2MB。在Marking完成之后堆内存中标记为存活的空间同样是6MB。<br>另外，你可以获得更多GC的细节通过增加gcpacertrace=1的标记。这会让回收器打印concurrent pacer的内部状态。<br>L6<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">export</span> GODEBUG=gctrace=1,gcpacertrace=1 ./app</span><br><span class="line"></span><br><span class="line">Sample output:</span><br><span class="line">gc 5 @0.071s 0%: 0.018+0.46+0.071 ms clock, 0.14+0/0.38/0.14+0.56 ms cpu, 29-&gt;29-&gt;29 MB, 30 MB goal, 8 P</span><br><span class="line"></span><br><span class="line">pacer: sweep <span class="keyword">done</span> at heap size 29MB; allocated 0MB of spans; swept 3752 pages at +6.183550e-004 pages/byte</span><br><span class="line"></span><br><span class="line">pacer: assist ratio=+1.232155e+000 (scan 1 MB <span class="keyword">in</span> 70-&gt;71 MB) workers=2+0</span><br><span class="line"></span><br><span class="line">pacer: H_m_prev=30488736 h_t=+2.334071e-001 H_T=37605024 h_a=+1.409842e+000 H_a=73473040 h_g=+1.000000e+000 H_g=60977472 u_a=+2.500000e-001 u_g=+2.500000e-001 W_a=308200 goalΔ=+7.665929e-001 actualΔ=+1.176435e+000 u_a/u_g=+1.000000e+000</span><br></pre></td></tr></table></figure></p>
<p>复制代码运行GC trace可以告诉你很多应用程序的健康状态以及回收器的速度。<br><strong>Pacing</strong><br>回收器有一个pacing算法，它会去确定什么时候回收去开始。算法依靠一个反馈循环，回收器会使用这种算法收集应用程序运行时候的信息，以及应用程序给堆造成的压力。压力可以定义为在给定的时间内应用程序在堆上的分配有多快。压力确定了回收器运行的速度。<br>在回收器开始回收之前，它会计算它认为完成回收所需的时间。 一旦回收运行，会造成正在运行的应用程序上的延迟，这将减慢应用程序的工作。 每次回收都会增加应用程序的整体延迟。<br>有一个错误观念就是认为降低回收器的速度是一种提高性能的方式。如果你可以推迟下一次的回收，你就会推迟它产生的延时。<br>但其实提升性能并不在于降低回收器的速度。<br>你可以决定改变GC Percentage的值，设置大于100。这会增加下次回收开始之前可以分配的内存大小。这会降低回收器的速度。但是不要考虑这么做。<br>图1.14<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d5567fa6dd7?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.14展示了改变GC percentage并改变了在下次回收之前可以分配的堆内存。你可以预想到回收器是怎么被降速的，因为它要等待堆内存in-use。<br>尝试直接影响回收速度并不能提升回收器性能。重要的事情是在于在每次回收的之间或者是回收的时候做更多事情，这个你可以通过减少work的堆内存的分配量来进行影响。<br><strong>注意：也可以尽量使用最小的堆来实现所需要的吞吐量。记住，在云环境中，最小化堆内存的使用很重要。</strong><br>图1.15<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d56d5e0fb36?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.15展示了go程序内部的一些统计。蓝色版本的统计展示了没有任何优化的情况下，应用程序处理10k请求情况。绿色版本表明了相同10k请求下，4.48GB的非生产性的内存分配产生而被发现后，从应用程序中移除之后的统计情况(降低堆内存分配压力)。<br>看一下两个版本的平均回收速度（2.08ms vs 1.96ms）。它们几乎差不太多，大概是2ms。不同的地方在于两个版本在每一次回收之间的work的量。应用程序每次回收之间，处理requests次数从3.98次变为<br>7.13次。可以看几乎相同的时间内有79.1%的工作量提升。可以看到，回收工作没有随着分配内存的减少而降速，而是保持了原来速度。成功点在于每次回收之间做了更多的事情。<br>调整回收器的回收速度，推迟延迟代价不是你提升应用程序的性能的方法，它只是减少了回收器需要运行的时间，这反过来会减少造成的延迟成本。回收器产生的延迟代价已经解释过了，但是这里再进行一个简单的总结说明。<br><strong>Collector 延迟代价</strong><br>每次回收工作，会带来两种类型的延迟。第一种是窃取CPU，在回收的时候这种窃取CPU的行为意味着你的应用程序没有以满CPU的状态运行。应用程序goroutines现在和回收器goroutine共享P，或者是进行Mark Assist。<br>图1.16<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d586f74b2f9?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.16表明了只有75%的CPU在进行应用程序工作。因为回收器占用了一个P。<br>图1.17<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d59e43699f8?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.17中，只有一半的CPU在处理应用程序work。因为P3正在进行Mark Assist，P1被collector占用。<br><strong>注意：Marking通常需要4 CPU-millsecondes/MB 的生存堆（举例，为了评估Marking阶段运行多少millseconds，这个值会设置为生存堆MB大小去除以0.25*CPU数目的值）。Marking实际运行大概是1MB/ms，但是只有1/4的CPU去处理。</strong><br>第二种类型的延迟就是在回收中产生的STW。STW就是没有任何goroutine进行工作的情况。整个应用程序本质上是停止状态。<br>图1.18<br><img src="https://user-gold-cdn.xitu.io/2019/7/12/16be4d5b80ff8585?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图1.18展示了，STW时候所有goroutine都停止了。每次回收会发生两次STW。如果你的应用程序处于健康状态，那么回收器会让STW时间保持在100微秒以内。<br><strong>降低GC延迟</strong><br>减少GC延迟的方式是识别哪些是应用程序中不必要的内存分配并移除它们，这会在几个方面帮助提高collector。<br>帮助回收器:</p>
<ul>
<li>维持了最小堆</li>
<li>找到最优的一致速度</li>
<li>每次回收保持在目标（goal）之内</li>
<li>最小化回收的时间，STW和Mark Assist</li>
</ul>
<p>这些事情都会帮助减少回收器产生的延迟，从而增加应用程序的吞吐量和性能。改变回收速度并没有什么用。你可以通过做出正确的工程方面决策，降低堆内存的分配压力来提升性能。<br><strong>理解应用程序正在运行的workload</strong><br>关于性能，还需要清楚你的workload的类型。理解你的workload意味着，确定你使用合理数量的goroutines来处理你的工作。CPU vs IO bound 的workloads是不同的，需要做出不同抉择。相关内容可以参考这里<br><strong>结论</strong><br>如果你花时间去专注于减少内存分配，你会得到性能上的提升。但是你不可能写出0分配的程序来，所以了解和确认productive（对程序有帮助的）的内存分配和not productive（损害性能）的分配是很重要的。之后你就可以信任垃圾回收器帮你维持好内存的健康和稳定，然后让你的程序持续的运行下去。<br>垃圾回收器是一个很好的折衷方式。花一点代价去进行垃圾回收，这样就不需要考虑内存管理的问题。Go 垃圾回收器能够让程序员更加高效和多产，可以让你写出足够快的程序。<br>原文链接：<a href="https://www.ardanlabs.com/blog/2018/12/garbage-collection-in-go-part1-semantics.html" rel="external nofollow noopener noreferrer" target="_blank">www.ardanlabs.com/blog/2018/1…</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go内存详解</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go内存详解</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言goroutine调度器初始化 十二</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/Go%E8%AF%AD%E8%A8%80goroutine%E8%B0%83%E5%BA%A6%E5%99%A8%E5%88%9D%E5%A7%8B%E5%8C%96-%E5%8D%81%E4%BA%8C/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/W9D4Sl-6jYfcpczzdPfByQ" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-05</p>
<h2 id="本文是《Go语言调度器源代码情景分析》系列的第12篇，也是第二章的第2小节。"><a href="#本文是《Go语言调度器源代码情景分析》系列的第12篇，也是第二章的第2小节。" class="headerlink" title="本文是《Go语言调度器源代码情景分析》系列的第12篇，也是第二章的第2小节。"></a>本文是《Go语言调度器源代码情景分析》系列的第12篇，也是第二章的第2小节。</h2><p>本章将以下面这个简单的Hello World程序为例，通过跟踪其从启动到退出这一完整的运行流程来分析Go语言调度器的初始化、goroutine的创建与退出、工作线程的调度循环以及goroutine的切换等重要内容。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(<span class="string">"Hello World!"</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>首先我们从程序启动开始分析调度器的初始化。</p>
<p>在分析程序的启动过程之前，我们首先来看看程序在执行第一条指令之前其栈的初始状态。</p>
<p>任何一个由编译型语言（不管是C，C++，go还是汇编语言）所编写的程序在被操作系统加载起来运行时都会顺序经过如下几个阶段：</p>
<p>从磁盘上把可执行程序读入内存；</p>
<p>创建进程和主线程；</p>
<p>为主线程分配栈空间；</p>
<p>把由用户在命令行输入的参数拷贝到主线程的栈；</p>
<p>把主线程放入操作系统的运行队列等待被调度执起来运行。</p>
<p>在主线程第一次被调度起来执行第一条指令之前，主线程的函数栈如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/01df3eb8b449a52a38f431f36c5fc530.png" alt></p>
<p>了解了程序的初始状态之后，下面我们正式开始。</p>
<p>程序入口</p>
<p>在Linux命令行用 go build 编译hello.go，得到可执行程序hello，然后使用gdb调试，在gdb中我们首先使用 info files 命令找到程序入口（Entry point）地址为0x452270，然后用 b *0x452270 在0x452270地址处下个断点，gdb告诉我们这个入口对应的源代码为 runtime/rt0_linux_amd64.s 文件的第8行。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bobo@ubuntu:~/study/go$ go build hello.go </span><br><span class="line">bobo@ubuntu:~/study/go$ gdb hello</span><br><span class="line">GNU gdb (GDB) 8.0.1</span><br><span class="line">(gdb) info files</span><br><span class="line">Symbols from <span class="string">"/home/bobo/study/go/main"</span>.</span><br><span class="line">Local <span class="built_in">exec</span> file:</span><br><span class="line">`/home/bobo/study/go/main<span class="string">', file type elf64-x86-64.</span></span><br><span class="line"><span class="string">Entry point: 0x452270</span></span><br><span class="line"><span class="string">0x0000000000401000 - 0x0000000000486aac is .text</span></span><br><span class="line"><span class="string">0x0000000000487000 - 0x00000000004d1a73 is .rodata</span></span><br><span class="line"><span class="string">0x00000000004d1c20 - 0x00000000004d27f0 is .typelink</span></span><br><span class="line"><span class="string">0x00000000004d27f0 - 0x00000000004d2838 is .itablink</span></span><br><span class="line"><span class="string">0x00000000004d2838 - 0x00000000004d2838 is .gosymtab</span></span><br><span class="line"><span class="string">0x00000000004d2840 - 0x00000000005426d9 is .gopclntab</span></span><br><span class="line"><span class="string">0x0000000000543000 - 0x000000000054fa9c is .noptrdata</span></span><br><span class="line"><span class="string">0x000000000054faa0 - 0x0000000000556790 is .data</span></span><br><span class="line"><span class="string">0x00000000005567a0 - 0x0000000000571ef0 is .bss</span></span><br><span class="line"><span class="string">0x0000000000571f00 - 0x0000000000574658 is .noptrbss</span></span><br><span class="line"><span class="string">0x0000000000400f9c - 0x0000000000401000 is .note.go.buildid</span></span><br><span class="line"><span class="string">(gdb) b *0x452270</span></span><br><span class="line"><span class="string">Breakpoint 1 at 0x452270: file /usr/local/go/src/runtime/rt0_linux_amd64.s, line 8.</span></span><br></pre></td></tr></table></figure></p>
<p>打开代码编辑器，找到 runtime/rt0_linx_amd64.s 文件，该文件是用go汇编语言编写而成的源代码文件，我们已经在本书的第一部分讨论过其格式。现在看看第8行：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/rt0_linx_amd64.s : <span class="number">8</span></span><br><span class="line"></span><br><span class="line">TEXT _rt0_amd64_linux(SB),NOSPLIT,$<span class="number">-8</span></span><br><span class="line">    JMP_rt0_amd64(SB)</span><br></pre></td></tr></table></figure></p>
<p>上面第一行代码定义了_rt0_amd64_linux这个符号，并不是真正的CPU指令，第二行的JMP指令才是主线程的第一条指令，这条指令简单的跳转到（相当于go语言或c中的goto）_rt0_amd64 这个符号处继续执行，_rt0_amd64 这个符号的定义在runtime/asm_amd64.s 文件中：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">14</span></span><br><span class="line"></span><br><span class="line">TEXT _rt0_amd64(SB),NOSPLIT,$<span class="number">-8</span></span><br><span class="line">    MOVQ0(SP), DI<span class="comment">// argc </span></span><br><span class="line">    LEAQ8(SP), SI <span class="comment">// argv</span></span><br><span class="line">    JMPruntime·rt0_go(SB)</span><br></pre></td></tr></table></figure></p>
<p>前两行指令把操作系统内核传递过来的参数argc和argv数组的地址分别放在DI和SI寄存器中，第三行指令跳转到 rt0_go 去执行。</p>
<p>rt0_go函数完成了go程序启动时的所有初始化工作，因此这个函数比较长，也比较繁杂，但这里我们只关注与调度器相关的一些初始化，下面我们分段来看：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">87</span></span><br><span class="line"></span><br><span class="line">TEXT runtime·rt0_go(SB),NOSPLIT,$<span class="number">0</span></span><br><span class="line">    <span class="comment">// copy arguments forward on an even stack</span></span><br><span class="line">    MOVQDI, AX<span class="comment">// AX = argc</span></span><br><span class="line">    MOVQSI, BX<span class="comment">// BX = argv</span></span><br><span class="line">    SUBQ$(<span class="number">4</span>*<span class="number">8</span>+<span class="number">7</span>), SP<span class="comment">// 2args 2auto</span></span><br><span class="line">    ANDQ$~<span class="number">15</span>, SP     <span class="comment">//调整栈顶寄存器使其按16字节对齐</span></span><br><span class="line">    MOVQAX, <span class="number">16</span>(SP) <span class="comment">//argc放在SP + 16字节处</span></span><br><span class="line">    MOVQBX, <span class="number">24</span>(SP) <span class="comment">//argv放在SP + 24字节处</span></span><br></pre></td></tr></table></figure></p>
<p>上面的第4条指令用于调整栈顶寄存器的值使其按16字节对齐，也就是让栈顶寄存器SP指向的内存的地址为16的倍数，之所以要按16字节对齐，是因为CPU有一组SSE指令，这些指令中出现的内存地址必须是16的倍数，最后两条指令把argc和argv搬到新的位置。这段代码的其它部分已经做了比较详细的注释，所以这里就不做过多的解释了。</p>
<p>初始化g0</p>
<p>继续看后面的代码，下面开始初始化全局变量g0，前面我们说过，g0的主要作用是提供一个栈供runtime代码执行，因此这里主要对g0的几个与栈有关的成员进行了初始化，从这里可以看出g0的栈大约有64K，地址范围为 SP - 64*1024 + 104 ～ SP。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">96</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// create istack out of the given (operating system) stack.</span></span><br><span class="line"><span class="comment">// _cgo_init may update stackguard.</span></span><br><span class="line"><span class="comment">//下面这段代码从系统线程的栈空分出一部分当作g0的栈，然后初始化g0的栈信息和stackgard</span></span><br><span class="line">MOVQ$runtime·g0(SB), DI       <span class="comment">//g0的地址放入DI寄存器</span></span><br><span class="line">LEAQ(<span class="number">-64</span>*<span class="number">1024</span>+<span class="number">104</span>)(SP), BX <span class="comment">//BX = SP - 64*1024 + 104</span></span><br><span class="line">MOVQBX, g_stackguard0(DI) <span class="comment">//g0.stackguard0 = SP - 64*1024 + 104</span></span><br><span class="line">MOVQBX, g_stackguard1(DI) <span class="comment">//g0.stackguard1 = SP - 64*1024 + 104</span></span><br><span class="line">MOVQBX, (g_stack+stack_lo)(DI) <span class="comment">//g0.stack.lo = SP - 64*1024 + 104</span></span><br><span class="line">MOVQSP, (g_stack+stack_hi)(DI) <span class="comment">//g0.stack.hi = SP</span></span><br></pre></td></tr></table></figure></p>
<p>运行完上面这几行指令后g0与栈之间的关系如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/846b4e78af61697ea4ab003df5ed3a5d.png" alt></p>
<p>主线程与m0绑定</p>
<p>设置好g0栈之后，我们跳过CPU型号检查以及cgo初始化相关的代码，直接从164行继续分析。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">164</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">//下面开始初始化tls(thread local storage,线程本地存储)</span></span><br><span class="line">LEAQruntime·m0+m_tls(SB), DI <span class="comment">//DI = &amp;m0.tls，取m0的tls成员的地址到DI寄存器</span></span><br><span class="line">CALLruntime·settls(SB) <span class="comment">//调用settls设置线程本地存储，settls函数的参数在DI寄存器中</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// store through it, to make sure it works</span></span><br><span class="line"><span class="comment">//验证settls是否可以正常工作，如果有问题则abort退出程序</span></span><br><span class="line">get_tls(BX) <span class="comment">//获取fs段基地址并放入BX寄存器，其实就是m0.tls[1]的地址，get_tls的代码由编译器生成</span></span><br><span class="line">MOVQ$<span class="number">0x123</span>, g(BX) <span class="comment">//把整型常量0x123拷贝到fs段基地址偏移-8的内存位置，也就是m0.tls[0]= 0x123</span></span><br><span class="line">MOVQruntime·m0+m_tls(SB), AX <span class="comment">//AX = m0.tls[0]</span></span><br><span class="line">CMPQAX, $<span class="number">0x123</span> <span class="comment">//检查m0.tls[0]的值是否是通过线程本地存储存入的0x123来验证tls功能是否正常</span></span><br><span class="line">JEQ <span class="number">2</span>(PC)</span><br><span class="line">CALLruntime·abort(SB) <span class="comment">//如果线程本地存储不能正常工作，退出程序</span></span><br></pre></td></tr></table></figure></p>
<p>这段代码首先调用settls函数初始化主线程的线程本地存储(TLS)，目的是把m0与主线程关联在一起，至于为什么要把m和工作线程绑定在一起，我们已经在上一节介绍过了，这里就不再重复。设置了线程本地存储之后接下来的几条指令在于验证TLS功能是否正常，如果不正常则直接abort退出程序。</p>
<p>下面我们详细来详细看一下settls函数是如何实现线程私有全局变量的。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/sys_linx_amd64.s : <span class="number">606</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// set tls base to DI</span></span><br><span class="line">TEXT runtime·settls(SB),NOSPLIT,$<span class="number">32</span></span><br><span class="line"><span class="comment">//......</span></span><br><span class="line"><span class="comment">//DI寄存器中存放的是m.tls[0]的地址，m的tls成员是一个数组，读者如果忘记了可以回头看一下m结构体的定义</span></span><br><span class="line"><span class="comment">//下面这一句代码把DI寄存器中的地址加8，为什么要+8呢，主要跟ELF可执行文件格式中的TLS实现的机制有关</span></span><br><span class="line"><span class="comment">//执行下面这句指令之后DI寄存器中的存放的就是m.tls[1]的地址了</span></span><br><span class="line">ADDQ$<span class="number">8</span>, DI<span class="comment">// ELF wants to use -8(FS)</span></span><br><span class="line"> </span><br><span class="line">  <span class="comment">//下面通过arch_prctl系统调用设置FS段基址</span></span><br><span class="line">MOVQDI, SI <span class="comment">//SI存放arch_prctl系统调用的第二个参数</span></span><br><span class="line">MOVQ$<span class="number">0x1002</span>, DI<span class="comment">// ARCH_SET_FS //arch_prctl的第一个参数</span></span><br><span class="line">MOVQ$SYS_arch_prctl, AX <span class="comment">//系统调用编号</span></span><br><span class="line">SYSCALL</span><br><span class="line">CMPQAX, $<span class="number">0xfffffffffffff001</span></span><br><span class="line">JLS2(PC)</span><br><span class="line">MOVL$<span class="number">0xf1</span>, <span class="number">0xf1</span> <span class="comment">// crash //系统调用失败直接crash</span></span><br><span class="line">RET</span><br></pre></td></tr></table></figure></p>
<p>从代码可以看到，这里通过arch_prctl系统调用把m0.tls[1]的地址设置成了fs段的段基址。CPU中有个叫fs的段寄存器与之对应，而每个线程都有自己的一组CPU寄存器值，操作系统在把线程调离CPU运行时会帮我们把所有寄存器中的值保存在内存中，调度线程起来运行时又会从内存中把这些寄存器的值恢复到CPU，这样，在此之后，工作线程代码就可以通过fs寄存器来找到m.tls，读者可以参考上面初始化tls之后对tls功能验证的代码来理解这一过程。</p>
<p>下面继续分析rt0_go，<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">174</span></span><br><span class="line"></span><br><span class="line">ok:</span><br><span class="line"><span class="comment">// set the per-goroutine and per-mach "registers"</span></span><br><span class="line">get_tls(BX) <span class="comment">//获取fs段基址到BX寄存器</span></span><br><span class="line">LEAQruntime·g0(SB), CX <span class="comment">//CX = g0的地址</span></span><br><span class="line">MOVQCX, g(BX) <span class="comment">//把g0的地址保存在线程本地存储里面，也就是m0.tls[0]=&amp;g0</span></span><br><span class="line">LEAQruntime·m0(SB), AX <span class="comment">//AX = m0的地址</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">//把m0和g0关联起来m0-&gt;g0 = g0，g0-&gt;m = m0</span></span><br><span class="line"><span class="comment">// save m-&gt;g0 = g0</span></span><br><span class="line">MOVQCX, m_g0(AX) <span class="comment">//m0.g0 = g0</span></span><br><span class="line"><span class="comment">// save m0 to g0-&gt;m </span></span><br><span class="line">MOVQAX, g_m(CX) <span class="comment">//g0.m = m0</span></span><br></pre></td></tr></table></figure></p>
<p>上面的代码首先把g0的地址放入主线程的线程本地存储中，然后通过<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">m0.g0 = &amp;g0</span><br><span class="line">g0.m = &amp;m0</span><br></pre></td></tr></table></figure></p>
<p>把m0和g0绑定在一起，这样，之后在主线程中通过get_tls可以获取到g0，通过g0的m成员又可以找到m0，于是这里就实现了m0和g0与主线程之间的关联。从这里还可以看到，保存在主线程本地存储中的值是g0的地址，也就是说工作线程的私有全局变量其实是一个指向g的指针而不是指向m的指针，目前这个指针指向g0，表示代码正运行在g0栈。此时，主线程，m0，g0以及g0的栈之间的关系如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bd0bfab9f20d9fa2ac42c317f72315ee.png" alt></p>
<p>初始化m0</p>
<p>下面代码开始处理命令行参数，这部分我们不关心，所以跳过。命令行参数处理完成后调用osinit函数获取CPU核的数量并保存在全局变量ncpu之中，调度器初始化时需要知道当前系统有多少个CPU核。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">189</span></span><br></pre></td></tr></table></figure></p>
<p>//准备调用args函数，前面四条指令把参数放在栈上<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">MOVL16(SP), AX<span class="comment">// AX = argc</span></span><br><span class="line">MOVLAX, <span class="number">0</span>(SP)       <span class="comment">// argc放在栈顶</span></span><br><span class="line">MOVQ24(SP), AX<span class="comment">// AX = argv</span></span><br><span class="line">MOVQAX, <span class="number">8</span>(SP)       <span class="comment">// argv放在SP + 8的位置</span></span><br><span class="line">CALLruntime·args(SB)  <span class="comment">//处理操作系统传递过来的参数和env，不需要关心</span></span><br></pre></td></tr></table></figure></p>
<p>//对于linx来说，osinit唯一功能就是获取CPU的核数并放在global变量ncpu中，<br>//调度器初始化时需要知道当前系统有多少CPU核<br>CALLruntime·osinit(SB)  //执行的结果是全局变量 ncpu = CPU核数<br>CALLruntime·schedinit(SB) //调度系统初始化<br>接下来继续看调度器是如何初始化的。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">526</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">schedinit</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="comment">// raceinit must be the first call to race detector.</span></span><br><span class="line"><span class="comment">// In particular, it must be done before mallocinit below calls racemapshadow.</span></span><br><span class="line">   </span><br><span class="line">    <span class="comment">//getg函数在源代码中没有对应的定义，由编译器插入类似下面两行代码</span></span><br><span class="line">    <span class="comment">//get_tls(CX) </span></span><br><span class="line">    <span class="comment">//MOVQ g(CX), BX; BX存器里面现在放的是当前g结构体对象的地址</span></span><br><span class="line">    _g_ := getg() <span class="comment">// _g_ = &amp;g0</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//设置最多启动10000个操作系统线程，也是最多10000个M</span></span><br><span class="line">    sched.maxmcount = <span class="number">10000</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">   </span><br><span class="line">    mcommoninit(_g_.m) <span class="comment">//初始化m0，因为从前面的代码我们知道g0-&gt;m = &amp;m0</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    sched.lastpoll = <span class="keyword">uint64</span>(nanotime())</span><br><span class="line">    procs := ncpu  <span class="comment">//系统中有多少核，就创建和初始化多少个p结构体对象</span></span><br><span class="line">    <span class="keyword">if</span> n, ok := atoi32(gogetenv(<span class="string">"GOMAXPROCS"</span>)); ok &amp;&amp; n &gt; <span class="number">0</span> &#123;</span><br><span class="line">        procs = n <span class="comment">//如果环境变量指定了GOMAXPROCS，则创建指定数量的p</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> procresize(procs) != <span class="literal">nil</span> &#123;<span class="comment">//创建和初始化全局变量allp</span></span><br><span class="line">        throw(<span class="string">"unknown runnable goroutine during bootstrap"</span>)</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>前面我们已经看到，g0的地址已经被设置到了线程本地存储之中，schedinit通过getg函数（getg函数是编译器实现的，我们在源代码中是找不到其定义的）从线程本地存储中获取当前正在运行的g，这里获取出来的是g0，然后调用mcommoninit函数对m0(g0.m)进行必要的初始化，对m0初始化完成之后调用procresize初始化系统需要用到的p结构体对象，按照go语言官方的说法，p就是processor的意思，它的数量决定了最多可以有都少个goroutine同时并行运行。schedinit函数除了初始化m0和p，还设置了全局变量sched的maxmcount成员为10000，限制最多可以创建10000个操作系统线程出来工作。</p>
<p>这里我们需要重点关注一下mcommoninit如何初始化m0以及procresize函数如何创建和初始化p结构体对象。首先我们深入到mcommoninit函数中一探究竟。这里需要注意的是不只是初始化的时候会执行该函数，在程序运行过程中如果创建了工作线程，也会执行它，所以我们会在函数中看到加锁和检查线程数量是否已经超过最大值等相关的代码。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">596</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">mcommoninit</span><span class="params">(mp *m)</span></span> &#123;</span><br><span class="line">    _g_ := getg() <span class="comment">//初始化过程中_g_ = g0</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// g0 stack won't make sense for user (and is not necessary unwindable).</span></span><br><span class="line">    <span class="keyword">if</span> _g_ != _g_.m.g0 &#123;  <span class="comment">//函数调用栈traceback，不需要关心</span></span><br><span class="line">        callers(<span class="number">1</span>, mp.createstack[:])</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    lock(&amp;sched.lock)</span><br><span class="line">    <span class="keyword">if</span> sched.mnext+<span class="number">1</span> &lt; sched.mnext &#123;</span><br><span class="line">        throw(<span class="string">"runtime: thread ID overflow"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    mp.id = sched.mnext</span><br><span class="line">    sched.mnext++</span><br><span class="line">    checkmcount() <span class="comment">//检查已创建系统线程是否超过了数量限制（10000）</span></span><br><span class="line">    <span class="comment">//random初始化</span></span><br><span class="line">    mp.fastrand[<span class="number">0</span>] = <span class="number">1597334677</span> * <span class="keyword">uint32</span>(mp.id)</span><br><span class="line">    mp.fastrand[<span class="number">1</span>] = <span class="keyword">uint32</span>(cputicks())</span><br><span class="line">    <span class="keyword">if</span> mp.fastrand[<span class="number">0</span>]|mp.fastrand[<span class="number">1</span>] == <span class="number">0</span> &#123;</span><br><span class="line">        mp.fastrand[<span class="number">1</span>] = <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//创建用于信号处理的gsignal，只是简单的从堆上分配一个g结构体对象,然后把栈设置好就返回了</span></span><br><span class="line">    mpreinit(mp)</span><br><span class="line">    <span class="keyword">if</span> mp.gsignal != <span class="literal">nil</span> &#123;</span><br><span class="line">        mp.gsignal.stackguard1 = mp.gsignal.stack.lo + _StackGuard</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//把m挂入全局链表allm之中</span></span><br><span class="line">    <span class="comment">// Add to allm so garbage collector doesn't free g-&gt;m</span></span><br><span class="line">    <span class="comment">// when it is just in a register or thread-local storage.</span></span><br><span class="line">    mp.alllink = allm </span><br><span class="line"> </span><br><span class="line">    <span class="comment">// NumCgoCall() iterates over allm w/o schedlock,</span></span><br><span class="line">    <span class="comment">// so we need to publish it safely.</span></span><br><span class="line">    atomicstorep(unsafe.Pointer(&amp;allm), unsafe.Pointer(mp))</span><br><span class="line">    unlock(&amp;sched.lock)</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Allocate memory to hold a cgo traceback if the cgo call crashes.</span></span><br><span class="line">    <span class="keyword">if</span> iscgo || GOOS == <span class="string">"solaris"</span> || GOOS == <span class="string">"windows"</span> &#123;</span><br><span class="line">        mp.cgoCallers = <span class="built_in">new</span>(cgoCallers)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>从这个函数的源代码可以看出，这里并未对m0做什么关于调度相关的初始化，所以可以简单的认为这个函数只是把m0放入全局链表allm之中就返回了。</p>
<p>m0完成基本的初始化后，继续调用procresize创建和初始化p结构体对象，在这个函数里面会创建指定个数（根据cpu核数或环境变量确定）的p结构体对象放在全变量allp里, 并把m0和allp[0]绑定在一起，因此当这个函数执行完成之后就有</p>
<p>m0.p = allp[0]<br>allp[0].m = &amp;m0<br>到此m0, g0, 和m需要的p完全关联在一起了。</p>
<p>初始化allp</p>
<p>下面我们来看procresize函数，考虑到初始化完成之后用户代码还可以通过 GOMAXPROCS()函数调用它重新创建和初始化p结构体对象，而在运行过程中再动态的调整p牵涉到的问题比较多，所以这个函数的处理比较复杂，但如果只考虑初始化，相对来说要简单很多，所以这里只保留了初始化时会执行的代码：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">3902</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">procresize</span><span class="params">(nprocs <span class="keyword">int32</span>)</span> *<span class="title">p</span></span> &#123;</span><br><span class="line">    old := gomaxprocs <span class="comment">//系统初始化时 gomaxprocs = 0</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Grow allp if necessary.</span></span><br><span class="line">    <span class="keyword">if</span> nprocs &gt; <span class="keyword">int32</span>(<span class="built_in">len</span>(allp)) &#123; <span class="comment">//初始化时 len(allp) == 0</span></span><br><span class="line">        <span class="comment">// Synchronize with retake, which could be running</span></span><br><span class="line">        <span class="comment">// concurrently since it doesn't run on a P.</span></span><br><span class="line">        lock(&amp;allpLock)</span><br><span class="line">        <span class="keyword">if</span> nprocs &lt;= <span class="keyword">int32</span>(<span class="built_in">cap</span>(allp)) &#123;</span><br><span class="line">            allp = allp[:nprocs]</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123; <span class="comment">//初始化时进入此分支，创建allp 切片</span></span><br><span class="line">            nallp := <span class="built_in">make</span>([]*p, nprocs)</span><br><span class="line">            <span class="comment">// Copy everything up to allp's cap so we</span></span><br><span class="line">            <span class="comment">// never lose old allocated Ps.</span></span><br><span class="line">            <span class="built_in">copy</span>(nallp, allp[:<span class="built_in">cap</span>(allp)])</span><br><span class="line">            allp = nallp</span><br><span class="line">        &#125;</span><br><span class="line">        unlock(&amp;allpLock)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// initialize new P's</span></span><br><span class="line">    <span class="comment">//循环创建nprocs个p并完成基本初始化</span></span><br><span class="line">    <span class="keyword">for</span> i := <span class="keyword">int32</span>(<span class="number">0</span>); i &lt; nprocs; i++ &#123;</span><br><span class="line">        pp := allp[i]</span><br><span class="line">        <span class="keyword">if</span> pp == <span class="literal">nil</span> &#123;</span><br><span class="line">            pp = <span class="built_in">new</span>(p)<span class="comment">//调用内存分配器从堆上分配一个struct p</span></span><br><span class="line">            pp.id = i</span><br><span class="line">            pp.status = _Pgcstop</span><br><span class="line">            ......</span><br><span class="line">            atomicstorep(unsafe.Pointer(&amp;allp[i]), unsafe.Pointer(pp))</span><br><span class="line">        &#125;</span><br><span class="line">        ......</span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">    _g_ := getg()  <span class="comment">// _g_ = g0</span></span><br><span class="line">    <span class="keyword">if</span> _g_.m.p != <span class="number">0</span> &amp;&amp; _g_.m.p.ptr().id &lt; nprocs &#123;<span class="comment">//初始化时m0-&gt;p还未初始化，所以不会执行这个分支</span></span><br><span class="line">        <span class="comment">// continue to use the current P</span></span><br><span class="line">        _g_.m.p.ptr().status = _Prunning</span><br><span class="line">        _g_.m.p.ptr().mcache.prepareForSweep()</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;<span class="comment">//初始化时执行这个分支</span></span><br><span class="line">        <span class="comment">// release the current P and acquire allp[0]</span></span><br><span class="line">        <span class="keyword">if</span> _g_.m.p != <span class="number">0</span> &#123;<span class="comment">//初始化时这里不执行</span></span><br><span class="line">            _g_.m.p.ptr().m = <span class="number">0</span></span><br><span class="line">        &#125;</span><br><span class="line">        _g_.m.p = <span class="number">0</span></span><br><span class="line">        _g_.m.mcache = <span class="literal">nil</span></span><br><span class="line">        p := allp[<span class="number">0</span>]</span><br><span class="line">        p.m = <span class="number">0</span></span><br><span class="line">        p.status = _Pidle</span><br><span class="line">        acquirep(p) <span class="comment">//把p和m0关联起来，其实是这两个strct的成员相互赋值</span></span><br><span class="line">        <span class="keyword">if</span> trace.enabled &#123;</span><br><span class="line">            traceGoStart()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//下面这个for 循环把所有空闲的p放入空闲链表</span></span><br><span class="line">    <span class="keyword">var</span> runnablePs *p</span><br><span class="line">    <span class="keyword">for</span> i := nprocs - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i-- &#123;</span><br><span class="line">        p := allp[i]</span><br><span class="line">        <span class="keyword">if</span> _g_.m.p.ptr() == p &#123;<span class="comment">//allp[0]跟m0关联了，所以是不能放任</span></span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        &#125;</span><br><span class="line">        p.status = _Pidle</span><br><span class="line">        <span class="keyword">if</span> runqempty(p) &#123;<span class="comment">//初始化时除了allp[0]其它p全部执行这个分支，放入空闲链表</span></span><br><span class="line">            pidleput(p)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            ......</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">   </span><br><span class="line">    <span class="keyword">return</span> runnablePs</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个函数代码比较长，但并不复杂，这里总结一下这个函数的主要流程：</p>
<p>使用make([]<em>p, nprocs)初始化全局变量allp，即allp = make([]</em>p, nprocs)</p>
<p>循环创建并初始化nprocs个p结构体对象并依次保存在allp切片之中</p>
<p>把m0和allp[0]绑定在一起，即m0.p = allp[0], allp[0].m = m0</p>
<p>把除了allp[0]之外的所有p放入到全局变量sched的pidle空闲队列之中</p>
<p>procresize函数执行完后，调度器相关的初始化工作就基本结束了，这时整个调度器相关的各组成部分之间的联系如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0e21c511e55325a7e1351a41993c270e.png" alt></p>
<p>分析完调度器的基本初始化后，下一节我们来看程序中的第一个goroutine是如何创建的。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器之调度main goroutine（14)</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/Go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E4%B9%8B%E8%B0%83%E5%BA%A6main-goroutine%EF%BC%8814/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/8eJm5hjwKXya85VnT4y8Cw" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-09</p>
<p>本文是《Go语言调度器源代码情景分析》系列的第14篇，也是第二章的第4小节</p>
<p>上一节我们通过分析main goroutine的创建详细讨论了goroutine的创建及初始化流程，这一节我们接着来分析调度器如何把main goroutine调度到CPU上去运行。本节需要重点关注的问题有：</p>
<p>如何保存g0的调度信息？</p>
<p>schedule函数有什么重要作用？</p>
<p>gogo函数如何完成从g0到main goroutine的切换？</p>
<p>接着前一节继续分析代码，从newproc返回到rt0_go，继续往下执行mstart。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">1153</span></span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">mstart</span><span class="params">()</span></span> &#123;</span><br><span class="line">    _g_ := getg() <span class="comment">//_g_ = g0</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">//对于启动过程来说，g0的stack.lo早已完成初始化，所以onStack = false</span></span><br><span class="line">    osStack := _g_.stack.lo == <span class="number">0</span></span><br><span class="line">    <span class="keyword">if</span> osStack &#123;</span><br><span class="line">        <span class="comment">// Initialize stack bounds from system stack.</span></span><br><span class="line">        <span class="comment">// Cgo may have left stack size in stack.hi.</span></span><br><span class="line">        <span class="comment">// minit may update the stack bounds.</span></span><br><span class="line">        size := _g_.stack.hi</span><br><span class="line">        <span class="keyword">if</span> size == <span class="number">0</span> &#123;</span><br><span class="line">            size = <span class="number">8192</span> * sys.StackGuardMultiplier</span><br><span class="line">        &#125;</span><br><span class="line">        _g_.stack.hi = <span class="keyword">uintptr</span>(noescape(unsafe.Pointer(&amp;size)))</span><br><span class="line">        _g_.stack.lo = _g_.stack.hi - size + <span class="number">1024</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// Initialize stack guards so that we can start calling</span></span><br><span class="line">    <span class="comment">// both Go and C functions with stack growth prologues.</span></span><br><span class="line">    _g_.stackguard0 = _g_.stack.lo + _StackGuard</span><br><span class="line">    _g_.stackguard1 = _g_.stackguard0</span><br><span class="line">    </span><br><span class="line">    mstart1()</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Exit this thread.</span></span><br><span class="line">    <span class="keyword">if</span> GOOS == <span class="string">"windows"</span> || GOOS == <span class="string">"solaris"</span> || GOOS == <span class="string">"plan9"</span> || GOOS == <span class="string">"darwin"</span> || GOOS == <span class="string">"aix"</span> &#123;</span><br><span class="line">        <span class="comment">// Window, Solaris, Darwin, AIX and Plan 9 always system-allocate</span></span><br><span class="line">        <span class="comment">// the stack, but put it in _g_.stack before mstart,</span></span><br><span class="line">        <span class="comment">// so the logic above hasn't set osStack yet.</span></span><br><span class="line">        osStack = <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">    mexit(osStack)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>mstart函数本身没啥说的，它继续调用mstart1函数。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">1184</span></span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">mstart1</span><span class="params">()</span></span> &#123;</span><br><span class="line">    _g_ := getg()  <span class="comment">//启动过程时 _g_ = m0的g0</span></span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> _g_ != _g_.m.g0 &#123;</span><br><span class="line">        throw(<span class="string">"bad runtime·mstart"</span>)</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Record the caller for use as the top of stack in mcall and</span></span><br><span class="line">    <span class="comment">// for terminating the thread.</span></span><br><span class="line">    <span class="comment">// We're never coming back to mstart1 after we call schedule,</span></span><br><span class="line">    <span class="comment">// so other calls can reuse the current frame.</span></span><br><span class="line">    <span class="comment">//getcallerpc()获取mstart1执行完的返回地址</span></span><br><span class="line">    <span class="comment">//getcallersp()获取调用mstart1时的栈顶地址</span></span><br><span class="line">    save(getcallerpc(), getcallersp())</span><br><span class="line">    asminit()  <span class="comment">//在AMD64 Linux平台中，这个函数什么也没做，是个空函数</span></span><br><span class="line">    minit()    <span class="comment">//与信号相关的初始化，目前不需要关心</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Install signal handlers; after minit so that minit can</span></span><br><span class="line">    <span class="comment">// prepare the thread to be able to handle the signals.</span></span><br><span class="line">    <span class="keyword">if</span> _g_.m == &amp;m0 &#123; <span class="comment">//启动时_g_.m是m0，所以会执行下面的mstartm0函数</span></span><br><span class="line">        mstartm0() <span class="comment">//也是信号相关的初始化，现在我们不关注</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> fn := _g_.m.mstartfn; fn != <span class="literal">nil</span> &#123; <span class="comment">//初始化过程中fn == nil</span></span><br><span class="line">        fn()</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> _g_.m != &amp;m0 &#123;<span class="comment">// m0已经绑定了allp[0]，不是m0的话还没有p，所以需要获取一个p</span></span><br><span class="line">        acquirep(_g_.m.nextp.ptr())</span><br><span class="line">        _g_.m.nextp = <span class="number">0</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//schedule函数永远不会返回</span></span><br><span class="line">    schedule()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>mstart1首先调用save函数来保存g0的调度信息，save这一行代码非常重要，是我们理解调度循环的关键点之一。这里首先需要注意的是代码中的getcallerpc()返回的是mstart调用mstart1时被call指令压栈的返回地址，getcallersp()函数返回的是调用mstart1函数之前mstart函数的栈顶地址，其次需要看看save函数到底做了哪些重要工作。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2733</span></span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"><span class="comment">// save updates getg().sched to refer to pc and sp so that a following</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// gogo will restore pc and sp.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// save must not have write barriers because invoking a write barrier</span></span><br><span class="line"><span class="comment">// can clobber getg().sched.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="comment">//go:nowritebarrierrec</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">save</span><span class="params">(pc, sp <span class="keyword">uintptr</span>)</span></span> &#123;</span><br><span class="line">    _g_ := getg()</span><br><span class="line"> </span><br><span class="line">    _g_.sched.pc = pc <span class="comment">//再次运行时的指令地址</span></span><br><span class="line">    _g_.sched.sp = sp <span class="comment">//再次运行时到栈顶</span></span><br><span class="line">    _g_.sched.lr = <span class="number">0</span></span><br><span class="line">    _g_.sched.ret = <span class="number">0</span></span><br><span class="line">    _g_.sched.g = guintptr(unsafe.Pointer(_g_))</span><br><span class="line">    <span class="comment">// We need to ensure ctxt is zero, but can't have a write</span></span><br><span class="line">    <span class="comment">// barrier here. However, it should always already be zero.</span></span><br><span class="line">    <span class="comment">// Assert that.</span></span><br><span class="line">    <span class="keyword">if</span> _g_.sched.ctxt != <span class="literal">nil</span> &#123;</span><br><span class="line">        badctxt()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到，save函数保存了调度相关的所有信息，包括最为重要的当前正在运行的g的下一条指令的地址和栈顶地址，不管是对g0还是其它goroutine来说这些信息在调度过程中都是必不可少的，我们会在后面的调度分析中看到调度器是如何利用这些信息来完成调度的。代码执行完save函数之后g0的状态如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/ab90055747ffb9e0de576987a3bd6377.png" alt></p>
<p>从上图可以看出，g0.sched.sp指向了mstart1函数执行完成后的返回地址，该地址保存在了mstart函数的栈帧之中；g0.sched.pc指向的是mstart函数中调用mstart1函数之后的 if 语句。</p>
<p>为什么g0已经执行到mstart1这个函数了而且还会继续调用其它函数，但g0的调度信息中的pc和sp却要设置在mstart函数中？难道下次切换到g0时要从mstart函数中的 if 语句继续执行？可是从mstart函数可以看到，if语句之后就要退出线程了！这看起来很奇怪，不过随着分析的进行，我们会看到这里为什么要这么做。</p>
<p>继续分析代码，save函数执行完成后，返回到mstart1继续其它跟m相关的一些初始化，完成这些初始化后则调用调度系统的核心函数schedule()完成goroutine的调度，之所以说它是核心，原因在于每次调度goroutine都是从schedule函数开始的。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2469</span></span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"><span class="comment">// One round of scheduler: find a runnable goroutine and execute it.</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Never returns.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">schedule</span><span class="params">()</span></span> &#123;</span><br><span class="line">    _g_ := getg()  <span class="comment">//_g_ = 每个工作线程m对应的g0，初始化时是m0的g0</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"> </span><br><span class="line">    <span class="keyword">var</span> gp *g</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> gp == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// Check the global runnable queue once in a while to ensure fairness.</span></span><br><span class="line">        <span class="comment">// Otherwise two goroutines can completely occupy the local runqueue</span></span><br><span class="line">        <span class="comment">// by constantly respawning each other.</span></span><br><span class="line">        <span class="comment">//为了保证调度的公平性，每进行61次调度就需要优先从全局运行队列中获取goroutine，</span></span><br><span class="line">        <span class="comment">//因为如果只调度本地队列中的g，那么全局运行队列中的goroutine将得不到运行</span></span><br><span class="line">        <span class="keyword">if</span> _g_.m.p.ptr().schedtick%<span class="number">61</span> == <span class="number">0</span> &amp;&amp; sched.runqsize &gt; <span class="number">0</span> &#123;</span><br><span class="line">            lock(&amp;sched.lock) <span class="comment">//所有工作线程都能访问全局运行队列，所以需要加锁</span></span><br><span class="line">            gp = globrunqget(_g_.m.p.ptr(), <span class="number">1</span>) <span class="comment">//从全局运行队列中获取1个goroutine</span></span><br><span class="line">            unlock(&amp;sched.lock)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> gp == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">//从与m关联的p的本地运行队列中获取goroutine</span></span><br><span class="line">        gp, inheritTime = runqget(_g_.m.p.ptr())</span><br><span class="line">        <span class="keyword">if</span> gp != <span class="literal">nil</span> &amp;&amp; _g_.m.spinning &#123;</span><br><span class="line">            throw(<span class="string">"schedule: spinning with local work"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> gp == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">//如果从本地运行队列和全局运行队列都没有找到需要运行的goroutine，</span></span><br><span class="line">        <span class="comment">//则调用findrunnable函数从其它工作线程的运行队列中偷取，如果偷取不到，则当前工作线程进入睡眠，</span></span><br><span class="line">        <span class="comment">//直到获取到需要运行的goroutine之后findrunnable函数才会返回。</span></span><br><span class="line">      gp, inheritTime = findrunnable() <span class="comment">// blocks until work is available</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//跟启动无关的代码.....</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">//当前运行的是runtime的代码，函数调用栈使用的是g0的栈空间</span></span><br><span class="line">    <span class="comment">//调用execte切换到gp的代码和栈空间去运行</span></span><br><span class="line">    execute(gp, inheritTime)  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>schedule函数通过调用globrunqget()和runqget()函数分别从全局运行队列和当前工作线程的本地运行队列中选取下一个需要运行的goroutine，如果这两个队列都没有需要运行的goroutine则通过findrunnalbe()函数从其它p的运行队列中盗取goroutine，一旦找到下一个需要运行的goroutine，则调用excute函数从g0切换到该goroutine去运行。对于我们这个场景来说，前面的启动流程已经创建好第一个goroutine并放入了当前工作线程的本地运行队列，所以这里会通过runqget把目前唯一的一个goroutine取出来，至于具体是如何取出来的，我们将在第三章讨论调度策略时再回头来详细分析globrunqget()，runqget()和findrunnable()这三个函数的实现流程，现在我们先来分析execute函数是如何把从运行队列中找出来的goroutine调度到CPU上运行的。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2136</span></span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"><span class="comment">// Schedules gp to run on the current M.</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// If inheritTime is true, gp inherits the remaining time in the</span></span><br><span class="line"><span class="comment">// current time slice. Otherwise, it starts a new time slice.</span></span><br><span class="line"><span class="comment">// Never returns.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Write barriers are allowed because this is called immediately after</span></span><br><span class="line"><span class="comment">// acquiring a P in several places.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//go:yeswritebarrierrec</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">execute</span><span class="params">(gp *g, inheritTime <span class="keyword">bool</span>)</span></span> &#123;</span><br><span class="line">    _g_ := getg() <span class="comment">//g0</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">//设置待运行g的状态为_Grunning</span></span><br><span class="line">    casgstatus(gp, _Grunnable, _Grunning)</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">//把g和m关联起来</span></span><br><span class="line">    _g_.m.curg = gp </span><br><span class="line">    gp.m = _g_.m</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">//gogo完成从g0到gp真正的切换</span></span><br><span class="line">    gogo(&amp;gp.sched)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>execute函数的第一个参数gp即是需要调度起来运行的goroutine，这里首先把gp的状态从_Grunnable修改为_Grunning，然后把gp和m关联起来，这样通过m就可以找到当前工作线程正在执行哪个goroutine，反之亦然。</p>
<p>完成gp运行前的准备工作之后，execute调用gogo函数完成从g0到gp的的切换：CPU执行权的转让以及栈的切换。</p>
<p>gogo函数也是通过汇编语言编写的，这里之所以需要使用汇编，是因为goroutine的调度涉及不同执行流之间的切换，前面我们在讨论操作系统切换线程时已经看到过，执行流的切换从本质上来说就是CPU寄存器以及函数调用栈的切换，然而不管是go还是c这种高级语言都无法精确控制CPU寄存器的修改，因而高级语言在这里也就无能为力了，只能依靠汇编指令来达成目的。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">251</span></span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"># <span class="function"><span class="keyword">func</span> <span class="title">gogo</span><span class="params">(buf *gobuf)</span></span></span><br><span class="line"></span><br><span class="line"># restore state from Gobuf; longjmp</span><br><span class="line">TEXT runtime·gogo(SB), NOSPLIT, $<span class="number">16</span><span class="number">-8</span></span><br><span class="line">    #buf = &amp;gp.sched</span><br><span class="line">    MOVQ  buf+<span class="number">0</span>(FP), BX   # BX = buf</span><br><span class="line"> </span><br><span class="line">    #gobuf-&gt;g --&gt; dx register</span><br><span class="line">    MOVQ  gobuf_g(BX), DX  # DX = gp.sched.g</span><br><span class="line"> </span><br><span class="line">    #下面这行代码没有实质作用，检查gp.sched.g是否是<span class="literal">nil</span>，如果是<span class="literal">nil</span>进程会crash死掉</span><br><span class="line">    MOVQ  <span class="number">0</span>(DX), CX   # <span class="built_in">make</span> sure g != <span class="literal">nil</span></span><br><span class="line"> </span><br><span class="line">    get_tls(CX) </span><br><span class="line"> </span><br><span class="line">    #把要运行的g的指针放入线程本地存储，这样后面的代码就可以通过线程本地存储</span><br><span class="line">    #获取到当前正在执行的goroutine的g结构体对象，从而找到与之关联的m和p</span><br><span class="line">    MOVQ  DX, g(CX)</span><br><span class="line"> </span><br><span class="line">    #把CPU的SP寄存器设置为sched.sp，完成了栈的切换</span><br><span class="line">    MOVQ  gobuf_sp(BX), SP  # restore SP</span><br><span class="line"> </span><br><span class="line">    #下面三条同样是恢复调度上下文到CPU相关寄存器</span><br><span class="line">    MOVQ  gobuf_ret(BX), AX</span><br><span class="line">    MOVQ  gobuf_ctxt(BX), DX</span><br><span class="line">    MOVQ  gobuf_bp(BX), BP</span><br><span class="line"> </span><br><span class="line">    #清空sched的值，因为我们已把相关值放入CPU对应的寄存器了，不再需要，这样做可以少gc的工作量</span><br><span class="line">    MOVQ  $<span class="number">0</span>, gobuf_sp(BX)  # clear to help garbage collector</span><br><span class="line">    MOVQ  $<span class="number">0</span>, gobuf_ret(BX)</span><br><span class="line">    MOVQ  $<span class="number">0</span>, gobuf_ctxt(BX)</span><br><span class="line">    MOVQ  $<span class="number">0</span>, gobuf_bp(BX)</span><br><span class="line"> </span><br><span class="line">    #把sched.pc值放入BX寄存器</span><br><span class="line">    MOVQ  gobuf_pc(BX), BX</span><br><span class="line"> </span><br><span class="line">    #JMP把BX寄存器的包含的地址值放入CPU的IP寄存器，于是，CPU跳转到该地址继续执行指令，</span><br><span class="line">    JMP BX</span><br></pre></td></tr></table></figure></p>
<p>gogo函数的这段汇编代码短小而强悍，虽然笔者已经在代码中做了详细的注释，但为了完全搞清楚它的工作原理，我们有必要再对这些指令进行逐条分析：</p>
<p>execute函数在调用gogo时把gp的sched成员的地址作为实参（型参buf）传递了过来，该参数位于FP寄存器所指的位置，所以第1条指令</p>
<p>MOVQ  buf+0(FP), BX # &amp;gp.sched –&gt; BX</p>
<p>把buf的值也就是gp.sched的地址放在了BX寄存器之中，这样便于后面的指令依靠BX寄存器来存取gp.sched的成员。sched成员保存了调度相关的信息，上一节我们已经看到，main goroutine创建时已经把这些信息设置好了。</p>
<p>第2条指令</p>
<p>MOVQ  gobuf_g(BX), DX  # gp.sched.g –&gt; DX</p>
<p>把gp.sched.g读取到DX寄存器，注意这条指令的源操作数是间接寻址，如果读者对间接寻址不熟悉的话可以参考预备知识汇编语言部分。</p>
<p>第3条指令</p>
<p>MOVQ  0(DX), CX # make sure g != nil</p>
<p>的作用在于检查gp.sched.g是否为nil，如果为nil指针的话，这条指令会导致程序死掉，有读者可能会有疑问，为什么要让它死掉啊，原因在于这个gp.sched.g是由go runtime代码负责设置的，按道理说不可能为nil，如果为nil，一定是程序逻辑写得有问题，所以需要把这个bug暴露出来，而不是把它隐藏起来。</p>
<p>第4条和第5条指令</p>
<p>get_tls(CX)</p>
<p>#把DX值也就是需要运行的goroutine的指针写入线程本地存储之中</p>
<p>#运行这条指令之前，线程本地存储存放的是g0的地址<br>MOVQ  DX, g(CX)<br>把DX寄存器的值也就是gp.sched.g(这是一个指向g的指针)写入线程本地存储之中，这样后面的代码就可以通过线程本地存储获取到当前正在执行的goroutine的g结构体对象，从而找到与之关联的m和p。</p>
<p>第6条指令</p>
<p>MOVQ  gobuf_sp(BX), SP # restore SP</p>
<p>设置CPU的栈顶寄存器SP为gp.sched.sp，这条指令完成了栈的切换，从g0的栈切换到了gp的栈。</p>
<p>第7～13条指令</p>
<p>#下面三条同样是恢复调度上下文到CPU相关寄存器</p>
<p>  MOVQ  gobuf_ret(BX), AX #系统调用的返回值放入AX寄存器<br>  MOVQ  gobuf_ctxt(BX), DX<br>  MOVQ  gobuf_bp(BX), BP</p>
<p>  //清空gp.sched中不再需要的值，因为我们已把相关值放入CPU对应的寄存器了，不再需要，这样做可以少gc的工作量<br>  MOVQ  $0, gobuf_sp(BX)  // clear to help garbage collector<br>  MOVQ  $0, gobuf_ret(BX)<br>  MOVQ  $0, gobuf_ctxt(BX)<br>  MOVQ  $0, gobuf_bp(BX)<br>一是根据gp.sched其它字段设置CPU相关寄存器，可以看到这里恢复了CPU的栈基地址寄存器BP，二是把gp.sched中已经不需要的成员设置为0，这样可以减少gc的工作量。</p>
<p>第14条指令</p>
<p>MOVQ  gobuf_pc(BX), BX</p>
<p>把gp.sched.pc的值读取到BX寄存器，这个pc值是gp这个goroutine马上需要执行的第一条指令的地址，对于我们这个场景来说它现在就是runtime.main函数的第一条指令，现在这条指令的地址就放在BX寄存器里面。最后一条指令</p>
<p>JMP  BX</p>
<p>这里的JMP BX指令把BX寄存器里面的指令地址放入CPU的rip寄存器，于是，CPU就会跳转到该地址继续执行属于gp这个goroutine的代码，这样就完成了goroutine的切换。</p>
<p>总结一下这15条指令，其实就只做了两件事：</p>
<p>把gp.sched的成员恢复到CPU的寄存器完成状态以及栈的切换；</p>
<p>跳转到gp.sched.pc所指的指令地址（runtime.main）处执行。</p>
<p>现在已经从g0切换到了gp这个goroutine，对于我们这个场景来说，gp还是第一次被调度起来运行，它的入口函数是runtime.main，所以接下来CPU就开始执行runtime.main函数：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">109</span></span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"><span class="comment">// The main goroutine.</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    g := getg()  <span class="comment">// g = main goroutine，不再是g0了</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Max stack size is 1 GB on 64-bit, 250 MB on 32-bit.</span></span><br><span class="line">    <span class="comment">// Using decimal instead of binary GB and MB because</span></span><br><span class="line">    <span class="comment">// they look nicer in the stack overflow failure message.</span></span><br><span class="line">    <span class="keyword">if</span> sys.PtrSize == <span class="number">8</span> &#123; <span class="comment">//64位系统上每个goroutine的栈最大可达1G</span></span><br><span class="line">        maxstacksize = <span class="number">1000000000</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        maxstacksize = <span class="number">250000000</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Allow newproc to start new Ms.</span></span><br><span class="line">    mainStarted = <span class="literal">true</span></span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> GOARCH != <span class="string">"wasm"</span> &#123; <span class="comment">// no threads on wasm yet, so no sysmon</span></span><br><span class="line">        <span class="comment">//现在执行的是main goroutine，所以使用的是main goroutine的栈，需要切换到g0栈去执行newm()</span></span><br><span class="line">        systemstack(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">            <span class="comment">//创建监控线程，该线程独立于调度器，不需要跟p关联即可运行</span></span><br><span class="line">             newm(sysmon, <span class="literal">nil</span>)</span><br><span class="line">        &#125;)</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//调用runtime包的初始化函数，由编译器实现</span></span><br><span class="line">    runtime_init() <span class="comment">// must be before defer</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Record when the world started.</span></span><br><span class="line">    runtimeInitTime = nanotime()</span><br><span class="line"> </span><br><span class="line">    gcenable()  <span class="comment">//开启垃圾回收器</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//main 包的初始化函数，也是由编译器实现，会递归的调用我们import进来的包的初始化函数</span></span><br><span class="line">    fn := main_init <span class="comment">// make an indirect call, as the linker doesn't know the address of the main package when laying down the runtime</span></span><br><span class="line">    fn()</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//调用main.main函数</span></span><br><span class="line">    fn = main_main <span class="comment">// make an indirect call, as the linker doesn't know the address of the main package when laying down the runtime</span></span><br><span class="line">    fn()</span><br><span class="line">    </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//进入系统调用，退出进程，可以看出main goroutine并未返回，而是直接进入系统调用退出进程了</span></span><br><span class="line">    exit(<span class="number">0</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//保护性代码，如果exit意外返回，下面的代码也会让该进程crash死掉</span></span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        <span class="keyword">var</span> x *<span class="keyword">int32</span></span><br><span class="line">        *x = <span class="number">0</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>runtime.main函数主要工作流程如下：</p>
<p>启动一个sysmon系统监控线程，该线程负责整个程序的gc、抢占调度以及netpoll等功能的监控，在抢占调度一章我们再继续分析sysmon是如何协助完成goroutine的抢占调度的；</p>
<p>执行runtime包的初始化；</p>
<p>执行main包以及main包import的所有包的初始化；</p>
<p>执行main.main函数；</p>
<p>从main.main函数返回后调用exit系统调用退出进程；</p>
<p>从上述流程可以看出，runtime.main执行完main包的main函数之后就直接调用exit系统调用结束进程了，它并没有返回到调用它的函数（还记得是从哪里开始执行的runtime.main吗？），其实runtime.main是main goroutine的入口函数，并不是直接被调用的，而是在schedule()-&gt;execute()-&gt;gogo()这个调用链的gogo函数中用汇编代码直接跳转过来的，所以从这个角度来说，goroutine确实不应该返回，没有地方可返回啊！可是从前面的分析中我们得知，在创建goroutine的时候已经在其栈上放好了一个返回地址，伪造成goexit函数调用了goroutine的入口函数，这里怎么没有用到这个返回地址啊？其实那是为非main goroutine准备的，非main goroutine执行完成后就会返回到goexit继续执行，而main goroutine执行完成后整个进程就结束了，这是main goroutine与其它goroutine的一个区别。</p>
<p>总结一下从g0切换到main goroutine的流程：</p>
<p>保存g0的调度信息，主要是保存CPU栈顶寄存器SP到g0.sched.sp成员之中；</p>
<p>调用schedule函数寻找需要运行的goroutine，我们这个场景找到的是main goroutine;</p>
<p>调用gogo函数首先从g0栈切换到main goroutine的栈，然后从main goroutine的g结构体对象之中取出sched.pc的值并使用JMP指令跳转到该地址去执行；</p>
<p>main goroutine执行完毕直接调用exit系统调用退出进程。</p>
<p>下一节我们将用例子来分析非main goroutine的退出。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器之盗取goroutine(17)</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/Go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E4%B9%8B%E7%9B%97%E5%8F%96goroutine-17/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/o2UmxmFFOwUAkgE-Ao9MsA" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-17</p>
<p>本文是《Go语言调度器源代码情景分析》系列的第17篇，也是第三章《Goroutine调度策略》的第2小节。</p>
<p>上一小节我们分析了从全局运行队列与工作线程的本地运行队列获取goroutine的过程，这一小节我们继续分析因无法从上述两个队列中拿到需要运行的goroutine而导致的从其它工作线程的本地运行队列中盗取goroutine的过程。</p>
<p>findrunnable() 函数负责处理与盗取相关的逻辑，该函数代码很繁杂，因为它还做了与gc和netpoll等相关的事情，为了不影响我们的分析思路，这里我们仍然把不相关的代码删掉了，不过代码还是比较多，但总结起来就一句话：尽力去各个运行队列中寻找goroutine，如果实在找不到则进入睡眠状态。下面是代码细节：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2176</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Finds a runnable goroutine to execute.</span></span><br><span class="line"><span class="comment">// Tries to steal from other P's, get g from global queue, poll network.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">findrunnable</span><span class="params">()</span> <span class="params">(gp *g, inheritTime <span class="keyword">bool</span>)</span></span> &#123;</span><br><span class="line">    _g_ := getg()</span><br><span class="line"></span><br><span class="line">    <span class="comment">// The conditions here and in handoffp must agree: if</span></span><br><span class="line">    <span class="comment">// findrunnable would return a G to run, handoffp must start</span></span><br><span class="line">    <span class="comment">// an M.</span></span><br><span class="line"></span><br><span class="line">top:</span><br><span class="line">    _p_ := _g_.m.p.ptr()</span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">// local runq</span></span><br><span class="line">    <span class="comment">//再次看一下本地运行队列是否有需要运行的goroutine</span></span><br><span class="line">    <span class="keyword">if</span> gp, inheritTime := runqget(_p_); gp != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> gp, inheritTime</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// global runq</span></span><br><span class="line">    <span class="comment">//再看看全局运行队列是否有需要运行的goroutine</span></span><br><span class="line">    <span class="keyword">if</span> sched.runqsize != <span class="number">0</span> &#123;</span><br><span class="line">        lock(&amp;sched.lock)</span><br><span class="line">        gp := globrunqget(_p_, <span class="number">0</span>)</span><br><span class="line">        unlock(&amp;sched.lock)</span><br><span class="line">        <span class="keyword">if</span> gp != <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> gp, <span class="literal">false</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Steal work from other P's.</span></span><br><span class="line">    <span class="comment">//如果除了当前工作线程还在运行外，其它工作线程已经处于休眠中，那么也就不用去偷了，肯定没有</span></span><br><span class="line">    procs := <span class="keyword">uint32</span>(gomaxprocs)</span><br><span class="line">    <span class="keyword">if</span> atomic.Load(&amp;sched.npidle) == procs<span class="number">-1</span> &#123;</span><br><span class="line">        <span class="comment">// Either GOMAXPROCS=1 or everybody, except for us, is idle already.</span></span><br><span class="line">        <span class="comment">// New work can appear from returning syscall/cgocall, network or timers.</span></span><br><span class="line">        <span class="comment">// Neither of that submits to local run queues, so no point in stealing.</span></span><br><span class="line">        <span class="keyword">goto</span> stop</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// If number of spinning M's &gt;= number of busy P's, block.</span></span><br><span class="line">    <span class="comment">// This is necessary to prevent excessive CPU consumption</span></span><br><span class="line">    <span class="comment">// when GOMAXPROCS&gt;&gt;1 but the program parallelism is low.</span></span><br><span class="line">    <span class="comment">// 这个判断主要是为了防止因为寻找可运行的goroutine而消耗太多的CPU。</span></span><br><span class="line">    <span class="comment">// 因为已经有足够多的工作线程正在寻找可运行的goroutine，让他们去找就好了，自己偷个懒去睡觉</span></span><br><span class="line">    <span class="keyword">if</span> !_g_.m.spinning &amp;&amp; <span class="number">2</span>*atomic.Load(&amp;sched.nmspinning) &gt;= procs-atomic.Load(&amp;sched.npidle) &#123;</span><br><span class="line">        <span class="keyword">goto</span> stop</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> !_g_.m.spinning &#123;</span><br><span class="line">        <span class="comment">//设置m的状态为spinning</span></span><br><span class="line">        _g_.m.spinning = <span class="literal">true</span></span><br><span class="line">        <span class="comment">//处于spinning状态的m数量加一</span></span><br><span class="line">        atomic.Xadd(&amp;sched.nmspinning, <span class="number">1</span>)</span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//从其它p的本地运行队列盗取goroutine</span></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">4</span>; i++ &#123;</span><br><span class="line">        <span class="keyword">for</span> enum := stealOrder.start(fastrand()); !enum.done(); enum.next() &#123;</span><br><span class="line">            <span class="keyword">if</span> sched.gcwaiting != <span class="number">0</span> &#123;</span><br><span class="line">                <span class="keyword">goto</span> top</span><br><span class="line">            &#125;</span><br><span class="line">            stealRunNextG := i &gt; <span class="number">2</span> <span class="comment">// first look for ready queues with more than 1 g</span></span><br><span class="line">            <span class="keyword">if</span> gp := runqsteal(_p_, allp[enum.position()], stealRunNextG); gp != <span class="literal">nil</span> &#123;</span><br><span class="line">                <span class="keyword">return</span> gp, <span class="literal">false</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">stop:</span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Before we drop our P, make a snapshot of the allp slice,</span></span><br><span class="line">    <span class="comment">// which can change underfoot once we no longer block</span></span><br><span class="line">    <span class="comment">// safe-points. We don't need to snapshot the contents because</span></span><br><span class="line">    <span class="comment">// everything up to cap(allp) is immutable.</span></span><br><span class="line">    allpSnapshot := allp</span><br><span class="line"></span><br><span class="line">    <span class="comment">// return P and block</span></span><br><span class="line">    lock(&amp;sched.lock)</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> sched.runqsize != <span class="number">0</span> &#123;</span><br><span class="line">        gp := globrunqget(_p_, <span class="number">0</span>)</span><br><span class="line">        unlock(&amp;sched.lock)</span><br><span class="line">        <span class="keyword">return</span> gp, <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">    <span class="comment">// 当前工作线程解除与p之间的绑定，准备去休眠</span></span><br><span class="line">    <span class="keyword">if</span> releasep() != _p_ &#123;</span><br><span class="line">        throw(<span class="string">"findrunnable: wrong p"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//把p放入空闲队列</span></span><br><span class="line">    pidleput(_p_)</span><br><span class="line">    unlock(&amp;sched.lock)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Delicate dance: thread transitions from spinning to non-spinning state,</span></span><br><span class="line"><span class="comment">// potentially concurrently with submission of new goroutines. We must</span></span><br><span class="line"><span class="comment">// drop nmspinning first and then check all per-P queues again (with</span></span><br><span class="line"><span class="comment">// #StoreLoad memory barrier in between). If we do it the other way around,</span></span><br><span class="line"><span class="comment">// another thread can submit a goroutine after we've checked all run queues</span></span><br><span class="line"><span class="comment">// but before we drop nmspinning; as the result nobody will unpark a thread</span></span><br><span class="line"><span class="comment">// to run the goroutine.</span></span><br><span class="line"><span class="comment">// If we discover new work below, we need to restore m.spinning as a signal</span></span><br><span class="line"><span class="comment">// for resetspinning to unpark a new worker thread (because there can be more</span></span><br><span class="line"><span class="comment">// than one starving goroutine). However, if after discovering new work</span></span><br><span class="line"><span class="comment">// we also observe no idle Ps, it is OK to just park the current thread:</span></span><br><span class="line"><span class="comment">// the system is fully loaded so no spinning threads are required.</span></span><br><span class="line"><span class="comment">// Also see "Worker thread parking/unparking" comment at the top of the file.</span></span><br><span class="line">    wasSpinning := _g_.m.spinning</span><br><span class="line">    <span class="keyword">if</span> _g_.m.spinning &#123;</span><br><span class="line">        <span class="comment">//m即将睡眠，状态不再是spinning</span></span><br><span class="line">        _g_.m.spinning = <span class="literal">false</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">int32</span>(atomic.Xadd(&amp;sched.nmspinning, <span class="number">-1</span>)) &lt; <span class="number">0</span> &#123;</span><br><span class="line">            throw(<span class="string">"findrunnable: negative nmspinning"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// check all runqueues once again</span></span><br><span class="line">    <span class="comment">// 休眠之前再看一下是否有工作要做</span></span><br><span class="line">    <span class="keyword">for</span> _, _p_ := <span class="keyword">range</span> allpSnapshot &#123;</span><br><span class="line">        <span class="keyword">if</span> !runqempty(_p_) &#123;</span><br><span class="line">            lock(&amp;sched.lock)</span><br><span class="line">            _p_ = pidleget()</span><br><span class="line">            unlock(&amp;sched.lock)</span><br><span class="line">            <span class="keyword">if</span> _p_ != <span class="literal">nil</span> &#123;</span><br><span class="line">                acquirep(_p_)</span><br><span class="line">                <span class="keyword">if</span> wasSpinning &#123;</span><br><span class="line">                    _g_.m.spinning = <span class="literal">true</span></span><br><span class="line">                    atomic.Xadd(&amp;sched.nmspinning, <span class="number">1</span>)</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">goto</span> top</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">//休眠</span></span><br><span class="line">    stopm()</span><br><span class="line">    <span class="keyword">goto</span> top</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>从上面的代码可以看到，工作线程在放弃寻找可运行的goroutine而进入睡眠之前，会反复尝试从各个运行队列寻找需要运行的goroutine，可谓是尽心尽力了。这个函数需要重点注意以下两点：</p>
<p>第一点，工作线程M的自旋状态(spinning)。工作线程在从其它工作线程的本地运行队列中盗取goroutine时的状态称为自旋状态。从上面代码可以看到，当前M在去其它p的运行队列盗取goroutine之前把spinning标志设置成了true，同时增加处于自旋状态的M的数量，而盗取结束之后则把spinning标志还原为false，同时减少处于自旋状态的M的数量，从后面的分析我们可以看到，当有空闲P又有goroutine需要运行的时候，这个处于自旋状态的M的数量决定了是否需要唤醒或者创建新的工作线程。</p>
<p>第二点，盗取算法。盗取过程用了两个嵌套for循环。内层循环实现了盗取逻辑，从代码可以看出盗取的实质就是遍历allp中的所有p，查看其运行队列是否有goroutine，如果有，则取其一半到当前工作线程的运行队列，然后从findrunnable返回，如果没有则继续遍历下一个p。但这里为了保证公平性，遍历allp时并不是固定的从allp[0]即第一个p开始，而是从随机位置上的p开始，而且遍历的顺序也随机化了，并不是现在访问了第i个p下一次就访问第i+1个p，而是使用了一种伪随机的方式遍历allp中的每个p，防止每次遍历时使用同样的顺序访问allp中的元素。下面是这个算法的伪代码：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">offset := <span class="keyword">uint32</span>(random()) % nprocs</span><br><span class="line">coprime := 随机选取一个小于nprocs且与nprocs互质的数</span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; nprocs; i++ &#123;</span><br><span class="line">    p := allp[offset]</span><br><span class="line">    从p的运行队列偷取goroutine</span><br><span class="line">    <span class="keyword">if</span> 偷取成功 &#123;</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">    &#125;</span><br><span class="line">    offset += coprime</span><br><span class="line">    offset = offset % nprocs</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>下面举例说明一下上述算法过程，现假设nprocs为8，也就是一共有8个p。</p>
<p>如果第一次随机选择的offset = 6，coprime = 3(3与8互质，满足算法要求)的话，则从allp切片中偷取的下标顺序为6, 1, 4, 7, 2, 5, 0, 3，计算过程：</p>
<p>6，(6+3)%8=1，(1+3)%8=4, (4+3)%8=7, (7+3)%8=2, (2+3)%8=5, (5+3)%8=0, (0+3)%8=3<br>如果第二次随机选择的offset = 4，coprime = 5的话，则从allp切片中偷取的下标顺序为1, 6, 3, 0, 5, 2, 7, 4，计算过程：</p>
<p>1，(1+5)%8=6，(6+5)%8=3, (3+5)%8=0, (0+5)%8=5, (5+5)%8=2, (2+5)%8=7, (7+5)%8=4<br>可以看到只要随机数不一样，偷取p的顺序也不一样，但可以保证经过8次循环，每个p都会被访问到。可以用数论知识证明，不管nprocs是多少，这个算法都可以保证经过nprocs次循环，每个p都可以得到访问。</p>
<p>挑选出盗取的对象p之后，则调用runqsteal盗取p的运行队列中的goroutine，runqsteal函数再调用runqgrap从p的队列中批量拿出多个goroutine，这两个函数本身比较简单，但runqgrab有一个小细节需要注意一下，见下面代码：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">4854</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Grabs a batch of goroutines from _p_'s runnable queue into batch.</span></span><br><span class="line"><span class="comment">// Batch is a ring buffer starting at batchHead.</span></span><br><span class="line"><span class="comment">// Returns number of grabbed goroutines.</span></span><br><span class="line"><span class="comment">// Can be executed by any P.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">runqgrab</span><span class="params">(_p_ *p, batch *[256]guintptr, batchHead <span class="keyword">uint32</span>, stealRunNextG <span class="keyword">bool</span>)</span> <span class="title">uint32</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        h := atomic.LoadAcq(&amp;_p_.runqhead) <span class="comment">// load-acquire, synchronize with other consumers</span></span><br><span class="line">        t := atomic.LoadAcq(&amp;_p_.runqtail) <span class="comment">// load-acquire, synchronize with the producer</span></span><br><span class="line">        n := t - h        <span class="comment">//计算队列中有多少个goroutine</span></span><br><span class="line">        n = n - n/<span class="number">2</span>     <span class="comment">//取队列中goroutine个数的一半</span></span><br><span class="line">        <span class="keyword">if</span> n == <span class="number">0</span> &#123;</span><br><span class="line">            ......</span><br><span class="line">            <span class="keyword">return</span> ......</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//小细节：按理说队列中的goroutine个数最多就是len(_p_.runq)，</span></span><br><span class="line">        <span class="comment">//所以n的最大值也就是len(_p_.runq)/2，那为什么需要这个判断呢？</span></span><br><span class="line">        <span class="keyword">if</span> n &gt; <span class="keyword">uint32</span>(<span class="built_in">len</span>(_p_.runq)/<span class="number">2</span>) &#123; <span class="comment">// read inconsistent h and t</span></span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        &#125;</span><br><span class="line">        ......</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>代码中n的计算很简单，从计算过程来看n应该是runq队列中goroutine数量的一半，它的最大值不会超过队列容量的一半，但为什么这里的代码却偏偏要去判断n是否大于队列容量的一半呢？这里关键点在于读取runqhead和runqtail是两个操作而非一个原子操作，当我们读取runqhead之后但还未读取runqtail之前，如果有其它线程快速的在增加（这是完全有可能的，其它偷取者从队列中偷取goroutine会增加runqhead，而队列的所有者往队列中添加goroutine会增加runqtail）这两个值，则会导致我们读取出来的runqtail已经远远大于我们之前读取出来放在局部变量h里面的runqhead了，也就是代码注释中所说的h和t已经不一致了，所以这里需要这个if判断来检测异常情况。</p>
<p>工作线程进入睡眠</p>
<p>分析完盗取过程，我们继续回到findrunnable函数。</p>
<p>如果工作线程经过多次努力一直找不到需要运行的goroutine则调用stopm进入睡眠状态，等待被其它工作线程唤醒。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">1918</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Stops execution of the current m until new work is available.</span></span><br><span class="line"><span class="comment">// Returns with acquired P.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">stopm</span><span class="params">()</span></span> &#123;</span><br><span class="line">    _g_ := getg()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> _g_.m.locks != <span class="number">0</span> &#123;</span><br><span class="line">        throw(<span class="string">"stopm holding locks"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> _g_.m.p != <span class="number">0</span> &#123;</span><br><span class="line">        throw(<span class="string">"stopm holding p"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> _g_.m.spinning &#123;</span><br><span class="line">        throw(<span class="string">"stopm spinning"</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    lock(&amp;sched.lock)</span><br><span class="line">    mput(_g_.m)   <span class="comment">//把m结构体对象放入sched.midle空闲队列</span></span><br><span class="line">    unlock(&amp;sched.lock)</span><br><span class="line">    notesleep(&amp;_g_.m.park)  <span class="comment">//进入睡眠状态</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">//被其它工作线程唤醒</span></span><br><span class="line">    noteclear(&amp;_g_.m.park)</span><br><span class="line">    acquirep(_g_.m.nextp.ptr())</span><br><span class="line">    _g_.m.nextp = <span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>stopm的核心是调用mput把m结构体对象放入sched的midle空闲队列，然后通过notesleep(&amp;m.park)函数让自己进入睡眠状态。</p>
<p>note是go runtime实现的一次性睡眠和唤醒机制，一个线程可以通过调用notesleep(<em>note)进入睡眠状态，而另外一个线程则可以通过notewakeup(</em>note)把其唤醒。note的底层实现机制跟操作系统相关，不同系统使用不同的机制，比如linux下使用的futex系统调用，而mac下则是使用的pthread_cond_t条件变量，note对这些底层机制做了一个抽象和封装，这种封装给扩展性带来了很大的好处，比如当睡眠和唤醒功能需要支持新平台时，只需要在note层增加对特定平台的支持即可，不需要修改上层的任何代码。</p>
<p>回到stopm，当从notesleep函数返回后，需要再次绑定一个p，然后返回到findrunnable函数继续重新寻找可运行的goroutine，一旦找到可运行的goroutine就会返回到schedule函数，并把找到的goroutine调度起来运行，如何把goroutine调度起来运行的代码我们已经分析过了。现在继续看notesleep函数。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/lock_futex.<span class="keyword">go</span> : <span class="number">139</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">notesleep</span><span class="params">(n *note)</span></span> &#123;</span><br><span class="line">    gp := getg()</span><br><span class="line">    <span class="keyword">if</span> gp != gp.m.g0 &#123;</span><br><span class="line">        throw(<span class="string">"notesleep not on g0"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    ns := <span class="keyword">int64</span>(<span class="number">-1</span>)  <span class="comment">//超时时间设置为-1，表示无限期等待</span></span><br><span class="line">    <span class="keyword">if</span> *cgo_yield != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// Sleep for an arbitrary-but-moderate interval to poll libc interceptors.</span></span><br><span class="line">        ns = <span class="number">10e6</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//使用循环，保证不是意外被唤醒</span></span><br><span class="line">    <span class="keyword">for</span> atomic.Load(key32(&amp;n.key)) == <span class="number">0</span> &#123;</span><br><span class="line">        gp.m.blocked = <span class="literal">true</span></span><br><span class="line">        futexsleep(key32(&amp;n.key), <span class="number">0</span>, ns)</span><br><span class="line">        <span class="keyword">if</span> *cgo_yield != <span class="literal">nil</span> &#123;</span><br><span class="line">            asmcgocall(*cgo_yield, <span class="literal">nil</span>)</span><br><span class="line">        &#125;</span><br><span class="line">        gp.m.blocked = <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>notesleep函数调用futexsleep进入睡眠，这里之所以需要用一个循环，是因为futexsleep有可能意外从睡眠中返回，所以从futexsleep函数返回后还需要检查note.key是否还是0，如果是0则表示并不是其它工作线程唤醒了我们，只是futexsleep意外返回了，需要再次调用futexsleep进入睡眠。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">futexsleep调用futex函数进入睡眠。</span><br><span class="line"></span><br><span class="line">runtime/os_linux.<span class="keyword">go</span> : <span class="number">32</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Atomically,</span></span><br><span class="line"><span class="comment">//if(*addr == val) sleep</span></span><br><span class="line"><span class="comment">// Might be woken up spuriously; that's allowed.</span></span><br><span class="line"><span class="comment">// Don't sleep longer than ns; ns &lt; 0 means forever.</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">futexsleep</span><span class="params">(addr *<span class="keyword">uint32</span>, val <span class="keyword">uint32</span>, ns <span class="keyword">int64</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> ts timespec</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Some Linux kernels have a bug where futex of</span></span><br><span class="line">    <span class="comment">// FUTEX_WAIT returns an internal error code</span></span><br><span class="line">    <span class="comment">// as an errno. Libpthread ignores the return value</span></span><br><span class="line">    <span class="comment">// here, and so can we: as it says a few lines up,</span></span><br><span class="line">    <span class="comment">// spurious wakeups are allowed.</span></span><br><span class="line">    <span class="keyword">if</span> ns &lt; <span class="number">0</span> &#123;</span><br><span class="line">        <span class="comment">//调用futex进入睡眠</span></span><br><span class="line">        futex(unsafe.Pointer(addr), _FUTEX_WAIT_PRIVATE, val, <span class="literal">nil</span>, <span class="literal">nil</span>, <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// It's difficult to live within the no-split stack limits here.</span></span><br><span class="line">    <span class="comment">// On ARM and 386, a 64-bit divide invokes a general software routine</span></span><br><span class="line">    <span class="comment">// that needs more stack than we can afford. So we use timediv instead.</span></span><br><span class="line">    <span class="comment">// But on real 64-bit systems, where words are larger but the stack limit</span></span><br><span class="line">    <span class="comment">// is not, even timediv is too heavy, and we really need to use just an</span></span><br><span class="line">    <span class="comment">// ordinary machine instruction.</span></span><br><span class="line">    <span class="keyword">if</span> sys.PtrSize == <span class="number">8</span> &#123;</span><br><span class="line">        ts.set_sec(ns / <span class="number">1000000000</span>)</span><br><span class="line">        ts.set_nsec(<span class="keyword">int32</span>(ns % <span class="number">1000000000</span>))</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        ts.tv_nsec = <span class="number">0</span></span><br><span class="line">        ts.set_sec(<span class="keyword">int64</span>(timediv(ns, <span class="number">1000000000</span>, (*<span class="keyword">int32</span>)(unsafe.Pointer(&amp;ts.tv_nsec)))))</span><br><span class="line">    &#125;</span><br><span class="line">    futex(unsafe.Pointer(addr), _FUTEX_WAIT_PRIVATE, val, unsafe.Pointer(&amp;ts), <span class="literal">nil</span>, <span class="number">0</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>futex是go汇编实现的函数，主要功能就是执行futex系统调用进入操作系统内核进行睡眠。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/sys_linux_amd64.s : <span class="number">525</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// int64 futex(int32 *uaddr, int32 op, int32 val,</span></span><br><span class="line"><span class="comment">//struct timespec *timeout, int32 *uaddr2, int32 val2);</span></span><br><span class="line">TEXT runtime·futex(SB),NOSPLIT,$<span class="number">0</span></span><br><span class="line">   #下面的<span class="number">6</span>条指令在为futex系统调用准备参数</span><br><span class="line">MOVQ  addr+<span class="number">0</span>(FP), DI</span><br><span class="line">MOVL   op+<span class="number">8</span>(FP), SI</span><br><span class="line">MOVL   val+<span class="number">12</span>(FP), DX</span><br><span class="line">MOVQ  ts+<span class="number">16</span>(FP), R10</span><br><span class="line">MOVQ  addr2+<span class="number">24</span>(FP), R8</span><br><span class="line">MOVL   val3+<span class="number">32</span>(FP), R9</span><br><span class="line"></span><br><span class="line">MOVL   $SYS_futex, AX   #系统调用编号放入AX寄存器</span><br><span class="line">SYSCALL  #执行futex系统调用进入睡眠，从睡眠中被唤醒后接着执行下一条MOVL指令</span><br><span class="line">MOVL   AX, ret+<span class="number">40</span>(FP)    #保存系统调用的返回值</span><br><span class="line">RET</span><br><span class="line">futex系统的参数比较多，其函数原型为</span><br><span class="line"></span><br><span class="line"><span class="keyword">int64</span> futex(<span class="keyword">int32</span> *uaddr, <span class="keyword">int32</span> op, <span class="keyword">int32</span> val, <span class="keyword">struct</span> timespec *timeout, <span class="keyword">int32</span> *uaddr2, <span class="keyword">int32</span> val2);</span><br></pre></td></tr></table></figure></p>
<p>这里，futex系统调用为我们提供的功能为如果 <em>uaddr == val 则进入睡眠，否则直接返回。顺便说一下，为什么futex系统调用需要第三个参数val，需要在内核判断</em>uaddr与val是否相等，而不能在用户态先判断它们是否相等，如果相等才进入内核睡眠岂不是更高效？原因在于判断<em>uaddr与val是否相等和进入睡眠这两个操作必须是一个原子操作，否则会存在一个竞态条件：如果不是原子操作，则当前线程在第一步判断完</em>uaddr与val相等之后进入睡眠之前的这一小段时间内，有另外一个线程通过唤醒操作把*uaddr的值修改了，这就会导致当前工作线程永远处于睡眠状态而无人唤醒它。而在用户态无法实现判断与进入睡眠这两步为一个原子操作，所以需要内核来为其实现原子操作。</p>
<p>我们知道线程一旦进入睡眠状态就停止了运行，那么如果后来又有可运行的goroutine需要工作线程去运行，正在睡眠的线程怎么知道有工作可做了呢？</p>
<p>从前面的代码我们已经看到，stopm调用notesleep时给它传递的参数是m结构体的park成员，而m又早已通过mput放入了全局的milde空闲队列，这样其它运行着的线程一旦发现有更多的goroutine需要运行时就可以通过全局的m空闲队列找到处于睡眠状态的m，然后调用notewakeup(&amp;m.park)将其唤醒，至于怎么唤醒，我们在其它章节继续讨论。</p>
<p>到此，我们已经完整分析了调度器的调度策略，从下一章起我们将开始讨论有关调度的另外一个话题：调度时机，即什么时候会发生调度。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>goroutine被动调度之一（18）</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/goroutine%E8%A2%AB%E5%8A%A8%E8%B0%83%E5%BA%A6%E4%B9%8B%E4%B8%80%EF%BC%8818%EF%BC%89/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/w3i5hVKmYW_M06nLaMlwvQ" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-22</p>
<p>本文是《Go语言调度器源代码情景分析》系列的第18篇，也是第四章《Goroutine被动调度》的第1小节。</p>
<p>前一章我们详细分析了调度器的调度策略，即调度器如何选取下一个进入运行的goroutine，但我们还不清楚什么时候以及什么情况下会发生调度，从这一章开始我们就来讨论这个问题。</p>
<p>总体说来，go语言的调度器会在以下三种情况下对goroutine进行调度：</p>
<p>goroutine执行某个操作因条件不满足需要等待而发生的调度；</p>
<p>goroutine主动调用Gosched()函数让出CPU而发生的调度；</p>
<p>goroutine运行时间太长或长时间处于系统调用之中而被调度器剥夺运行权而发生的调度。</p>
<p>本章主要分析我们称之为被动调度的第1种调度，剩下的两种调度将在后面两章分别进行讨论。</p>
<p>Demo例子</p>
<p>我们以一个demo程序为例来分析因阻塞而发生的被动调度。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">start</span><span class="params">(c <span class="keyword">chan</span> <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">    c &lt;- <span class="number">100</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    c := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">int</span>)</span><br><span class="line">    <span class="keyword">go</span> start(c)</span><br><span class="line">    &lt;-c</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>该程序启动时，main goroutine首先会创建一个无缓存的channel，然后启动一个goroutine(为了方便讨论我们称它为g2)向channel发送数据，而main自己则去读取这个channel。</p>
<p>这两个goroutine读写channel时一定会发生一次阻塞，不是main goroutine读取channel时发生阻塞就是g2写入channel时发生阻塞。</p>
<p>创建g2 goroutine</p>
<p>首先用gdb反汇编一下main函数，看看汇编代码。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0x44f4d0 &lt;+0&gt;: mov   %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">0x44f4d9 &lt;+9&gt;: cmp   0x10(%rcx),%rsp</span><br><span class="line">0x44f4dd &lt;+13&gt;: jbe   0x44f549 &lt;main.main+121&gt;</span><br><span class="line">0x44f4df &lt;+15&gt;: sub   <span class="variable">$0x28</span>,%rsp</span><br><span class="line">0x44f4e3 &lt;+19&gt;: mov   %rbp,0x20(%rsp)</span><br><span class="line">0x44f4e8 &lt;+24&gt;: lea   0x20(%rsp),%rbp</span><br><span class="line">0x44f4ed &lt;+29&gt;: lea   0xb36c(%rip),%rax        </span><br><span class="line">0x44f4f4 &lt;+36&gt;: mov   %rax,(%rsp)</span><br><span class="line">0x44f4f8 &lt;+40&gt;: movq   <span class="variable">$0x0</span>,0x8(%rsp)</span><br><span class="line">0x44f501 &lt;+49&gt;: callq    0x404330 &lt;runtime.makechan&gt;  <span class="comment">#创建channel</span></span><br><span class="line">0x44f506 &lt;+54&gt;: mov   0x10(%rsp),%rax</span><br><span class="line">0x44f50b &lt;+59&gt;: mov   %rax,0x18(%rsp)</span><br><span class="line">0x44f510 &lt;+64&gt;: movl   <span class="variable">$0x8</span>,(%rsp)</span><br><span class="line">0x44f517 &lt;+71&gt;: lea   0x240f2(%rip),%rcx        </span><br><span class="line">0x44f51e &lt;+78&gt;: mov   %rcx,0x8(%rsp)</span><br><span class="line">0x44f523 &lt;+83&gt;: callq   0x42c1b0 &lt;runtime.newproc&gt; <span class="comment">#创建goroutine</span></span><br><span class="line">0x44f528 &lt;+88&gt;: mov   0x18(%rsp),%rax</span><br><span class="line">0x44f52d &lt;+93&gt;: mov   %rax,(%rsp)</span><br><span class="line">0x44f531 &lt;+97&gt;: movq   <span class="variable">$0x0</span>,0x8(%rsp)</span><br><span class="line">0x44f53a &lt;+106&gt;: callq   0x405080 &lt;runtime.chanrecv1&gt; <span class="comment">#从channel读取数据</span></span><br><span class="line">0x44f53f &lt;+111&gt;: mov   0x20(%rsp),%rbp</span><br><span class="line">0x44f544 &lt;+116&gt;: add   <span class="variable">$0x28</span>,%rsp</span><br><span class="line">0x44f548 &lt;+120&gt;: retq   </span><br><span class="line">0x44f549 &lt;+121&gt;: callq 0x447390 &lt;runtime.morestack_noctxt&gt;</span><br><span class="line">0x44f54e &lt;+126&gt;: jmp   0x44f4d0 &lt;main.main&gt;</span><br></pre></td></tr></table></figure></p>
<p>从main函数的汇编代码我们可以看到，创建goroutine的go关键字被编译器翻译成了对runtime.newproc函数的调用，第二章我们对这个函数的主要流程做过详细分析，这里简单的回顾一下：</p>
<p>切换到g0栈；</p>
<p>分配g结构体对象；</p>
<p>初始化g对应的栈信息，并把参数拷贝到新g的栈上；</p>
<p>设置好g的sched成员，该成员包括调度g时所必须pc, sp, bp等调度信息；</p>
<p>调用runqput函数把g放入运行队列；</p>
<p>返回</p>
<p>因为当时我们的主要目标是调度器的初始化部分，所以并没有详细分析上述流程中的第5步，也就是runqput是如何把goroutine放入运行队列的，现在就回头分析一下这个过程，下面我们直接从runqput函数开始。</p>
<p>通过runqput函数把goroutine挂入运行队列<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">4746</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// runqput tries to put g on the local runnable queue.</span></span><br><span class="line"><span class="comment">// If next is false, runqput adds g to the tail of the runnable queue.</span></span><br><span class="line"><span class="comment">// If next is true, runqput puts g in the _p_.runnext slot.</span></span><br><span class="line"><span class="comment">// If the run queue is full, runnext puts g on the global queue.</span></span><br><span class="line"><span class="comment">// Executed only by the owner P.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">runqput</span><span class="params">(_p_ *p, gp *g, next <span class="keyword">bool</span>)</span></span>   &#123;</span><br><span class="line">    <span class="keyword">if</span> randomizeScheduler &amp;&amp; next &amp;&amp; fastrand() % <span class="number">2</span> == <span class="number">0</span>  &#123;</span><br><span class="line">        next = <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> next  &#123;</span><br><span class="line">        <span class="comment">//把gp放在_p_.runnext成员里，</span></span><br><span class="line">        <span class="comment">//runnext成员中的goroutine会被优先调度起来运行</span></span><br><span class="line">    retryNext:</span><br><span class="line">        oldnext := _p_.runnext</span><br><span class="line">        <span class="keyword">if</span> !_p_.runnext.cas(oldnext, guintptr(unsafe.Pointer(gp)))  &#123;</span><br><span class="line">             <span class="comment">//有其它线程在操作runnext成员，需要重试</span></span><br><span class="line">            <span class="keyword">goto</span> retryNext</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> oldnext == <span class="number">0</span>  &#123; <span class="comment">//原本runnext为nil，所以没任何事情可做了，直接返回</span></span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// Kick the old runnext out to the regular run queue.</span></span><br><span class="line">        gp = oldnext.ptr() <span class="comment">//原本存放在runnext的gp需要放入runq的尾部</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">retry:</span><br><span class="line">    <span class="comment">//可能有其它线程正在并发修改runqhead成员，所以需要跟其它线程同步</span></span><br><span class="line">    h := atomic.LoadAcq(&amp;_p_.runqhead) <span class="comment">// load-acquire, synchronize with consumers</span></span><br><span class="line">    t := _p_.runqtail</span><br><span class="line">    <span class="keyword">if</span> t - h &lt; <span class="keyword">uint32</span>(<span class="built_in">len</span>(_p_.runq))  &#123; <span class="comment">//判断队列是否满了</span></span><br><span class="line">        <span class="comment">//队列还没有满，可以放入</span></span><br><span class="line">        _p_.runq[t % <span class="keyword">uint32</span>(<span class="built_in">len</span>(_p_.runq))].set(gp)</span><br><span class="line">       </span><br><span class="line">        <span class="comment">// store-release, makes it available for consumption</span></span><br><span class="line">        <span class="comment">//虽然没有其它线程并发修改这个runqtail，但其它线程会并发读取该值以及p的runq成员</span></span><br><span class="line">        <span class="comment">//这里使用StoreRel是为了：</span></span><br><span class="line">        <span class="comment">//1，原子写入runqtail</span></span><br><span class="line">        <span class="comment">//2，防止编译器和CPU乱序，保证上一行代码对runq的修改发生在修改runqtail之前</span></span><br><span class="line">        <span class="comment">//3，可见行屏障，保证当前线程对运行队列的修改对其它线程立马可见</span></span><br><span class="line">        atomic.StoreRel(&amp;_p_.runqtail, t + <span class="number">1</span>) </span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//p的本地运行队列已满，需要放入全局运行队列</span></span><br><span class="line">    <span class="keyword">if</span> runqputslow(_p_, gp, h, t) &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// the queue is not full, now the put above must succeed</span></span><br><span class="line">    <span class="keyword">goto</span> retry</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>runqput函数流程很清晰，它首先尝试把gp放入_p_的本地运行队列，如果本地队列满了，则通过runqputslow函数把gp放入全局运行队列。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">4784</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Put g and a batch of work from local runnable queue on global queue.</span></span><br><span class="line"><span class="comment">// Executed only by the owner P.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">runqputslow</span><span class="params">(_p_ *p, gp *g, h, t <span class="keyword">uint32</span>)</span> <span class="title">bool</span></span>  &#123;</span><br><span class="line">    <span class="keyword">var</span> batch [<span class="built_in">len</span>(_p_.runq) / <span class="number">2</span> + <span class="number">1</span>]*g  <span class="comment">//gp加上_p_本地队列的一半</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// First, grab a batch from local queue.</span></span><br><span class="line">    n := t - h</span><br><span class="line">    n = n / <span class="number">2</span></span><br><span class="line">    <span class="keyword">if</span> n != <span class="keyword">uint32</span>(<span class="built_in">len</span>(_p_.runq) / <span class="number">2</span>)  &#123;</span><br><span class="line">        throw(<span class="string">"runqputslow: queue is not full"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> i := <span class="keyword">uint32</span>(<span class="number">0</span>); i &lt; n; i++ &#123; <span class="comment">//取出p本地队列的一半</span></span><br><span class="line">        batch[i] = _p_.runq[(h+i) % <span class="keyword">uint32</span>(<span class="built_in">len</span>(_p_.runq))].ptr()</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> !atomic.CasRel(&amp;_p_.runqhead, h, h + n)  &#123; <span class="comment">// cas-release, commits consume</span></span><br><span class="line">        <span class="comment">//如果cas操作失败，说明已经有其它工作线程从_p_的本地运行队列偷走了一些goroutine，所以直接返回</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    batch[n] = gp</span><br><span class="line">    <span class="keyword">if</span> randomizeScheduler &#123;</span><br><span class="line">        <span class="keyword">for</span> i := <span class="keyword">uint32</span>(<span class="number">1</span>); i &lt;= n; i++ &#123;</span><br><span class="line">            j := fastrandn(i + <span class="number">1</span>)</span><br><span class="line">            batch[i], batch[j] = batch[j], batch[i]</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// Link the goroutines.</span></span><br><span class="line">    <span class="comment">//全局运行队列是一个链表，这里首先把所有需要放入全局运行队列的g链接起来，</span></span><br><span class="line">    <span class="comment">//减少后面对全局链表的锁住时间，从而降低锁冲突</span></span><br><span class="line">    <span class="keyword">for</span> i := <span class="keyword">uint32</span>(<span class="number">0</span>); i &lt; n; i++  &#123;</span><br><span class="line">        batch[i].schedlink.set(batch[i+<span class="number">1</span>])</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> q gQueue</span><br><span class="line">    q.head.set(batch[<span class="number">0</span>])</span><br><span class="line">    q.tail.set(batch[n])</span><br><span class="line">    <span class="comment">// Now put the batch on global queue.</span></span><br><span class="line">    lock(&amp;sched.lock)</span><br><span class="line">    globrunqputbatch(&amp;q, <span class="keyword">int32</span>(n+<span class="number">1</span>))</span><br><span class="line">    unlock(&amp;sched.lock)</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>runqputslow函数首先使用链表把从_p_的本地队列中取出的一半连同gp一起串联起来，然后在加锁成功之后通过globrunqputbatch函数把该链表链入全局运行队列（全局运行队列是使用链表实现的）。值的一提的是runqputslow函数并没有一开始就把全局运行队列锁住，而是等所有的准备工作做完之后才锁住全局运行队列，这是并发编程加锁的基本原则，需要尽量减小锁的粒度，降低锁冲突的概率。</p>
<p>分析完runqput函数是如何把goroutine放入运行队列之后，接下来我们继续分析main goroutine因读取channel而发生的阻塞流程。</p>
<p>因读取channel阻塞而发生的被动调度</p>
<p>从代码逻辑的角度来说，我们不能确定main goroutine和新创建出来的g2谁先运行，但对于我们分析来说我们可以假定某个goroutine先运行，因为不管谁先运行，都会阻塞在channel的读或则写上，所以这里我们假设main创建好g2后首先阻塞在了对channel的读操作上。下面我们看看读取channel的过程。</p>
<p>从前面的反汇编代码我们知道读取channel是通过调用runtime.chanrecv1函数来完成的，我们就从它开始分析，不过在分析过程中我们不会把精力放在对channel的操作上，而是分析这个过程中跟调度有关的细节。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/<span class="keyword">chan</span>.<span class="keyword">go</span> : <span class="number">403</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// entry points for &lt;- c from compiled code</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">chanrecv1</span><span class="params">(c *hchan, elem unsafe.Pointer)</span></span> &#123;</span><br><span class="line">    chanrecv(c, elem, <span class="literal">true</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// runtime/chan.go : 415</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">chanrecv</span><span class="params">(c *hchan, ep unsafe.Pointer, block <span class="keyword">bool</span>)</span> <span class="params">(selected, received <span class="keyword">bool</span>)</span></span> &#123;</span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">//省略部分的代码逻辑主要在判断读取操作是否可以立即完成，如果不能立即完成</span></span><br><span class="line">    <span class="comment">//就需要把g挂在channel c的读取队列上，然后调用goparkunlock函数阻塞此goroutine</span></span><br><span class="line">    goparkunlock(&amp;c.lock, waitReasonChanReceive, traceEvGoBlockRecv, <span class="number">3</span>)</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>chanrecv1直接调用chanrecv函数实现读取操作，chanrecv首先会判断channel是否有数据可读，如果有数据则直接读取并返回，但如果没有数据，则需要把当前goroutine挂入channel的读取队列之中并调用goparkunlock函数阻塞该goroutine.<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">304</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Puts the current goroutine into a waiting state and unlocks the lock.</span></span><br><span class="line"><span class="comment">// The goroutine can be made runnable again by calling goready(gp).</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">goparkunlock</span><span class="params">(lock *mutex, reason waitReason, traceEv <span class="keyword">byte</span>, traceskip <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">    gopark(parkunlock_c, unsafe.Pointer(lock), reason, traceEv, traceskip)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// runtime/proc.go : 276</span></span><br><span class="line"><span class="comment">// Puts the current goroutine into a waiting state and calls unlockf.</span></span><br><span class="line"><span class="comment">// If unlockf returns false, the goroutine is resumed.</span></span><br><span class="line"><span class="comment">// unlockf must not access this G's stack, as it may be moved between</span></span><br><span class="line"><span class="comment">// the call to gopark and the call to unlockf.</span></span><br><span class="line"><span class="comment">// Reason explains why the goroutine has been parked.</span></span><br><span class="line"><span class="comment">// It is displayed in stack traces and heap dumps.</span></span><br><span class="line"><span class="comment">// Reasons should be unique and descriptive.</span></span><br><span class="line"><span class="comment">// Do not re-use reasons, add new ones.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">gopark</span><span class="params">(unlockf <span class="keyword">func</span>(*g, unsafe.Pointer)</span> <span class="title">bool</span>, <span class="title">lock</span> <span class="title">unsafe</span>.<span class="title">Pointer</span>, <span class="title">reason</span>     <span class="title">waitReason</span>,<span class="title">traceEv</span> <span class="title">byte</span>, <span class="title">traceskip</span> <span class="title">int</span>)</span> &#123;</span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// can't do anything that might move the G between Ms here.</span></span><br><span class="line">    mcall(park_m) <span class="comment">//切换到g0栈执行park_m函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>goparkunlock函数直接调用gopark函数，gopark则调用mcall从当前main goroutine切换到g0去执行park_m函数（mcall前面我们分析过，其主要作用就是保存当前goroutine的现场，然后切换到g0栈去调用作为参数传递给它的函数）<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2581</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// park continuation on g0.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">park_m</span><span class="params">(gp *g)</span></span> &#123;</span><br><span class="line">    _g_ := getg()</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> trace.enabled &#123;</span><br><span class="line">        traceGoPark(_g_.m.waittraceev, _g_.m.waittraceskip)</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    casgstatus(gp, _Grunning, _Gwaiting)</span><br><span class="line">    dropg()  <span class="comment">//解除g和m之间的关系</span></span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">   </span><br><span class="line">    schedule()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>park_m首先把当前goroutine的状态设置为_Gwaiting（因为它正在等待其它goroutine往channel里面写数据），然后调用dropg函数解除g和m之间的关系，最后通过调用schedule函数进入调度循环，schedule函数我们也详细分析过，它首先会从运行队列中挑选出一个goroutine，然后调用gogo函数切换到被挑选出来的goroutine去运行。因为main goroutine在读取channel被阻塞之前已经把创建好的g2放入了运行队列，所以在这里schedule会把g2调度起来运行，这里完成了一次从main goroutine到g2调度（我们假设只有一个工作线程在进行调度）。</p>
<p>唤醒阻塞在channel上的goroutine</p>
<p>g2 goroutine的入口是start函数，下面我们就从该函数开始分析g2写channel的流程，看它如何唤醒正在等待着读取channel的main goroutine。还是先来反汇编一下start函数的代码：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0x44f480 &lt;+0&gt;:mov   %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">0x44f489 &lt;+9&gt;:cmp   0x10(%rcx),%rsp</span><br><span class="line">0x44f48d &lt;+13&gt;:jbe   0x44f4c1 &lt;main.start+65&gt;</span><br><span class="line">0x44f48f &lt;+15&gt;:sub   <span class="variable">$0x18</span>,%rsp</span><br><span class="line">0x44f493 &lt;+19&gt;:mov   %rbp,0x10(%rsp)</span><br><span class="line">0x44f498 &lt;+24&gt;:lea   0x10(%rsp),%rbp</span><br><span class="line">0x44f49d &lt;+29&gt;:mov   0x20(%rsp),%rax</span><br><span class="line">0x44f4a2 &lt;+34&gt;:mov   %rax,(%rsp)</span><br><span class="line">0x44f4a6 &lt;+38&gt;:lea   0x2d71b(%rip),%rax        </span><br><span class="line">0x44f4ad &lt;+45&gt;:mov   %rax,0x8(%rsp)</span><br><span class="line">0x44f4b2 &lt;+50&gt;:callq   0x404560 &lt;runtime.chansend1&gt; <span class="comment">#写channel</span></span><br><span class="line">0x44f4b7 &lt;+55&gt;:mov   0x10(%rsp),%rbp</span><br><span class="line">0x44f4bc &lt;+60&gt;:add   <span class="variable">$0x18</span>,%rsp</span><br><span class="line">0x44f4c0 &lt;+64&gt;:retq   </span><br><span class="line">0x44f4c1 &lt;+65&gt;:callq    0x447390 &lt;runtime.morestack_noctxt&gt;</span><br><span class="line">0x44f4c6 &lt;+70&gt;:jmp   0x44f480 &lt;main.start&gt;</span><br></pre></td></tr></table></figure></p>
<p>可以看到，编译器把对channel的发送操作翻译成了对runtime.chansend1函数的调用<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/<span class="keyword">chan</span>.<span class="keyword">go</span> : <span class="number">124</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// entry point for c &lt;- x from compiled code</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">chansend1</span><span class="params">(c *hchan, elem unsafe.Pointer)</span></span> &#123;</span><br><span class="line">    chansend(c, elem, <span class="literal">true</span>, getcallerpc())</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// runtime/chan.go : 142</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">chansend</span><span class="params">(c *hchan, ep unsafe.Pointer, block <span class="keyword">bool</span>, callerpc <span class="keyword">uintptr</span>)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">    ......</span><br><span class="line">    <span class="keyword">if</span> sg := c.recvq.dequeue(); sg != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">// Found a waiting receiver. We pass the value we want to send</span></span><br><span class="line">        <span class="comment">// directly to the receiver, bypassing the channel buffer (if any).</span></span><br><span class="line">        <span class="comment">//可以直接发送数据给sg</span></span><br><span class="line">        send(c, sg, ep, <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123; unlock(&amp;c.lock) &#125;, <span class="number">3</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// runtime/chan.go : 269</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">send</span><span class="params">(c *hchan, sg *sudog, ep unsafe.Pointer, unlockf <span class="keyword">func</span>()</span>, <span class="title">skip</span> <span class="title">int</span>)</span> &#123;</span><br><span class="line">    ......</span><br><span class="line">    goready(gp, skip+<span class="number">1</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// runtime/proc.go : 310</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">goready</span><span class="params">(gp *g, traceskip <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">    systemstack(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        ready(gp, traceskip, <span class="literal">true</span>)</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>channel发送和读取的流程类似，如果能够立即发送则立即发送并返回，如果不能立即发送则需要阻塞，在我们这个场景中，因为main goroutine此时此刻正挂在channel的读取队列上等待数据，所以这里直接调用send函数发送给main goroutine，send函数则调用goready函数切换到g0栈并调用ready函数来唤醒sg对应的goroutine，即正在等待读channel的main goroutine。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">639</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Mark gp ready to run.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">ready</span><span class="params">(gp *g, traceskip <span class="keyword">int</span>, next <span class="keyword">bool</span>)</span></span> &#123;</span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// Mark runnable.</span></span><br><span class="line">    _g_ := getg()</span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// status is Gwaiting or Gscanwaiting, make Grunnable and put on runq</span></span><br><span class="line">    casgstatus(gp, _Gwaiting, _Grunnable)</span><br><span class="line">    runqput(_g_.m.p.ptr(), gp, next) <span class="comment">//放入运行队列</span></span><br><span class="line">    <span class="keyword">if</span> atomic.Load(&amp;sched.npidle) != <span class="number">0</span> &amp;&amp; atomic.Load(&amp;sched.nmspinning) == <span class="number">0</span> &#123;</span><br><span class="line">        <span class="comment">//有空闲的p而且没有正在偷取goroutine的工作线程，则需要唤醒p出来工作</span></span><br><span class="line">        wakep()</span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>ready函数首先把需要唤醒的goroutine的状态设置为_Grunnable，然后把其放入运行队列之中等待调度器的调度。</p>
<p>对于本章我们分析的场景，执行到这里main goroutine已经被放入了运行队列，但还未被调度起来运行，而g2 goroutine在向channel写完数据之后就从这里的ready函数返回并退出了，从第二章我们对goroutine的退出流程的分析可以得知，在g2的退出过程中将会在goexit0函数中调用schedule函数进入下一轮调度，从而把刚刚放入运行队列的main goroutine调度起来运行。</p>
<p>在上面分析ready函数时我们略过了一种情况：如果当前有空闲的p而且没有工作线程正在尝试从各个工作线程的本地运行队列偷取goroutine的话（没有处于spinning状态的工作线程），那么就需要通过wakep函数把空闲的p唤醒起来工作。为了不让篇幅过长，下一节我们再来分析wakep如何去唤醒和创建新的工作线程。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>goroutine调度器概述(11)</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/goroutine%E8%B0%83%E5%BA%A6%E5%99%A8%E6%A6%82%E8%BF%B0-11/</url>
    <content><![CDATA[<p>原创 <strong>爱写程序的阿波张</strong> <a href="https://mp.weixin.qq.com/s/2wkZyOFAyhHgvNkEkXamkQ" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-01</p>
<p><strong>本文是《go调度器源代码情景分析》系列的第11篇，也是第二章的第1小节。</strong></p>
<p><strong><em>goroutine简介</em></strong></p>
<p>goroutine是Go语言实现的用户态线程，主要用来解决操作系统线程太“重”的问题，所谓的太重，主要表现在以下两个方面：</p>
<p>创建和切换太重：操作系统线程的创建和切换都需要进入内核，而进入内核所消耗的性能代价比较高，开销较大；</p>
<p>内存使用太重：一方面，为了尽量避免极端情况下操作系统线程栈的溢出，内核在创建操作系统线程时默认会为其分配一个较大的栈内存（虚拟地址空间，内核并不会一开始就分配这么多的物理内存），然而在绝大多数情况下，系统线程远远用不了这么多内存，这导致了浪费；另一方面，栈内存空间一旦创建和初始化完成之后其大小就不能再有变化，这决定了在某些特殊场景下系统线程栈还是有溢出的风险。</p>
<p>而相对的，用户态的goroutine则轻量得多：</p>
<p>goroutine是用户态线程，其创建和切换都在用户代码中完成而无需进入操作系统内核，所以其开销要远远小于系统线程的创建和切换；</p>
<p>goroutine启动时默认栈大小只有2k，这在多数情况下已经够用了，即使不够用，goroutine的栈也会自动扩大，同时，如果栈太大了过于浪费它还能自动收缩，这样既没有栈溢出的风险，也不会造成栈内存空间的大量浪费。</p>
<p>正是因为Go语言中实现了如此轻量级的线程，才使得我们在Go程序中，可以轻易的创建成千上万甚至上百万的goroutine出来并发的执行任务而不用太担心性能和内存等问题。</p>
<p>注意：为了避免混淆，从现在开始，后面出现的所有的线程一词均是指操作系统线程，而goroutine我们不再称之为什么什么线程而是直接使用goroutine这个词。</p>
<p><strong><em>线程模型与调度器</em></strong></p>
<p>第一章讨论操作系统线程调度的时候我们曾经提到过，goroutine建立在操作系统线程基础之上，它与操作系统线程之间实现了一个多对多(M:N)的两级线程模型。</p>
<p>这里的 M:N 是指M个goroutine运行在N个操作系统线程之上，内核负责对这N个操作系统线程进行调度，而这N个系统线程又负责对这M个goroutine进行调度和运行。</p>
<p>所谓的对goroutine的调度，是指程序代码按照一定的算法在适当的时候挑选出合适的goroutine并放到CPU上去运行的过程，这些负责对goroutine进行调度的程序代码我们称之为goroutine调度器。用极度简化了的伪代码来描述goroutine调度器的工作流程大概是下面这个样子：</p>
<p>// 程序启动时的初始化代码<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">......</span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; N; i++ &#123; <span class="comment">// 创建N个操作系统线程执行schedule函数</span></span><br><span class="line">    create_os_thread(schedule) <span class="comment">// 创建一个操作系统线程执行schedule函数</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//schedule函数实现调度逻辑</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">schedule</span><span class="params">()</span></span> &#123;</span><br><span class="line">   <span class="keyword">for</span> &#123; <span class="comment">//调度循环</span></span><br><span class="line">         <span class="comment">// 根据某种算法从M个goroutine中找出一个需要运行的goroutine</span></span><br><span class="line">         g := find_a_runnable_goroutine_from_M_goroutines()</span><br><span class="line">         run_g(g) <span class="comment">// CPU运行该goroutine，直到需要调度其它goroutine才返回</span></span><br><span class="line">         save_status_of_g(g) <span class="comment">// 保存goroutine的状态，主要是寄存器的值</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这段伪代码表达的意思是，程序运行起来之后创建了N个由内核调度的操作系统线程（为了方便描述，我们称这些系统线程为工作线程）去执行shedule函数，而schedule函数在一个调度循环中反复从M个goroutine中挑选出一个需要运行的goroutine并跳转到该goroutine去运行，直到需要调度其它goroutine时才返回到schedule函数中通过save_status_of_g保存刚刚正在运行的goroutine的状态然后再次去寻找下一个goroutine。</p>
<p>需要强调的是，这段伪代码对goroutine的调度代码做了高度的抽象、修改和简化处理，放在这里只是为了帮助我们从宏观上了解goroutine的两级调度模型，具体的实现原理和细节将从本章开始进行全面介绍。</p>
<p><strong><em>调度器数据结构概述</em></strong></p>
<p>第一章我们讨论操作系统线程及其调度时还说过，可以把内核对系统线程的调度简单的归纳为：在执行操作系统代码时，内核调度器按照一定的算法挑选出一个线程并把该线程保存在内存之中的寄存器的值放入CPU对应的寄存器从而恢复该线程的运行。</p>
<p>万变不离其宗，系统线程对goroutine的调度与内核对系统线程的调度原理是一样的，实质都是通过保存和修改CPU寄存器的值来达到切换线程/goroutine的目的。</p>
<p>因此，为了实现对goroutine的调度，需要引入一个数据结构来保存CPU寄存器的值以及goroutine的其它一些状态信息，在Go语言调度器源代码中，这个数据结构是一个名叫g的结构体，它保存了goroutine的所有信息，该结构体的每一个实例对象都代表了一个goroutine，调度器代码可以通过g对象来对goroutine进行调度，当goroutine被调离CPU时，调度器代码负责把CPU寄存器的值保存在g对象的成员变量之中，当goroutine被调度起来运行时，调度器代码又负责把g对象的成员变量所保存的寄存器的值恢复到CPU的寄存器。</p>
<p>要实现对goroutine的调度，仅仅有g结构体对象是不够的，至少还需要一个存放所有（可运行）goroutine的容器，便于工作线程寻找需要被调度起来运行的goroutine，于是Go调度器又引入了schedt结构体，一方面用来保存调度器自身的状态信息，另一方面它还拥有一个用来保存goroutine的运行队列。因为每个Go程序只有一个调度器，所以在每个Go程序中schedt结构体只有一个实例对象，该实例对象在源代码中被定义成了一个共享的全局变量，这样每个工作线程都可以访问它以及它所拥有的goroutine运行队列，我们称这个运行队列为全局运行队列。</p>
<p>既然说到全局运行队列，读者可能猜想到应该还有一个局部运行队列。确实如此，因为全局运行队列是每个工作线程都可以读写的，因此访问它需要加锁，然而在一个繁忙的系统中，加锁会导致严重的性能问题。于是，调度器又为每个工作线程引入了一个私有的局部goroutine运行队列，工作线程优先使用自己的局部运行队列，只有必要时才会去访问全局运行队列，这大大减少了锁冲突，提高了工作线程的并发性。在Go调度器源代码中，局部运行队列被包含在p结构体的实例对象之中，每一个运行着go代码的工作线程都会与一个p结构体的实例对象关联在一起。</p>
<p>除了上面介绍的g、schedt和p结构体，Go调度器源代码中还有一个用来代表工作线程的m结构体，每个工作线程都有唯一的一个m结构体的实例对象与之对应，m结构体对象除了记录着工作线程的诸如栈的起止位置、当前正在执行的goroutine以及是否空闲等等状态信息之外，还通过指针维持着与p结构体的实例对象之间的绑定关系。于是，通过m既可以找到与之对应的工作线程正在运行的goroutine，又可以找到工作线程的局部运行队列等资源。下面是g、p、m和schedt之间的关系图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/eefded6bac4850334088848174587557.png" alt></p>
<p>上图中圆形图案代表g结构体的实例对象，三角形代表m结构体的实例对象，正方形代表p结构体的实例对象，其中红色的g表示m对应的工作线程正在运行的goroutine，而灰色的g表示处于运行队列之中正在等待被调度起来运行的goroutine。</p>
<p>从上图可以看出，每个m都绑定了一个p，每个p都有一个私有的本地goroutine队列，m对应的线程从本地和全局goroutine队列中获取goroutine并运行之。</p>
<p>前面我们说每个工作线程都有一个m结构体对象与之对应，但并未详细说明它们之间是如何对应起来的，工作线程执行的代码是如何找到属于自己的那个m结构体实例对象的呢？</p>
<p>如果只有一个工作线程，那么就只会有一个m结构体对象，问题就很简单，定义一个全局的m结构体变量就行了。可是我们有多个工作线程和多个m需要一一对应，怎么办呢？还记得第一章我们讨论过的线程本地存储吗？当时我们说过，线程本地存储其实就是线程私有的全局变量，这不正是我们所需要的吗？！只要每个工作线程拥有了各自私有的m结构体全局变量，我们就能在不同的工作线程中使用相同的全局变量名来访问不同的m结构体对象，这完美的解决我们的问题。</p>
<p>具体到goroutine调度器代码，每个工作线程在刚刚被创建出来进入调度循环之前就利用线程本地存储机制为该工作线程实现了一个指向m结构体实例对象的私有全局变量，这样在之后的代码中就使用该全局变量来访问自己的m结构体对象以及与m相关联的p和g对象。</p>
<p>有了上述数据结构以及工作线程与数据结构之间的映射机制，我们可以把前面的调度伪代码写得更丰满一点：</p>
<p>// 程序启动时的初始化代码<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">......</span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; N; i++ &#123; <span class="comment">// 创建N个操作系统线程执行schedule函数</span></span><br><span class="line">     create_os_thread(schedule) <span class="comment">// 创建一个操作系统线程执行schedule函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>// 定义一个线程私有全局变量，注意它是一个指向m结构体对象的指针<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ThreadLocal用来定义线程私有全局变量</span></span><br><span class="line">ThreadLocal self *m </span><br><span class="line"><span class="comment">//schedule函数实现调度逻辑</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">schedule</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">// 创建和初始化m结构体对象，并赋值给私有全局变量self</span></span><br><span class="line">    self = initm()   </span><br><span class="line">    <span class="keyword">for</span> &#123; <span class="comment">//调度循环</span></span><br><span class="line">          <span class="keyword">if</span> (self.p.runqueue is empty) &#123;</span><br><span class="line">                 <span class="comment">// 根据某种算法从全局运行队列中找出一个需要运行的goroutine</span></span><br><span class="line">                 g := find_a_runnable_goroutine_from_global_runqueue()</span><br><span class="line">           &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                 <span class="comment">// 根据某种算法从私有的局部运行队列中找出一个需要运行的goroutine</span></span><br><span class="line">                 g := find_a_runnable_goroutine_from_local_runqueue()</span><br><span class="line">           &#125;</span><br><span class="line">          run_g(g) <span class="comment">// CPU运行该goroutine，直到需要调度其它goroutine才返回</span></span><br><span class="line">          save_status_of_g(g) <span class="comment">// 保存goroutine的状态，主要是寄存器的值</span></span><br><span class="line">     &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>仅仅从上面这个伪代码来看，我们完全不需要线程私有全局变量，只需在schedule函数中定义一个局部变量就行了。但真实的调度代码错综复杂，不光是这个schedule函数会需要访问m，其它很多地方还需要访问它，所以需要使用全局变量来方便其它地方对m的以及与m相关的g和p的访问。</p>
<p>在简单的介绍了Go语言调度器以及它所需要的数据结构之后，下面我们来看一下Go的调度代码中对上述的几个结构体的定义。</p>
<p>重要的结构体<br>下面介绍的这些结构体中的字段非常多，牵涉到的细节也很庞杂，光是看这些结构体的定义我们没有必要也无法真正理解它们的用途，所以在这里我们只需要大概了解一下就行了，看不懂记不住都没有关系，随着后面对代码逐步深入的分析，我们也必将会对这些结构体有越来越清晰的认识。为了节省篇幅，下面各结构体的定义略去了跟调度器无关的成员。另外，这些结构体的定义全部位于Go语言的源代码路径下的runtime/runtime2.go文件之中。</p>
<p>stack结构体</p>
<p>stack结构体主要用来记录goroutine所使用的栈的信息，包括栈顶和栈底位置：</p>
<p>// Stack describes a Go execution stack.<br>// The bounds of the stack are exactly [lo, hi),<br>// with no implicit data structures on either side.<br>//用于记录goroutine使用的栈的起始和结束位置<br>type stack struct {<br>    lo uintptr    // 栈顶，指向内存低地址<br>    hi uintptr    // 栈底，指向内存高地址<br>}<br>gobuf结构体</p>
<p>gobuf结构体用于保存goroutine的调度信息，主要包括CPU的几个寄存器的值：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> gobuf <span class="keyword">struct</span> &#123;</span><br><span class="line">    <span class="comment">// The offsets of sp, pc, and g are known to (hard-coded in) libmach.</span></span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="comment">// ctxt is unusual with respect to GC: it may be a</span></span><br><span class="line">    <span class="comment">// heap-allocated funcval, so GC needs to track it, but it</span></span><br><span class="line">    <span class="comment">// needs to be set and cleared from assembly, where it's</span></span><br><span class="line">    <span class="comment">// difficult to have write barriers. However, ctxt is really a</span></span><br><span class="line">    <span class="comment">// saved, live register, and we only ever exchange it between</span></span><br><span class="line">    <span class="comment">// the real register and the gobuf. Hence, we treat it as a</span></span><br><span class="line">    <span class="comment">// root during stack scanning, which means assembly that saves</span></span><br><span class="line">    <span class="comment">// and restores it doesn't need write barriers. It's still</span></span><br><span class="line">    <span class="comment">// typed as a pointer so that any other writes from Go get</span></span><br><span class="line">    <span class="comment">// write barriers.</span></span><br><span class="line">    sp   <span class="keyword">uintptr</span>  <span class="comment">// 保存CPU的rsp寄存器的值</span></span><br><span class="line">    pc   <span class="keyword">uintptr</span>  <span class="comment">// 保存CPU的rip寄存器的值</span></span><br><span class="line">    g    guintptr <span class="comment">// 记录当前这个gobuf对象属于哪个goroutine</span></span><br><span class="line">    ctxt unsafe.Pointer</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 保存系统调用的返回值，因为从系统调用返回之后如果p被其它工作线程抢占，</span></span><br><span class="line">    <span class="comment">// 则这个goroutine会被放入全局运行队列被其它工作线程调度，其它线程需要知道系统调用的返回值。</span></span><br><span class="line">    ret  sys.Uintreg  </span><br><span class="line">    lr   <span class="keyword">uintptr</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 保存CPU的rip寄存器的值</span></span><br><span class="line">    bp   <span class="keyword">uintptr</span> <span class="comment">// for GOEXPERIMENT=framepointer</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>g结构体</p>
<p>g结构体用于代表一个goroutine，该结构体保存了goroutine的所有信息，包括栈，gobuf结构体和其它的一些状态信息：</p>
<p>// 前文所说的g结构体，它代表了一个goroutine<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> g <span class="keyword">struct</span> &#123;</span><br><span class="line">    <span class="comment">// Stack parameters.</span></span><br><span class="line">    <span class="comment">// stack describes the actual stack memory: [stack.lo, stack.hi).</span></span><br><span class="line">    <span class="comment">// stackguard0 is the stack pointer compared in the Go stack growth prologue.</span></span><br><span class="line">    <span class="comment">// It is stack.lo+StackGuard normally, but can be StackPreempt to trigger a preemption.</span></span><br><span class="line">    <span class="comment">// stackguard1 is the stack pointer compared in the C stack growth prologue.</span></span><br><span class="line">    <span class="comment">// It is stack.lo+StackGuard on g0 and gsignal stacks.</span></span><br><span class="line">    <span class="comment">// It is ~0 on other goroutine stacks, to trigger a call to morestackc (and crash).</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 记录该goroutine使用的栈</span></span><br><span class="line">    stack       stack   <span class="comment">// offset known to runtime/cgo</span></span><br><span class="line">    <span class="comment">// 下面两个成员用于栈溢出检查，实现栈的自动伸缩，抢占调度也会用到stackguard0</span></span><br><span class="line">    stackguard0 <span class="keyword">uintptr</span> <span class="comment">// offset known to liblink</span></span><br><span class="line">    stackguard1 <span class="keyword">uintptr</span> <span class="comment">// offset known to liblink</span></span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 此goroutine正在被哪个工作线程执行</span></span><br><span class="line">    m              *m      <span class="comment">// current m; offset known to arm liblink</span></span><br><span class="line">    <span class="comment">// 保存调度信息，主要是几个寄存器的值</span></span><br><span class="line">    sched          gobuf</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// schedlink字段指向全局运行队列中的下一个g，</span></span><br><span class="line">    <span class="comment">//所有位于全局运行队列中的g形成一个链表</span></span><br><span class="line">    schedlink      guintptr</span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// 抢占调度标志，如果需要抢占调度，设置preempt为true</span></span><br><span class="line">    preempt        <span class="keyword">bool</span>       <span class="comment">// preemption signal, duplicates stackguard0 = stackpreempt</span></span><br><span class="line"></span><br><span class="line">   ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>m结构体</p>
<p>m结构体用来代表工作线程，它保存了m自身使用的栈信息，当前正在运行的goroutine以及与m绑定的p等信息，详见下面定义中的注释：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> m <span class="keyword">struct</span> &#123;</span><br><span class="line">    <span class="comment">// g0主要用来记录工作线程使用的栈信息，在执行调度代码时需要使用这个栈</span></span><br><span class="line">    <span class="comment">// 执行用户goroutine代码时，使用用户goroutine自己的栈，调度时会发生栈的切换</span></span><br><span class="line">    g0      *g     <span class="comment">// goroutine with scheduling stack</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 通过TLS实现m结构体对象与工作线程之间的绑定</span></span><br><span class="line">    tls           [<span class="number">6</span>]<span class="keyword">uintptr</span>   <span class="comment">// thread-local storage (for x86 extern register)</span></span><br><span class="line">    mstartfn      <span class="function"><span class="keyword">func</span><span class="params">()</span></span></span><br><span class="line">    <span class="comment">// 指向工作线程正在运行的goroutine的g结构体对象</span></span><br><span class="line">    curg          *g       <span class="comment">// current running goroutine</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 记录与当前工作线程绑定的p结构体对象</span></span><br><span class="line">    p             puintptr <span class="comment">// attached p for executing go code (nil if not executing go code)</span></span><br><span class="line">    nextp         puintptr</span><br><span class="line">    oldp          puintptr <span class="comment">// the p that was attached before executing a syscall</span></span><br><span class="line">   </span><br><span class="line">    <span class="comment">// spinning状态：表示当前工作线程正在试图从其它工作线程的本地运行队列偷取goroutine</span></span><br><span class="line">    spinning      <span class="keyword">bool</span> <span class="comment">// m is out of work and is actively looking for work</span></span><br><span class="line">    blocked       <span class="keyword">bool</span> <span class="comment">// m is blocked on a note</span></span><br><span class="line">   </span><br><span class="line">    <span class="comment">// 没有goroutine需要运行时，工作线程睡眠在这个park成员上，</span></span><br><span class="line">    <span class="comment">// 其它线程通过这个park唤醒该工作线程</span></span><br><span class="line">    park          note</span><br><span class="line">    <span class="comment">// 记录所有工作线程的一个链表</span></span><br><span class="line">    alllink       *m <span class="comment">// on allm</span></span><br><span class="line">    schedlink     muintptr</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Linux平台thread的值就是操作系统线程ID</span></span><br><span class="line">    thread        <span class="keyword">uintptr</span> <span class="comment">// thread handle</span></span><br><span class="line">    freelink      *m      <span class="comment">// on sched.freem</span></span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>p结构体</p>
<p>p结构体用于保存工作线程执行go代码时所必需的资源，比如goroutine的运行队列，内存分配用到的缓存等等。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> p <span class="keyword">struct</span> &#123;</span><br><span class="line">    lock mutex</span><br><span class="line"></span><br><span class="line">    status       <span class="keyword">uint32</span> <span class="comment">// one of pidle/prunning/...</span></span><br><span class="line">    link            puintptr</span><br><span class="line">    schedtick   <span class="keyword">uint32</span>     <span class="comment">// incremented on every scheduler call</span></span><br><span class="line">    syscalltick  <span class="keyword">uint32</span>     <span class="comment">// incremented on every system call</span></span><br><span class="line">    sysmontick  sysmontick <span class="comment">// last tick observed by sysmon</span></span><br><span class="line">    m                muintptr   <span class="comment">// back-link to associated m (nil if idle)</span></span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Queue of runnable goroutines. Accessed without lock.</span></span><br><span class="line">    <span class="comment">//本地goroutine运行队列</span></span><br><span class="line">    runqhead <span class="keyword">uint32</span>  <span class="comment">// 队列头</span></span><br><span class="line">    runqtail <span class="keyword">uint32</span>     <span class="comment">// 队列尾</span></span><br><span class="line">    runq     [<span class="number">256</span>]guintptr  <span class="comment">//使用数组实现的循环队列</span></span><br><span class="line">    <span class="comment">// runnext, if non-nil, is a runnable G that was ready'd by</span></span><br><span class="line">    <span class="comment">// the current G and should be run next instead of what's in</span></span><br><span class="line">    <span class="comment">// runq if there's time remaining in the running G's time</span></span><br><span class="line">    <span class="comment">// slice. It will inherit the time left in the current time</span></span><br><span class="line">    <span class="comment">// slice. If a set of goroutines is locked in a</span></span><br><span class="line">    <span class="comment">// communicate-and-wait pattern, this schedules that set as a</span></span><br><span class="line">    <span class="comment">// unit and eliminates the (potentially large) scheduling</span></span><br><span class="line">    <span class="comment">// latency that otherwise arises from adding the ready'd</span></span><br><span class="line">    <span class="comment">// goroutines to the end of the run queue.</span></span><br><span class="line">    runnext guintptr</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Available G's (status == Gdead)</span></span><br><span class="line">    gFree <span class="keyword">struct</span> &#123;</span><br><span class="line">        gList</span><br><span class="line">        n <span class="keyword">int32</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>schedt结构体</p>
<p>schedt结构体用来保存调度器的状态信息和goroutine的全局运行队列：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> schedt <span class="keyword">struct</span> &#123;</span><br><span class="line">    <span class="comment">// accessed atomically. keep at top to ensure alignment on 32-bit systems.</span></span><br><span class="line">    goidgen  <span class="keyword">uint64</span></span><br><span class="line">    lastpoll <span class="keyword">uint64</span></span><br><span class="line"></span><br><span class="line">    lock mutex</span><br><span class="line"></span><br><span class="line">    <span class="comment">// When increasing nmidle, nmidlelocked, nmsys, or nmfreed, be</span></span><br><span class="line">    <span class="comment">// sure to call checkdead().</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 由空闲的工作线程组成链表</span></span><br><span class="line">    midle        muintptr <span class="comment">// idle m's waiting for work</span></span><br><span class="line">    <span class="comment">// 空闲的工作线程的数量</span></span><br><span class="line">    nmidle       <span class="keyword">int32</span>    <span class="comment">// number of idle m's waiting for work</span></span><br><span class="line">    nmidlelocked <span class="keyword">int32</span>    <span class="comment">// number of locked m's waiting for work</span></span><br><span class="line">    mnext        <span class="keyword">int64</span>    <span class="comment">// number of m's that have been created and next M ID</span></span><br><span class="line">    <span class="comment">// 最多只能创建maxmcount个工作线程</span></span><br><span class="line">    maxmcount    <span class="keyword">int32</span>    <span class="comment">// maximum number of m's allowed (or die)</span></span><br><span class="line">    nmsys        <span class="keyword">int32</span>    <span class="comment">// number of system m's not counted for deadlock</span></span><br><span class="line">    nmfreed      <span class="keyword">int64</span>    <span class="comment">// cumulative number of freed m's</span></span><br><span class="line"></span><br><span class="line">    ngsys <span class="keyword">uint32</span> <span class="comment">// number of system goroutines; updated atomically</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 由空闲的p结构体对象组成的链表</span></span><br><span class="line">    pidle      puintptr <span class="comment">// idle p's</span></span><br><span class="line">    <span class="comment">// 空闲的p结构体对象的数量</span></span><br><span class="line">    npidle     <span class="keyword">uint32</span></span><br><span class="line">    nmspinning <span class="keyword">uint32</span> <span class="comment">// See "Worker thread parking/unparking" comment in proc.go.</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Global runnable queue.</span></span><br><span class="line">    <span class="comment">// goroutine全局运行队列</span></span><br><span class="line">    runq     gQueue</span><br><span class="line">    runqsize <span class="keyword">int32</span></span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Global cache of dead G's.</span></span><br><span class="line">    <span class="comment">// gFree是所有已经退出的goroutine对应的g结构体对象组成的链表</span></span><br><span class="line">    <span class="comment">// 用于缓存g结构体对象，避免每次创建goroutine时都重新分配内存</span></span><br><span class="line">    gFree <span class="keyword">struct</span> &#123;</span><br><span class="line">        lock          mutex</span><br><span class="line">        stack        gList <span class="comment">// Gs with stacks</span></span><br><span class="line">        noStack   gList <span class="comment">// Gs without stacks</span></span><br><span class="line">        n              <span class="keyword">int32</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>重要的全局变量<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">allgs     []*g     <span class="comment">// 保存所有的g</span></span><br><span class="line">allm       *m    <span class="comment">// 所有的m构成的一个链表，包括下面的m0</span></span><br><span class="line">allp       []*p    <span class="comment">// 保存所有的p，len(allp) == gomaxprocs</span></span><br><span class="line"></span><br><span class="line">ncpu             <span class="keyword">int32</span>   <span class="comment">// 系统中cpu核的数量，程序启动时由runtime代码初始化</span></span><br><span class="line">gomaxprocs <span class="keyword">int32</span>   <span class="comment">// p的最大值，默认等于ncpu，但可以通过GOMAXPROCS修改</span></span><br><span class="line"></span><br><span class="line">sched      schedt     <span class="comment">// 调度器结构体对象，记录了调度器的工作状态</span></span><br><span class="line"></span><br><span class="line">m0  m       <span class="comment">// 代表进程的主线程</span></span><br><span class="line">g0   g        <span class="comment">// m0的g0，也就是m0.g0 = &amp;g0</span></span><br></pre></td></tr></table></figure></p>
<p>在程序初始化时，这些全变量都会被初始化为0值，指针会被初始化为nil指针，切片初始化为nil切片，int被初始化为数字0，结构体的所有成员变量按其本类型初始化为其类型的0值。所以程序刚启动时allgs，allm和allp都不包含任何g,m和p。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>因goroutine运行时间过长而发生的抢占调度（21）</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/%E5%9B%A0goroutine%E8%BF%90%E8%A1%8C%E6%97%B6%E9%97%B4%E8%BF%87%E9%95%BF%E8%80%8C%E5%8F%91%E7%94%9F%E7%9A%84%E6%8A%A2%E5%8D%A0%E8%B0%83%E5%BA%A6%EF%BC%8821%EF%BC%89/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/i8DQ0HrO5Bt-qE5cX7c1ng" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-28</p>
<p>本文是《Go语言调度器源代码情景分析》系列的第21篇，也是第六章《抢占调度》的第1小节。</p>
<p>前面几节我们分析了Goroutine因读写channel等阻塞而导致的被动调度以及通过调用Gosched函数发起的主动调度，现在还剩下最后一种调度方式即抢占调度未讨论，从本节开始，我们就来对它进行分析。</p>
<p>本小节我们需要重点关注：</p>
<p>什么情况下会发生抢占调度；</p>
<p>因运行时间过长而发生的抢占调度有什么特点。</p>
<p>retake函数</p>
<p>在分析调度器初始化的时候我们说过，sysmon系统监控线程会定期（10毫秒）通过retake函数对goroutine发起抢占，下面我们直接从retake函数开始。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">4376</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// forcePreemptNS is the time slice given to a G before it is</span></span><br><span class="line"><span class="comment">// preempted.</span></span><br><span class="line"><span class="keyword">const</span> forcePreemptNS = <span class="number">10</span> * <span class="number">1000</span> * <span class="number">1000</span> <span class="comment">// 10ms</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">retake</span><span class="params">(now <span class="keyword">int64</span>)</span> <span class="title">uint32</span></span> &#123;</span><br><span class="line">    n := <span class="number">0</span></span><br><span class="line">    <span class="comment">// Prevent allp slice changes. This lock will be completely</span></span><br><span class="line">    <span class="comment">// uncontended unless we're already stopping the world.</span></span><br><span class="line">    lock(&amp;allpLock)</span><br><span class="line">    <span class="comment">// We can't use a range loop over allp because we may</span></span><br><span class="line">    <span class="comment">// temporarily drop the allpLock. Hence, we need to re-fetch</span></span><br><span class="line">    <span class="comment">// allp each time around the loop.</span></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="built_in">len</span>(allp); i++ &#123; <span class="comment">//遍历所有的P</span></span><br><span class="line">        _p_ := allp[i]</span><br><span class="line">        <span class="keyword">if</span> _p_ == <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="comment">// This can happen if procresize has grown</span></span><br><span class="line">            <span class="comment">// allp but not yet created new Ps.</span></span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        &#125;</span><br><span class="line">       </span><br><span class="line">        <span class="comment">//_p_.sysmontick用于sysmon线程记录被监控p的系统调用时间和运行时间</span></span><br><span class="line">        pd := &amp;_p_.sysmontick</span><br><span class="line">        s := _p_.status</span><br><span class="line">        <span class="keyword">if</span> s == _Psyscall &#123; <span class="comment">//P处于系统调用之中，需要检查是否需要抢占</span></span><br><span class="line">            <span class="comment">// Retake P from syscall if it's there for more than 1 sysmon tick (at least 20us).</span></span><br><span class="line">            t := <span class="keyword">int64</span>(_p_.syscalltick)</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">int64</span>(pd.syscalltick) != t &#123;</span><br><span class="line">                pd.syscalltick = <span class="keyword">uint32</span>(t)</span><br><span class="line">                pd.syscallwhen = now</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// On the one hand we don't want to retake Ps if there is no other work to do,</span></span><br><span class="line">            <span class="comment">// but on the other hand we want to retake them eventually</span></span><br><span class="line">            <span class="comment">// because they can prevent the sysmon thread from deep sleep.</span></span><br><span class="line">            <span class="keyword">if</span> runqempty(_p_) &amp;&amp;  atomic.Load(&amp;sched.nmspinning)+atomic.Load(&amp;sched.npidle)&gt; <span class="number">0</span> &amp;&amp; pd.syscallwhen+<span class="number">10</span>*<span class="number">1000</span>*<span class="number">1000</span> &gt; now &#123;</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// Drop allpLock so we can take sched.lock.</span></span><br><span class="line">            unlock(&amp;allpLock)</span><br><span class="line">            <span class="comment">// Need to decrement number of idle locked M's</span></span><br><span class="line">            <span class="comment">// (pretending that one more is running) before the CAS.</span></span><br><span class="line">            <span class="comment">// Otherwise the M from which we retake can exit the syscall,</span></span><br><span class="line">            <span class="comment">// increment nmidle and report deadlock.</span></span><br><span class="line">            incidlelocked(<span class="number">-1</span>)</span><br><span class="line">            <span class="keyword">if</span> atomic.Cas(&amp;_p_.status, s, _Pidle) &#123;</span><br><span class="line">                <span class="keyword">if</span> trace.enabled &#123;</span><br><span class="line">                    traceGoSysBlock(_p_)</span><br><span class="line">                    traceProcStop(_p_)</span><br><span class="line">                &#125;</span><br><span class="line">                n++</span><br><span class="line">                _p_.syscalltick++</span><br><span class="line">                handoffp(_p_)</span><br><span class="line">            &#125;</span><br><span class="line">            incidlelocked(<span class="number">1</span>)</span><br><span class="line">            lock(&amp;allpLock)</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> s == _Prunning &#123; <span class="comment">//P处于运行状态，需要检查其是否运行得太久了</span></span><br><span class="line">            <span class="comment">// Preempt G if it's running for too long.</span></span><br><span class="line">            <span class="comment">//_p_.schedtick：每发生一次调度，调度器++该值</span></span><br><span class="line">            t := <span class="keyword">int64</span>(_p_.schedtick)</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">int64</span>(pd.schedtick) != t &#123;</span><br><span class="line">                <span class="comment">//监控线程监控到一次新的调度，所以重置跟sysmon相关的schedtick和schedwhen变量</span></span><br><span class="line">                pd.schedtick = <span class="keyword">uint32</span>(t)</span><br><span class="line">                pd.schedwhen = now</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            &#125;</span><br><span class="line">           </span><br><span class="line">            <span class="comment">//pd.schedtick == t说明(pd.schedwhen ～ now)这段时间未发生过调度，</span></span><br><span class="line">            <span class="comment">//所以这段时间是同一个goroutine一直在运行，下面检查一直运行是否超过了10毫秒</span></span><br><span class="line">            <span class="keyword">if</span> pd.schedwhen+forcePreemptNS &gt; now &#123;</span><br><span class="line">                <span class="comment">//从某goroutine第一次被sysmon线程监控到正在运行一直运行到现在还未超过10毫秒</span></span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//连续运行超过10毫秒了，设置抢占请求</span></span><br><span class="line">            preemptone(_p_)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    unlock(&amp;allpLock)</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">uint32</span>(n)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>从代码可以看出，retake函数会根据p的两种不同状态检查是否需要抢占：</p>
<p>_Prunning，表示对应的goroutine正在运行，如果其运行时间超过了10毫秒则对需要抢占；</p>
<p>_Psyscall，表示对应的goroutine正在内核执行系统调用，此时需要根据多个条件来判断是否需要抢占。这些判断我们会在后面进行详细描述。</p>
<p>我们首先来分析由于goroutine运行时间过长而导致的抢占，然后分析goroutine进入系统调用之后发生的抢占。</p>
<p>监控线程提出抢占请求</p>
<p>sysmon线程如果监控到某个goroutine连续运行超过了10毫秒（具体是如何监控到的可以看上面代码中笔者的注释），则会调用preemptone函数向该goroutine发出抢占请求。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">4465</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Tell the goroutine running on processor P to stop.</span></span><br><span class="line"><span class="comment">// This function is purely best-effort. It can incorrectly fail to inform the</span></span><br><span class="line"><span class="comment">// goroutine. It can send inform the wrong goroutine. Even if it informs the</span></span><br><span class="line"><span class="comment">// correct goroutine, that goroutine might ignore the request if it is</span></span><br><span class="line"><span class="comment">// simultaneously executing newstack.</span></span><br><span class="line"><span class="comment">// No lock needs to be held.</span></span><br><span class="line"><span class="comment">// Returns true if preemption request was issued.</span></span><br><span class="line"><span class="comment">// The actual preemption will happen at some point in the future</span></span><br><span class="line"><span class="comment">// and will be indicated by the gp-&gt;status no longer being</span></span><br><span class="line"><span class="comment">// Grunning</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">preemptone</span><span class="params">(_p_ *p)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">    mp := _p_.m.ptr()</span><br><span class="line">    <span class="keyword">if</span> mp == <span class="literal">nil</span> || mp == getg().m &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//gp是被抢占的goroutine</span></span><br><span class="line">    gp := mp.curg</span><br><span class="line">    <span class="keyword">if</span> gp == <span class="literal">nil</span> || gp == mp.g0 &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    gp.preempt = <span class="literal">true</span>  <span class="comment">//设置抢占标志</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Every call in a go routine checks for stack overflow by</span></span><br><span class="line">    <span class="comment">// comparing the current stack pointer to gp-&gt;stackguard0.</span></span><br><span class="line">    <span class="comment">// Setting gp-&gt;stackguard0 to StackPreempt folds</span></span><br><span class="line">    <span class="comment">// preemption into the normal stack overflow check.</span></span><br><span class="line">    <span class="comment">//stackPreempt是一个常量0xfffffffffffffade，是非常大的一个数</span></span><br><span class="line">    gp.stackguard0 = stackPreempt  <span class="comment">//设置stackguard0使被抢占的goroutine去处理抢占请求</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看出，preemptone函数只是简单的设置了被抢占goroutine对应的g结构体中的 preempt成员为true和stackguard0成员为stackPreempt（stackPreempt是一个常量0xfffffffffffffade，是非常大的一个数）就返回了，并未真正强制被抢占的goroutine暂停下来。</p>
<p>既然设置了一些抢占标志，那么就一定需要对这些标志进行处理，下面我们就来分析被抢占的goroutine如何处理这些标志去响应监控线程提出的抢占请求。</p>
<p>响应抢占请求</p>
<p>因为我们并不知道什么地方会对抢占标志进行处理，所以我们首先使用文本搜索工具在源代码中查找”stackPreempt”、”stackguard0”以及”preempt”这3个字符串，可以找到处理抢占请求的函数为newstack()，在该函数中如果发现自己被抢占，则会暂停当前goroutine的执行。然后再查找哪些函数会调用newstack函数，顺藤摸瓜便可以找到相关的函数调用链为</p>
<p>morestack_noctxt()-&gt;morestack()-&gt;newstack()<br>从源代码中morestack函数的注释可以知道，该函数会被编译器自动插入到函数序言(prologue)中。我们以下面这个程序为例来做进一步的说明。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">sum</span><span class="params">(a, b <span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line">    a2 := a * a</span><br><span class="line">    b2 := b * b</span><br><span class="line">    c := a2 + b2</span><br><span class="line"> </span><br><span class="line">    fmt.Println(c)</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> c</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    sum(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>为了看清楚编译器会把对morestack函数的调用插入到什么地方，我们用gdb来反汇编一下main函数：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">=&gt; 0x0000000000486a80 &lt;+0&gt;:   mov   %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">     0x0000000000486a89 &lt;+9&gt;:   cmp   0x10(%rcx),%rsp</span><br><span class="line">     0x0000000000486a8d &lt;+13&gt;:  jbe   0x486abd &lt;main.main+61&gt;</span><br><span class="line">     0x0000000000486a8f &lt;+15&gt;:  sub   <span class="variable">$0x20</span>,%rsp</span><br><span class="line">     0x0000000000486a93 &lt;+19&gt;: mov   %rbp,0x18(%rsp)</span><br><span class="line">     0x0000000000486a98 &lt;+24&gt;: lea   0x18(%rsp),%rbp</span><br><span class="line">     0x0000000000486a9d &lt;+29&gt;: movq   <span class="variable">$0x1</span>,(%rsp)</span><br><span class="line">     0x0000000000486aa5 &lt;+37&gt;: movq   <span class="variable">$0x2</span>,0x8(%rsp)</span><br><span class="line">     0x0000000000486aae &lt;+46&gt;: callq   0x4869c0 &lt;main.sum&gt;</span><br><span class="line">     0x0000000000486ab3 &lt;+51&gt;: mov   0x18(%rsp),%rbp</span><br><span class="line">     0x0000000000486ab8 &lt;+56&gt;: add   <span class="variable">$0x20</span>,%rsp</span><br><span class="line">     0x0000000000486abc &lt;+60&gt;: retq   </span><br><span class="line">     0x0000000000486abd &lt;+61&gt;: callq  0x44ece0 &lt;runtime.morestack_noctxt&gt;</span><br><span class="line">     0x0000000000486ac2 &lt;+66&gt;: jmp   0x486a80 &lt;main.main&gt;</span><br></pre></td></tr></table></figure></p>
<p>在main函数的尾部我们看到了对runtime.morestack_noctxt函数的调用，往前我们可以看到，对runtime.morestack_noctxt的调用是通过main函数的第三条jbe指令跳转过来的。</p>
<p>0x0000000000486a8d &lt;+13&gt;: jbe    0x486abd &lt;main.main+61&gt;<br>……<br>0x0000000000486abd &lt;+61&gt;: callq  0x44ece0 &lt;runtime.morestack_noctxt&gt;<br>jbe是条件跳转指令，它依靠上一条指令的执行结果来判断是否需要跳转。这里的上一条指令是main函数的第二条指令，为了看清楚这里到底在干什么，我们把main函数的前三条指令都列出来：</p>
<p>0x0000000000486a80 &lt;+0&gt;: mov   %fs:0xfffffffffffffff8,%rcx #main函数第一条指令，rcx = g<br>0x0000000000486a89 &lt;+9&gt;: cmp   0x10(%rcx),%rsp<br>0x0000000000486a8d &lt;+13&gt;: jbe   0x486abd &lt;main.main+61&gt;<br>第二章我们已经介绍过，go语言使用fs寄存器实现系统线程的本地存储（TLS），main函数的第一条指令就是从TLS中读取当前正在运行的g的指针并放入rcx寄存器，第二条指令的源操作数是间接寻址，从内存中读取相对于g偏移16这个地址中的内容到rsp寄存器，我们来看看g偏移16的地址是放的什么东西，首先再来回顾一下g结构体的定义：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> g <span class="keyword">struct</span> &#123;</span><br><span class="line">    stack               stack   </span><br><span class="line">    stackguard0    <span class="keyword">uintptr</span> </span><br><span class="line">    stackguard1    <span class="keyword">uintptr</span> </span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">type</span> stack <span class="keyword">struct</span> &#123;</span><br><span class="line">    lo <span class="keyword">uintptr</span>  <span class="comment">//8 bytes</span></span><br><span class="line">    hi <span class="keyword">uintptr</span>  <span class="comment">//8 bytes</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到结构体g的第一个成员stack占16个字节（lo和hi各占8字节），所以g结构体变量的起始位置加偏移16就应该对应到stackguard0字段。因此main函数的第二条指令相当于在比较栈顶寄存器rsp的值是否比stackguard0的值小，如果rsp的值更小，说明当前g的栈要用完了，有溢出风险，需要扩栈，假设main goroutine被设置了抢占标志，那么rsp的值就会远远小于stackguard0，因为从上一节的分析我们知道sysmon监控线程在设置抢占标志时把需要被抢占的goroutine的stackguard0成员设置成了0xfffffffffffffade，而对于goroutine来说其rsp栈顶不可能这么大。因此stackguard0一旦被设置为抢占标记，代码将会跳转到 0x0000000000486abd 处执行call指令调用morestack_noctxt函数，该call指令会把紧跟call后面的一条指令的地址 0x0000000000486ac2 先压入堆栈，然后再跳转到morestack_noctxt函数去执行。下图展示了这一条call指令执行后g，rsp寄存器与main函数栈之间的关系：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/826970f964717116366dce0709b35f75.png" alt></p>
<p>morestack_noctxt函数使用JMP指令直接跳转到morestack继续执行，注意这里没有使用CALL指令调用morestack函数，所以rsp栈顶寄存器并没有发生发生变化，与上图一样还是指向存放返回地址的内存处。</p>
<p>morestack函数执行的流程类似于前面我们分析过的mcall函数，首先保存调用morestack函数的goroutine（我们这个场景是main goroutine）的调度信息到对应的g结构的sched成员之中，然后切换到当前工作线程的g0栈继续执行newstack函数。morestack代码如下，跟mcall一样都是使用go汇编语言编写的，这些代码跟mcall和gogo的代码非常类似，所以这里就不再对其进行详细分析了，读者可以自行参考下面的注释理解morestack函数的实现机制。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">433</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// morestack but not preserving ctxt.</span></span><br><span class="line">TEXT runtime·morestack_noctxt(SB),NOSPLIT,$<span class="number">0</span></span><br><span class="line">    MOVL  $<span class="number">0</span>, DX</span><br><span class="line">    JMP  runtime·morestack(SB)</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="comment">// Called during function prolog when more stack is needed.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// The traceback routines see morestack on a g0 as being</span></span><br><span class="line"><span class="comment">// the top of a stack (for example, morestack calling newstack</span></span><br><span class="line"><span class="comment">// calling the scheduler calling newm calling gc), so we must</span></span><br><span class="line"><span class="comment">// record an argument size. For that purpose, it has no arguments.</span></span><br><span class="line">TEXT runtime·morestack(SB),NOSPLIT,$<span class="number">0</span><span class="number">-0</span></span><br><span class="line">    ......</span><br><span class="line">    get_tls(CX)</span><br><span class="line">    MOVQ  g(CX), SI  # SI = g(main goroutine对应的g结构体变量)</span><br><span class="line">    ......</span><br><span class="line">    #SP栈顶寄存器现在指向的是morestack_noctxt函数的返回地址，</span><br><span class="line">    #所以下面这一条指令执行完成后AX = <span class="number">0x0000000000486ac2</span></span><br><span class="line">    MOVQ  <span class="number">0</span>(SP), AX </span><br><span class="line"> </span><br><span class="line">    #下面两条指令给g.sched.PC和g.sched.g赋值，我们这个例子g.sched.PC被赋值为<span class="number">0x0000000000486ac2</span>，</span><br><span class="line">    #也就是执行完morestack_noctxt函数之后应该返回去继续执行指令的地址。</span><br><span class="line">    MOVQ  AX, (g_sched+gobuf_pc)(SI) #g.sched.pc = <span class="number">0x0000000000486ac2</span></span><br><span class="line">    MOVQ  SI, (g_sched+gobuf_g)(SI) #g.sched.g = g</span><br><span class="line"> </span><br><span class="line">    LEAQ  <span class="number">8</span>(SP), AX  #main函数在调用morestack_noctxt之前的rsp寄存器</span><br><span class="line"> </span><br><span class="line">    #下面三条指令给g.sched.sp，g.sched.bp和g.sched.ctxt赋值</span><br><span class="line">    MOVQ  AX, (g_sched+gobuf_sp)(SI)</span><br><span class="line">    MOVQ  BP, (g_sched+gobuf_bp)(SI)</span><br><span class="line">    MOVQ  DX, (g_sched+gobuf_ctxt)(SI)</span><br><span class="line">    #上面几条指令把g的现场保存了起来，下面开始切换到g0运行</span><br><span class="line"> </span><br><span class="line">    #切换到g0栈，并设置tls的g为g0</span><br><span class="line">    #Call newstack on m-&gt;g0<span class="string">'s stack.</span></span><br><span class="line"><span class="string">    MOVQ  m_g0(BX), BX</span></span><br><span class="line"><span class="string">    MOVQ  BX, g(CX)  #设置TLS中的g为g0</span></span><br><span class="line"><span class="string">    #把g0栈的栈顶寄存器的值恢复到CPU的寄存器，达到切换栈的目的，下面这一条指令执行之前，</span></span><br><span class="line"><span class="string">    #CPU还是使用的调用此函数的g的栈，执行之后CPU就开始使用g0的栈了</span></span><br><span class="line"><span class="string">    MOVQ  (g_sched+gobuf_sp)(BX), SP</span></span><br><span class="line"><span class="string">    CALL  runtime·newstack(SB)</span></span><br><span class="line"><span class="string">    CALL  runtime·abort(SB)// crash if newstack returns</span></span><br><span class="line"><span class="string">    RET</span></span><br></pre></td></tr></table></figure></p>
<p>在切换到g0运行之前，当前goroutine的现场信息被保存到了对应的g结构体变量的sched成员之中（见下图）。这样我们这个场景中的main goroutine下次被调度起来运行时，调度器就可以把g.sched.sp恢复到CPU的rsp寄存器完成栈的切换，然后把g.sched.PC恢复到rip寄存器，于是CPU继续执行callq后面的</p>
<p>0x0000000000486ac2 &lt;+66&gt;: jmp   0x486a80 &lt;main.main&gt;<br>这条指令，就好像是从morestack_noctxt函数返回的一样，虽然实际上并不是从morestack_noctxt函数返回的，但效果一样。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f4e8c677b0329f74e9ec421757c3b1f4.png" alt></p>
<p>接下来我们继续看newstack函数，该函数主要有两个职责，一个是扩栈，另一个是响应sysmon提出的抢占请求，扩栈部分我们不关注，所以这里只看抢占相关的代码。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/stack.<span class="keyword">go</span> : <span class="number">899</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Called from runtime·morestack when more stack is needed.</span></span><br><span class="line"><span class="comment">// Allocate larger stack and relocate to new stack.</span></span><br><span class="line"><span class="comment">// Stack growth is multiplicative, for constant amortized cost.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// g-&gt;atomicstatus will be Grunning or Gscanrunning upon entry.</span></span><br><span class="line"><span class="comment">// If the GC is trying to stop this g then it will set preemptscan to true.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// This must be nowritebarrierrec because it can be called as part of</span></span><br><span class="line"><span class="comment">// stack growth from other nowritebarrierrec functions, but the</span></span><br><span class="line"><span class="comment">// compiler doesn't check this.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//go:nowritebarrierrec</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">newstack</span><span class="params">()</span></span> &#123;</span><br><span class="line">    thisg := getg() <span class="comment">// thisg = g0</span></span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// 这行代码获取g0.m.curg，也就是需要扩栈或响应抢占的goroutine</span></span><br><span class="line">    <span class="comment">// 对于我们这个例子gp = main goroutine</span></span><br><span class="line">    gp := thisg.m.curg</span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// <span class="doctag">NOTE:</span> stackguard0 may change underfoot, if another thread</span></span><br><span class="line">    <span class="comment">// is about to try to preempt gp. Read it just once and use that same</span></span><br><span class="line">    <span class="comment">// value now and below.</span></span><br><span class="line">    <span class="comment">//检查g.stackguard0是否被设置为stackPreempt</span></span><br><span class="line">    preempt := atomic.Loaduintptr(&amp;gp.stackguard0) == stackPreempt</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Be conservative about where we preempt.</span></span><br><span class="line">    <span class="comment">// We are interested in preempting user Go code, not runtime code.</span></span><br><span class="line">    <span class="comment">// If we're holding locks, mallocing, or preemption is disabled, don't</span></span><br><span class="line">    <span class="comment">// preempt.</span></span><br><span class="line">    <span class="comment">// This check is very early in newstack so that even the status change</span></span><br><span class="line">    <span class="comment">// from Grunning to Gwaiting and back doesn't happen in this case.</span></span><br><span class="line">    <span class="comment">// That status change by itself can be viewed as a small preemption,</span></span><br><span class="line">    <span class="comment">// because the GC might change Gwaiting to Gscanwaiting, and then</span></span><br><span class="line">    <span class="comment">// this goroutine has to wait for the GC to finish before continuing.</span></span><br><span class="line">    <span class="comment">// If the GC is in some way dependent on this goroutine (for example,</span></span><br><span class="line">    <span class="comment">// it needs a lock held by the goroutine), that small preemption turns</span></span><br><span class="line">    <span class="comment">// into a real deadlock.</span></span><br><span class="line">    <span class="keyword">if</span> preempt &#123;</span><br><span class="line">        <span class="comment">//检查被抢占goroutine的状态</span></span><br><span class="line">        <span class="keyword">if</span> thisg.m.locks != <span class="number">0</span> || thisg.m.mallocing != <span class="number">0</span> || thisg.m.preemptoff != <span class="string">""</span> || thisg.m.p.ptr().status != _Prunning &#123;</span><br><span class="line">            <span class="comment">// Let the goroutine keep running for now.</span></span><br><span class="line">            <span class="comment">// gp-&gt;preempt is set, so it will be preempted next time.</span></span><br><span class="line">            <span class="comment">//还原stackguard0为正常值，表示我们已经处理过抢占请求了</span></span><br><span class="line">            gp.stackguard0 = gp.stack.lo + _StackGuard</span><br><span class="line">           </span><br><span class="line">            <span class="comment">//不抢占，调用gogo继续运行当前这个g，不需要调用schedule函数去挑选另一个goroutine</span></span><br><span class="line">            gogo(&amp;gp.sched) <span class="comment">// never return</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//省略的代码做了些其它检查所以这里才有两个同样的判断</span></span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> preempt &#123;</span><br><span class="line">        <span class="keyword">if</span> gp == thisg.m.g0 &#123;</span><br><span class="line">            throw(<span class="string">"runtime: preempt g0"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> thisg.m.p == <span class="number">0</span> &amp;&amp; thisg.m.locks == <span class="number">0</span> &#123;</span><br><span class="line">            throw(<span class="string">"runtime: g is running but p is not"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">        ......</span><br><span class="line">        <span class="comment">//下面开始响应抢占请求</span></span><br><span class="line">        <span class="comment">// Act like goroutine called runtime.Gosched.</span></span><br><span class="line">        <span class="comment">//设置gp的状态，省略的代码在处理gc时把gp的状态修改成了_Gwaiting</span></span><br><span class="line">        casgstatus(gp, _Gwaiting, _Grunning)</span><br><span class="line">       </span><br><span class="line">        <span class="comment">//调用gopreempt_m把gp切换出去</span></span><br><span class="line">        gopreempt_m(gp) <span class="comment">// never return</span></span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>newstack函数首先检查g.stackguard0是否被设置为stackPreempt，如果是则表示sysmon已经发现我们运行得太久了并对我们发起了抢占请求。在做了一些基本的检查后如果当前goroutine可以被抢占则调用gopreempt_m函数完成调度。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2644</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">gopreempt_m</span><span class="params">(gp *g)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> trace.enabled &#123;</span><br><span class="line">        traceGoPreempt()</span><br><span class="line">    &#125;</span><br><span class="line">    goschedImpl(gp)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>gopreempt_m通过调用goschedImpl函数完成实际的调度切换工作，我们在前面主动调度一节已经详细分析过goschedImpl函数，该函数首先把gp的状态从_Grunning设置成_Grunnable，并通过dropg函数解除当前工作线程m和gp之间的关系，然后把gp放入全局队列等待被调度器调度，最后调用schedule()函数进入新一轮调度。</p>
<p>小结</p>
<p>上面我们分析了由于运行时间过长导致的抢占调度，可以看到go的抢占调度机制并非无条件的抢占。需要抢占时，监控线程负责给被抢占的goroutine设置抢占标记，被抢占的goroutine再在函数的的入口处检查g的stackguard0成员决定是否需要调用morestack_noctxt函数，从而最终调用到newstack函数处理抢占请求。</p>
<p>下一节我们再来看因系统调用而发生的抢占调度。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>工作线程的唤醒及创建(19)</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/%E5%B7%A5%E4%BD%9C%E7%BA%BF%E7%A8%8B%E7%9A%84%E5%94%A4%E9%86%92%E5%8F%8A%E5%88%9B%E5%BB%BA-19/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/T9CDaNF5KUFjE_Z6YW7mRw" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-05-23</p>
<p>本文是《Go语言调度器源代码情景分析》系列的第19篇，也是第四章《Goroutine被动调度》的第2小节。</p>
<p>本文需要重点关注：</p>
<p>如何唤醒睡眠中的工作线程</p>
<p>如何创建新的工作线程</p>
<p>上一篇文章我们分析到了ready函数通过把需要唤醒的goroutine放入运行队列来唤醒它，本文接着上文继续分析。</p>
<p>唤醒空闲的P</p>
<p>为了充分利用CPU，ready函数在唤醒goroutine之后会去判断是否需要启动新工作线程出来工作，判断规则是，如果当前有空闲的p而且没有工作线程正在尝试从各个工作线程的本地运行队列偷取goroutine的话（没有处于spinning状态的工作线程），那么就需要把空闲的p唤醒起来工作，详见下面的ready函数：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">639</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Mark gp ready to run.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">ready</span><span class="params">(gp *g, traceskip <span class="keyword">int</span>, next <span class="keyword">bool</span>)</span></span> &#123;</span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// Mark runnable.</span></span><br><span class="line">    _g_ := getg()</span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// status is Gwaiting or Gscanwaiting, make Grunnable and put on runq</span></span><br><span class="line">    casgstatus(gp, _Gwaiting, _Grunnable)</span><br><span class="line">    runqput(_g_.m.p.ptr(), gp, next) <span class="comment">//放入运行队列</span></span><br><span class="line">    <span class="keyword">if</span> atomic.Load(&amp;sched.npidle) != <span class="number">0</span> &amp;&amp; atomic.Load(&amp;sched.nmspinning) == <span class="number">0</span> &#123;</span><br><span class="line">        <span class="comment">//有空闲的p而且没有正在偷取goroutine的工作线程，则需要唤醒p出来工作</span></span><br><span class="line">        wakep()</span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>而唤醒空闲的p是由wakep函数完成的。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2051</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Tries to add one more P to execute G's.</span></span><br><span class="line"><span class="comment">// Called when a G is made runnable (newproc, ready).</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">wakep</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">// be conservative about spinning threads</span></span><br><span class="line">    <span class="keyword">if</span> !atomic.Cas(&amp;sched.nmspinning, <span class="number">0</span>, <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    startm(<span class="literal">nil</span>, <span class="literal">true</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>wakep首先通过cas操作再次确认是否有其它工作线程正处于spinning状态，这里之所以需要使用cas操作再次进行确认，原因在于，在当前工作线程通过如下条件</p>
<p>atomic.Load(&amp;sched.npidle) != 0 &amp;&amp; atomic.Load(&amp;sched.nmspinning) == 0<br>判断到需要启动工作线程之后到真正启动工作线程之前的这一段时间之内，如果已经有工作线程进入了spinning状态而在四处寻找需要运行的goroutine，这样的话我们就没有必要再启动一个多余的工作线程出来了。</p>
<p>如果cas操作成功，则继续调用startm创建一个新的或唤醒一个处于睡眠状态的工作线程出来工作。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">1947</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Schedules some M to run the p (creates an M if necessary).</span></span><br><span class="line"><span class="comment">// If p==nil, tries to get an idle P, if no idle P's does nothing.</span></span><br><span class="line"><span class="comment">// May run with m.p==nil, so write barriers are not allowed.</span></span><br><span class="line"><span class="comment">// If spinning is set, the caller has incremented nmspinning and startm will</span></span><br><span class="line"><span class="comment">// either decrement nmspinning or set m.spinning in the newly started M.</span></span><br><span class="line"><span class="comment">//go:nowritebarrierrec</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">startm</span><span class="params">(_p_ *p, spinning <span class="keyword">bool</span>)</span></span> &#123;</span><br><span class="line">    lock(&amp;sched.lock)</span><br><span class="line">    <span class="keyword">if</span> _p_ == <span class="literal">nil</span> &#123; <span class="comment">//没有指定p的话需要从p的空闲队列中获取一个p</span></span><br><span class="line">        _p_ = pidleget() <span class="comment">//从p的空闲队列中获取空闲p</span></span><br><span class="line">        <span class="keyword">if</span> _p_ == <span class="literal">nil</span> &#123;</span><br><span class="line">            unlock(&amp;sched.lock)</span><br><span class="line">            <span class="keyword">if</span> spinning &#123;</span><br><span class="line">                <span class="comment">// The caller incremented nmspinning, but there are no idle Ps,</span></span><br><span class="line">                <span class="comment">// so it's okay to just undo the increment and give up.</span></span><br><span class="line">                <span class="comment">//spinning为true表示进入这个函数之前已经对sched.nmspinning加了1，需要还原</span></span><br><span class="line">                <span class="keyword">if</span> <span class="keyword">int32</span>(atomic.Xadd(&amp;sched.nmspinning, <span class="number">-1</span>)) &lt; <span class="number">0</span> &#123;</span><br><span class="line">                    throw(<span class="string">"startm: negative nmspinning"</span>)</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="comment">//没有空闲的p，直接返回</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    mp := mget() <span class="comment">//从m空闲队列中获取正处于睡眠之中的工作线程，所有处于睡眠状态的m都在此队列中</span></span><br><span class="line">    unlock(&amp;sched.lock)</span><br><span class="line">    <span class="keyword">if</span> mp == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="comment">//没有处于睡眠状态的工作线程</span></span><br><span class="line">        <span class="keyword">var</span> fn <span class="function"><span class="keyword">func</span><span class="params">()</span></span></span><br><span class="line">        <span class="keyword">if</span> spinning &#123;</span><br><span class="line">            <span class="comment">// The caller incremented nmspinning, so set m.spinning in the new M.</span></span><br><span class="line">            fn = mspinning</span><br><span class="line">        &#125;</span><br><span class="line">        newm(fn, _p_) <span class="comment">//创建新的工作线程</span></span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> mp.spinning &#123;</span><br><span class="line">        throw(<span class="string">"startm: m is spinning"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> mp.nextp != <span class="number">0</span> &#123;</span><br><span class="line">        throw(<span class="string">"startm: m has p"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> spinning &amp;&amp; !runqempty(_p_) &#123;</span><br><span class="line">        throw(<span class="string">"startm: p has runnable gs"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// The caller incremented nmspinning, so set m.spinning in the new M.</span></span><br><span class="line">    mp.spinning = spinning</span><br><span class="line">    mp.nextp.set(_p_)</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//唤醒处于休眠状态的工作线程</span></span><br><span class="line">    notewakeup(&amp;mp.park)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>startm函数首先判断是否有空闲的p结构体对象，如果没有则直接返回，如果有则需要创建或唤醒一个工作线程出来与之绑定，从这里可以看出所谓的唤醒p，其实就是把空闲的p利用起来。</p>
<p>在确保有可以绑定的p对象之后，startm函数首先尝试从m的空闲队列中查找正处于休眠状态的工作线程，如果找到则通过notewakeup函数唤醒它，否则调用newm函数创建一个新的工作线程出来。</p>
<p>下面我们首先分析notewakeup函数是如何唤醒工作线程的，然后再讨论newm函数创建工作线程的流程。</p>
<p>唤醒睡眠中的工作线程</p>
<p>在第三章我们讨论过，当找不到需要运行的goroutine时，工作线程会通过notesleep函数睡眠在m.park成员上，所以这里使用m.park成员作为参数调用notewakeup把睡眠在该成员之上的工作线程唤醒。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/lock_futex.<span class="keyword">go</span> : <span class="number">130</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">notewakeup</span><span class="params">(n *note)</span></span> &#123;</span><br><span class="line">    <span class="comment">//设置n.key = 1, 被唤醒的线程通过查看该值是否等于1来确定是被其它线程唤醒还是意外从睡眠中苏醒</span></span><br><span class="line">    old := atomic.Xchg(key32(&amp;n.key), <span class="number">1</span>)  </span><br><span class="line">    <span class="keyword">if</span> old != <span class="number">0</span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"notewakeup - double wakeup ("</span>, old, <span class="string">")\n"</span>)</span><br><span class="line">        throw(<span class="string">"notewakeup - double wakeup"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//调用futexwakeup唤醒</span></span><br><span class="line">    futexwakeup(key32(&amp;n.key), <span class="number">1</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>notewakeup函数首先使用atomic.Xchg设置note.key值为1，这是为了使被唤醒的线程可以通过查看该值是否等于1来确定是被其它线程唤醒还是意外从睡眠中苏醒了过来，如果该值为1则表示是被唤醒的，可以继续工作了，但如果该值为0则表示是意外苏醒，需要再次进入睡眠，工作线程苏醒之后的处理逻辑我们已经在notesleep函数中见过，所以这里略过。</p>
<p>把note.key的值设置为1后，notewakeup函数继续调用futexwakeup函数<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/os_linux.<span class="keyword">go</span> : <span class="number">66</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// If any procs are sleeping on addr, wake up at most cnt.</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">futexwakeup</span><span class="params">(addr *<span class="keyword">uint32</span>, cnt <span class="keyword">uint32</span>)</span></span> &#123;</span><br><span class="line">    <span class="comment">//调用futex函数唤醒工作线程</span></span><br><span class="line">    ret := futex(unsafe.Pointer(addr), _FUTEX_WAKE_PRIVATE, cnt, <span class="literal">nil</span>, <span class="literal">nil</span>, <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">if</span> ret &gt;= <span class="number">0</span> &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// I don't know that futex wakeup can return</span></span><br><span class="line">    <span class="comment">// EAGAIN or EINTR, but if it does, it would be</span></span><br><span class="line">    <span class="comment">// safe to loop and call futex again.</span></span><br><span class="line">    systemstack(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"futexwakeup addr="</span>, addr, <span class="string">" returned "</span>, ret, <span class="string">"\n"</span>)</span><br><span class="line">    &#125;)</span><br><span class="line"> </span><br><span class="line">    *(*<span class="keyword">int32</span>)(unsafe.Pointer(<span class="keyword">uintptr</span>(<span class="number">0x1006</span>))) = <span class="number">0x1006</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>对于Linux平台来说，工作线程通过note睡眠其实是通过futex系统调用睡眠在内核之中，所以唤醒处于睡眠状态的线程也需要通过futex系统调用进入内核来唤醒，所以这里的futexwakeup又继续调用包装了futex系统调用的futex函数来实现唤醒睡眠在内核中的工作线程。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/sys_linux_amd64.s : <span class="number">525</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// int64 futex(int32 *uaddr, int32 op, int32 val,</span></span><br><span class="line"><span class="comment">//struct timespec *timeout, int32 *uaddr2, int32 val2);</span></span><br><span class="line">TEXT runtime·futex(SB),NOSPLIT,$<span class="number">0</span></span><br><span class="line">    MOVQ  addr+<span class="number">0</span>(FP), DI  #这<span class="number">6</span>条指令在为futex系统调用准备参数</span><br><span class="line">    MOVL  op+<span class="number">8</span>(FP), SI</span><br><span class="line">    MOVL  val+<span class="number">12</span>(FP), DX</span><br><span class="line">    MOVQ  ts+<span class="number">16</span>(FP), R10</span><br><span class="line">    MOVQ  addr2+<span class="number">24</span>(FP), R8</span><br><span class="line">    MOVL  val3+<span class="number">32</span>(FP), R9</span><br><span class="line">    MOVL  $SYS_futex, AX   #futex系统调用编号放入AX寄存器</span><br><span class="line">    SYSCALL   #系统调用，进入内核</span><br><span class="line">    MOVL  AX, ret+<span class="number">40</span>(FP) #系统调用通过AX寄存器返回返回值，这里把返回值保存到内存之中</span><br><span class="line">    RET</span><br></pre></td></tr></table></figure></p>
<p>futex函数由汇编代码写成，前面的几条指令都在为futex系统调用准备参数，参数准备完成之后则通过SYSCALL指令进入操作系统内核完成线程的唤醒功能，内核在完成唤醒工作之后当前工作线程则从内核返回到futex函数继续执行SYSCALL指令之后的代码并按函数调用链原路返回，继续执行其它代码，而被唤醒的工作线程则由内核负责在适当的时候调度到CPU上运行。</p>
<p>看完唤醒流程，下面我们来分析工作线程的创建。</p>
<p>创建工作线程</p>
<p>回到startm函数，如果没有正处于休眠状态的工作线程，则需要调用newm函数新建一个工作线程。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">1807</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Create a new m. It will start off with a call to fn, or else the scheduler.</span></span><br><span class="line"><span class="comment">// fn needs to be static and not a heap allocated closure.</span></span><br><span class="line"><span class="comment">// May run with m.p==nil, so write barriers are not allowed.</span></span><br><span class="line"><span class="comment">//go:nowritebarrierrec</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">newm</span><span class="params">(fn <span class="keyword">func</span>()</span>, _<span class="title">p_</span> *<span class="title">p</span>)</span> &#123;</span><br><span class="line">    mp := allocm(_p_, fn)</span><br><span class="line">    mp.nextp.set(_p_)</span><br><span class="line">    ......</span><br><span class="line">    newm1(mp)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>newm首先调用allocm函数从堆上分配一个m结构体对象，然后调用newm1函数。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">1843</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">newm1</span><span class="params">(mp *m)</span></span> &#123;</span><br><span class="line">      <span class="comment">//省略cgo相关代码.......</span></span><br><span class="line">      execLock.rlock() <span class="comment">// Prevent process clone.</span></span><br><span class="line">      newosproc(mp)</span><br><span class="line">      execLock.runlock()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>newm1继续调用newosproc函数，newosproc的主要任务是调用clone函数创建一个系统线程，而新建的这个系统线程将从mstart函数开始运行。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/os_linux.<span class="keyword">go</span> : <span class="number">143</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// May run with m.p==nil, so write barriers are not allowed.</span></span><br><span class="line"><span class="comment">//go:nowritebarrier</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">newosproc</span><span class="params">(mp *m)</span></span> &#123;</span><br><span class="line">    stk := unsafe.Pointer(mp.g0.stack.hi)                     </span><br><span class="line">    ......</span><br><span class="line">    ret := clone(cloneFlags, stk, unsafe.Pointer(mp), unsafe.Pointer(mp.g0),        unsafe.Pointer(funcPC(mstart)))</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//clone系统调用的Flags选项</span></span><br><span class="line">cloneFlags = _CLONE_VM | <span class="comment">/* share memory */</span> <span class="comment">//指定父子线程共享进程地址空间</span></span><br><span class="line">  _CLONE_FS | <span class="comment">/* share cwd, etc */</span></span><br><span class="line">  _CLONE_FILES | <span class="comment">/* share fd table */</span></span><br><span class="line">  _CLONE_SIGHAND | <span class="comment">/* share sig handler table */</span></span><br><span class="line">  _CLONE_SYSVSEM | <span class="comment">/* share SysV semaphore undo lists (see issue #20763) */</span></span><br><span class="line">  _CLONE_THREAD <span class="comment">/* revisit - okay for now */</span>  <span class="comment">//创建子线程而不是子进程</span></span><br><span class="line">clone函数是由汇编语言实现的，该函数使用clone系统调用完成创建系统线程的核心功能。我们分段来看</span><br><span class="line"></span><br><span class="line">runtime/sys_linux_amd64.s : <span class="number">539</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// int32 clone(int32 flags, void *stk, M *mp, G *gp, void (*fn)(void));</span></span><br><span class="line">TEXT runtime·clone(SB),NOSPLIT,$<span class="number">0</span></span><br><span class="line">    MOVL  flags+<span class="number">0</span>(FP), DI <span class="comment">//系统调用的第一个参数</span></span><br><span class="line">    MOVQ  stk+<span class="number">8</span>(FP), SI   <span class="comment">//系统调用的第二个参数</span></span><br><span class="line">    MOVQ  $<span class="number">0</span>, DX         <span class="comment">//第三个参数</span></span><br><span class="line">    MOVQ  $<span class="number">0</span>, R10         <span class="comment">//第四个参数</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Copy mp, gp, fn off parent stack for use by child.</span></span><br><span class="line">    <span class="comment">// Careful: Linux system call clobbers CX and R11.</span></span><br><span class="line">    MOVQ  mp+<span class="number">16</span>(FP), R8</span><br><span class="line">    MOVQ  gp+<span class="number">24</span>(FP), R9</span><br><span class="line">    MOVQ  fn+<span class="number">32</span>(FP), R12</span><br><span class="line"> </span><br><span class="line">    MOVL  $SYS_clone, AX</span><br><span class="line">    SYSCALL</span><br></pre></td></tr></table></figure></p>
<p>clone函数首先用了4条指令为clone系统调用准备参数，该系统调用一共需要四个参数，根据Linux系统调用约定，这四个参数需要分别放入rdi， rsi，rdx和r10寄存器中，这里最重要的是第一个参数和第二个参数，分别用来指定内核创建线程时需要的选项和新线程应该使用的栈。因为即将被创建的线程与当前线程共享同一个进程地址空间，所以这里必须为子线程指定其使用的栈，否则父子线程会共享同一个栈从而造成混乱，从上面的newosproc函数可以看出，新线程使用的栈为m.g0.stack.lo～m.g0.stack.hi这段内存，而这段内存是newm函数在创建m结构体对象时从进程的堆上分配而来的。</p>
<p>准备好系统调用的参数之后，还有另外一件很重的事情需要做，那就是把clone函数的其它几个参数（mp, gp和线程入口函数）保存到寄存器中，之所以需要在系统调用之前保存这几个参数，原因在于这几个参数目前还位于父线程的栈之中，而一旦通过系统调用把子线程创建出来之后，子线程将会使用我们在clone系统调用时给它指定的栈，所以这里需要把这几个参数先保存到寄存器，等子线程从系统调用返回后直接在寄存器中获取这几个参数。这里要注意的是虽然这个几个参数值保存在了父线程的寄存器之中，但创建子线程时，操作系统内核会把父线程的所有寄存器帮我们复制一份给子线程，所以当子线程开始运行时就能拿到父线程保存在寄存器中的值，从而拿到这几个参数。这些准备工作完成之后代码调用syscall指令进入内核，由内核帮助我们创建系统线程。</p>
<p>clone系统调用完成后实际上就多了一个操作系统线程，新创建的子线程和当前线程都得从系统调用返回然后继续执行后面的代码，那么从系统调用返回之后我们怎么知道哪个是父线程哪个是子线程，从而来决定它们的执行流程？使用过fork系统调用的读者应该知道，我们需要通过返回值来判断父子线程，系统调用的返回值如果是0则表示这是子线程，不为0则表示这个是父线程。用c代码来描述大概就是这个样子：</p>
<p>if (clone(…) == 0) { //子线程<br>    子线程代码<br>} else { //父线程<br>    父线程代码<br>}<br>虽然这里只有一次clone调用，但它却返回了2次，一次返回到父线程，一次返回到子线程，然后2个线程各自执行自己的代码流程。</p>
<p>回到clone函数，下面代码的第一条指令就在判断系统调用的返回值，如果是子线程则跳转到后面的代码继续执行，如果是父线程，它创建子线程的任务已经完成，所以这里把返回值保存在栈上之后就直接执行ret指令返回到newosproc函数了。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/sys_linux_amd64.s : <span class="number">555</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// In parent, return.</span></span><br><span class="line">    CMPQ  AX, $<span class="number">0</span>  #判断clone系统调用的返回值</span><br><span class="line">    JEQ  <span class="number">3</span>(PC) / #跳转到子线程部分</span><br><span class="line">    MOVL  AX, ret+<span class="number">40</span>(FP) #父线程需要执行的指令</span><br><span class="line">    RET  #父线程需要执行的指令</span><br></pre></td></tr></table></figure></p>
<p>而对于子线程来说，还有很多初始化工作要做，下面是子线程需要继续执行的指令。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/sys_linux_amd64.s : <span class="number">561</span></span><br><span class="line"></span><br><span class="line">    # In child, on <span class="built_in">new</span> stack.</span><br><span class="line">    #子线程需要继续执行的指令</span><br><span class="line">    MOVQ  SI, SP  #设置CPU栈顶寄存器指向子线程的栈顶，这条指令看起来是多余的？内核应该已经把SP设置好了</span><br><span class="line"> </span><br><span class="line">    # If g or m are <span class="literal">nil</span>, skip Go-related setup.</span><br><span class="line">    CMPQ  R8, $<span class="number">0</span>    # m，新创建的m结构体对象的地址，由父线程保存在R8寄存器中的值被复制到了子线程</span><br><span class="line">    JEQ  nog</span><br><span class="line">    CMPQ  R9, $<span class="number">0</span>    # g，m.g0的地址，由父线程保存在R9寄存器中的值被复制到了子线程</span><br><span class="line">    JEQ  nog</span><br><span class="line"> </span><br><span class="line">    # Initialize m-&gt;procid to Linux tid</span><br><span class="line">    MOVL  $SYS_gettid, AX  #通过gettid系统调用获取线程ID（tid）</span><br><span class="line">    SYSCALL</span><br><span class="line">    MOVQ  AX, m_procid(R8)  #m.procid = tid</span><br><span class="line"> </span><br><span class="line">    #Set FS to point at m-&gt;tls.</span><br><span class="line">    #新线程刚刚创建出来，还未设置线程本地存储，即m结构体对象还未与工作线程关联起来，</span><br><span class="line">    #下面的指令负责设置新线程的TLS，把m对象和工作线程关联起来</span><br><span class="line">    LEAQ  m_tls(R8), DI  #取m.tls字段的地址</span><br><span class="line">    CALL  runtime·settls(SB)</span><br><span class="line"> </span><br><span class="line">    #In child, set up <span class="built_in">new</span> stack</span><br><span class="line">    get_tls(CX)</span><br><span class="line">    MOVQ  R8, g_m(R9)  # g.m = m </span><br><span class="line">    MOVQ  R9, g(CX)      # tls.g = &amp;m.g0</span><br><span class="line">    CALL  runtime·stackcheck(SB)</span><br><span class="line"> </span><br><span class="line">nog:</span><br><span class="line">    # Call fn</span><br><span class="line">    CALL  R12  #这里调用mstart函数</span><br><span class="line">    ......</span><br></pre></td></tr></table></figure></p>
<p>这段代码的第一条指令把CPU寄存器的栈顶指针设置为新线程的的栈顶，这条指令看起来是多余的，因为我们在clone系统调用时已经把栈信息告诉操作系统了，操作系统在把新线程调度起来运行时已经帮我们把CPU的rsp寄存器设置好了，这里应该没必要自己去设置。接下来的4条指令判断m和g是否为nil，如果是则直接去执行fn函数，对于我们这个流程来说，因为现在正在创建工作线程，所以m和g（其实是m.g0）都不为空，因而需要继续对m进行初始化。</p>
<p>对新创建出来的工作线程的初始化过程从上面代码片段的第6条指令开始，它首先通过系统调用获取到子线程的线程id，并赋值给m.procid，然后调用settls设置线程本地存储并通过把m.g0的地址放入线程本地存储之中，从而实现了m结构体对象与工作线程之间的关联，settls函数我们已经在第二章详细分析过，所以这里直接跳过。</p>
<p>新工作线程的初始化完成之后，便开始执行mstart函数，我们在第二章也见过该函数，主线程初始化完成之后也是调用的它。回忆一下，mstart函数首先会去设置m.g0的stackguard成员，然后调用mstart1()函数把当前工作线程的g0的调度信息保存在m.g0.sched成员之中，最后通过调用schedule函数进入调度循环。</p>
<p>总结</p>
<p>本章仅以读写channel为例分析了goroutine因操作被阻塞而发生的被动调度，其实发生被动调度的情况还比较多，比如因读写网络连接而阻塞、加锁被阻塞或select操作阻塞等等都会发生被动调度，读者可以自行阅读相关源代码。</p>
<p>本章还分析了睡眠中的工作线程是如何被唤起起来工作的以及新工作线程的创建和初始化流程。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>go深入理解defer（下）defer实现机制</title>
    <url>/go/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3defer/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3defer%EF%BC%88%E4%B8%8B%EF%BC%89defer%E5%AE%9E%E7%8E%B0%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/iEtMbRXW4yYyCG0TTW5y9g" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-06-17</p>
<p>上一篇文章我们主要从使用的角度介绍了 defer 的基础知识，本文我们来分析一下 defer 的实现机制。</p>
<p>还是从一个例子程序开始。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">sum</span><span class="params">(a, b <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">    c := a + b</span><br><span class="line">    fmt.Println(<span class="string">"sum:"</span> , c)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f</span><span class="params">(a, b <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">defer</span> sum(a, b)</span><br><span class="line"> </span><br><span class="line">    fmt.Printf(<span class="string">"a: %d, b: %d\n"</span>, a, b)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    a, b := <span class="number">1</span>, <span class="number">2</span></span><br><span class="line">    f(a, b)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>从前一篇文章我们得知，编译器会把 defer 语句翻译成对 deferproc 函数的调用，同时，编译器也会在使用了 defer 语句的 go 函数的末尾插入对 deferreturn 函数的调用，下面我们来看一下这两个函数的实现代码。</p>
<p>deferproc 函数</p>
<p>先来看看 deferproc 的函数原型：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/<span class="built_in">panic</span>.<span class="keyword">go</span> : <span class="number">89</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Create a new deferred function fn with siz bytes of arguments.</span></span><br><span class="line"><span class="comment">// The compiler turns a defer statement into a call to this.</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">deferproc</span><span class="params">(siz <span class="keyword">int32</span>, fn *funcval)</span></span></span><br><span class="line">deferproc 函数的第一个参数 siz 是 defered 函数（比如本例中的 sum 函数）的参数以字节为单位的大小，第二个参数 funcval 是一个变长结构体：</span><br><span class="line"></span><br><span class="line">proc/runtime2.<span class="keyword">go</span> : <span class="number">139</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> funcval <span class="keyword">struct</span> &#123;</span><br><span class="line">    fn <span class="keyword">uintptr</span></span><br><span class="line">    <span class="comment">// variable-size, fn-specific data here</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>于是，在64位系统中本文例子中的 defer sum(a, b) 大致等价于</p>
<p>deferproc(16, &amp;funcval{sum})<br>因为 sum 函数有 2 个 int 型的参数共 16 字节，所以在调用 deferproc 函数时第一个参数为16，第二个参数 funcval 结构体对象的 fn 成员为 sum 函数的地址。我们可以先想一下为什么需要把 sum 函数的参数大小传递给 deferproc() 函数？另外为什么没看到 sum 函数需要的两个参数呢？</p>
<p>为了搞清楚编译器到底会怎么翻译 defer 关键字，我们需要看一下 f() 函数的汇编代码：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">0x0000000000488de0 &lt;+0&gt;: mov   %fs:0xfffffffffffffff8,%rcx</span><br><span class="line">0x0000000000488de9 &lt;+9&gt;: cmp   0x10(%rcx),%rsp</span><br><span class="line">0x0000000000488ded &lt;+13&gt;: jbe   0x488f10 &lt;main.f+304&gt;</span><br><span class="line">0x0000000000488df3 &lt;+19&gt;: sub   $0x80,%rsp</span><br><span class="line">0x0000000000488dfa &lt;+26&gt;: mov   %rbp,0x78(%rsp)</span><br><span class="line">0x0000000000488dff &lt;+31&gt;: lea   0x78(%rsp),%rbp   # 这一条指令以及前面几条指令是函数序言，跟defer无关</span><br><span class="line">0x0000000000488e04 &lt;+36&gt;: movl   $0x10,(%rsp)     # deferproc的第一个参数siz</span><br><span class="line">0x0000000000488e0b &lt;+43&gt;: lea   0x39076(%rip),%rax      </span><br><span class="line">0x0000000000488e12 &lt;+50&gt;: mov   %rax,0x8(%rsp)  # 第二个参数funcval结构体对象的地址</span><br><span class="line">0x0000000000488e17 &lt;+55&gt;: mov   0x88(%rsp),%rax</span><br><span class="line">0x0000000000488e1f &lt;+63&gt;: mov   %rax,0x10(%rsp)  # f()函数的第一个参数a，a = 1</span><br><span class="line">0x0000000000488e24 &lt;+68&gt;: mov   0x90(%rsp),%rcx</span><br><span class="line">0x0000000000488e2c &lt;+76&gt;: mov   %rcx,0x18(%rsp)  # f()函数的第二个参数b, b = 2</span><br><span class="line">0x0000000000488e31 &lt;+81&gt;: callq   0x426c00 &lt;runtime.deferproc&gt;  # 调用deferproc函数</span><br><span class="line"># 注意deferproc函数本来是没有返回值的，下面的test指令在检查deferproc的隐性返回值</span><br><span class="line"># 这条指令是编译器专门针对deferproc函数而插入的，对其它go函数调用时编译器不会插入该指令</span><br><span class="line">0x0000000000488e36 &lt;+86&gt;: test   %eax,%eax  </span><br><span class="line"># 如果deferproc返回不为0则直接跳转到函数结尾去执行deferreturn函数</span><br><span class="line">0x0000000000488e38 &lt;+88&gt;: jne   0x488efd &lt;main.f+285&gt; </span><br><span class="line">0x0000000000488e3e &lt;+94&gt;: mov   0x88(%rsp),%rax</span><br><span class="line">......</span><br><span class="line">0x0000000000488ee5 &lt;+261&gt;: callq  0x480b20 &lt;fmt.Fprintf&gt;  #输出a b的值</span><br><span class="line">0x0000000000488eea &lt;+266&gt;: nop</span><br><span class="line">#调用deferreturn函数完成对sum函数的延迟调用</span><br><span class="line">0x0000000000488eeb &lt;+267&gt;: callq   0x427490 &lt;runtime.deferreturn&gt;  </span><br><span class="line">0x0000000000488ef0 &lt;+272&gt;: mov   0x78(%rsp),%rbp</span><br><span class="line">0x0000000000488ef5 &lt;+277&gt;: add   $0x80,%rsp</span><br><span class="line">0x0000000000488efc &lt;+284&gt;: retq </span><br><span class="line">0x0000000000488efd &lt;+285&gt;: nop</span><br><span class="line">0x0000000000488efe &lt;+286&gt;: callq   0x427490 &lt;runtime.deferreturn&gt;</span><br><span class="line">0x0000000000488f03 &lt;+291&gt;: mov   0x78(%rsp),%rbp</span><br><span class="line">0x0000000000488f08 &lt;+296&gt;: add   $0x80,%rsp</span><br><span class="line">0x0000000000488f0f &lt;+303&gt;: retq   </span><br><span class="line">0x0000000000488f10 &lt;+304&gt;: callq  0x44f300 &lt;runtime.morestack_noctxt&gt; #扩栈处理</span><br><span class="line">0x0000000000488f15 &lt;+309&gt;: jmpq   0x488de0 &lt;main.f&gt;</span><br></pre></td></tr></table></figure></p>
<p>从 f() 函数的汇编代码可以看出，在调用 runtime.deferproc 时，栈上除了保存了 deferproc 函数需要的两个参数之外，还保存了 defered 函数所需要的参数（我们这个例子中 defered 函数是 sum 函数，它的2个参数 a 和 b 也都保存在了栈上，它们紧邻 deferproc 函数的第二个参数），也就是说，在执行 defer 语句时，defer 后面的函数的参数已经确定了。</p>
<p>另外需要注意的是，从 deferproc 函数的原型可以知道它并没有返回值，但上面的汇编代码在调用了 deferproc 函数之后却检查了 rax 寄存器的值是否为0(0x0000000000488e36 &lt;+86&gt;: test   %eax,%eax)，也就是说 deferproc 函数实际上会通过 rax 寄存器返回一个隐性的返回值！</p>
<p>接着我们继续看 deferproc 函数的实现:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">proc/<span class="built_in">panic</span>.<span class="keyword">go</span> : <span class="number">89</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Create a new deferred function fn with siz bytes of arguments.</span></span><br><span class="line"><span class="comment">// The compiler turns a defer statement into a call to this.</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">deferproc</span><span class="params">(siz <span class="keyword">int32</span>, fn *funcval)</span></span> &#123; <span class="comment">// arguments of fn follow fn</span></span><br><span class="line">    <span class="keyword">if</span> getg().m.curg != getg() &#123;  <span class="comment">//用户goroutine才能使用defer</span></span><br><span class="line">        <span class="comment">// go code on the system stack can't defer</span></span><br><span class="line">        throw(<span class="string">"defer on system stack"</span>)</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// the arguments of fn are in a perilous state. The stack map</span></span><br><span class="line">    <span class="comment">// for deferproc does not describe them. So we can't let garbage</span></span><br><span class="line">    <span class="comment">// collection or stack copying trigger until we've copied them out</span></span><br><span class="line">    <span class="comment">// to somewhere safe. The memmove below does that.</span></span><br><span class="line">    <span class="comment">// Until the copy completes, we can only call nosplit routines.</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 对getcallersp()和getcallerpc() 函数的分析可以参考本公众号的其它文章</span></span><br><span class="line">    sp := getcallersp() <span class="comment">//sp = 调用deferproc之前的rsp寄存器的值</span></span><br><span class="line">    <span class="comment">// argp指向defer函数的第一个参数，本例为sum函数的参数a</span></span><br><span class="line">    argp := <span class="keyword">uintptr</span>(unsafe.Pointer(&amp;fn)) + unsafe.Sizeof(fn)</span><br><span class="line">    callerpc := getcallerpc() <span class="comment">// deferproc函数的返回地址</span></span><br><span class="line"> </span><br><span class="line">    d := newdefer(siz)</span><br><span class="line">    <span class="keyword">if</span> d._panic != <span class="literal">nil</span> &#123;</span><br><span class="line">        throw(<span class="string">"deferproc: d.panic != nil after newdefer"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    d.fn = fn <span class="comment">//需要延迟执行的函数</span></span><br><span class="line">    d.pc = callerpc  <span class="comment">//记录deferproc函数的返回地址，主要用于panic/recover</span></span><br><span class="line">    d.sp = sp <span class="comment">//调用deferproc之前rsp寄存器的值</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">//把defer函数需要用到的参数拷贝到d结构体后面，下面的deferrArgs返回的是一个地址</span></span><br><span class="line">    <span class="comment">//deferArgs(d) = d + sizeof(d) ，newdefer返回的内存空间 &gt;= deferArgs(d)</span></span><br><span class="line">    <span class="keyword">switch</span> siz &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">0</span>:</span><br><span class="line">        <span class="comment">// Do nothing.</span></span><br><span class="line">    <span class="keyword">case</span> sys.PtrSize: <span class="comment">//如果defered函数的参数只有指针大小则直接通过赋值来拷贝参数</span></span><br><span class="line">        *(*<span class="keyword">uintptr</span>)(deferArgs(d)) = *(*<span class="keyword">uintptr</span>)(unsafe.Pointer(argp))</span><br><span class="line">    <span class="keyword">default</span>: <span class="comment">//通过memmove拷贝defered函数的参数</span></span><br><span class="line">        memmove(deferArgs(d), unsafe.Pointer(argp), <span class="keyword">uintptr</span>(siz))</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// deferproc returns 0 normally.</span></span><br><span class="line">    <span class="comment">// a deferred func that stops a panic</span></span><br><span class="line">    <span class="comment">// makes the deferproc return 1.</span></span><br><span class="line">    <span class="comment">// the code the compiler generates always</span></span><br><span class="line">    <span class="comment">// checks the return value and jumps to the</span></span><br><span class="line">    <span class="comment">// end of the function if deferproc returns != 0.</span></span><br><span class="line">    return0()  <span class="comment">//通过汇编指令设置rax = 0</span></span><br><span class="line">    <span class="comment">// No code can go here - the C return register has</span></span><br><span class="line">    <span class="comment">// been set and must not be clobbered.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>deferproc 函数流程很清晰，它首先通过 newdefer 函数分配一个 _defer 结构体对象，然后把需要延迟执行的函数以及该函数需要用到的参数、调用 deferproc 函数时的 rsp 寄存器的值以及 deferproc 函数的返回地址保存在  _defer 结构体对象之中，最后通过 return0() 设置 rax 寄存器的值为 0 隐性的给调用者返回一个 0 值。</p>
<p>不知道大家是否会觉得很奇怪，deferproc 明明只会隐性的返回 0 值，但为什么上面的 f() 函数在调用了 deferproc 之后还用了一条指令来判断返回值是否是 0 呢，这不多此一举吗？事实上这里主要与 panic 和 recover 的实现机制有关，当程序发生 panic 之后，程序会“再次从 deferproc 函数返回”，这种情况下返回值就不是 0 了，因为 panic/recover 机制这一块比较复杂，所以我们以后会专门写一篇文章来分析，现在略过。</p>
<p>回到主题，_defer 结构体的定义为：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/runtime2.<span class="keyword">go</span> : <span class="number">727</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// A _defer holds an entry on the list of deferred calls.</span></span><br><span class="line"><span class="comment">// If you add a field here, add code to clear it in freedefer.</span></span><br><span class="line"><span class="keyword">type</span> _defer <span class="keyword">struct</span> &#123;</span><br><span class="line">    siz           <span class="keyword">int32</span>  <span class="comment">//defer函数的参数大小</span></span><br><span class="line">    started    <span class="keyword">bool</span></span><br><span class="line">    sp           <span class="keyword">uintptr</span>  <span class="comment">// sp at time of defer</span></span><br><span class="line">    pc           <span class="keyword">uintptr</span>  <span class="comment">// defer语句下一条语句的地址</span></span><br><span class="line">    fn           *funcval  <span class="comment">//需要被延迟执行的函数</span></span><br><span class="line">    _panic    *_panic  <span class="comment">// panic that is running defer</span></span><br><span class="line">    link         *_defer  <span class="comment">//同一个goroutine所有被延迟执行的函数通过该成员链在一起形成一个链表</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>该结构中的 sp、pc以及 _panic 成员主要与 panic/recover 有关，这里我们无需过多关注（ sp 成员还会被用来判断 _defer 结构体对象中保存的延迟执行函数是否应该在当前函数结束时执行，后面我们会分析到）。</p>
<p>对于本文的例子，初始化完成后的 _defer 结构体对象各成员的值大致如下：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">d.siz = <span class="number">16</span></span><br><span class="line">d.started = <span class="literal">false</span></span><br><span class="line">d.sp = 调用deferproc函数之前的rsp寄存器的值</span><br><span class="line">d.pc = <span class="number">0x0000000000488e36</span></span><br><span class="line">d.fn = &amp;funcval&#123;sum&#125;</span><br><span class="line">d._panic = <span class="literal">nil</span></span><br><span class="line">d._defer = <span class="literal">nil</span></span><br></pre></td></tr></table></figure></p>
<p>sum函数的参数a<br>sum函数的参数b<br>注意，defered 函数的参数并未在 _defer 结构体中定义，它所需要的参数在内存中紧跟在 _defer 结构体对象的后面。</p>
<p>我们接着看 newdefer 是如何分配 _defer 结构体对象的：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">proc/<span class="built_in">panic</span>.<span class="keyword">go</span> : <span class="number">205</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Allocate a Defer, usually using per-P pool.</span></span><br><span class="line"><span class="comment">// Each defer must be released with freedefer.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// This must not grow the stack because there may be a frame without</span></span><br><span class="line"><span class="comment">// stack map information when this is called.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">newdefer</span><span class="params">(siz <span class="keyword">int32</span>)</span> *_<span class="title">defer</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> d *_defer</span><br><span class="line">    sc := deferclass(<span class="keyword">uintptr</span>(siz))</span><br><span class="line">    gp := getg()  <span class="comment">//获取当前goroutine的g结构体对象</span></span><br><span class="line">    <span class="keyword">if</span> sc &lt; <span class="keyword">uintptr</span>(<span class="built_in">len</span>(p&#123;&#125;.deferpool)) &#123;</span><br><span class="line">        pp := gp.m.p.ptr()  <span class="comment">//与当前工作线程绑定的p</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">len</span>(pp.deferpool[sc]) == <span class="number">0</span> &amp;&amp; sched.deferpool[sc] != <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="comment">// Take the slow path on the system stack so</span></span><br><span class="line">            <span class="comment">// we don't grow newdefer's stack.</span></span><br><span class="line">            systemstack(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123; <span class="comment">//切换到系统栈</span></span><br><span class="line">                lock(&amp;sched.deferlock)</span><br><span class="line">                <span class="comment">//从全局_defer对象池拿一些到p的本地_defer对象池</span></span><br><span class="line">                <span class="keyword">for</span> <span class="built_in">len</span>(pp.deferpool[sc]) &lt; <span class="built_in">cap</span>(pp.deferpool[sc])/<span class="number">2</span> &amp;&amp; sched.deferpool[sc] != <span class="literal">nil</span> &#123;</span><br><span class="line">                    d := sched.deferpool[sc]</span><br><span class="line">                    sched.deferpool[sc] = d.link</span><br><span class="line">                    d.link = <span class="literal">nil</span></span><br><span class="line">                    pp.deferpool[sc] = <span class="built_in">append</span>(pp.deferpool[sc], d)</span><br><span class="line">                &#125;</span><br><span class="line">                unlock(&amp;sched.deferlock)</span><br><span class="line">            &#125;)</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> n := <span class="built_in">len</span>(pp.deferpool[sc]); n &gt; <span class="number">0</span> &#123;</span><br><span class="line">            d = pp.deferpool[sc][n<span class="number">-1</span>]</span><br><span class="line">            pp.deferpool[sc][n<span class="number">-1</span>] = <span class="literal">nil</span></span><br><span class="line">            pp.deferpool[sc] = pp.deferpool[sc][:n<span class="number">-1</span>]</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> d == <span class="literal">nil</span> &#123; <span class="comment">//如果p的缓存中没有可用的_defer结构体对象则从堆上分配</span></span><br><span class="line">        <span class="comment">// Allocate new defer+args.</span></span><br><span class="line">        <span class="comment">//因为roundupsize以及mallocgc函数都不会处理扩栈，所以需要切换到系统栈执行</span></span><br><span class="line">        systemstack(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123; </span><br><span class="line">            total := roundupsize(totaldefersize(<span class="keyword">uintptr</span>(siz)))</span><br><span class="line">            d = (*_defer)(mallocgc(total, deferType, <span class="literal">true</span>))</span><br><span class="line">        &#125;)</span><br><span class="line">        <span class="keyword">if</span> debugCachedWork &#123;</span><br><span class="line">            <span class="comment">// Duplicate the tail below so if there's a</span></span><br><span class="line">            <span class="comment">// crash in checkPut we can tell if d was just</span></span><br><span class="line">            <span class="comment">// allocated or came from the pool.</span></span><br><span class="line">            d.siz = siz</span><br><span class="line">            <span class="comment">//把新分配出来的d放入当前goroutine的_defer链表头</span></span><br><span class="line">            d.link = gp._defer</span><br><span class="line">            gp._defer = d</span><br><span class="line">            <span class="keyword">return</span> d</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    d.siz = siz</span><br><span class="line">    <span class="comment">//把新分配出来的d放入当前goroutine的_defer链表头</span></span><br><span class="line">    d.link = gp._defer</span><br><span class="line">    gp._defer = d  <span class="comment">//把新分配出来的d放入当前goroutine的_defer链表头</span></span><br><span class="line">    <span class="keyword">return</span> d</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>newdefer 函数比较长，大家可以参考上面的代码和注释加以理解。newdefer 函数首先会尝试从与当前工作线程绑定的 p 的 _defer 对象池和全局对象池中获取一个满足大小要求(sizeof(_defer) + siz向上取整至16的倍数)的 _defer 结构体对象，如果没有能够满足要求的空闲 _defer 对象则从堆上分一个，最后把分配到的对象链入当前 goroutine 的 _defer 链表的表头。</p>
<p>到此 defer 语句中被延迟执行的函数已经挂入当前 goroutine 的 _defer 链表，我们来简单的总结一下这个过程：</p>
<p>编译器会把 go 代码中 defer 语句翻译成对 deferproc 函数的调用；</p>
<p>deferproc 函数通过 newdefer 函数分配一个 _defer 结构体对象并放入当前 goroutine 的 _defer 链表的表头；</p>
<p>在 _defer 结构体对象中保存被延迟执行的函数 fn 的地址以及 fn 所需的参数；</p>
<p>返回到调用 deferproc 的函数继续执行后面的代码。</p>
<p>deferreturn 函数</p>
<p>分析完 deferproc，我们接着分析 deferreturn：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/<span class="built_in">panic</span>.<span class="keyword">go</span> : <span class="number">331</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Run a deferred function if there is one.</span></span><br><span class="line"><span class="comment">// The compiler inserts a call to this at the end of any</span></span><br><span class="line"><span class="comment">// function which calls defer.</span></span><br><span class="line"><span class="comment">// If there is a deferred function, this will call runtime·jmpdefer,</span></span><br><span class="line"><span class="comment">// which will jump to the deferred function such that it appears</span></span><br><span class="line"><span class="comment">// to have been called by the caller of deferreturn at the point</span></span><br><span class="line"><span class="comment">// just before deferreturn was called. The effect is that deferreturn</span></span><br><span class="line"><span class="comment">// is called again and again until there are no more deferred functions.</span></span><br><span class="line"><span class="comment">// Cannot split the stack because we reuse the caller's frame to</span></span><br><span class="line"><span class="comment">// call the deferred function.</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// The single argument isn't actually used - it just has its address</span></span><br><span class="line"><span class="comment">// taken so it can be matched against pending defers.</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">deferreturn</span><span class="params">(arg0 <span class="keyword">uintptr</span>)</span></span> &#123;</span><br><span class="line">    gp := getg()  <span class="comment">//获取当前goroutine对应的g结构体对象</span></span><br><span class="line">    d := gp._defer <span class="comment">//defer函数链表</span></span><br><span class="line">    <span class="keyword">if</span> d == <span class="literal">nil</span> &#123; </span><br><span class="line">        <span class="comment">//没有需要执行的函数直接返回，deferreturn和deferproc是配对使用的</span></span><br><span class="line">        <span class="comment">//为什么这里d可能为nil？因为deferreturn其实是一个递归调用，这个是递归结束条件之一</span></span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    sp := getcallersp()  <span class="comment">//获取调用deferreturn时的栈顶位置</span></span><br><span class="line">    <span class="keyword">if</span> d.sp != sp &#123;  <span class="comment">//递归结束条件之二</span></span><br><span class="line">        <span class="comment">//如果保存在_defer对象中的sp值与调用deferretuen时的栈顶位置不一样，直接返回</span></span><br><span class="line">       <span class="comment">//因为sp不一样表示d代表的是在其他函数中通过defer注册的延迟调用函数，比如:</span></span><br><span class="line">       <span class="comment">//a()-&gt;b()-&gt;c()它们都通过defer注册了延迟函数，那么当c()执行完时只能执行在c中注册的函数</span></span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Moving arguments around.</span></span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="comment">// Everything called after this point must be recursively</span></span><br><span class="line">    <span class="comment">// nosplit because the garbage collector won't know the form</span></span><br><span class="line">    <span class="comment">// of the arguments until the jmpdefer can flip the PC over to</span></span><br><span class="line">    <span class="comment">// fn.</span></span><br><span class="line">   <span class="comment">//把保存在_defer对象中的fn函数需要用到的参数拷贝到栈上，准备调用fn</span></span><br><span class="line">   <span class="comment">//注意fn的参数放在了调用调用者的栈帧中，而不是此函数的栈帧中</span></span><br><span class="line">    <span class="keyword">switch</span> d.siz &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0</span>:</span><br><span class="line">            <span class="comment">// Do nothing.</span></span><br><span class="line">        <span class="keyword">case</span> sys.PtrSize:</span><br><span class="line">            *(*<span class="keyword">uintptr</span>)(unsafe.Pointer(&amp;arg0)) = *(*<span class="keyword">uintptr</span>)(deferArgs(d))</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            memmove(unsafe.Pointer(&amp;arg0), deferArgs(d), <span class="keyword">uintptr</span>(d.siz))</span><br><span class="line">    &#125;</span><br><span class="line">    fn := d.fn</span><br><span class="line">    d.fn = <span class="literal">nil</span></span><br><span class="line">    gp._defer = d.link <span class="comment">//使gp._defer指向下一个_defer结构体对象</span></span><br><span class="line">   <span class="comment">//因为需要调用的函数d.fn已经保存在了fn变量中，它的参数也已经拷贝到了栈上，所以释放_defer结构体对象</span></span><br><span class="line">    freedefer(d) </span><br><span class="line">    jmpdefer(fn, <span class="keyword">uintptr</span>(unsafe.Pointer(&amp;arg0)))  <span class="comment">//调用fn</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>deferreturn 函数主要流程为：</p>
<p>通过当前 goroutine 对应的 g 结构体对象的 _defer 链表判断是否有需要执行的 defered 函数，如果没有（g._defer == nil 或则 defered 函数不是在 deferreturn 的 caller 函数中注册的函数）则直接返回；</p>
<p>从 _defer 对象中把 defered 函数需要的参数拷贝到栈上；</p>
<p>释放 _defer 结构体对象；</p>
<p>通过 jmpdefer 函数调用 defered 函数（比如本文的sum函数）。</p>
<p>deferreturn 函数虽然比较简单，但有2点需要注意：</p>
<p>代码中的两个提前return的条件：d == nil 和 d.sp != sp。其中 d == nil 在判断是否有 defered 函数需要执行，可能有些读者会有疑问，deferreturn 明明是与 deferproc 配套使用的，这里怎么会是nil呢？这个是因为deferreturn 函数其实是被递归调用的，每次调用它只会执行一个 defered 函数，比如本文使用的例子在 f() 函数中注册了一个 defered 函数(sum函数)，所以 deferreturn 函数会被调用两次，第一次进入时会去执行 sum 函数，第二次进入时 d 为 nil 就直接返回了；另外一个条件 d.sp != sp 在判断 d 对象所包装的 defered 函数现在是否应该被执行，比如有函数调用链a()-&gt;b()-&gt;c()，即 a 函数调用了 b 函数，b 函数又调用了 c 函数，它们都通过 defer 注册了延迟函数，那么当 c() 执行完时只能执行在 c 中注册的函数，而不能执行 a 函数和 b 函数注册的 defered 函数；</p>
<p>defered 函数的参数并不是放在 deferreturn 函数的栈帧中的，比如前面的例子，f() 调用 deferreturn 函数，所以 deferreturn 函数通过 memmove 把 sum 函数的两个参数 copy 到了 f() 函数的栈中，结合前面 f() 函数的汇编代码，可知在调用 jmpdefer 函数之前，f() 以及 deferreturn() 函数的栈帧大致如下：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/3820c64f460811abdab0e3a135deda76.png" alt></p>
<p>jmpdefer 函数</p>
<p>下面我们来看 jmpdefer 函数，该函数使用了些技巧实现了一个对 deferreturn 函数的递归调用：<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/asm_amd64.s : <span class="number">560</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// func jmpdefer(fv *funcval, argp uintptr)</span></span><br><span class="line"><span class="comment">// argp is a caller SP.</span></span><br><span class="line"><span class="comment">// called from deferreturn.</span></span><br><span class="line"><span class="comment">// 1. pop the caller</span></span><br><span class="line"><span class="comment">// 2. sub 5 bytes from the callers return</span></span><br><span class="line"><span class="comment">// 3. jmp to the argument</span></span><br><span class="line">TEXT runtime·jmpdefer(SB), NOSPLIT, $<span class="number">0</span><span class="number">-16</span></span><br><span class="line">    MOVQ  fv+<span class="number">0</span>(FP), DX  # fn，fn.fn = sum函数的地址</span><br><span class="line">    MOVQ  argp+<span class="number">8</span>(FP), BX  # caller sp</span><br><span class="line">    LEAQ  <span class="number">-8</span>(BX), SP  # caller sp after CALL</span><br><span class="line">    MOVQ  <span class="number">-8</span>(SP), BP  # restore BP as <span class="keyword">if</span> deferreturn returned (harmless <span class="keyword">if</span> framepointers not in use)</span><br><span class="line">    SUBQ  $<span class="number">5</span>, (SP)  # <span class="keyword">return</span> to CALL again</span><br><span class="line">    MOVQ  <span class="number">0</span>(DX), BX  </span><br><span class="line">    JMP  BX  # but first run the deferred function</span><br></pre></td></tr></table></figure></p>
<p>要理解这个函数的功能，需要对函数调用栈及函数调用过程有比较清晰的认识，如果对这部分不熟悉，可以先去看一下网上的资料或本公众号之前写的文章。</p>
<p>下面我们结合本文前面的例子程序来逐条分析 jmpdefer 函数的 7 条汇编指令。</p>
<p>第1条指令：</p>
<p>MOVQ  fv+0(FP), DX  # fn，fn.fn = sum函数的地址<br>把jmpdefer的第一个参数也就是结构体对象fn的地址放入DX寄存器，之后的代码就可以通过DX寄存器访问到fn.fn从而拿到 sum 函数的地址。</p>
<p>第2条指令：</p>
<p>MOVQ  argp+8(FP), BX  # caller sp<br>把jmpdefer的第二个参数放入 BX 寄存器，该参数是一个指针，它指向 sum 函数的第一个参数，见上图的箭头。</p>
<p>第3条指令：</p>
<p>LEAQ  -8(BX), SP  # caller sp after CALL<br>从第二条指令得知 BX 存放的是一个指针，BX - 8所指的位置是 deferreturn 函数执行完后的返回地址 0x488ef0，所以这条指令的作用是让 SP 寄存器指向 deferreturn 函数的返回地址所在的栈内存单元，执行完这条指令后 SP 寄存器与栈之间的关系如下图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/234c6e28bd0882798042ea05c15a04d3.png" alt></p>
<p>第4条指令：</p>
<p>MOVQ  -8(SP), BP  # restore BP as if deferreturn returned (harmless if framepointers not in use)<br>调整 BP 寄存器的值，因为此时 SP - 8 的位置存放的是 f() 函数的 rbp 寄存器的值，所以这条指令在调整 rbp 寄存器的值使其指向 f() 函数的栈帧的适当位置，执行完这条指令后 rbp 寄存器与栈之间的关系如下图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/5359c3a66d5cd2adb43c9366323568e7.png" alt></p>
<p>经过第3条和第4条指令之后，deferreturn 函数的栈帧被抛弃了，因为 jmpdefer 函数并不会直接返回到 deferreturn 之中，所以这里抛弃 deferreturn 函数的栈帧没有问题。</p>
<p>第5条指令：</p>
<p>SUBQ  $5, (SP)  # return to CALL again<br>CPU 在执行这条指令是，rsp 寄存器指向的是 deferreturn 函数的返回地址，也就是 f() 函数中的 0x0000000000488ef0 &lt;+272&gt;: mov    0x78(%rsp),%rbp 这一条指令的地址，即0x488ef0，所以这条指令把 rsp 寄存器所指的内存中的值 0x488ef0 减了 5 得到 0x488eeb，对照前面f函数的汇编代码可知，这个地址指向的是 0x0000000000488eeb &lt;+267&gt;: callq  0x427490 &lt;runtime.deferreturn&gt; 这条指令。注意看下图中rsp寄存器所指的内存单元中的值的变化：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2ae661c7dbf52ff6ebd5e59e4446721f.png" alt></p>
<p>到此，sum 函数的参数以及 sum 函数执行完的返回地址在栈上已经构造完成，下面开始调用 sum 函数。</p>
<p>第6～7条指令</p>
<p>MOVQ  0(DX), BX       # BX = fn.fn<br>JMP  BX  # but first run the deferred function<br>会跳转到 sum 函数去执行，完成对 sum 函数的调用，在 sum 函数的执行过程中，栈如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/fe24f63524c7b5426bc5aadd759e2f82.png" alt></p>
<p>因为 sum 函数的返回地址被上面的第5条指令设置成了 0x488eeb，所以等 sum 函数执行完成之后它会直接返回到 f() 函数的</p>
<p>0x0000000000488eeb &lt;+267&gt;: callq 0x427490 &lt;runtime.deferreturn&gt;<br>指令处继续执行，而这条指令又调用了 deferreturn 函数。回忆一下sum函数的调用路径 f()-&gt;deferreturn()-&gt;jmpdefer()-&gt;sum()，sum函数返回到 f() 时又再一次调用了 deferreturn ，这个过程从形式上看起来就是一个递归，只不过每次“递归”时并没有增加栈空间！</p>
<p>因为 f 函数只使用了一次 defer 语句，所以这里的第二次进入 deferreturn 函数会因为 d == nil 这个条件结束递归，然后返回到 f() 函数中继续执行后面的指令。</p>
<p>总结</p>
<p>最后我们来总结一下 defer 的实现机制。</p>
<p>对于如下所示的 defer 语句</p>
<p>func x() {<br>     …….<br>     defer y(……)<br>     …….<br>}<br>首先，编译器会把 defer 语句翻译成对 deferproc 函数的调用，deferproc 负责构造一个用来保存 y 函数的地址以及 y 函数需要用到的参数的 _defer 结构体对象，并把该对象加入当前 goroutine 对应的 g 结构体对象的 _defer 链表表头；</p>
<p>然后，编译器会在 x 函数的结尾处插入对 deferreturn 的调用，deferreturn 负责递归的调用 x 函数通过 defer 语句注册的函数。</p>
<p>总体说来，在不考虑 panic/recover 的情况下，go语言对 defer 的实现机制还是比较简单，但其具体实现细节还是有很多地方值得我们仔细思考和学习的。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>理解golang调度之二 ：Go调度器</title>
    <url>/go/%E7%90%86%E8%A7%A3go%E8%B0%83%E5%BA%A6/%E7%90%86%E8%A7%A3golang%E8%B0%83%E5%BA%A6%E4%B9%8B%E4%BA%8C%20%EF%BC%9AGo%E8%B0%83%E5%BA%A6%E5%99%A8/</url>
    <content><![CDATA[<p><a href="https://juejin.cn/post/6844903846825705485" rel="external nofollow noopener noreferrer" target="_blank">原文链接</a></p>
<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a><strong>简介</strong></h2><p>第一篇文章解释了关于操作系统层级的调度，这对于理解Go的调度是很重要的。这一部分我会在语义层级解释Go调度器是如何工作的，并且着重关注它的一些高级特性。Go 调度器是一个十分复杂的系统，特别细节的地方不重要，重要的是对于它的工作模式有一个好的理解，这会让你做出更好的工程方面的决定。</p>
<h2 id="从一个程序开始"><a href="#从一个程序开始" class="headerlink" title="从一个程序开始"></a><strong>从一个程序开始</strong></h2><p>当你的go程序启动，主机上定义的每一个虚拟内核都会为它分配一个逻辑处理器(P)，如果你的处理器上每个物理内核有多个硬件线程（超线程），每个硬件线程对于你的go程序来说就是一个虚拟内核。为了理解这个事情，看一下我的MacBook Pro的系统配置。<br>图2.1<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf54c9af604f8?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>你可以看到一个单独处理器有4个物理核心。配置表上没说每个物理核心有多少个硬件线程。Intel Core i7 处理器有自己的超线程，也就是每个物理内核上有两个硬件线程。因此Go程序知道并行执行操作系统线程的时候，会有8个虚拟内核可以用<br>验证一下，看一下下面的程序<br><strong>L1</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">"fmt"</span></span><br><span class="line">	<span class="string">"runtime"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// NumCPU returns the number of logical</span></span><br><span class="line">    <span class="comment">// CPUs usable by the current process.</span></span><br><span class="line">    fmt.Println(runtime.NumCPU())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>我在我的本机上运行这个程序，NumCPU()方法会返回8，我在本机上跑的任何Go程序会分配8个逻辑处理器(P)。<br>每个P会分配一个<strong>OS线程（M）</strong>。M代表machine。这个线程是OS来处理的，并且OS还负责把线程放置到一个core上去执行。这意味着当我跑一个Go程序在我的机器上，我有8个可用的线程去执行我的工作，每个线程单独连到一个P上。<br>每个Go程序同时也会有一个初始的<strong>Goroutine（G）</strong>。一个Goroutine本质上是一个协程（<a href="https://en.wikipedia.org/wiki/Coroutine" rel="external nofollow noopener noreferrer" target="_blank">Coroutine）</a>，但是在go里，把字面“C”替换为“G”所以我们叫Goroutine。你可以认为Goroutine是一个用户程序级别的线程而且它跟OS线程很多方面都类似。区别仅仅是OS线程在内核(Core)上进行上下文切换，而Goroutines是在M上。<br>最后一个让人困惑的就是运行队列。在Go 调度器中有两种不同的运行队列：全局运行队列<strong>（GRQ）</strong>和本地运行队列<strong>(LRQ)</strong>。每个P会分配一个LRQ去处理P的上下文要执行的Goroutines 。这些Goroutines会在绑定到P的M上进行上下文的切换。GRQ会处理还没有分配到P上的Goroutines 。Goroutines从GRQ挪到LRQ的过程一会我们一会儿会说。<br>图2.2是包含了所有相关组件的一张图片<br>图2.2<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf550e7092dd9?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br><strong>协作调度</strong><br>我们在第一部分的内容讲到了，OS调度器是一个抢占式调度器。也就是说你不知道调度器下一步会执行什么。内核所做的决定都是不确定的。运行在OS顶层的应用程序无法控制内核里面的调度，除非你使用同步的原始操作，例如atomic指令和mutex调用<br>Go调度器是Go runtime的一部分，Go runtime会编译到你应用程序里。这意味着Go调度器运行在内核之上的<a href="https://en.wikipedia.org/wiki/User_space" rel="external nofollow noopener noreferrer" target="_blank">用户空间(user space)</a><br>当前Go调度器采用的不是抢占式调度器，而是协作试调度器。协作试调度器，意味着调度器需要代码中安全点处发生的定义好的用户空间事件去做出调度决策。<br>Go的协作调度有一个非常棒的地方就是，它看上去像是抢占式的。你没办法预测Go调度器将要做什么，调度决策不是开发人员而是go runtime去做的。将Go调度器看做是一个抢占式调度器是很重要的，因为调度是不确定的，这里不需要再过多延伸。</p>
<h2 id="Goroutine状态"><a href="#Goroutine状态" class="headerlink" title="Goroutine状态"></a><strong>Goroutine状态</strong></h2><p>和线程一样。Goroutine有三种相同的高级状态。Goroutine可以是任何一种状态：等待（Waiting）、可执行（Runnable）、运行中（Executing）.<br>等待：此时Goroutine已经停止并且等待事件发生然后再次执行。这可能是出于等待操作系统（系统调用）或同步调用（原子操作atomic和互斥操作mutex）等原因。 这些类型的延迟是性能不佳的根本原因。<br>可执行： 此时Goroutine想要在M上执行分配给它的指令。如果有很多Goroutines想要M上的时间片，那么Goroutines必须等待更长时间。而且，随着更多Goroutines争夺时间片，单独Goroutines分配的时间就会缩短，这种类型的调度延时也会导致性能很差。<br>运行中：这意味着Goroutines已经放置在M上并且执行它的指令。此时应用程序的工作即将完成，这是我们想要的状态。</p>
<h2 id="上下文切换（Context-Switching）"><a href="#上下文切换（Context-Switching）" class="headerlink" title="上下文切换（Context Switching）"></a><strong>上下文切换（Context Switching）</strong></h2><p>Go调度程序需要明确定义的用户空间事件，这些事件发生在代码中的安全点以进行上下文切换。这些事件和安全点在函数调用时发生。函数调用对Go调度器的运行状况至关重要。Go 1.11 或者更低版本中，如果你跑一个不做函数调用的死循环，会导致调度器延时和垃圾回收延时。合理的时机使用函数调用十分重要。<br><strong>注意：相关issue和建议已经被提出来，并且应用到了1.12版本中。应用非协作的抢占式技术，使得在tight loop中进行抢占。</strong><br>Go程序中有4种类型的事件，允许调度器去做出调度决策。</p>
<ul>
<li>使用关键字 go</li>
<li>垃圾回收</li>
<li>系统调用</li>
<li>同步处理</li>
</ul>
<p><strong>使用关键字 go</strong><br>使用关键字go来创建Goroutine。一旦一个新的Goroutine创建好，调度器便有机会去做出调度决定<br><strong>垃圾回收</strong><br>GC时候会有它自己的Goroutines，这些Goroutines也需要M上的时间片。这会导致GC产生很多调度混乱。但是调度器很聪明，它知道Goroutines在做什么，然后会做出合理的调度决策。一个调度策略就是对那些想要访问堆的Goroutine，以及GC时候不会访问堆的Goroutine进行上下文切换。GC发生的时候有很多调度策略。<br><strong>系统调用</strong><br>如果一个Goroutine进行系统调用导致了M的阻塞，调度器有时候会用一个新的Goroutine从M上替换下这个Goroutine。但是有时候会需要一个新的M去执行挂在P队列上的Goroutine，这种情况我会在下一部分讲解。<br><strong>同步处理</strong><br>如果atomic、mutex或者是channel操作的调用导致了Goroutine的阻塞，调度器会切换一个新的Goroutine去执行。一旦那个Goroutine又可以重新执行了，他会被挂到队列上并最终在M上会上下文切换回去。</p>
<h2 id="异步系统调用"><a href="#异步系统调用" class="headerlink" title="异步系统调用"></a><strong>异步系统调用</strong></h2><p>当OS有能力去处理异步的系统调用时候，使用<a href="https://golang.org/src/runtime/netpoll.go" rel="external nofollow noopener noreferrer" target="_blank">网络轮询器(network poller)</a>去处理系统调用会更加高效。不同的操作系统分别使用了kqueue (MacOS)、epoll (Linux) 、 iocp (Windows) 对此作了实现。<br>今天许多操作系统都能处理基于网络(Networking-based)的系统调用。这也是网络轮询器(network poller)这一名字的由来，因为它的主要用途就是处理网络操作。网络系统上通过使用network poller，调度器可以防止Goroutines在系统调用的时候阻塞M。这可以让M能够去执行P的 LRQ上面的其他Goroutines，而不是再去新建一个M。这可以减少OS上的调度加载。<br>最好的方式就是给一个例子看看这些东西是如何工作的。</p>
<p>图2.3<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf5560f4e49d5?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.3展示了基本的调用图例。Goroutine-1正在M上面执行并且有3个Goroutine在LRQ上等待想要获取M的时间片。network poller此时空闲没事做。</p>
<p>图2.4<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf55982d27f34?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.4中 Goroutine-1想要进行network system调用，因此Goroutine-1移到了network poller上面然后处理异步调用，一旦Goroutine-1从M上移到network poller，M便可以去执行其他LRQ上的Goroutine。此时 Goroutine-2切换到了M上面。</p>
<p>图2.5<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf55bbbed11db?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.5中，network poller的异步网络调用完成并且Goroutine-1回到了P的LRQ上面。一旦Goroutine-1能够切换回M上，Go的相关代码便能够再次执行。很大好处是，在执行network system调用时候，我们不需要其他额外的M。network poller有一个OS线程能够有效的处理事件循环。</p>
<h2 id="同步系统调用"><a href="#同步系统调用" class="headerlink" title="同步系统调用"></a><strong>同步系统调用</strong></h2><p>当Goroutine想进行系统调用无法异步进行该怎么办呢？这种情况下，无法使用 network poller并且Goroutine产生的系统调用会阻塞M。很不幸但是我们无法阻止这种情况发生。一个例子就是基于文件的系统调用。如果你使用CGO，当你调用C函数的时候也会有其他情况发生会阻塞M。<br><strong>注意：Windows操作系统确实有能力去异步进行基于文件的系统调用。从技术上讲，在Windows上运行时可以使用network poller。</strong><br>我们看一下同步系统调用(比如file I/O)阻塞M的时候会发生什么。</p>
<p>图2.6<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf55e32f07226?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.6又一次展示了我们的基本调度图例。但是这一次Goroutine-1的同步系统调用会阻塞M1</p>
<p>图2.7<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf55ff1310409?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.7中，调度器能够确定Goroutine-1已经阻塞了M。这时，调度器会从P上拿下来M1，Goroutine-1依旧在M1上。然后调度器会拿来一个新的M2去服务P。此时LRQ上的Goroutine-2会上下文切换到M2上。如果已经有一个可用的M了，那么直接用它会比新建一个M要更快。</p>
<p>图2.8<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf561edabcd86?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.8中，Goroutine-1的阻塞系统调用结束了。此时Goroutine-1能够回到LRQ的后面并且能够重新被P执行。M1之后会被放置一边供未来类似的情况使用。<br><strong>工作窃取（Work Stealing）</strong><br>从另一个层面看，调度器的工作方式其实是work-stealing的。这种行为在一些情况下能够让调度更有效率。我们最不想看到的事情是一个M进入了等待状态，因为这一旦发生，OS将会把M从core上切换下来。这意味着即使有可执行的Goroutine， P此时也没法干活了，直到M重新切换回core上。Work stealing同时也会平衡P上的所有Goroutines从而能够使工作更好的分配，更有效率。<br>让我们看一个例子</p>
<p>图2.9<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf563f3514ef3?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.9里，我们有个多线程的Go程序。两个P分别服务4个Goroutines。并且一个单独的Goroutine在GRQ上。那么如果其中一个P很快执行完它所有的Goroutines会怎么样？</p>
<p>图2.10<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf565adfce212?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>P1没有更多Goroutine去执行了，但是在GRQ和P2的LRQ中都有可执行的Goroutines。这种情况P1会去窃取工作，Work Stealing的规则如下<br><strong>L2</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime.schedule() &#123;</span><br><span class="line">    <span class="comment">// only 1/61 of the time, check the global runnable queue for a G.</span></span><br><span class="line">    <span class="comment">// if not found, check the local queue.</span></span><br><span class="line">    <span class="comment">// if not found,</span></span><br><span class="line">    <span class="comment">//     try to steal from other Ps.</span></span><br><span class="line">    <span class="comment">//     if not, check the global runnable queue.</span></span><br><span class="line">    <span class="comment">//     if not found, poll network.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>所以基于L2的规则，P1需要去看P2的LRQ上的Goroutines并且拿走一半。</p>
<p>图2.11<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf56793b6a00a?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.11中，一半的Goroutines从P2上偷走，P1现在可以执行那些Goroutines<br>如果P2完成了所有Goroutines的执行，并且P1的LRQ上已经空了会怎么样？</p>
<p><strong>图2.12</strong><br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf5697020672f?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.12中，P2完成了它所有的工作，现在想要偷点什么。首先，它会去看P1的LRQ却发现什么也没有了。接下来他会去看GRQ。他会找到Goroutine-9</p>
<p>图2.13<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf56b8b29690a?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.13中，P2从GRQ上偷走了Goroutine-9并且开始执行它的工作。这种work stealing的很大好处是，它让M一直有事情做而不是闲下来。这种work stealing 可以看做内部的M的轮转，这种轮转的好处在这篇博客里做了很好的解释。<br><strong>实际例子</strong><br>为了看一下Go调度器为了在同一时间里做更多事情，了解这一切是如何一块发生的。首先想象这样一个多线程的C语言应用，程序需要处理两个OS线程，他们俩互相进行通信。</p>
<p>图2.14<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf57086d6631b?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.14中，有两个线程，相互通信。线程1上下文切换到Core1上并且现在正在执行，这允许线程1向线程2发送消息。<br>注意：通信方式不重要。重要的是这个过程里的线程状态。</p>
<p>图2.15<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf5726cf2f753?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>在图2.15中，一旦线程1完成发送消息，它就需要等待响应。这会导致线程1从Core1切换下来并处于等待状态。一旦线程2收到消息通知，它就会进入可执行的状态。现在OS进行上下文切换然后线程2在一个Core2上面执行。接下来线程2处理消息然后给线程1发送一个新消息。</p>
<p>图2.16<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf5742a161615?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.16里。随着线程1收到线程2的消息，又一次发生了上下文切换。现在线程2从执行中的状态切换为等待的状态。并且线程1从等待状态切换到了可执行状态，最终回到运行状态。现在线程1可以处理并发送一个新消息回去。<br>所有的上下文切换(context switches)和状态的改变都需要花费时间去处理，这就限制了工作速度。每一次上下文切换<br>会导致50ns的潜在延迟，硬件执行指令的期望时间是每ns 12个指令，你会看到上下文切换的时候就少执行600个指令。因为这些线程在不同的core之前切来切去，cache-line未命中导致的延迟也会增加。<br>我们来看一下相同例子，使用Goroutines和Go调度器做替换。</p>
<p>图2.17<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf575d12a0a17?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.17中，有两个Goroutines相互传递消息。G1上下文切换到M1上进行工作处理，之前这都是在Core1上发生的事情。现在是G1向G2发送消息。</p>
<p>图2.18<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf5777b735f0b?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图2.18中，一旦G1发送完消息，它就会等待响应返回。这会让G1从M1上切换下来，并且进入到等到状态。一旦G2收到消息通知，它会进入可执行状态。现在Go调度器会把G2切换到M1上去执行，M1依旧在Core1上跑着。接下来G2处理消息然后给G1发送一个新消息。</p>
<p>图2.19<br><img src="https://user-gold-cdn.xitu.io/2019/5/19/16acf579161597fa?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>在图2.19中，随着G1收到G2发送来的消息，又一次发生上下文切换。现在G2从执行中的状态切换到等待状态并且G1从等待中切换到可执行状态，最终回到运行的状态，G1又能够处理并向G2发送新的消息了。<br>表面上事情并没有什么不同。不论你使用线程还是Goroutines都有上下文切换和状态改变的过程。但是线程和Goroutines之间有一个重要的差别可能不会被明显注意到。<br>在使用Goroutines的场景，整个过程一直使用的是相同的OS线程和Core。这也就意味着，从OS的视角，OS线程从来没有进入到waiting状态，一次也没有。结果就是我们在线程中上下文切换丢失的指令在Goroutines中不会丢失。<br>本质上讲，在OS层级go把io/blocking类型的工作转变成了cpu密集型的工作。由于所有上下文切换的过程都发生在应用程序的级别，上下文切换不会像线程一样丢掉600个指令（平均来说）。Go调度器还有助于提高cache-line的效率和NUMA。这也是为什么我们不需要比虚拟内核数更多的线程。在Go里，随着时间推移更多事情会被处理，因为Go调度器会尝试用更少的线程并且每个线程去做更多事情，这有助于减少OS和硬件层级的加载延迟。<br><strong>结论</strong><br>Go调度程序的设计在考虑操作系统和硬件工作复杂性方面确实令人惊讶。 在操作系统级别将IO /blocking工作转换为CPU密集型工作，是在利用更多CPU容量的过程中获得巨大成功的地方。 这就是为什么你不需要比虚拟内核数更多的OS线程。 每个虚拟内核一个OS线程情况下，你可以合理的期望你的所有工作(CPU密集、IO密集)都能够完成。对于网络程序和那些不需要系统调用阻塞OS线程的程序，也能够完成。<br>作为开发人员，你依旧需要理解在处理不同类型工作的时候你的程序正在做什么。你不能为了想要更好性能去无限制创建goroutine。Less is always more，但是通过理解了go调度器，你可以更好的做出决定。下一部分，我会探讨以保守的方式利用并发来提升性能的方法，但是对于代码的复杂性还是要做出平衡。</p>
<p>原文链接：<a href="https://www.ardanlabs.com/blog/2018/08/scheduling-in-go-part2.html" rel="external nofollow noopener noreferrer" target="_blank">www.ardanlabs.com/blog/2018/0…</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go调度</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go调度</tag>
      </tags>
  </entry>
  <entry>
    <title>理解golang调度之三：并发</title>
    <url>/go/%E7%90%86%E8%A7%A3go%E8%B0%83%E5%BA%A6/%E7%90%86%E8%A7%A3golang%E8%B0%83%E5%BA%A6%E4%B9%8B%E4%B8%89%EF%BC%9A%E5%B9%B6%E5%8F%91/</url>
    <content><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a><strong>简介</strong></h2><p>当我在解决一个问题尤其是新问题的时候，我开始不会去考虑并发(concurrency)是否合适。我首先会去找一系列的解决方式然后确保它有效。然后在可读性和技术方案评估之后，我会开始去考虑并发是否实际合理。有些时候并发的好处是显而易见的，但是有时候并不是很明显。<br>第一篇文章，我解释了OS调度器的相关内容，我觉得这部分对于你写多线程代码很重要。第二篇里，我讲解了一些Go调度器的一些内容，这部分对于你理解和写go的并发代码很有帮助。在这篇文章里，我会在OS和Go调度器层面让你去深层次的理解并发到底是什么。<br>这部分内容的目标是：</p>
<p>你的工作负载(workloads)使用并发是否合适，为此提供一些指导建议<br>不同工作负载的含义，并针对其作出相应的工程方面的决策。</p>
<h2 id="什么是并发"><a href="#什么是并发" class="headerlink" title="什么是并发"></a><strong>什么是并发</strong></h2><p>并发的含义就是无序的执行。给你一系列的指令，去找到一个方式可以无序执行而且和有序执行产生同样的结果。这个问题在你面前，显而易见的是无序执行会增加一些足够的性能增益在计算了复杂性成本之后，但是你可能会觉得无序执行是不可能的甚至是没有意义的。<br>你也要清楚一点，并发和并行是不一样的。并行是在相同时间内同时执行两个或两个以上的指令，这和并发的概念不一样。<br>图3.1<br><img src="https://user-gold-cdn.xitu.io/2019/5/20/16ad5476fb5b267c?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt><br>图3.1里，你看到主机上有两个逻辑处理器。每个都有他们单独的OS线程(M)依附于一个独立的硬件线程(Core)。你可以看到2个Goroutine(G1和G2) 正在并行在各自的OS/硬件线程上面同时执行它们的指令。在每个逻辑处理器里，有3个Goroutines以轮转的方式共享OS线程。这些Goroutines正在以无序的方式并发地执行它们的指令，并且在OS线程上共享时间片。<br>这里有一个问题。有些时候利用并发而不采用并行实际上会降低你的吞吐量，有趣的是，有时候利用并发同时加上并行处理也不会为你带来你理想中的性能增益。</p>
<h2 id="工作负载-workloads"><a href="#工作负载-workloads" class="headerlink" title="工作负载(workloads)"></a><strong>工作负载(workloads)</strong></h2><p>你是如何知道无序执行(并发)是可行的呢？了解你所处理问题的工作负载(workload)是一个起点。有两种类型的工作负载在并发的时候要考虑到。</p>
<ul>
<li><strong>CPU密集(CPU-Bound)</strong>：这种工作负载情况不会有Goroutines自动切换到waiting状态的情况，也不会有自动从waiting状态切到其他状态的情况。这种情况发生在进行持续计算的时候。线程计算Pi值就是CPU-Bound。</li>
<li><strong>IO密集(IO-Bound)</strong>：这种工作负载会导致Goroutines自动进入等待状态。这种工作发生在持续地请求网络资源、或者是进行系统调用、或者是等待事件发生的情况。一个Goroutines需要读文件就是IO-Bound。我把同步事件(mutexes，atomic)类似导致Goroutine等待的情况归到此类。</li>
</ul>
<p>cpu-bound的工作负载，你需要并行去使用并发。一个单独的OS/硬件线程处理多个Goroutines效率很低，因为Goroutines在这个工作负载里不会主动进入或者是离开等待状态。Goroutines数多于OS/硬件线程数的时候会降低工作负载的执行速度，因为从OS线程换上或者是换下Goroutines会有延迟(切换的时间)。上下文切换会在workload里创建出“一切都停止”事件，因为在切换的时候你的所有workload都不会执行。<br>在IO-Bound的workloads里，你不需要并行去使用并发。一个单独OS/硬件线程可以有效率地处理多个Goroutines，因为Goroutines作为它自己workload的一部分可以自动进入或者离开等待状态(waiting)。Goroutines数量多于OS/硬件线程数可以加速workload的执行,因为Goroutines在OS线程上切换不会创建“一切都停止”事件。你的workload会自然停止并且这会让一个不同的Goroutine去有效率地使用相同的OS/硬件线程，而不是让OS/硬件线程空闲下来。<br>你如何知道每个硬件线程设置多少个Goroutines会有最好的吞吐量呢？太少的Goroutines你会有更多空闲时间。太多Goroutines你会有更多上下文切换延迟。这件事情你需要考虑，但是这超出了<br>本篇文章讲述的范围。<br>现在，我们需要看一些代码来巩固你去判断什么时候workload可以利用并发，以及什么时候需要利用并行什么时候不需要并行。</p>
<h2 id="整数累加"><a href="#整数累加" class="headerlink" title="整数累加"></a><strong>整数累加</strong></h2><p>不需要太复杂的代码，就看一下下面的add函数。它计算了一堆整数的和。<br><strong>L1</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">36</span> <span class="function"><span class="keyword">func</span> <span class="title">add</span><span class="params">(numbers []<span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="number">37</span>     <span class="keyword">var</span> v <span class="keyword">int</span></span><br><span class="line"><span class="number">38</span>     <span class="keyword">for</span> _, n := <span class="keyword">range</span> numbers &#123;</span><br><span class="line"><span class="number">39</span>         v += n</span><br><span class="line"><span class="number">40</span>     &#125;</span><br><span class="line"><span class="number">41</span>     <span class="keyword">return</span> v</span><br><span class="line"><span class="number">42</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>在L1的36行，声明了add方法，他接受一个int型的slice，然后返回它们的和。37定义了一个变量v去做数字累加。38行函数遍历这些整数，39行把当前数加上去。最后41行返回它们的和。<br>Question: add是否适合无序执行？我相信答案肯定是yes。整数集可以被分解成更小的lists，并且这些lists可以并行去处理。一旦所有lists都各自加完，这一系列lists的和可以加到一起，得到上面代码里一样的结果。<br>但是，另一个问题来了。我们应该分多少个lists去分别单独处理才能得到最好的吞吐量呢？为了回答这个问题，你需要知道add方法运行到底是哪种workload。add方法处理的是CPU-Bound类型的workload因为这是一个纯数学计算的方法，它不会导致goroutines进入自动等待状态。这意味着每个OS/硬件线程一个Goroutine即可获得理想的吞吐量。<br>下面的L2是add方法的并发版本。<br><strong>注意：你有多种方式去写add的并发版本，不必去纠结代码本身。</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">44</span> <span class="function"><span class="keyword">func</span> <span class="title">addConcurrent</span><span class="params">(goroutines <span class="keyword">int</span>, numbers []<span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="number">45</span>     <span class="keyword">var</span> v <span class="keyword">int64</span></span><br><span class="line"><span class="number">46</span>     totalNumbers := <span class="built_in">len</span>(numbers)</span><br><span class="line"><span class="number">47</span>     lastGoroutine := goroutines - <span class="number">1</span></span><br><span class="line"><span class="number">48</span>     stride := totalNumbers / goroutines</span><br><span class="line"><span class="number">49</span></span><br><span class="line"><span class="number">50</span>     <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"><span class="number">51</span>     wg.Add(goroutines)</span><br><span class="line"><span class="number">52</span></span><br><span class="line"><span class="number">53</span>     <span class="keyword">for</span> g := <span class="number">0</span>; g &lt; goroutines; g++ &#123;</span><br><span class="line"><span class="number">54</span>         <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(g <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line"><span class="number">55</span>             start := g * stride</span><br><span class="line"><span class="number">56</span>             end := start + stride</span><br><span class="line"><span class="number">57</span>             <span class="keyword">if</span> g == lastGoroutine &#123;</span><br><span class="line"><span class="number">58</span>                 end = totalNumbers</span><br><span class="line"><span class="number">59</span>             &#125;</span><br><span class="line"><span class="number">60</span></span><br><span class="line"><span class="number">61</span>             <span class="keyword">var</span> lv <span class="keyword">int</span></span><br><span class="line"><span class="number">62</span>             <span class="keyword">for</span> _, n := <span class="keyword">range</span> numbers[start:end] &#123;</span><br><span class="line"><span class="number">63</span>                 lv += n</span><br><span class="line"><span class="number">64</span>             &#125;</span><br><span class="line"><span class="number">65</span></span><br><span class="line"><span class="number">66</span>             atomic.AddInt64(&amp;v, <span class="keyword">int64</span>(lv))</span><br><span class="line"><span class="number">67</span>             wg.Done()</span><br><span class="line"><span class="number">68</span>         &#125;(g)</span><br><span class="line"><span class="number">69</span>     &#125;</span><br><span class="line"><span class="number">70</span></span><br><span class="line"><span class="number">71</span>     wg.Wait()</span><br><span class="line"><span class="number">72</span></span><br><span class="line"><span class="number">73</span>     <span class="keyword">return</span> <span class="keyword">int</span>(v)</span><br><span class="line"><span class="number">74</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>在L2里面，addConcurrent方法是add方法的并发版本。这里有很多代码因此我只讲解重要的代码行<br><strong>Line 48：每个Goroutine会有它单独的一个小的list去处理。list的size由整数集的size去除以Goroutines的数量得到。<br>Line 53：创建goroutines线程池去处理加数操作。<br>Line 57-59：最后一个goroutines会处理剩下的最后一个list，它可能比其他list的size要大。<br>Line 66：所有lists算出来的sum，加到一起得到最后的一个sum。</strong><br>并发版本比有序版本更复杂，这种复杂度是否值得呢？回答这个问题最好的方式就是写一个benchmark。这里我用了一个一千万个数大小整数集，并且关掉了垃圾回收。这里对add和addConcurrent进行了对比。<br><strong>L3</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">BenchmarkSequential</span><span class="params">(b *testing.B)</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; b.N; i++ &#123;</span><br><span class="line">        add(numbers)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">BenchmarkConcurrent</span><span class="params">(b *testing.B)</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; b.N; i++ &#123;</span><br><span class="line">        addConcurrent(runtime.NumCPU(), numbers)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>L3展示了benchmark函数。下面是当Goroutines只有一个单独的OS/硬件线程能用的情况。有序版本使用1个Goroutine然后并发版本使用runtime.NumCPU数，我的机器上是8。这个例子下面，并发版本没有使用并行去做并发。<br>L4<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">10 Million Numbers using 8 goroutines with 1 core</span><br><span class="line">2.9 GHz Intel 4 Core i7</span><br><span class="line">Concurrency WITHOUT Parallelism</span><br><span class="line">-----------------------------------------------------------------------------</span><br><span class="line">$ GOGC=off go <span class="built_in">test</span> -cpu 1 -run none -bench . -benchtime 3s</span><br><span class="line">goos: darwin</span><br><span class="line">goarch: amd64</span><br><span class="line">pkg: github.com/ardanlabs/gotraining/topics/go/testing/benchmarks/cpu-bound</span><br><span class="line">BenchmarkSequential      	    1000	   5720764 ns/op : ~10% Faster</span><br><span class="line">BenchmarkConcurrent      	    1000	   6387344 ns/op</span><br><span class="line">BenchmarkSequentialAgain 	    1000	   5614666 ns/op : ~13% Faster</span><br><span class="line">BenchmarkConcurrentAgain 	    1000	   6482612 ns/op</span><br></pre></td></tr></table></figure></p>
<p><strong>注意：在你的本机上跑BenchMark很复杂。有很多因素会导致你的benchmarks不够精确。你的机器尽可能的处于空闲状态这样可以去跑一段时间benchmark，以确保自己看到的结果和上面的大体一致。使用测试工具跑两遍benchmark能够得到更一致的结果。</strong><br>L4给出的benchmark表明，在仅有一个单独OS/硬件线程时候有序版本比并发版本大约要快%10–%13。这在我们的意料之中，因为并发版本需要在一个单独的OS线程上频繁进行上下文切换(context switches)以及处理Goroutines。<br>下面是每个Goroutines有一个单独的OS/硬件线程的情况下的结果。有序版本用一个Goroutine然后并发版本使用runtime.NumCPU，在我本机上是8个。这种情况下利用了并行去处理并发。<br><strong>L5</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">10 Million Numbers using 8 goroutines with 8 cores</span><br><span class="line">2.9 GHz Intel 4 Core i7</span><br><span class="line">Concurrency WITH Parallelism</span><br><span class="line">-----------------------------------------------------------------------------</span><br><span class="line">$ GOGC=off go <span class="built_in">test</span> -cpu 8 -run none -bench . -benchtime 3s</span><br><span class="line">goos: darwin</span><br><span class="line">goarch: amd64</span><br><span class="line">pkg: github.com/ardanlabs/gotraining/topics/go/testing/benchmarks/cpu-bound</span><br><span class="line">BenchmarkSequential-8        	    1000	   5910799 ns/op</span><br><span class="line">BenchmarkConcurrent-8        	    2000	   3362643 ns/op : ~43% Faster</span><br><span class="line">BenchmarkSequentialAgain-8   	    1000	   5933444 ns/op</span><br><span class="line">BenchmarkConcurrentAgain-8   	    2000	   3477253 ns/op : ~41% Faster</span><br></pre></td></tr></table></figure></p>
<p>L5中的benchmark表明了,每个Goroutines使用一个OS/硬件线程的时候并发版本比有序版本要快大约41%–43%。这是我们期望中的事情，因为所有的Goroutines现在都在并行执行，8个Goroutines现在都在同一时间并发执行。</p>
<h2 id="排序"><a href="#排序" class="headerlink" title="排序"></a><strong>排序</strong></h2><p>需要明白，不是所有的CPU-bound的workloads都适合并发处理。当把工作拆解或者是把结果合并需要花费很大代价的时候这种说法是正确的。这种情况我们可以看一个算法的例子：冒泡排序。看一下下Go实现的冒泡排序。<br>L6<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">01</span> <span class="keyword">package</span> main</span><br><span class="line"><span class="number">02</span></span><br><span class="line"><span class="number">03</span> <span class="keyword">import</span> <span class="string">"fmt"</span></span><br><span class="line"><span class="number">04</span></span><br><span class="line"><span class="number">05</span> <span class="function"><span class="keyword">func</span> <span class="title">bubbleSort</span><span class="params">(numbers []<span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line"><span class="number">06</span>     n := <span class="built_in">len</span>(numbers)</span><br><span class="line"><span class="number">07</span>     <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; n; i++ &#123;</span><br><span class="line"><span class="number">08</span>         <span class="keyword">if</span> !sweep(numbers, i) &#123;</span><br><span class="line"><span class="number">09</span>             <span class="keyword">return</span></span><br><span class="line"><span class="number">10</span>         &#125;</span><br><span class="line"><span class="number">11</span>     &#125;</span><br><span class="line"><span class="number">12</span> &#125;</span><br><span class="line"><span class="number">13</span></span><br><span class="line"><span class="number">14</span> <span class="function"><span class="keyword">func</span> <span class="title">sweep</span><span class="params">(numbers []<span class="keyword">int</span>, currentPass <span class="keyword">int</span>)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line"><span class="number">15</span>     <span class="keyword">var</span> idx <span class="keyword">int</span></span><br><span class="line"><span class="number">16</span>     idxNext := idx + <span class="number">1</span></span><br><span class="line"><span class="number">17</span>     n := <span class="built_in">len</span>(numbers)</span><br><span class="line"><span class="number">18</span>     <span class="keyword">var</span> swap <span class="keyword">bool</span></span><br><span class="line"><span class="number">19</span></span><br><span class="line"><span class="number">20</span>     <span class="keyword">for</span> idxNext &lt; (n - currentPass) &#123;</span><br><span class="line"><span class="number">21</span>         a := numbers[idx]</span><br><span class="line"><span class="number">22</span>         b := numbers[idxNext]</span><br><span class="line"><span class="number">23</span>         <span class="keyword">if</span> a &gt; b &#123;</span><br><span class="line"><span class="number">24</span>             numbers[idx] = b</span><br><span class="line"><span class="number">25</span>             numbers[idxNext] = a</span><br><span class="line"><span class="number">26</span>             swap = <span class="literal">true</span></span><br><span class="line"><span class="number">27</span>         &#125;</span><br><span class="line"><span class="number">28</span>         idx++</span><br><span class="line"><span class="number">29</span>         idxNext = idx + <span class="number">1</span></span><br><span class="line"><span class="number">30</span>     &#125;</span><br><span class="line"><span class="number">31</span>     <span class="keyword">return</span> swap</span><br><span class="line"><span class="number">32</span> &#125;</span><br><span class="line"><span class="number">33</span></span><br><span class="line"><span class="number">34</span> <span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="number">35</span>     org := []<span class="keyword">int</span>&#123;<span class="number">1</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">4</span>, <span class="number">8</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">0</span>&#125;</span><br><span class="line"><span class="number">36</span>     fmt.Println(org)</span><br><span class="line"><span class="number">37</span></span><br><span class="line"><span class="number">38</span>     bubbleSort(org)</span><br><span class="line"><span class="number">39</span>     fmt.Println(org)</span><br><span class="line"><span class="number">40</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>在L6里，给出了Go版本的冒泡排序。排序算法遍历每个值并在整数集上进行数据交替。根据初始顺序不同，排序可能需要多次的遍历。<br>Question: bubbleSort的workload适合无序执行吗？答案肯定是no。整数集可以分解成更小的lists并且这些lists可以并发地排序。但是所有并发工作完成之后，并没有一个有效的方式再去把这些小的lists排序到一起。这里是一个并发版本的冒泡排序。<br><strong>L8</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">01</span> <span class="function"><span class="keyword">func</span> <span class="title">bubbleSortConcurrent</span><span class="params">(goroutines <span class="keyword">int</span>, numbers []<span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line"><span class="number">02</span>     totalNumbers := <span class="built_in">len</span>(numbers)</span><br><span class="line"><span class="number">03</span>     lastGoroutine := goroutines - <span class="number">1</span></span><br><span class="line"><span class="number">04</span>     stride := totalNumbers / goroutines</span><br><span class="line"><span class="number">05</span></span><br><span class="line"><span class="number">06</span>     <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"><span class="number">07</span>     wg.Add(goroutines)</span><br><span class="line"><span class="number">08</span></span><br><span class="line"><span class="number">09</span>     <span class="keyword">for</span> g := <span class="number">0</span>; g &lt; goroutines; g++ &#123;</span><br><span class="line"><span class="number">10</span>         <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(g <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line"><span class="number">11</span>             start := g * stride</span><br><span class="line"><span class="number">12</span>             end := start + stride</span><br><span class="line"><span class="number">13</span>             <span class="keyword">if</span> g == lastGoroutine &#123;</span><br><span class="line"><span class="number">14</span>                 end = totalNumbers</span><br><span class="line"><span class="number">15</span>             &#125;</span><br><span class="line"><span class="number">16</span></span><br><span class="line"><span class="number">17</span>             bubbleSort(numbers[start:end])</span><br><span class="line"><span class="number">18</span>             wg.Done()</span><br><span class="line"><span class="number">19</span>         &#125;(g)</span><br><span class="line"><span class="number">20</span>     &#125;</span><br><span class="line"><span class="number">21</span></span><br><span class="line"><span class="number">22</span>     wg.Wait()</span><br><span class="line"><span class="number">23</span></span><br><span class="line"><span class="number">24</span>     <span class="comment">// Ugh, we have to sort the entire list again.</span></span><br><span class="line"><span class="number">25</span>     bubbleSort(numbers)</span><br><span class="line"><span class="number">26</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>L8中，bubbleSortConcurrent方法是bubbleSort的并发版本。它使用多个Goroutines去并发地排序整个整数集的一部分。结果你得到的是各自的排序的list。结果你最终在25行还是要整个list做一次排序。<br>因为冒泡排序的本质就是遍历整个list。25行调用bubbleSort直接否定了任何并发的潜在收益。冒泡排序里，使用并发并没有性能上的增益。</p>
<h2 id="读取文件"><a href="#读取文件" class="headerlink" title="读取文件"></a><strong>读取文件</strong></h2><p>我们给出了2个CPU-Bound类型的workloads，那么IO-Bound类型的workload情况是什么样的？当Goroutines自动进入或者是离开waiting状态，情况会有什么不同么？看一个IO-bound类型的workload，它的工作内容是读取文件并查找文本。<br>第一个版本是一个有序版本的find方法<br><strong>L10</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">42</span> <span class="function"><span class="keyword">func</span> <span class="title">find</span><span class="params">(topic <span class="keyword">string</span>, docs []<span class="keyword">string</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="number">43</span>     <span class="keyword">var</span> found <span class="keyword">int</span></span><br><span class="line"><span class="number">44</span>     <span class="keyword">for</span> _, doc := <span class="keyword">range</span> docs &#123;</span><br><span class="line"><span class="number">45</span>         items, err := read(doc)</span><br><span class="line"><span class="number">46</span>         <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="number">47</span>             <span class="keyword">continue</span></span><br><span class="line"><span class="number">48</span>         &#125;</span><br><span class="line"><span class="number">49</span>         <span class="keyword">for</span> _, item := <span class="keyword">range</span> items &#123;</span><br><span class="line"><span class="number">50</span>             <span class="keyword">if</span> strings.Contains(item.Description, topic) &#123;</span><br><span class="line"><span class="number">51</span>                 found++</span><br><span class="line"><span class="number">52</span>             &#125;</span><br><span class="line"><span class="number">53</span>         &#125;</span><br><span class="line"><span class="number">54</span>     &#125;</span><br><span class="line"><span class="number">55</span>     <span class="keyword">return</span> found</span><br><span class="line"><span class="number">56</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>在L10里面，你看到一个有序版本的find函数。line 43定义了一个found变量去存topic在文档里的出现次数。line 44，对所有文档进行遍历，并且在45行上使用read方法对每个doc进行读取。最后从49–53行，使用strings包的Contains方法去检查topic是否在读取到的items里面。如果发现，found变量就对应加一。<br>这里是find调用的read方法的实现。<br><strong>L11</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">33</span> <span class="function"><span class="keyword">func</span> <span class="title">read</span><span class="params">(doc <span class="keyword">string</span>)</span> <span class="params">([]item, error)</span></span> &#123;</span><br><span class="line"><span class="number">34</span>     time.Sleep(time.Millisecond) <span class="comment">// Simulate blocking disk read.</span></span><br><span class="line"><span class="number">35</span>     <span class="keyword">var</span> d document</span><br><span class="line"><span class="number">36</span>     <span class="keyword">if</span> err := xml.Unmarshal([]<span class="keyword">byte</span>(file), &amp;d); err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="number">37</span>         <span class="keyword">return</span> <span class="literal">nil</span>, err</span><br><span class="line"><span class="number">38</span>     &#125;</span><br><span class="line"><span class="number">39</span>     <span class="keyword">return</span> d.Channel.Items, <span class="literal">nil</span></span><br><span class="line"><span class="number">40</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>read方法以一个time.Sleep方法开始。这个里模拟了真实从硬盘读取文档的系统调用所产生的延迟。设置这个延迟对我们精确地测试有序版本和并发版本find方法的性能差异十分重要。然后在35–39行，测试的xml文档存储在fine的全局变量里，它被反序列化成一个要去处理的struct。最后返回了一个items的集合。<br>下面是一个并发版本代码。<br>注意：有多种方式去写并发版本代码，不要纠结于这个代码本身实现。<br><strong>L12</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number">58</span> <span class="function"><span class="keyword">func</span> <span class="title">findConcurrent</span><span class="params">(goroutines <span class="keyword">int</span>, topic <span class="keyword">string</span>, docs []<span class="keyword">string</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="number">59</span>     <span class="keyword">var</span> found <span class="keyword">int64</span></span><br><span class="line"><span class="number">60</span></span><br><span class="line"><span class="number">61</span>     ch := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">string</span>, <span class="built_in">len</span>(docs))</span><br><span class="line"><span class="number">62</span>     <span class="keyword">for</span> _, doc := <span class="keyword">range</span> docs &#123;</span><br><span class="line"><span class="number">63</span>         ch &lt;- doc</span><br><span class="line"><span class="number">64</span>     &#125;</span><br><span class="line"><span class="number">65</span>     <span class="built_in">close</span>(ch)</span><br><span class="line"><span class="number">66</span></span><br><span class="line"><span class="number">67</span>     <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"><span class="number">68</span>     wg.Add(goroutines)</span><br><span class="line"><span class="number">69</span></span><br><span class="line"><span class="number">70</span>     <span class="keyword">for</span> g := <span class="number">0</span>; g &lt; goroutines; g++ &#123;</span><br><span class="line"><span class="number">71</span>         <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="number">72</span>             <span class="keyword">var</span> lFound <span class="keyword">int64</span></span><br><span class="line"><span class="number">73</span>             <span class="keyword">for</span> doc := <span class="keyword">range</span> ch &#123;</span><br><span class="line"><span class="number">74</span>                 items, err := read(doc)</span><br><span class="line"><span class="number">75</span>                 <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="number">76</span>                     <span class="keyword">continue</span></span><br><span class="line"><span class="number">77</span>                 &#125;</span><br><span class="line"><span class="number">78</span>                 <span class="keyword">for</span> _, item := <span class="keyword">range</span> items &#123;</span><br><span class="line"><span class="number">79</span>                     <span class="keyword">if</span> strings.Contains(item.Description, topic) &#123;</span><br><span class="line"><span class="number">80</span>                         lFound++</span><br><span class="line"><span class="number">81</span>                     &#125;</span><br><span class="line"><span class="number">82</span>                 &#125;</span><br><span class="line"><span class="number">83</span>             &#125;</span><br><span class="line"><span class="number">84</span>             atomic.AddInt64(&amp;found, lFound)</span><br><span class="line"><span class="number">85</span>             wg.Done()</span><br><span class="line"><span class="number">86</span>         &#125;()</span><br><span class="line"><span class="number">87</span>     &#125;</span><br><span class="line"><span class="number">88</span></span><br><span class="line"><span class="number">89</span>     wg.Wait()</span><br><span class="line"><span class="number">90</span></span><br><span class="line"><span class="number">91</span>     <span class="keyword">return</span> <span class="keyword">int</span>(found)</span><br><span class="line"><span class="number">92</span> &#125;</span><br></pre></td></tr></table></figure></p>
<p>L12是find方法的并发版本。并发版本有30行代码，而非并发版本代码只有13行。我的目标是处理未知数量的documents时候控制Goroutines的数量。这里我选择在池化模式里使用一个channel去给池子里的goroutines喂数据。<br>这部分代码比较多，我只讲解重要部分<br>Line 61-64: 创建一个channel去处理所有的documents。<br>Line 65 关闭这个channel，来让池子里的goroutines在所有documents处理完成后能自动停止。<br>Line 70:创建一个goroutines线程池<br>Line 73–83:每一个池子里的goroutine从channel接受一个document，读取到内存然后检查内容是否有topic。匹配的话,lfound就加一个。<br>Line 84:把每个单独goroutines跑出来的数加到一起。<br>并发版本确实比有序版本代码更加复杂，这个复杂性是否值得？验证的最好方式就是再次写一个benchmark。我用了1000个documents的集合，并且关闭了垃圾回收。一个是顺序版本find，一个是并发版本findConcurrent<br><strong>L13</strong><br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">BenchmarkSequential</span><span class="params">(b *testing.B)</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; b.N; i++ &#123;</span><br><span class="line">        find(<span class="string">"test"</span>, docs)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">BenchmarkConcurrent</span><span class="params">(b *testing.B)</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; b.N; i++ &#123;</span><br><span class="line">        findConcurrent(runtime.NumCPU(), <span class="string">"test"</span>, docs)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>L13给出了benchmark。下面是当所有goroutines只有一个OS/硬件线程的时候。顺序代码使用1个goroutines，而并发版本是runtime.NumCPU的数，在我本机上是8。这种情况下，我们没用并行去做并发。<br><strong>L14</strong><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">10 Thousand Documents using 8 goroutines with 1 core</span><br><span class="line">2.9 GHz Intel 4 Core i7</span><br><span class="line">Concurrency WITHOUT Parallelism</span><br><span class="line">-----------------------------------------------------------------------------</span><br><span class="line">$ GOGC=off go <span class="built_in">test</span> -cpu 1 -run none -bench . -benchtime 3s</span><br><span class="line">goos: darwin</span><br><span class="line">goarch: amd64</span><br><span class="line">pkg: github.com/ardanlabs/gotraining/topics/go/testing/benchmarks/io-bound</span><br><span class="line">BenchmarkSequential      	       3	1483458120 ns/op</span><br><span class="line">BenchmarkConcurrent      	      20	 188941855 ns/op : ~87% Faster</span><br><span class="line">BenchmarkSequentialAgain 	       2	1502682536 ns/op</span><br><span class="line">BenchmarkConcurrentAgain 	      20	 184037843 ns/op : ~88% Faster</span><br></pre></td></tr></table></figure></p>
<p>L14里面表明了，在只有一个单独OS/硬件线程的时候，并发版本大概要比顺序版本代码快87%–%88。这是我们预料到的因为每个Goroutines都能有效的共享这一个OS/硬件线程。在read调用的时候每个goroutines能够自动进行上下文切换，这样OS/硬件线程会一直有事情做。<br>下面是使用并行去做并发处理。<br>L15<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">10 Thousand Documents using 8 goroutines with 1 core</span><br><span class="line">2.9 GHz Intel 4 Core i7</span><br><span class="line">Concurrency WITH Parallelism</span><br><span class="line">-----------------------------------------------------------------------------</span><br><span class="line">$ GOGC=off go <span class="built_in">test</span> -run none -bench . -benchtime 3s</span><br><span class="line">goos: darwin</span><br><span class="line">goarch: amd64</span><br><span class="line">pkg: github.com/ardanlabs/gotraining/topics/go/testing/benchmarks/io-bound</span><br><span class="line">BenchmarkSequential-8        	       3	1490947198 ns/op</span><br><span class="line">BenchmarkConcurrent-8        	      20	 187382200 ns/op : ~88% Faster</span><br><span class="line">BenchmarkSequentialAgain-8   	       3	1416126029 ns/op</span><br><span class="line">BenchmarkConcurrentAgain-8   	      20	 185965460 ns/op : ~87% Faster</span><br></pre></td></tr></table></figure></p>
<p>L15的benchmark结果说明，额外的OS/硬件线程并没有提供更好的性能。</p>
<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a><strong>结论</strong></h2><p>这篇文章的目的就是让你知道什么时候你的workload适合使用并发。考虑到不同的场景，我给出了不同的例子。<br>你可以清楚的看到IO-Bound类型的workload并不需要使用并行处理去获得性能的大幅增加，这正好跟CPU-Bound类型的工作截然相反。像类似冒泡算法这种，使用并发其实会增加代码复杂度，而且不会有任何性能增益。所以，一定要确定你的workload是否适合使用并发场景，这是很重要的事情。</p>
<p>原文链接：<a href="https://juejin.cn/post/6844903847568080904" rel="external nofollow noopener noreferrer" target="_blank">www.ardanlabs.com/blog/2018/1…</a></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go内存详解</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go内存详解</tag>
      </tags>
  </entry>
  <entry>
    <title>为什么 Go 占用那么多的虚拟内存？</title>
    <url>/go/%E7%9F%A5%E8%AF%86%E7%82%B9/%E4%B8%BA%E4%BB%80%E4%B9%88%20Go%20%E5%8D%A0%E7%94%A8%E9%82%A3%E4%B9%88%E5%A4%9A%E7%9A%84%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98%EF%BC%9F/</url>
    <content><![CDATA[<p><a href="https://mp.weixin.qq.com/s?__biz=MzUxMDI4MDc1NA==&amp;mid=2247484139&amp;idx=1&amp;sn=f1bd1793d2ae3d9ead1277807596b0b9&amp;chksm=f90417b6ce739ea0e3d6bbb969708e92e2e958b721908791706c452fccd119c4add3c44292d4&amp;scene=178&amp;cur_album_id=1515516076481101825#rd" rel="external nofollow noopener noreferrer" target="_blank">转载自煎鱼的blog</a></p>
<p>前段时间，某同学说某服务的容器因为超出内存限制，不断地重启，问我们是不是有内存泄露，赶紧排查，然后解决掉，省的出问题。</p>
<p>我们大为震惊，赶紧查看监控+报警系统和性能分析，发现应用指标压根就不高，不像有泄露的样子。</p>
<p>问题到底是出在哪里了呢，我们进入某个容器里查看了 <code>top</code> 的系统指标：</p>
<figure class="highlight jboss-cli"><table><tr><td class="code"><pre><span class="line">PID       VSZ    RSS   <span class="string">...</span> COMMAND</span><br><span class="line">67459     2007m  136m  <span class="string">...</span> <span class="string">./eddycjy-server</span></span><br></pre></td></tr></table></figure>
<p>看上去也没什么大开销的东西，就一个 Go 进程？就这？</p>
<p><img src="1.gif" alt="图片"></p>
<p>再定眼一看，某同学就说 VSZ 那么高，而某云上的容器内存指标居然恰好和 VSZ 的值相接近，因此就怀疑是不是 VSZ 所导致的，觉得存在一定的关联关系。</p>
<p>这个猜测的结果到底是否正确呢？</p>
<h2 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h2><p>本篇文章将<strong>主要围绕 Go 进程的 VSZ 来进行剖析</strong>，看看到底它为什么那么 “高”。</p>
<p>第一节为前置的补充知识，大家可按顺序阅读。</p>
<h3 id="什么是-VSZ"><a href="#什么是-VSZ" class="headerlink" title="什么是 VSZ"></a>什么是 VSZ</h3><p>VSZ 是该进程所能使用的虚拟内存总大小，它包括进程可以访问的所有内存，其中包括了被换出的内存（Swap）、已分配但未使用的内存以及来自共享库的内存。</p>
<h3 id="为什么要虚拟内存"><a href="#为什么要虚拟内存" class="headerlink" title="为什么要虚拟内存"></a>为什么要虚拟内存</h3><p>在前面我们有了解到 VSZ 其实就是该进程的虚拟内存总大小，那<strong>如果我们想了解 VSZ 的话，那我们得先了解 “为什么要虚拟内存？”</strong>。</p>
<p>本质上来讲，在一个系统中的进程是与其他进程共享 CPU 和主存资源的。</p>
<p>因此在现代的操作系统中，多进程的使用非常的常见，如果太多的进程需要太多的内存，在没有虚拟内存的情况下，物理内存很可能会不够用，就会导致其中有些任务无法运行，更甚至会出现一些很奇怪的现象。</p>
<p>例如 “某一个进程不小心写了另一个进程使用的内存”，就会造成内存破坏，因此虚拟内存是非常重要的一个媒介。</p>
<h3 id="虚拟内存包含了什么"><a href="#虚拟内存包含了什么" class="headerlink" title="虚拟内存包含了什么"></a>虚拟内存包含了什么</h3><p>虚拟内存，又分为：</p>
<ul>
<li>内核虚拟内存。</li>
<li>进程虚拟内存。</li>
</ul>
<p>每一个进程的虚拟内存都是独立的， 内部结构如下图所示。</p>
<p><img src="2.png" alt="图片"></p>
<p>在内核虚拟内存中，包含了内核中的代码和数据结构。</p>
<p>内核虚拟内存中的某些区域会被映射到所有进程共享的物理页面中去，因此你会看到 ”内核虚拟内存“ 中实际上是包含了 ”物理内存“ 的，它们两者存在映射关系。</p>
<p>而从应用场景上来讲，每个进程也会去共享内核的代码和全局数据结构，因此就会被映射到所有进程的物理页面中去。</p>
<p><img src="3.png" alt="图片"></p>
<h3 id="虚拟内存的重要能力"><a href="#虚拟内存的重要能力" class="headerlink" title="虚拟内存的重要能力"></a>虚拟内存的重要能力</h3><p>为了更有效地管理内存并且减少出错，现代系统提供了一种对主存的抽象概念，也就是今天的主角，叫做虚拟内存（VM）。</p>
<p>虚拟内存是硬件异常、硬件地址翻译、主存、磁盘文件和内核软件交互的地方，它为每个进程提供了一个大的、一致的和私有的地址空间，虚拟内存提供了三个重要的能力：</p>
<ol>
<li>它将主存看成是一个存储在磁盘上的地址空间的高速缓存，在主存中只保存活动区域，并根据需要在磁盘和主存之间来回传送数据，通过这种方式，它高效地使用了主存。</li>
<li>它为每个进程提供了一致的地址空间，从而简化了内存管理。</li>
<li>它保护了每个进程的地址空间不被其他进程破坏。</li>
</ol>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>上面发散的可能比较多，简单来讲，对于本文我们重点关注这些知识点，如下：</p>
<ul>
<li>虚拟内存它是有各式各样内存交互的地方，它包含的不仅仅是 “自己”，<strong>而在本文中，我们只需要关注 VSZ，也就是进程虚拟内存，它包含了你的代码、数据、堆、栈段和共享库</strong>。</li>
<li>虚拟内存作为内存保护的工具，能够保证进程之间的内存空间独立，不受其他进程的影响，因此每一个进程的 VSZ 大小都不一样，互不影响。</li>
<li>虚拟内存的存在，系统给各进程分配的内存之和是可以大于实际可用的物理内存的，因此你也会发现你进程的物理内存总是比虚拟内存低的多的多。</li>
</ul>
<h2 id="排查问题"><a href="#排查问题" class="headerlink" title="排查问题"></a>排查问题</h2><p>在了解了基础知识后，我们正式开始排查问题，第一步我们先编写一个测试程序，看看没有什么业务逻辑的 Go 程序，它初始的 VSZ 是怎么样的。</p>
<h3 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h3><p>应用代码：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">func</span> <span class="selector-tag">main</span>() &#123;</span><br><span class="line"> <span class="attribute">r </span>:= gin.<span class="built_in">Default</span>()</span><br><span class="line"> r.<span class="built_in">GET</span>(<span class="string">"/ping"</span>, func(c *gin.Context) &#123;</span><br><span class="line">  c.<span class="built_in">JSON</span>(<span class="number">200</span>, gin.H&#123;</span><br><span class="line">   <span class="string">"message"</span>: <span class="string">"pong"</span>,</span><br><span class="line">  &#125;)</span><br><span class="line"> &#125;)</span><br><span class="line"> <span class="selector-tag">r</span><span class="selector-class">.Run</span>("<span class="selector-pseudo">:8001")</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>查看进程情况：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">$ ps aux 67459</span><br><span class="line">USER      PID  %CPU %MEM      VSZ    RSS   <span class="built_in">..</span>.</span><br><span class="line">eddycjy 67459   0.0  0.0  4297048    960   <span class="built_in">..</span>.</span><br></pre></td></tr></table></figure>
<p>从结果上来看，VSZ 为 4297048K，也就是 4G 左右，咋一眼看过去还是挺吓人的，明明没有什么业务逻辑，但是为什么那么高呢，真是令人感到好奇。</p>
<h3 id="确认有没有泄露"><a href="#确认有没有泄露" class="headerlink" title="确认有没有泄露"></a>确认有没有泄露</h3><p>在未知的情况下，我们可以首先看下 <code>runtime.MemStats</code> 和 <code>pprof</code>，确定应用到底有没有泄露。不过我们这块是演示程序，什么业务逻辑都没有，因此可以确定和应用没有直接关系。</p>
<figure class="highlight vala"><table><tr><td class="code"><pre><span class="line"><span class="meta"># runtime.MemStats</span></span><br><span class="line"><span class="meta"># Alloc = 1298568</span></span><br><span class="line"><span class="meta"># TotalAlloc = 1298568</span></span><br><span class="line"><span class="meta"># Sys = 71893240</span></span><br><span class="line"><span class="meta"># Lookups = 0</span></span><br><span class="line"><span class="meta"># Mallocs = 10013</span></span><br><span class="line"><span class="meta"># Frees = 834</span></span><br><span class="line"><span class="meta"># HeapAlloc = 1298568</span></span><br><span class="line"><span class="meta"># HeapSys = 66551808</span></span><br><span class="line"><span class="meta"># HeapIdle = 64012288</span></span><br><span class="line"><span class="meta"># HeapInuse = 2539520</span></span><br><span class="line"><span class="meta"># HeapReleased = 64012288</span></span><br><span class="line"><span class="meta"># HeapObjects = 9179</span></span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<h3 id="Go-FAQ"><a href="#Go-FAQ" class="headerlink" title="Go FAQ"></a>Go FAQ</h3><p>接着我第一反应是去翻了 Go FAQ（因为看到过，有印象），其问题为 “Why does my Go process use so much virtual memory?”，回答如下：</p>
<blockquote>
<p>The Go memory allocator reserves a large region of virtual memory as an arena for allocations. This virtual memory is local to the specific Go process; the reservation does not deprive other processes of memory.</p>
<p>To find the amount of actual memory allocated to a Go process, use the Unix top command and consult the RES (Linux) or RSIZE (macOS) columns.</p>
</blockquote>
<p>这个 FAQ 是在 2012 年 10 月 提交 的，这么多年了也没有更进一步的说明，再翻了 issues 和 forum，一些关闭掉的 issue 都指向了 FAQ，这显然无法满足我的求知欲，因此我继续往下探索，看看里面到底都摆了些什么。</p>
<h3 id="查看内存映射"><a href="#查看内存映射" class="headerlink" title="查看内存映射"></a>查看内存映射</h3><p>在上图中，我们有提到进程虚拟内存，主要包含了你的代码、数据、堆、栈段和共享库，那初步怀疑是不是进程做了什么内存映射，导致了大量的内存空间被保留呢，为了确定这一点，我们通过如下命令去排查：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">$ vmmap --wide <span class="number">67459</span></span><br><span class="line">...</span><br><span class="line">==== Non-writable regions for process <span class="number">67459</span></span><br><span class="line">REGION TYPE                      START - END             [ VSIZE  RSDNT  DIRTY   SWAP] PRT/MAX SHRMOD PURGE    REGION DETAIL</span><br><span class="line">__TEXT                 <span class="number">00000001065</span>ff000<span class="number">-000000010667</span>b000 [  <span class="number">496</span>K   <span class="number">492</span>K     <span class="number">0</span>K     <span class="number">0</span>K] r-x/rwx SM=COW          /bin/zsh</span><br><span class="line">__LINKEDIT             <span class="number">0000000106687000</span><span class="number">-0000000106699000</span> [   <span class="number">72</span>K    <span class="number">44</span>K     <span class="number">0</span>K     <span class="number">0</span>K] r--/rwx SM=COW          /bin/zsh</span><br><span class="line">MALLOC metadata        <span class="number">000000010669</span>b000<span class="number">-000000010669</span>c000 [    <span class="number">4</span>K     <span class="number">4</span>K     <span class="number">4</span>K     <span class="number">0</span>K] r--/rwx SM=COW          DefaultMallocZone_0x10669b000 zone structure</span><br><span class="line">...</span><br><span class="line">__TEXT                 <span class="number">00007</span>fff76c31000<span class="number">-00007</span>fff76c5f000 [  <span class="number">184</span>K   <span class="number">168</span>K     <span class="number">0</span>K     <span class="number">0</span>K] r-x/r-x SM=COW          /usr/lib/system/libxpc.dylib</span><br><span class="line">__LINKEDIT             <span class="number">00007</span>fffe7232000<span class="number">-00007</span>ffff32cb000 [<span class="number">192.6</span>M  <span class="number">17.4</span>M     <span class="number">0</span>K     <span class="number">0</span>K] r--/r-- SM=COW          dyld shared cache combined __LINKEDIT</span><br><span class="line">...        </span><br><span class="line"></span><br><span class="line">==== Writable regions for process <span class="number">67459</span></span><br><span class="line">REGION TYPE                      START - END             [ VSIZE  RSDNT  DIRTY   SWAP] PRT/MAX SHRMOD PURGE    REGION DETAIL</span><br><span class="line">__DATA                 <span class="number">000000010667</span>b000<span class="number">-0000000106682000</span> [   <span class="number">28</span>K    <span class="number">28</span>K    <span class="number">28</span>K     <span class="number">0</span>K] rw-/rwx SM=COW          /bin/zsh</span><br><span class="line">...   </span><br><span class="line">__DATA                 <span class="number">0000000106716000</span><span class="number">-000000010671e000</span> [   <span class="number">32</span>K    <span class="number">28</span>K    <span class="number">28</span>K     <span class="number">4</span>K] rw-/rwx SM=COW          /usr/lib/zsh/<span class="number">5.3</span>/zsh/zle.so</span><br><span class="line">__DATA                 <span class="number">000000010671e000</span><span class="number">-000000010671</span>f000 [    <span class="number">4</span>K     <span class="number">4</span>K     <span class="number">4</span>K     <span class="number">0</span>K] rw-/rwx SM=COW          /usr/lib/zsh/<span class="number">5.3</span>/zsh/zle.so</span><br><span class="line">__DATA                 <span class="number">0000000106745000</span><span class="number">-0000000106747000</span> [    <span class="number">8</span>K     <span class="number">8</span>K     <span class="number">8</span>K     <span class="number">0</span>K] rw-/rwx SM=COW          /usr/lib/zsh/<span class="number">5.3</span>/zsh/complete.so</span><br><span class="line">__DATA                 <span class="number">000000010675</span>a000<span class="number">-000000010675</span>b000 [    <span class="number">4</span>K     <span class="number">4</span>K     <span class="number">4</span>K     <span class="number">0</span>K] rw-</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<p>这块主要是利用 macOS 的 <code>vmmap</code> 命令去查看内存映射情况，这样就可以知道这个进程的内存映射情况，从输出分析来看，<strong>这些关联共享库占用的空间并不大，导致 VSZ 过高的根本原因不在共享库和二进制文件上，但是并没有发现大量保留内存空间的行为，这是一个问题点</strong>。</p>
<p>注：若是 Linux 系统，可使用 <code>cat /proc/PID/maps</code> 或 <code>cat /proc/PID/smaps</code> 查看。</p>
<h3 id="查看系统调用"><a href="#查看系统调用" class="headerlink" title="查看系统调用"></a>查看系统调用</h3><p>既然在内存映射中，我们没有明确的看到保留内存空间的行为，那我们接下来看看该进程的系统调用，确定一下它是否存在内存操作的行为，如下：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="string">$</span> <span class="string">sudo</span> <span class="string">dtruss</span> <span class="string">-a</span> <span class="string">./awesomeProject</span></span><br><span class="line"><span class="string">...</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15620</span>       <span class="number">6</span>      <span class="number">3</span> <span class="string">mprotect(0x1BC4000,</span> <span class="number">0x1000</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0</span> <span class="number">0</span></span><br><span class="line"><span class="string">...</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15781</span>       <span class="number">9</span>      <span class="number">4</span> <span class="string">sysctl([CTL_HW,</span> <span class="number">3</span><span class="string">,</span> <span class="number">0</span><span class="string">,</span> <span class="number">0</span><span class="string">,</span> <span class="number">0</span><span class="string">,</span> <span class="number">0</span><span class="string">]</span> <span class="string">(2),</span> <span class="number">0x7FFEEFBFFA64</span><span class="string">,</span> <span class="number">0x7FFEEFBFFA68</span><span class="string">,</span> <span class="number">0x0</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0</span> <span class="number">0</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15783</span>       <span class="number">3</span>      <span class="number">1</span> <span class="string">sysctl([CTL_HW,</span> <span class="number">7</span><span class="string">,</span> <span class="number">0</span><span class="string">,</span> <span class="number">0</span><span class="string">,</span> <span class="number">0</span><span class="string">,</span> <span class="number">0</span><span class="string">]</span> <span class="string">(2),</span> <span class="number">0x7FFEEFBFFA64</span><span class="string">,</span> <span class="number">0x7FFEEFBFFA68</span><span class="string">,</span> <span class="number">0x0</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0</span> <span class="number">0</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15899</span>       <span class="number">7</span>      <span class="number">2</span> <span class="string">mmap(0x0,</span> <span class="number">0x40000</span><span class="string">,</span> <span class="number">0x3</span><span class="string">,</span> <span class="number">0x1002</span><span class="string">,</span> <span class="number">0xFFFFFFFFFFFFFFFF</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0x4000000</span> <span class="number">0</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15930</span>       <span class="number">3</span>      <span class="number">1</span> <span class="string">mmap(0xC000000000,</span> <span class="number">0x4000000</span><span class="string">,</span> <span class="number">0x0</span><span class="string">,</span> <span class="number">0x1002</span><span class="string">,</span> <span class="number">0xFFFFFFFFFFFFFFFF</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0xC000000000</span> <span class="number">0</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15934</span>       <span class="number">4</span>      <span class="number">2</span> <span class="string">mmap(0xC000000000,</span> <span class="number">0x4000000</span><span class="string">,</span> <span class="number">0x3</span><span class="string">,</span> <span class="number">0x1012</span><span class="string">,</span> <span class="number">0xFFFFFFFFFFFFFFFF</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0xC000000000</span> <span class="number">0</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15936</span>       <span class="number">2</span>      <span class="number">0</span> <span class="string">mmap(0x0,</span> <span class="number">0x2000000</span><span class="string">,</span> <span class="number">0x3</span><span class="string">,</span> <span class="number">0x1002</span><span class="string">,</span> <span class="number">0xFFFFFFFFFFFFFFFF</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0x59B7000</span> <span class="number">0</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15942</span>       <span class="number">2</span>      <span class="number">0</span> <span class="string">mmap(0x0,</span> <span class="number">0x210800</span><span class="string">,</span> <span class="number">0x3</span><span class="string">,</span> <span class="number">0x1002</span><span class="string">,</span> <span class="number">0xFFFFFFFFFFFFFFFF</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0x4040000</span> <span class="number">0</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15947</span>       <span class="number">2</span>      <span class="number">0</span> <span class="string">mmap(0x0,</span> <span class="number">0x10000</span><span class="string">,</span> <span class="number">0x3</span><span class="string">,</span> <span class="number">0x1002</span><span class="string">,</span> <span class="number">0xFFFFFFFFFFFFFFFF</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0x1BD0000</span> <span class="number">0</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">15993</span>       <span class="number">3</span>      <span class="number">0</span> <span class="string">madvise(0xC000000000,</span> <span class="number">0x2000</span><span class="string">,</span> <span class="number">0x8</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0</span> <span class="number">0</span></span><br><span class="line"> <span class="attr">4374/0x206a2:</span>     <span class="number">16004</span>       <span class="number">2</span>      <span class="number">0</span> <span class="string">mmap(0x0,</span> <span class="number">0x10000</span><span class="string">,</span> <span class="number">0x3</span><span class="string">,</span> <span class="number">0x1002</span><span class="string">,</span> <span class="number">0xFFFFFFFFFFFFFFFF</span><span class="string">,</span> <span class="number">0x0</span><span class="string">)</span>   <span class="string">=</span> <span class="number">0x1BE0000</span> <span class="number">0</span></span><br><span class="line"><span class="string">...</span></span><br></pre></td></tr></table></figure>
<p>在这小节中，我们通过 macOS 的 <code>dtruss</code> 命令监听并查看了运行这个程序所进行的所有系统调用，发现了与内存管理有一定关系的方法如下：</p>
<ul>
<li>mmap：创建一个新的虚拟内存区域，但这里需要注意，<strong>就是当系统调用 mmap 时，它只是从虚拟内存中申请了一段空间出来，并不会去分配和映射真实的物理内存，而当你访问这段空间的时候，才会在当前时间真正的去分配物理内存</strong>。那么对应到我们实际应用的进程中，那就是 VSZ 的增长后，而该内存空间又未正式使用的话，物理内存是不会有增长的。</li>
<li>madvise：提供有关使用内存的建议，例如：MADV_NORMAL、MADV_RANDOM、MADV_SEQUENTIAL、MADV_WILLNEED、MADV_DONTNEED 等等。</li>
<li>mprotect：设置内存区域的保护情况，例如：PROT_NONE、PROT_READ、PROT_WRITE、PROT_EXEC、PROT_SEM、PROT_SAO、PROT_GROWSUP、PROT_GROWSDOWN 等等。</li>
<li>sysctl：在内核运行时动态地修改内核的运行参数。</li>
</ul>
<p>在此比较可疑的是 <code>mmap</code> 方法，它在 <code>dtruss</code> 的最终统计中一共调用了 10 余次，我们可以相信它在 Go Runtime 的时候进行了大量的虚拟内存申请。</p>
<p>我们再接着往下看，看看到底是在什么阶段进行了虚拟内存空间的申请。</p>
<p>注：若是 Linux 系统，可使用 <code>strace</code> 命令。</p>
<h3 id="查看-Go-Runtime"><a href="#查看-Go-Runtime" class="headerlink" title="查看 Go Runtime"></a>查看 Go Runtime</h3><h4 id="启动流程"><a href="#启动流程" class="headerlink" title="启动流程"></a>启动流程</h4><p>通过上述的分析，我们可以知道在 Go 程序启动的时候 VSZ 就已经不低了，并且确定不是共享库等的原因，且程序在启动时系统调用确实存在 <code>mmap</code> 等方法的调用。</p>
<p>那么我们可以充分怀疑 Go 在初始化阶段就保留了该内存空间。那我们第一步要做的就是查看一下 Go 的引导启动流程，看看是在哪里申请的。</p>
<p>引导过程如下：</p>
<figure class="highlight vim"><table><tr><td class="code"><pre><span class="line">graph TD</span><br><span class="line">A(rt0_darwin_amd64.<span class="variable">s:8</span>&lt;<span class="keyword">br</span>/&gt;_rt0_amd64_darwin) --&gt;|JMP| B(asm_amd64.<span class="variable">s:15</span>&lt;<span class="keyword">br</span>/&gt;_rt0_amd64)</span><br><span class="line">B --&gt; |JMP|C(asm_amd64.<span class="variable">s:87</span>&lt;<span class="keyword">br</span>/&gt;<span class="keyword">runtime</span>-rt0_go)</span><br><span class="line">C --&gt; D(runtime1.<span class="keyword">go</span>:<span class="number">60</span>&lt;<span class="keyword">br</span>/&gt;<span class="keyword">runtime</span>-<span class="keyword">args</span>)</span><br><span class="line">D --&gt; E(os_darwin.<span class="keyword">go</span>:<span class="number">50</span>&lt;<span class="keyword">br</span>/&gt;<span class="keyword">runtime</span>-osinit)</span><br><span class="line">E --&gt; F(proc.<span class="keyword">go</span>:<span class="number">472</span>&lt;<span class="keyword">br</span>/&gt;<span class="keyword">runtime</span>-schedinit)</span><br><span class="line">F --&gt; G(proc.<span class="keyword">go</span>:<span class="number">3236</span>&lt;<span class="keyword">br</span>/&gt;<span class="keyword">runtime</span>-newproc)</span><br><span class="line">G --&gt; H(proc.<span class="keyword">go</span>:<span class="number">1170</span>&lt;<span class="keyword">br</span>/&gt;<span class="keyword">runtime</span>-mstart)</span><br><span class="line">H --&gt; I(在新创建的 <span class="keyword">p</span> 和 <span class="keyword">m</span> 上运行 <span class="keyword">runtime</span>-main)</span><br></pre></td></tr></table></figure>
<ul>
<li>runtime-osinit：获取 CPU 核心数。</li>
<li>runtime-schedinit：初始化程序运行环境（包括栈、内存分配器、垃圾回收、P等）。</li>
<li>runtime-newproc：创建一个新的 G 和 绑定 runtime.main。</li>
<li>runtime-mstart：启动线程 M。</li>
</ul>
<p>注：来自@曹大的 《Go 程序的启动流程》和@全成的 《Go 程序是怎样跑起来的》，推荐大家阅读。</p>
<h4 id="初始化运行环境"><a href="#初始化运行环境" class="headerlink" title="初始化运行环境"></a>初始化运行环境</h4><p>显然，我们要研究的是 runtime 里的 <code>schedinit</code> 方法，如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">schedinit</span><span class="params">()</span></span> &#123;</span><br><span class="line"> ...</span><br><span class="line"> stackinit()</span><br><span class="line"> mallocinit()</span><br><span class="line"> mcommoninit(_g_.m)</span><br><span class="line"> cpuinit()       <span class="comment">// must run before alginit</span></span><br><span class="line"> alginit()       <span class="comment">// maps must not be used before this call</span></span><br><span class="line"> modulesinit()   <span class="comment">// provides activeModules</span></span><br><span class="line"> typelinksinit() <span class="comment">// uses maps, activeModules</span></span><br><span class="line"> itabsinit()     <span class="comment">// uses activeModules</span></span><br><span class="line"></span><br><span class="line"> msigsave(_g_.m)</span><br><span class="line"> initSigmask = _g_.m.sigmask</span><br><span class="line"></span><br><span class="line"> goargs()</span><br><span class="line"> goenvs()</span><br><span class="line"> parsedebugvars()</span><br><span class="line"> gcinit()</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从用途来看，非常明显， <code>mallocinit</code> 方法会进行内存分配器的初始化，我们继续往下看。</p>
<h4 id="初始化内存分配器"><a href="#初始化内存分配器" class="headerlink" title="初始化内存分配器"></a>初始化内存分配器</h4><h5 id="mallocinit"><a href="#mallocinit" class="headerlink" title="mallocinit"></a>mallocinit</h5><p>接下来我们正式的分析一下 <code>mallocinit</code> 方法，在引导流程中， <code>mallocinit</code> 主要承担 Go 程序的内存分配器的初始化动作，而今天主要是针对虚拟内存地址这块进行拆解，如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">mallocinit</span><span class="params">()</span></span> &#123;</span><br><span class="line"> ...</span><br><span class="line"> <span class="keyword">if</span> sys.PtrSize == <span class="number">8</span> &#123;</span><br><span class="line">  <span class="keyword">for</span> i := <span class="number">0x7f</span>; i &gt;= <span class="number">0</span>; i-- &#123;</span><br><span class="line">   <span class="keyword">var</span> p <span class="keyword">uintptr</span></span><br><span class="line">   <span class="keyword">switch</span> &#123;</span><br><span class="line">   <span class="keyword">case</span> GOARCH == <span class="string">"arm64"</span> &amp;&amp; GOOS == <span class="string">"darwin"</span>:</span><br><span class="line">    p = <span class="keyword">uintptr</span>(i)&lt;&lt;<span class="number">40</span> | uintptrMask&amp;(<span class="number">0x0013</span>&lt;&lt;<span class="number">28</span>)</span><br><span class="line">   <span class="keyword">case</span> GOARCH == <span class="string">"arm64"</span>:</span><br><span class="line">    p = <span class="keyword">uintptr</span>(i)&lt;&lt;<span class="number">40</span> | uintptrMask&amp;(<span class="number">0x0040</span>&lt;&lt;<span class="number">32</span>)</span><br><span class="line">   <span class="keyword">case</span> GOOS == <span class="string">"aix"</span>:</span><br><span class="line">    <span class="keyword">if</span> i == <span class="number">0</span> &#123;</span><br><span class="line">     <span class="keyword">continue</span></span><br><span class="line">    &#125;</span><br><span class="line">    p = <span class="keyword">uintptr</span>(i)&lt;&lt;<span class="number">40</span> | uintptrMask&amp;(<span class="number">0xa0</span>&lt;&lt;<span class="number">52</span>)</span><br><span class="line">   <span class="keyword">case</span> raceenabled:</span><br><span class="line">    ...</span><br><span class="line">   <span class="keyword">default</span>:</span><br><span class="line">    p = <span class="keyword">uintptr</span>(i)&lt;&lt;<span class="number">40</span> | uintptrMask&amp;(<span class="number">0x00c0</span>&lt;&lt;<span class="number">32</span>)</span><br><span class="line">   &#125;</span><br><span class="line">   hint := (*arenaHint)(mheap_.arenaHintAlloc.alloc())</span><br><span class="line">   hint.addr = p</span><br><span class="line">   hint.next, mheap_.arenaHints = mheap_.arenaHints, hint</span><br><span class="line">  &#125;</span><br><span class="line"> &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      ...</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>判断当前是 64 位还是 32 位的系统。</li>
<li>从 0x7fc000000000~0x1c000000000 开始设置保留地址。</li>
<li>判断当前 <code>GOARCH</code>、<code>GOOS</code> 或是否开启了竞态检查，根据不同的情况申请不同大小的连续内存地址，而这里的 <code>p</code> 是即将要要申请的连续内存地址的开始地址。</li>
<li>保存刚刚计算的 arena 的信息到 <code>arenaHint</code> 中。</li>
</ul>
<p>可能会有小伙伴问，为什么要判断是 32 位还是 64 位的系统，这是因为不同位数的虚拟内存的寻址范围是不同的，因此要进行区分，否则会出现高位的虚拟内存映射问题。而在申请保留空间时，我们会经常提到 <code>arenaHint</code> 结构体，它是 <code>arenaHints</code>链表里的一个节点，结构如下：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">type</span> <span class="title">arenaHint</span></span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line"> addr uintptr</span><br><span class="line"> down <span class="built_in">bool</span></span><br><span class="line"> next *arenaHint</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>addr：<code>arena</code> 的起始地址</li>
<li>down：是否最后一个 <code>arena</code></li>
<li>next：下一个 <code>arenaHint</code> 的指针地址</li>
</ul>
<p>那么这里疯狂提到的 <code>arena</code> 又是什么东西呢，这其实是 Go 的内存管理中的概念，Go Runtime 会把申请的虚拟内存分为三个大块，如下：</p>
<p><img src="4.png" alt="图片"></p>
<ul>
<li>spans：记录 arena 区域页号和 mspan 的映射关系。</li>
<li>bitmap：标识 arena 的使用情况，在功能上来讲，会用于标识 arena 的哪些空间地址已经保存了对象。</li>
<li>arean：arean 其实就是 Go 的堆区，是由 mheap 进行管理的，它的 MaxMem 是 512GB-1。而在功能上来讲，Go 会在初始化的时候申请一段连续的虚拟内存空间地址到 arean 保留下来，在真正需要申请堆上的空间时再从 arean 中取出来处理，这时候就会转变为物理内存了。</li>
</ul>
<p>在这里的话，你需要理解 arean 区域在 Go 内存里的作用就可以了。</p>
<h5 id="mmap"><a href="#mmap" class="headerlink" title="mmap"></a>mmap</h5><p>我们刚刚通过上述的分析，已经知道 <code>mallocinit</code> 的用途了，但是你可能还是会有疑惑，就是我们之前所看到的 <code>mmap</code> 系统调用，和它又有什么关系呢，怎么就关联到一起了，接下来我们先一起来看看更下层的代码，如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">sysAlloc</span><span class="params">(n <span class="keyword">uintptr</span>, sysStat *<span class="keyword">uint64</span>)</span> <span class="title">unsafe</span>.<span class="title">Pointer</span></span> &#123;</span><br><span class="line"> p, err := mmap(<span class="literal">nil</span>, n, _PROT_READ|_PROT_WRITE, _MAP_ANON|_MAP_PRIVATE, <span class="number">-1</span>, <span class="number">0</span>)</span><br><span class="line"> ...</span><br><span class="line"> mSysStatInc(sysStat, n)</span><br><span class="line"> <span class="keyword">return</span> p</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">sysReserve</span><span class="params">(v unsafe.Pointer, n <span class="keyword">uintptr</span>)</span> <span class="title">unsafe</span>.<span class="title">Pointer</span></span> &#123;</span><br><span class="line"> p, err := mmap(v, n, _PROT_NONE, _MAP_ANON|_MAP_PRIVATE, <span class="number">-1</span>, <span class="number">0</span>)</span><br><span class="line"> ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">sysMap</span><span class="params">(v unsafe.Pointer, n <span class="keyword">uintptr</span>, sysStat *<span class="keyword">uint64</span>)</span></span> &#123;</span><br><span class="line"> ...</span><br><span class="line"> munmap(v, n)</span><br><span class="line"> p, err := mmap(v, n, _PROT_READ|_PROT_WRITE, _MAP_ANON|_MAP_FIXED|_MAP_PRIVATE, <span class="number">-1</span>, <span class="number">0</span>)</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在 Go Runtime 中存在着一系列的系统级内存调用方法，本文涉及的主要如下：</p>
<ul>
<li>sysAlloc：从 OS 系统上申请清零后的内存空间，调用参数是 <code>_PROT_READ|_PROT_WRITE, _MAP_ANON|_MAP_PRIVATE</code>，得到的结果需进行内存对齐。</li>
<li>sysReserve：从 OS 系统中保留内存的地址空间，这时候还没有分配物理内存，调用参数是 <code>_PROT_NONE, _MAP_ANON|_MAP_PRIVATE</code>，得到的结果需进行内存对齐。</li>
<li>sysMap：通知 OS 系统我们要使用已经保留了的内存空间，调用参数是 <code>_PROT_READ|_PROT_WRITE, _MAP_ANON|_MAP_FIXED|_MAP_PRIVATE</code>。</li>
</ul>
<p>看上去好像很有道理的样子，但是 <code>mallocinit</code> 方法在初始化时，到底是在哪里涉及了 <code>mmap</code> 方法呢，表面看不出来，如下：</p>
<figure class="highlight roboconf"><table><tr><td class="code"><pre><span class="line">for i := 0x7f; i &gt;= 0; i-- &#123;</span><br><span class="line"> ...</span><br><span class="line"> <span class="attribute">hint</span> := (*arenaHint)(mheap_<span class="variable">.arenaHintAlloc</span><span class="variable">.alloc</span>())</span><br><span class="line"> hint<span class="variable">.addr</span> = p</span><br><span class="line"> hint<span class="variable">.next</span>, mheap_<span class="variable">.arenaHints</span> = mheap_<span class="variable">.arenaHints</span>, hint</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>实际上在调用 <code>mheap_.arenaHintAlloc.alloc()</code> 时，调用的是 <code>mheap</code> 下的 <code>sysAlloc</code> 方法，而 <code>sysAlloc</code> 又会与 <code>mmap</code> 方法产生调用关系，并且这个方法与常规的 <code>sysAlloc</code> 还不大一样，如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> mheap_ mheap</span><br><span class="line">...</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(h *mheap)</span> <span class="title">sysAlloc</span><span class="params">(n <span class="keyword">uintptr</span>)</span> <span class="params">(v unsafe.Pointer, size <span class="keyword">uintptr</span>)</span></span> &#123;</span><br><span class="line"> ...</span><br><span class="line"> <span class="keyword">for</span> h.arenaHints != <span class="literal">nil</span> &#123;</span><br><span class="line">  hint := h.arenaHints</span><br><span class="line">  p := hint.addr</span><br><span class="line">  <span class="keyword">if</span> hint.down &#123;</span><br><span class="line">   p -= n</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> p+n &lt; p &#123;</span><br><span class="line">   v = <span class="literal">nil</span></span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> arenaIndex(p+n<span class="number">-1</span>) &gt;= <span class="number">1</span>&lt;&lt;arenaBits &#123;</span><br><span class="line">   v = <span class="literal">nil</span></span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">   v = sysReserve(unsafe.Pointer(p), n)</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>你可以惊喜的发现 <code>mheap.sysAlloc</code> 里其实有调用 <code>sysReserve</code> 方法，而 <code>sysReserve</code> 方法又正正是从 OS 系统中保留内存的地址空间的特定方法，是不是很惊喜，一切似乎都串起来了。</p>
<h4 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h4><p>在本节中，我们先写了一个测试程序，然后根据非常规的排查思路进行了一步步的跟踪怀疑，整体流程如下：</p>
<ul>
<li>通过 <code>top</code> 或 <code>ps</code> 等命令，查看进程运行情况，分析基础指标。</li>
<li>通过 <code>pprof</code> 或 <code>runtime.MemStats</code> 等工具链查看应用运行情况，分析应用层面是否有泄露或者哪儿高。</li>
<li>通过 <code>vmmap</code> 命令，查看进程的内存映射情况，分析是不是进程虚拟空间内的某个区域比较高，例如：共享库等。</li>
<li>通过 <code>dtruss</code> 命令，查看程序的系统调用情况，分析可能出现的一些特殊行为，例如：在分析中我们发现  <code>mmap</code> 方法调用的比例是比较高的，那我们有充分的理由怀疑 Go 在启动时就进行了大量的内存空间保留。</li>
<li>通过上述的分析，确定可能是在哪个环节申请了那么多的内存空间后，再到 Go Runtime 中去做进一步的源码分析，因为源码面前，了无秘密，没必要靠猜。</li>
</ul>
<p>从结论上而言，VSZ（进程虚拟内存大小）与共享库等没有太大的关系，主要与 Go Runtime 存在直接关联，也就是在前图中表示的运行时堆（malloc）。转换到 Go Runtime 里，就是在 <code>mallocinit</code> 这个内存分配器的初始化阶段里进行了一定量的虚拟空间的保留。</p>
<p>而保留虚拟内存空间时，受什么影响，又是一个哲学问题。从源码上来看，主要如下：</p>
<ul>
<li>受不同的 OS 系统架构（GOARCH/GOOS）和位数（32/64 位）的影响。</li>
<li>受内存对齐的影响，计算回来的内存空间大小是需要经过对齐才会进行保留。</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>我们通过一步步地分析，讲解了 Go 会在哪里，又会受什么因素，去调用了什么方法保留了那么多的虚拟内存空间，但是我们肯定会忧心进程虚拟内存（VSZ）高，会不会存在问题呢，我分析如下：</p>
<ul>
<li>VSZ 并不意味着你真正使用了那些物理内存，因此是不需要担心的。</li>
<li>VSZ 并不会给 GC 带来压力，GC 管理的是进程实际使用的物理内存，而 VSZ 在你实际使用它之前，它并没有过多的代价。</li>
<li>VSZ 基本都是不可访问的内存映射，也就是它并没有内存的访问权限（不允许读、写和执行）。</li>
</ul>
<h2 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h2><p>看到这里舒一口气，因为 Go VSZ 的高，并不会对我们产生什么非常实质性的问题，但是又仔细一想，为什么 Go 要申请那么多的虚拟内存呢？</p>
<p>总体考虑如下：</p>
<ul>
<li>Go 的设计是考虑到 <code>arena</code> 和  <code>bitmap</code> 的后续使用，先提早保留了整个内存地址空间。</li>
<li>Go Runtime 和应用的逐步使用，肯定也会开始实际的申请和使用内存，这时候 <code>arena</code> 和 <code>bitmap</code> 的内存分配器就只需要将事先申请好的内存地址空间保留更改为实际可用的物理内存就好了，这样子可以极大的提高效能。</li>
</ul>
<p><strong>参考</strong></p>
<ul>
<li>High virtual memory allocation by golang</li>
<li>GO MEMORY MANAGEMENT</li>
<li>GoBigVirtualSize</li>
<li>GoProgramMemoryUse</li>
<li>曹大的 Go 程序的启动流程</li>
<li>全成大佬的 Go 程序是怎样跑起来的</li>
<li>欧神的 go-under-the-hood</li>
</ul>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>干货满满的 Go Modules 知识分享</title>
    <url>/go/%E7%9F%A5%E8%AF%86%E7%82%B9/%E5%B9%B2%E8%B4%A7%E6%BB%A1%E6%BB%A1%E7%9A%84%20Go%20Modules%20%E7%9F%A5%E8%AF%86%E5%88%86%E4%BA%AB/</url>
    <content><![CDATA[<p><a href="https://mp.weixin.qq.com/s/uUNTH06_s6yzy5urtjPMsg" rel="external nofollow noopener noreferrer" target="_blank">转载自煎鱼的blog</a></p>
<p>大家好，我是煎鱼。</p>
<p>马上 2021 年了，Go 也即将在明年发布 Go1.16。但 Go Modules 仍然是大家关注的话题之一。早期汇总过傲飞分享的 《Go Modules、Go Module Proxy 和 goproxy.cn》。</p>
<p><img src="1.png" alt="图片"></p>
<p>今天正式在自己公众号分享出来，希望对大家的学习有所帮助。</p>
<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>Go 1.11 推出的模块（Modules）为 Go 语言开发者打开了一扇新的大门，理想化的依赖管理解决方案使得 Go 语言朝着计算机编程史上的第一个依赖乌托邦（Deptopia）迈进。</p>
<p>随着模块一起推出的还有模块代理协议（Module proxy protocol），通过这个协议我们可以实现 Go 模块代理（Go module proxy），也就是依赖镜像。</p>
<p>Go 1.13 的发布为模块带来了大量的改进，所以模块的扶正就是这次 Go 1.13 发布中开发者能直接感觉到的最大变化。</p>
<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul>
<li><p>Go Modules 简介</p>
</li>
<li><p>快速迁移项目至 Go Modules</p>
</li>
<li><p>使用 Go Modules 时常遇见的坑</p>
</li>
<li><ul>
<li>坑 1:判断项目是否启用了 Go Modules</li>
<li>坑 2:管理 Go 的环境变量</li>
<li>坑 3:从 dep、glide 等迁移至 Go Modules</li>
<li>坑 4:拉取私有模块</li>
<li>坑 5:更新现有的模块</li>
<li>坑 6:主版本号</li>
</ul>
</li>
<li><p>Go Module Proxy 简介</p>
</li>
<li><p>Goproxy 中国(goproxy.cn)</p>
</li>
</ul>
<h2 id="Go-Modules-简介"><a href="#Go-Modules-简介" class="headerlink" title="Go Modules 简介"></a>Go Modules 简介</h2><p><img src="2.jpg" alt="图片"></p>
<p>Go modules (前身 vgo) 是 Go team (Russ Cox) <strong>强推</strong>的一个<strong>理想化</strong>的<strong>类语言级</strong>依赖管理解决方案，它是和 Go1.11 一同发布的，在 Go1.13 做了大量的优化和调整，目前已经变得比较不错，如果你想用 Go modules，但还停留在 1.11/1.12 版本的话，强烈建议升级。</p>
<h3 id="三个关键字"><a href="#三个关键字" class="headerlink" title="三个关键字"></a>三个关键字</h3><h4 id="强推"><a href="#强推" class="headerlink" title="强推"></a>强推</h4><p>首先这并不是乱说的，因为 Go modules 确实是被强推出来的，如下：</p>
<ul>
<li>之前：大家都知道在 Go modules 之前还有一个叫 dep 的项目，它也是 Go 的一个官方的实验性项目，目的同样也是为了解决 Go 在依赖管理方面的短板。在 Russ Cox 还没有提出 Go modules 的时候，社区里面几乎所有的人都认为 dep 肯定就是未来 Go 官方的依赖管理解决方案了。</li>
<li>后来：谁都没想到半路杀出个程咬金，Russ Cox 义无反顾地推出了 Go modules，这瞬间导致一石激起千层浪，让社区炸了锅。大家一致认为 Go team 实在是太霸道、太独裁了，连个招呼都不打一声。我记得当时有很多人在网上跟 Russ Cox 口水战，各种依赖管理解决方案的专家都冒出来发表意见，讨论范围甚至一度超出了 Go 语言的圈子触及到了其他语言的领域。</li>
</ul>
<h4 id="理想化"><a href="#理想化" class="headerlink" title="理想化"></a>理想化</h4><p>从他强制要求使用语义化版本控制这一点来说就很理想化了，如下：</p>
<ul>
<li>Go modules 狠到如果你的 Tag 没有遵循语义化版本控制那么它就会忽略你的 Tag，然后根据你的 Commit 时间和哈希值再为你生成一个假定的符合语义化版本控制的版本号。</li>
<li>Go modules 还默认认为，只要你的主版本号不变，那这个模块版本肯定就不包含 Breaking changes，因为语义化版本控制就是这么规定的啊。是不是很理想化。</li>
</ul>
<h4 id="类语言级："><a href="#类语言级：" class="headerlink" title="类语言级："></a>类语言级：</h4><p>这个关键词其实是我自己瞎编的，我只是单纯地个人认为 Go modules 在设计上就像个语言级特性一样，比如如果你的主版本号发生变更，那么你的代码里的 import path 也得跟着变，它认为主版本号不同的两个模块版本是完全不同的两个模块。此外，Go moduels 在设计上跟 go 整个命令都结合得相当紧密，无处不在，所以我才说它是一个有点儿像语言级的特性，虽然不是太严谨。</p>
<h3 id="推-Go-Modules-的人是谁"><a href="#推-Go-Modules-的人是谁" class="headerlink" title="推 Go Modules 的人是谁"></a>推 Go Modules 的人是谁</h3><p>那么在上文中提到的 Russ Cox 何许人也呢，很多人应该都知道他，他是 Go 这个项目目前代码提交量最多的人，甚至是第二名的两倍还要多。</p>
<p>Russ Cox 还是 Go 现在的掌舵人（大家应该知道之前 Go 的掌舵人是 Rob Pike，但是听说由于他本人不喜欢特朗普执政所以离开了美国，然后他岁数也挺大的了，所以也正在逐渐交权，不过现在还是在参与 Go 的发展）。</p>
<p>Russ Cox 的个人能力相当强，看问题的角度也很独特，这也就是为什么他刚一提出 Go modules 的概念就能引起那么大范围的响应。虽然是被强推的，但事实也证明当下的 Go modules 表现得确实很优秀，所以这表明一定程度上的 “独裁” 还是可以接受的，至少可以保证一个项目能更加专一地朝着一个方向发展。</p>
<p>总之，无论如何 Go modules 现在都成了 Go 语言的一个密不可分的组件。</p>
<h3 id="GOPATH"><a href="#GOPATH" class="headerlink" title="GOPATH"></a>GOPATH</h3><p>Go modules 出现的目的之一就是为了解决 GOPATH 的问题，也就相当于是抛弃 GOPATH 了。</p>
<h3 id="Opt-in"><a href="#Opt-in" class="headerlink" title="Opt-in"></a>Opt-in</h3><p>Go modules 还处于 Opt-in 阶段，就是你想用就用，不用就不用，不强制你。但是未来很有可能 Go2 就强制使用了。</p>
<h3 id="“module”-“package”"><a href="#“module”-“package”" class="headerlink" title="“module” != “package”"></a>“module” != “package”</h3><p>有一点需要纠正，就是“模块”和“包”，也就是 “module” 和 “package” 这两个术语并不是等价的，是 “集合” 跟 “元素” 的关系，“模块” 包含 “包”，“包” 属于 “模块”，一个 “模块” 是零个、一个或多个 “包” 的集合。</p>
<h2 id="Go-Modules-相关属性"><a href="#Go-Modules-相关属性" class="headerlink" title="Go Modules 相关属性"></a>Go Modules 相关属性</h2><p><img src="3.jpg" alt="图片"></p>
<h3 id="go-mod"><a href="#go-mod" class="headerlink" title="go.mod"></a>go.mod</h3><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">module example.com/foobar</span><br><span class="line"></span><br><span class="line">go <span class="number">1.13</span></span><br><span class="line"></span><br><span class="line">require (</span><br><span class="line">    example.com/apple v0<span class="number">.1</span><span class="number">.2</span></span><br><span class="line">    example.com/banana v1<span class="number">.2</span><span class="number">.3</span></span><br><span class="line">    example.com/banana/v2 v2<span class="number">.3</span><span class="number">.4</span></span><br><span class="line">    example.com/pineapple v0<span class="number">.0</span><span class="number">.0</span><span class="number">-20190924185754</span><span class="number">-1</span>b0db40df49a</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">exclude example.com/banana v1<span class="number">.2</span><span class="number">.4</span></span><br><span class="line">replace example.com/apple v0<span class="number">.1</span><span class="number">.2</span> =&gt; example.com/rda v0<span class="number">.1</span><span class="number">.0</span> </span><br><span class="line">replace example.com/banana =&gt; example.com/hugebanana</span><br></pre></td></tr></table></figure>
<p>go.mod 是启用了 Go moduels 的项目所必须的最重要的文件，它描述了当前项目（也就是当前模块）的元信息，每一行都以一个动词开头，目前有以下 5 个动词:</p>
<ul>
<li>module：用于定义当前项目的模块路径。</li>
<li>go：用于设置预期的 Go 版本。</li>
<li>require：用于设置一个特定的模块版本。</li>
<li>exclude：用于从使用中排除一个特定的模块版本。</li>
<li>replace：用于将一个模块版本替换为另外一个模块版本。</li>
</ul>
<p>这里的填写格式基本为包引用路径+版本号，另外比较特殊的是 <code>go $version</code>，目前从 Go1.13 的代码里来看，还只是个标识作用，暂时未知未来是否有更大的作用。</p>
<h3 id="go-sum"><a href="#go-sum" class="headerlink" title="go.sum"></a>go.sum</h3><p>go.sum 是类似于比如 dep 的 Gopkg.lock 的一类文件，它详细罗列了当前项目直接或间接依赖的所有模块版本，并写明了那些模块版本的 SHA-256 哈希值以备 Go 在今后的操作中保证项目所依赖的那些模块版本不会被篡改。</p>
<figure class="highlight maxima"><table><tr><td class="code"><pre><span class="line"><span class="built_in">example</span>.com/apple v0<span class="number">.1</span><span class="number">.2</span> h1:WXkYYl6Yr3qBf1K79EBnL4mak0OimBfB0XUf9Vl28OQ= </span><br><span class="line"><span class="built_in">example</span>.com/apple v0<span class="number">.1</span><span class="number">.2</span>/<span class="built_in">go</span>.<span class="built_in">mod</span> h1:xHWCNGjB5oqiDr8zfno3MHue2Ht5sIBksp03qcyfWMU= <span class="built_in">example</span>.com/banana v1<span class="number">.2</span><span class="number">.3</span> h1:qHgHjyoNFV7jgucU8QZUuU4gcdhfs8QW1kw68OD2Lag= </span><br><span class="line"><span class="built_in">example</span>.com/banana v1<span class="number">.2</span><span class="number">.3</span>/<span class="built_in">go</span>.<span class="built_in">mod</span> h1:HSdplMjZKSmBqAxg5vPj2TmRDmfkzw+cTzAElWljhcU= <span class="built_in">example</span>.com/banana/v2 v2<span class="number">.3</span><span class="number">.4</span> h1:zl/OfRA6nftbBK9qTohYBJ5xvw6C/oNKizR7cZGl3cI= <span class="built_in">example</span>.com/banana/v2 v2<span class="number">.3</span><span class="number">.4</span>/<span class="built_in">go</span>.<span class="built_in">mod</span> h1:eZbhyaAYD41SGSSsnmcpxVoRiQ/MPUTjUdIIOT9Um7Q= </span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<p>我们可以看到一个模块路径可能有如下两种：</p>
<figure class="highlight maxima"><table><tr><td class="code"><pre><span class="line"><span class="built_in">example</span>.com/apple v0<span class="number">.1</span><span class="number">.2</span> h1:WXkYYl6Yr3qBf1K79EBnL4mak0OimBfB0XUf9Vl28OQ= </span><br><span class="line"><span class="built_in">example</span>.com/apple v0<span class="number">.1</span><span class="number">.2</span>/<span class="built_in">go</span>.<span class="built_in">mod</span> h1:xHWCNGjB5oqiDr8zfno3MHue2Ht5sIBksp03qcyfWMU=</span><br></pre></td></tr></table></figure>
<p>前者为 Go modules 打包整个模块包文件 zip 后再进行 hash 值，而后者为针对 go.mod 的 hash 值。他们两者，要不就是同时存在，要不就是只存在 go.mod hash。</p>
<p>那什么情况下会不存在 zip hash 呢，就是当 Go 认为肯定用不到某个模块版本的时候就会省略它的 zip hash，就会出现不存在 zip hash，只存在 go.mod hash 的情况。</p>
<h3 id="GO111MODULE"><a href="#GO111MODULE" class="headerlink" title="GO111MODULE"></a>GO111MODULE</h3><p>这个环境变量主要是 Go modules 的开关，主要有以下参数：</p>
<ul>
<li>auto：只在项目包含了 go.mod 文件时启用 Go modules，在 Go 1.13 中仍然是默认值，详见 ：golang.org/issue/31857。</li>
<li>on：无脑启用 Go modules，推荐设置，未来版本中的默认值，让 GOPATH 从此成为历史。</li>
<li>off：禁用 Go modules。</li>
</ul>
<h3 id="GOPROXY"><a href="#GOPROXY" class="headerlink" title="GOPROXY"></a>GOPROXY</h3><p>这个环境变量主要是用于设置 Go 模块代理，主要如下：</p>
<ul>
<li><p>它的值是一个以英文逗号 “,” 分割的 Go module proxy 列表（稍后讲解）</p>
</li>
<li><ul>
<li>作用：用于使 Go 在后续拉取模块版本时能够脱离传统的 VCS 方式从镜像站点快速拉取。它拥有一个默认：<code>https://proxy.golang.org,direct</code>，但很可惜 <code>proxy.golang.org</code> 在中国无法访问，故而建议使用 <code>goproxy.cn</code> 作为替代，可以执行语句：<code>go env -w GOPROXY=https://goproxy.cn,direct</code>。</li>
<li>设置为 “off” ：禁止 Go 在后续操作中使用任 何 Go module proxy。</li>
</ul>
</li>
</ul>
<p>刚刚在上面，我们可以发现值列表中有 “direct” ，它又有什么作用呢。其实值列表中的 “direct” 为特殊指示符，用于指示 Go 回源到模块版本的源地址去抓取(比如 GitHub 等)，当值列表中上一个 Go module proxy 返回 404 或 410 错误时，Go 自动尝试列表中的下一个，遇见 “direct” 时回源，遇见 EOF 时终止并抛出类似 “invalid version: unknown revision…” 的错误。</p>
<h3 id="GOSUMDB"><a href="#GOSUMDB" class="headerlink" title="GOSUMDB"></a>GOSUMDB</h3><p>它的值是一个 Go checksum database，用于使 Go 在拉取模块版本时(无论是从源站拉取还是通过 Go module proxy 拉取)保证拉取到的模块版本数据未经篡改，也可以是“off”即禁止 Go 在后续操作中校验模块版本</p>
<ul>
<li>格式 1：<code>&lt;SUMDB_NAME&gt;+&lt;PUBLIC_KEY&gt;</code>。</li>
<li>格式 2：<code>&lt;SUMDB_NAME&gt;+&lt;PUBLIC_KEY&gt; &lt;SUMDB_URL&gt;</code>。</li>
<li>拥有默认值：<code>sum.golang.org</code> (之所以没有按照上面的格式是因为 Go 对默认值做了特殊处理)。</li>
<li>可被 Go module proxy 代理 (详见：Proxying a Checksum Database)。</li>
<li><code>sum.golang.org</code> 在中国无法访问，故而更加建议将 GOPROXY 设置为 <code>goproxy.cn</code>，因为 <code>goproxy.cn</code>支持代理 <code>sum.golang.org</code>。</li>
</ul>
<h3 id="Go-Checksum-Database"><a href="#Go-Checksum-Database" class="headerlink" title="Go Checksum Database"></a>Go Checksum Database</h3><p>Go checksum database 主要用于保护 Go 不会从任何源头拉到被篡改过的非法 Go 模块版本，其作用（左）和工作机制（右）如下图：</p>
<p><img src="4.jpg" alt="图片"></p>
<p>如果有兴趣的小伙伴可以看看 Proposal: Secure the Public Go Module Ecosystem，有详细介绍其算法机制，如果想简单一点，查看 <code>go help module-auth</code> 也是一个不错的选择。</p>
<h3 id="GONOPROXY-GONOSUMDB-GOPRIVATE"><a href="#GONOPROXY-GONOSUMDB-GOPRIVATE" class="headerlink" title="GONOPROXY/GONOSUMDB/GOPRIVATE"></a>GONOPROXY/GONOSUMDB/GOPRIVATE</h3><p>这三个环境变量都是用在当前项目依赖了私有模块，也就是依赖了由 GOPROXY 指定的 Go module proxy 或由 GOSUMDB 指定 Go checksum database 无法访问到的模块时的场景</p>
<ul>
<li>它们三个的值都是一个以英文逗号 “,” 分割的模块路径前缀，匹配规则同 path.Match。</li>
<li>其中 GOPRIVATE 较为特殊，它的值将作为 GONOPROXY 和 GONOSUMDB 的默认值，所以建议的最佳姿势是只是用 GOPRIVATE。</li>
</ul>
<p>在使用上来讲，比如 <code>GOPRIVATE=*.corp.example.com</code> 表示所有模块路径以 <code>corp.example.com</code> 的下一级域名 (如 <code>team1.corp.example.com</code>) 为前缀的模块版本都将不经过 Go module proxy 和 Go checksum database，需要注意的是不包括 <code>corp.example.com</code> 本身。</p>
<h3 id="Global-Caching"><a href="#Global-Caching" class="headerlink" title="Global Caching"></a>Global Caching</h3><p>这个主要是针对 Go modules 的全局缓存数据说明，如下：</p>
<ul>
<li>同一个模块版本的数据只缓存一份，所有其他模块共享使用。</li>
<li>目前所有模块版本数据均缓存在 <code>$GOPATH/pkg/mod</code>和 <code>$GOPATH/pkg/sum</code> 下，未来或将移至 <code>$GOCACHE/mod</code>和<code>$GOCACHE/sum</code> 下( 可能会在当 <code>$GOPATH</code> 被淘汰后)。</li>
<li>可以使用 <code>go clean -modcache</code> 清理所有已缓存的模块版本数据。</li>
</ul>
<p>另外在 Go1.11 之后 GOCACHE 已经不允许设置为 off 了，我想着这也是为了模块数据缓存移动位置做准备，因此大家应该尽快做好适配。</p>
<h2 id="快速迁移项目至-Go-Modules"><a href="#快速迁移项目至-Go-Modules" class="headerlink" title="快速迁移项目至 Go Modules"></a>快速迁移项目至 Go Modules</h2><ul>
<li><p>第一步: 升级到 Go 1.13。</p>
</li>
<li><p>第二步: 让 GOPATH 从你的脑海中完全消失，早一步踏入未来。</p>
</li>
<li><ul>
<li>修改 GOBIN 路径（可选）：<code>go env -w GOBIN=$HOME/bin</code>。</li>
<li>打开 Go modules：<code>go env -w GO111MODULE=on</code>。</li>
<li>设置 GOPROXY：<code>go env -w GOPROXY=https://goproxy.cn,direct</code> # 在中国是必须的，因为它的默认值被墙了。</li>
</ul>
</li>
<li><p>第三步(可选): 按照你喜欢的目录结构重新组织你的所有项目。</p>
</li>
<li><p>第四步: 在你项目的根目录下执行 <code>go mod init &lt;OPTIONAL_MODULE_PATH&gt;</code> 以生成 go.mod 文件。</p>
</li>
<li><p>第五步: 想办法说服你身边所有的人都去走一下前四步。</p>
</li>
</ul>
<h2 id="迁移后-go-get-行为的改变"><a href="#迁移后-go-get-行为的改变" class="headerlink" title="迁移后 go get 行为的改变"></a>迁移后 go get 行为的改变</h2><ul>
<li><p>用 <code>go help module-get</code> 和 <code>go help gopath-get</code>分别去了解 Go modules 启用和未启用两种状态下的 go get 的行为</p>
</li>
<li><p>用 <code>go get</code> 拉取新的依赖</p>
</li>
<li><ul>
<li>拉取最新的版本(优先择取 tag)：<code>go get golang.org/x/text@latest</code></li>
<li>拉取 <code>master</code> 分支的最新 commit：<code>go get golang.org/x/text@master</code></li>
<li>拉取 tag 为 v0.3.2 的 commit：<code>go get golang.org/x/text@v0.3.2</code></li>
<li>拉取 hash 为 342b231 的 commit，最终会被转换为 v0.3.2：<code>go get golang.org/x/text@342b2e</code></li>
<li>用 <code>go get -u</code> 更新现有的依赖</li>
<li>用 <code>go mod download</code> 下载 go.mod 文件中指明的所有依赖</li>
<li>用 <code>go mod tidy</code> 整理现有的依赖</li>
<li>用 <code>go mod graph</code> 查看现有的依赖结构</li>
<li>用 <code>go mod init</code> 生成 go.mod 文件 (Go 1.13 中唯一一个可以生成 go.mod 文件的子命令)</li>
</ul>
</li>
<li><p>用 <code>go mod edit</code> 编辑 go.mod 文件</p>
</li>
<li><p>用 <code>go mod vendor</code> 导出现有的所有依赖 (事实上 Go modules 正在淡化 Vendor 的概念)</p>
</li>
<li><p>用 <code>go mod verify</code> 校验一个模块是否被篡改过</p>
</li>
</ul>
<p>这里我们注意到有两点比较特别，分别是：</p>
<ul>
<li>第一点：为什么 “拉取 hash 为 342b231 的 commit，最终会被转换为 v0.3.2” 呢。这是因为虽然我们设置了拉取 @342b2e commit，但是因为 Go modules 会与 tag 进行对比，若发现对应的 commit 与 tag 有关联，则进行转换。</li>
<li>第二点：为什么不建议使用 <code>go mod vendor</code>，因为 Go modules 正在淡化 Vendor 的概念，很有可能 Go2 就去掉了。</li>
</ul>
<h2 id="使用-Go-Modules-时常遇见的坑"><a href="#使用-Go-Modules-时常遇见的坑" class="headerlink" title="使用 Go Modules 时常遇见的坑"></a>使用 Go Modules 时常遇见的坑</h2><h3 id="坑-1-判断项目是否启用了-Go-Modules"><a href="#坑-1-判断项目是否启用了-Go-Modules" class="headerlink" title="坑 1: 判断项目是否启用了 Go Modules"></a>坑 1: 判断项目是否启用了 Go Modules</h3><p><img src="5.jpg" alt="图片"></p>
<h3 id="坑-2-管理-Go-的环境变量"><a href="#坑-2-管理-Go-的环境变量" class="headerlink" title="坑 2: 管理 Go 的环境变量"></a>坑 2: 管理 Go 的环境变量</h3><p><img src="6.jpg" alt="图片"></p>
<p>这里主要是提到 Go1.13 新增了 <code>go env -w</code> 用于写入环境变量，而写入的地方是 <code>os.UserConfigDir</code> 所返回的路径，需要注意的是 <code>go env -w</code> 不会覆写。</p>
<h3 id="坑-3-从-dep、glide-等迁移至-Go-Modules"><a href="#坑-3-从-dep、glide-等迁移至-Go-Modules" class="headerlink" title="坑 3: 从 dep、glide 等迁移至 Go Modules"></a>坑 3: 从 dep、glide 等迁移至 Go Modules</h3><p><img src="7.jpg" alt="图片"></p>
<p>这里主要是指从旧有的依赖包管理工具（dep/glide 等）进行迁移时，因为 BUG 的原因会导致不经过 GOPROXY 的代理，解决方法有如下两个：</p>
<ul>
<li>手动创建一个 go.mod 文件，再执行 go mod tidy 进行补充。</li>
<li>上代理，相当于不使用 GOPROXY 了。</li>
</ul>
<h3 id="坑-4-拉取私有模块"><a href="#坑-4-拉取私有模块" class="headerlink" title="坑 4:拉取私有模块"></a>坑 4:拉取私有模块</h3><p><img src="8.jpg" alt="图片"></p>
<p>这里主要想涉及两块知识点，如下：</p>
<ul>
<li>GOPROXY 是无权访问到任何人的私有模块的，所以你放心，安全性没问题。</li>
<li>GOPROXY 除了设置模块代理的地址以外，还需要增加 “direct” 特殊标识才可以成功拉取私有库。</li>
</ul>
<h3 id="坑-5-更新现有的模块"><a href="#坑-5-更新现有的模块" class="headerlink" title="坑 5:更新现有的模块"></a>坑 5:更新现有的模块</h3><p><img src="9.jpg" alt="图片"></p>
<h3 id="坑-6-主版本号"><a href="#坑-6-主版本号" class="headerlink" title="坑 6:主版本号"></a>坑 6:主版本号</h3><p><img src="10.jpg" alt="图片"></p>
<h2 id="Go-Module-Proxy-简介"><a href="#Go-Module-Proxy-简介" class="headerlink" title="Go Module Proxy 简介"></a>Go Module Proxy 简介</h2><p><img src="11.jpg" alt="图片"></p>
<p>在这里再次强调了 Go Module Proxy 的作用（图左），以及其对应的协议交互流程（图右），有兴趣的小伙伴可以认真看一下。</p>
<h2 id="Q-amp-A"><a href="#Q-amp-A" class="headerlink" title="Q&amp;A"></a>Q&amp;A</h2><p><strong>Q：如何解决 Go 1.13 在从 GitLab 拉取模块版本时遇到的，Go 错误地按照非期望值的路径寻找目标模块版本结果致使最终目标模块拉取失败的问题？</strong></p>
<p><strong>A：</strong>GitLab 中配合 goget 而设置的 <code>&lt;meta&gt;</code> 存在些许问题，导致 Go 1.13 错误地识别了模块的具体路径，这是个 Bug，据说在 GitLab 的新版本中已经被修复了，详细内容可以看 <a href="https://github.com/golang/go/issues/34094" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/golang/go/issues/34094</a> 这个 Issue。然后目前的解决办法的话除了升级 GitLab 的版本外，还可以参考 <a href="https://github.com/developer-learning/night-reading-go/issues/468#issuecomment-535850154" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/developer-learning/night-reading-go/issues/468#issuecomment-535850154</a> 这条回复。</p>
<p><strong>Q：使用 Go modules 时可以同时依赖同一个模块的不同的两个或者多个小版本（修订版本号不同）吗？</strong></p>
<p><strong>A：</strong>不可以的，Go modules 只可以同时依赖一个模块的不同的两个或者多个大版本（主版本号不同）。比如可以同时依赖 <a href="mailto:example.com/foobar@v1.2.3" rel="external nofollow noopener noreferrer" target="_blank">example.com/foobar@v1.2.3</a> 和 <a href="mailto:example.com/foobar/v2@v2.3.4" rel="external nofollow noopener noreferrer" target="_blank">example.com/foobar/v2@v2.3.4</a>，因为他们的模块路径（module path）不同，Go modules 规定主版本号不是 v0 或者 v1 时，那么主版本号必须显式地出现在模块路径的尾部。但是，同时依赖两个或者多个小版本是不支持的。比如如果模块 A 同时直接依赖了模块 B 和模块 C，且模块 A 直接依赖的是模块 C 的 v1.0.0 版本，然后模块 B 直接依赖的是模块 C 的 v1.0.1 版本，那么最终 Go modules 会为模块 A 选用模块 C 的 v1.0.1 版本而不是模块 A 的 go.mod 文件中指明的 v1.0.0 版本。</p>
<p>这是因为 Go modules 认为只要主版本号不变，那么剩下的都可以直接升级采用最新的。但是如果采用了最新的结果导致项目 Break 掉了，那么 Go modules 就会 Fallback 到上一个老的版本，比如在前面的例子中就会 Fallback 到 v1.0.0 版本。</p>
<p><strong>Q：在 go.sum 文件中的一个模块版本的 Hash 校验数据什么情况下会成对出现，什么情况下只会存在一行？</strong></p>
<p><strong>A：</strong>通常情况下，在 go.sum 文件中的一个模块版本的 Hash 校验数据会有两行，前一行是该模块的 ZIP 文件的 Hash 校验数据，后一行是该模块的 go.mod 文件的 Hash 校验数据。但是也有些情况下只会出现一行该模块的 go.mod 文件的 Hash 校验数据，而不包含该模块的 ZIP 文件本身的 Hash 校验数据，这个情况发生在 Go modules 判定为你当前这个项目完全用不到该模块，根本也不会下载该模块的 ZIP 文件，所以就没必要对其作出 Hash 校验保证，只需要对该模块的 go.mod 文件作出 Hash 校验保证即可，因为 go.mod 文件是用得着的，在深入挖取项目依赖的时候要用。</p>
<p><strong>Q：能不能更详细地讲解一下 go.mod 文件中的 replace 动词的行为以及用法？</strong></p>
<p><strong>A：</strong>这个 replace 动词的作用是把一个“模块版本”替换为另外一个“模块版本”，这是“模块版本”和“模块版本（module path）”之间的替换，“=&gt;”标识符前面的内容是待替换的“模块版本”的“模块路径”，后面的内容是要替换的目标“模块版本”的所在地，即路径，这个路径可以是一个本地磁盘的相对路径，也可以是一个本地磁盘的绝对路径，还可以是一个网络路径，但是这个目标路径并不会在今后你的项目代码中作为你“导入路径（import path）”出现，代码里的“导入路径”还是得以你替换成的这个目标“模块版本”的“模块路径”作为前缀。</p>
<p>另外需要注意，Go modules 是不支持在 “导入路径” 里写相对路径的。举个例子，如果项目 A 依赖了模块 B，比如模块 B 的“模块路径”是 example.com/b，然后它在的磁盘路径是 ~/b，在项目 A 里的 go.mod 文件中你有一行 replace example.com/b=&gt;~/b，然后在项目 A 里的代码中的“导入路基”就是 import”example.com/b”，而不是 import”~/b”，剩下的工作是 Go modules 帮你自动完成了的。</p>
<p>然后就是我在分享中也提到了， exclude 和 replace 这两个动词只作用于当前主模块，也就是当前项目，它所依赖的那些其他模块版本中如果出现了你待替换的那个模块版本的话，Go modules 还是会为你依赖的那个模块版本去拉取你的这个待替换的模块版本。</p>
<p>举个例子，比如项目 A 直接依赖了模块 B 和模块 C，然后模块 B 也直接依赖了模块 C，那么你在项目 A 中的 go.mod 文件里的 replace c=&gt;~/some/path/c 是只会影响项目 A 里写的代码中，而模块 B 所用到的还是你 replace 之前的那个 c，并不是你替换成的 ~/some/path/c 这个。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在 Go1.13 发布后，接触 Go modules 和 Go module proxy 的人越来越多，经常在各种群看到各种小伙伴在咨询，包括我自己也贡献了好几枚 “坑”。</p>
<p>因此我觉得傲飞的这一次 《Go Modules、Go Module Proxy 和 goproxy.cn》的技术分享，非常的有实践意义。</p>
<p>如果后续大家还有什么建议或问题，欢迎随时来讨论。</p>
<h2 id="进一步阅读"><a href="#进一步阅读" class="headerlink" title="进一步阅读"></a>进一步阅读</h2><ul>
<li>night-reading-go/issues/468</li>
<li>B站：【Go 夜读】第 61 期 Go Modules、Go Module Proxy 和 goproxy.cn</li>
<li>youtube：【Go 夜读】第 61 期 Go Modules、Go Module Proxy 和 goproxy.cn</li>
</ul>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>史上最完整的《指环王》上古时期大事记（编年体长文）</title>
    <url>/%E5%85%B4%E8%B6%A3%E7%88%B1%E5%A5%BD/%E5%8F%B2%E4%B8%8A%E6%9C%80%E5%AE%8C%E6%95%B4%E7%9A%84%E3%80%8A%E6%8C%87%E7%8E%AF%E7%8E%8B%E3%80%8B%E4%B8%8A%E5%8F%A4%E6%97%B6%E6%9C%9F%E5%A4%A7%E4%BA%8B%E8%AE%B0/</url>
    <content><![CDATA[<p> <a href="https://www.toutiao.com/i6870476899894690315/?group_id=6870476899894690315" rel="external nofollow noopener noreferrer" target="_blank">转载自</a></p>
<h1 id="史上最完整的《指环王》上古时期大事记（编年体长文）"><a href="#史上最完整的《指环王》上古时期大事记（编年体长文）" class="headerlink" title="史上最完整的《指环王》上古时期大事记（编年体长文）"></a>史上最完整的《指环王》上古时期大事记（编年体长文）</h1><p>原创跃马酒馆小招待2020-09-10 11:48:07</p>
<p>许多朋友反应对于整个<strong>《指环王》</strong>的历史脉络不太熟悉，导致对我的文章看得云里雾里，那么今天我就尝试梳理一下从<strong>伊露维塔</strong>创世纪开始，到<strong>《精灵宝钻》</strong>故事的完整时间线，方便那些对<strong>《指环王》历史</strong>感兴趣的小伙伴查阅。</p>
<p>其实这个工作早就想做了，但是<strong>指环王</strong>的故事结构太庞大，除了几本正传著作之外，<strong>托尔金</strong>老爷子还有大量的书信内容都对指环王世界观的设定做过不少补充，所以要理顺这些细节是一个巨大的工作量，有哪些遗漏的地方欢迎大家指正。</p>
<h1 id="纪年方式"><a href="#纪年方式" class="headerlink" title="纪年方式"></a>纪年方式</h1><p>整个阿尔达世界有多个种族，其纪年方式也五花八门，本文就选取最重要和最具有普遍性的两种纪年方式，<strong>努门诺尔人</strong>的<strong>太阳年</strong>和在<strong>维林诺</strong>使用的<strong>维拉年</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/7dc137e1c8c64b06ab007a228726dea6?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>一亚</p>
<p><strong>太阳年</strong>和地球年一样为365天，没有闰年，同样分12个月，不过在在<strong>双圣树</strong>枯萎之前，<strong>阿尔达</strong>世界是没有太阳和月亮的，所以<strong>太阳年</strong>在上古时期不存在，需要用到<strong>维拉年</strong>，<strong>维拉年</strong>由一千个<strong>维拉日</strong>组成，一个维拉日就是<strong>双圣树</strong>的花朵从绽放到闭合一次的完整过程（相当地规律）。</p>
<p>至于<strong>维拉年</strong>和<strong>太阳年</strong>的换算，基本上一个维拉年约等于十个太阳年（当中有微小的偏差，不过这样记更方便一些）。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/05e5a6c6e6964affb1a42390e51f0ca1?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>维拉</p>
<h1 id="创世纪大乐章"><a href="#创世纪大乐章" class="headerlink" title="创世纪大乐章"></a>创世纪大乐章</h1><p>独一主神<strong>伊露维塔</strong>（又称<strong>“**</strong>一如<strong>**”</strong>）自祂的意念中创造了众<strong>爱努</strong>，然后祂带领这些爱努进行大合唱。在众神的歌声中，整个宇宙被创造出来，这个宇宙被称为<strong>“一亚”</strong>。至于为什么创世的过程要用唱的，<strong>托尔金</strong>就是什么写的，个人猜测可能是一种具象的表达方式，用以替代那些无法用言语表达的伟大场景。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/587401daab39476797efd62bab0264f9?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>欢乐合唱团</p>
<p>在这个欢乐合唱团当中，有两个最卖力的同学，祂们是<strong>曼威</strong>和<strong>米尔寇</strong>。<strong>米尔寇</strong>特别想要在<strong>一如</strong>面前表现自己，突显自己与众不同的歌唱技巧。</p>
<p>但是大家都知道，唱歌切忌用力过猛，<strong>米尔寇</strong>的音调唱歪了，这些不和谐的音调成了宇宙中的黑暗力量，引起了<strong>一如</strong>的不满，但是已经开始的乐章无法重来，独一真神狠狠批评了<strong>米尔寇</strong>，让祂从此记恨在心。</p>
<h1 id="阿尔达的建立"><a href="#阿尔达的建立" class="headerlink" title="阿尔达的建立"></a>阿尔达的建立</h1><p>随着乐章的深入，<strong>一如</strong>向众神介绍了自己创造的两个全新种族——<strong>精灵</strong>和<strong>人类</strong>，并且已经在宇宙中为他们挑选了一处居住之地，那里被称为<strong>“阿尔达”</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/24d20aadeb284da6b82efb391b35145e?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>阿尔达</p>
<p><strong>爱努</strong>们都没有见过这两个新的种族，感到十分好奇，其中以<strong>米尔寇</strong>尤甚。一些<strong>爱努</strong>向<strong>一如</strong>请求让祂们留在<strong>阿尔达</strong>，帮助精灵和人类开拓那片原始的土地，<strong>一如</strong>同意了。</p>
<p><strong>一如</strong>不会满足于仅仅创造<strong>阿尔达</strong>，祂将要离开，而选择留在这里的<strong>爱努</strong>们，其中的14名最强者被称为<strong>维拉</strong>，其余的次级爱努都从属于祂们，被称为<strong>迈雅</strong>。</p>
<h1 id="巨灯纪-1-1500维拉年"><a href="#巨灯纪-1-1500维拉年" class="headerlink" title="巨灯纪 1-1500维拉年"></a>巨灯纪 1-1500维拉年</h1><p><strong>米尔寇</strong>没有一如一起离开，留在了阿尔达，作为能力比<strong>曼威</strong>还强一些的<strong>爱努</strong>，祂十分不满其他<strong>爱努</strong>将<strong>曼威</strong>推举为<strong>维拉</strong>之首。就在众神们忙着改造原始<strong>阿尔达</strong>的时候，<strong>米尔寇</strong>在这段时间里一直在塑造自己的形体，并且终于完成了自己强大的身躯开始在<strong>阿尔达</strong>大肆破坏。众神匆忙与<strong>米尔寇</strong>开战，但没有一个是祂的对手。</p>
<p><img src="https://p3-tt.byteimg.com/origin/dfic-imagehandler/6e0fb2b7-e94f-41d7-a10c-66683ecbada5?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<h1 id="巨灯纪-1600维拉年"><a href="#巨灯纪-1600维拉年" class="headerlink" title="巨灯纪 1600维拉年"></a>巨灯纪 1600维拉年</h1><p><strong>一如</strong>身边的又一位爱努来到了<strong>阿尔达</strong>，名为<strong>托卡斯</strong>，祂听闻众神在<strong>阿尔达</strong>开战的消息，赶来帮助曼威，<strong>米尔寇</strong>落败，在宇宙的黑暗中潜伏了下来。<strong>托卡斯</strong>也成为了第15名<strong>维拉</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/835256392e624cc385f0c73c6316ebb0?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>托卡斯</p>
<h1 id="巨灯纪-1900-3400维拉年"><a href="#巨灯纪-1900-3400维拉年" class="headerlink" title="巨灯纪 1900-3400维拉年"></a>巨灯纪 1900-3400维拉年</h1><p>众神对<strong>阿尔达</strong>的播种需要光亮，于是祂们合力在大陆上竖起两根巨大的灯柱：<strong>伊路因</strong>和<strong>欧尔瑁</strong>，<strong>维拉</strong>之王曼威亲自为两盏巨灯封圣，<strong>曼威</strong>的妻子<strong>瓦尔坦</strong>召唤<strong>一如</strong>的秘火将它们点亮，整个<strong>阿尔达</strong>被笼罩在圣光之中，万物开始生长，后世将这段时间称为<strong>阿尔达之春</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/14bd4d7d4e854766b33663cbc519e523?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>伊路因</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/43f102d0a31f43429ba66a4f9e7c317e?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>欧尔瑁</p>
<h1 id="巨灯纪-3400维拉年"><a href="#巨灯纪-3400维拉年" class="headerlink" title="巨灯纪 3400维拉年"></a>巨灯纪 3400维拉年</h1><p><strong>托卡斯</strong>与另一名维拉<strong>奈莎</strong>结婚。</p>
<h1 id="巨灯纪-3430维拉年"><a href="#巨灯纪-3430维拉年" class="headerlink" title="巨灯纪 3430维拉年"></a>巨灯纪 3430维拉年</h1><p><strong>米尔寇</strong>一直在黑暗中悄悄对<strong>阿尔达</strong>施加影响，一些迈雅接受了祂的蛊惑站到了祂那一边，其中就包括<strong>索伦</strong>。</p>
<p>积聚了足够力量的<strong>米尔寇</strong>回到了<strong>阿尔达</strong>，带着祂的追随者建造了<strong>乌图姆诺</strong>，在那里用黑暗的法术创造邪物。另外还有宇宙之初就诞生于阴影中的<strong>黑暗巨蛛昂哥立安（Ungoliant）</strong>也响应了米尔寇的召唤，有人认为<strong>昂哥立安</strong>就是<strong>创世大乐章</strong>中由<strong>米尔寇</strong>的那些不和谐之音产生的。</p>
<h1 id="巨灯纪-3440维拉年"><a href="#巨灯纪-3440维拉年" class="headerlink" title="巨灯纪 3440维拉年"></a>巨灯纪 3440维拉年</h1><p><strong>米尔寇</strong>着迷于<strong>阿瑞恩的美丽，</strong>试图诱惑这名<strong>迈雅</strong>娶她为妻，但遭到拒绝，<strong>米尔寇</strong>找到机会强暴了她，令<strong>阿瑞恩</strong>后来舍弃了自己的肉体，专心侍奉维拉<strong>瓦娜</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/204c84e9afc5412aa32d6e9d5b38232c?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>阿瑞恩</p>
<p>但是一些原本从属于<strong>阿瑞恩</strong>的火灵（掌握火焰的<strong>迈雅</strong>）自她失去肉体后，遭到<strong>米尔寇</strong>的蛊惑，成了后来的<strong>炎魔</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/dfic-imagehandler/6f903d98-5591-4fcf-9f0b-c8984054c4e5?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>炎魔</p>
<h1 id="巨灯纪-3450维拉年"><a href="#巨灯纪-3450维拉年" class="headerlink" title="巨灯纪 3450维拉年"></a>巨灯纪 3450维拉年</h1><p><strong>米尔寇</strong>摧毁了两座灯柱，巨灯在<strong>阿尔达</strong>的大地上砸出了两个深坑，其后被水填满形成两个巨大的内海，<strong>赫尔卡内海</strong>与<strong>凛吉尔内海</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/910d861126b94d22bd72c974d2a8eba4?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>米尔寇摧毁巨灯</p>
<p><strong>米尔寇</strong>面对应声赶来的<strong>托卡斯</strong>，不敢与之匹敌，逃回了<strong>乌图姆诺</strong>。</p>
<h1 id="巨灯纪-3500维拉年"><a href="#巨灯纪-3500维拉年" class="headerlink" title="巨灯纪 3500维拉年"></a>巨灯纪 3500维拉年</h1><p>众神在<strong>阿门洲</strong>的<strong>维林诺</strong>种下两颗圣树，<strong>雄株泰尔佩瑞安</strong>和<strong>雌株劳瑞林</strong>，双树在生长期间由<strong>阿瑞恩</strong>照料，<strong>阿尔达</strong>随之进入新的纪元。</p>
<h1 id="双树纪-1-500维拉年"><a href="#双树纪-1-500维拉年" class="headerlink" title="双树纪 1-500维拉年"></a>双树纪 1-500维拉年</h1><p>双树在这段时间里迅速生长，其树叶散发出的光芒能够辐射整个<strong>阿尔达</strong>。各地的迈雅们纷纷被吸引来到<strong>阿门洲</strong>。从此，这里便成了众神们的居住之地。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/5f6f7e1183674ab2bf033655d0c93ccd?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>维林诺的双圣树</p>
<h1 id="双树纪-800维拉年"><a href="#双树纪-800维拉年" class="headerlink" title="双树纪 800维拉年"></a>双树纪 800维拉年</h1><p><strong>维拉</strong>之中的铁匠<strong>奥力</strong>，克制不住想要创造的欲望，用岩石和金属创造了七个人型生物，这些丑陋、矮小的生物就是后来的<strong>矮人</strong>。不过这个行为引起了<strong>一如</strong>的不满，祂虽然准许奥力保留了这些矮人的性命，但告诫祂不可以让<strong>矮人</strong>在自己的首生子女——<strong>精灵</strong>之前苏醒。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/fa27f6eaf7ea4b0f91a4c06e53fb42d8?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>奥力创造矮人</p>
<h1 id="双树纪-1040维拉年"><a href="#双树纪-1040维拉年" class="headerlink" title="双树纪 1040维拉年"></a>双树纪 1040维拉年</h1><p>首批100多名精灵在中土东部的<strong>奎维耶能湖畔</strong>苏醒，由于是<strong>伊露维塔</strong>的创造，他们第一次来到这个世界就能使用自己的语言，即<strong>原始昆迪语</strong>（后来分化成<strong>昆雅语</strong>、<strong>辛达语</strong>等）。</p>
<h1 id="双树纪-1050维拉年"><a href="#双树纪-1050维拉年" class="headerlink" title="双树纪 1050维拉年"></a>双树纪 1050维拉年</h1><p><strong>美丽安</strong>（<strong>迈雅</strong>之一）来到中土，定居在<strong>贝烈瑞安德</strong>的西部地区。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/4865dfc1791444e1a4c303bb48af7786?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>中土的贝烈瑞安德地区，是相当广袤的一块</p>
<h1 id="双树纪-1080维拉年"><a href="#双树纪-1080维拉年" class="headerlink" title="双树纪 1080维拉年"></a>双树纪 1080维拉年</h1><p>精灵们在中土世界开始繁衍，但是<strong>米尔寇</strong>最先发现了他们在中土大陆东侧的定居点，于是祂将一些精灵抓回了<strong>乌图姆诺堡垒</strong>，在那里用酷刑和黑暗法术折磨他们，创造出了<strong>兽人</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/66a6c8fa9286447d82c5f59d014847fc?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>兽人</p>
<p>部分精灵为了躲避米尔寇和其手下的追捕，逃到了大陆的中部地带。</p>
<h1 id="双树纪-1085维拉年"><a href="#双树纪-1085维拉年" class="headerlink" title="双树纪 1085维拉年"></a>双树纪 1085维拉年</h1><p><strong>维拉</strong>中的狩猎之神<strong>欧洛米</strong>在中土游历的时候发现了<strong>精灵</strong>，祂很喜欢这个种族，将他们称为<strong>“星辰之子”</strong>，并将这个消息带回维林诺，众神们这才知道<strong>一如</strong>的首生子女已然苏醒，而且遭到了<strong>米尔寇</strong>的袭击。</p>
<h1 id="双树纪-1090维拉年"><a href="#双树纪-1090维拉年" class="headerlink" title="双树纪 1090维拉年"></a>双树纪 1090维拉年</h1><p>为了保护精灵，<strong>维拉</strong>们再次与<strong>米尔寇</strong>宣战，并且由欧洛米保护着精灵们，战争的结果由<strong>米尔寇</strong>惨败而告终，祂被囚禁在<strong>曼督斯</strong>的厅堂之中，而祂的追随者纷纷四散而逃。</p>
<h1 id="双树纪-1100维拉年"><a href="#双树纪-1100维拉年" class="headerlink" title="双树纪 1100维拉年"></a>双树纪 1100维拉年</h1><p>维拉们认为虽然<strong>米尔寇</strong>被囚禁，但世间的邪物没有被消灭干净，要保护这些<strong>精灵</strong>最好的办法还是让他们前往<strong>维林诺</strong>，接受众神的庇佑，于是精灵历史上最伟大的西迁进程开始。</p>
<p>不过还是有一部分精灵不愿离开家园，这些留在中土大陆中部和东部的<strong>精灵</strong>被称为<strong>南多族</strong>，也就是后来的<strong>西尔凡精灵</strong>（<strong>《霍比特人》</strong>中的女精灵队长<strong>塔瑞尔</strong>就是一名西尔凡精灵）。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/313fc0e8e11249d6b297dfb9e6424ba5?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>塔瑞尔</p>
<h1 id="双树纪-1130维拉年"><a href="#双树纪-1130维拉年" class="headerlink" title="双树纪 1130维拉年"></a>双树纪 1130维拉年</h1><p>选择西迁的精灵有<strong>凡雅族</strong>、<strong>诺多族</strong>和<strong>泰勒瑞族</strong>，他们西进至<strong>贝烈瑞安德</strong>地区的时候被大海阻隔，精灵们畏惧这片无边无际的汪洋，不敢继续前进。在停滞的期间，一位<strong>泰勒瑞族</strong>的精灵小伙<strong>埃路·辛葛</strong>遇见了森林中的<strong>美丽安</strong>，被深深吸引。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/cf2cf0ffad4145d4a9940c23d553d8f0?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>美丽安和辛葛</p>
<h1 id="双树纪-1132维拉年"><a href="#双树纪-1132维拉年" class="headerlink" title="双树纪 1132维拉年"></a>双树纪 1132维拉年</h1><p><strong>维拉</strong>中的海洋之神<strong>乌欧牟</strong>直接在海湾里升起一座岛屿，让精灵们登上岛屿，并驱使着直接航向<strong>阿门洲</strong>的<strong>维林诺</strong>。</p>
<p>与此同时<strong>泰勒瑞族</strong>的精灵为了寻找<strong>辛葛</strong>不得不放弃西迁，他们一部分留在<strong>贝烈瑞安德</strong>继续寻找，另一部分在南方的海边建起了一些海港城市，这部分<strong>泰勒瑞族精灵</strong>就成了后来擅长造船的<strong>海洋精灵</strong>。</p>
<h1 id="双树纪-1135维拉年"><a href="#双树纪-1135维拉年" class="headerlink" title="双树纪 1135维拉年"></a>双树纪 1135维拉年</h1><p><strong>凡雅族</strong>和<strong>诺多族</strong>抵达阿门洲，并开始在众神的教导下学习知识和各种技艺。</p>
<h1 id="双树纪-1152维拉年"><a href="#双树纪-1152维拉年" class="headerlink" title="双树纪 1152维拉年"></a>双树纪 1152维拉年</h1><p><strong>辛葛</strong>带着<strong>美丽安</strong>从森林中出现，两人已经成婚，剩余还留在<strong>贝烈瑞安德</strong>的<strong>泰勒瑞族精灵</strong>惊异于<strong>辛葛</strong>的执着和大胆，决定留在这里尊奉<strong>辛葛</strong>为王，自此这部分精灵就自称<strong>“辛达族”</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/900b829ec07a42d7a0ddbff917c079d7?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>辛葛</p>
<h1 id="双树纪-1169维拉年"><a href="#双树纪-1169维拉年" class="headerlink" title="双树纪 1169维拉年"></a>双树纪 1169维拉年</h1><p><strong>诺多族</strong>精灵王<strong>芬威</strong>的长子<strong>费诺（Fëanor）</strong>出生，但是他的母亲<strong>弥瑞尔</strong>在生下<strong>费诺</strong>后感到生命力枯竭，向<strong>曼督斯</strong>请求从痛苦的负担中解脱，她独自来到<strong>罗瑞恩</strong>的花园中躺下，灵魂离开了躯体进入<strong>曼督斯</strong>的厅堂。</p>
<p>这是历史上第一起精灵凭自我意志离开人世的事件，让<strong>维拉</strong>们震惊不已，但<strong>弥瑞尔</strong>还是被允许复活，但之后她只能生活在<strong>维林诺</strong>，侍奉维拉之一的<strong>薇瑞</strong>。</p>
<h1 id="双树纪-1200维拉年"><a href="#双树纪-1200维拉年" class="headerlink" title="双树纪 1200维拉年"></a>双树纪 1200维拉年</h1><p>矮人的七位始祖和<strong>奥力</strong>为他们创造的妻子们先后在群山的地底苏醒，<strong>长须氏族</strong>的<strong>都林</strong>来到<strong>迷雾山脉</strong>中部，在那里建立起了<strong>卡扎**</strong>督姆<strong>（后更名为</strong>摩瑞亚**）。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/6899db021346466b9e8a3e19795cbc8c?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>卡扎督姆</p>
<p>也是在该年，<strong>辛葛</strong>和<strong>美丽安</strong>的女儿<strong>露西安</strong>出生，她是<strong>阿尔达</strong>历史上第一个混血精灵，而且混的还是<strong>迈雅</strong>的血脉。</p>
<h1 id="双树纪-1250维拉年"><a href="#双树纪-1250维拉年" class="headerlink" title="双树纪 1250维拉年"></a>双树纪 1250维拉年</h1><p><strong>费诺</strong>展现出了惊人的天赋和才能，他首次将精灵的<strong>昆雅语</strong>以书面方式记录下来，创造了<strong>滕格瓦字母</strong>，后来这种字母又传播到其他精灵族群那里，包括<strong>辛达语</strong>、<strong>泰勒瑞语</strong>在内的精灵语系都是用<strong>滕格瓦字母</strong>书写的。就连后来出现的人类<strong>努曼诺尔</strong>语也是由<strong>滕格瓦字母</strong>发展而来。</p>
<p>在这一年，<strong>火须</strong>和<strong>宽梁氏族</strong>的<strong>矮人</strong>在<strong>贝烈瑞安德</strong>地区第一次遇到<strong>辛达族精灵</strong>，他们被精灵的优雅和先进的文化所吸引，精灵们也对这个全新的智慧种族很感兴趣。</p>
<p>后来两个种族之间的交流日渐平凡，<strong>矮人</strong>通过学习展现出了高超的锻造技艺和工程方面的天赋，他们为<strong>辛达族</strong>精灵建造了首都<strong>明霓国斯</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/b2a71dd3667b46cf8f11c1015fcb8e33?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>明霓国斯</p>
<h1 id="双树纪-1280维拉年"><a href="#双树纪-1280维拉年" class="headerlink" title="双树纪 1280维拉年"></a>双树纪 1280维拉年</h1><p>由于担心那些贪婪的<strong>矮人</strong>对中土世界无节制的开采和破坏，大地之后<strong>雅凡娜</strong>向至高神<strong>一如</strong>请求帮助。<strong>一如</strong>同意<strong>雅凡娜</strong>再创造一种生物来守护中土地面上的动植物，于是<strong>树人（Ents）</strong>便诞生了。</p>
<p>不过与<strong>精灵</strong>、<strong>人类</strong>和<strong>矮人</strong>不同，<strong>树人</strong>并没有被<strong>一如</strong>灌注<strong>秘火</strong>，所以它们严格来说不能算作<strong>阿尔达</strong>的智慧生物，虽然它们也能够讲话，但这种能力只是后来从精灵那里学习来的。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/8ce0fd5382014b6d8635b7fb77347946?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>树人</p>
<h1 id="双树纪-1330维拉年"><a href="#双树纪-1330维拉年" class="headerlink" title="双树纪 1330维拉年"></a>双树纪 1330维拉年</h1><p><strong>兽人</strong>在米尔寇被囚禁的这段时间里一直躲在北方山脉之中，它们强大的适应能力让其种群迅速扩散。终于在这一年，<strong>辛达族精灵</strong>在他们王国的北部边境遇到了这个野蛮好战的种族。其后双方时有战争发生，<strong>蓝色山脉</strong>的<strong>火须</strong>和<strong>宽梁氏族</strong>的矮人也加入进来，为精灵们打造武器和盔甲，积极备战。</p>
<h1 id="双树纪-1362维拉年"><a href="#双树纪-1362维拉年" class="headerlink" title="双树纪 1362维拉年"></a>双树纪 1362维拉年</h1><p><strong>凯兰崔尔</strong>出生。</p>
<h1 id="双树纪-1400维拉年"><a href="#双树纪-1400维拉年" class="headerlink" title="双树纪 1400维拉年"></a>双树纪 1400维拉年</h1><p>遭到囚禁300年后，<strong>米尔寇</strong>得到释放，其后他一直表现良好，向<strong>维林诺</strong>的精灵们表达了自己的歉意。</p>
<h1 id="双树纪-1450维拉年"><a href="#双树纪-1450维拉年" class="headerlink" title="双树纪 1450维拉年"></a>双树纪 1450维拉年</h1><p><strong>费诺</strong>完成三颗<strong>精灵宝钻</strong>的制作，这个惊世之作让维拉们也感到震撼，<strong>米尔寇</strong>更是对<strong>精灵宝钻</strong>垂涎三尺。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/66d3f410824143089384118944162e5c?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>费诺</p>
<h1 id="双树纪-1490维拉年"><a href="#双树纪-1490维拉年" class="headerlink" title="双树纪 1490维拉年"></a>双树纪 1490维拉年</h1><p><strong>米尔寇</strong>一直在<strong>诺多精灵</strong>的王族之间挑拨离间，真就导致了王族内部产生分裂，<strong>费诺</strong>甚至拔剑想要杀死自己同父异母的弟弟<strong>芬**</strong>国昐**，但遭到制止。</p>
<p>维拉介入，在对<strong>费诺</strong>的审判中得知这些都是<strong>米尔寇</strong>的阴谋，<strong>米尔寇</strong>仓惶出逃，而<strong>费诺</strong>则被<strong>维拉</strong>驱逐出<strong>维林诺</strong>，搬去位于阿门洲北方的堡垒<strong>佛米诺斯</strong>。</p>
<h1 id="双树纪-1492维拉年"><a href="#双树纪-1492维拉年" class="headerlink" title="双树纪 1492维拉年"></a>双树纪 1492维拉年</h1><p><strong>米尔寇</strong>找到了躲藏在<strong>阿尔达</strong>南部大陆阴影之地的<strong>昂哥立安</strong>，向其许以精灵们多年来积攒的珍宝，邀请它再次出山帮助自己。同时北方要塞<strong>安格班</strong>被建立，<strong>兽人</strong>、<strong>妖狼</strong>、<strong>炎魔</strong>等邪物重新聚集起来。</p>
<h1 id="双树纪-1495维拉年"><a href="#双树纪-1495维拉年" class="headerlink" title="双树纪 1495维拉年"></a>双树纪 1495维拉年</h1><p>在<strong>昂哥立安</strong>编织的黑暗之网的帮助下，<strong>米尔寇</strong>和<strong>昂哥立安</strong>利用<strong>维林诺</strong>正在举办盛大庆典的空挡，躲开了巡逻的精灵守卫进入圣地。<strong>米尔寇</strong>用武器重创了<strong>双圣树</strong>的核心，随后<strong>昂哥立安</strong>扑上去吮吸它们的汁液，同时还注入了黑暗的毒素。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/c9491ea109a844369a4606de7bd5c471?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>米尔寇和昂哥立安摧毁圣树</p>
<p><strong>双圣树</strong>遭到重创，<strong>雅凡娜</strong>请求<strong>费诺</strong>提供<strong>精灵宝钻</strong>来拯救双树，由于这会导致宝钻被摧毁，这个请求遭到<strong>费诺</strong>的拒绝。</p>
<p>与此同时，<strong>米尔寇</strong>又偷偷来到北方堡垒佛米诺斯，杀死了诺多族<strong>精灵王芬威</strong>，偷走了宝库中的三颗<strong>精灵宝钻</strong>和其他财宝。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/a2c4c681bb8946afb49fd79d08e2f8a2?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>魔苟斯</p>
<p>得知此事的<strong>费诺</strong>大怒，痛斥<strong>米尔寇</strong>为<strong>魔苟斯</strong>（意为“黑暗之敌”），并且煽动他的七个儿子和部分<strong>诺多族精灵</strong>以<strong>一如</strong>的名义起誓，定要夺回<strong>精灵宝钻</strong>，而且任何沾手<strong>精灵宝钻</strong>的人不论是谁，都将是他们的敌人。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/d27d2a65c11546a18565a4f69f03b9f1?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>费诺众子</p>
<h1 id="双树纪-1496维拉年"><a href="#双树纪-1496维拉年" class="headerlink" title="双树纪 1496维拉年"></a>双树纪 1496维拉年</h1><p><strong>魔苟斯</strong>与<strong>昂哥立安</strong>遵照事先约定在中土北方山脉的某处见面，由于分赃不均（<strong>魔苟斯</strong>拒绝交出三颗<strong>精灵宝钻</strong>）而触怒了<strong>昂哥立安</strong>，黑暗巨蛛差一点将<strong>魔苟斯</strong>杀死，但闻讯赶来的<strong>炎魔</strong>救下了它们的主子。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/efaf8aea03fc4f088ee3cbd2c7022f23?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>魔苟斯与昂哥立安</p>
<p>同一年，为了夺回宝钻而起兵的<strong>费诺</strong>在<strong>阿门洲</strong>港口城市<strong>澳阔泷迪</strong>与<strong>泰勒瑞族</strong>的<strong>海洋精灵</strong>发生冲突，这些海洋精灵拒绝同<strong>诺多族</strong>一起前往中土，也拒绝提供船只，试图强行登船的<strong>诺多族</strong>与他们展开械斗，最终演变为蔓延全城的战争。只有轻装甲和弓箭的<strong>泰勒瑞族</strong>不是装备精良的<strong>诺多族</strong>的对手，许多<strong>泰勒瑞族</strong>精灵被杀，夺取了船只的<strong>诺多族</strong>驾船北上，离开了<strong>阿门洲</strong>。史称<strong>“第一次亲族相残”</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/095df87d8b1a4b0cbf5dc3de12e1b6f9?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>第一次亲族相残</p>
<p>这次事件导致一些<strong>诺多族</strong>的精灵对他们的行为产生了怀疑，从而放弃了前往中土的想法，返回维林诺，其中就包括<strong>芬威</strong>的儿子<strong>菲纳芬</strong>（<strong>凯兰崔尔</strong>的父亲），但是年轻的<strong>凯兰崔尔</strong>并没有跟随父亲回到维林诺。</p>
<p>亲族相残造成了恶劣影响，<strong>维拉</strong>对那些不思悔改的<strong>诺多精灵</strong>降下了诅咒，他们今后永远不得返回<strong>维林诺</strong>，死后也无法在这里复活（<strong>凯兰崔尔</strong>后来一直在寻找解决的办法，直到她成功拒绝了魔戒的诱惑，才通过考验得已重返<strong>维林诺</strong>）。</p>
<h1 id="双树纪-1497维拉年"><a href="#双树纪-1497维拉年" class="headerlink" title="双树纪 1497维拉年"></a>双树纪 1497维拉年</h1><p>取得了三颗宝钻的<strong>魔苟斯</strong>不可一世，同时他麾下的兽人部队不时地袭扰<strong>贝烈瑞安德</strong>的<strong>辛达族</strong>精灵王国<strong>多瑞亚斯</strong>，<strong>美丽安</strong>在王国周边设置了<strong>美丽安环带（Girdle of Melian）</strong>，没有人可以在未经允许的情况下通过这条环带。</p>
<p>同年，<strong>费诺</strong>和他的追随者们抵达了中土大陆，<strong>辛达族</strong>精灵国王拒绝为他们提供帮助，<strong>费诺</strong>只能带着疲惫的<strong>诺多族精灵</strong>与齐装满员的<strong>魔苟斯</strong>军队交战。</p>
<p>战争过程中<strong>费诺</strong>被<strong>魔苟斯</strong>手下的炎魔大将<strong>勾斯魔格（Gothmog）</strong>杀死，其长子<strong>迈兹洛斯</strong>被魔苟斯俘虏，<strong>诺多精灵</strong>惨败，由于发生在双圣树倒下，没有光明的时分，故称<strong>“星下之战”</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/dfic-imagehandler/63b51052-2509-4272-b8d0-f732ab1b455f?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>星下之战</p>
<h1 id="双树纪-1500维拉年"><a href="#双树纪-1500维拉年" class="headerlink" title="双树纪 1500维拉年"></a>双树纪 1500维拉年</h1><p>双树彻底枯萎之前，雄株<strong>泰尔佩瑞安</strong>最后的花朵和雌株<strong>劳瑞林</strong>最后的果实被取下，<strong>奥力</strong>为其打造了两艘飞船，分别由两位迈雅<strong>提理安</strong>和<strong>阿瑞恩</strong>驾驶着升上天空，花朵为月，果实为日，继续为整个<strong>阿尔达</strong>带去光明，<strong>太阳纪元</strong>开始。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/666fef95f90248e785c67bc69c798ed9?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>提理安和阿瑞恩</p>
<h1 id="第一纪元-1太阳年-（以下纪年方式都是太阳年）"><a href="#第一纪元-1太阳年-（以下纪年方式都是太阳年）" class="headerlink" title="第一纪元 1太阳年 （以下纪年方式都是太阳年）"></a>第一纪元 1太阳年 （以下纪年方式都是太阳年）</h1><p>人类在中土大陆东方的<strong>希尔多瑞恩</strong>苏醒。没多久，<strong>魔苟斯</strong>便注意到了这个年轻的智慧种族。</p>
<h1 id="第一纪元-5年"><a href="#第一纪元-5年" class="headerlink" title="第一纪元 5年"></a>第一纪元 5年</h1><p><strong>费诺</strong>同父异母的弟弟<strong>芬国**</strong>昐<strong>不计前嫌，率众来到中土帮助自己的同胞讨伐</strong>魔苟斯<strong>，没想到得知</strong>费诺<strong>已死的消息。</strong>芬国<strong>**昐</strong>的儿子<strong>芬巩</strong>将<strong>迈兹洛斯</strong>从魔苟斯的囚禁中救出，为表感激，他放弃了自己继承诺多精灵至高王的权利，让贤给了<strong>芬国昐</strong>。</p>
<h1 id="第一纪元-10-50年"><a href="#第一纪元-10-50年" class="headerlink" title="第一纪元 10-50年"></a>第一纪元 10-50年</h1><p>生活在中土的诺多族精灵由<strong>芬**</strong>国昐<strong>统领，当务之急是为整个族群寻找一个栖身之所，辛葛领导的多瑞亚斯自然不会接纳他们（第一次亲族相残，</strong>诺多族<strong>残杀的</strong>泰勒瑞族<strong>是他们的亲缘），于是</strong>芬国昐<strong>决定在</strong>多瑞亚斯<strong>的西侧，</strong>纳国斯隆德<strong>定居，都城</strong>纳国斯隆德要塞**开始建立。</p>
<h1 id="第一纪元-55年"><a href="#第一纪元-55年" class="headerlink" title="第一纪元 55年"></a>第一纪元 55年</h1><p><strong>魔苟斯</strong>来到中土东部的人类聚居地，煽动部分人类改变对<strong>一如</strong>的信仰，转而崇拜他这个黑暗大帝（自卖自夸）。那些没有被其蛊惑的人类选择离开往西迁徙，成为最早的<strong>伊甸人</strong>。留下的人类将<strong>魔苟斯</strong>奉为真神，在腐化中堕落，成为东方的蛮夷部落。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/7ce2db9a255243d6af96c61f29c7d191?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>东夷</p>
<p>不过据说还有一些人类既没有西迁，也没有接受<strong>魔苟斯</strong>的蛊惑，转而进入迷雾山脉周遭的森林中躲藏起来，后来的<strong>森林野人</strong>和<strong>贝奥恩一族</strong>都是他们的后代。进入<strong>第三纪元</strong>才出现的<strong>霍比特人</strong>被认为是那些森林野人的后代。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/3d67bd73424445e4ab5d10da79523182?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>霍比特人</p>
<h1 id="第一纪元-60年"><a href="#第一纪元-60年" class="headerlink" title="第一纪元 60年"></a>第一纪元 60年</h1><p><strong>魔苟斯</strong>的兽人大军再次南下，对刚刚建立没多久的诺多族精灵王国<strong>纳国斯隆德</strong>发起突袭，随即<strong>芬国昐</strong>联合<strong>费诺之子</strong>及他们的追随者顶住了敌人的进攻，并乘势将整个战线向北推进，这次军事行动十分迅猛，史称<strong>“荣耀之战”</strong>。</p>
<p><strong>荣耀之战</strong>直接导致了长达350多年的<strong>安格班</strong>合围。诺多精灵众多将领和贵族们都认为与其在<strong>纳国斯隆德</strong>被动挨打，不如主动将战线推进到<strong>安格班</strong>，虽然他们的军力还不足以攻破<strong>安格班</strong>这个坚固的堡垒，但至少可以保证自己后方国土的安全。</p>
<h1 id="第一纪元-64年"><a href="#第一纪元-64年" class="headerlink" title="第一纪元 64年"></a>第一纪元 64年</h1><p>为了给<strong>安格班合围</strong>运送大量的给养，<strong>诺多族</strong>精灵和<strong>辛达族</strong>精灵联合在北方建立了数个重镇（<strong>辛葛</strong>认为安格班合围对<strong>多瑞亚斯</strong>北方的安全有好处，所以愿意提供部分帮助），其中最重要的城市就是<strong>贡多林</strong>，后来<strong>贡多林</strong>发展成<strong>贝烈瑞安德</strong>北方最大的精灵城市。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/491bd52ef3be4760b19ea5c4986a11ac?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>刚建成时的贡多林</p>
<h1 id="第一纪元-115年"><a href="#第一纪元-115年" class="headerlink" title="第一纪元 115年"></a>第一纪元 115年</h1><p><strong>纳国斯隆德</strong>要塞和<strong>贡多林</strong>相继建成，此时<strong>火须</strong>和<strong>宽梁</strong>两个矮人氏族也在蓝色山脉中建立起宏伟的矮人城市<strong>贝烈戈斯特</strong>与<strong>诺格罗德</strong>。由于安格班合围给这片地区带来的和平期，矮人与精灵开展了频繁的贸易活动，<strong>贝烈瑞安德</strong>的商路开始活跃起来。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/158f463366f4406a985989745bfbba72?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>纳国斯隆德要塞</p>
<h1 id="第一纪元-117年"><a href="#第一纪元-117年" class="headerlink" title="第一纪元 117年"></a>第一纪元 117年</h1><p><strong>魔苟斯</strong>用黑暗的法术混合着一些中土大陆上的原生生物制造出一只可以喷火的巨大爬虫，将其命名为<strong>“格劳龙”</strong>，它被认为是后来那些恶龙们的始祖。</p>
<h1 id="第一纪元-260年"><a href="#第一纪元-260年" class="headerlink" title="第一纪元 260年"></a>第一纪元 260年</h1><p>急于打破合围的<strong>魔苟斯</strong>将当时还未完全成形的<strong>格劳龙</strong>派上战场，虽然造成一些战果，但还是被诺多王子芬巩带领一队骑射手打退。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/f6e050e52ccf4e81b6249e4149a19897?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>未成年的格劳龙</p>
<p>往后的近200年里，<strong>魔苟斯</strong>都不敢尝试打破合围，只能在城堡里慢慢憋。</p>
<h1 id="第一纪元-310年"><a href="#第一纪元-310年" class="headerlink" title="第一纪元 310年"></a>第一纪元 310年</h1><p><strong>伊甸人</strong>中的<strong>贝奥家族</strong>翻越了蓝色山脉，首次进入<strong>贝烈瑞安德</strong>，随后又有数个<strong>伊甸人</strong>家族来到这里，他们定居在东贝烈瑞安德，期间他们与<strong>矮人</strong>和<strong>精灵</strong>都发生了接触。由于都是<strong>一如</strong>的子女，精灵们对这些人类格外关心。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/17cb81d15fbe49779a8b2197b17a622c?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>精灵与伊甸人</p>
<h1 id="第一纪元-310-450年"><a href="#第一纪元-310-450年" class="headerlink" title="第一纪元 310-450年"></a>第一纪元 310-450年</h1><p>这段时期是<strong>贝烈瑞安德</strong>难得的和平时期，<strong>精灵</strong>、<strong>矮人</strong>、<strong>人类</strong>都在这里有着自己的生存空间，彼此交流、学习。其中<strong>人类</strong>是受益最大的群体，他们最晚苏醒，刚刚来到这个世界不久，矮人和精灵在这段时间里都有精力教导这个年轻的种族。</p>
<p>不同与<strong>精灵</strong>拥有不朽的生命，人类的寿命短得多，虽然看上去是个劣势，但这也让人类在种群繁衍的速度上远远快过那两个种族。在这段将近140多年的时间里，人类的数量迅速增加，很快就达到了和精灵差不多的程度，许多人类早期的英雄，如<strong>贝伦</strong>、<strong>胡林</strong>、<strong>胡奥</strong>、都在这段时间里相继出生。</p>
<h1 id="第一纪元-455-456年"><a href="#第一纪元-455-456年" class="headerlink" title="第一纪元 455-456年"></a>第一纪元 455-456年</h1><p><strong>“骤火之战”</strong>爆发，憋了好久的<strong>魔苟斯</strong>终于忍不住了，手下的<strong>兽人</strong>、<strong>妖狼</strong>、<strong>巨魔</strong>、<strong>炎魔</strong>和成年体的<strong>格劳龙</strong>倾巢而出，合围安格班的精灵军队措手不及，迅速被分割包围。</p>
<p>诺多族精灵<strong>至高王芬国昐</strong>亲自来到安格班门前挑战<strong>魔苟斯</strong>，虽然刺伤了魔王的左脚，但还是因为体力不支死在了魔苟斯的巨锤<strong>“格龙得”</strong>之下。</p>
<p><img src="https://p6-tt.byteimg.com/origin/dfic-imagehandler/45f9df44-9dbf-40e3-bc6b-c041ea6d0e2d?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>至高王芬国昐的陨落</p>
<p>西线的战场由于有<strong>伊甸人</strong>的军队赶到增援，精灵军队得已撤出大部，但是<strong>格劳龙</strong>的龙焰还是导致大量联军的伤亡，不少<strong>伊甸人</strong>部族的首领都死在了这场战争中。</p>
<p>随后<strong>魔苟斯</strong>的部队在<strong>索伦</strong>的带领下席卷了整个北方，几乎推进到<strong>贡多林</strong>附近。</p>
<h1 id="第一纪元-457年"><a href="#第一纪元-457年" class="headerlink" title="第一纪元 457年"></a>第一纪元 457年</h1><p>精灵们意识到<strong>人类</strong>在对抗<strong>魔苟斯</strong>时发挥的巨大作用，认为应该加强与他们的合作。这一年<strong>胡林</strong>和<strong>胡奥</strong>作为<strong>哈多家族</strong>的继承人受到新任<strong>至高王芬巩</strong>的邀请前往<strong>纳国斯隆德</strong>，期间他们游历了整个精灵王国，与<strong>芬巩</strong>约定将联手阻击<strong>魔苟斯</strong>的大军。</p>
<h1 id="第一纪元-458年"><a href="#第一纪元-458年" class="headerlink" title="第一纪元 458年"></a>第一纪元 458年</h1><p>为了应对<strong>格劳龙</strong>的龙焰，<strong>贝烈戈斯特</strong>与<strong>诺格罗德</strong>的矮人们开始打造一种融合了精灵魔法的头盔。</p>
<h1 id="第一纪元-464年"><a href="#第一纪元-464年" class="headerlink" title="第一纪元 464年"></a>第一纪元 464年</h1><p><strong>贝奥家族</strong>的<strong>贝伦</strong>游历到<strong>多瑞亚斯</strong>附近，无意间闯入了<strong>美丽安环带</strong>，这个迈雅制造的环带竟然没有阻止这个年轻人。在那里他遇见了<strong>露西安</strong>，两人一见钟情，但是遭到了露西安的父亲，精灵王<strong>辛葛</strong>的反对。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/1a0cb08235c5442ebdd4fe02ec05b6b6?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>露西安与贝伦</p>
<p>同年<strong>胡林</strong>的儿子<strong>图林·图伦拔</strong>出生。</p>
<h1 id="第一纪元-465年"><a href="#第一纪元-465年" class="headerlink" title="第一纪元 465年"></a>第一纪元 465年</h1><p><strong>贝伦</strong>接受了<strong>辛葛</strong>荒唐的条件，出发前往<strong>安格班</strong>夺取<strong>精灵宝钻</strong>，作为自己对<strong>露西安</strong>爱情的证明。期间他得到了许多人的帮助，<strong>露西安</strong>也在<strong>神犬胡安</strong>的协助下来到贝伦身边与他一同前往<strong>安格班</strong>。</p>
<h1 id="第一纪元-466年"><a href="#第一纪元-466年" class="headerlink" title="第一纪元 466年"></a>第一纪元 466年</h1><p><strong>露西安</strong>和<strong>贝伦</strong>成功盗得一颗<strong>精灵宝钻</strong>，但贝伦自己也被<strong>巨狼卡哈洛斯</strong>咬成重伤，不久离世。<strong>露西安</strong>抱着爱人的躯体，也自愿放弃了生命。在后来<strong>曼督斯</strong>的宽恕下，<strong>露西安</strong>和<strong>贝伦</strong>得以重生，但<strong>露西安</strong>只能作为人类重生，将同普通人一样生老病死。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/05c1998be49745808fac6ae489410cfc?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>露西安与贝伦</p>
<h1 id="第一纪元-470年"><a href="#第一纪元-470年" class="headerlink" title="第一纪元 470年"></a>第一纪元 470年</h1><p><strong>露西安</strong>和<strong>贝伦</strong>的儿子<strong>迪奥</strong>出生。</p>
<h1 id="第一纪元-472年"><a href="#第一纪元-472年" class="headerlink" title="第一纪元 472年"></a>第一纪元 472年</h1><p><strong>费诺</strong>的长子<strong>迈兹洛斯</strong>听闻<strong>贝伦</strong>和<strong>露西安</strong>成功盗得一颗<strong>精灵宝钻</strong>的消息，对抗击<strong>魔苟斯</strong>的事业重燃希望。他召集了自己的兄弟，费诺的其他几个儿子，联合诺多精灵<strong>至高王芬巩</strong>以及伊甸人和矮人，组成联盟再次进攻<strong>安格班</strong>。</p>
<p>由于早前<strong>费诺</strong>的儿子当中有两个纨绔子弟曾经绑架过<strong>露西安</strong>，辛达族精灵王<strong>辛葛</strong>拒绝了联盟的邀请，而且那两个活宝早前还曾在<strong>纳国斯隆德</strong>招摇撞骗，导致许多诺多精灵的贵族也不愿意参战，诺多精灵<strong>至高王芬巩</strong>只带了一些自己的亲卫队赶来支持自己的好友。</p>
<p>整个战争局势并不如<strong>迈兹洛斯</strong>预想的那样，他们的行动早就被间谍回报给<strong>安格班</strong>，在安<strong>法乌格砾斯平原</strong>，西线的联军被<strong>勾斯魔格</strong>领导的军队击溃，<strong>至高王芬巩</strong>苦战至最后一刻，在两只炎魔的夹击下，被<strong>炎魔之王勾斯魔格</strong>杀死。</p>
<p>东线的战局也好不到哪去，<strong>至高王芬巩</strong>的弟弟<strong>图尔巩</strong>和<strong>伊甸人</strong>的联军被<strong>格劳龙</strong>的龙焰击溃，<strong>图尔巩</strong>原本打算效仿他的哥哥战死，但被<strong>胡林</strong>和<strong>胡奥</strong>劝阻，表示<strong>诺多族</strong>不能没有<strong>至高王</strong>，他这样战死其实是怯懦的行为，<strong>图尔巩</strong>只好带着自己的卫队退出了战场。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/a14f743d42004fb6b62afe3b13abe0b7?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>胡林苦战被俘</p>
<p><strong>胡林</strong>、<strong>胡</strong>奥率领的伊甸人军队为了掩护<strong>图尔巩</strong>失去了撤退的时机，他们留下来和同样一步没退的<strong>贝烈戈斯特矮人</strong>一起战斗到了最后。</p>
<p><strong>矮人</strong>对火焰有一定抵抗力，戴着灌注了精灵魔法的头盔更是让他们能够与<strong>格劳龙</strong>抗衡，矮人们将格劳龙与其他的兽人部队分隔开，对<strong>格劳龙</strong>造成重创。要不是其他阵线都被兽人突破，<strong>格劳龙</strong>必然死在这里。接应<strong>矮人</strong>的伊甸人部队也被西线赶来增援的炎魔和兽人淹没，<strong>胡奥</strong>战死，<strong>胡林</strong>被俘。整个战争使联军蒙受了巨大损失，史称<strong>“**</strong>泪雨<strong>**之战”</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/de672655b6a64abc9645ca6bcba9c904?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>泪雨之战</p>
<h1 id="第一纪元-473年"><a href="#第一纪元-473年" class="headerlink" title="第一纪元 473年"></a>第一纪元 473年</h1><p>由于<strong>胡林</strong>在战争中被俘，<strong>辛葛</strong>建议<strong>图林</strong>的母亲将<strong>图林·图伦拔</strong>送来<strong>多瑞亚斯</strong>抚养，<strong>图林</strong>在辛达族精灵那里成长为一名出色的伊甸人战士。</p>
<h1 id="第一纪元-497年"><a href="#第一纪元-497年" class="headerlink" title="第一纪元 497年"></a>第一纪元 497年</h1><p><strong>贝伦</strong>和<strong>露西安</strong>之子<strong>迪奥</strong>与辛达族精灵贵族<strong>宁洛丝</strong>结婚。</p>
<h1 id="第一纪元-499年"><a href="#第一纪元-499年" class="headerlink" title="第一纪元 499年"></a>第一纪元 499年</h1><p>不可一世的<strong>格劳龙</strong>深入<strong>贝烈瑞安德</strong>腹地，带领手下在布瑞希尔森林大肆破坏，<strong>图林</strong>在<strong>卡贝得深谷</strong>伏击<strong>格劳龙</strong>，他用自己的宝剑<strong>“死亡之铁”</strong>切开了巨龙腹部的薄弱部位杀死了它。<strong>图林</strong>也因为一系列误会，误杀了自己的好友，最后拔剑自尽。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/eaf6e5cd24af4e49aa2207ca306ae962?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>格劳龙之死</p>
<h1 id="第一纪元-502年"><a href="#第一纪元-502年" class="headerlink" title="第一纪元 502年"></a>第一纪元 502年</h1><p>精灵王<strong>辛葛</strong>得到了<strong>瑙格拉弥尔项链</strong>，蓝色山脉矮人早年间铸造的一款稀世珍品，再加上之前得到的一颗<strong>精灵宝钻</strong>，使得辛葛成为目前为止与<strong>魔苟斯</strong>对抗过程中的最大受益者，其他种族都在战争中蒙受了巨大损失。</p>
<p><strong>费诺众子们</strong>曾经在<strong>泪雨**</strong>之战<strong>前找</strong>辛葛<strong>讨要</strong>精灵宝钻<strong>，但被</strong>辛葛**严词拒绝。</p>
<p>如今<strong>辛葛</strong>决定将<strong>精灵宝钻</strong>镶嵌在<strong>瑙格拉弥尔项链</strong>上，以宣示自己对这两款宝物的所有权。但是接到这个工作的一群<strong>诺格罗德矮人</strong>工匠却对这两个宝物起了歹念。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/6a64287364fa4b29a2f35c60d483ef3c?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>辛葛被刺</p>
<p>在项链完工之后，那些矮人工匠杀死了<strong>辛葛</strong>，夺走了镶嵌着宝钻的项链仓皇出逃。这些矮人工匠大部分被<strong>多瑞亚斯</strong>的守卫追上并杀死，但仍有两位逃回了<strong>蓝色山脉</strong>，在他们颠倒黑白的叙述中，<strong>辛葛</strong>反而成了背信弃义之人，矮人王大怒，发动军队进攻<strong>多瑞亚斯</strong>。</p>
<p>此时悲伤的<strong>美丽安</strong>已经离开中土回到<strong>维林诺</strong>，<strong>美丽安环带</strong>也随之失去作用，矮人大军长驱直入将整个精灵都城洗劫一空，所有的精灵财宝都被矮人抢走，当然也包括那根镶嵌着宝钻的项链。</p>
<p>此时身为凡人的<strong>贝伦</strong>实际已经超过70岁，听说<strong>多瑞亚斯</strong>被矮人进攻的消息后与自己的儿子<strong>迪奥</strong>一起召集了一些本地的<strong>伊甸人</strong>和<strong>西尔凡精灵</strong>北上阻止矮人。但他们赶到时<strong>明霓国斯</strong>已经覆灭，刚好赶上矮人将抢得的财宝搬回蓝色山脉，贝伦的军队突袭了矮人，<strong>贝伦</strong>还与矮人王<strong>瑙格拉都尔</strong>对决，他杀死了这名矮人王，将<strong>瑙格拉弥尔项链</strong>夺回。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/5c9d2586cf76423f910150c62acb3685?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>贝伦夺回项链</p>
<h1 id="第一纪元-503年"><a href="#第一纪元-503年" class="headerlink" title="第一纪元 503年"></a>第一纪元 503年</h1><p><strong>迪奥</strong>和<strong>宁洛丝</strong>的女儿<strong>埃尔汶</strong>出生，<strong>埃尔汶</strong>后来嫁给了另一个著名的半精灵，图奥的儿子<strong>埃雅仁迪尔</strong>。</p>
<h1 id="第一纪元-506年"><a href="#第一纪元-506年" class="headerlink" title="第一纪元 506年"></a>第一纪元 506年</h1><p><strong>贝伦</strong>和<strong>露西安</strong>一同去世，<strong>瑙格拉弥尔项链</strong>被交给<strong>迪奥</strong>保管。不久之后，<strong>费诺众子</strong>便向<strong>迪奥</strong>讨要<strong>精灵宝钻</strong>，遭到迪奥拒绝。随即<strong>费诺众子</strong>率领麾下的<strong>诺多精灵</strong>进攻<strong>多瑞亚斯</strong>。刚刚经历过一次矮人洗劫的<strong>多瑞亚斯</strong>根本没有力量抵抗，<strong>迪奥</strong>和<strong>宁洛丝</strong>双双被杀，当时才3岁的<strong>埃尔汶</strong>带着<strong>精灵宝钻</strong>由卫队保护逃离<strong>多瑞亚斯</strong>，悄悄在<strong>西瑞安河口</strong>的一个港口城市住了下来，史称<strong>“第二次亲族相残”</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/717fb240a38f49c59deb9fe5b81100a5?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>第二次亲族相残</p>
<h1 id="第一纪元-510年"><a href="#第一纪元-510年" class="headerlink" title="第一纪元 510年"></a>第一纪元 510年</h1><p><strong>魔苟斯</strong>进攻<strong>贡多林</strong>，在内奸的帮助下，顺利拿下这个城市，包括<strong>至高王图尔巩</strong>在内的众多诺多精灵贵族均在此役战死，不过<strong>炎魔之王勾斯魔格</strong>在这场战役中被一位英勇的诺多精灵战士撞下深谷，同归于尽。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/2c2591f986134245a176ad113254b7bb?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>炎魔之王勾斯魔格</p>
<p>图奥（胡奥之子）的儿子<strong>埃雅仁迪尔</strong>和继任的<strong>至高王吉尔-加拉德</strong>（芬巩之子）以及一些贡多林的难民们逃了出来，在西瑞安河口住下，在那里，<strong>埃雅仁迪尔</strong>遇见了同样落难的<strong>埃尔汶</strong>。</p>
<h1 id="第一纪元-515年"><a href="#第一纪元-515年" class="headerlink" title="第一纪元 515年"></a>第一纪元 515年</h1><p>吸取了<strong>格劳龙</strong>的教训，<strong>魔苟斯</strong>开始秘密培育新一代的巨龙，其中最强大的那只被取名为<strong>“安卡拉刚”</strong>。</p>
<h1 id="第一纪元-525年"><a href="#第一纪元-525年" class="headerlink" title="第一纪元 525年"></a>第一纪元 525年</h1><p><strong>埃雅仁迪尔</strong>与<strong>埃尔汶</strong>成婚。与此同时<strong>费诺众子</strong>仍未放弃寻找<strong>精灵宝钻</strong>，他们得知宝钻在<strong>西瑞安河口</strong>的<strong>埃尔汶</strong>那里。</p>
<h1 id="第一纪元-532年"><a href="#第一纪元-532年" class="headerlink" title="第一纪元 532年"></a>第一纪元 532年</h1><p><strong>埃雅仁迪尔</strong>与<strong>埃尔汶</strong>的双胞胎儿子<strong>埃尔隆德</strong>和<strong>埃尔洛斯</strong>出生。</p>
<h1 id="第一纪元-534年"><a href="#第一纪元-534年" class="headerlink" title="第一纪元 534年"></a>第一纪元 534年</h1><p><strong>埃雅仁迪尔</strong>驾船出航，希望能打通前往<strong>维林诺</strong>的航道。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/db1dcf615d5d49fd93bf85f31ef81087?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>埃雅仁迪尔的远航</p>
<h1 id="第一纪元-538年"><a href="#第一纪元-538年" class="headerlink" title="第一纪元 538年"></a>第一纪元 538年</h1><p>在写信给<strong>埃尔汶</strong>多次讨要<strong>精灵宝钻</strong>未果后，<strong>费诺众子</strong>已经被之前的誓言折磨得太久，几乎全都有些神经质了，他们不顾手下们的反对执意要进攻<strong>西瑞安河口</strong>，发起<strong>“第二次亲族残杀”</strong>。这一举动导致许多原先他们的部下纷纷倒戈，转而帮助<strong>埃尔汶</strong>她们。</p>
<p>虽然最后还是<strong>费诺众子</strong>赢得了胜利，<strong>埃尔汶</strong>的双胞胎儿子<strong>埃尔隆德</strong>和<strong>埃尔洛斯</strong>也被掳走，但是他们还是没能得到<strong>精灵宝钻</strong>。最后一刻<strong>埃尔汶</strong>带着宝钻投海自尽，却被海洋之神<strong>乌欧牟</strong>救起，送到了正在海中航行的<strong>埃雅仁迪尔</strong>身边。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/4d97ca51851043068d635caa2deb0bc8?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>埃尔汶</p>
<h1 id="第一纪元-540年"><a href="#第一纪元-540年" class="headerlink" title="第一纪元 540年"></a>第一纪元 540年</h1><p>整个<strong>贝烈瑞安德</strong>被费诺众子搅得一团乱，<strong>纳国斯隆德</strong>也独木难支，很快被<strong>魔苟斯</strong>攻陷。至此，<strong>贝烈瑞安德</strong>全境都归<strong>魔苟斯</strong>所有。</p>
<h1 id="第一纪元-542年"><a href="#第一纪元-542年" class="headerlink" title="第一纪元 542年"></a>第一纪元 542年</h1><p>借助<strong>精灵宝钻</strong>的力量，<strong>埃雅仁迪尔</strong>和<strong>埃尔汶</strong>抵达<strong>维林诺</strong>，他们祈求众神拯救中土大陆。由于<strong>埃雅仁迪尔</strong>作为人类和遭到流放的诺多精灵的后代，被禁止踏上<strong>维林诺</strong>的土地，不过曼威决定给他们夫妇一个选择的机会，成为人类还是精灵，不论选择如何，他们俩都不能再回到中土了。</p>
<p><strong>埃雅仁迪尔</strong>追随<strong>埃尔汶</strong>的意愿选择成为<strong>精灵</strong>，<strong>埃雅仁迪尔</strong>驾驶的船只也被<strong>曼威</strong>封圣，成为能在苍穹之中航行的圣船。<strong>埃雅仁迪尔</strong>将那颗<strong>精灵宝钻</strong>带在额前，成为阿尔达的<strong>“大希望之星”</strong>，在夜空中巡游。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/e802d998a5c14965a12f526cd0af63af?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>埃雅仁迪尔</p>
<h1 id="第一纪元-545-587年"><a href="#第一纪元-545-587年" class="headerlink" title="第一纪元 545-587年"></a>第一纪元 545-587年</h1><p><strong>众神</strong>带领留在<strong>阿门洲</strong>的<strong>凡雅族精灵</strong>和<strong>诺多族精灵</strong>向中土进发讨伐魔苟斯，<strong>“愤怒之战”</strong>开始，维拉决定先解放<strong>贝烈瑞安德</strong>被<strong>魔苟斯</strong>军队屠戮的地方，整场战争持续了相当长的时间。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/abeaede911bf4035a8e40ee26b9203ee?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>愤怒之战</p>
<p><strong>矮人</strong>、<strong>伊甸人</strong>、残余的<strong>诺多精灵</strong>和<strong>辛达精灵</strong>都参与了进来，<strong>魔苟斯</strong>一边也同样全面动员，就连东方的蛮夷部落也被祂拉来参战。经过数次会战，联军终于把战线重新推回到<strong>安格班</strong>附近。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/f7d53836a4cb4179aca0bd445007f08d?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>黑龙安卡拉刚</p>
<p><strong>魔苟斯</strong>此时放出了他最后的王牌，<strong>黑龙安卡拉刚</strong>和它带领的恶龙军团。安卡拉刚的翅膀遮天蔽日，吐出的龙焰连维拉们都无法抵挡。但没想到<strong>魔苟斯</strong>手中的这张王牌首战即终战，<strong>“大希望之星”埃雅仁迪尔</strong>驾船前来，在<strong>巨鹰之王**</strong>梭隆<strong>**多</strong>的协助下与<strong>安卡拉刚</strong>和它的巨龙们交战，整个战斗持续了整整一个昼夜，<strong>埃雅仁迪尔</strong>最终利用宝钻的力量将<strong>安卡拉刚</strong>击杀，巨龙的身躯从天空陨落，甚至撞毁了三座山峰。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/3be31404e3cc448fb1df9949f7118302?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>愤怒之战</p>
<p>这场战争造成了严重的后果，整个<strong>贝烈瑞安德</strong>地区遭到致命的破坏，大陆架不再能承受陆地的重量，慢慢沉入了大海。被大海淹没的<strong>贝烈瑞安德</strong>地区相当广大，几乎将原本中土的面积减少了一半。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/af7d93b28ac246568bf246f819cafc0f?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>蓝色为沉没的贝烈瑞安德地区</p>
<p>随着战争的失败，<strong>魔苟斯</strong>的爪牙也四散奔逃，部分残存的<strong>巨魔</strong>、<strong>兽人</strong>和<strong>妖狼</strong>躲进了北方的山脉，<strong>索伦</strong>原本打算向<strong>曼威</strong>忏悔，但得知众神要求祂回到<strong>维林诺</strong>受审，<strong>索伦</strong>立即改换形象去往东方的土地躲藏了起来，据说还有一小部分炎魔也逃脱了</p>
<p><strong>魔苟斯</strong>被<strong>维拉</strong>擒获，等待审判，其冠冕上还留着的两颗<strong>精灵宝钻</strong>被取下。</p>
<p>但是<strong>费诺众子</strong>中还仅剩的两位：<strong>迈兹洛斯</strong>和<strong>玛格洛尔</strong>仍旧被当年的誓言驱使着，居然袭击了看守的精灵，盗走两颗<strong>精灵宝钻</strong>。多年以来犯下的恶行令<strong>精灵宝钻</strong>不停灼烧他们的双手，绝望之下，<strong>迈兹洛斯</strong>带着一枚跳入了熔岩，<strong>玛格洛尔</strong>则将另一枚投入了大海，带着悔恨活在永世的痛苦之中。</p>
<h1 id="第一纪元-590年"><a href="#第一纪元-590年" class="headerlink" title="第一纪元 590年"></a>第一纪元 590年</h1><p>经过审判，<strong>魔苟斯</strong>被关押在虚空之境，但是据<strong>曼督斯</strong>的预言，魔王将会在末日之战时再次归来（关于这一点作者<strong>托尔金</strong>没有明确回应过，也没有把末日之战的内容补齐，不知道将来会不会有人接续这个故事）。</p>
<p>维拉们宽恕了出走的<strong>诺多族精灵</strong>，准许他们返回维林诺，但还是有少数决定留下，比如<strong>凯兰崔尔</strong>和她的丈夫<strong>凯勒博恩</strong>，<strong>至高王吉尔-加拉德</strong>，<strong>“造船者”奇尔丹</strong>以及费诺的孙子<strong>凯勒布林博</strong>。</p>
<p>（<strong>凯兰崔尔</strong>留在中土的原因比较复杂，和她好强的个性不无关系。）</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/1e28110f130946bb805eb0351688c826?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>凯兰崔尔</p>
<p>由于<strong>埃雅仁迪尔</strong>在对抗<strong>安卡拉刚</strong>时的伟大功绩，他与埃尔汶的两个儿子<strong>埃尔隆德</strong>和<strong>埃尔洛斯</strong>也得到了<strong>曼威</strong>赐予的选择权，他们可以在人类和精灵的命运中二选一。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/de3e4c1ddba1417883cae7604f8e75cc?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>埃尔隆德和埃尔洛斯</p>
<p><strong>埃尔隆德</strong>选择了精灵的命运，但他没有选择去往<strong>维林诺</strong>，而是继续留在中土；而<strong>埃尔洛斯</strong>则选择成为人类，他身上集合了<strong>贝奥</strong>、<strong>哈烈**</strong>丝<strong>和</strong>哈多<strong>三大</strong>伊甸人<strong>家族的血脉，</strong>埃尔洛斯<strong>当仁不让地成为了</strong>伊甸人**的统领。</p>
<p>为了奖励<strong>伊甸人</strong>在<strong>“愤怒之战”</strong>中的贡献，<strong>维拉们</strong>在大海中央升起了一座岛屿，准许这些<strong>伊甸人</strong>在岛上居住，并且赐予他们远超其他人类的寿命，那个岛屿叫做<strong>努曼诺尔岛</strong>，他们也自称为<strong>努曼诺尔人</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/bb0cc44fb4ab4e5b9305d55b11258b32?from=pc" alt="史上最完整的《指环王》上古时期大事记（编年体长文）"></p>
<p>努曼诺尔岛</p>
<p>第一纪元终结。</p>
<p>好了，关于《指环王》上古时期（精灵宝钻）的时间线整理就到这里，原创不易，如果您对这方面内容感兴趣的话，点击关注我吧！感谢您的阅读！</p>
]]></content>
      <categories>
        <category>兴趣爱好</category>
        <category>指环王</category>
      </categories>
      <tags>
        <tag>兴趣爱好</tag>
        <tag>指环王</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part1：虚拟机的基础--Lua基本数据结构、栈和基于栈的C函数调用的设计与实现</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part1/</url>
    <content><![CDATA[<p><a href="http://manistein.club/post/program/let-us-build-a-lua-interpreter/%E6%9E%84%E5%BB%BAlua%E8%A7%A3%E9%87%8A%E5%99%A8part1/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>   从本篇开始，我们正式进入到Lua解释器的开发阶段（这是一个遵循Lua5.3标准的项目）。本篇并不直接接入到设计和实现语法分析器和词法分析器的阶段，而是先设计和实现Lua虚拟机的基础数据结构（包括Lua最基本的数据结构，如基本数据类型，表示虚拟机状态的global_State和lua_State结构，在函数调用中扮演重要角色的CallInfo结构等）以及设计和实现基于栈的C函数调用流程。这些都是理解后面虚拟机运作的基础。由于这是一个仿制项目，为了和官方版本做区分，就称之为dummylua，后面要称呼本项目时，一律用dummylua来表示。</p>
<p>   本篇将分为几个部分：首先介绍工程目录结构的组织，以及为什么要这样组织，每个目录分别包含哪些文件，这些文件分别包含哪些内容；其次是着手进行Lua基本类型的设计与实现，实现表示Lua虚拟机状态的global_State和lua_State结构，以及用于函数调用的CallInfo结构；接着是设计我们在使用层，要实现C函数，在栈中调用流程的api，并且构建从虚拟机状态初始化到函数完成调用的逻辑图，阐述这个过程；最后通过编写代码，将所有的流程实现。第一部分的功能已经完成开发和测试（ubuntu、mac和windows平台），获取源码可以点击这里，欢迎Star。</p>
<h1 id="目录结构"><a href="#目录结构" class="headerlink" title="目录结构"></a>目录结构</h1><p>   在开始介绍项目的目录结构之前，我们不妨先回顾以下Lua运作的两种基本模式。一种是创建Lua虚拟机，直接加载脚本并且直接运行，其遵循如下流程：</p>
<ul>
<li>创建Lua虚拟机状态实例</li>
<li>加载标准库</li>
<li>加载脚本，通过词法分析器lexer和语法分析器parser将脚本编译成Lua虚拟机能够识别的opcodes，并存储在虚拟机状态实例中</li>
<li><p>运行虚拟机，对虚拟机状态实例中的opcodes进行执行<br>还有一种则是，预先将脚本编译，然后将内存中的指令信息，dump到文件中，以bytecode的形式存在，以后要运行的时候，直接加载dump文件的bytecode并且直接运行：</p>
</li>
<li><p>创建Lua虚拟机状态实例</p>
</li>
<li>加载标准库</li>
<li>加载脚本，通过词法分析器lexer和语法分析器parser将脚本编译成Lua虚拟机能够识别的opcodes，并存储在虚拟机状态实例中</li>
<li>将虚拟机指令dump成二进制文件，以bytecode的形式保存</li>
<li>在将来某个时刻，运行Lua虚拟机，并加载dump后的文件，直接通过dump数据，将指令结构还原回来，保存在虚拟机状态实例中</li>
<li><p>运行虚拟机，对虚拟机状态实例中的opcodes进行执行<br>这两种方式，前者从虚拟机创建到加载脚本，再到运行一气呵成。后者需要预先将Lua脚本编译成bytecode，然后要使用的时候再加载运行，运行时省去了编译流程，比前者更快。不过现在Lua直接加载脚本并运行已经足够快了，除非对性能有极其苛刻的要求，否则前者已经能够满足我们的日常需要了。下面引用一张《The Lua Architecture》[1]中的一张图，来展示一下后面一种方式的流程。<br><img src="initializing Lua and loading a script file.png" alt><br>从上图，我们可以了解到一个流程，就是我们要运行Lua脚本，首先要创建Lua解释器（由于Lua是采用纯C来写的工程，因此函数和数据是分离的，这里其实也只是创建一个Lua虚拟机状态实例，也就是后面我们要介绍的lua_State结构和global_State结构），然后通过编译器（lexer和parser）将脚本编译成虚拟机能够识别的指令（opcodes），再交给虚拟机执行。因此，我们可以将编译和运行分割开来，他们共同使用的部分也单独抽离出来，于是我们的目录结构可以按如下所示的方式组织：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">+ 3rd/     d         <span class="comment">#引用的第三方库均放置在这里</span></span><br><span class="line">+ bin/              <span class="comment">#编译生成的二进制文件放置在这里</span></span><br><span class="line">+ clib/             <span class="comment">#外部要在c层使用Lua的C API，那么只能调用clib里提供的接口，而不能调用其他内部接口</span></span><br><span class="line">+ common/           <span class="comment">#vm和compiler共同使用的结构、接口均放置在这里</span></span><br><span class="line">+ compiler/         <span class="comment">#编译器相关的部分放置在这里</span></span><br><span class="line">+ <span class="built_in">test</span>/             <span class="comment">#测试用例全部放置在这里</span></span><br><span class="line">+ vm/               <span class="comment">#虚拟机相关的部分放置在这里</span></span><br><span class="line">+ main.c</span><br><span class="line">+ makefile</span><br></pre></td></tr></table></figure>
<p> 我们有理由相信，目录组织也是架构的一部分，上面附上了目录说明，能够清晰说明他们的分类和作用。我想构建的逻辑层次图如下所示<br><img src="folder_architecture.png" alt><br>3rd和common作为vm和compiler的基础模块而存在，外部在使用c接口的时候，只能通过clib里的辅助库来进行，以隐藏不必要暴露的细节。在定下目录结构以后，接下来将展示不同的文件分别有哪些文件。</p>
<pre><code>目录组织完成以后，接下来确定有哪些文件了，我将文件内容展示到下面部分：
</code></pre><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">+ 3rd/              <span class="comment">#引用的第三方库均放置在这里</span></span><br><span class="line">+ bin/              <span class="comment">#编译生成的二进制文件放置在这里</span></span><br><span class="line">~ clib/             <span class="comment">#外部要在c层使用Lua的C API，那么只能调用clib里提供的接口，而不能调用其他内部接口</span></span><br><span class="line">  luaaux.h              <span class="comment">#供外部使用的辅助库</span></span><br><span class="line">  luaaux.c</span><br><span class="line">~ common/           <span class="comment">#vm和compiler共同使用的结构、接口均放置在这里</span></span><br><span class="line">  lua.h                 <span class="comment">#提供lua基本类型的定义，错误码定义，全项目都可能用到的宏均会放置在这里</span></span><br><span class="line">  luamem.h              <span class="comment">#lua内存分配器</span></span><br><span class="line">  luamem.c</span><br><span class="line">  luaobject.h           <span class="comment">#lua基本类型</span></span><br><span class="line">  luaobject.c</span><br><span class="line">  luastate.h            <span class="comment">#虚拟机状态结构，以及对其相关操作的接口均放置于此</span></span><br><span class="line">  luastate.c</span><br><span class="line">+ compiler/         <span class="comment">#编译器相关的部分放置在这里</span></span><br><span class="line">+ <span class="built_in">test</span>/             <span class="comment">#测试用例全部放置在这里</span></span><br><span class="line">~ vm/               <span class="comment">#虚拟机相关的部分放置在这里</span></span><br><span class="line">  luado.h               <span class="comment">#函数调用相关的接口均放置于此</span></span><br><span class="line">  luado.c</span><br><span class="line">  main.c</span><br><span class="line">  makefile</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>上面展示了我们本部分要实现的部分，后续开发会陆续添加新的文件，后面章节也会陆续引用这个片段。到现在为止，我们的目录结构就介绍完了，后面将介绍基本数据结构。</p>
<h1 id="基本数据结构"><a href="#基本数据结构" class="headerlink" title="基本数据结构"></a>基本数据结构</h1><h2 id="基本类型"><a href="#基本类型" class="headerlink" title="基本类型"></a>基本类型</h2><p>   Lua的基本类型，包括lua_Integer、lua_Number、lu_byte、lua_CFunction等，当然最典型的的则是其能够代表任何基本类型的TValue结构了。现在我们将逐一实现这些类型。<br>   首先我们要实现两个宏，LUA_INTEGER和LUA_NUMBER在common/lua.h里<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/lua.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> lua_h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> lua_h </span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">if</span> defined(LLONG_MAX) </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_INTEGER long long</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_NUMBER double</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_INTEGER int </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_NUMBER float </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span> </span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure></p>
<p>然后在common/luaobject.h中加入下面几行代码：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luaobject.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> luaobject_h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> luaobject_h </span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"lua.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> LUA_INTEGER lua_Integer;</span><br><span class="line"><span class="keyword">typedef</span> LUA_NUMBER lua_Number;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure></p>
<p>此时我们的lua_Integer和lua_Number类型就定义完了，这里要先在common/lua.h中定义LUA_INTEGER的目的是让LUA_INTEGER适配32bit和64bit两种编译环境的逻辑保留在lua.h中。适配的目的也是为了后面的Value结构的各个字段能够完美对齐。官方实现版本，甚至对编译器类型做了适配，这里我们只考虑32bit和64bit的情况。</p>
<pre><code>接下来我们要定义lu_byte和lua_CFunction两种基本类型，他们的定义如下所示：
</code></pre><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luaobject.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> luaobject_h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> luaobject_h </span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"lua.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span> <span class="title">lua_State</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> LUA_INTEGER lua_Integer;</span><br><span class="line"><span class="keyword">typedef</span> LUA_NUMBER lua_Number;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">char</span> lu_byte;</span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">int</span> <span class="params">(*lua_CFunction)</span><span class="params">(lua_State* L)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<p>lu_byte是个unsigned char类型，官方将其取名作lu_byte也许是表示lua unsigned byte的意思吧。而lua_CFunction基本上就是Lua栈中，能被调用的light c function的形式了。<br>   在完成了最基本类型的定义后，现在要来定义Lua的通用数据类型Value和TValue了，我们将这两个数据结构定义在common/luaobject.h中：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luaobject.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> luaobject_h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> luaobject_h </span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"lua.h"</span></span></span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> lua_Value &#123;</span><br><span class="line">    <span class="keyword">void</span>* p;            <span class="comment">// light userdata</span></span><br><span class="line">    <span class="keyword">int</span> b;              <span class="comment">// boolean: 1 = true, 0 = false</span></span><br><span class="line">    lua_Integer i;</span><br><span class="line">    lua_Number n;</span><br><span class="line">    lua_CFunction f;</span><br><span class="line">&#125; Value;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">lua_TValue</span> &#123;</span></span><br><span class="line">    Value value_;</span><br><span class="line">    <span class="keyword">int</span> tt_;</span><br><span class="line">&#125; TValue;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure></p>
<p>Value是一个union类型，以上5种类型在32bit环境下，共用4个字节的内存，在64bit环境下共用8个字节的内存，这样做的目的是为了节约内存，p指针是用来存放light userdata的，这种值在官方Lua中，需要我们自行管理内存，由于目前我们没有实现gc，因此所有的自定义类型被创建出来后，均放在p中。TValue包含了Value类型的值value_，以及使用一个int变量tt_表示其类型。我们可以使用TValue来表示任何Lua对象。此外TValue的类型定义在common/lua.h中，如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/lua.h </span></span><br><span class="line"></span><br><span class="line"><span class="comment">// basic object type</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TNUMBER 1</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TLIGHTUSERDATA 2</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TBOOLEAN 3</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TSTRING 4</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TNIL 5</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TTABLE 6</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TFUNCTION 7</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TTHREAD 8</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TNONE 9</span></span><br></pre></td></tr></table></figure></p>
<p>由于名称都很直观，这里就不加注释了。上面阐述的还是一般类型，其中LUA_TNUMBER、LUA_TSTRING和LUA_TFUNCTION还可以细分，我将细分的类型在common/luaobject.h里定义，放在这里更加贴近使用它的接口：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luaobject.h</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// lua number type </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_NUMINT (LUA_TNUMBER | (0 &lt;&lt; 4))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_NUMFLT (LUA_TNUMBER | (1 &lt;&lt; 4))</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// lua function type </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TLCL (LUA_TFUNCTION | (0 &lt;&lt; 4))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TLCF (LUA_TFUNCTION | (1 &lt;&lt; 4))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TCCL (LUA_TFUNCTION | (2 &lt;&lt; 4))</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// string type </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_LNGSTR (LUA_TSTRING | (0 &lt;&lt; 4))</span></span><br></pre></td></tr></table></figure></p>
<p>#define LUA_SHRSTR (LUA_TSTRING | (1 &lt;&lt; 4))<br>可以观察到，Lua数值的大类型定义的值在1~8之间，也就是00012~10002之间，那么小类型不能占用低四位，只能往高位作文章，因此他们分别的含义为：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">LUA_NUMINT &lt;=&gt; LUA_TNUMBER | (<span class="number">0</span> &lt;&lt; <span class="number">4</span>) &lt;=&gt; <span class="number">00012</span> | <span class="number">00002</span> &lt;=&gt; <span class="number">00012</span> &lt;=&gt; <span class="number">1</span></span><br><span class="line">LUA_NUMFLT &lt;=&gt; LUA_TNUMBER | (<span class="number">1</span> &lt;&lt; <span class="number">4</span>) &lt;=&gt; <span class="number">00012</span> | <span class="number">100002</span> &lt;=&gt; <span class="number">100012</span> &lt;=&gt; <span class="number">17</span></span><br><span class="line">LUA_TLCL &lt;=&gt; LUA_TFUNCTION | (<span class="number">0</span> &lt;&lt; <span class="number">4</span>) &lt;=&gt; <span class="number">01112</span> | <span class="number">00002</span> &lt;=&gt; <span class="number">01112</span> &lt;=&gt; <span class="number">7</span></span><br><span class="line">LUA_TLCF &lt;=&gt; LUA_TFUNCTION | (<span class="number">1</span> &lt;&lt; <span class="number">4</span>) &lt;=&gt; <span class="number">01112</span> | <span class="number">100002</span> &lt;=&gt; <span class="number">101112</span> &lt;=&gt; <span class="number">23</span></span><br><span class="line">LUA_TCCL &lt;=&gt; LUA_TFUNCTION | (<span class="number">2</span> &lt;&lt; <span class="number">4</span>) &lt;=&gt; <span class="number">01112</span> | <span class="number">1000002</span> &lt;=&gt; <span class="number">1001112</span> &lt;=&gt; <span class="number">39</span></span><br><span class="line">LUA_LNGSTR &lt;=&gt; LUA_TSTRING | (<span class="number">0</span> &lt;&lt; <span class="number">4</span>) &lt;=&gt; <span class="number">01002</span> | <span class="number">00002</span> &lt;=&gt; <span class="number">01002</span> &lt;=&gt; <span class="number">4</span></span><br><span class="line">LUA_SHRSTR &lt;=&gt; LUA_TSTRING | (<span class="number">1</span> &lt;&lt; <span class="number">4</span>) &lt;=&gt; <span class="number">01002</span> | <span class="number">100002</span> &lt;=&gt; <span class="number">101002</span> &lt;=&gt; <span class="number">20</span></span><br></pre></td></tr></table></figure></p>
<p>这些类型值，将被存储在TValue的tt_变量中。到目前为止我们已经介绍完了Lua基本数据类型。接下来我们将定义lua_State和global_State。</p>
<h2 id="lua-State、CallInfo和global-State"><a href="#lua-State、CallInfo和global-State" class="headerlink" title="lua_State、CallInfo和global_State"></a>lua_State、CallInfo和global_State</h2><p>   在介绍完TValue数据结构以后，接下来要介绍和虚拟机息息相关的虚拟机状态数据结构lua_State和global_State。如果说，虚拟机指令要在栈上运行，那么这个栈就是保存在lua_State这个结构体中。同时我们的CallInfo结构，用于标记函数在栈中的位置，标记调用函数时，它的栈顶位于lua_State栈中的哪个位置，同时它还保存要返回多少个返回值的标记。而global_State则是包含了lua_State和一个内存分配器等，透过global_State来管理内存和lua_State是非常方便的事情，在官方的Lua版本中，我们还需要透过它来管理gc。现在我们来定义这些个结构体，将其定义在common/luastate.h中：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luastate.h</span></span><br><span class="line"><span class="keyword">typedef</span> TValue* StkId;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span> &#123;</span></span><br><span class="line">    StkId func;                     <span class="comment">// 被调用函数在栈中的位置</span></span><br><span class="line">    StkId top;                      <span class="comment">// 被调用函数的栈顶位置</span></span><br><span class="line">    <span class="keyword">int</span> nresult;                    <span class="comment">// 有多少个返回值</span></span><br><span class="line">    <span class="keyword">int</span> callstatus;                 <span class="comment">// 调用状态</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">next</span>;</span>          <span class="comment">// 下一个调用</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">previous</span>;</span>      <span class="comment">// 上一个调用</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span> &#123;</span></span><br><span class="line">    StkId <span class="built_in">stack</span>;                    <span class="comment">// 栈</span></span><br><span class="line">    StkId stack_last;               <span class="comment">// 从这里开始，栈不能被使用</span></span><br><span class="line">    StkId top;                      <span class="comment">// 栈顶，调用函数时动态改变</span></span><br><span class="line">    <span class="keyword">int</span> stack_size;                 <span class="comment">// 栈的整体大小</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_longjmp</span>* <span class="title">errorjmp</span>;</span>   <span class="comment">// 保护模式中，要用到的结构，当异常抛出时，跳出逻辑</span></span><br><span class="line">    <span class="keyword">int</span> status;                     <span class="comment">// lua_State的状态</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">next</span>;</span>         <span class="comment">// 下一个lua_State，通常创建协程时会产生</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">previous</span>;</span>     </span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span> <span class="title">base_ci</span>;</span>        <span class="comment">// 和lua_State生命周期一致的函数调用信息</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">ci</span>;</span>            <span class="comment">// 当前运作的CallInfo</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">l_G</span>;</span>       <span class="comment">// global_State指针</span></span><br><span class="line">    <span class="keyword">ptrdiff_t</span> errorfunc;            <span class="comment">// 错误函数位于栈的哪个位置</span></span><br><span class="line">    <span class="keyword">int</span> ncalls;                     <span class="comment">// 进行多少次函数调用</span></span><br><span class="line">&#125; lua_State;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">mainthread</span>;</span>   <span class="comment">// 我们的lua_State其实是lua thread，某种程度上来说，它也是协程</span></span><br><span class="line">    lua_Alloc frealloc;             <span class="comment">// 一个可以自定义的内存分配函数</span></span><br><span class="line">    <span class="keyword">void</span>* ud;                       <span class="comment">// 当我们自定义内存分配器时，可能要用到这个结构，但是我们用官方默认的版本</span></span><br><span class="line">                                    <span class="comment">// 因此它始终是NULL</span></span><br><span class="line">    lua_CFunction panic;            <span class="comment">// 当调用LUA_THROW接口时，如果当前不处于保护模式，那么会直接调用panic函数</span></span><br><span class="line">                                    <span class="comment">// panic函数通常是输出一些关键日志</span></span><br><span class="line">&#125; global_State;</span><br></pre></td></tr></table></figure></p>
<p>我将结构的说明，直接写到了注释里，这些注释能够对结构进行很好的解释和说明了。现在我们也完成了CallInfo、lua_State和global_State的定义了，接下来进入下一个阶段。</p>
<h1 id="函数调用流程"><a href="#函数调用流程" class="headerlink" title="函数调用流程"></a>函数调用流程</h1><p>   到目前为止，我们已经完成了Lua基本数据结构的定义，本篇的目标除了定义这些数据结构，对其中的字段加以说明之外，还将设计和实现c函数在lua_State栈中的调用。在开始实现具体细节之前，我们不妨从应用程序调用的视角来观察这个流程。我们以如下的代码为例子：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// main.c</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"clib/luaaux.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">add_op</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> left = luaL_tointeger(L, <span class="number">-2</span>);</span><br><span class="line">    <span class="keyword">int</span> right = luaL_tointeger(L, <span class="number">-1</span>);</span><br><span class="line"></span><br><span class="line">    luaL_pushinteger(L, left + right);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">L</span> = <span class="title">luaL_newstate</span>();</span>              <span class="comment">// 创建虚拟机状态实例</span></span><br><span class="line">    luaL_pushcfunction(L, &amp;add_op);                     <span class="comment">// 将要被调用的函数add_op入栈</span></span><br><span class="line">    luaL_pushinteger(L, <span class="number">1</span>);                             <span class="comment">// 参数入栈</span></span><br><span class="line">    luaL_pushinteger(L, <span class="number">1</span>);</span><br><span class="line">    luaL_pcall(L, <span class="number">2</span>, <span class="number">1</span>);                                <span class="comment">// 调用add_op函数，并将结果push到栈中</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> result = luaL_tointeger(L, <span class="number">-1</span>);                 <span class="comment">// 完成函数调用，栈顶就是add_op放入的结果</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"result is %d\n"</span>, result);</span><br><span class="line">    luaL_pop(L);                                        <span class="comment">// 结果出栈，保证栈的正确性</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"final stack size %d\n"</span>, luaL_stacksize(L));</span><br><span class="line"></span><br><span class="line">    luaL_close(L);                                      <span class="comment">// 销毁虚拟机状态实例</span></span><br><span class="line"></span><br><span class="line">    system(<span class="string">"pause"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>正如我们之前设计的那样，要使用dummylua的接口，只能使用clib库里的luaaux.h里定义的接口，这些接口的开头一律以luaL<em>表示，L指代Lib（官方能调用的接口并非均是以luaL</em>开头的，这点需要强调一下）。上面一段代码，我们先创建了一个lua_State实例，然后push了要调用的函数和参数，最终调用了这个函数，这个函数最终将结果入栈，并返回给调用者。在探索代码细节之前，我先绘制几张逻辑图，来描述这个过程，以更好地让大家理解这个逻辑调用过程。</p>
<p>首先我们调用了luaL_newstate接口，用于创建和初始化lua_State实例，这个接口不仅仅会创建lua_State实例，也会创建一个global_State实例，并且初始化它们，global_State对外部是不可见的，它是Lua虚拟机内部使用的一个结构，在完成函数调用以后，我们可以得到图3的结构：<br><img src="luaL_newstate.png" alt><br>我们可以看到，global_State实例也被一起创建了，并且将mainthread指针指向了lua_State实例，lua_State默认就有一个CallInfo类型的变量base_ci，这个CallInfo的func和top指针，指明了一个被调用的函数所能使用的栈的范围，每当我们调用一个新的函数时，会从lua_State的stack中，取出一段作为该函数的栈空间，开始位置是CallInfo类型变量func指针的下一个位置，而能被使用的栈空间限制一般是20个（官方版本由LUA_MINSTACK这个宏指定）。此外lua_State的stack指针，指向了栈起始的位置，而从stack_last开始，包括后面的EXTRA_SPACE则是不能被使用的空间。lua_State中的top指针，则是在函数调用的过程中，动态改变的，它标记着正在被调用函数的栈顶位置。另外还要强调的一点，则是CallInfo的func指针，实质上是指向一个函数实例（base_ci除外），而这个被func指针指向的位置，是不能作为栈顶存在的，也就是说，当前函数在被调用时，如果它是空栈，那么lua_State的top指针将落在L-&gt;ci-&gt;func的上一格位置。正如上图所示，现在它是一个空栈。</p>
<p>   在完成虚拟机状态实例创建以后，我们需要往lua_State的栈中，push我们希望被调用的函数，这个函数在我们使用的例子中，就是add_op，我们通过一张图展示，在luaL_pushcfunction执行完以后的状态：<br><img src="luaL_pushcfunction.png" alt><br>目前看起来平平无奇，接下来将两个参数入栈，于是得到函数调用前的栈状态：<br><img src="luaL_pushinteger.png" alt><br>在完成参数入栈以后，我们就需要开始调用add_op这个函数了，调用它的方式是执行luaL_pcall(L, 2, 1)这行代码，这两个参数分别表示，我们传入两个参数，并且期待一个返回值。函数调用的过程稍微有点复杂，但是相对于后面涉及到的虚拟机运作，还算是小巫见大巫。要开始函数调用，首先要构建新的CallInfo实例，它会指定add_op运行时，该函数栈可以使用的范围同时也包含，这个函数被期待返回多少个函数，于是我们可以得到如图6所示的状态：<br><img src="create_call_info.png" alt><br>从图中我们可以看到，新创建的CallInfo对象归属于add_op这个函数，CallInfo的func指针指向了add_op这个函数的位置，同时新创建的CallInfo对象的func和top指针限定了add_op能够使用的栈空间的范围。同时我们也可以观察到lua_State数据实例的ci指针指向了新创建的CallInfo实例，函数调用结束后，它会指向上一个CallInfo数据实例，ci指针本质就是用来标记当前调用的是哪个函数。在完成CallInfo创建以后，就可以开始调用add_op这个函数了。在开始调用之前，我们先来看看栈的索引，我们可以观察到如图7所示，当索引为正时，add_op函数栈的栈底从1开始，并且朝栈顶递增，如果索引为负数时，add_op函数栈的栈顶从-1开始，并且朝栈底递减。<br><img src="stack_index.png" alt><br>在了解了栈索引的操作以后，add_op内部的操作也就清晰明了了，就是把两个参数取出相加以后入栈（add_op最后的return 1，则表示add_op实际上有一个返回值）于是得到如图8所示的状态：<br><img src="call_add_op.png" alt><br>现在进入到add_op函数的最后调用阶段，就是销毁当前的CallInfo实例，并且将返回值移到add_op的位置（大家可以尝试推导一下多个返回值的情况），于是得到图9的状态<br><a href="ajust_result.png"></a><br>现在控制权又回到了main函数了，main函数直接将栈顶变量取出打印，然后是把add_op的返回值出栈，于是我们就完成了完整的函数调用了。</p>
<h1 id="函数调用实现"><a href="#函数调用实现" class="headerlink" title="函数调用实现"></a>函数调用实现</h1><h2 id="创建虚拟机实例"><a href="#创建虚拟机实例" class="headerlink" title="创建虚拟机实例"></a>创建虚拟机实例</h2><p>   在完成展现逻辑图以后，我们现在开始着手实现具体的逻辑。本节将逐步设计和实现上节引用例子的各个步骤，首先我们要实现的是luaL_newstate接口，这个接口我定义在clib/luaaux.h clib/luaaux.c中，它的定义和实现如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// clib/luaaux.h</span></span><br><span class="line"></span><br><span class="line"><span class="function">struct lua_State* <span class="title">luaL_newstate</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">// clib/luaaux.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="title">l_alloc</span><span class="params">(<span class="keyword">void</span>* ud, <span class="keyword">void</span>* ptr, <span class="keyword">size_t</span> osize, <span class="keyword">size_t</span> nsize)</span> </span>&#123;</span><br><span class="line">    (<span class="keyword">void</span>)ud;</span><br><span class="line">    (<span class="keyword">void</span>)osize;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// printf("l_alloc nsize:%ld\n", nsize);</span></span><br><span class="line">    <span class="keyword">if</span> (nsize == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">free</span>(ptr);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">realloc</span>(ptr, nsize);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">struct lua_State* <span class="title">luaL_newstate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">L</span> = <span class="title">lua_newstate</span>(&amp;<span class="title">l_alloc</span>, <span class="title">NULL</span>);</span></span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>如上所示，luaL_newstate实际上，是转调了另一个库的接口lua_newstate，luaL_newstate为其指定了一个内存分配函数l_alloc。而我们的lua_newstate函数则定义在common/luastate.h common/luastate.c中，它们的定义是：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luastate.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_EXTRASPACE sizeof(void*) </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> G(L) ((L)-&gt;l_G)</span></span><br><span class="line"></span><br><span class="line"><span class="function">struct lua_State* <span class="title">lua_newstate</span><span class="params">(lua_Alloc alloc, <span class="keyword">void</span>* ud)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// common/luastate.c</span></span><br><span class="line"><span class="function">struct lua_State* <span class="title">lua_newstate</span><span class="params">(lua_Alloc alloc, <span class="keyword">void</span>* ud)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">L</span>;</span></span><br><span class="line">    </span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">LG</span>* <span class="title">lg</span> = (<span class="title">struct</span> <span class="title">LG</span>*)(*<span class="title">alloc</span>)(<span class="title">ud</span>, <span class="title">NULL</span>, <span class="title">LUA_TTHREAD</span>, <span class="title">sizeof</span>(<span class="title">struct</span> <span class="title">LG</span>));</span></span><br><span class="line">    <span class="keyword">if</span> (!lg) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    g = &amp;lg-&gt;g;</span><br><span class="line">    g-&gt;ud = ud;</span><br><span class="line">    g-&gt;frealloc = alloc;</span><br><span class="line">    g-&gt;panic = <span class="literal">NULL</span>;</span><br><span class="line">    </span><br><span class="line">    L = &amp;lg-&gt;l.l;</span><br><span class="line">    G(L) = g;</span><br><span class="line">    g-&gt;mainthread = L;</span><br><span class="line"></span><br><span class="line">    stack_init(L);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>lua_newstate实际上是为global_State和lua_State开辟内存，并完成初始化。这里lua_newstate使用了从luaL_newstate传入的内存分配函数，这个函数的作用和c语言中的realloc类似，但是它规定，当nsize（意为new size）为0时，要将内存释放掉。对于realloc[2]这个函数，使用它非常方便，它的作用是先开辟nsize的内存，将旧的内容拷贝到新的内存块后再释放原来的内存，省去了我们手工处理的逻辑。而开辟内存本身，我们可以注意到，它并不是单独为global_State和lua_State开辟内存，而是使用了一个叫做LG的数据结构：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luastate.c</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LX</span> &#123;</span></span><br><span class="line">    lu_byte extra_[LUA_EXTRASPACE];</span><br><span class="line">    lua_State l;</span><br><span class="line">&#125; LX;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LG</span> &#123;</span></span><br><span class="line">    LX l;</span><br><span class="line">    global_State g;</span><br><span class="line">&#125; LG;</span><br></pre></td></tr></table></figure></p>
<p>这是参照官方版本做的设计，我们可以看到这里整合了一个global_State变量和一个LX变量，LX结构包含了一个lua_State类型的变量和一个LUA_EXTRASPACE大小的lu_byte数组变量extra_，这个变量我在官方版本中没有搜到使用到他的地方，也许是个历史遗留问题。至于为什么要通过一个LG结构，将global_State和lua_State绑定在一起？这里我在云风的《lua源码欣赏》找到这样的解释[3]：</p>
<blockquote>
<p>Lua的实现尽可能的避免内存碎片，同时也减少内存分配和释放的次数。它采用了一个小技巧，利用一个LG结构，把主线程lua_State和global_State分配在一起。</p>
</blockquote>
<p>余下的逻辑就是global_State和lua_State的初始化操作，对于lua_State而言，它还需要进行栈相关的初始化stack_init：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luastate.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">stack_init</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    L-&gt;<span class="built_in">stack</span> = (StkId)luaM_realloc(L, <span class="literal">NULL</span>, <span class="number">0</span>, LUA_STACKSIZE * <span class="keyword">sizeof</span>(TValue));</span><br><span class="line">    L-&gt;stack_size = LUA_STACKSIZE;</span><br><span class="line">    L-&gt;stack_last = L-&gt;<span class="built_in">stack</span> + LUA_STACKSIZE - LUA_EXTRASTACK;</span><br><span class="line">    L-&gt;next = L-&gt;previous = <span class="literal">NULL</span>;</span><br><span class="line">    L-&gt;status = LUA_OK;</span><br><span class="line">    L-&gt;errorjmp = <span class="literal">NULL</span>;</span><br><span class="line">    L-&gt;top = L-&gt;<span class="built_in">stack</span>;</span><br><span class="line">    L-&gt;errorfunc = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; L-&gt;stack_size; i++) &#123;</span><br><span class="line">        setnilvalue(L-&gt;<span class="built_in">stack</span> + i);</span><br><span class="line">    &#125;</span><br><span class="line">    L-&gt;top++;</span><br><span class="line"></span><br><span class="line">    L-&gt;ci = &amp;L-&gt;base_ci;</span><br><span class="line">    L-&gt;ci-&gt;func = L-&gt;<span class="built_in">stack</span>;</span><br><span class="line">    L-&gt;ci-&gt;top = L-&gt;<span class="built_in">stack</span> + LUA_MINSTACK;</span><br><span class="line">    L-&gt;ci-&gt;previous = L-&gt;ci-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里它为lua_State开辟LUA_STACKSIZE（官方定义40）大小的栈，并且设定了L-&gt;top不能访问的区域，即stack_last以及往后的EXTRA_SPACE部分。至于EXTRA_SPACE的作用，我个人的观点是避免爆栈时（L-&gt;top指针超越L-&gt;stack_last），访问了其他内存区域破坏其他内存块，这里被作为一个容错的缓冲区。其他部分则是各个变量的初始化，包括为base_ci赋初值（限定栈范围等）。直接通过调用global_State的frealloc函数来开辟内存，是非常繁琐的，而且还可能遇到内存不足的情况，遇到这种情况为每一个调用内存的地方做NULL指针判定是相当冗余的，于是我在common/luamem.h common/luamem.c中定义了一个luaM_realloc接口，M代表memory：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luamem.h</span></span><br><span class="line"><span class="function"><span class="keyword">void</span>* <span class="title">luaM_realloc</span><span class="params">(struct lua_State* L, <span class="keyword">void</span>* ptr, <span class="keyword">size_t</span> osize, <span class="keyword">size_t</span> nsize)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// luamem.c</span></span><br><span class="line"><span class="function"><span class="keyword">void</span>* <span class="title">luaM_realloc</span><span class="params">(struct lua_State* L, <span class="keyword">void</span>* ptr, <span class="keyword">size_t</span> osize, <span class="keyword">size_t</span> nsize)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    <span class="keyword">int</span> oldsize = ptr ? osize : <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">void</span>* ret = (*g-&gt;frealloc)(g-&gt;ud, ptr, oldsize, nsize);</span><br><span class="line">    <span class="keyword">if</span> (ret == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        luaD_throw(L, LUA_ERRMEM);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个接口只是多做了一个NULL指针判定，当内存申请失败时，调用luaD_throw抛出异常，这个luaD_throw后面介绍以保护模式调用函数时会进行说明。走到这一步我们的luaL_newstate流程也完成了，它的形态就是图3所示的那样。</p>
<h2 id="销毁虚拟机实例"><a href="#销毁虚拟机实例" class="headerlink" title="销毁虚拟机实例"></a>销毁虚拟机实例</h2><p>   在介绍完创建虚拟机状态，那么相应的需要介绍销毁虚拟机状态的接口，这个就是我们要实现的luaL_close函数，和luaL_newstate一样，它定义在clib/luaaux.h clib/luaaux.c中<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// clib/luaaux.h</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaL_close</span><span class="params">(struct lua_State* L)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// clib/luaaux.c</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaL_close</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    lua_close(L);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>它同样是转调common/luastate.h里的接口<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luastate.h</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lua_close</span><span class="params">(struct lua_State* L)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// common/luastate.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> fromstate(L) (cast(LX*, cast(lu_byte*, (L)) - offsetof(LX, l)))</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">free_stack</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    global_State* g = G(L);</span><br><span class="line">    (*g-&gt;frealloc)(g-&gt;ud, L-&gt;<span class="built_in">stack</span>, <span class="keyword">sizeof</span>(TValue), <span class="number">0</span>);</span><br><span class="line">    L-&gt;<span class="built_in">stack</span> = L-&gt;stack_last = L-&gt;top = <span class="literal">NULL</span>;</span><br><span class="line">    L-&gt;stack_size = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lua_close</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">L1</span> = <span class="title">g</span>-&gt;<span class="title">mainthread</span>;</span> <span class="comment">// only mainthread can be close</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// because I have not implement gc, so we should free ci manual </span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">ci</span> = &amp;<span class="title">L1</span>-&gt;<span class="title">base_ci</span>;</span></span><br><span class="line">    <span class="keyword">while</span>(ci-&gt;next) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">next</span> = <span class="title">ci</span>-&gt;<span class="title">next</span>-&gt;<span class="title">next</span>;</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">free_ci</span> = <span class="title">ci</span>-&gt;<span class="title">next</span>;</span></span><br><span class="line"></span><br><span class="line">        (*g-&gt;frealloc)(g-&gt;ud, free_ci, <span class="keyword">sizeof</span>(struct CallInfo), <span class="number">0</span>);</span><br><span class="line">        ci = next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    free_stack(L1);    </span><br><span class="line">    (*g-&gt;frealloc)(g-&gt;ud, fromstate(L1), <span class="keyword">sizeof</span>(LG), <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>整个逻辑很简单，先把CallInfo实例释放掉，然后再把lua_State的stack释放掉，最后把整个LG释放掉（上面已经解释过它是包含global_State和lua_State类型的结构）。这里需要注意的一点，则是宏fromstate，它实际上是通过luaState指针L，通过offsetof函数，找到LG的起始位置，最终将整个LG释放掉，而 offsetof需要我们指定类或结构体中成员的名称，offsetof函数接受两个参数，第一个是类或结构体的名称，第二个是成员变量名，将第一个成员变量名传入我们会获得0。由于fromstate这个宏中的offsetof调用，是将LX的成员l传入，因此offsetof获得值就是sizeof(extra )，对于lua_State L而言，它正好能找到LG的起始地址，因此可以通过这种方式一并将lua_State和global_State释放。</p>
<h2 id="参数入栈"><a href="#参数入栈" class="headerlink" title="参数入栈"></a>参数入栈</h2><p>   参数入栈其实就是将指定类型的值push到栈中，为此我实现了luaL_pushinteger、luaL_pushnumber、luaL_pushlightuserdata、luaL_pushnil、luaL_pushcfunction和luaL_pushboolean，这些函数都在clib/luaaux.h clib/luaaux.c中，他们同样是转调common/luastate.h common/luastate.c的接口，这里就不一一列举他们的实现了。push的操作也很简单，其实就是对L-&gt;top指向的位置赋值，然后L-&gt;top++，因此我们首先要对每种类型都实现一个set函数：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luastate.h</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setivalue</span><span class="params">(StkId target, <span class="keyword">int</span> integer)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setfvalue</span><span class="params">(StkId target, lua_CFunction f)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setfltvalue</span><span class="params">(StkId target, <span class="keyword">float</span> number)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setbvalue</span><span class="params">(StkId target, <span class="keyword">bool</span> b)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setnilvalue</span><span class="params">(StkId target)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setpvalue</span><span class="params">(StkId target, <span class="keyword">void</span>* p)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setobj</span><span class="params">(StkId target, StkId value)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// common/luastate.c</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setivalue</span><span class="params">(StkId target, <span class="keyword">int</span> integer)</span> </span>&#123;</span><br><span class="line">    target-&gt;value_.i = integer;</span><br><span class="line">    target-&gt;tt_ = LUA_NUMINT;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setfvalue</span><span class="params">(StkId target, lua_CFunction f)</span> </span>&#123;</span><br><span class="line">    target-&gt;value_.f = f;</span><br><span class="line">    target-&gt;tt_ = LUA_TLCF;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setfltvalue</span><span class="params">(StkId target, <span class="keyword">float</span> number)</span> </span>&#123;</span><br><span class="line">    target-&gt;value_.n = number;</span><br><span class="line">    target-&gt;tt_ = LUA_NUMFLT;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setbvalue</span><span class="params">(StkId target, <span class="keyword">bool</span> b)</span> </span>&#123;</span><br><span class="line">    target-&gt;value_.b = b ? <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">    target-&gt;tt_ = LUA_TBOOLEAN;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setnilvalue</span><span class="params">(StkId target)</span> </span>&#123;</span><br><span class="line">    target-&gt;tt_ = LUA_TNIL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setpvalue</span><span class="params">(StkId target, <span class="keyword">void</span>* p)</span> </span>&#123;</span><br><span class="line">    target-&gt;value_.p = p;</span><br><span class="line">    target-&gt;tt_ = LUA_TLIGHTUSERDATA;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setobj</span><span class="params">(StkId target, StkId value)</span> </span>&#123;</span><br><span class="line">    target-&gt;value_ = value-&gt;value_;</span><br><span class="line">    target-&gt;tt_ = value-&gt;tt_;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>之前我们介绍了TValue，这里其实就是为每一种类型的域赋值，并且为其加上类型，功能简单没有什么要说明的，对于push函数，这里仅举一例lua_pushinteger，其他的实现方法类似，大家可以直接去dummylua的工程里找.<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luastate.h</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">increase_top</span><span class="params">(struct lua_State* L)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lua_pushinteger</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> integer)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// common/luastate.c</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">increase_top</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    L-&gt;top++;</span><br><span class="line">    assert(L-&gt;top &lt;= L-&gt;ci-&gt;top);    </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lua_pushinteger</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> integer)</span> </span>&#123;</span><br><span class="line">    setivalue(L-&gt;top, integer);</span><br><span class="line">    increase_top(L);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="出栈"><a href="#出栈" class="headerlink" title="出栈"></a>出栈</h2><pre><code>出栈操作非常简单，只需要让L-&gt;top–，同时要注意top指针不要&lt;=L-&gt;ci-&gt;func
</code></pre><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luastate.h</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lua_settop</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> idx)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">lua_gettop</span><span class="params">(struct lua_State* L)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lua_pop</span><span class="params">(struct lua_State* L)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// common/luastate.c</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">lua_gettop</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> cast(<span class="keyword">int</span>, L-&gt;top - (L-&gt;ci-&gt;func + <span class="number">1</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lua_settop</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> idx)</span> </span>&#123;</span><br><span class="line">    StkId func = L-&gt;ci-&gt;func;</span><br><span class="line">    <span class="keyword">if</span> (idx &gt;=<span class="number">0</span>) &#123;</span><br><span class="line">        assert(idx &lt;= L-&gt;stack_last - (func + <span class="number">1</span>));</span><br><span class="line">        <span class="keyword">while</span>(L-&gt;top &lt; (func + <span class="number">1</span>) + idx) &#123;</span><br><span class="line">            setnilvalue(L-&gt;top++);</span><br><span class="line">        &#125;</span><br><span class="line">        L-&gt;top = func + <span class="number">1</span> +idx;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        assert(L-&gt;top + idx &gt; func);</span><br><span class="line">        L-&gt;top = L-&gt;top + idx;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lua_pop</span><span class="params">(struct lua_State* L)</span> </span>&#123;</span><br><span class="line">    lua_settop(L, <span class="number">-1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里引入了设置栈顶指针的函数settop，逻辑也非常直观，这里不作解释。</p>
<h2 id="获取栈上的值"><a href="#获取栈上的值" class="headerlink" title="获取栈上的值"></a>获取栈上的值</h2><p>   获取栈上的值，实际上就是要传入栈的索引，然后获取它的值，通常我们需要该位置的值是什么类型，因此通常是一个尝试性的操作如lua_tointeger(L, -1)，是尝试将栈顶(L-&gt;top - 1)的值转成integer类型，期间可能成功，也可能失败，这里仅举lua_tointeger的例子，其他部分可以到dummylua工程里查阅。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// clib/luaaux.h</span></span><br><span class="line"><span class="function">lua_Integer <span class="title">luaL_tointeger</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> idx)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// clib/luaaux.c</span></span><br><span class="line"><span class="function">lua_Integer <span class="title">luaL_tointeger</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> idx)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> isnum = <span class="number">0</span>;</span><br><span class="line">    lua_Integer ret = lua_tointegerx(L, idx, &amp;isnum);</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// common/luastate.h</span></span><br><span class="line"><span class="function">lua_Integer <span class="title">lua_tointegerx</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> idx, <span class="keyword">int</span>* isnum)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// common/luastate.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> TValue* <span class="title">index2addr</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> idx)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (idx &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        assert(L-&gt;ci-&gt;func + idx &lt; L-&gt;ci-&gt;top);</span><br><span class="line">        <span class="keyword">return</span> L-&gt;ci-&gt;func + idx;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        assert(L-&gt;top + idx &gt; L-&gt;ci-&gt;func);</span><br><span class="line">        <span class="keyword">return</span> L-&gt;top + idx;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">lua_Integer <span class="title">lua_tointegerx</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> idx, <span class="keyword">int</span>* isnum)</span> </span>&#123;</span><br><span class="line">    lua_Integer ret = <span class="number">0</span>;</span><br><span class="line">    TValue* addr = index2addr(L, idx); </span><br><span class="line">    <span class="keyword">if</span> (addr-&gt;tt_ == LUA_NUMINT) &#123;</span><br><span class="line">        ret = addr-&gt;value_.i;</span><br><span class="line">        *isnum = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        *isnum = <span class="number">0</span>;</span><br><span class="line">        LUA_ERROR(L, <span class="string">"can not convert to integer!\n"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="luaL-pcall实现"><a href="#luaL-pcall实现" class="headerlink" title="luaL_pcall实现"></a>luaL_pcall实现</h2><p>   在开始介绍pcall之前，我们先来看看不在保护模式下的函数调用逻辑是怎么实现的，这一些列操作包含在luaD_call函数内，它被定义在vm/luado.h vm/luado.c上<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vm/luado.h</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_call</span><span class="params">(struct lua_State* L, StkId func, <span class="keyword">int</span> nresult)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// vm/luado.c</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_call</span><span class="params">(struct lua_State* L, StkId func, <span class="keyword">int</span> nresult)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (++L-&gt;ncalls &gt; LUA_MAXCALLS) &#123;</span><br><span class="line">        luaD_throw(L, <span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!luaD_precall(L, func, nresult)) &#123;</span><br><span class="line">        <span class="comment">// TODO luaV_execute(L);</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    L-&gt;ncalls--;</span><br><span class="line">    <span class="keyword">return</span> LUA_OK;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>其中的func参数，指定了要被调用函数的栈地址，而nresult则指定了，这个函数被期望返回多少个返回值。这里并没有直接执行func，而是调用了一个luaD_precall为函数调用做准备，这个函数主要预处理lua函数调用的情况，如果调用的是light c function，那么就是直接执行，而不用进入到后续章节我们将实现的luaV_execute中去执行虚拟机指令。这个函数同样定义在vm/luado.h 和vm/luado.c中<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vm/luado.h</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_precall</span><span class="params">(struct lua_State* L, StkId func, <span class="keyword">int</span> nresult)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// vm/luado.c </span></span><br><span class="line"><span class="comment">// prepare for function call. </span></span><br><span class="line"><span class="comment">// if we call a c function, just directly call it</span></span><br><span class="line"><span class="comment">// if we call a lua function, just prepare for call it</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_precall</span><span class="params">(struct lua_State* L, StkId func, <span class="keyword">int</span> nresult)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">switch</span>(func-&gt;tt_) &#123;</span><br><span class="line">        <span class="keyword">case</span> LUA_TLCF: &#123;</span><br><span class="line">            lua_CFunction f = func-&gt;value_.f;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">ptrdiff_t</span> func_diff = savestack(L, func);</span><br><span class="line">            luaD_checkstack(L, LUA_MINSTACK);           <span class="comment">// 检查lua_State的空间是否充足，如果不充足，则需要扩展</span></span><br><span class="line">            func = restorestack(L, func_diff);</span><br><span class="line"></span><br><span class="line">            next_ci(L, func, nresult);                        </span><br><span class="line">            <span class="keyword">int</span> n = (*f)(L);                            <span class="comment">// 对func指向的函数进行调用，并获取实际返回值的数量</span></span><br><span class="line">            assert(L-&gt;ci-&gt;func + n &lt;= L-&gt;ci-&gt;top);</span><br><span class="line">            luaD_poscall(L, L-&gt;top - n, n);             <span class="comment">// 处理返回值</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>; </span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:<span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>目前我只处理了LUA_TLCF也就是light c function这一种情况。调用func之前，首先会检查一下lua_State的栈是否空间充足，如果新创建的CallInfo的top指针，不能在lua_State栈的有效范围之内，那么栈就要扩充，通常是扩充为原来的两倍，这一段逻辑写在luaD_checkstack内，大家可以直接到dummylua工程里查看，这里就不列举了。这里需要注意的是，拓展栈需要申请一块新的内存空间，因此L-&gt;stack的地址也会改变，在扩充之后，需要修正func所指向的地址。完成这一系列的操作之后，就是为调用func创建一个CallInfo实例，大家可以将下面这段代码和之前展示的逻辑图联系起来，就能很好得理解了<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> struct CallInfo* <span class="title">next_ci</span><span class="params">(struct lua_State* L, StkId func, <span class="keyword">int</span> nresult)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">ci</span>;</span></span><br><span class="line">    ci = luaM_realloc(L, <span class="literal">NULL</span>, <span class="number">0</span>, <span class="keyword">sizeof</span>(struct CallInfo));</span><br><span class="line">    ci-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    ci-&gt;previous = L-&gt;ci;</span><br><span class="line">    L-&gt;ci-&gt;next = ci;</span><br><span class="line">    ci-&gt;nresult = nresult;</span><br><span class="line">    ci-&gt;callstatus = LUA_OK;</span><br><span class="line">    ci-&gt;func = func;</span><br><span class="line">    ci-&gt;top = L-&gt;top + LUA_MINSTACK;</span><br><span class="line">    L-&gt;ci = ci;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ci;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>接下来就是对func函数进行调用，调用完成后，它会返回一个值n，告诉我们它实际有多少个返回值，然后需要根据这个n值对返回值进行处理，这些处理逻辑实现在vm/luado.h vm/luado.c中的luaD_poscall函数中<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vm/luado.h</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_poscall</span><span class="params">(struct lua_State* L, StkId first_result, <span class="keyword">int</span> nresult)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// vm/luado.c</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_poscall</span><span class="params">(struct lua_State* L, StkId first_result, <span class="keyword">int</span> nresult)</span> </span>&#123;</span><br><span class="line">    StkId func = L-&gt;ci-&gt;func;</span><br><span class="line">    <span class="keyword">int</span> nwant = L-&gt;ci-&gt;nresult;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">switch</span>(nwant) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0</span>: &#123;</span><br><span class="line">            L-&gt;top = L-&gt;ci-&gt;func;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">1</span>: &#123;</span><br><span class="line">            <span class="keyword">if</span> (nresult == <span class="number">0</span>) &#123;</span><br><span class="line">                first_result-&gt;value_.p = <span class="literal">NULL</span>;</span><br><span class="line">                first_result-&gt;tt_ = LUA_TNIL;</span><br><span class="line">            &#125;</span><br><span class="line">            setobj(func, first_result);</span><br><span class="line">            first_result-&gt;value_.p = <span class="literal">NULL</span>;</span><br><span class="line">            first_result-&gt;tt_ = LUA_TNIL;</span><br><span class="line"></span><br><span class="line">            L-&gt;top = func + nwant;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LUA_MULRET: &#123;</span><br><span class="line">            <span class="keyword">int</span> nres = cast(<span class="keyword">int</span>, L-&gt;top - first_result);</span><br><span class="line">            <span class="keyword">int</span> i;</span><br><span class="line">            <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nres; i++) &#123;</span><br><span class="line">                StkId current = first_result + i;</span><br><span class="line">                setobj(func + i, current);</span><br><span class="line">                current-&gt;value_.p = <span class="literal">NULL</span>;</span><br><span class="line">                current-&gt;tt_ = LUA_TNIL;</span><br><span class="line">            &#125;</span><br><span class="line">            L-&gt;top = func + nres;</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>: &#123;</span><br><span class="line">            <span class="keyword">if</span> (nwant &gt; nresult) &#123;</span><br><span class="line">                <span class="keyword">int</span> i;</span><br><span class="line">                <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nwant; i++) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (i &lt; nresult) &#123;</span><br><span class="line">                        StkId current = first_result + i;</span><br><span class="line">                        setobj(func + i, current);</span><br><span class="line">                        current-&gt;value_.p = <span class="literal">NULL</span>;</span><br><span class="line">                        current-&gt;tt_ = LUA_TNIL;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        StkId <span class="built_in">stack</span> = func + i;</span><br><span class="line">                        <span class="built_in">stack</span>-&gt;tt_ = LUA_TNIL;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                L-&gt;top = func + nwant;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">int</span> i;</span><br><span class="line">                <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nresult; i++) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (i &lt; nwant) &#123;</span><br><span class="line">                        StkId current = first_result + i;</span><br><span class="line">                        setobj(func + i, current);</span><br><span class="line">                        current-&gt;value_.p = <span class="literal">NULL</span>;</span><br><span class="line">                        current-&gt;tt_ = LUA_TNIL;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        StkId <span class="built_in">stack</span> = func + i;</span><br><span class="line">                        <span class="built_in">stack</span>-&gt;value_.p = <span class="literal">NULL</span>;</span><br><span class="line">                        <span class="built_in">stack</span>-&gt;tt_ = LUA_TNIL; </span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                L-&gt;top = func + nresult;</span><br><span class="line">            &#125; </span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">ci</span> = <span class="title">L</span>-&gt;<span class="title">ci</span>;</span></span><br><span class="line">    L-&gt;ci = ci-&gt;previous;</span><br><span class="line">    L-&gt;ci-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// because we have not implement gc, so we should free ci manually</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    (*g-&gt;frealloc)(g-&gt;ud, ci, <span class="keyword">sizeof</span>(struct CallInfo), <span class="number">0</span>); </span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> LUA_OK;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>函数第二个参数first_result记录了第一个返回值在栈中的位置，而第三个参数nresult则指明了实际的返回值数量。很多情况下，我们调用函数时，期待的返回值数量nwant和实际的返回值数量nresult是不一样的，因此在luaD_poscall函数里需要对各种情况进行处理。如果我们不期待有返回值，也就是nwant为0，那么实际的返回值不管有多少个都会被丢弃，此时L-&gt;top指针也会指向L-&gt;ci-&gt;func的位置，函数以及之前push进来的参数都会被出栈。当我们期待只有一个返回值时，返回值结果会被移动到L-&gt;ci-&gt;func的位置，如果实际没有返回值，他就会被设置为NIL。当我们期待的返回值比实际的返回值多时，缺少的部分会被nil值补足。当我们期待的返回值比实际少时，多出来的会被丢弃。这里还有一个特殊的情况，就是nwant为LUA_MULRET的情况，这种情况就是实际有多少个返回值，他就返回多少个，不会丢弃，事实上LUA_MULRET的值是-1。经过一系列操作，对于被调用者而言现在栈上存在的不再是被调用函数自己其参数，而是被调用函数留下的返回值。</p>
<p>   描述c函数在栈上的调用流程，是非常难写的，这也是为什么在讨论具体实现之前，先通过图的方式来在概念上展示这个流程，目的是为了让大家有宏观上的感知，如果能理解上节的内容，那么这章节的内容也不难理解，虽然它可能很枯燥。在完成函数调用的实现以后，我们还需要讨论的一个东西，就是在保护模式下进行函数调用。事实上提供给外部使用的函数调用接口时luaL_pcall，也就是默认所有的c函数调用都应该在保护模式下进行，它的定义如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// clib/luaaux.h</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaL_pcall</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> narg, <span class="keyword">int</span> nresult)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// clib/luaaux.c</span></span><br><span class="line"><span class="comment">// function call</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">CallS</span> &#123;</span></span><br><span class="line">    StkId func;</span><br><span class="line">    <span class="keyword">int</span> nresult;</span><br><span class="line">&#125; CallS;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">f_call</span><span class="params">(lua_State* L, <span class="keyword">void</span>* ud)</span> </span>&#123;</span><br><span class="line">    CallS* c = cast(CallS*, ud);</span><br><span class="line">    luaD_call(L, c-&gt;func, c-&gt;nresult);</span><br><span class="line">    <span class="keyword">return</span> LUA_OK;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaL_pcall</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> narg, <span class="keyword">int</span> nresult)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> status = LUA_OK;</span><br><span class="line">    CallS c;</span><br><span class="line">    c.func = L-&gt;top - (narg + <span class="number">1</span>);</span><br><span class="line">    c.nresult = nresult; </span><br><span class="line"></span><br><span class="line">    status = luaD_pcall(L, &amp;f_call, &amp;c, savestack(L, L-&gt;top), <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">return</span> status;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>luaL_pcall接口只是让使用者填入，被调用的函数有多少个参数，以及期望有多少个返回值，luaL_pcall，则是通过narg这个表示参数个数的值，推断出被调用函数在栈中的位置，并且调用另一个函数luaD_pcall，我们可以观察到，这里一并将f_call函数传入到luaD_pcall中，在保护模式下执行f_call，实际上就是执行我们刚刚讨论的luaD_call。调用luaD_pcall，除了需要传入实际执行栈函数调用的f_call以外，我们还需要传入一个CallS类型的变量c，这个变量会在f_call中使用到，其实是保存调用信息临时数据的一个结构。此外我们还需要传入栈顶在函数调用前的位置，用于异常后恢复栈之用。下面我们来看看luaD_pcall的定义：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vm/luado.h</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_pcall</span><span class="params">(struct lua_State* L, Pfunc f, <span class="keyword">void</span>* ud, <span class="keyword">ptrdiff_t</span> oldtop, <span class="keyword">ptrdiff_t</span> ef)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// vm/luado.c</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_pcall</span><span class="params">(struct lua_State* L, Pfunc f, <span class="keyword">void</span>* ud, <span class="keyword">ptrdiff_t</span> oldtop, <span class="keyword">ptrdiff_t</span> ef)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> status;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">old_ci</span> = <span class="title">L</span>-&gt;<span class="title">ci</span>;</span></span><br><span class="line">    <span class="keyword">ptrdiff_t</span> old_errorfunc = L-&gt;errorfunc;    </span><br><span class="line">    </span><br><span class="line">    status = luaD_rawrunprotected(L, f, ud);</span><br><span class="line">    <span class="keyword">if</span> (status != LUA_OK) &#123;</span><br><span class="line">        <span class="comment">// because we have not implement gc, so we should free ci manually</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">free_ci</span> = <span class="title">L</span>-&gt;<span class="title">ci</span>;</span></span><br><span class="line">        <span class="keyword">while</span>(free_ci) &#123;</span><br><span class="line">            <span class="keyword">if</span> (free_ci == old_ci) &#123;</span><br><span class="line">                free_ci = free_ci-&gt;next;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">previous</span> = <span class="title">free_ci</span>-&gt;<span class="title">previous</span>;</span></span><br><span class="line">            previous-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">            </span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">CallInfo</span>* <span class="title">next</span> = <span class="title">free_ci</span>-&gt;<span class="title">next</span>;</span></span><br><span class="line">            (*g-&gt;frealloc)(g-&gt;ud, free_ci, <span class="keyword">sizeof</span>(struct CallInfo), <span class="number">0</span>);</span><br><span class="line">            free_ci = next;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        L-&gt;ci = old_ci;</span><br><span class="line">        L-&gt;top = restorestack(L, oldtop);</span><br><span class="line">        seterrobj(L, status);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    L-&gt;errorfunc = old_errorfunc; </span><br><span class="line">    <span class="keyword">return</span> status;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们的f_call最终会在luaD_rawrunprotected中被调用，而在调用之前，我们需要对栈的一些信息进行保存，包括栈当前调用的是哪个函数（保存ci），栈当前的错误处理函数处于哪个位置。而一旦调用失败，我们就需要恢复原来栈的状态。</p>
<p>   现在我们来看看luaD_rawrunprotected的定义<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vm/luado.h</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_rawrunprotected</span><span class="params">(struct lua_State* L, Pfunc f, <span class="keyword">void</span>* ud)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// vm/luado.c</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_TRY(L, c, a) <span class="meta-keyword">if</span> (_setjmp((c)-&gt;b) == 0) &#123; a &#125; </span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> _WINDOWS_PLATFORM_ </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_THROW(c) longjmp((c)-&gt;b, 1) </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span> </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LUA_THROW(c) _longjmp((c)-&gt;b, 1) </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span> </span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">lua_longjmp</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_longjmp</span>* <span class="title">previous</span>;</span></span><br><span class="line">    jmp_buf b;</span><br><span class="line">    <span class="keyword">int</span> status;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaD_rawrunprotected</span><span class="params">(struct lua_State* L, Pfunc f, <span class="keyword">void</span>* ud)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> old_ncalls = L-&gt;ncalls;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lua_longjmp</span> <span class="title">lj</span>;</span></span><br><span class="line">    lj.previous = L-&gt;errorjmp;</span><br><span class="line">    lj.status = LUA_OK;</span><br><span class="line">    L-&gt;errorjmp = &amp;lj;</span><br><span class="line"></span><br><span class="line">    LUA_TRY(</span><br><span class="line">        L,</span><br><span class="line">        L-&gt;errorjmp,</span><br><span class="line">        (*f)(L, ud);</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    L-&gt;errorjmp = lj.previous;</span><br><span class="line">    L-&gt;ncalls = old_ncalls;</span><br><span class="line">    <span class="keyword">return</span> lj.status;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里我们定义了一个lua_longjmp的结构，这是用来辅助我们处理异常情况用的。在初始化lua_State的时候，我们的errorjmp的值为NULL，errorjmp是否为NULL是判断当前函数是否处于保护模式的重要指标，这个我们后面会提到。我们的f_call函数，是在LUA_TRY这个宏里被调用的，而这个宏的作用是什么呢？由于c语言没有try catch机制，因此我们需要通过一种方式来模拟try catch机制来捕捉异常，这里就需要用到我们的setjmp和longjmp函数了。setjmp和longjmp是什么接口？我们来引用Linux man page longjmp的一段说明：</p>
<blockquote>
<p>Description<br>longjmp() and setjmp(3) are useful for dealing with errors and interrupts encountered in a low-level subroutine of a program. longjmp() restores the environment saved by the last call of setjmp(3) with the corresponding env argument. After longjmp() is completed, program execution continues as if the corresponding call of setjmp(3) had just returned the value val. longjmp() cannot cause 0 to be returned. If longjmp() is invoked with a second argument of 0, 1 will be returned instead.siglongjmp() is similar to longjmp() except for the type of its env argument. If, and only if, the sigsetjmp(3) call that set this env used a nonzero savesigs flag, siglongjmp() also restores the signal mask that was saved by sigsetjmp(3).</p>
</blockquote>
<p>是不是看着很晕？那我们就通过一个例子来加以解释和说明：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;setjmp.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">jmp_buf b;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123; </span><br><span class="line">    <span class="keyword">int</span> ret = setjmp(b);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"setjmp result = %d\n"</span>, ret);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (ret == <span class="number">0</span>)</span><br><span class="line">        longjmp(b, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line">```c</span><br><span class="line">此时的输出是：</span><br><span class="line">```bash</span><br><span class="line">setjmp result = <span class="number">0</span></span><br><span class="line">setjmp result = <span class="number">1</span></span><br></pre></td></tr></table></figure></p>
<p>为什么printf函数会被调用两次？答案是我们第一次调用setjmp的时候，将当前的栈信息保存在了jmp_buf变量b中，并且返回0值，此时程序继续执行，这时候因为返回值ret为0，因此我们会调用longjmp函数。这个longjmp函数会恢复jmp_buf变量b中记录的环境，因此程序会跳回到setjmp调用的地方，并且longjmp还将第二个参数1，带给了setjmp，并作为其第二次调用的返回值。利用这种特性，我们可以模拟C++的try catch机制，下面再举个例子<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;setjmp.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> except = <span class="number">0</span>;</span><br><span class="line">jmp_buf b;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"foo\n"</span>);</span><br><span class="line">    longjmp(b, <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123; </span><br><span class="line">    <span class="keyword">if</span> ((except = setjmp(b)) == <span class="number">0</span>) &#123;</span><br><span class="line">        foo();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"except type %d\n"</span>, except);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>// 输出<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">foo</span><br><span class="line">except <span class="built_in">type</span> 1</span><br></pre></td></tr></table></figure></p>
<p>把setjmp(b)不为0时，执行的部分，作为捕捉异常的逻辑，把longjmp作为throw函数使用，回顾以下我们的LUA_TRY和LUA_THROW宏，是不是有一种异曲同工之妙？当然，我们不会直接在被调用的函数里，发生异常时直接调用LUA_THROW宏，而是使用另一个接口luaD_throw<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vm/luado.h</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaD_throw</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> error)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// vm/luado.c</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaD_throw</span><span class="params">(struct lua_State* L, <span class="keyword">int</span> error)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">global_State</span>* <span class="title">g</span> = <span class="title">G</span>(<span class="title">L</span>);</span></span><br><span class="line">    <span class="keyword">if</span> (L-&gt;errorjmp) &#123;</span><br><span class="line">        L-&gt;errorjmp-&gt;status = error;</span><br><span class="line">        LUA_THROW(L-&gt;errorjmp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (g-&gt;panic) &#123;</span><br><span class="line">            (*g-&gt;panic)(L);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">abort</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个函数，根据L-&gt;errorjmp的值，判断当前函数是否处于保护模式中，处于与不处于处理的方式是不相同的，如果L-&gt;errorjmp为非NULL值，那说明函数是在LUA_TRY的包裹下执行的，因此可以调用LUA_THROW跳出当前的调用，如果不是那么则调用之前设置好的panic函数（如果有），然后退出进程，出手这么重，难怪用panic这个名字。<br>        到目前为止，函数调用和保护模式的代码我们均已实现。</p>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>   这是一篇篇幅较长的一个章节（虽然不是我写过的最长的单篇博文，笑），但是我们的目标是清晰的，设计和实现Lua基本数据结构、栈和基于栈的C函数调用的设计与实现。并对这些内容进行了不同程度的解析，尽管我们现在并未涉及到任何编译器和虚拟机指令相关的内容，但是当前的内容是理解后续内容的基础，也是关键所在，希望后续内容能够给大家带来更精彩的内容。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言调度器源代码情景分析之七：函数调用过程</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90%E4%B9%8B%E4%B8%83%EF%BC%9A%E5%87%BD%E6%95%B0%E8%B0%83%E7%94%A8%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<p>原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/3RUjui-q6bgRnUW7TgOjmA" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-04-22</p>
<p>前面几节我们介绍了CPU寄存器、内存、汇编指令以及栈等基础知识，为了达到融会贯通加深理解的目的，这一节我们来综合运用一下所学知识，看看函数的执行和调用过程。</p>
<p>本节我们需要重点关注的问题有：</p>
<p>CPU是如何从调用者跳转到被调用函数执行的？</p>
<p>参数是如何从调用者传递给被调用函数的？</p>
<p>函数局部变量所占内存是怎么在栈上分配的？</p>
<p>返回值是如何从被调用函数返回给调用者的？</p>
<p>函数执行完成之后又需要做哪些清理工作？</p>
<p>解决了这些问题，我们对计算机执行程序的原理就有了一个大致的了解，这对于我们理解goroutine的调度有非常重要的作用。</p>
<p>相对于go来说，C语言更接近于硬件，编译后的汇编代码也更加简单直观，更容易让我们掌握函数调用的基本原理，所以我们首先来看C语言的函数调用在汇编指令层面是如何实现的，然后在此基础上分析go语言的函数调用过程。</p>
<p>C语言函数调用过程</p>
<p>我们用一个简单的例子程序来开始分析。</p>
<p>#include &lt;stdio.h&gt;</p>
<p>// 对参数 a 和 b 求和<br>int sum(int a, int b)<br>{<br>        int s = a + b;</p>
<pre><code>return s;
</code></pre><p>}</p>
<p>// main函数：程序入口<br>int main(int argc, char *argv[])<br>{<br>        int n = sum(1, 2); // 调用sum函数对求和</p>
<pre><code>printf(&quot;n: %d\n&quot;, n);  //在屏幕输出 n 的值

return 0;
</code></pre><p>}<br>用gcc编译这个程序得到可执行程序call，然后使用gdb调试。在gdb中我们通过disass main反汇编main函数找到main的第一条指令所在的地址为0x0000000000400540，然后使用b *0x0000000000400540在该地址下一个断点并运行程序：</p>
<p>bobo@ubuntu:~/study/c$ gdb ./call<br>(gdb) disass main<br>Dump of assembler code for function main:<br>  0x0000000000400540 &lt;+0&gt;:push   %rbp<br>  0x0000000000400541 &lt;+1&gt;:mov   %rsp,%rbp<br>  0x0000000000400544 &lt;+4&gt;:sub   $0x20,%rsp<br>  0x0000000000400548 &lt;+8&gt;:mov   %edi,-0x14(%rbp)<br>  0x000000000040054b &lt;+11&gt;:mov   %rsi,-0x20(%rbp)<br>  0x000000000040054f &lt;+15&gt;:mov   $0x2,%esi<br>  0x0000000000400554 &lt;+20&gt;:mov   $0x1,%edi<br>  0x0000000000400559 &lt;+25&gt;:callq 0x400526 <sum><br>  0x000000000040055e &lt;+30&gt;:mov   %eax,-0x4(%rbp)<br>  0x0000000000400561 &lt;+33&gt;:mov   -0x4(%rbp),%eax<br>  0x0000000000400564 &lt;+36&gt;:mov   %eax,%esi<br>  0x0000000000400566 &lt;+38&gt;:mov   $0x400604,%edi<br>  0x000000000040056b &lt;+43&gt;:mov   $0x0,%eax<br>  0x0000000000400570 &lt;+48&gt;:callq 0x400400 &lt;printf@plt&gt;<br>  0x0000000000400575 &lt;+53&gt;:mov   $0x0,%eax<br>  0x000000000040057a &lt;+58&gt;:leaveq<br>  0x000000000040057b &lt;+59&gt;:retq<br>End of assembler dump.<br>(gdb) b *0x0000000000400540<br>Breakpoint 1 at 0x400540<br>(gdb) r<br>Starting program: /home/bobo/study/c/call<br>Breakpoint 1, 0x0000000000400540 in main ()<br>程序停在了我们下的断点处，也就是main函数的第一条指令的位置。再次反汇编一下将要执行的main函数，我们先来看其最前面的3条指令：</sum></p>
<p>(gdb) disass<br>Dump of assembler code for function main:<br>=&gt; 0x0000000000400540 &lt;+0&gt;:push   %rbp<br>  0x0000000000400541 &lt;+1&gt;:mov   %rsp,%rbp<br>  0x0000000000400544 &lt;+4&gt;:sub   $0x20,%rsp<br>  ……<br>这3条指令我们一般称之为函数序言，基本上每个函数都以函数序言开始，其主要作用在于保存调用者的rbp寄存器以及为当前函数分配栈空间，后面我们会详细介绍这3条指令，我们先来说明一下gdb输出的反汇编代码的格式，gdb反汇编出来的代码主要分为3个部分：</p>
<p>指令地址</p>
<p>指令相对于当前函数起始地址以字节为单位的偏移</p>
<p>指令</p>
<p>比如第一行代码 0x0000000000400540 &lt;+0&gt;: push   %rbp，表示main函数的第一条指令push   %rbp在内存中的地址为0x0000000000400540，偏移为0（因为它是main函数的第一条指令）。这行代码各组成部分如下图所示：</p>
<p>这里需要说明一点，gdb反汇编输出的结果中的指令地址和偏移只是gdb为了让我们更容易阅读代码而附加上去的，保存在内存中以及被CPU执行的代码只有上图指令部分。</p>
<p>注意，上面反汇编结果中的第一行代码的最左边还有一个 =&gt; 符号，它表示这条指令是CPU将要执行的下一条指令，也就是rip寄存器目前的值为0x0000000000400540，当前的状态是前一条指令已经执行完毕，这一条指令还未开始执行，使用i r rbp rsp rip察看一下rbp、rsp和rip这3个寄存器的值：</p>
<p>(gdb) i r rbp rsp rip<br>rbp           0x4005800x400580 &lt;__libc_csu_init&gt;<br>rsp           0x7fffffffe5180x7fffffffe518<br>rip           0x4005400x400540 <main><br>根据这些寄存器的值，当前时刻函数调用栈、rbp、 rsp和rip的状态以及它们之间的关系如下图所示：</main></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/07766b0821bc3b92a29377b6803d6cd8.png" alt></p>
<p>因为rbp、rsp和rip存放的都是地址，所以这几个寄存器每个都相当于一个指针，看上图，rip指向的是main函数的第一条指令，rsp指向了当前函数调用栈的栈顶，而rbp寄存器并未指向我们关注的栈和指令，于是并未画出它的具体指向，只是显示了它的值。</p>
<p>为了更加清晰的理解程序的执行流程，现在我们开始模拟CPU从main函数的第一条指令开始，一直到执行完整个main函数。</p>
<p>现在开始执行第1条指令，</p>
<p>0x0000000000400540 &lt;+0&gt;:push   %rbp # 保存调用者的rbp寄存器的值<br>这条指令把栈基地址寄存器rbp的值临时保存在main函数的栈帧里，因为main函数需要使用这个寄存器来存放自己的栈基地址，而调用者在调用main函数之前也把它的栈基地址保存在了这个寄存器里面，所以main函数需要把这个寄存器里面的值先保存起来，等main执行完后返回时再把这个寄存器恢复原样，如果不恢复原样，main函数返回后调用者使用rbp寄存器时就会有问题，因为在执行调用者的代码时rbp本应该指向调用者的栈但现在却指向了main函数的栈。</p>
<p>在这条指令之前，代码还在使用调用者的栈帧，执行完这条指令之后，开始使用main函数的栈帧，目前main函数的栈帧里面只保存有调用者的rbp这一个值，在继续执行下一条指令之前，栈和寄存器的状态如下图，图中标红的指令表示刚执行完成的指令。可以看到rsp和rip寄存器的值都已经发生了改变，它们都指向了新的位置。rsp指向了main函数的栈帧的起始位置，而rip指向了main函数的第2条指令。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/000484b0af6734d0b7de248d1fd5edbe.png" alt></p>
<p>在汇编指令一节我们介绍过，执行push指令会修改rsp寄存器的值，但它并不会修改rip寄存器，为什么这里rip也变了呢？其实这是CPU自动完成的，CPU自己知道它要执行的每一条指令的长度有几个字节，比如这里的push %rbp指令只有1个字节长，于是它在开始执行这条指令时就会把rip的值+1，因为执行这条指令之前rip的值为0x400540，+1之后就变成了0x400541，也就是说它指向了main函数的第2条指令。</p>
<p>接着执行第2条指令，</p>
<p>0x0000000000400541 &lt;+1&gt;:mov   %rsp,%rbp # 调整rbp寄存器，使其指向main函数栈帧的起始位置<br>这条指令把rsp的值拷贝给rbp寄存器，让其指向main函数栈帧的起始位置，执行完这条指令之后rsp和rbp寄存器具有相同的值，他们都指向了main函数的栈帧的起始位置，如下图所示：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0ce6863fc6ee65a5a5587140a6229835.png" alt></p>
<p>接着执行第3条指令，</p>
<p>0x0000000000400544 &lt;+4&gt;:sub   $0x20,%rsp # 调整rsp寄存器的值，为局部和临时变量预留栈空间<br>这条指令把rsp寄存器的值减去了32(16进制的0x20)，使其指向了栈空间中一个更低的位置，这一步看似只是简单的修改了rsp寄存器的值，其实质却是给main函数的局部变量和临时变量预留了32（0x20）字节的栈空间，为什么说是预留而不是分配，因为栈的分配是操作系统自动完成的，程序启动时操作系统就会给我们分配一大块内存用作函数调用栈，程序到底使用了多少栈内存由rsp栈顶寄存器来确定。</p>
<p>该指令执行完成之后，从rsp所指位置到rbp所指的这一段栈内存就构成了main函数的完整栈帧，其大小为40字节（8字节用于保存调用者的rbp，另外32字节用于main函数的局部和临时变量），如下图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f198381a8443f0529436182167c45b54.png" alt></p>
<p>接下来的4条指令我们一起把它们执行了，</p>
<p>0x0000000000400548 &lt;+8&gt;:mov   %edi,-0x14(%rbp)<br>0x000000000040054b &lt;+11&gt;:mov   %rsi,-0x20(%rbp)<br>0x000000000040054f &lt;+15&gt;:mov   $0x2,%esi #sum函数的第2个参数放入esi寄存器<br>0x0000000000400554 &lt;+20&gt;:mov   $0x1,%edi #sum函数的第1个参数放入edi寄存器<br>前两条指令负责把main函数得到的两个参数保存在main函数的栈帧里面，可以看到，这里使用了rbp加偏移量的方式来访问栈内存。这里之所以要保存main函数的两个参数，是因为调用者在调用main函数时使用了edi和rsi两个寄存器来给main函数分别传递argc（整数）和argv（指针）两个参数，而main又需要用这两个寄存器给sum函数传递参数，为了不覆盖argc和argv，所以这里需要先把这两个参数保存在栈里面，然后再把传递给sum函数的两个参数1和2放入这两个寄存器之中。</p>
<p>后面两条指令在给sum函数准备参数，我们可以看到，传递给sum的第一个参数放在了edi寄存器里面，第二个参数放在了esi里面。可能你会问，被调用函数怎么知道参数放在这两个寄存器里面的啊？其实这是一个约定而已，大家约定好：调用函数时调用者负责把第一个参数放在rdi里面，第二个参数放在rsi里面，而被调函数直接去这两个寄存器里面把参数拿出来。这里还有个细节，传递给sum的两个参数都是用的edi和esi而不是rdi和rsi，原因在于C语言中int是32位的，而rdi和rsi都是64位的，edi和esi可以分别当成rdi和rsi的一部分来使用。</p>
<p>回到正题，执行完这4条指令后栈和寄存器的状态图(注意，下图中的argc使用的是图中连续8字节内存中的高4字节，低4字节未用)：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/e83772279a8cc8b1ca33b34bb8ffb864.png" alt></p>
<p>参数准备好了之后，接着执行call指令调用sum函数，</p>
<p>0x0000000000400559 &lt;+25&gt;:callq 0x400526 <sum>  #调用sum函数<br>call指令有点特殊，刚开始执行它的时候rip指向的是call指令的下一条指令，也就是说rip寄存器的值是0x40055e这个地址，但在call指令执行过程中，call指令会把当前rip的值（0x40055e）入栈，然后把rip的值修改为call指令后面的操作数，这里是0x400526，也就是sum函数第一条指令的地址，这样cpu就会跳转到sum函数去执行。</sum></p>
<p>call指令执行完成后栈及寄存器的状态如下图所示，可以看到rip已经指向了sum函数的第1条指令，sum函数执行完成返回之后需要执行的指令的地址0x40055e也已经保存到了main函数的栈帧之中。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/fb53314db89670757b6cad142184e7e5.png" alt></p>
<p>由于在main中执行了调用sum函数的call指令，CPU现在跳转到sum函数开始执行，</p>
<p>0x0000000000400526 &lt;+0&gt;:push   %rbp<br>0x0000000000400527 &lt;+1&gt;:mov   %rsp,%rbp<br>0x000000000040052a &lt;+4&gt;:mov   %edi,-0x14(%rbp)<br>0x000000000040052d &lt;+7&gt;:mov   %esi,-0x18(%rbp)<br>0x0000000000400530 &lt;+10&gt;:mov   -0x14(%rbp),%edx<br>0x0000000000400533 &lt;+13&gt;:mov   -0x18(%rbp),%eax<br>0x0000000000400536 &lt;+16&gt;:add   %edx,%eax<br>0x0000000000400538 &lt;+18&gt;:mov   %eax,-0x4(%rbp)<br>0x000000000040053b &lt;+21&gt;:mov   -0x4(%rbp),%eax<br>0x000000000040053e &lt;+24&gt;:pop   %rbp<br>0x000000000040053f &lt;+25&gt;:retq<br>sum函数的前2条指令跟main函数前两条指令一模一样，</p>
<p>0x0000000000400526 &lt;+0&gt;:push   %rbp            # sum函数序言，保存调用者的rbp<br>0x0000000000400527 &lt;+1&gt;:mov   %rsp,%rbp   # sum函数序言，调整rbp寄存器指向自己的栈帧起始位置<br>都是在保存调用者的rbp然后设置新值使其指向当前函数栈帧的起始位置，这里sum函数保存了main函数的rbp寄存器的值（0x7fffffffe510），并使rbp寄存器指向了自己的栈帧的起始位置(地址为0x7fffffffe4e0)。</p>
<p>可以看到，sum的函数序言并未像main函数一样通过调整rsp寄存器的值来给sum函数预留用于局部变量和临时变量的栈空间，那这是不是说明sum函数就没有使用栈来保存局部变量呢，其实不是，从后面的分析可以看到，sum函数的局部变量s还是保存在栈上的。没有预留为什么可以使用呢，原因前面也说过，栈上的内存不需要在应用层代码中分配，操作系统已经给我们分配好了，尽管用就行了。main函数之所以需要调整rsp寄存器的值是因为它需要使用call指令来调用sum函数，而call指令会自动把rsp寄存器的值减去8然后把函数的返回地址保存到rsp所指的栈内存位置，如果main函数不调整rsp的值，则call指令保存函数返回地址时会覆盖局部变量或临时变量的值；而sum函数中没有任何指令会自动使用rsp寄存器来保存数据到栈上，所以不需要调整rsp寄存器。</p>
<p>紧接着的4条指令，</p>
<p>0x000000000040052a &lt;+4&gt;:mov   %edi,-0x14(%rbp)  # 把第1个参数a放入临时变量<br>0x000000000040052d &lt;+7&gt;:mov   %esi,-0x18(%rbp)  # 把第2个参数b放入临时变量<br>0x0000000000400530 &lt;+10&gt;:mov   -0x14(%rbp),%edx # 从临时变量中读取第1个到edx寄存器<br>0x0000000000400533 &lt;+13&gt;:mov   -0x18(%rbp),%eax # 从临时变量中读取第2个到eax寄存器<br>通过rbp寄存器加偏移量的方式把main传递给sum的参数保存在当前栈帧的合适位置，然后又取出来放入寄存器，这里有点多此一举，因为我们编译的时候未给gcc指定优化级别，gcc编译程序时默认不做任何优化，所以代码看起来比较啰嗦。</p>
<p>紧接着的几条指令</p>
<p>0x0000000000400536 &lt;+16&gt;:add   %edx,%eax            # 执行a + b并把结果保存到eax寄存器<br>0x0000000000400538 &lt;+18&gt;:mov   %eax,-0x4(%rbp)  # 把加法结果赋值给变量s<br>0x000000000040053b &lt;+21&gt;:mov   -0x4(%rbp),%eax  # 读取s变量的值到eax寄存器<br>第一条add指令负责执行加法运算并把结果3存入eax寄存器，第二条指令负责把eax寄存器的值保存到了s变量所在的内存，第三条指令又把s变量的值读取到eax寄存器，可以看到局部变量s被编译器安排在了rbp - 0x4这个地址所对应的内存之中。</p>
<p>到此，sum函数的主要功能已经完成，在继续执行最后的两条指令之前我们先来看看寄存器和栈的状态：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/7c006d3f94ce6fec1ec31bb27545a6bc.png" alt></p>
<p>上图有1点需要说明一下：</p>
<p>sum函数的两个参数和返回值都是int型的，在内存中只占4个字节，而我们的示意图中每个栈内存单元占8个字节且按8字节地址边界进行了对齐，所以才是现在示意图中的这个样子。</p>
<p>我们来继续执行pop %rbp这条指令，这条指令包含两个操作：</p>
<p>把当前rsp所指栈内存中的值放入rbp寄存器，这样rbp就恢复到了还未执行sum函数的第一条指令时的值，也就是重新指向了main函数的栈帧起始地址。</p>
<p>把rsp寄存器中的值加8，这样rsp就指向了包含0x40055e这个值的栈内存，而这个栈单元中的值是当初main函数调用sum函数时call指令放入的，放入的这个值就是紧跟在call指令后面的下一条指令的地址。</p>
<p>还是来看看示意图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/0472553543af85afe4200fd7d4630fbd.png" alt></p>
<p>继续retq指令，该指令把rsp指向的栈单元中的0x40055e取出给rip寄存器，同时rsp加8，这样rip寄存器中的值就变成了main函数中调用sum的call指令的下一条指令，于是就返回到main函数中继续执行。注意此时eax寄存器中的值是3，也就是sum函数执行后的返回值。还是来看一下状态。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d4c56a51274590d55cf29f77397da2cf.png" alt></p>
<p>继续执行main函数中的</p>
<p>mov   %eax,-0x4(%rbp)  # 把sum函数的返回值赋给变量n<br>该指令把eax寄存器中的值（3）放入rbp - 4所指的内存，这里是变量n所在的位置，所以这条语句其实就是把sum函数的返回值赋值给变量n。这时状态为：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/58abebc86c28882a7795e830a5e7a788.png" alt></p>
<p>后面的几条指令</p>
<p>0x0000000000400561 &lt;+33&gt;:mov   -0x4(%rbp),%eax<br>0x0000000000400564 &lt;+36&gt;:mov   %eax,%esi<br>0x0000000000400566 &lt;+38&gt;:mov   $0x400604,%edi<br>0x000000000040056b &lt;+43&gt;:mov   $0x0,%eax<br>0x0000000000400570 &lt;+48&gt;:callq 0x400400 &lt;printf@plt&gt;<br>0x0000000000400575 &lt;+53&gt;:mov   $0x0,%eax<br>首先为printf函数准备参数然后调用printf函数，在此我们就不分析它们了，因为调用printf和sum的过程差不多，我们让CPU快速执行完这几条指令然后暂停在main函数的倒数第二条的leaveq指令处，这时栈和寄存器状态如下：</p>
<p>leaveq指令上面的一条指令mov $0x0, %eax的作用在于把main函数的返回值0放在eax寄存器中，等main返回后调用main函数的函数可以拿到这个返回值。下面执行leaveq指令，</p>
<p>0x000000000040057a &lt;+58&gt;:leaveq<br>该指令相当于如下两条指令：</p>
<p>mov %rbp, %rsp<br>pop %rbp<br>leaveq指令首先把rbp寄存器中的值复制给rsp，这样rsp就指向了rbp所指的栈单元，然后把使该内存单元中的值POP给rbp寄存器，这样rbp和rsp的值就恢复成刚刚进入main函数时的状态了。看图：</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f805244da968e3f9162e45a786153b58.png" alt></p>
<p>到此main函数就只剩下retq指令了，前面分析sum函数时已经详细分析过它了，这条指令执行完成后就会完全返回到调用main函数的函数中去继续执行。</p>
<p>go语言中的函数调用过程</p>
<p>前面花了很大篇幅分析了C语言的函数调用过程，包括参数的传递，call指令，ret指令，还有返回值如何从被调用函数返回给调用函数的，有了这些基础， 接下来我们来看go语言中的函数调用过程，其实二者原理是一样的，只是细节上有一点差异。还是用一个简单的例子来分析。</p>
<p>package main</p>
<p>//计算a, b的平方和<br>func sum(a, b int) int {<br>        a2 := a <em> a<br>        b2 := b </em> b<br>        c := a2 + b2</p>
<pre><code>return c
</code></pre><p>}</p>
<p>func main() {<br>sum(1, 2)<br>}<br>使用go build编译该程序，注意这里需要指定 -gcflags “-N -l” 关闭编译器优化，否则编译器可能把对sum函数的调用优化掉。</p>
<p>bobo@ubuntu:~/study/go$ go build  -gcflags “-N -l” sum.go<br>编译后得到二进制可执行程序sum， 首先来看main函数的反汇编代码：</p>
<p>Dump of assembler code for function main.main:<br>  0x000000000044f4e0 &lt;+0&gt;: mov   %fs:0xfffffffffffffff8,%rcx #暂时不关注<br>  0x000000000044f4e9 &lt;+9&gt;: cmp   0x10(%rcx),%rsp #暂时不关注<br>  0x000000000044f4ed &lt;+13&gt;: jbe   0x44f51d &lt;main.main+61&gt; #暂时不关注<br>  0x000000000044f4ef &lt;+15&gt;: sub   $0x20,%rsp #为main函数预留32字节栈空间<br>  0x000000000044f4f3 &lt;+19&gt;: mov   %rbp,0x18(%rsp) #保存调用者的rbp寄存器<br>  0x000000000044f4f8 &lt;+24&gt;: lea   0x18(%rsp),%rbp #调整rbp使其指向main函数栈帧开始地址<br>  0x000000000044f4fd &lt;+29&gt;: movq   $0x1,(%rsp) #sum函数的第一个参数（1）入栈<br>  0x000000000044f505 &lt;+37&gt;: movq   $0x2,0x8(%rsp) #sum函数的第二个参数（2）入栈<br>  0x000000000044f50e &lt;+46&gt;: callq 0x44f480 &lt;main.sum&gt; #调用sum函数<br>  0x000000000044f513 &lt;+51&gt;: mov   0x18(%rsp),%rbp #恢复rbp寄存器的值为调用者的rbp<br>  0x000000000044f518 &lt;+56&gt;: add   $0x20,%rsp #调整rsp使其指向保存有调用者返回地址的栈单元<br>  0x000000000044f51c &lt;+60&gt;: retq   #返回到调用者<br>  0x000000000044f51d &lt;+61&gt;: callq 0x447390 &lt;runtime.morestack_noctxt&gt; #暂时不关注<br>  0x000000000044f522 &lt;+66&gt;: jmp   0x44f4e0 &lt;main.main&gt; #暂时不关注<br>End of assembler dump.<br>main函数前面三条和最后两条指令是go编译器插入用于检查栈溢出的代码，我们现在不需要关注。其它部分跟C语言中的函数差不多，不过有点差别的是go语言函数调用时参数放在了栈上（第7和第8条指令把参数放在了栈上），从第4条指令可以看出，编译器给main函数预留了32个字节用于存放main的栈基址rbp、调用sum函数时的两个参数，这三项各占8个字节所以共占24字节，那还有8个字节拿来干什么的呢？从下面的sum函数可以看出来，剩下的8个字节用于存放sum函数的返回值。</p>
<p>Dump of assembler code for function main.sum:<br>  0x000000000044f480 &lt;+0&gt;: sub   $0x20,%rsp #为sum函数预留32字节的栈空间<br>  0x000000000044f484 &lt;+4&gt;: mov   %rbp,0x18(%rsp) #保存main函数的rbp<br>  0x000000000044f489 &lt;+9&gt;: lea   0x18(%rsp),%rbp #设置sum函数的rbp<br>  0x000000000044f48e &lt;+14&gt;: movq   $0x0,0x38(%rsp) #返回值初始化为0<br>  0x000000000044f497 &lt;+23&gt;: mov   0x28(%rsp),%rax #从内存中读取第一个参数a(1)到rax<br>  0x000000000044f49c &lt;+28&gt;: mov   0x28(%rsp),%rcx #从内存中读取第一个参数a(1)到rcx<br>  0x000000000044f4a1 &lt;+33&gt;: imul   %rax,%rcx #计算a <em> a，并把结果放在rcx<br>  0x000000000044f4a5 &lt;+37&gt;: mov   %rcx,0x10(%rsp) #把rcx的值（a </em> a）赋值给变量a2<br>  0x000000000044f4aa &lt;+42&gt;: mov   0x30(%rsp),%rax #从内存中读取第二个参数a(2)到rax<br>  0x000000000044f4af &lt;+47&gt;: mov   0x30(%rsp),%rcx #从内存中读取第二个参数a(2)到rcx<br>  0x000000000044f4b4 &lt;+52&gt;: imul   %rax,%rcx #计算b <em> b，并把结果放在rcx<br>  0x000000000044f4b8 &lt;+56&gt;: mov   %rcx,0x8(%rsp) #把rcx的值（b </em> b）赋值给变量b2<br>  0x000000000044f4bd &lt;+61&gt;: mov   0x10(%rsp),%rax #从内存中读取a2到寄存器rax<br>  0x000000000044f4c2 &lt;+66&gt;: add   %rcx,%rax #计算a2 + b2,并把结果保存在rax<br>  0x000000000044f4c5 &lt;+69&gt;: mov   %rax,(%rsp) #把rax赋值给变量c, c = a2 + b2<br>  0x000000000044f4c9 &lt;+73&gt;: mov   %rax,0x38(%rsp) #将rax的值（a2 + b2）复制给返回值<br>  0x000000000044f4ce &lt;+78&gt;: mov   0x18(%rsp),%rbp #恢复main函数的rbp<br>  0x000000000044f4d3 &lt;+83&gt;: add   $0x20,%rsp #调整rsp使其指向保存有返回地址的栈单元<br>  0x000000000044f4d7 &lt;+87&gt;: retq   #返回main函数<br>End of assembler dump.<br>sum函数的汇编代码比较直观，基本上就是对go语言sum函数的直接翻译，可以看到sum函数通过rsp寄存器从main函数栈中获取参数，返回值也通过rsp保存在了main函数的栈帧中。</p>
<p>下图是已经执行完成sum函数的0x000000000044f4c9 &lt;+73&gt;: mov    %rax,0x38(%rsp)这条指令但还未开始执行下一条指令时栈以及栈寄存器之间的关系图，读者可以结合上面的汇编代码和该图，加深对函数调用过程中的参数传递、返回值以及局部变量在栈上的位置和关系的理解。</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/d1faa652ec67b8eb4845716b0ca6b6e3.png" alt></p>
<p>总结</p>
<p>最后我们来总结一下函数调用过程：</p>
<p>参数传递。gcc编译的c/c++代码一般通过寄存器传递参数，在AMD64 Linux 平台，gcc约定函数调用时前面6个参数分别通过rdi, rsi, rdx, r10, r8及r9传递；而go语言函数调用时参数是通过栈传递给被调用函数的，最后一个参数最先入栈，第一个参数最后入栈，参数在调用者的栈帧之中，被调用函数通过rsp加一定的偏移量来获取参数；</p>
<p>call指令负责把执行call指令时的rip寄存器（函数返回地址）入栈；</p>
<p>gcc通过rbp加偏移量的方式来访问局部和临时变量，而go编译器则使用rsp寄存器加偏移量的方式来访问它们；</p>
<p>ret指令负责把call指令入栈的返回地址出栈给rip，从而实现从被调用函数返回到调用函数继续执行；</p>
<p>gcc使用rax寄存器返回函数调用的返回值，而go使用栈返回函数调用的返回值。</p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt></p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>抢占系统调用执行时间过长的goroutine（22）</title>
    <url>/go/go%E8%AF%AD%E8%A8%80%E8%B0%83%E5%BA%A6%E5%99%A8%E6%BA%90%E4%BB%A3%E7%A0%81%E6%83%85%E6%99%AF%E5%88%86%E6%9E%90/%E6%8A%A2%E5%8D%A0%E7%B3%BB%E7%BB%9F%E8%B0%83%E7%94%A8%E6%89%A7%E8%A1%8C%E6%97%B6%E9%97%B4%E8%BF%87%E9%95%BF%E7%9A%84goroutine%EF%BC%8822%EF%BC%89/</url>
    <content><![CDATA[<p>​​​​​​​​​原创 爱写程序的阿波张 <a href="https://mp.weixin.qq.com/s/If0-35Pt8dN7G1gD4L9-hw" rel="external nofollow noopener noreferrer" target="_blank">源码游记</a> 2019-06-01</p>
<p>本文是《Go语言调度器源代码情景分析》系列的第22篇，也是第六章《抢占调度》的第2小节。</p>
<p>上一节我们分析了因运行时间过长而导致的抢占调度，这一节我们来分析因进入系统调用时间过长而发生的抢占调度。</p>
<p>剥夺工作线程的p</p>
<p>现在重新回到sysmon监控线程定期调用的retake函数:<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">4380</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">retake</span><span class="params">(now <span class="keyword">int64</span>)</span> <span class="title">uint32</span></span> &#123;</span><br><span class="line">    ......</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="built_in">len</span>(allp); i++ &#123;  <span class="comment">//遍历所有p，然后根据p的状态进行抢占</span></span><br><span class="line">        _p_ := allp[i]</span><br><span class="line">        <span class="keyword">if</span> _p_ == <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="comment">// This can happen if procresize has grown</span></span><br><span class="line">            <span class="comment">// allp but not yet created new Ps.</span></span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        &#125;</span><br><span class="line">       </span><br><span class="line">        <span class="comment">//_p_.sysmontick用于sysmon监控线程监控p时记录系统调用时间和运行时间，由sysmon监控线程记录</span></span><br><span class="line">        pd := &amp;_p_.sysmontick</span><br><span class="line">        s := _p_.status</span><br><span class="line">        <span class="keyword">if</span> s == _Psyscall &#123; <span class="comment">//系统调用抢占处理</span></span><br><span class="line">            <span class="comment">// Retake P from syscall if it's there for more than 1 sysmon tick (at least 20us).</span></span><br><span class="line">            <span class="comment">//_p_.syscalltick用于记录系统调用的次数，主要由工作线程在完成系统调用之后++</span></span><br><span class="line">            t := <span class="keyword">int64</span>(_p_.syscalltick)</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">int64</span>(pd.syscalltick) != t &#123;</span><br><span class="line">                <span class="comment">//pd.syscalltick != _p_.syscalltick，说明已经不是上次观察到的系统调用了，</span></span><br><span class="line">                <span class="comment">//而是另外一次系统调用，所以需要重新记录tick和when值</span></span><br><span class="line">                pd.syscalltick = <span class="keyword">uint32</span>(t)</span><br><span class="line">                pd.syscallwhen = now</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            &#125;</span><br><span class="line">           </span><br><span class="line">            <span class="comment">//pd.syscalltick == _p_.syscalltick，说明还是之前观察到的那次系统调用，</span></span><br><span class="line">            <span class="comment">//计算这次系统调用至少过了多长时间了</span></span><br><span class="line">           </span><br><span class="line">            <span class="comment">// On the one hand we don't want to retake Ps if there is no other work to do,</span></span><br><span class="line">            <span class="comment">// but on the other hand we want to retake them eventually</span></span><br><span class="line">            <span class="comment">// because they can prevent the sysmon thread from deep sleep.</span></span><br><span class="line">            <span class="comment">// 只要满足下面三个条件中的任意一个，则抢占该p，否则不抢占</span></span><br><span class="line">            <span class="comment">// 1. p的运行队列里面有等待运行的goroutine</span></span><br><span class="line">            <span class="comment">// 2. 没有无所事事的p</span></span><br><span class="line">            <span class="comment">// 3. 从上一次监控线程观察到p对应的m处于系统调用之中到现在已经超过10了毫秒</span></span><br><span class="line">            <span class="keyword">if</span> runqempty(_p_) &amp;&amp;  atomic.Load(&amp;sched.nmspinning)+atomic.Load(&amp;sched.npidle)&gt; <span class="number">0</span> &amp;&amp; pd.syscallwhen+<span class="number">10</span>*<span class="number">1000</span>*<span class="number">1000</span> &gt; now &#123;</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// Drop allpLock so we can take sched.lock.</span></span><br><span class="line">            unlock(&amp;allpLock)</span><br><span class="line">            <span class="comment">// Need to decrement number of idle locked M's</span></span><br><span class="line">            <span class="comment">// (pretending that one more is running) before the CAS.</span></span><br><span class="line">            <span class="comment">// Otherwise the M from which we retake can exit the syscall,</span></span><br><span class="line">            <span class="comment">// increment nmidle and report deadlock.</span></span><br><span class="line">            incidlelocked(<span class="number">-1</span>)</span><br><span class="line">            <span class="keyword">if</span> atomic.Cas(&amp;_p_.status, s, _Pidle) &#123;</span><br><span class="line">                ......</span><br><span class="line">                _p_.syscalltick++</span><br><span class="line">                handoffp(_p_)  <span class="comment">//寻找一个新的m出来接管P</span></span><br><span class="line">            &#125;</span><br><span class="line">            incidlelocked(<span class="number">1</span>)</span><br><span class="line">            lock(&amp;allpLock)</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> s == _Prunning &#123; <span class="comment">//运行时间太长，抢占处理，前面已经分析</span></span><br><span class="line">            ......</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>retake函数所做的主要事情就在遍历所有的p，并根据每个p的状态以及处于该状态的时长来决定是否需要发起抢占。从代码可以看出只有当p处于 _Prunning 或 _Psyscall 状态时才会进行抢占，而因p处于_Prunning状态的时间过长而发生的抢占调度我们在上一节已经分析过了，现在我们来看看如何对处于系统调用之中的p（对应的goroutine）进行抢占。</p>
<p>根据retake函数的代码，只要满足下面三个条件中的任意一个就需要对处于_Psyscall 状态的p进行抢占：</p>
<p>p的运行队列里面有等待运行的goroutine。这用来保证当前p的本地运行队列中的goroutine得到及时的调度，因为该p对应的工作线程正处于系统调用之中，无法调度队列中goroutine，所以需要寻找另外一个工作线程来接管这个p从而达到调度这些goroutine的目的；</p>
<p>没有空闲的p。表示其它所有的p都已经与工作线程绑定且正忙于执行go代码，这说明系统比较繁忙，所以需要抢占当前正处于系统调用之中而实际上系统调用并不需要的这个p并把它分配给其它工作线程去调度其它goroutine。</p>
<p>从上一次监控线程观察到p对应的m处于系统调用之中到现在已经超过10了毫秒。这表示只要系统调用超时，就对其抢占，而不管是否真的有goroutine需要调度，这样保证sysmon线程不至于觉得无事可做（sysmon线程会判断retake函数的返回值，如果为0，表示retake并未做任何抢占，所以会觉得没啥事情做）而休眠太长时间最终会降低sysmon监控的实时性。至于如何计算某一次系统调用时长可以参考上面代码及注释。</p>
<p>retake函数发现如果需要抢占，则通过使用cas修改p的状态来获取p的使用权（为什么需要使用cas呢？从后面的分析我们将知道，工作线程此时此刻可能正好从系统调用返回了，也正在获取p的使用权），如果使用权获取成功则调用handoffp寻找新的工作线程来接管这个p。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">1995</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Hands off P from syscall or locked M.</span></span><br><span class="line"><span class="comment">// Always runs without a P, so write barriers are not allowed.</span></span><br><span class="line"><span class="comment">//go:nowritebarrierrec</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">handoffp</span><span class="params">(_p_ *p)</span></span> &#123;</span><br><span class="line">    <span class="comment">// handoffp must start an M in any situation where</span></span><br><span class="line">    <span class="comment">// findrunnable would return a G to run on _p_.</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// if it has local work, start it straight away</span></span><br><span class="line">    <span class="comment">//运行队列不为空，需要启动m来接管</span></span><br><span class="line">    <span class="keyword">if</span> !runqempty(_p_) || sched.runqsize != <span class="number">0</span> &#123;</span><br><span class="line">        startm(_p_, <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// if it has GC work, start it straight away</span></span><br><span class="line">    <span class="comment">//有垃圾回收工作需要做，也需要启动m来接管</span></span><br><span class="line">    <span class="keyword">if</span> gcBlackenEnabled != <span class="number">0</span> &amp;&amp; gcMarkWorkAvailable(_p_) &#123;</span><br><span class="line">        startm(_p_, <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// no local work, check that there are no spinning/idle M's,</span></span><br><span class="line">    <span class="comment">// otherwise our help is not required</span></span><br><span class="line">    <span class="comment">//所有其它p都在运行goroutine，说明系统比较忙，需要启动m</span></span><br><span class="line">    <span class="keyword">if</span> atomic.Load(&amp;sched.nmspinning)+atomic.Load(&amp;sched.npidle) == <span class="number">0</span> &amp;&amp; atomic.Cas(&amp;sched.nmspinning, <span class="number">0</span>, <span class="number">1</span>) &#123; <span class="comment">// <span class="doctag">TODO:</span> fast atomic</span></span><br><span class="line">        startm(_p_, <span class="literal">true</span>)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    lock(&amp;sched.lock)</span><br><span class="line">    <span class="keyword">if</span> sched.gcwaiting != <span class="number">0</span> &#123; <span class="comment">//如果gc正在等待Stop The World</span></span><br><span class="line">        _p_.status = _Pgcstop</span><br><span class="line">        sched.stopwait--</span><br><span class="line">        <span class="keyword">if</span> sched.stopwait == <span class="number">0</span> &#123;</span><br><span class="line">            notewakeup(&amp;sched.stopnote)</span><br><span class="line">        &#125;</span><br><span class="line">        unlock(&amp;sched.lock)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">    <span class="keyword">if</span> sched.runqsize != <span class="number">0</span> &#123; <span class="comment">//全局运行队列有工作要做</span></span><br><span class="line">        unlock(&amp;sched.lock)</span><br><span class="line">        startm(_p_, <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// If this is the last running P and nobody is polling network,</span></span><br><span class="line">    <span class="comment">// need to wakeup another M to poll network.</span></span><br><span class="line">    <span class="comment">//不能让所有的p都空闲下来，因为需要监控网络连接读写事件</span></span><br><span class="line">    <span class="keyword">if</span> sched.npidle == <span class="keyword">uint32</span>(gomaxprocs<span class="number">-1</span>) &amp;&amp; atomic.Load64(&amp;sched.lastpoll) != <span class="number">0</span> &#123;</span><br><span class="line">        unlock(&amp;sched.lock)</span><br><span class="line">        startm(_p_, <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    pidleput(_p_)  <span class="comment">//无事可做，把p放入全局空闲队列</span></span><br><span class="line">    unlock(&amp;sched.lock)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>handoffp函数流程比较简单，它的主要任务是通过各种条件判断是否需要启动工作线程来接管_p_，如果不需要则把_p_放入P的全局空闲队列。</p>
<p>从handoffp的代码可以看出，在如下几种情况下则需要调用我们已经分析过的startm函数启动新的工作线程出来接管_p_：</p>
<p>_p_的本地运行队列或全局运行队列里面有待运行的goroutine；</p>
<p>需要帮助gc完成标记工作；</p>
<p>系统比较忙，所有其它_p_都在运行goroutine，需要帮忙；</p>
<p>所有其它P都已经处于空闲状态，如果需要监控网络连接读写事件，则需要启动新的m来poll网络连接。</p>
<p>到此，sysmon监控线程对处于系统调用之中的p的抢占就已经完成。</p>
<p>系统调用</p>
<p>从上面的分析可以看出，这里对正在进行系统调用的goroutine的抢占实质上是剥夺与其对应的工作线程所绑定的p，虽然说处于系统调用之中的工作线程并不需要p，但一旦从操作系统内核返回到用户空间之后就必须绑定一个p才能运行go代码，那么，工作线程从系统调用返回之后如果发现它进入系统调用之前所使用的p被监控线程拿走了，该怎么办呢？接下来我们就来分析这个问题。</p>
<p>为了搞清楚工作线程从系统调用返回之后需要做哪些事情，我们需要找到相关的代码，怎么找代码呢？这里我们通过对一个使用了系统调用的程序的调试来寻找。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    <span class="string">"os"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fd, err := os.Open(<span class="string">"./syscall.go"</span>)  <span class="comment">//一定会执行系统调用</span></span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(err)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    fd.Close()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>使用gdb跟踪调试上面这个程序可以发现，main函数调用的os.Open函数最终会调用到Syscall6函数，因为中间调用过程与我们分析目标没关系，所以我们直接从Syscall6函数开始分析。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">syscall/asm_linux_amd64.s : <span class="number">42</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// func Syscall6(trap, a1, a2, a3, a4, a5, a6 uintptr) (r1, r2, err uintptr)</span></span><br><span class="line">TEXT ·Syscall6(SB), NOSPLIT, $<span class="number">0</span><span class="number">-80</span></span><br><span class="line">    CALL  runtime·entersyscall(SB)</span><br><span class="line"></span><br><span class="line">    #按照linux系统约定复制参数到寄存器并调用syscall指令进入内核</span><br><span class="line">    MOVQ  a1+<span class="number">8</span>(FP), DI</span><br><span class="line">    MOVQ  a2+<span class="number">16</span>(FP), SI</span><br><span class="line">    MOVQ  a3+<span class="number">24</span>(FP), DX</span><br><span class="line">    MOVQ  a4+<span class="number">32</span>(FP), R10</span><br><span class="line">    MOVQ  a5+<span class="number">40</span>(FP), R8</span><br><span class="line">    MOVQ  a6+<span class="number">48</span>(FP), R9</span><br><span class="line">    MOVQ  trap+<span class="number">0</span>(FP), AX#syscall entry，系统调用编号放入AX</span><br><span class="line">    SYSCALL  #进入内核</span><br><span class="line"></span><br><span class="line">    #从内核返回，判断返回值，linux使用 <span class="number">-1</span> ~ <span class="number">-4095</span> 作为错误码</span><br><span class="line">    CMPQ  AX, $<span class="number">0xfffffffffffff001</span></span><br><span class="line">    JLS  ok6</span><br><span class="line"></span><br><span class="line">    #系统调用返回错误，为Syscall6函数准备返回值</span><br><span class="line">    MOVQ  $<span class="number">-1</span>, r1+<span class="number">56</span>(FP)</span><br><span class="line">    MOVQ  $<span class="number">0</span>, r2+<span class="number">64</span>(FP)</span><br><span class="line">    NEGQ  AX</span><br><span class="line">    MOVQ  AX, err+<span class="number">72</span>(FP)</span><br><span class="line">    CALL  runtime·exitsyscall(SB)</span><br><span class="line">    RET</span><br><span class="line">ok6:      #系统调用返回错误</span><br><span class="line">    MOVQ  AX, r1+<span class="number">56</span>(FP)</span><br><span class="line">    MOVQ  DX, r2+<span class="number">64</span>(FP)</span><br><span class="line">    MOVQ  $<span class="number">0</span>, err+<span class="number">72</span>(FP)</span><br><span class="line">    CALL  runtime·exitsyscall(SB)</span><br><span class="line">    RET</span><br></pre></td></tr></table></figure></p>
<p>Syscall6函数主要依次干了如下3件事：</p>
<p>调用runtime.entersyscall函数；</p>
<p>使用SYSCALL指令进入系统调用；</p>
<p>调用runtime.exitsyscall函数。</p>
<p>根据前面的分析和这段代码我们可以猜测，exitsyscall函数将会处理当前工作线程进入系统调用之前所拥有的p被监控线程抢占剥夺的情况。但这里怎么会有个entersyscall呢，它是干啥的？我们先来看看。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">entersyscall函数</span><br><span class="line"></span><br><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2847</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Standard syscall entry used by the go syscall library and normal cgo calls.</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">entersyscall</span><span class="params">()</span></span> &#123;</span><br><span class="line">    reentersyscall(getcallerpc(), getcallersp())</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">reentersyscall</span><span class="params">(pc, sp <span class="keyword">uintptr</span>)</span></span> &#123;</span><br><span class="line">    _g_ := getg()  <span class="comment">//执行系统调用的goroutine</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Disable preemption because during this function g is in Gsyscall status,</span></span><br><span class="line">    <span class="comment">// but can have inconsistent g-&gt;sched, do not let GC observe it.</span></span><br><span class="line">    _g_.m.locks++</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Entersyscall must not call any function that might split/grow the stack.</span></span><br><span class="line">    <span class="comment">// (See details in comment above.)</span></span><br><span class="line">    <span class="comment">// Catch calls that might, by replacing the stack guard with something that</span></span><br><span class="line">    <span class="comment">// will trip any stack check and leaving a flag to tell newstack to die.</span></span><br><span class="line">    _g_.stackguard0 = stackPreempt</span><br><span class="line">    _g_.throwsplit = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Leave SP around for GC and traceback.</span></span><br><span class="line">    save(pc, sp)  <span class="comment">//save函数分析过，用来保存g的现场信息，rsp, rbp, rip等等</span></span><br><span class="line">    _g_.syscallsp = sp</span><br><span class="line">    _g_.syscallpc = pc</span><br><span class="line">    casgstatus(_g_, _Grunning, _Gsyscall)  </span><br><span class="line">    ......</span><br><span class="line">    _g_.m.syscalltick = _g_.m.p.ptr().syscalltick</span><br><span class="line">    _g_.sysblocktraced = <span class="literal">true</span></span><br><span class="line">    _g_.m.mcache = <span class="literal">nil</span></span><br><span class="line">    pp := _g_.m.p.ptr()</span><br><span class="line">    pp.m = <span class="number">0</span>  <span class="comment">//p解除与m之间的绑定</span></span><br><span class="line">    _g_.m.oldp.set(pp)   <span class="comment">//把p记录在oldp中，等从系统调用返回时，优先绑定这个p</span></span><br><span class="line">    _g_.m.p = <span class="number">0</span>  <span class="comment">//m解除与p之间的绑定</span></span><br><span class="line">    atomic.Store(&amp;pp.status, _Psyscall)  <span class="comment">//修改当前p的状态，sysmon线程依赖状态实施抢占</span></span><br><span class="line">    .....</span><br><span class="line">    _g_.m.locks--</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>entersyscall函数直接调用了reentersyscall函数，reentersyscall首先把现场信息保存在当前g的sched成员中，然后解除m和p的绑定关系并设置p的状态为_Psyscall，前面我们已经看到sysmon监控线程需要依赖该状态实施抢占。</p>
<p>这里有几个问题需要澄清一下：</p>
<p>有sysmon监控线程来抢占剥夺，为什么这里还需要主动解除m和p之间的绑定关系呢？原因主要在于这里主动解除m和p的绑定关系之后，sysmon线程就不需要通过加锁或cas操作来修改m.p成员从而解除m和p之间的关系；</p>
<p>为什么要记录工作线程进入系统调用之前所绑定的p呢？因为记录下来可以让工作线程从系统调用返回之后快速找到一个可能可用的p，而不需要加锁从sched的pidle全局队列中去寻找空闲的p。</p>
<p>为什么要把进入系统调用之前所绑定的p搬到m的oldp中，而不是直接使用m的p成员？笔者第一次看到这里也有疑惑，于是翻看了github上的提交记录，从代码作者的提交注释来看，这里主要是从保持m的p成员清晰的语义方面考虑的，因为处于系统调用的m事实上并没有绑定p，所以如果记录在p成员中，p的语义并不够清晰明了。</p>
<p>看完进入系统调用之前调用的entersyscall函数后，我们再来看系统调用返回之后需要调用的exitsyscall函数。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">exitsyscall函数</span><br><span class="line"></span><br><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">2931</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// The goroutine g exited its system call.</span></span><br><span class="line"><span class="comment">// Arrange for it to run on a cpu again.</span></span><br><span class="line"><span class="comment">// This is called only from the go syscall library, not</span></span><br><span class="line"><span class="comment">// from the low-level system calls used by the runtime.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Write barriers are not allowed because our P may have been stolen.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="comment">//go:nowritebarrierrec</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">exitsyscall</span><span class="params">()</span></span> &#123;</span><br><span class="line">    _g_ := getg()</span><br><span class="line">    ......</span><br><span class="line">    oldp := _g_.m.oldp.ptr()  <span class="comment">//进入系统调用之前所绑定的p</span></span><br><span class="line">    _g_.m.oldp = <span class="number">0</span></span><br><span class="line">    <span class="keyword">if</span> exitsyscallfast(oldp) &#123;<span class="comment">//因为在进入系统调用之前已经解除了m和p之间的绑定，所以现在需要绑定p</span></span><br><span class="line">        <span class="comment">//绑定成功，设置一些状态</span></span><br><span class="line">        ......</span><br><span class="line">       </span><br><span class="line">        <span class="comment">// There's a cpu for us, so we can run.</span></span><br><span class="line">        _g_.m.p.ptr().syscalltick++  <span class="comment">//系统调用完成，增加syscalltick计数，sysmon线程依靠它判断是否是同一次系统调用</span></span><br><span class="line">        <span class="comment">// We need to cas the status and scan before resuming...</span></span><br><span class="line">        <span class="comment">//casgstatus函数会处理一些垃圾回收相关的事情，我们只需知道该函数重新把g设置成_Grunning状态即可</span></span><br><span class="line">        casgstatus(_g_, _Gsyscall, _Grunning)</span><br><span class="line">        ......</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">    _g_.m.locks--</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Call the scheduler.</span></span><br><span class="line">    <span class="comment">//没有绑定到p，调用mcall切换到g0栈执行exitsyscall0函数</span></span><br><span class="line">    mcall(exitsyscall0)</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>因为在进入系统调用之前，工作线程调用entersyscall解除了m和p之间的绑定，现在已经从系统调用返回需要重新绑定一个p才能继续运行go代码，所以exitsyscall函数首先就调用exitsyscallfast去尝试绑定一个空闲的p，如果绑定成功则结束exitsyscall函数按函数调用链原路返回去执行其它用户代码，否则则调用mcall函数切换到g0栈执行exitsyscall0函数。下面先来看exitsyscallfast如何尝试绑定一个p，然后再去分析exitsyscall0函数。</p>
<p>exitsyscallfast首先尝试绑定进入系统调用之前所使用的p，如果绑定失败就需要调用exitsyscallfast_pidle去获取空闲的p来绑定。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">3020</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">exitsyscallfast</span><span class="params">(oldp *p)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">    _g_ := getg()</span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">// Try to re-acquire the last P.</span></span><br><span class="line">    <span class="comment">//尝试快速绑定进入系统调用之前所使用的p</span></span><br><span class="line">    <span class="keyword">if</span> oldp != <span class="literal">nil</span> &amp;&amp; oldp.status == _Psyscall &amp;&amp; atomic.Cas(&amp;oldp.status, _Psyscall, _Pidle) &#123;</span><br><span class="line">        <span class="comment">//使用cas操作获取到p的使用权，所以之后的代码不需要使用锁就可以直接操作p</span></span><br><span class="line">        <span class="comment">// There's a cpu for us, so we can run.</span></span><br><span class="line">        wirep(oldp) <span class="comment">//绑定p</span></span><br><span class="line">        exitsyscallfast_reacquired()</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Try to get any other idle P.</span></span><br><span class="line">    <span class="keyword">if</span> sched.pidle != <span class="number">0</span> &#123;</span><br><span class="line">        <span class="keyword">var</span> ok <span class="keyword">bool</span></span><br><span class="line">        systemstack(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">            ok = exitsyscallfast_pidle()  <span class="comment">//从全局队列中寻找空闲的p，需要加锁，比较慢</span></span><br><span class="line">            ......</span><br><span class="line">        &#125;)</span><br><span class="line">        <span class="keyword">if</span> ok &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>exitsyscallfast首先尝试快速绑定进入系统调用之前所使用的p，因为该p的状态目前还是_Psyscall，监控线程此时可能也正好准备操作这个p的状态，所以这里需要使用cas原子操作来修改状态，保证只有一个线程的cas能够成功，一旦cas操作成功，就表示当前线程获取到了p的使用权，这样当前线程的后续代码就可以直接操作该p了。具体到exitsyscallfast函数，一旦我们拿到p的使用权，就调用wirep把工作线程m和p关联起来，完成绑定工作。所谓的绑定其实就是设置m的p成员指向p和p的m成员指向m。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">4099</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// wirep is the first step of acquirep, which actually associates the</span></span><br><span class="line"><span class="comment">// current M to _p_. This is broken out so we can disallow write</span></span><br><span class="line"><span class="comment">// barriers for this part, since we don't yet have a P.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//go:nowritebarrierrec</span></span><br><span class="line"><span class="comment">//go:nosplit</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">wirep</span><span class="params">(_p_ *p)</span></span> &#123;</span><br><span class="line">    _g_ := getg()</span><br><span class="line">    ......</span><br><span class="line">    <span class="comment">//相互赋值，绑定m和p</span></span><br><span class="line">    _g_.m.mcache = _p_.mcache</span><br><span class="line">    _g_.m.p.set(_p_)</span><br><span class="line">    _p_.m.set(_g_.m)</span><br><span class="line">    _p_.status = _Prunning</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>exitsyscallfast函数如果绑定进入系统调用之前所使用的p失败，则调用exitsyscallfast_pidle从p的全局空闲队列中获取一个p出来绑定，注意这里使用了systemstack(func())函数来调用exitsyscallfast_pidle，systemstack(func())函数有一个func()类型的参数，该函数首先会把栈切换到g0栈，然后调用通过参数传递进来的函数(这里是一个闭包，包含了对exitsyscallfast_pidle函数的调用)，最后再切换回原来的栈并返回，为什么这些代码需要在系统栈也就是g0的栈上执行呢？原则上来说，只要调用链上某个函数有nosplit这个编译器指示就需要在g0栈上去执行，因为有nosplit指示的话编译器就不会插入检查溢出的代码，这样在非g0栈上执行这些nosplit函数就有可能导致栈溢出，g0栈其实就是操作系统线程所使用的栈，它的空间比较大，不需要对runtime代码中的每个函数都做栈溢出检查，否则会严重影响效率。</p>
<p>为什么绑定进入系统调用之前所使用的p会失败呢？原因就在于这个p可能被sysmon监控线程拿走并绑定到其它工作线程，这部分内容我们已经在前面分析过了。</p>
<p>现在继续看exitsyscallfast_pidle函数，从代码可以看到从全局空闲队列获取p需要加锁，如果锁冲突比较严重的话，这个过程就很慢了，这也是为什么exitsyscallfast函数首先会去尝试绑定之前使用的p的原因。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">3083</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">exitsyscallfast_pidle</span><span class="params">()</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">    lock(&amp;sched.lock)</span><br><span class="line">    _p_ := pidleget()<span class="comment">//从全局空闲队列中获取p</span></span><br><span class="line">    <span class="keyword">if</span> _p_ != <span class="literal">nil</span> &amp;&amp; atomic.Load(&amp;sched.sysmonwait) != <span class="number">0</span> &#123;</span><br><span class="line">        atomic.Store(&amp;sched.sysmonwait, <span class="number">0</span>)</span><br><span class="line">        notewakeup(&amp;sched.sysmonnote)</span><br><span class="line">    &#125;</span><br><span class="line">    unlock(&amp;sched.lock)</span><br><span class="line">    <span class="keyword">if</span> _p_ != <span class="literal">nil</span> &#123;</span><br><span class="line">        acquirep(_p_)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>回到exitsyscall函数，如果exitsyscallfast绑定p失败，则调用mcall执行exitsyscall0函数，mcall我们已经见到过多次，所以这里只分析exitsyscall0函数。<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">runtime/proc.<span class="keyword">go</span> : <span class="number">3098</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// exitsyscall slow path on g0.</span></span><br><span class="line"><span class="comment">// Failed to acquire P, enqueue gp as runnable.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//go:nowritebarrierrec</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">exitsyscall0</span><span class="params">(gp *g)</span></span> &#123;</span><br><span class="line">    _g_ := getg()</span><br><span class="line"></span><br><span class="line">    casgstatus(gp, _Gsyscall, _Grunnable)</span><br><span class="line">   </span><br><span class="line">    <span class="comment">//当前工作线程没有绑定到p,所以需要解除m和g的关系</span></span><br><span class="line">    dropg()</span><br><span class="line">    lock(&amp;sched.lock)</span><br><span class="line">    <span class="keyword">var</span> _p_ *p</span><br><span class="line">    <span class="keyword">if</span> schedEnabled(_g_) &#123;</span><br><span class="line">        _p_ = pidleget() <span class="comment">//再次尝试获取空闲的p</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> _p_ == <span class="literal">nil</span> &#123; <span class="comment">//还是没有空闲的p</span></span><br><span class="line">        globrunqput(gp)  <span class="comment">//把g放入全局运行队列</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> atomic.Load(&amp;sched.sysmonwait) != <span class="number">0</span> &#123;</span><br><span class="line">        atomic.Store(&amp;sched.sysmonwait, <span class="number">0</span>)</span><br><span class="line">        notewakeup(&amp;sched.sysmonnote)</span><br><span class="line">    &#125;</span><br><span class="line">    unlock(&amp;sched.lock)</span><br><span class="line">    <span class="keyword">if</span> _p_ != <span class="literal">nil</span> &#123;<span class="comment">//获取到了p</span></span><br><span class="line">        acquirep(_p_) <span class="comment">//绑定p</span></span><br><span class="line">        <span class="comment">//继续运行g</span></span><br><span class="line">        execute(gp, <span class="literal">false</span>) <span class="comment">// Never returns.</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> _g_.m.lockedg != <span class="number">0</span> &#123;</span><br><span class="line">        <span class="comment">// Wait until another thread schedules gp and so m again.</span></span><br><span class="line">        stoplockedm()</span><br><span class="line">        execute(gp, <span class="literal">false</span>) <span class="comment">// Never returns.</span></span><br><span class="line">    &#125;</span><br><span class="line">    stopm()  <span class="comment">//当前工作线程进入睡眠，等待被其它线程唤醒</span></span><br><span class="line">   </span><br><span class="line">    <span class="comment">//从睡眠中被其它线程唤醒，执行schedule调度循环重新开始工作</span></span><br><span class="line">    schedule() <span class="comment">// Never returns.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>因为工作线程没有绑定p是不能运行goroutine的，所以这里会再次尝试从全局空闲队列找一个p出来绑定，找到了就通过execute函数继续执行当前这个goroutine，如果找不到则把当前goroutine放入全局运行队列，由其它工作线程负责把它调度起来运行，自己则调用stopm函数进入睡眠状态。execute和stopm函数我们已经分析过，所以这里就不再重复。</p>
<p>至此，我们已经分析完工作线程从系统调用返回需要做到，</p>
<p>小结</p>
<p>从上一节和本小节的分析我们可以看出，因运行时间过长与因系统调用时间过长而导致的抢占是有差别的：</p>
<p>对于运行时间过长的goroutine，系统监控线程首先会提出抢占请求，然后工作线程在适当的时候会去响应这个请求并暂停被抢占goroutine的运行，最后工作线程再调用schedule函数继续去调度其它goroutine；</p>
<p>而对于系统调用执行时间过长的goroutine，调度器并没有暂停其执行，只是剥夺了正在执行系统调用的工作线程所绑定的p，要等到工作线程从系统调用返回之后绑定p失败的情况下该goroutine才会真正被暂停运行。</p>
<p>思考</p>
<p>最后，我们用一个思考题来结束本专题，读者朋友可以思考一下当GOMAXPROCS等于1时，下面这个程序会输出什么？<br><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    <span class="string">"runtime"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">g2</span><span class="params">()</span></span> &#123;</span><br><span class="line">    sum := <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        sum++</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">go</span> g2()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        runtime.Gosched()</span><br><span class="line">        fmt.Println(<span class="string">"main is scheduled!"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>最后，如果你觉得本文对你有帮助的话，麻烦帮忙点一下文末右下角的 在看 或转发到朋友圈，非常感谢！<br><img src="https://img-blog.csdnimg.cn/img_convert/bb1306280e12ad8be259c63547ad1b1f.png" alt>​​​​</p>
]]></content>
      <categories>
        <category>go</category>
        <category>go语言调度器源代码情景分析</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go语言调度器源代码情景分析</tag>
      </tags>
  </entry>
  <entry>
    <title>万字长文 | 从实践到原理，带你参透 gRPC</title>
    <url>/go/%E7%9F%A5%E8%AF%86%E7%82%B9/%E4%BB%8E%E5%AE%9E%E8%B7%B5%E5%88%B0%E5%8E%9F%E7%90%86%EF%BC%8C%E5%B8%A6%E4%BD%A0%E5%8F%82%E9%80%8F%20gRPC/</url>
    <content><![CDATA[<p><a href="https://mp.weixin.qq.com/s/o-K7G9ywCdmW7et6Q4WMeA" rel="external nofollow noopener noreferrer" target="_blank">转载自煎鱼的blog</a></p>
<h2 id="万字长文-从实践到原理，带你参透-gRPC"><a href="#万字长文-从实践到原理，带你参透-gRPC" class="headerlink" title="万字长文 | 从实践到原理，带你参透 gRPC"></a>万字长文 | 从实践到原理，带你参透 gRPC</h2><p>原创 陈煎鱼 <a href="javascript:void(0" target="_blank" rel="noopener">脑子进煎鱼了</a>;) <em>2020-12-14</em></p>
<p>大家好，我是煎鱼。</p>
<p>gRPC 在 Go 语言中大放异彩，越来越多的小伙伴在使用，最近也在公司安利了一波，希望这一篇文章能带你一览 gRPC 的巧妙之处，本文篇幅比较长，请做好阅读准备。</p>
<p><img src="1.png" alt="图片"></p>
<p>本文目录如下：</p>
<p><img src="2.png" alt="图片"></p>
<h2 id="简述"><a href="#简述" class="headerlink" title="简述"></a>简述</h2><p>gRPC 是一个高性能、开源和通用的 RPC 框架，面向移动和 HTTP/2 设计。目前提供 C、Java 和 Go 语言版本，分别是：grpc, grpc-java, grpc-go. 其中 C 版本支持 C, C++, Node.js, Python, Ruby, Objective-C, PHP 和 C# 支持。</p>
<p>gRPC 基于 HTTP/2 标准设计，带来诸如双向流、流控、头部压缩、单 TCP 连接上的多复用请求等特性。这些特性使得其在移动设备上表现更好，更省电和节省空间占用。</p>
<h2 id="调用模型"><a href="#调用模型" class="headerlink" title="调用模型"></a>调用模型</h2><p><img src="3.png" alt="图片"></p>
<p>1、客户端（gRPC Stub）调用 A 方法，发起 RPC 调用。</p>
<p>2、对请求信息使用 Protobuf 进行对象序列化压缩（IDL）。</p>
<p>3、服务端（gRPC Server）接收到请求后，解码请求体，进行业务逻辑处理并返回。</p>
<p>4、对响应结果使用 Protobuf 进行对象序列化压缩（IDL）。</p>
<p>5、客户端接受到服务端响应，解码请求体。回调被调用的 A 方法，唤醒正在等待响应（阻塞）的客户端调用并返回响应结果。</p>
<h2 id="调用方式"><a href="#调用方式" class="headerlink" title="调用方式"></a>调用方式</h2><h3 id="一、Unary-RPC：一元-RPC"><a href="#一、Unary-RPC：一元-RPC" class="headerlink" title="一、Unary RPC：一元 RPC"></a>一、Unary RPC：一元 RPC</h3><p><img src="4.png" alt="图片"></p>
<h4 id="Server"><a href="#Server" class="headerlink" title="Server"></a>Server</h4><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">type SearchService struct&#123;&#125;</span><br><span class="line"></span><br><span class="line">func (s *SearchService) Search(ctx context.Context, r *pb.SearchRequest) (*pb.SearchResponse, error) &#123;</span><br><span class="line">    return &amp;pb.SearchResponse&#123;Response: r.GetRequest() + <span class="string">" Server"</span>&#125;, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const<span class="built_in"> PORT </span>= <span class="string">"9001"</span></span><br><span class="line"></span><br><span class="line">func main() &#123;</span><br><span class="line">   <span class="built_in"> server </span>:= grpc.NewServer()</span><br><span class="line">    pb.RegisterSearchServiceServer(server, &amp;SearchService&#123;&#125;)</span><br><span class="line"></span><br><span class="line">    lis, err := net.Listen(<span class="string">"tcp"</span>, <span class="string">":"</span>+PORT)</span><br><span class="line">    <span class="built_in">..</span>.</span><br><span class="line"></span><br><span class="line">    server.Serve(lis)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>创建 gRPC Server 对象，你可以理解为它是 Server 端的抽象对象。</li>
<li>将 SearchService（其包含需要被调用的服务端接口）注册到 gRPC Server。的内部注册中心。这样可以在接受到请求时，通过内部的 “服务发现”，发现该服务端接口并转接进行逻辑处理。</li>
<li>创建 Listen，监听 TCP 端口。</li>
<li>gRPC Server 开始 lis.Accept，直到 Stop 或 GracefulStop。</li>
</ul>
<h4 id="Client"><a href="#Client" class="headerlink" title="Client"></a>Client</h4><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">    conn, err := grpc.Dial(<span class="string">":"</span>+PORT, grpc.WithInsecure())</span><br><span class="line">    <span class="built_in">..</span>.</span><br><span class="line">    defer conn.Close()</span><br><span class="line"></span><br><span class="line">   <span class="built_in"> client </span>:= pb.NewSearchServiceClient(conn)</span><br><span class="line">    resp, err := client.Search(context.Background(), &amp;pb.SearchRequest&#123;</span><br><span class="line">        Request: <span class="string">"gRPC"</span>,</span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="built_in">..</span>.</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>创建与给定目标（服务端）的连接句柄。</li>
<li>创建 SearchService 的客户端对象。</li>
<li>发送 RPC 请求，等待同步响应，得到回调后返回响应结果。</li>
</ul>
<h3 id="二、Server-side-streaming-RPC：服务端流式-RPC"><a href="#二、Server-side-streaming-RPC：服务端流式-RPC" class="headerlink" title="二、Server-side streaming RPC：服务端流式 RPC"></a>二、Server-side streaming RPC：服务端流式 RPC</h3><p><img src="5.png" alt="图片"></p>
<h4 id="Server-1"><a href="#Server-1" class="headerlink" title="Server"></a>Server</h4><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *StreamService)</span> <span class="title">List</span><span class="params">(r *pb.StreamRequest, stream pb.StreamService_ListServer)</span> <span class="title">error</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> n := <span class="number">0</span>; n &lt;= <span class="number">6</span>; n++ &#123;</span><br><span class="line">        stream.Send(&amp;pb.StreamResponse&#123;</span><br><span class="line">            Pt: &amp;pb.StreamPoint&#123;</span><br><span class="line">                ...</span><br><span class="line">            &#125;,</span><br><span class="line">        &#125;)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="Client-1"><a href="#Client-1" class="headerlink" title="Client"></a>Client</h4><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">printLists</span><span class="params">(client pb.StreamServiceClient, r *pb.StreamRequest)</span> <span class="title">error</span></span> &#123;</span><br><span class="line">    stream, err := client.List(context.Background(), r)</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        resp, err := stream.Recv()</span><br><span class="line">        <span class="keyword">if</span> err == io.EOF &#123;</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="三、Client-side-streaming-RPC：客户端流式-RPC"><a href="#三、Client-side-streaming-RPC：客户端流式-RPC" class="headerlink" title="三、Client-side streaming RPC：客户端流式 RPC"></a>三、Client-side streaming RPC：客户端流式 RPC</h3><p><img src="6.png" alt="图片"></p>
<h4 id="Server-2"><a href="#Server-2" class="headerlink" title="Server"></a>Server</h4><figure class="highlight nimrod"><table><tr><td class="code"><pre><span class="line">func (s *<span class="type">StreamService</span>) <span class="type">Record</span>(stream pb.<span class="type">StreamService_RecordServer</span>) error &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        r, err := stream.<span class="type">Recv</span>()</span><br><span class="line">        <span class="keyword">if</span> err == io.<span class="type">EOF</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> stream.<span class="type">SendAndClose</span>(&amp;pb.<span class="type">StreamResponse</span>&#123;<span class="type">Pt</span>: &amp;pb.<span class="type">StreamPoint</span><span class="meta">&#123;...&#125;</span>&#125;)</span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="Client-2"><a href="#Client-2" class="headerlink" title="Client"></a>Client</h4><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">printRecord</span><span class="params">(client pb.StreamServiceClient, r *pb.StreamRequest)</span> <span class="title">error</span></span> &#123;</span><br><span class="line">    stream, err := client.Record(context.Background())</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> n := <span class="number">0</span>; n &lt; <span class="number">6</span>; n++ &#123;</span><br><span class="line">        stream.Send(r)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    resp, err := stream.CloseAndRecv()</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="四、Bidirectional-streaming-RPC：双向流式-RPC"><a href="#四、Bidirectional-streaming-RPC：双向流式-RPC" class="headerlink" title="四、Bidirectional streaming RPC：双向流式 RPC"></a>四、Bidirectional streaming RPC：双向流式 RPC</h3><p><img src="7.png" alt="图片"></p>
<h4 id="Server-3"><a href="#Server-3" class="headerlink" title="Server"></a>Server</h4><figure class="highlight nimrod"><table><tr><td class="code"><pre><span class="line">func (s *<span class="type">StreamService</span>) <span class="type">Route</span>(stream pb.<span class="type">StreamService_RouteServer</span>) error &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        stream.<span class="type">Send</span>(&amp;pb.<span class="type">StreamResponse</span><span class="meta">&#123;...&#125;</span>)</span><br><span class="line">        r, err := stream.<span class="type">Recv</span>()</span><br><span class="line">        <span class="keyword">if</span> err == io.<span class="type">EOF</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">nil</span></span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="Client-3"><a href="#Client-3" class="headerlink" title="Client"></a>Client</h4><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">printRoute</span><span class="params">(client pb.StreamServiceClient, r *pb.StreamRequest)</span> <span class="title">error</span></span> &#123;</span><br><span class="line">    stream, err := client.Route(context.Background())</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> n := <span class="number">0</span>; n &lt;= <span class="number">6</span>; n++ &#123;</span><br><span class="line">        stream.Send(r)</span><br><span class="line">        resp, err := stream.Recv()</span><br><span class="line">        <span class="keyword">if</span> err == io.EOF &#123;</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    stream.CloseSend()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="客户端与服务端是如何交互的"><a href="#客户端与服务端是如何交互的" class="headerlink" title="客户端与服务端是如何交互的"></a>客户端与服务端是如何交互的</h2><p>在开始分析之前，我们要先 gRPC 的调用有一个初始印象。那么最简单的就是对 Client 端调用 Server 端进行抓包去剖析，看看整个过程中它都做了些什么事。如下图：</p>
<p><img src="8.png" alt="图片"></p>
<ul>
<li>Magic</li>
<li>SETTINGS</li>
<li>HEADERS</li>
<li>DATA</li>
<li>SETTINGS</li>
<li>WINDOW_UPDATE</li>
<li>PING</li>
<li>HEADERS</li>
<li>DATA</li>
<li>HEADERS</li>
<li>WINDOW_UPDATE</li>
<li>PING</li>
</ul>
<p>我们略加整理发现共有十二个行为，是比较重要的。在开始分析之前，建议你自己先想一下，它们的作用都是什么？大胆猜测一下，带着疑问去学习效果更佳。</p>
<h3 id="行为分析"><a href="#行为分析" class="headerlink" title="行为分析"></a>行为分析</h3><h4 id="Magic"><a href="#Magic" class="headerlink" title="Magic"></a>Magic</h4><p><img src="9.png" alt="图片"></p>
<p>Magic 帧的主要作用是建立 HTTP/2 请求的前言。在 HTTP/2 中，要求两端都要发送一个连接前言，作为对所使用协议的最终确认，并确定 HTTP/2 连接的初始设置，客户端和服务端各自发送不同的连接前言。</p>
<p>而上图中的 Magic 帧是客户端的前言之一，内容为 <code>PRI * HTTP/2.0\r\n\r\nSM\r\n\r\n</code>，以确定启用 HTTP/2 连接。</p>
<h4 id="SETTINGS"><a href="#SETTINGS" class="headerlink" title="SETTINGS"></a>SETTINGS</h4><p><img src="10.png" alt="图片"></p>
<p>SETTINGS 帧的主要作用是设置这一个连接的参数，作用域是整个连接而并非单一的流。</p>
<p>而上图的 SETTINGS 帧都是空 SETTINGS 帧，图一是客户端连接的前言（Magic 和 SETTINGS 帧分别组成连接前言）。图二是服务端的。另外我们从图中可以看到多个 SETTINGS 帧，这是为什么呢？是因为发送完连接前言后，客户端和服务端还需要有一步互动确认的动作。对应的就是带有 ACK 标识 SETTINGS 帧。</p>
<h4 id="HEADERS"><a href="#HEADERS" class="headerlink" title="HEADERS"></a>HEADERS</h4><p><img src="11.png" alt="图片"></p>
<p>HEADERS 帧的主要作用是存储和传播 HTTP 的标头信息。我们关注到 HEADERS 里有一些眼熟的信息，分别如下：</p>
<ul>
<li>method：POST</li>
<li>scheme：http</li>
<li>path：/proto.SearchService/Search</li>
<li>authority：:10001</li>
<li>content-type：application/grpc</li>
<li>user-agent：grpc-go/1.20.0-dev</li>
</ul>
<p>你会发现这些东西非常眼熟，其实都是 gRPC 的基础属性，实际上远远不止这些，只是设置了多少展示多少。例如像平时常见的 <code>grpc-timeout</code>、<code>grpc-encoding</code> 也是在这里设置的。</p>
<h4 id="DATA"><a href="#DATA" class="headerlink" title="DATA"></a>DATA</h4><p><img src="12.png" alt="图片"></p>
<p>DATA 帧的主要作用是装填主体信息，是数据帧。而在上图中，可以很明显看到我们的请求参数 gRPC 存储在里面。只需要了解到这一点就可以了。</p>
<h4 id="HEADERS-DATA-HEADERS"><a href="#HEADERS-DATA-HEADERS" class="headerlink" title="HEADERS, DATA, HEADERS"></a>HEADERS, DATA, HEADERS</h4><p><img src="13.png" alt="图片"></p>
<p>在上图中 HEADERS 帧比较简单，就是告诉我们 HTTP 响应状态和响应的内容格式。</p>
<p><img src="14.png" alt="图片"></p>
<p>在上图中 DATA 帧主要承载了响应结果的数据集，图中的 gRPC Server 就是我们 RPC 方法的响应结果。</p>
<p><img src="15.png" alt="图片"></p>
<p>在上图中 HEADERS 帧主要承载了 gRPC 状态 和 gRPC 状态消息，图中的 <code>grpc-status</code> 和 <code>grpc-message</code> 就是我们的 gRPC 调用状态的结果。</p>
<h3 id="其它步骤"><a href="#其它步骤" class="headerlink" title="其它步骤"></a>其它步骤</h3><h4 id="WINDOW-UPDATE"><a href="#WINDOW-UPDATE" class="headerlink" title="WINDOW_UPDATE"></a>WINDOW_UPDATE</h4><p>主要作用是管理和流的窗口控制。通常情况下打开一个连接后，服务器和客户端会立即交换 SETTINGS 帧来确定流控制窗口的大小。默认情况下，该大小设置为约 65 KB，但可通过发出一个 WINDOW_UPDATE 帧为流控制设置不同的大小。</p>
<p><img src="16.png" alt="图片"></p>
<h4 id="PING-PONG"><a href="#PING-PONG" class="headerlink" title="PING/PONG"></a>PING/PONG</h4><p>主要作用是判断当前连接是否仍然可用，也常用于计算往返时间。其实也就是 PING/PONG，大家对此应该很熟。</p>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p><img src="17.png" alt="图片"></p>
<ul>
<li>在建立连接之前，客户端/服务端都会发送<strong>连接前言</strong>（Magic+SETTINGS），确立协议和配置项。</li>
<li>在传输数据时，是会涉及滑动窗口（WINDOW_UPDATE）等流控策略的。</li>
<li>传播 gRPC 附加信息时，是基于 HEADERS 帧进行传播和设置；而具体的请求/响应数据是存储的 DATA 帧中的。</li>
<li>请求/响应结果会分为 HTTP 和 gRPC 状态响应两种类型。</li>
<li>客户端发起 PING，服务端就会回应 PONG，反之亦可。</li>
</ul>
<p>这块 gRPC 的基础使用，你可以看看我另外的 《gRPC 入门系列》，相信对你一定有帮助。</p>
<h2 id="浅谈理解"><a href="#浅谈理解" class="headerlink" title="浅谈理解"></a>浅谈理解</h2><h3 id="服务端"><a href="#服务端" class="headerlink" title="服务端"></a>服务端</h3><p><img src="101.png" alt="图片"></p>
<p>为什么四行代码，就能够起一个 gRPC Server，内部做了什么逻辑。你有想过吗？接下来我们一步步剖析，看看里面到底是何方神圣。</p>
<h3 id="一、初始化"><a href="#一、初始化" class="headerlink" title="一、初始化"></a>一、初始化</h3><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// grpc.NewServer()</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">NewServer</span><span class="params">(opt ...ServerOption)</span> *<span class="title">Server</span></span> &#123;</span><br><span class="line"> opts := defaultServerOptions</span><br><span class="line"> <span class="keyword">for</span> _, o := <span class="keyword">range</span> opt &#123;</span><br><span class="line">  o(&amp;opts)</span><br><span class="line"> &#125;</span><br><span class="line"> s := &amp;Server&#123;</span><br><span class="line">  lis:    <span class="built_in">make</span>(<span class="keyword">map</span>[net.Listener]<span class="keyword">bool</span>),</span><br><span class="line">  opts:   opts,</span><br><span class="line">  conns:  <span class="built_in">make</span>(<span class="keyword">map</span>[io.Closer]<span class="keyword">bool</span>),</span><br><span class="line">  m:      <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="keyword">string</span>]*service),</span><br><span class="line">  quit:   <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">struct</span>&#123;&#125;),</span><br><span class="line">  done:   <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">struct</span>&#123;&#125;),</span><br><span class="line">  czData: <span class="built_in">new</span>(channelzData),</span><br><span class="line"> &#125;</span><br><span class="line"> s.cv = sync.NewCond(&amp;s.mu)</span><br><span class="line"> ...</span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> s</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这块比较简单，主要是实例 grpc.Server 并进行初始化动作。涉及如下：</p>
<ul>
<li>lis：监听地址列表。</li>
<li>opts：服务选项，这块包含 Credentials、Interceptor 以及一些基础配置。</li>
<li>conns：客户端连接句柄列表。</li>
<li>m：服务信息映射。</li>
<li>quit：退出信号。</li>
<li>done：完成信号。</li>
<li>czData：用于存储 ClientConn，addrConn 和 Server 的 channelz 相关数据。</li>
<li>cv：当优雅退出时，会等待这个信号量，直到所有 RPC 请求都处理并断开才会继续处理。</li>
</ul>
<h3 id="二、注册"><a href="#二、注册" class="headerlink" title="二、注册"></a>二、注册</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">pb</span><span class="selector-class">.RegisterSearchServiceServer</span>(<span class="selector-tag">server</span>, &amp;<span class="selector-tag">SearchService</span>&#123;&#125;)</span><br></pre></td></tr></table></figure>
<h4 id="步骤一：Service-API-interface"><a href="#步骤一：Service-API-interface" class="headerlink" title="步骤一：Service API interface"></a>步骤一：Service API interface</h4><figure class="highlight elm"><table><tr><td class="code"><pre><span class="line">// search.pb.go</span><br><span class="line"><span class="keyword">type</span> <span class="type">SearchServiceServer</span> interface &#123;</span><br><span class="line"> <span class="type">Search</span>(context.<span class="type">Context</span>, *<span class="type">SearchRequest</span>) (*<span class="type">SearchResponse</span>, error)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title">func</span> <span class="type">RegisterSearchServiceServer</span>(s *grpc.<span class="type">Server</span>, srv <span class="type">SearchServiceServer</span>) &#123;</span><br><span class="line"> s.<span class="type">RegisterService</span>(&amp;_SearchService_serviceDesc, srv)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>还记得我们平时编写的 Protobuf 吗？在生成出来的 <code>.pb.go</code> 文件中，会定义出 Service APIs interface 的具体实现约束。而我们在 gRPC Server 进行注册时，会传入应用 Service 的功能接口实现，此时生成的 <code>RegisterServer</code> 方法就会保证两者之间的一致性。</p>
<h4 id="步骤二：Service-API-IDL"><a href="#步骤二：Service-API-IDL" class="headerlink" title="步骤二：Service API IDL"></a>步骤二：Service API IDL</h4><p>你想乱传糊弄一下？不可能的，请乖乖定义与 Protobuf 一致的接口方法。但是那个 <code>&amp;_SearchService_serviceDesc</code> 又有什么作用呢？代码如下：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="comment">// search.pb.go</span></span><br><span class="line">var _SearchService_serviceDesc = grpc.ServiceDesc&#123;</span><br><span class="line"><span class="symbol"> ServiceName:</span> <span class="string">"proto.SearchService"</span>,</span><br><span class="line"><span class="symbol"> HandlerType:</span> (*SearchServiceServer)(nil),</span><br><span class="line"><span class="symbol"> Methods:</span> []grpc.MethodDesc&#123;</span><br><span class="line">  &#123;</span><br><span class="line"><span class="symbol">   MethodName:</span> <span class="string">"Search"</span>,</span><br><span class="line"><span class="symbol">   Handler:</span>    _SearchService_Search_Handler,</span><br><span class="line">  &#125;,</span><br><span class="line"> &#125;,</span><br><span class="line"><span class="symbol"> Streams:</span>  []grpc.StreamDesc&#123;&#125;,</span><br><span class="line"><span class="symbol"> Metadata:</span> <span class="string">"search.proto"</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这看上去像服务的描述代码，用来向内部表述 “我” 都有什么。涉及如下:</p>
<ul>
<li>ServiceName：服务名称</li>
<li>HandlerType：服务接口，用于检查用户提供的实现是否满足接口要求</li>
<li>Methods：一元方法集，注意结构内的 <code>Handler</code> 方法，其对应最终的 RPC 处理方法，在执行 RPC 方法的阶段会使用。</li>
<li>Streams：流式方法集</li>
<li>Metadata：元数据，是一个描述数据属性的东西。在这里主要是描述 <code>SearchServiceServer</code> 服务</li>
</ul>
<h4 id="步骤三：Register-Service"><a href="#步骤三：Register-Service" class="headerlink" title="步骤三：Register Service"></a>步骤三：Register Service</h4><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *Server)</span> <span class="title">register</span><span class="params">(sd *ServiceDesc, ss <span class="keyword">interface</span>&#123;&#125;)</span></span> &#123;</span><br><span class="line">    ...</span><br><span class="line"> srv := &amp;service&#123;</span><br><span class="line">  server: ss,</span><br><span class="line">  md:     <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="keyword">string</span>]*MethodDesc),</span><br><span class="line">  sd:     <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="keyword">string</span>]*StreamDesc),</span><br><span class="line">  mdata:  sd.Metadata,</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">for</span> i := <span class="keyword">range</span> sd.Methods &#123;</span><br><span class="line">  d := &amp;sd.Methods[i]</span><br><span class="line">  srv.md[d.MethodName] = d</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">for</span> i := <span class="keyword">range</span> sd.Streams &#123;</span><br><span class="line">  ...</span><br><span class="line"> &#125;</span><br><span class="line"> s.m[sd.ServiceName] = srv</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在最后一步中，我们会将先前的服务接口信息、服务描述信息给注册到内部 <code>service</code> 去，以便于后续实际调用的使用。涉及如下：</p>
<ul>
<li>server：服务的接口信息</li>
<li>md：一元服务的 RPC 方法集</li>
<li>sd：流式服务的 RPC 方法集</li>
<li>mdata：metadata，元数据</li>
</ul>
<h4 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h4><p>在这一章节中，主要介绍的是 gRPC Server 在启动前的整理和注册行为，看上去很简单，但其实一切都是为了后续的实际运行的预先准备。因此我们整理一下思路，将其串联起来看看，如下：</p>
<p><img src="18.png" alt="图片"></p>
<h3 id="三、监听"><a href="#三、监听" class="headerlink" title="三、监听"></a>三、监听</h3><p>接下来到了整个流程中，最重要也是大家最关注的监听/处理阶段，核心代码如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *Server)</span> <span class="title">Serve</span><span class="params">(lis net.Listener)</span> <span class="title">error</span></span> &#123;</span><br><span class="line"> ...</span><br><span class="line"> <span class="keyword">var</span> tempDelay time.Duration</span><br><span class="line"> <span class="keyword">for</span> &#123;</span><br><span class="line">  rawConn, err := lis.Accept()</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="keyword">if</span> ne, ok := err.(<span class="keyword">interface</span> &#123;</span><br><span class="line">    Temporary() <span class="keyword">bool</span></span><br><span class="line">   &#125;); ok &amp;&amp; ne.Temporary() &#123;</span><br><span class="line">    <span class="keyword">if</span> tempDelay == <span class="number">0</span> &#123;</span><br><span class="line">     tempDelay = <span class="number">5</span> * time.Millisecond</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">     tempDelay *= <span class="number">2</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> max := <span class="number">1</span> * time.Second; tempDelay &gt; max &#123;</span><br><span class="line">     tempDelay = max</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">    timer := time.NewTimer(tempDelay)</span><br><span class="line">    <span class="keyword">select</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> &lt;-timer.C:</span><br><span class="line">    <span class="keyword">case</span> &lt;-s.quit:</span><br><span class="line">     timer.Stop()</span><br><span class="line">     <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">continue</span></span><br><span class="line">   &#125;</span><br><span class="line">   ...</span><br><span class="line">   <span class="keyword">return</span> err</span><br><span class="line">  &#125;</span><br><span class="line">  tempDelay = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">  s.serveWG.Add(<span class="number">1</span>)</span><br><span class="line">  <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">   s.handleRawConn(rawConn)</span><br><span class="line">   s.serveWG.Done()</span><br><span class="line">  &#125;()</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Serve 会根据外部传入的 Listener 不同而调用不同的监听模式，这也是 <code>net.Listener</code> 的魅力，灵活性和扩展性会比较高。而在 gRPC Server 中最常用的就是 <code>TCPConn</code>，基于 TCP Listener 去做。接下来我们一起看看具体的处理逻辑，如下：</p>
<p><img src="19.png" alt="图片"></p>
<ul>
<li>循环处理连接，通过 <code>lis.Accept</code> 取出连接，如果队列中没有需处理的连接时，会形成阻塞等待。</li>
<li>若 <code>lis.Accept</code> 失败，则触发休眠机制，若为第一次失败那么休眠 5ms，否则翻倍，再次失败则不断翻倍直至上限休眠时间 1s，而休眠完毕后就会尝试去取下一个 “它”。</li>
<li>若 <code>lis.Accept</code> 成功，则重置休眠的时间计数和启动一个新的 goroutine 调用 <code>handleRawConn</code> 方法去执行/处理新的请求，也就是大家很喜欢说的 “每一个请求都是不同的 goroutine 在处理”。</li>
<li>在循环过程中，包含了 “退出” 服务的场景，主要是硬关闭和优雅重启服务两种情况。</li>
</ul>
<h2 id="客户端"><a href="#客户端" class="headerlink" title="客户端"></a>客户端</h2><p><img src="102.png" alt="图片"></p>
<h3 id="一、创建拨号连接"><a href="#一、创建拨号连接" class="headerlink" title="一、创建拨号连接"></a>一、创建拨号连接</h3><figure class="highlight vim"><table><tr><td class="code"><pre><span class="line">// grpc.Dial(<span class="string">":"</span>+PORT, grpc.WithInsecure())</span><br><span class="line">func DialContext(ctx context.Context, target <span class="built_in">string</span>, opts ...DialOption) (conn *ClientConn, err error) &#123;</span><br><span class="line"> <span class="keyword">cc</span> := &amp;ClientConn&#123;</span><br><span class="line">  targe<span class="variable">t:</span>            target,</span><br><span class="line">  csMgr:             &amp;connectivityStateManager&#123;&#125;,</span><br><span class="line">  conn<span class="variable">s:</span>             <span class="keyword">make</span>(<span class="keyword">map</span>[*addrConn]struct&#123;&#125;),</span><br><span class="line">  dopt<span class="variable">s:</span>             defaultDialOptions(),</span><br><span class="line">  blockingpicker:    newPickerWrapper(),</span><br><span class="line">  czDat<span class="variable">a:</span>            <span class="keyword">new</span>(channelzData),</span><br><span class="line">  firstResolveEven<span class="variable">t:</span> grpcsync.NewEvent(),</span><br><span class="line"> &#125;</span><br><span class="line"> ...</span><br><span class="line"> chainUnaryClientInterceptors(<span class="keyword">cc</span>)</span><br><span class="line"> chainStreamClientInterceptors(<span class="keyword">cc</span>)</span><br><span class="line"></span><br><span class="line"> ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>grpc.Dial</code> 方法实际上是对于 <code>grpc.DialContext</code> 的封装，区别在于 <code>ctx</code> 是直接传入 <code>context.Background</code>。其主要功能是<strong>创建</strong>与给定目标的客户端连接，其承担了以下职责：</p>
<ul>
<li>初始化 ClientConn</li>
<li>初始化（基于进程 LB）负载均衡配置</li>
<li>初始化 channelz</li>
<li>初始化重试规则和客户端一元/流式拦截器</li>
<li>初始化协议栈上的基础信息</li>
<li>相关 context 的超时控制</li>
<li>初始化并解析地址信息</li>
<li>创建与服务端之间的连接</li>
</ul>
<h4 id="连没连"><a href="#连没连" class="headerlink" title="连没连"></a>连没连</h4><p>之前听到有的人说调用 <code>grpc.Dial</code> 后客户端就已经与服务端建立起了连接，但这对不对呢？我们先鸟瞰全貌，看看正在跑的 goroutine。如下：</p>
<p><img src="20.png" alt="图片"></p>
<p>我们可以有几个核心方法一直在等待/处理信号，通过分析底层源码可得知。涉及如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(ac *addrConn)</span> <span class="title">connect</span><span class="params">()</span></span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(ac *addrConn)</span> <span class="title">resetTransport</span><span class="params">()</span></span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(ac *addrConn)</span> <span class="title">createTransport</span><span class="params">(addr resolver.Address, copts transport.ConnectOptions, connectDeadline time.Time)</span></span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(ac *addrConn)</span> <span class="title">getReadyTransport</span><span class="params">()</span></span></span><br></pre></td></tr></table></figure>
<p>在这里主要分析 goroutine 提示的 <code>resetTransport</code> 方法，看看都做了啥。核心代码如下：</p>
<figure class="highlight roboconf"><table><tr><td class="code"><pre><span class="line">func (ac *addrConn) resetTransport() &#123;</span><br><span class="line"> <span class="attribute">for i</span> := 0; ; <span class="attribute">i++ &#123;</span></span><br><span class="line"><span class="attribute">  if ac.state == connectivity.Shutdown &#123;</span></span><br><span class="line"><span class="attribute">   return</span></span><br><span class="line"><span class="attribute">  &#125;</span></span><br><span class="line"><span class="attribute">  ...</span></span><br><span class="line"><span class="attribute">  connectDeadline</span> := time<span class="variable">.Now</span>()<span class="variable">.Add</span>(dialDuration)</span><br><span class="line">  ac<span class="variable">.updateConnectivityState</span>(connectivity<span class="variable">.Connecting</span>)</span><br><span class="line">  newTr, addr, reconnect, err := ac<span class="variable">.tryAllAddrs</span>(addrs, connectDeadline)</span><br><span class="line">  if err != nil &#123;</span><br><span class="line">   if ac<span class="variable">.state</span> == connectivity<span class="variable">.Shutdown</span> &#123;</span><br><span class="line">    return</span><br><span class="line">   &#125;</span><br><span class="line">   ac<span class="variable">.updateConnectivityState</span>(connectivity<span class="variable">.TransientFailure</span>)</span><br><span class="line">   timer := time<span class="variable">.NewTimer</span>(backoffFor)</span><br><span class="line">   select &#123;</span><br><span class="line">   case &lt;-timer<span class="variable">.C</span>:</span><br><span class="line">    ...</span><br><span class="line">   &#125;</span><br><span class="line">   continue</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  if ac<span class="variable">.state</span> == connectivity<span class="variable">.Shutdown</span> &#123;</span><br><span class="line">   newTr<span class="variable">.Close</span>()</span><br><span class="line">   return</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line">  if !healthcheckManagingState &#123;</span><br><span class="line">   ac<span class="variable">.updateConnectivityState</span>(connectivity<span class="variable">.Ready</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">  if ac<span class="variable">.state</span> == connectivity<span class="variable">.Shutdown</span> &#123;</span><br><span class="line">   return</span><br><span class="line">  &#125;</span><br><span class="line">  ac<span class="variable">.updateConnectivityState</span>(connectivity<span class="variable">.TransientFailure</span>)</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在该方法中会不断地去尝试创建连接，若成功则结束。否则不断地根据 <code>Backoff</code> 算法的重试机制去尝试创建连接，直到成功为止。从结论上来讲，单纯调用 <code>DialContext</code> 是异步建立连接的，也就是并不是马上生效，处于 <code>Connecting</code> 状态，而正式下要到达 <code>Ready</code> 状态才可用。</p>
<h4 id="真的连了吗"><a href="#真的连了吗" class="headerlink" title="真的连了吗"></a>真的连了吗</h4><p><img src="21.png" alt="图片"></p>
<p>在抓包工具上提示一个包都没有，那么这算真正连接了吗？我认为这是一个表述问题，我们应该尽可能的严谨。如果你真的想通过 <code>DialContext</code> 方法就打通与服务端的连接，则需要调用 <code>WithBlock</code> 方法，虽然会导致阻塞等待，但最终连接会到达 <code>Ready</code> 状态（握手成功）。如下图：</p>
<p><img src="22.png" alt="图片"></p>
<h3 id="二、实例化-Service-API"><a href="#二、实例化-Service-API" class="headerlink" title="二、实例化 Service API"></a>二、实例化 Service API</h3><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">type SearchServiceClient<span class="built_in"> interface </span>&#123;</span><br><span class="line"> Search(ctx context.Context, <span class="keyword">in</span> *SearchRequest, opts <span class="built_in">..</span>.grpc.CallOption) (*SearchResponse, error)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">type searchServiceClient struct &#123;</span><br><span class="line"> cc *grpc.ClientConn</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func NewSearchServiceClient(cc *grpc.ClientConn) SearchServiceClient &#123;</span><br><span class="line"> return &amp;searchServiceClient&#123;cc&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这块就是实例 Service API interface，比较简单。</p>
<h3 id="三、调用"><a href="#三、调用" class="headerlink" title="三、调用"></a>三、调用</h3><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// search.pb.go</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(c *searchServiceClient)</span> <span class="title">Search</span><span class="params">(ctx context.Context, in *SearchRequest, opts ...grpc.CallOption)</span> <span class="params">(*SearchResponse, error)</span></span> &#123;</span><br><span class="line"> out := <span class="built_in">new</span>(SearchResponse)</span><br><span class="line"> err := c.cc.Invoke(ctx, <span class="string">"/proto.SearchService/Search"</span>, in, out, opts...)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">nil</span>, err</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">return</span> out, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>proto 生成的 RPC 方法更像是一个包装盒，把需要的东西放进去，而实际上调用的还是 <code>grpc.invoke</code> 方法。如下：</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">func invoke(ctx context.Context, method string, req, reply interface&#123;&#125;, <span class="keyword">cc</span> *ClientConn, opts ...CallOption) <span class="keyword">error</span> &#123;</span><br><span class="line"> <span class="keyword">cs</span>, <span class="keyword">err</span> := newClientStream(ctx, unaryStreamDesc, <span class="keyword">cc</span>, method, opts...)</span><br><span class="line"> <span class="keyword">if</span> <span class="keyword">err</span> != nil &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">if</span> <span class="keyword">err</span> := <span class="keyword">cs</span>.SendMsg(req); <span class="keyword">err</span> != nil &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">err</span></span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">return</span> <span class="keyword">cs</span>.RecvMsg(reply)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通过概览，可以关注到三块调用。如下：</p>
<ul>
<li>newClientStream：获取传输层 Trasport 并组合封装到 ClientStream 中返回，在这块会涉及负载均衡、超时控制、 Encoding、 Stream 的动作，与服务端基本一致的行为。</li>
<li>cs.SendMsg：发送 RPC 请求出去，但其并不承担等待响应的功能。</li>
<li>cs.RecvMsg：阻塞等待接受到的 RPC 方法响应结果。</li>
</ul>
<h4 id="连接"><a href="#连接" class="headerlink" title="连接"></a>连接</h4><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// clientconn.go</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(cc *ClientConn)</span> <span class="title">getTransport</span><span class="params">(ctx context.Context, failfast <span class="keyword">bool</span>, method <span class="keyword">string</span>)</span> <span class="params">(transport.ClientTransport, <span class="keyword">func</span>(balancer.DoneInfo)</span>, <span class="title">error</span>)</span> &#123;</span><br><span class="line"> t, done, err := cc.blockingpicker.pick(ctx, failfast, balancer.PickOptions&#123;</span><br><span class="line">  FullMethodName: method,</span><br><span class="line"> &#125;)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">nil</span>, <span class="literal">nil</span>, toRPCErr(err)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">return</span> t, done, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在 <code>newClientStream</code> 方法中，我们通过 <code>getTransport</code> 方法获取了 Transport 层中抽象出来的 ClientTransport 和 ServerTransport，实际上就是获取一个连接给后续 RPC 调用传输使用。</p>
<h3 id="四、关闭连接"><a href="#四、关闭连接" class="headerlink" title="四、关闭连接"></a>四、关闭连接</h3><figure class="highlight stata"><table><tr><td class="code"><pre><span class="line"><span class="comment">// conn.Close()</span></span><br><span class="line">func (<span class="keyword">cc</span> *ClientConn) <span class="keyword">Close</span>() <span class="keyword">error</span> &#123;</span><br><span class="line"> defer <span class="keyword">cc</span>.cancel()</span><br><span class="line">    ...</span><br><span class="line"> <span class="keyword">cc</span>.csMgr.updateState(connectivity.Shutdown)</span><br><span class="line">    ...</span><br><span class="line"> <span class="keyword">cc</span>.blockingpicker.<span class="keyword">close</span>()</span><br><span class="line"> <span class="keyword">if</span> rWrapper != nil &#123;</span><br><span class="line">  rWrapper.<span class="keyword">close</span>()</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">if</span> bWrapper != nil &#123;</span><br><span class="line">  bWrapper.<span class="keyword">close</span>()</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">for</span> <span class="keyword">ac</span> := <span class="keyword">range</span> conns &#123;</span><br><span class="line">  <span class="keyword">ac</span>.tearDown(ErrClientConnClosing)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">if</span> channelz.IsOn() &#123;</span><br><span class="line">  ...</span><br><span class="line">  channelz.AddTraceEvent(<span class="keyword">cc</span>.channelzID, ted)</span><br><span class="line">  channelz.RemoveEntry(<span class="keyword">cc</span>.channelzID)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">return</span> nil</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>该方法会取消 ClientConn 上下文，同时关闭所有底层传输。涉及如下：</p>
<ul>
<li>Context Cancel</li>
<li>清空并关闭客户端连接</li>
<li>清空并关闭解析器连接</li>
<li>清空并关闭负载均衡连接</li>
<li>添加跟踪引用</li>
<li>移除当前通道信息</li>
</ul>
<h2 id="Q-amp-A"><a href="#Q-amp-A" class="headerlink" title="Q&amp;A"></a>Q&amp;A</h2><h3 id="1-gRPC-Metadata-是通过什么传输？"><a href="#1-gRPC-Metadata-是通过什么传输？" class="headerlink" title="1. gRPC Metadata 是通过什么传输？"></a>1. gRPC Metadata 是通过什么传输？</h3><p><img src="23.png" alt="图片"></p>
<h3 id="2-调用-grpc-Dial-会真正的去连接服务端吗？"><a href="#2-调用-grpc-Dial-会真正的去连接服务端吗？" class="headerlink" title="2. 调用 grpc.Dial 会真正的去连接服务端吗？"></a>2. 调用 grpc.Dial 会真正的去连接服务端吗？</h3><p>会，但是是异步连接的，连接状态为正在连接。但如果你设置了 <code>grpc.WithBlock</code> 选项，就会阻塞等待（等待握手成功）。另外你需要注意，当未设置 <code>grpc.WithBlock</code> 时，ctx 超时控制对其无任何效果。</p>
<h3 id="3-调用-ClientConn-不-Close-会导致泄露吗？"><a href="#3-调用-ClientConn-不-Close-会导致泄露吗？" class="headerlink" title="3. 调用 ClientConn 不 Close 会导致泄露吗？"></a>3. 调用 ClientConn 不 Close 会导致泄露吗？</h3><p>会，除非你的客户端不是常驻进程，那么在应用结束时会被动地回收资源。但如果是常驻进程，你又真的忘记执行 <code>Close</code> 语句，会造成的泄露。如下图：</p>
<p><strong>3.1. 客户端</strong></p>
<p><img src="24.png" alt="图片"></p>
<p><strong>3.2. 服务端</strong></p>
<p><img src="25.png" alt="图片"></p>
<p><strong>3.3. TCP</strong></p>
<p><img src="26.png" alt="图片"></p>
<h3 id="4-不控制超时调用的话，会出现什么问题？"><a href="#4-不控制超时调用的话，会出现什么问题？" class="headerlink" title="4. 不控制超时调用的话，会出现什么问题？"></a>4. 不控制超时调用的话，会出现什么问题？</h3><p>短时间内不会出现问题，但是会不断积蓄泄露，积蓄到最后当然就是服务无法提供响应了。如下图：</p>
<p><img src="27.png" alt="图片"></p>
<h3 id="5-为什么默认的拦截器不可以传多个？"><a href="#5-为什么默认的拦截器不可以传多个？" class="headerlink" title="5. 为什么默认的拦截器不可以传多个？"></a>5. 为什么默认的拦截器不可以传多个？</h3><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">chainUnaryClientInterceptors</span><span class="params">(cc *ClientConn)</span></span> &#123;</span><br><span class="line"> interceptors := cc.dopts.chainUnaryInts</span><br><span class="line"> <span class="keyword">if</span> cc.dopts.unaryInt != <span class="literal">nil</span> &#123;</span><br><span class="line">  interceptors = <span class="built_in">append</span>([]UnaryClientInterceptor&#123;cc.dopts.unaryInt&#125;, interceptors...)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">var</span> chainedInt UnaryClientInterceptor</span><br><span class="line"> <span class="keyword">if</span> <span class="built_in">len</span>(interceptors) == <span class="number">0</span> &#123;</span><br><span class="line">  chainedInt = <span class="literal">nil</span></span><br><span class="line"> &#125; <span class="keyword">else</span> <span class="keyword">if</span> <span class="built_in">len</span>(interceptors) == <span class="number">1</span> &#123;</span><br><span class="line">  chainedInt = interceptors[<span class="number">0</span>]</span><br><span class="line"> &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  chainedInt = <span class="function"><span class="keyword">func</span><span class="params">(ctx context.Context, method <span class="keyword">string</span>, req, reply <span class="keyword">interface</span>&#123;&#125;, cc *ClientConn, invoker UnaryInvoker, opts ...CallOption)</span> <span class="title">error</span></span> &#123;</span><br><span class="line">   <span class="keyword">return</span> interceptors[<span class="number">0</span>](ctx, method, req, reply, cc, getChainUnaryInvoker(interceptors, <span class="number">0</span>, invoker), opts...)</span><br><span class="line">  &#125;</span><br><span class="line"> &#125;</span><br><span class="line"> cc.dopts.unaryInt = chainedInt</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当存在多个拦截器时，取的就是第一个拦截器。因此结论是允许传多个，但并没有用。</p>
<h3 id="6-真的需要用到多个拦截器的话，怎么办？"><a href="#6-真的需要用到多个拦截器的话，怎么办？" class="headerlink" title="6. 真的需要用到多个拦截器的话，怎么办？"></a>6. 真的需要用到多个拦截器的话，怎么办？</h3><p>可以使用 go-grpc-middleware 提供的 <code>grpc.UnaryInterceptor</code> 和 <code>grpc.StreamInterceptor</code> 链式方法，方便快捷省心。</p>
<p>单单会用还不行，我们再深剖一下，看看它是怎么实现的。核心代码如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">ChainUnaryClient</span><span class="params">(interceptors ...grpc.UnaryClientInterceptor)</span> <span class="title">grpc</span>.<span class="title">UnaryClientInterceptor</span></span> &#123;</span><br><span class="line"> n := <span class="built_in">len</span>(interceptors)</span><br><span class="line"> <span class="keyword">if</span> n &gt; <span class="number">1</span> &#123;</span><br><span class="line">  lastI := n - <span class="number">1</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">func</span><span class="params">(ctx context.Context, method <span class="keyword">string</span>, req, reply <span class="keyword">interface</span>&#123;&#125;, cc *grpc.ClientConn, invoker grpc.UnaryInvoker, opts ...grpc.CallOption)</span> <span class="title">error</span></span> &#123;</span><br><span class="line">   <span class="keyword">var</span> (</span><br><span class="line">    chainHandler grpc.UnaryInvoker</span><br><span class="line">    curI         <span class="keyword">int</span></span><br><span class="line">   )</span><br><span class="line"></span><br><span class="line">   chainHandler = <span class="function"><span class="keyword">func</span><span class="params">(currentCtx context.Context, currentMethod <span class="keyword">string</span>, currentReq, currentRepl <span class="keyword">interface</span>&#123;&#125;, currentConn *grpc.ClientConn, currentOpts ...grpc.CallOption)</span> <span class="title">error</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> curI == lastI &#123;</span><br><span class="line">     <span class="keyword">return</span> invoker(currentCtx, currentMethod, currentReq, currentRepl, currentConn, currentOpts...)</span><br><span class="line">    &#125;</span><br><span class="line">    curI++</span><br><span class="line">    err := interceptors[curI](currentCtx, currentMethod, currentReq, currentRepl, currentConn, chainHandler, currentOpts...)</span><br><span class="line">    curI--</span><br><span class="line">    <span class="keyword">return</span> err</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">return</span> interceptors[<span class="number">0</span>](ctx, method, req, reply, cc, chainHandler, opts...)</span><br><span class="line">  &#125;</span><br><span class="line"> &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当拦截器数量大于 1 时，从 <code>interceptors[1]</code> 开始递归，每一个递归的拦截器 <code>interceptors[i]</code> 会不断地执行，最后才真正的去执行 <code>handler</code> 方法。同时也经常有人会问拦截器的执行顺序是什么，通过这段代码你得出结论了吗？</p>
<h3 id="7-频繁创建-ClientConn-有什么问题？"><a href="#7-频繁创建-ClientConn-有什么问题？" class="headerlink" title="7. 频繁创建 ClientConn 有什么问题？"></a>7. 频繁创建 ClientConn 有什么问题？</h3><p>这个问题我们可以反向验证一下，假设不公用 ClientConn 看看会怎么样？如下:</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">BenchmarkSearch</span><span class="params">(b *testing.B)</span></span> &#123;</span><br><span class="line"> <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; b.N; i++ &#123;</span><br><span class="line">  conn, err := GetClientConn()</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   b.Errorf(<span class="string">"GetClientConn err: %v"</span>, err)</span><br><span class="line">  &#125;</span><br><span class="line">  _, err = Search(context.Background(), conn)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   b.Errorf(<span class="string">"Search err: %v"</span>, err)</span><br><span class="line">  &#125;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>输出结果：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">    <span class="built_in">..</span>.<span class="built_in"> connection </span>error: desc = <span class="string">"transport: Error while dialing dial tcp :10001: socket: too many open files"</span></span><br><span class="line">    <span class="built_in">..</span>.<span class="built_in"> connection </span>error: desc = <span class="string">"transport: Error while dialing dial tcp :10001: socket: too many open files"</span></span><br><span class="line">    <span class="built_in">..</span>.<span class="built_in"> connection </span>error: desc = <span class="string">"transport: Error while dialing dial tcp :10001: socket: too many open files"</span></span><br><span class="line">    <span class="built_in">..</span>.<span class="built_in"> connection </span>error: desc = <span class="string">"transport: Error while dialing dial tcp :10001: socket: too many open files"</span></span><br><span class="line">FAIL</span><br><span class="line">exit status 1</span><br></pre></td></tr></table></figure>
<p>当你的应用场景是存在高频次同时生成/调用 ClientConn 时，可能会导致系统的文件句柄占用过多。这种情况下你可以变更应用程序生成/调用 ClientConn 的模式，又或是池化它，这块可以参考 grpc-go-pool 项目。</p>
<h3 id="8-客户端请求失败后会默认重试吗？"><a href="#8-客户端请求失败后会默认重试吗？" class="headerlink" title="8. 客户端请求失败后会默认重试吗？"></a>8. 客户端请求失败后会默认重试吗？</h3><p>会不断地进行重试，直到上下文取消。而重试时间方面采用 backoff 算法作为的重连机制，默认的最大重试时间间隔是 120s。</p>
<h3 id="9-为什么要用-HTTP-2-作为传输协议？"><a href="#9-为什么要用-HTTP-2-作为传输协议？" class="headerlink" title="9. 为什么要用 HTTP/2 作为传输协议？"></a>9. 为什么要用 HTTP/2 作为传输协议？</h3><p>许多客户端要通过 HTTP 代理来访问网络，gRPC 全部用 HTTP/2 实现，等到代理开始支持 HTTP/2 就能透明转发 gRPC 的数据。不光如此，负责负载均衡、访问控制等等的反向代理都能无缝兼容 gRPC，比起自己设计 wire protocol 的 Thrift，这样做科学不少。@ctiller @滕亦飞</p>
<h3 id="10-在-Kubernetes-中-gRPC-负载均衡有问题？"><a href="#10-在-Kubernetes-中-gRPC-负载均衡有问题？" class="headerlink" title="10. 在 Kubernetes 中 gRPC 负载均衡有问题？"></a>10. 在 Kubernetes 中 gRPC 负载均衡有问题？</h3><p>gRPC 的 RPC 协议是基于 HTTP/2 标准实现的，HTTP/2 的一大特性就是不需要像 HTTP/1.1 一样，每次发出请求都要重新建立一个新连接，而是会复用原有的连接。</p>
<p>所以这将导致 kube-proxy 只有在连接建立时才会做负载均衡，而在这之后的每一次 RPC 请求都会利用原本的连接，那么实际上后续的每一次的 RPC 请求都跑到了同一个地方。</p>
<p>注：使用 k8s service 做负载均衡的情况下</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li>gRPC 基于 HTTP/2 + Protobuf。</li>
<li>gRPC 有四种调用方式，分别是一元、服务端/客户端流式、双向流式。</li>
<li>gRPC 的附加信息都会体现在 HEADERS 帧，数据在 DATA 帧上。</li>
<li>Client 请求若使用 grpc.Dial 默认是异步建立连接，当时状态为 Connecting。</li>
<li>Client 请求若需要同步则调用 WithBlock()，完成状态为 Ready。</li>
<li>Server 监听是循环等待连接，若没有则休眠，最大休眠时间 1s；若接收到新请求则起一个新的 goroutine 去处理。</li>
<li>grpc.ClientConn 不关闭连接，会导致 goroutine 和 Memory 等泄露。</li>
<li>任何内/外调用如果不加超时控制，会出现泄漏和客户端不断重试。</li>
<li>特定场景下，如果不对 grpc.ClientConn 加以调控，会影响调用。</li>
<li>拦截器如果不用 go-grpc-middleware 链式处理，会覆盖。</li>
<li>在选择 gRPC 的负载均衡模式时，需要谨慎。</li>
</ul>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul>
<li><a href="http://doc.oschina.net/grpc" rel="external nofollow noopener noreferrer" target="_blank">http://doc.oschina.net/grpc</a></li>
<li><a href="https://github.com/grpc/grpc/blob/master/doc/PROTOCOL-HTTP2.md" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/grpc/grpc/blob/master/doc/PROTOCOL-HTTP2.md</a></li>
<li><a href="https://juejin.im/post/5b88a4f56fb9a01a0b31a67e" rel="external nofollow noopener noreferrer" target="_blank">https://juejin.im/post/5b88a4f56fb9a01a0b31a67e</a></li>
<li><a href="https://www.ibm.com/developerworks/cn/web/wa-http2-under-the-hood/index.html" rel="external nofollow noopener noreferrer" target="_blank">https://www.ibm.com/developerworks/cn/web/wa-http2-under-the-hood/index.html</a></li>
<li><a href="https://github.com/grpc/grpc-go/issues/1953" rel="external nofollow noopener noreferrer" target="_blank">https://github.com/grpc/grpc-go/issues/1953</a></li>
<li><a href="https://www.zhihu.com/question/52670041" rel="external nofollow noopener noreferrer" target="_blank">https://www.zhihu.com/question/52670041</a></li>
</ul>
]]></content>
      <categories>
        <category>go</category>
        <category>go知识点</category>
      </categories>
      <tags>
        <tag>go</tag>
        <tag>go知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>史上最完整的《指环王》魔戒战争大事记（编年体长文）</title>
    <url>/%E5%85%B4%E8%B6%A3%E7%88%B1%E5%A5%BD/%E5%8F%B2%E4%B8%8A%E6%9C%80%E5%AE%8C%E6%95%B4%E7%9A%84%E3%80%8A%E6%8C%87%E7%8E%AF%E7%8E%8B%E3%80%8B%E9%AD%94%E6%88%92%E6%88%98%E4%BA%89%E5%A4%A7%E4%BA%8B%E8%AE%B0/</url>
    <content><![CDATA[<p><a href="https://www.toutiao.com/i6872919198092231179/" rel="external nofollow noopener noreferrer" target="_blank">转载自</a></p>
<h1 id="史上最完整的《指环王》魔戒战争大事记（编年体长文）"><a href="#史上最完整的《指环王》魔戒战争大事记（编年体长文）" class="headerlink" title="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></a>史上最完整的《指环王》魔戒战争大事记（编年体长文）</h1><p>原创跃马酒馆小招待2020-09-16 13:33:12</p>
<p>大家好，本文承接上一篇文章对<strong>《指环王》</strong>历史的梳理，这次从更加激动人心的第二纪元开始将整个魔戒故事以编年体的方式进行叙述。</p>
<p><strong>（如果第一次打开这个系列的朋友建议从前**</strong>一片<strong>**文章开始看，可能会有更好的体验。）</strong></p>
<p><a href="https://www.toutiao.com/i6870476899894690315/?group_id=6870476899894690315" rel="external nofollow noopener noreferrer" target="_blank">史上最完整的《指环王》上古时期大事记（编年体长文）</a></p>
<h1 id="第二纪元-1-30年"><a href="#第二纪元-1-30年" class="headerlink" title="第二纪元 1-30年"></a>第二纪元 1-30年</h1><p>沉没的<strong>贝烈瑞安德</strong>地区让整个中土大陆的地貌发生了极大改变，沉没的过程大概持续了一个太阳年，期间在<strong>维林诺</strong>精灵的帮助下，许多<strong>贝烈瑞安德</strong>地区的居民，包括精灵、人类和矮人都不得不向东迁徙。原先位于大陆中部的<strong>蓝色山脉</strong>如今只剩下了一部分，坐落于大陆西侧临海的位置，并且从中间被<strong>隆恩河</strong>截断。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/503e47053aaf47458dd9104af76f7d2a?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>蓝色为沉没的贝烈瑞安德地区</p>
<p>部分<strong>辛达族精灵</strong>在<strong>“造船者”奇尔丹</strong>的带领下沿着隆恩河湾修建了数个港口城市，一部分尚未西渡的<strong>伊甸人类</strong>也和他们在一起，因为他们需要精灵们的造船技术来为其建造能够穿越大洋，前往努曼诺尔岛的大船。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/05dbdb7d3fae4a8fa3b41e225bafd157?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>隆恩河湾著名的灰港</p>
<p>另一部分<strong>辛达族精灵</strong>和<strong>诺多族精灵</strong>一道在<strong>林顿</strong>地区居住了下来，由诺多族精灵<strong>至高王吉尔-加拉德</strong>领导，但许多<strong>辛达族精灵</strong>对过去<strong>费诺众子</strong>的行为心存芥蒂，所以两者的相处并不融洽，这也为后来<strong>辛达族精灵</strong>的出走留下了伏笔。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/df02d799bfe54186b371823d60bcc238?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>绿色为早期的精灵活动区</p>
<h1 id="第二纪元-32年"><a href="#第二纪元-32年" class="headerlink" title="第二纪元 32年"></a>第二纪元 32年</h1><p>大部分<strong>伊甸人</strong>完成了西渡，居住于大海中央的<strong>努曼诺尔岛</strong>，<strong>埃尔洛斯</strong>成为第一任<strong>努曼诺尔国王</strong>。</p>
<h1 id="第二纪元-40年"><a href="#第二纪元-40年" class="headerlink" title="第二纪元 40年"></a>第二纪元 40年</h1><p><strong>火须</strong>和<strong>宽梁氏族</strong>的矮人在东迁的过程中决定前往<strong>卡扎督姆</strong>投奔他们的血亲<strong>长须氏族</strong>。这两个氏族给<strong>卡扎督姆</strong>带去的不仅是大量的人口，还有先进的锻造、雕刻、铸造、冶炼方面的技艺，再加上<strong>卡扎督姆</strong>地底发现的<strong>秘银</strong>矿脉，很快这里便成为中土矮人文明的中心，盛极一时。</p>
<h1 id="第二纪元-442年"><a href="#第二纪元-442年" class="headerlink" title="第二纪元 442年"></a>第二纪元 442年</h1><p><strong>埃尔洛斯国王</strong>去世，在位410年，其寿命超过500岁。埃尔洛斯是最长寿的<strong>努曼诺尔人</strong>，其后代，即努曼诺尔王室成员的平均寿命大约为400岁，普通努曼诺尔人平均寿命大约200岁。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/20250e9f3e2348c781f648f4144bfe3f?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>埃尔洛斯国王</p>
<p>安逸和平的岛上生活让他们繁衍后代的欲望大大加强，期间又鲜有人死去，使得岛上的人口迅速膨胀。好在这座维拉升起的岛屿十分肥沃，能够养得起数量庞大的努曼诺尔人。</p>
<p>不同于精灵，人类对于权利和金钱要更加贪婪，等到第一代从中土迁徙而来的努曼诺尔人死去之后，第二第三代的人很快就不再满足于蜗居于这个大洋中间的小岛，渴望向外探索。</p>
<h1 id="第二纪元-500年"><a href="#第二纪元-500年" class="headerlink" title="第二纪元 500年"></a>第二纪元 500年</h1><p><strong>索伦</strong>悄悄潜回中土，在<strong>伊菲尔督斯</strong>（后来的阴影山脉）东面鲜有人烟的地方住了下来。这片区域三面都是巨大的山脉，交通不便且土地贫瘠，很少有智慧生物愿意在这里生活。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/ef110e4c18bf4162af09acb022a10574?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>魔多地形</p>
<h1 id="第二纪元-598年"><a href="#第二纪元-598年" class="headerlink" title="第二纪元 598年"></a>第二纪元 598年</h1><p><strong>努曼诺尔王室</strong>终于下定决心出海，由于被明确禁止航向<strong>维林诺</strong>，他们只能将出海的目的地选在中土，由舰队统帅<strong>维安图尔</strong>率领一支很小的船队从<strong>努曼诺尔</strong>出发，并在两年后成功抵达<strong>林顿</strong>地区的外海。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/41ca5400fcd24bf1bc05ab6a4c1b821d?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>努曼诺尔岛的位置</p>
<p>在那里，他们受到了精灵们的欢迎，并被带到<strong>灰港</strong>受到<strong>奇尔丹</strong>的接见。虽然两者语言不通，但相互都能感受到对方的热情，对于精灵来说这是数百年前一同对抗<strong>魔苟斯</strong>的同伴；对于人类来说，精灵们是祖辈口中曾经对他们施以帮助的恩人。</p>
<p><strong>维安图尔</strong>一行人还在<strong>灰港</strong>遇到了一些人类，他们是第一纪元末不愿意放弃中土生活航向<strong>努曼诺尔岛</strong>的<strong>伊甸人</strong>，虽然他们并不像努曼诺尔人一样长寿，但也并不像历史书里记载的那样：<strong>留在中土的伊甸人都是魔苟斯的爪牙</strong>。</p>
<p>从这里开始，<strong>努曼诺尔人</strong>对中土的探索便频繁了起来，相互的交流日渐增多，这条海上航路成了繁忙的贸易通道。</p>
<h1 id="第二纪元-700年"><a href="#第二纪元-700年" class="headerlink" title="第二纪元 700年"></a>第二纪元 700年</h1><p>诺多精灵贵族<strong>凯兰崔尔夫人</strong>厌倦了在<strong>林顿</strong>的生活，与丈夫<strong>凯勒博恩</strong>（<strong>辛达族精灵</strong>）一同前往东方游历，一路上有相当多的同伴加入他们，其中不乏许多<strong>辛达族</strong>和<strong>西尔凡族</strong>的精灵。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/f44641758e184a149dd9875d8c9a0d82?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>凯兰崔尔夫人</p>
<p>他们在夏尔地区逗留了一段时间，当时<strong>凯兰崔尔</strong>对东方渐渐兴起的邪恶力量感到不安，尽管她不能确定到底是什么盘踞在那里，还是让她感觉自己有责任组织起一支力量对抗邪恶。</p>
<p>最终他们在<strong>迷雾山脉</strong>西侧的<strong>埃瑞吉安</strong>（即后来的<strong>瑞文戴尔</strong>）安住了下来，那里是著名的<strong>珠宝匠行会</strong>的所在地。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/1aa5f9c5ed0940c78a81d415bcff7da7?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>绿色为埃瑞吉安地区</p>
<h1 id="第二纪元-858年"><a href="#第二纪元-858年" class="headerlink" title="第二纪元 858年"></a>第二纪元 858年</h1><p><strong>埃瑞吉安</strong>的诺多族精灵领主<strong>凯勒布理鹏</strong>被推举为珠宝匠行会的会长，他亦是<strong>精灵宝钻</strong>的铸造者<strong>费诺</strong>的孙子。</p>
<p><strong>埃瑞吉安</strong>与矮人城市<strong>卡扎督姆</strong>相距不远，对于铸造技艺的共同爱好使得双方的关系还算融洽，<strong>凯勒布理鹏</strong>还与<strong>卡扎督姆</strong>的矮人工匠配合打造了著名的<strong>卡扎督姆西门</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/942a48bfd85a4154a9edf4250e93fe32?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>卡扎督姆西门</p>
<h1 id="第二纪元-935年"><a href="#第二纪元-935年" class="headerlink" title="第二纪元 935年"></a>第二纪元 935年</h1><p><strong>林顿</strong>地区的<strong>辛达族精灵</strong>一直渴望能有一个自己的国度，于是在贵族<strong>欧洛斐尔</strong>和其子<strong>瑟兰督伊</strong>的带领下，<strong>辛达族精灵</strong>集体东迁。辗转了一些地方后他们翻越了高山隘口，在迷雾山脉东侧的大绿林中定居下来。<strong>辛达族精灵</strong>与原本就生活在这里的<strong>西尔凡精灵</strong>混居，<strong>欧洛斐尔</strong>成为第一任<strong>林地之王</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/29f4c1149ba646f2818acdf480673ed5?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>瑟兰督伊</p>
<h1 id="第二纪元-约1000年"><a href="#第二纪元-约1000年" class="headerlink" title="第二纪元 约1000年"></a>第二纪元 约1000年</h1><p><strong>索伦</strong>感觉自己蛰伏的时间足够久，开始正式经营<strong>魔多</strong>，昔日魔苟斯手下的邪恶又一次开始聚集到一起，<strong>黑塔巴拉督尔</strong>开始修建。</p>
<h1 id="第二纪元-1200年"><a href="#第二纪元-1200年" class="headerlink" title="第二纪元 1200年"></a>第二纪元 1200年</h1><p><strong>索伦</strong>化身美丽、善良的精灵贵族<strong>“安纳塔”</strong>潜入精灵社会，希望借此挑拨精灵族群内部相互之间的矛盾。起初他的目的地是<strong>林顿</strong>，但是他的行为被至高王<strong>吉尔-加拉德</strong>所不齿，直接遭到驱逐。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/f6bd89ba47ad4bc5a5ff03086769f85c?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>真善美帅哥安纳塔</p>
<h1 id="第二纪元-1205年"><a href="#第二纪元-1205年" class="headerlink" title="第二纪元 1205年"></a>第二纪元 1205年</h1><p><strong>努曼诺尔人</strong>开始在林顿地区的南部，<strong>灰泛河</strong>入海口修建永久性的港城<strong>隆德戴尔</strong>。</p>
<h1 id="第二纪元-1210年"><a href="#第二纪元-1210年" class="headerlink" title="第二纪元 1210年"></a>第二纪元 1210年</h1><p><strong>安纳塔</strong>来到<strong>埃瑞吉安</strong>，他展现出对锻造技艺的非凡理解让<strong>凯勒布理鹏</strong>为首的<strong>珠宝匠行会</strong>推崇备至，不过<strong>凯兰崔尔</strong>并不信任这个有着美丽外表的神秘精灵，但她也无法看穿<strong>安纳塔</strong>的真实身份。</p>
<h1 id="第二纪元-1350年"><a href="#第二纪元-1350年" class="headerlink" title="第二纪元 1350年"></a>第二纪元 1350年</h1><p>在<strong>安纳塔</strong>的蛊惑下，<strong>凯勒布理鹏</strong>逐渐将<strong>埃瑞吉安</strong>的权利一手包揽，甚至还驱逐了一些反对他这样做的贵族。面对这种情形，<strong>凯兰崔尔</strong>和<strong>凯勒博恩</strong>尽管深感不安，但是别无选择，只有带领自己的追随者再次东迁，他们后来穿过了矮人城市<strong>卡扎督姆</strong>，进入迷雾山脉东侧的<strong>洛丝罗瑞恩</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/57e4e1c71726466d99a4e29a21cc9028?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>绿色为洛丝罗瑞恩地区</p>
<h1 id="第二纪元-约1400年"><a href="#第二纪元-约1400年" class="headerlink" title="第二纪元 约1400年"></a>第二纪元 约1400年</h1><p><strong>努曼诺尔人</strong>的商船已经遍布整个中土的港口城市。但是人类的贪婪在此时体现出来，他们将生活在中土的人视为低等人类，开始在这里建立大范围的殖民地，强迫他们劳作。</p>
<h1 id="第二纪元-1500年"><a href="#第二纪元-1500年" class="headerlink" title="第二纪元 1500年"></a>第二纪元 1500年</h1><p>使用<strong>安纳塔</strong>传授的铸造技术，<strong>凯勒布理鹏</strong>和<strong>珠宝匠行会</strong>的精灵们打造出十六枚<strong>力量之戒</strong>（即矮人七戒和人类九戒），<strong>索伦</strong>此时悄悄离开了<strong>埃瑞吉安</strong>，前往<strong>魔多</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/54cbec8d143b46d48a695a2413dc2346?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第二纪元-1590年"><a href="#第二纪元-1590年" class="headerlink" title="第二纪元 1590年"></a>第二纪元 1590年</h1><p><strong>凯勒布理鹏</strong>又偷偷打造了三枚更为强大的<strong>力量之戒</strong>，分别为：<strong>水戒维雅</strong>、<strong>风戒能雅</strong>、<strong>火戒纳雅</strong>，又称<strong>“精灵三戒”</strong>。虽然他使用了<strong>安纳塔</strong>教授的技艺，但是铸造时<strong>安纳塔</strong>并不在场，所以后来索伦对这三枚戒指的影响有限。</p>
<h1 id="第二纪元-1600年"><a href="#第二纪元-1600年" class="headerlink" title="第二纪元 1600年"></a>第二纪元 1600年</h1><p><strong>索伦</strong>在<strong>魔多</strong>境内的<strong>末日火山</strong>中铸造了<strong>至尊魔戒</strong>，并在其中灌注了自己的灵魂，其力量可以让<strong>索伦</strong>得以控制其他的<strong>力量之戒</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/c12c3922c5894a15a4f4b27bd5a93dea?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>至尊魔戒</p>
<p>同年<strong>黑塔巴拉督尔</strong>建成，索伦在中土的行动开始公开化。</p>
<h1 id="第二纪元-1605年"><a href="#第二纪元-1605年" class="headerlink" title="第二纪元 1605年"></a>第二纪元 1605年</h1><p><strong>凯勒布理鹏</strong>和其他两位精灵贵族戴上<strong>精灵三戒</strong>的时候立即发觉了异样，他们能够感觉到有更强大的力量在影响三戒的佩戴者，他们试图摧毁三戒却没能做到。于是<strong>凯勒布理鹏</strong>将其中的两枚，<strong>维雅</strong>和<strong>纳雅</strong>送到林顿，交由<strong>吉尔-加拉德</strong>保管，<strong>能雅</strong>则送到<strong>洛丝罗瑞恩</strong>，由<strong>凯兰崔尔夫人</strong>保管。</p>
<h1 id="第二纪元-1693-1697年"><a href="#第二纪元-1693-1697年" class="headerlink" title="第二纪元 1693-1697年"></a>第二纪元 1693-1697年</h1><p><strong>索伦</strong>积极备战，打算入侵<strong>埃瑞吉安</strong>。<strong>林顿</strong>精灵由<strong>埃尔隆德</strong>带领，他们与<strong>凯勒博恩</strong>带领的罗瑞恩精灵汇合，一同前往支援<strong>埃瑞吉安</strong>，但是由于力量相差悬殊，精灵军队被击溃。1697年，兽人大军攻陷<strong>珠宝匠行会</strong>的所在地，<strong>凯勒布理鹏</strong>被生擒。</p>
<p>受尽折磨的<strong>凯勒布理鹏</strong>交出了十六枚<strong>力量之戒</strong>，但是拒绝说出<strong>精灵三戒</strong>的下落，最后他被<strong>索伦</strong>的手下杀死，尸体也遭到侮辱，<strong>费诺</strong>的血脉就此终结。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/285e9e5f55c8446a963cfb57f76ad0d9?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>折磨凯勒布理鹏</p>
<h1 id="第二纪元-1700年"><a href="#第二纪元-1700年" class="headerlink" title="第二纪元 1700年"></a>第二纪元 1700年</h1><p><strong>索伦</strong>的再次现身，并且似乎得到了强大的法器，这在<strong>维林诺</strong>掀起了不小的波澜。众神召开会议讨论应对之策，最终决定派遣五位<strong>迈雅</strong>前往中土帮助那里的自由民抵抗邪恶，这五名迈雅将以人类老者的面貌示人，即后来的五名<strong>巫师</strong>。</p>
<p>三年之后，狩猎之神<strong>欧洛米</strong>麾下的两名<strong>迈雅</strong>先行出发前往中土（即两名<strong>蓝袍</strong>）。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/0292058c07254ae38f32e99fbbc35cd2?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>蓝袍巫师</p>
<h1 id="第二纪元-1705年"><a href="#第二纪元-1705年" class="headerlink" title="第二纪元 1705年"></a>第二纪元 1705年</h1><p><strong>索伦</strong>大军已经完全占领了<strong>埃瑞吉安</strong>，并且威胁到了<strong>林顿</strong>地区以及<strong>努曼诺尔人</strong>在中土的殖民地。</p>
<p><strong>他</strong>还将手中的十六枚<strong>力量之戒</strong>分别赠予九位人类贵族（中土人类，并非努曼诺尔人）和七位矮人领主，条件是希望他们在接下来的战争中保持中立。</p>
<p>努曼诺尔王储<strong>米那斯提尔</strong>率领一支庞大的舰队前往中土讨伐索伦，他们分别在<strong>灰港</strong>和<strong>隆德戴尔</strong>登陆，在<strong>“格瓦斯罗之战”</strong>中一举歼灭<strong>索伦</strong>的主力部队，<strong>索伦</strong>本人侥幸逃脱。</p>
<h1 id="第二纪元-1712年"><a href="#第二纪元-1712年" class="headerlink" title="第二纪元 1712年"></a>第二纪元 1712年</h1><p><strong>白道会</strong>第一次召开会议，参与者有<strong>吉尔-加拉德</strong>、<strong>埃尔隆德</strong>以及<strong>凯兰崔尔女士</strong>，该会议将在以后定期举行讨论<strong>中土</strong>的局势。也是在这个会议上，<strong>吉尔-加拉德</strong>将<strong>水戒维雅</strong>交予了<strong>埃尔隆德</strong>，并将他任命为自己的副手，令其管理当时还未建成的<strong>瑞文戴尔</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/9f09cfcf2f074b60833da2b675537b1a?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>埃尔隆德</p>
<h1 id="第二纪元-1713-1800年"><a href="#第二纪元-1713-1800年" class="headerlink" title="第二纪元 1713-1800年"></a>第二纪元 1713-1800年</h1><p>经过这场战争后，<strong>努曼诺尔人</strong>确立了自己在中土的统治地位，变得更加目中无人，国内一些派别甚至主张放弃信仰独一真神<strong>一如</strong>。</p>
<p><strong>索伦</strong>被打败后只好暂时放弃对中土富庶地区的野心，将重心放在了中土的东南方，那里还有相当一部分当年没有西迁的<strong>东夷人</strong>，如今已经成长为数个庞大的部落社会。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/fe48f2ad2e0246a2a78b07a80b902c5c?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>东夷人</p>
<h1 id="第二纪元-2029年"><a href="#第二纪元-2029年" class="headerlink" title="第二纪元 2029年"></a>第二纪元 2029年</h1><p><strong>塔尔-阿塔那米尔</strong>继任为<strong>努曼诺尔</strong>的第十三任国王，他在位期间加强了对中土殖民地的控制，甚至开始要求殖民地定期向<strong>努曼诺尔王室</strong>朝贡。大量的财富汇集到<strong>努曼诺尔岛</strong>，但是却加速了<strong>努曼诺尔王国</strong>的灭亡。</p>
<p><strong>阿塔那米尔国王</strong>对<strong>维拉们</strong>不再信任，认为祂们制定<strong>“人类不得踏足维林诺”</strong>的规定是对<strong>努曼诺尔王室</strong>的侮辱，国内在这名国王的纵容下出现了两种完全对立的政治派别：忠于<strong>维拉</strong>的<strong>忠贞派</strong>与拥护国王渎神主张的<strong>忠王派</strong>，为以后<strong>努曼诺尔</strong>的分裂埋下种子。</p>
<h1 id="第二纪元-2251年"><a href="#第二纪元-2251年" class="headerlink" title="第二纪元 2251年"></a>第二纪元 2251年</h1><p>中土大陆第一次出现一种身披黑袍的无形邪物，它们是被九枚<strong>力量之戒</strong>腐化了的人类贵族，死后其灵体继续遭到<strong>索伦</strong>奴役。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/06a15a1e41f348cb921a041aa8b5ae41?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>戒灵</p>
<h1 id="第二纪元-2380年"><a href="#第二纪元-2380年" class="headerlink" title="第二纪元 2380年"></a>第二纪元 2380年</h1><p>中土南方港口城市<strong>昂巴</strong>建成，成为王国对中土殖民地控制的重要前沿堡垒。</p>
<h1 id="第二纪元-3209年"><a href="#第二纪元-3209年" class="headerlink" title="第二纪元 3209年"></a>第二纪元 3209年</h1><p><strong>伊熙尔杜</strong>出生。</p>
<h1 id="第二纪元-3255年"><a href="#第二纪元-3255年" class="headerlink" title="第二纪元 3255年"></a>第二纪元 3255年</h1><p><strong>阿尔-法拉宗</strong>篡夺了其表姐的王位，成为努曼诺尔王国的末代国王。</p>
<h1 id="第二纪元-3261年"><a href="#第二纪元-3261年" class="headerlink" title="第二纪元 3261年"></a>第二纪元 3261年</h1><p><strong>索伦</strong>自称<strong>“人类之王”</strong>并且开始频繁骚扰<strong>努曼诺尔人</strong>在中土南方的殖民地，<strong>阿尔-法拉宗</strong>率部亲征，其舰队的船帆遮天蔽日，但是没想到刚刚抵达中土的港口<strong>昂巴</strong>，<strong>索伦</strong>便向人类国王投降。</p>
<p><img src="https://p6-tt.byteimg.com/origin/dfic-imagehandler/56ff66f0-77bd-408d-a81c-33fca26fd34a?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>一位曾经的<strong>迈雅</strong>向人类国王臣服极具象征意义，<strong>国王</strong>可以借此打压国内的<strong>忠贞派</strong>，于是<strong>阿尔-法拉宗</strong>将<strong>索伦</strong>扣为人质带回了<strong>努曼诺尔岛</strong>。</p>
<h1 id="第二纪元-3270年"><a href="#第二纪元-3270年" class="headerlink" title="第二纪元 3270年"></a>第二纪元 3270年</h1><p>由于对<strong>忠王派</strong>势力不断抬头的担心，努曼诺尔国内的<strong>忠贞派</strong>领袖<strong>阿门迪尔</strong>前往<strong>林顿</strong>与精灵们商讨<strong>努曼诺尔人</strong>的未来，精灵们认为<strong>忠王派</strong>这种渎神的行为继续下去必将遭到天谴。</p>
<p><strong>阿门迪尔</strong>离开时，<strong>吉尔-加拉德</strong>将一批由<strong>费诺</strong>打造的<strong>真知晶石</strong>赠送给他（具体数量未知），这种宝石可以用于远距离交流。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/16d063bc6f1b413f927670deec9a87cb?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第二纪元-3290年"><a href="#第二纪元-3290年" class="headerlink" title="第二纪元 3290年"></a>第二纪元 3290年</h1><p>沦为阶下囚的那段时间，<strong>索伦</strong>一直表现得十分恭顺，<strong>国王阿尔-法拉宗</strong>也时常找到<strong>索伦</strong>与之交谈，渐渐地<strong>阿尔-法拉宗</strong>放松了对<strong>索伦</strong>的戒备并且放任他在王城自由活动。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/980288cad1ba47bcb578f59daaed890a?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第二纪元-3310年"><a href="#第二纪元-3310年" class="headerlink" title="第二纪元 3310年"></a>第二纪元 3310年</h1><p><strong>阿尔-法拉宗</strong>年事已高，对死亡的恐惧使得他越发地对<strong>维拉</strong>感到不满。</p>
<p>再加上<strong>索伦</strong>此时的蛊惑渐渐生效，<strong>忠王派</strong>在国王的默认下打压<strong>忠贞派</strong>，甚至岛上各地开始修建崇拜<strong>魔苟斯</strong>的祭坛，一些<strong>忠贞派</strong>的领袖被当成祭品进献给<strong>黑暗魔王</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/2ea4929f20f242aa800f779cd768add3?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>这场疯狂的渎神行为最终演变为不切实际的幻想，人们都认为凭借他们辉煌的舰队能够驶入<strong>维林诺</strong>，击败众神，到时候所有<strong>忠王派</strong>的<strong>努曼诺尔人</strong>都将得到永生，统治整个<strong>阿尔达</strong>世界。</p>
<p>岛内的港口开始大肆建造战舰，囤积补给，整个岛屿陷入可怕的战争阴影。</p>
<h1 id="第二纪元-3319年"><a href="#第二纪元-3319年" class="headerlink" title="第二纪元 3319年"></a>第二纪元 3319年</h1><p><strong>忠王派</strong>的无敌舰队出发攻打<strong>维林诺</strong>，但出动没多久便被众神摧毁，<strong>努曼诺尔岛</strong>也被众神愤怒的波浪淹没，永沉海底。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/617fc3aec72d4ba1afee1cdbfab05fe5?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>一些<strong>忠贞派</strong>的<strong>努曼诺尔人</strong>在<strong>埃兰迪尔</strong>以及他两个儿子<strong>伊熙尔杜</strong>和<strong>阿纳瑞安</strong>的带领下逃离了这场劫难，前往中土避难。</p>
<h1 id="第二纪元-3320年"><a href="#第二纪元-3320年" class="headerlink" title="第二纪元 3320年"></a>第二纪元 3320年</h1><p>两个<strong>努曼诺尔</strong>的继承王国，<strong>亚尔诺</strong>和<strong>刚铎</strong>被分别建立起来，<strong>埃兰迪尔</strong>成为中土人类的<strong>至高王</strong>，统领两个王国。从努曼诺尔岛带出的七颗<strong>真知晶石</strong>被分别放置在两国境内，方便相互通讯。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/59846900f2a34c148fd1aa5e549569b3?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>亚尔诺和刚铎</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/e899fc700d16439b9bafda1c5044d6ac?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>刚铎境内的王者双柱，依据伊熙尔杜和阿纳瑞安的形象而建</p>
<p>同年，两座姐妹城市，<strong>米那斯伊希尔</strong>和<strong>米那斯提力斯</strong>（原名<strong>米那斯阿诺尔</strong>）开始修建。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/fbb0cc5e58b94e6fa9cc1be2b057dbe2?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>米那斯伊希尔</p>
<p>另一边，<strong>索伦</strong>也回到<strong>魔多</strong>，准备再一次入侵中土的富饶地区。</p>
<h1 id="第二纪元-约3400年"><a href="#第二纪元-约3400年" class="headerlink" title="第二纪元 约3400年"></a>第二纪元 约3400年</h1><p>一部分幸存的努曼诺尔<strong>忠王派</strong>同样驾船来到中土，由于已经无法融入两个全新的人类王国，他们在大陆南方的海岸边住下，与<strong>“东夷”</strong>部落的人类混居，建立了一些海港城市，这些人后来成为了<strong>“昂巴海盗”</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/383578751042494aa6c97ea329b0d026?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>昂巴海盗</p>
<h1 id="第二纪元-3429年"><a href="#第二纪元-3429年" class="headerlink" title="第二纪元 3429年"></a>第二纪元 3429年</h1><p><strong>索伦</strong>进攻刚刚成立不久的<strong>刚铎</strong>，<strong>米那斯伊希尔</strong>失守，<strong>伊熙尔杜</strong>逃往<strong>亚尔诺</strong>寻求支援，他的弟弟<strong>阿纳瑞安</strong>苦守<strong>欧斯吉利亚斯，</strong>抵挡<strong>索伦</strong>的大军长达数年之久。</p>
<h1 id="第二纪元-3430-3441年"><a href="#第二纪元-3430-3441年" class="headerlink" title="第二纪元 3430-3441年"></a>第二纪元 3430-3441年</h1><p><strong>埃兰迪尔</strong>向精灵求助，诺多<strong>至高王吉尔-加拉德</strong>与<strong>埃尔隆德</strong>分别派兵参战，大绿林的<strong>林地之王欧洛斐尔</strong>和其子<strong>瑟兰督伊</strong>率领族人加入，不过他们并不接受<strong>吉尔-加拉德</strong>的指挥，一小部分<strong>长须氏族</strong>的矮人也加入进来，史称<strong>“最后的联盟大战”</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/ab6d860d449c47678dd3244ed426e1dd?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>过程中，联盟一方<strong>阿纳瑞安</strong>、<strong>欧洛斐尔</strong>相继战死（其子<strong>瑟兰督伊</strong>继任<strong>林地之王</strong>后带领族人继续战斗）。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/dcfd89e798b3463895c467f24ff7b1df?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>最后的<strong>巴拉督尔围城战</strong>中，<strong>索伦</strong>又杀死了<strong>吉尔-加拉德</strong>与<strong>埃兰迪尔</strong>，危急时刻，<strong>伊熙尔杜</strong>执起父亲的断剑<strong>“纳熙尔”</strong>斩断了<strong>索伦</strong>的手指。<strong>索伦</strong>肉体被摧毁，灵魂逃遁于无形，<strong>至尊魔戒</strong>被<strong>伊熙尔杜</strong>收获，第二纪元终结。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/076c135117774a9bad87637fec45e28f?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/57e56e00b321465b9ca3d8131dbd456b?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-1-2年"><a href="#第三纪元-1-2年" class="headerlink" title="第三纪元 1-2年"></a>第三纪元 1-2年</h1><p><strong>伊熙尔杜</strong>拒绝摧毁<strong>至尊魔戒</strong>，他将<strong>刚铎</strong>交给侄子<strong>美尼尔迪尔</strong>统治，自己继任<strong>至高王</strong>。<strong>伊熙尔杜</strong>与自己的几个儿子在前往北方王国<strong>亚尔诺</strong>的时候路过<strong>金鸢尾沼地</strong>，惨祸发生。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/c67fb76230ac47c0a2dabb4ec6f9f528?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>一行人遭到兽人小分队的偷袭，<strong>伊熙尔杜</strong>命令自己的贴身侍卫带着<strong>纳熙尔圣剑残片</strong>先走，他自己却带着<strong>至尊魔戒</strong>被兽人杀死，魔戒从此下落不明。</p>
<h1 id="第三纪元-3年"><a href="#第三纪元-3年" class="headerlink" title="第三纪元 3年"></a>第三纪元 3年</h1><p><strong>纳熙尔**</strong>圣剑残片<strong>被带到</strong>瑞文戴尔**。</p>
<h1 id="第三纪元-4-10年"><a href="#第三纪元-4-10年" class="headerlink" title="第三纪元 4-10年"></a>第三纪元 4-10年</h1><p><strong>吉尔-加拉德</strong>在<strong>最后联盟大战</strong>中陨落，他并没有留下后代，<strong>诺多族至高王</strong>的头衔就此失传，<strong>林顿</strong>的精灵们纷纷选择西渡，这片区域很快便萧条了下来。</p>
<p>后来中土西部沿海只有<strong>“造船者”奇尔丹</strong>统治的几个港口城市还有精灵居住。</p>
<h1 id="第三纪元-241年"><a href="#第三纪元-241年" class="headerlink" title="第三纪元 241年"></a>第三纪元 241年</h1><p><strong>埃尔隆德</strong>和<strong>凯勒布莉安</strong>之女<strong>阿尔玟</strong>出生，<strong>埃尔隆德</strong>预感到她可能是在中土大陆出生的最后一代精灵，为其赠名<strong>“暮星”</strong>。</p>
<h1 id="第三纪元-490年"><a href="#第三纪元-490年" class="headerlink" title="第三纪元 490年"></a>第三纪元 490年</h1><p><strong>索伦</strong>在<strong>最后联盟大战</strong>中落败后，其治下的<strong>“东夷”</strong>部落一直没有被妥善安置，虽然有时候他们也会与<strong>刚铎</strong>在边境进行一些小规模的贸易，但总得来说还是经常有摩擦出现。</p>
<p>到了490年，<strong>“东夷”</strong>部落第一次被整合，作为一个统一的大部落向<strong>刚铎</strong>发起进攻，进入刚铎的领土抢劫财富。</p>
<p>其后的上千年时间里，<strong>刚铎</strong>一直在与这些<strong>“东夷”部落</strong>作战，互有胜负。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/f999ce619c3040ef97b1a9b689f75158?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-861-864年"><a href="#第三纪元-861-864年" class="headerlink" title="第三纪元 861-864年"></a>第三纪元 861-864年</h1><p>第十代人类<strong>至高王埃雅仁都尔</strong>去世，他的三个儿子陷入王位争夺战，阿尔诺王国进入动荡期，此时的<strong>刚铎</strong>正陷入与<strong>东夷部落</strong>和<strong>海盗</strong>的作战中无暇他顾。</p>
<p>3年后，<strong>阿尔诺王国</strong>分裂，三个王子分别统领王国的一部分地区，分裂成三个更小的邦国，<strong>人类至高王</strong>的头衔旁落。</p>
<h1 id="第三纪元-1000年"><a href="#第三纪元-1000年" class="headerlink" title="第三纪元 1000年"></a>第三纪元 1000年</h1><p>剩余的三名巫师终于乘船来到中土，他们先后在<strong>灰港</strong>登陆，其中<strong>甘道夫</strong>抵达的时候，<strong>“造船者”奇尔丹</strong>将自己保管的<strong>火戒纳雅</strong>交付于祂，引发了<strong>萨鲁曼</strong>的不满。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/56a326bcaa924060927dc9f6dd21c71d?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>白袍萨鲁曼、褐袍瑞达加斯特、灰袍甘道夫</p>
<h1 id="第三纪元-1050年"><a href="#第三纪元-1050年" class="headerlink" title="第三纪元 1050年"></a>第三纪元 1050年</h1><p><strong>索伦</strong>秘密返回大绿林中的<strong>多古尔都要塞</strong>，在那里悄悄积聚力量恢复肉身，他的到来引来了许多邪物的聚集，<strong>大绿林</strong>开始被慢慢腐蚀，后来被改称<strong>“幽暗密林”</strong>。</p>
<h1 id="第三纪元-1060年"><a href="#第三纪元-1060年" class="headerlink" title="第三纪元 1060年"></a>第三纪元 1060年</h1><p>首次有人目击一种矮小的人类穿过<strong>迷雾山脉</strong>进入大陆西部，他们明确提到这些人没有矮人的长须，也不像矮人那样壮实。后来人们知道他们自称为<strong>霍比特人</strong>当中的<strong>毛脚氏族</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/f41c040bb7b648feb3b7d0f5070547ba?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-1100年"><a href="#第三纪元-1100年" class="headerlink" title="第三纪元 1100年"></a>第三纪元 1100年</h1><p><strong>甘道夫</strong>注意到了<strong>大绿林</strong>正发生的奇怪改变，但他当时判断可能是<strong>索伦</strong>手下的一位<strong>戒灵</strong>盘踞在那里。</p>
<h1 id="第三纪元-约1300年"><a href="#第三纪元-约1300年" class="headerlink" title="第三纪元 约1300年"></a>第三纪元 约1300年</h1><p><strong>兽人</strong>在迷雾山脉中的活动越来越频繁，和矮人之间的冲突加剧。</p>
<p><strong>戒灵之首</strong>来到<strong>亚尔诺王国</strong>的北方<strong>安格玛山脉</strong>附近建立起一个据点——<strong>卡恩督姆</strong>，在那里聚集兽人、恶龙以及一些堕落的人类，后来这里发展成<strong>安格玛王国</strong>，成为<strong>亚尔诺</strong>北方的巨大威胁。</p>
<p>该戒灵之首从此便自称<strong>安格玛巫王</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/dfic-imagehandler/e4610cfb-d2b4-46a0-8c8d-2e71a03967df?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>安格玛巫王</p>
<h1 id="第三纪元-1302年"><a href="#第三纪元-1302年" class="headerlink" title="第三纪元 1302年"></a>第三纪元 1302年</h1><p><strong>霍比特人</strong>的<strong>毛脚氏族</strong>继续向西迁徙后进入<strong>布理</strong>，在<strong>夏尔地区</strong>定居下来，其他氏族也陆续到来。</p>
<h1 id="第三纪元-1409年"><a href="#第三纪元-1409年" class="headerlink" title="第三纪元 1409年"></a>第三纪元 1409年</h1><p>趁着<strong>阿尔诺王国</strong>中分裂出来的三个小国发生内乱，<strong>安格玛王国</strong>突袭了位于最北方的<strong>鲁道尔王国</strong>，将其王室彻底消灭。<strong>巫王</strong>将<strong>鲁道尔</strong>分给了那些使用黑暗巫术的堕落人类。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/4944f798155041cf86cf65c0fdacab68?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>安格玛巫王</p>
<h1 id="第三纪元-1432年"><a href="#第三纪元-1432年" class="headerlink" title="第三纪元 1432年"></a>第三纪元 1432年</h1><p><strong>刚铎</strong>的第二十一任国王<strong>埃尔达卡</strong>继位，他由于过分器重王国北方的本土人类，而忽视了王国内部努曼诺尔贵族的诉求（<strong>埃尔达卡</strong>本身就是<strong>努曼诺尔人</strong>与本土人类的混血），导致一场内乱，其王位被<strong>卡斯塔米尔</strong>篡夺达10年之久，后来他在北方本土人类的帮助下才夺回王位。</p>
<p>都城<strong>欧斯吉利亚斯</strong>在这场动乱中被焚毁，<strong>真知晶石</strong>不知去向。</p>
<h1 id="第三纪元-1636年"><a href="#第三纪元-1636年" class="headerlink" title="第三纪元 1636年"></a>第三纪元 1636年</h1><p>席卷整个中土大陆的<strong>大瘟疫</strong>爆发，这场传染病对<strong>刚铎</strong>的打击最大，都城<strong>欧斯吉利亚斯</strong>内的人口减少了三分之一，甚至<strong>国王泰伦纳</strong>自己也死在了这场瘟疫中。王室不得不将都城迁往<strong>米那斯提利斯</strong>，<strong>刚铎</strong>的衰败就此开始。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/be91a8383dfb4a9db816c582a098458d?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>大瘟疫蔓延区</p>
<p>与此同时，北方的形势也好不到哪去，<strong>安格玛巫王</strong>派出<strong>古冢尸妖</strong>来加速瘟疫的蔓延，夏尔的<strong>霍比特人</strong>，以及其他北方地区的人类都遭受重大损失。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/0749835c2c6445bb806c8ebdcc1cbc77?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>古冢尸妖</p>
<h1 id="第三纪元-1851-1944年"><a href="#第三纪元-1851-1944年" class="headerlink" title="第三纪元 1851-1944年"></a>第三纪元 1851-1944年</h1><p><strong>刚铎</strong>东南方向又崛起了一股新的蛮夷势力——<strong>“战车民”</strong>。他们受<strong>索伦</strong>挑动，装备着巨大的战车向刚铎进攻，开启了近百年的<strong>“战车民战争”</strong>。<strong>刚铎</strong>在这场战争中苦苦支撑，虽然最后打败了<strong>战车民</strong>，但是当时的<strong>国王昂多赫尔</strong>和他的儿子们都在战争中相继死去，王国陷入继承危机。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/5d100f699ccb4458895f8845a710ed9f?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>战车民</p>
<h1 id="第三纪元-1969-1974年"><a href="#第三纪元-1969-1974年" class="headerlink" title="第三纪元 1969-1974年"></a>第三纪元 1969-1974年</h1><p><strong>安格玛王国</strong>全面入侵北方诸国，最后一个小国<strong>阿塞丹</strong>沦陷，其王储<strong>阿拉纳斯</strong>开始流亡。北方王国<strong>阿尔诺</strong>彻底灭亡。原本打算北上驰援的<strong>刚铎援军</strong>和<strong>瑞文戴尔</strong>的精灵军队晚到一步，没有能够挽救<strong>阿塞丹</strong>，但是他们重新集结了大军与<strong>安格玛王国</strong>的军队展开决战，史称<strong>“佛诺斯特之战”</strong>。这场战争中<strong>安格玛巫王</strong>大败，联军顺势向北方推进，将这个邪恶王国彻底铲除。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/c9537330992d4bb2a04e4e1520f84f5f?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>佛诺斯特之战</p>
<h1 id="第三纪元-1976年"><a href="#第三纪元-1976年" class="headerlink" title="第三纪元 1976年"></a>第三纪元 1976年</h1><p><strong>阿塞丹</strong>流亡王储<strong>阿拉纳斯</strong>开始自称<strong>“杜内丹人族长”</strong>，<strong>巴拉希尔之戒</strong>、<strong>纳熙尔圣剑碎片</strong>、<strong>安努米那斯权杖</strong>等阿尔诺王室宝物被交给<strong>瑞文戴尔</strong>的<strong>埃尔隆德</strong>保管。</p>
<h1 id="第三纪元-1977年"><a href="#第三纪元-1977年" class="headerlink" title="第三纪元 1977年"></a>第三纪元 1977年</h1><p><strong>刚铎北方</strong>的本土人类在帮助<strong>刚铎</strong>先后击败了<strong>战车民</strong>以及<strong>安格玛王国</strong>之后，获得了王国上下的肯定，王室将北部的一大片土地赠予他们供其自治，那里被称为<strong>伊奥希奥德</strong>，也就是后来的<strong>洛汗国</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/cb74e887396845eab3b79a25e56b1b3e?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>洛汗国位置</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/51f787a5984e45b8bb060708b785910f?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>伊奥希奥德人</p>
<h1 id="第三纪元-1980-1982年"><a href="#第三纪元-1980-1982年" class="headerlink" title="第三纪元 1980-1982年"></a>第三纪元 1980-1982年</h1><p><strong>矮人</strong>在<strong>卡扎督姆</strong>地底开采<strong>秘银</strong>的时候越挖越深，终于唤醒了沉睡千年的<strong>炎魔</strong>，这个生物对矮人城市造成了巨大破坏，先后两任<strong>矮人国王</strong>都被其杀死。</p>
<p>再加上<strong>迷雾山脉</strong>的<strong>兽人</strong>乘机进犯，最后整座城市变得无法继续居住，矮人只能放弃这里，从此<strong>卡扎督姆</strong>被阴影笼罩，被人称为为<strong>摩瑞亚</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/9adf137e453244e9a21976870bcf0e7f?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>炎魔</p>
<h1 id="第三纪元-1985年"><a href="#第三纪元-1985年" class="headerlink" title="第三纪元 1985年"></a>第三纪元 1985年</h1><p><strong>伊奥希奥德人</strong>的首领<strong>弗拉姆</strong>在灰色山脉杀死<strong>恶龙史卡沙</strong>并缴获了恶龙原本占据着的<strong>矮人宝库</strong>。矮人们要求<strong>弗拉姆</strong>归还财宝但是遭到拒绝。<strong>弗拉姆</strong>还将<strong>史卡沙</strong>的牙齿拔下来做成项链还给矮人作为羞辱，愤怒的矮人杀死了<strong>弗拉姆</strong>，夺走了财宝。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/3548fc94cd3746fb8db4f77f713d4321?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>弗拉姆杀死史卡沙</p>
<p>但是一部分宝藏还是被<strong>弗拉姆</strong>的族人保留了下来，成为后来洛汗国起家的资本，<strong>洛汗</strong>著名的<strong>银号角</strong>就是宝藏的一部分。</p>
<h1 id="第三纪元-1999年"><a href="#第三纪元-1999年" class="headerlink" title="第三纪元 1999年"></a>第三纪元 1999年</h1><p>放弃了<strong>卡扎督姆</strong>的矮人们在<strong>瑟莱因一世</strong>的带领下来到孤山，修建了<strong>埃瑞伯</strong>。</p>
<h1 id="第三纪元-2000年"><a href="#第三纪元-2000年" class="headerlink" title="第三纪元 2000年"></a>第三纪元 2000年</h1><p><strong>安格玛巫王</strong>回到<strong>魔多</strong>，率领<strong>戒灵</strong>包围了<strong>米那斯伊希尔</strong>，一年后城破，从此这座城市被改名为<strong>米那斯魔古尔</strong>，意为<strong>“妖术之塔”</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/4a43ec84247e41a186890b13e5ed26ff?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>米那斯魔古尔</p>
<h1 id="第三纪元-2005年"><a href="#第三纪元-2005年" class="headerlink" title="第三纪元 2005年"></a>第三纪元 2005年</h1><p>“群山之心”<strong>阿肯宝钻在</strong>孤山之下被发现，成为矮人王室的象征。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/095284bcf12c41e695bd557d1ac56f93?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>“群山之心”阿肯宝钻</p>
<h1 id="第三纪元-2050年"><a href="#第三纪元-2050年" class="headerlink" title="第三纪元 2050年"></a>第三纪元 2050年</h1><p><strong>刚铎</strong>的末代国王<strong>埃雅努尔</strong>接受<strong>安格玛巫王</strong>的挑衅，前往米<strong>那斯魔古尔</strong>与之决一雌雄，但是自此便杳无音讯，<strong>刚铎</strong>陷入国王失踪且没有后继者的窘境，国事由<strong>执政宰相</strong>代为管理。</p>
<h1 id="第三纪元-2060年"><a href="#第三纪元-2060年" class="headerlink" title="第三纪元 2060年"></a>第三纪元 2060年</h1><p><strong>多古尔都</strong>的势力逐渐壮大，<strong>甘道夫</strong>进入多古尔都探查，<strong>索伦</strong>退却回到东方的魔多，警戒和平时期开始。</p>
<h1 id="第三纪元-2063-2463年"><a href="#第三纪元-2063-2463年" class="headerlink" title="第三纪元 2063-2463年"></a>第三纪元 2063-2463年</h1><p>这400年被称为<strong>警戒和平时期</strong>，各方还算相安无事，不过这段时间<strong>索伦</strong>在<strong>魔多</strong>悄悄培植了各种黑暗物种，其中就包括<strong>强兽人（Uruk-hai）</strong>以及<strong>堕落飞兽</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/4518935a8bfc4af0ba9b52c7207deb79?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-2463年"><a href="#第三纪元-2463年" class="headerlink" title="第三纪元 2463年"></a>第三纪元 2463年</h1><p><strong>索伦</strong>的肉身已经完全恢复，回到了<strong>多古尔都</strong>，警戒和平时期结束，<strong>白道会</strong>再次召开，<strong>萨鲁曼</strong>被选为领袖。</p>
<p>同年<strong>至尊魔戒</strong>现世，<strong>斯图尔族</strong>的<strong>霍比特人狄戈</strong>在外出钓鱼时发现了这枚魔戒，他被其好友<strong>斯密戈</strong>杀死，魔戒暂时归属于<strong>斯密戈（即后来的咕噜）</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/e76a916d4f8f469880ff6299e8734f16?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>咕噜</p>
<h1 id="第三纪元-2480年"><a href="#第三纪元-2480年" class="headerlink" title="第三纪元 2480年"></a>第三纪元 2480年</h1><p>许多<strong>迷雾山脉</strong>的兽人进入<strong>摩瑞亚</strong>，占据了从前矮人的城市。在那里，他们奉<strong>炎魔</strong>为神明，利用<strong>摩瑞亚</strong>的地下通道，兽人可以轻松跨越<strong>迷雾山脉</strong>，对山脉两侧的自由之地造成威胁。</p>
<h1 id="第三纪元-2509年"><a href="#第三纪元-2509年" class="headerlink" title="第三纪元 2509年"></a>第三纪元 2509年</h1><p><strong>摩瑞亚</strong>的<strong>兽人</strong>南下进犯刚铎的领土，<strong>刚铎</strong>摄政宰相<strong>奇瑞安</strong>向<strong>伊奥希奥德人</strong>求援，<strong>凯勒布兰特原野之战</strong>爆发。击退兽人后，作为答谢，<strong>刚铎</strong>承认了<strong>伊奥希奥德人</strong>独立建国的意愿，两国领导人也共同发誓两者之间绝不会发生战争，世代友好，<strong>“镖骑国”洛汗</strong>正式成立。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/7c6481180e4443878f0cfef651fd8875?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>“镖骑国”骑兵</p>
<h1 id="第三纪元-2710年"><a href="#第三纪元-2710年" class="headerlink" title="第三纪元 2710年"></a>第三纪元 2710年</h1><p>荒废的<strong>艾辛格</strong>和周边森林被<strong>黑蛮地人</strong>占据，对<strong>刚铎</strong>和<strong>洛汗</strong>的西侧边疆造成威胁。</p>
<h1 id="第三纪元-2746年"><a href="#第三纪元-2746年" class="headerlink" title="第三纪元 2746年"></a>第三纪元 2746年</h1><p><strong>梭林二世</strong>出生。</p>
<h1 id="第三纪元-2759年"><a href="#第三纪元-2759年" class="headerlink" title="第三纪元 2759年"></a>第三纪元 2759年</h1><p><strong>萨鲁曼</strong>向刚铎<strong>摄政宰相贝伦</strong>提议由他来代管<strong>艾辛格</strong>的防务，<strong>刚铎</strong>和<strong>洛汗</strong>都很高兴能有一位巫师来替他们解除后方的麻烦，欣然答应。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/2c280b8ca40f4da4bd65fc49485926d1?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>艾辛格的位置</p>
<p><strong>萨鲁曼</strong>整顿了<strong>艾辛格</strong>之后，在哨塔顶部发现了一颗<strong>真知晶石</strong>。</p>
<h1 id="第三纪元-2770年"><a href="#第三纪元-2770年" class="headerlink" title="第三纪元 2770年"></a>第三纪元 2770年</h1><p>北方恶龙<strong>史矛革</strong>突袭孤山，<strong>河谷城</strong>被毁，<strong>埃瑞伯</strong>遭到占据，矮人们再次无家可归。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/0dad4ddfe4bd45d2bc43a8500d94412d?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-2793-2799年"><a href="#第三纪元-2793-2799年" class="headerlink" title="第三纪元 2793-2799年"></a>第三纪元 2793-2799年</h1><p>失去了家园的矮人到处漂泊，<strong>矮人王瑟莱因二世</strong>打算集结其他矮人氏族的力量攻打<strong>摩瑞亚</strong>，一方面是为自己的父亲<strong>瑟罗尔</strong>报仇，另一方面为了夺回矮人曾经引以为傲的城市<strong>卡扎督姆</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/1b5c5dc4131645128fe85ac19d4f96a0?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>梭林二世和瑟莱因二世</p>
<p>战争持续到2799年，<strong>迷雾山脉</strong>的其他兽人据点基本被扫清，兽人全都集结到了<strong>摩瑞亚</strong>之内，于是在东大门爆发了最后一场<strong>阿扎努比扎之战</strong>。</p>
<p>这场战争以矮人的惨胜告终，虽然杀死了兽人首领<strong>阿佐格</strong>，但是<strong>炎魔</strong>仍旧盘踞在内，剩余的矮人不足以收复<strong>卡扎督姆</strong>，其他部族的矮人都想着返回家园，所以长须一族在<strong>矮人王瑟莱因二世</strong>和<strong>梭林二世·橡木盾</strong>的带领下再次踏上流亡之路。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/8201e98330f04527b3cb3d79013fa75e?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>阿扎努比扎之战</p>
<h1 id="第三纪元-2841年"><a href="#第三纪元-2841年" class="headerlink" title="第三纪元 2841年"></a>第三纪元 2841年</h1><p>长须矮人在中土西侧的蓝色山脉居住了一段日子，期间<strong>瑟莱因二世</strong>承受着失去家园的痛苦，同时他还带着其中一枚<strong>力量之戒</strong>——<strong>瑟罗尔</strong>，这更加重了<strong>瑟莱因二世</strong>的疯狂，他竟然只带了一个小队就打算重新收复<strong>埃瑞伯</strong>。</p>
<p>这支队伍行至<strong>幽暗密林</strong>的时候被那里的兽人侦察部队发现，<strong>瑟莱因二世</strong>居然就此失踪。</p>
<h1 id="第三纪元-2850年"><a href="#第三纪元-2850年" class="headerlink" title="第三纪元 2850年"></a>第三纪元 2850年</h1><p><strong>甘道夫</strong>再次侦察<strong>多古尔都</strong>的时候发现了关押在那里奄奄一息的<strong>瑟莱因二世</strong>，此时他已经几乎没有了神智。最后他将进入<strong>孤山</strong>的关键：一把钥匙和地图交给了<strong>甘道夫</strong>，不久便离开了人世。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/75a6d1e7024644148a71a2e53f01963d?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>瑟莱因二世</p>
<h1 id="第三纪元-2851年"><a href="#第三纪元-2851年" class="headerlink" title="第三纪元 2851年"></a>第三纪元 2851年</h1><p><strong>甘道夫</strong>召集<strong>白道会</strong>，会议期间<strong>甘道夫</strong>力主进攻<strong>多古尔都</strong>，但是萨鲁曼以不能打草惊蛇为理由驳回了这个提议。</p>
<h1 id="第三纪元-2890年"><a href="#第三纪元-2890年" class="headerlink" title="第三纪元 2890年"></a>第三纪元 2890年</h1><p><strong>比尔博·巴金斯</strong>出生。</p>
<h1 id="第三纪元-2931年"><a href="#第三纪元-2931年" class="headerlink" title="第三纪元 2931年"></a>第三纪元 2931年</h1><p><strong>阿拉贡二世</strong>出生，到他两岁的时候，其父亲就遭到兽人杀害，<strong>阿拉贡二世</strong>成为<strong>杜内丹人</strong>的第十六任族长。其母将他送去<strong>瑞文戴尔</strong>，<strong>埃尔隆德</strong>将其收为养子。</p>
<h1 id="第三纪元-2940-2941年"><a href="#第三纪元-2940-2941年" class="headerlink" title="第三纪元 2940-2941年"></a>第三纪元 2940-2941年</h1><p><strong>甘道夫</strong>担心北方的局势，他认为等到开战的时候恶龙<strong>史矛革</strong>容易被敌人利用，于是他与<strong>梭林·橡木盾</strong>在<strong>布理</strong>见面，商讨如何帮助矮人们夺回<strong>埃瑞伯</strong>，<strong>孤山任务</strong>开始。</p>
<p>矮人一行和<strong>甘道夫</strong>拜访了霍比特人<strong>比尔博·巴金斯</strong>，半强迫式地将他带去冒险，<strong>孤山小队</strong>成立。</p>
<p>该年<strong>白道会</strong>再次召开，<strong>萨鲁曼</strong>为了阻止<strong>索伦</strong>在安度因河上游搜索<strong>魔戒</strong>，终于同意进攻<strong>多古尔都</strong>，但<strong>索伦</strong>早有放弃多古尔都的打算。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/cfd0df5366524af1a018835f3a772f31?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p><strong>比尔博·巴金斯</strong>在冒险途中遇到<strong>咕噜</strong>，并且幸运地拾到了<strong>至尊魔戒</strong>，将其从咕噜身边带走。</p>
<p><strong>弓箭手巴德</strong>将<strong>史矛革</strong>射死在<strong>长湖镇</strong>，随即五军之战爆发，<strong>梭林·橡木盾</strong>战死，<strong>铁丘陵</strong>的矮人<strong>戴因二世</strong>继任山下之王。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/602cd97df87e4276b00f6061d160d830?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/6b9f5618fa0e4cd1926a83a851346ac9?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-2942年"><a href="#第三纪元-2942年" class="headerlink" title="第三纪元 2942年"></a>第三纪元 2942年</h1><p><strong>比尔博·巴金斯</strong>带着至尊魔戒以及相当一部分财宝，包括<strong>秘银甲</strong>和<strong>刺叮**</strong>剑<strong>回到了</strong>夏尔<strong>，</strong>索伦<strong>则返回</strong>魔多**。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/d6095b2264b94fe097551644ef9e2fce?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>于此同时，<strong>萨鲁曼</strong>正在各地秘密培植自己的间谍，继续打听<strong>魔戒</strong>的下落。</p>
<h1 id="第三纪元-2951年"><a href="#第三纪元-2951年" class="headerlink" title="第三纪元 2951年"></a>第三纪元 2951年</h1><p><strong>阿拉贡</strong>年满20岁，<strong>埃尔隆德</strong>向其揭示了他的真实身份，并将<strong>巴拉希尔之戒</strong>和<strong>纳熙尔圣剑</strong>的碎片交付与他。</p>
<p>同年阿拉贡遇上了自外祖母<strong>凯兰崔尔</strong>那里归来的<strong>阿尔玟</strong>，两人一见钟情。</p>
<h1 id="第三纪元-2956年"><a href="#第三纪元-2956年" class="headerlink" title="第三纪元 2956年"></a>第三纪元 2956年</h1><p><strong>阿拉贡</strong>在游历过程中遇上了<strong>甘道夫</strong>，两人结下友谊，<strong>阿拉贡</strong>也感觉到了东方正在崛起的邪恶力量，<strong>甘道夫</strong>建议他注意<strong>夏尔</strong>地区，于是他化名<strong>“梭隆吉尔”</strong>开始在中土各地游历，搜集有用的线索。</p>
<h1 id="第三纪元-2968年"><a href="#第三纪元-2968年" class="headerlink" title="第三纪元 2968年"></a>第三纪元 2968年</h1><p><strong>弗罗多·巴金斯</strong>出生。</p>
<h1 id="第三纪元-2980年"><a href="#第三纪元-2980年" class="headerlink" title="第三纪元 2980年"></a>第三纪元 2980年</h1><p><strong>阿拉贡</strong>结束游历，来到<strong>洛丝罗瑞恩</strong>，与<strong>阿尔玟</strong>互赠礼物，私定终身。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/0cf7451b0c4b41d68c2036c66fac201a?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>同年，<strong>山姆怀斯·甘姆吉</strong>出生。</p>
<h1 id="第三纪元-3000年"><a href="#第三纪元-3000年" class="headerlink" title="第三纪元 3000年"></a>第三纪元 3000年</h1><p><strong>萨鲁曼</strong>使用<strong>真知晶石</strong>打探<strong>甘道夫</strong>和<strong>阿拉贡</strong>等人的行踪，但是与<strong>索伦</strong>掌握的另一颗晶石发生联系，<strong>索伦</strong>的魔音立即将<strong>萨鲁曼</strong>腐化，这名<strong>白道会</strong>的领袖彻底堕落。</p>
<h1 id="第三纪元-3001年"><a href="#第三纪元-3001年" class="headerlink" title="第三纪元 3001年"></a>第三纪元 3001年</h1><p><strong>比尔博·巴金斯</strong>在<strong>霍比屯</strong>举办自己的111岁生日宴会，由于一直持有<strong>至尊魔戒</strong>的缘故，比尔博看上去比实际年龄小很多。</p>
<p>在宴会上，他利用戒指凭空消失的小戏法却引起了<strong>甘道夫</strong>的怀疑。他忠告<strong>比尔博</strong>将戒指留下不要带走，<strong>比尔博</strong>艰难地做出了这个决定，放下了心中的负担，轻装离去。<strong>魔戒</strong>暂时交给<strong>比尔博</strong>的侄子<strong>佛罗多·巴金斯</strong>保管。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/53809678c99144b6950958cf6883b9a3?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p><strong>甘道夫</strong>回忆起<strong>比尔博</strong>的说法，这枚戒指是他从<strong>咕噜</strong>那里偷来的。于是他请求<strong>阿拉贡</strong>的帮助，让他帮忙追查<strong>咕噜</strong>的行踪。</p>
<h1 id="第三纪元-3017年2月"><a href="#第三纪元-3017年2月" class="headerlink" title="第三纪元 3017年2月"></a>第三纪元 3017年2月</h1><p><strong>阿拉贡</strong>在<strong>死亡沼泽</strong>抓获<strong>咕噜</strong>，并将其带到<strong>林地之王瑟兰杜伊</strong>那里看管。<strong>甘道夫</strong>根据<strong>咕噜</strong>的说法，结合<strong>米那斯提利斯</strong>留存的史料，拼凑出了<strong>魔戒</strong>自失落以来的完整历史。</p>
<h1 id="第三纪元-3017年6月"><a href="#第三纪元-3017年6月" class="headerlink" title="第三纪元 3017年6月"></a>第三纪元 3017年6月</h1><p><strong>林地王国</strong>遭到北方山脉中的兽人部队偷袭，<strong>咕噜</strong>逃脱，不知去向。<strong>瑟兰杜伊</strong>派遣自己的儿子<strong>莱戈拉斯</strong>前往<strong>瑞文戴尔</strong>通报此事。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/75e494e6248247aabee92d324c8eca60?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>莱戈拉斯</p>
<h1 id="第三纪元-3017年10月"><a href="#第三纪元-3017年10月" class="headerlink" title="第三纪元 3017年10月"></a>第三纪元 3017年10月</h1><p><strong>索伦</strong>手下的<strong>强兽人</strong>军队进攻<strong>欧斯吉利亚斯</strong>，这些兽人不惧白天的阳光，刚铎无力抵挡，<strong>魔戒战争</strong>正式爆发。</p>
<p>与此同时，另一支兽人部队向北方出击，打算联合北方山脉中的兽人夹击<strong>幽暗密林</strong>中的<strong>林地之王瑟兰杜伊</strong>。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/223c8ca28cff4d40bed97a2171600ced?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>魔多的进军方向</p>
<h1 id="第三纪元-3018年4月"><a href="#第三纪元-3018年4月" class="headerlink" title="第三纪元 3018年4月"></a>第三纪元 3018年4月</h1><p><strong>甘道夫</strong>回到<strong>霍比屯</strong>，将<strong>戒指</strong>掷入火中，戒身上现出那段著名的铭文，<strong>至尊魔戒</strong>的身份最终被确认。与此同时，他知道<strong>咕噜</strong>曾经被<strong>索伦</strong>的爪牙抓获，所以认定<strong>夏尔</strong>已经不再安全，<strong>佛罗多</strong>主动提出应该由他带走魔戒，保证大家的安全。</p>
<h1 id="第三纪元-3018年5月"><a href="#第三纪元-3018年5月" class="headerlink" title="第三纪元 3018年5月"></a>第三纪元 3018年5月</h1><p><strong>萨鲁曼</strong>发现<strong>九戒灵</strong>出动前往<strong>夏尔地区</strong>搜索，他猜测<strong>索伦</strong>一定是发现了某些线索，联想到<strong>甘道夫</strong>一直以来都徘徊在那里，他认为<strong>甘道夫</strong>一定知道些什么。</p>
<h1 id="第三纪元-3018年6月"><a href="#第三纪元-3018年6月" class="headerlink" title="第三纪元 3018年6月"></a>第三纪元 3018年6月</h1><p><strong>甘道夫</strong>在<strong>布理</strong>遇到了<strong>瑞达加斯特</strong>，他带来了<strong>萨鲁曼</strong>的口信：</p>
<blockquote>
<p>九位戒灵伪装成黑骑士正赶往夏尔，如果有需要，萨鲁曼会提供协助。</p>
</blockquote>
<p><strong>甘道夫</strong>在得到口信后认为自己应该返回<strong>艾辛格</strong>面见<strong>萨鲁曼</strong>，如果此时能争取<strong>白袍</strong>的协助是极为有利的，没想到这是一个致命的错误。</p>
<h1 id="第三纪元-3018年7月"><a href="#第三纪元-3018年7月" class="headerlink" title="第三纪元 3018年7月"></a>第三纪元 3018年7月</h1><p><strong>甘道夫</strong>抵达<strong>艾辛格</strong>，但是被<strong>萨鲁曼</strong>囚禁，直到9月，才在<strong>大鹰之王格怀希尔</strong>的帮助下，逃离了<strong>欧尔桑克高塔</strong>。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/283d0a0228fe465fbac9f4734a397bbc?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>大鹰之王格怀希尔</p>
<h1 id="第三纪元-3018年8月"><a href="#第三纪元-3018年8月" class="headerlink" title="第三纪元 3018年8月"></a>第三纪元 3018年8月</h1><p><strong>佛罗多</strong>等四位<strong>霍比特人</strong>到达<strong>布理</strong>，遇到了在这里等<strong>甘道夫</strong>的<strong>“大步佬”阿拉贡</strong>，于是他保护着四个霍比特人上路，前往<strong>瑞文戴尔</strong>。</p>
<h1 id="第三纪元-3018年10月"><a href="#第三纪元-3018年10月" class="headerlink" title="第三纪元 3018年10月"></a>第三纪元 3018年10月</h1><p>众人都抵达了<strong>瑞文戴尔</strong>，<strong>埃尔隆德</strong>主持了此次秘密会议，主要讨论如何处置现世的<strong>至尊魔戒</strong>，最后一致决定魔戒需要被摧毁，但是不能由军队强攻，必须由一个秘密小队执行，<strong>佛罗多·巴金斯</strong>主动承担了这个任务。</p>
<p>还有另外8人也加入了这个任务，<strong>护戒小队</strong>成立。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/355887f3303d4588b789b7a892f9ef09?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>小队出发之前，<strong>阿拉贡</strong>的<strong>纳熙尔圣剑残片</strong>被重铸，重生后的圣剑被命名为<strong>安督利尔</strong>意为<strong>“西方之焰”</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/a69905c7abaf4a67b46656544c5728dd?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-3019年1月"><a href="#第三纪元-3019年1月" class="headerlink" title="第三纪元 3019年1月"></a>第三纪元 3019年1月</h1><p><strong>护戒小队</strong>原本打算翻越<strong>红角峰</strong>，穿过<strong>迷雾山脉</strong>，但是在攀爬红角峰时遇到暴风雪，众人不得不折返选择通过<strong>摩瑞亚</strong>。在摩瑞亚的<strong>马扎布尔王厅</strong>，他们遭到了兽人和食人妖的袭击，小队不得不边战边撤。</p>
<p>在<strong>卡扎督姆桥</strong>上，甘道夫为了给其他成员争取时间，将桥体击碎，与<strong>炎魔</strong>一同坠入深渊。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/410f69fb3119463f9e405968e5fe06ee?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p><strong>甘道夫</strong>与<strong>炎魔</strong>一起坠落至地底的黑暗之海，最后终于将<strong>炎魔</strong>杀死，但<strong>甘道夫</strong>自己也力竭而亡。</p>
<p>祂一直以来忠于使命的行为让<strong>一如</strong>感动，于是破例重塑了其形体并将之送回中土，同时披上了白袍，<strong>凯兰崔尔</strong>更是将一把新的手杖赠予<strong>甘道夫</strong>。</p>
<p>另一边，剩余的小队成员虽然摆脱了追捕，但是<strong>甘道夫</strong>的死亡对他们打击很大，在<strong>帕斯嘉兰草坪</strong>，<strong>佛罗多</strong>决定不再拖累其他成员独自上路，<strong>山姆</strong>则追上<strong>佛罗多</strong>执意要与之同行。</p>
<p>另外两名<strong>霍比特人梅里</strong>与<strong>皮平</strong>被<strong>萨鲁曼</strong>派出的强兽人掳走，<strong>波洛米尔</strong>战死，<strong>阿拉贡</strong>、<strong>莱戈拉斯</strong>和<strong>金姆利</strong>不得不追踪这些兽人拯救两名同伴。</p>
<p><img src="https://p1-tt.byteimg.com/origin/dfic-imagehandler/81b1a331-f414-45e9-b0da-5ab0d12a6191?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-3019年2月"><a href="#第三纪元-3019年2月" class="headerlink" title="第三纪元 3019年2月"></a>第三纪元 3019年2月</h1><p>重生后的<strong>甘道夫</strong>与<strong>阿拉贡</strong>一行人汇合，得知了<strong>护戒小队</strong>分裂的消息，他意识到自己无法直接为<strong>持戒人</strong>提供帮助，于是将两名<strong>霍比特人</strong>交给<strong>树人</strong>照顾后，自己和<strong>阿拉贡</strong>他们前往<strong>洛汗</strong>对付<strong>萨鲁曼</strong>。</p>
<h1 id="第三纪元-3019年3月2日"><a href="#第三纪元-3019年3月2日" class="headerlink" title="第三纪元 3019年3月2日"></a>第三纪元 3019年3月2日</h1><p><strong>甘道夫</strong>一行人抵达<strong>洛汗</strong>首都<strong>埃多拉斯</strong>，展示了自己白袍身份的<strong>甘道夫</strong>气势惊人，将<strong>萨鲁曼</strong>的荼毒从<strong>国王希奥顿</strong>身上逼退，洛汗重获新生，开始整顿军力与<strong>萨鲁曼</strong>对抗。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/17bdcf821c594f79ab9629607ea0ae14?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-3019年3月4日"><a href="#第三纪元-3019年3月4日" class="headerlink" title="第三纪元 3019年3月4日"></a>第三纪元 3019年3月4日</h1><p><strong>圣盔谷大战</strong>爆发，<strong>萨鲁曼</strong>倾其所有，赌上了全部兵力要将<strong>洛汗</strong>拿下。但是<strong>甘道夫</strong>带着<strong>洛汗</strong>国内其他贵族的援兵及时赶到，击退了<strong>萨鲁曼</strong>的兽人部队，而<strong>艾辛格</strong>也同时遭到树人的突袭，<strong>萨鲁曼</strong>被囚禁。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/6b3f3b05c6164c38adf307a9187536eb?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-3019年3月5日"><a href="#第三纪元-3019年3月5日" class="headerlink" title="第三纪元 3019年3月5日"></a>第三纪元 3019年3月5日</h1><p>利用<strong>萨鲁曼</strong>的<strong>真知晶石</strong>，<strong>阿拉贡</strong>向索伦揭露了自己<strong>伊熙尔杜</strong>后人的身份，为正在向魔多进发的<strong>佛罗多</strong>争取时间，<strong>索伦</strong>将注意力转向西方的<strong>刚铎</strong>。</p>
<p><strong>洛汗国</strong>的骑兵开始集结，准备驰援<strong>刚铎</strong>。</p>
<p><strong>阿拉贡</strong>判断这场战争还需要额外的力量，所以和<strong>莱戈拉斯</strong>、<strong>金姆利</strong>一起踏上了<strong>亡者之路</strong>。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/0a45615937f44ae0a09fc5c59fa43b65?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-3019年3月10日"><a href="#第三纪元-3019年3月10日" class="headerlink" title="第三纪元 3019年3月10日"></a>第三纪元 3019年3月10日</h1><p><strong>甘道夫</strong>没有过多耽搁，骑上<strong>捷影</strong>向刚铎疾驰，他知道魔多的主力正在集结，抵挡<strong>索伦</strong>的最后堡垒<strong>米那斯提利斯</strong>正危在旦夕。</p>
<h1 id="第三纪元-3019年3月11日"><a href="#第三纪元-3019年3月11日" class="headerlink" title="第三纪元 3019年3月11日"></a>第三纪元 3019年3月11日</h1><p>中土北方先燃起了战火，<strong>林地王国</strong>遭到大股<strong>兽人</strong>、<strong>座狼</strong>和<strong>食人魔</strong>军队的突袭。</p>
<p><strong>洛丝罗瑞恩</strong>的精灵部队在<strong>凯勒博恩</strong>的带领下紧急动员，向北方的<strong>幽暗密林</strong>进发。</p>
<h1 id="第三纪元-3019年3月12日"><a href="#第三纪元-3019年3月12日" class="headerlink" title="第三纪元 3019年3月12日"></a>第三纪元 3019年3月12日</h1><p><strong>佛罗多</strong>在<strong>咕噜</strong>的带领下穿过<strong>奇立斯乌苟隘口</strong>，却进入了<strong>巨蛛尸罗</strong>的巢穴，<strong>佛罗多</strong>被尸罗刺伤，后来又被巡逻的兽人发现并带回了<strong>奇立斯乌苟哨塔</strong>，两次都是<strong>山姆</strong>勇敢地挺身而出，将<strong>佛罗多</strong>救下。</p>
<h1 id="第三纪元-3019年3月14日"><a href="#第三纪元-3019年3月14日" class="headerlink" title="第三纪元 3019年3月14日"></a>第三纪元 3019年3月14日</h1><p><strong>米那斯提利斯</strong>攻城战打响，<strong>执政宰相德内梭尔二世</strong>正陷入痛失二子的悲痛之中，守城的指挥权被交给<strong>甘道夫</strong>，但是凭借目前的兵力，<strong>米那斯提利斯</strong>根本无力抵抗。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/4fa82aa7ecf841eeb94a7ba03976f1b0?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-3019年3月15日破晓"><a href="#第三纪元-3019年3月15日破晓" class="headerlink" title="第三纪元 3019年3月15日破晓"></a>第三纪元 3019年3月15日破晓</h1><p><strong>洛汗</strong>的六千名骑兵赶到战场，在他们的冲锋下，兽人围城的地面部队被冲散，不过<strong>希奥顿王</strong>遭到<strong>安格玛巫王</strong>重创。<strong>伊奥温</strong>和霍比特人<strong>梅里·图克</strong>挺身而出，合力将<strong>巫王</strong>刺死。</p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/99af4c7122f54218a3991f1fa7610ca1?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/1414961897144ae7a5099b77a0c9af73?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>此时<strong>阿拉贡</strong>已经用<strong>亡者之魂</strong>平定了<strong>昂巴海盗</strong>的进犯，并且利用他们的战船载着一千名<strong>刚铎</strong>南方各贵族的援军抵达，<strong>索伦</strong>的军队被彻底击溃。</p>
<p><img src="https://p3-tt.byteimg.com/origin/pgc-image/62da9cafdb574ab1a3fb83679c566efe?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p><img src="https://p1-tt.byteimg.com/origin/pgc-image/d363380d51794a8fa485a4b1da1200f0?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<h1 id="第三纪元-3019年3月16日"><a href="#第三纪元-3019年3月16日" class="headerlink" title="第三纪元 3019年3月16日"></a>第三纪元 3019年3月16日</h1><p><strong>阿拉贡</strong>认为虽然在一场战役中取胜，但无法保证后面的战争中能够继续取胜，他们唯一的希望就是<strong>佛罗多</strong>和<strong>山姆</strong>。于是<strong>阿拉贡</strong>决定持续给<strong>魔多</strong>施压，带领疲惫不堪的军队继续开拔，向<strong>魔多</strong>的<strong>黑门</strong>进发。</p>
<h1 id="第三纪元-3019年3月25日"><a href="#第三纪元-3019年3月25日" class="headerlink" title="第三纪元 3019年3月25日"></a>第三纪元 3019年3月25日</h1><p><strong>阿拉贡</strong>带着不足六千人的军队来到黑门之下，<strong>魔栏农之战</strong>爆发。从黑门涌出的魔多大军迅速包围了他们，正在人类军队将要崩溃之时，<strong>索伦</strong>感觉到了<strong>佛罗多</strong>带上<strong>至尊魔戒</strong>，所有的敌人开始害怕起来，人类军队开始反冲锋。</p>
<p>末日火山之内，<strong>咕噜</strong>扑上去，抱住了带上<strong>魔戒</strong>的<strong>佛罗多</strong>，两人扭打在一起时，<strong>咕噜</strong>将<strong>佛罗多</strong>的手指咬下，终于得到了<strong>魔戒</strong>，但他在兴奋之余跌落了悬崖，和<strong>至尊魔戒</strong>一起被火山的熔岩吞噬。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/a8af821f029c4f3280422bdedc0afe2c?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>随着带有自己灵魂的<strong>至尊魔戒</strong>遭到摧毁，<strong>索伦</strong>被彻底击溃，其灵魂被放逐到<strong>虚空之境</strong>。</p>
<p>完成任务的<strong>佛罗多</strong>和<strong>山姆</strong>被大鹰救出，两人昏迷了14天才苏醒。</p>
<h1 id="第三纪元-3019年5月1日"><a href="#第三纪元-3019年5月1日" class="headerlink" title="第三纪元 3019年5月1日"></a>第三纪元 3019年5月1日</h1><p><strong>阿拉贡</strong>带领军队将<strong>刚铎</strong>其他区域的叛乱平定之后回到了<strong>王城米那斯提利斯</strong>，在众人的注视下加冕为<strong>国王</strong>，其后他又重建了北方的<strong>阿尔诺王国</strong>，成为<strong>阿尔诺</strong>和<strong>刚铎</strong>重联王国的<strong>至高王</strong>。</p>
<h1 id="第三纪元-3019年9月"><a href="#第三纪元-3019年9月" class="headerlink" title="第三纪元 3019年9月"></a>第三纪元 3019年9月</h1><p><strong>萨鲁曼</strong>从囚禁他的<strong>欧尔桑克</strong>逃脱，这个昔日的白袍巫师居然去到<strong>夏尔</strong>的<strong>霍比屯</strong>兴风作浪，靠着早先安插在<strong>夏尔</strong>的间谍大肆破坏。</p>
<p><strong>佛罗多</strong>、<strong>山姆</strong>、<strong>梅里</strong>、<strong>皮聘</strong>四人本打算荣归故里，但没想到家乡已经遭到<strong>萨鲁曼</strong>破坏，四名霍比特人纠集其他愤怒的村民对<strong>萨鲁曼</strong>和他的手下发起进攻，<strong>傍水镇之战</strong>爆发。</p>
<p><strong>萨鲁曼</strong>虽然生为一个迈雅，但那时居然没有了什么法力，众人将其俘虏后，他被自己的手下<strong>格里马</strong>杀死。其的灵魂不得回到维林诺，得到了和<strong>索伦</strong>同样的下场。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/8224d07cf712467294eac5ebd6a152b5?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>傍水镇之战</p>
<h1 id="第三纪元-3021年"><a href="#第三纪元-3021年" class="headerlink" title="第三纪元 3021年"></a>第三纪元 3021年</h1><p><strong>弗罗多</strong>、<strong>比尔博</strong>、<strong>埃尔隆德</strong>、<strong>凯兰崔尔</strong>、<strong>甘道夫</strong>等人从<strong>灰港</strong>渡海西去，第三纪元结束。</p>
<p><img src="https://p6-tt.byteimg.com/origin/pgc-image/0931a4df949747f9b37ee6ff4a1eec7c?from=pc" alt="史上最完整的《指环王》魔戒战争大事记（编年体长文）"></p>
<p>好了，对于《指环王》魔戒战争时期的时间线梳理就到这里，如果您觉得有什么需要补充，欢迎给我留言，还是那句话，原创不易，请多多关注，感谢您的阅读!</p>
]]></content>
      <categories>
        <category>兴趣爱好</category>
        <category>指环王</category>
      </categories>
      <tags>
        <tag>兴趣爱好</tag>
        <tag>指环王</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part8：构建完整的语法分析器(下)</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part8/</url>
    <content><![CDATA[<p><a href="http://manistein.club/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="构建Lua解释器Part8：构建完整的语法分析器-下"><a href="#构建Lua解释器Part8：构建完整的语法分析器-下" class="headerlink" title="构建Lua解释器Part8：构建完整的语法分析器(下)"></a>构建Lua解释器Part8：构建完整的语法分析器(下)</h1><h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>​    前面，我已经用3篇blog的巨大篇幅，详细论述了Lua编译模块的一些基本知识。其中，Part5通过一个简单的打印hello world的例子，论述了编译出来的虚拟机指令，如何存到Proto结构中，最后再通过虚拟机执行的流程。Part6详细论述了，词法分析器的设计与实现。而Part7则论述了，expression的编译流程。本章，将作为Lua内置编译器论述的最后一个部分。往后的部分，将不再涉及编译相关的内容。当然，经历这篇，相信读者能够完全理解Lua内置编译器的设计与实现，个人认为完整度还是比较高的。</p>
<p>​    <a href="https://github.com/Manistein/dummylua-tutorial" rel="external nofollow noopener noreferrer" target="_blank">dummylua</a>基本仿照lua-5.3.5的设计，由于所有的实现，是我自己重新编写，因此在很多实现细节上，与官方的源码有所不同。但是基本的设计路线，遵循官方Lua的设计思路，测试用例的结果，也与官方Lua对照过。dummylua基本实现了Lua的语法，但是没有实现goto语句。本文将分为几个部分，首先讨论了Block的概念，然后先后论述了local-statement、do-end-statement、if-statement、while-statement、for-statement、repeat-statement、function-statement和return-statement的编译流程。最后由于本人水平有限，如果写的有不当的地方，希望各位大侠不吝赐教。如果你喜欢我的文章，喜欢技术讨论，可以加Q群185017593，也可以关注我的<a href="https://www.zhihu.com/people/wu-yin-jie" rel="external nofollow noopener noreferrer" target="_blank">知乎账号</a></p>
<h1 id="Lua的Block"><a href="#Lua的Block" class="headerlink" title="Lua的Block"></a>Lua的Block</h1><p>​    我们首先要厘清的第一个问题，就是lua的block，什么是block？简单的来说，lua函数体内或一些statement语句（如if-statement、while-statement、for-statement和reapeat-statement）内部的statement list的集合就是block，比如下面所示的：</p>
<figure class="highlight fortran"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span></span> <span class="keyword">name</span>()</span><br><span class="line">	<span class="keyword">block</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>函数体内部的所有代码，就组成了我们的block。那么如果我们的一个脚本文件，从未定义过任何函数，那么它是否包含在block中呢？比如我们的一个lua脚本，姑且称之为test.lua，其代码如下所示：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">-- test.lua</span><br><span class="line">local <span class="selector-tag">a</span> = <span class="number">1</span></span><br><span class="line">local <span class="selector-tag">b</span> = <span class="number">1</span></span><br><span class="line"><span class="function"><span class="title">print</span><span class="params">(a+b)</span></span></span><br></pre></td></tr></table></figure>
<p>test.lua脚本中，未定义任何一个函数，但是里面的代码，仍然属于一个block，这个block属于top-level function，我们编译一个脚本时，这个脚本就会被作为一个函数看待，这个函数就是top-level function。block与block之间的关系，我们可以通过图1来展示：<br><img src="block.png" alt="image">图1<br>由图可知，蓝色部分的block，属于top-level function的block，而函数foo内部，右括号到end之间的部分，则是函数foo的block。此外，带有do end结构的statement中，do和end之间的部分也是block。if-statement中，then和elseif、else或end之间的部分也是block。如果block内部的statement，也可以有它自己的block。现在我们对block的组织有了进一步的了解，那么它有什么用呢？block概念的存在，能够使我们快速清空一些local变量，并且更易于实现break语句，在后续的论述中，我将对这些进行解释和说明。</p>
<h4 id="block的数据结构"><a href="#block的数据结构" class="headerlink" title="block的数据结构"></a>block的数据结构</h4><p>​    现在我们来看一下block在dummylua中的数据结构，该结构被定义在luaparser.h文件中：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">BlockCnt</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">BlockCnt</span>* <span class="title">previous</span>;</span>  <span class="comment">// 前一个block</span></span><br><span class="line">	lu_byte nactvar; 			<span class="comment">// 进入新的block前，local变量总数</span></span><br><span class="line">	<span class="keyword">int</span> is_loop;  				<span class="comment">// 是不是循环语句的block</span></span><br><span class="line">	<span class="keyword">int</span> firstlabel;             <span class="comment">// 第一个标签，在标签列表中的位置，在</span></span><br><span class="line">	                            <span class="comment">// dummylua中，标签指的是break标签</span></span><br><span class="line">&#125; BlockCnt;</span><br></pre></td></tr></table></figure>
<p>现在，我逐一对BlockCnt内的字段进行说明：</p>
<ul>
<li>previous指针：该指针指向前一个block，当其是top-level function的block时，previous的值为NULL。<img src="block-previous.png" alt="image">图2</li>
<li>nactvar：活动变量总数，其实本质就是local变量的总数，这里存的是，进入新block前，当前的local变量总数，在退出当前block的时候，将根据这个值，清空当前block的local变量。</li>
<li>is_loop：循环语句的block，is_loop会被设置为true，当其为true时，在退出block阶段，将用于处理break语句的跳转。</li>
<li>firstlabel：当前block，第一个break语句，在全局标签列表中的位置。用于查找与循环语句相对应的break指令。</li>
</ul>
<p>上述逻辑，我对BlockCnt的成员变量，进行了简要说明，接下来的内容，将会逐步解开这些变量的神秘面纱。</p>
<h4 id="enterblock的逻辑流程"><a href="#enterblock的逻辑流程" class="headerlink" title="enterblock的逻辑流程"></a>enterblock的逻辑流程</h4><p>​    现在我们来讨论enterblock和leaveblock的逻辑流程，先来看看enterblock我们都做了什么事情，从实现层面来看，其逻辑代码如下所示：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line">static void enterblock(struct lua_State* L, LexState* ls, BlockCnt* bl, int is_loop) &#123;</span><br><span class="line">	F<span class="function"><span class="title">uncState</span>* fs = ls-&gt;</span>fs;</span><br><span class="line">	<span class="function"><span class="title">bl</span>-&gt;</span><span class="function"><span class="title">nactvar</span> = fs-&gt;</span><span class="function"><span class="title">nactvars</span>;  // 将当前local变量总数赋值给bl-&gt;</span>nactvar</span><br><span class="line">	<span class="function"><span class="title">bl</span>-&gt;</span>is_loop = is_loop;       <span class="comment">// 指定这个block是否是循环语句的block</span></span><br><span class="line">	<span class="function"><span class="title">bl</span>-&gt;</span><span class="function"><span class="title">previous</span> = fs-&gt;</span>bl;       <span class="comment">// 将当前FuncState实例的block，设置为新block的前一个block</span></span><br><span class="line">	<span class="function"><span class="title">fs</span>-&gt;</span>bl = bl;                 <span class="comment">// 将新block设置为FuncState实例的当前block</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (is_loop) &#123;</span><br><span class="line">		<span class="function"><span class="title">bl</span>-&gt;</span><span class="function"><span class="title">firstlabel</span> = ls-&gt;</span><span class="function"><span class="title">dyd</span>-&gt;</span>labellist.n; <span class="comment">// 记录该block第一个break语句应该处在的位置</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> &#123;</span><br><span class="line">		<span class="function"><span class="title">bl</span>-&gt;</span>firstlabel = -<span class="number">1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>每一步操作，注释都有说明，正如Part7所写，我们的FuncState实例，实际上是对某个function进行编译的时候，创建的用于暂时存储编译信息的临时变量。同样的，它也包含了当前编译block的信息，进行不同部分的编译，它也会切换block的指针。</p>
<h4 id="leaveblock的逻辑流程"><a href="#leaveblock的逻辑流程" class="headerlink" title="leaveblock的逻辑流程"></a>leaveblock的逻辑流程</h4><p>​    接下来，我们来看一下leaveblock的逻辑流程：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line">static void leaveblock(struct lua_State* L, LexState* ls) &#123;</span><br><span class="line">	F<span class="function"><span class="title">uncState</span>* fs = ls-&gt;</span>fs;</span><br><span class="line">	B<span class="function"><span class="title">lockCnt</span>* bl = fs-&gt;</span>bl;      </span><br><span class="line"></span><br><span class="line">	removevars(L, ls);          <span class="comment">// 清空当前local变量的统计，可以理解为当前block的local变量将被清空</span></span><br><span class="line">	<span class="function"><span class="title">fs</span>-&gt;</span><span class="function"><span class="title">nactvars</span> = bl-&gt;</span>nactvar; <span class="comment">// 获得除了当前block以外的local变量数量</span></span><br><span class="line">	<span class="function"><span class="title">fs</span>-&gt;</span><span class="function"><span class="title">freereg</span> = fs-&gt;</span>nactvars; <span class="comment">// 调整寄存器指示变量</span></span><br><span class="line">	<span class="function"><span class="title">fs</span>-&gt;</span><span class="function"><span class="title">nlocvars</span> = bl-&gt;</span>nactvar; <span class="comment">// 重置local变量，为进入当前block之前的状态，实际上也是清空本block</span></span><br><span class="line">								<span class="comment">// 的local变量的操作</span></span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="title">if</span> (bl-&gt;</span>is_loop) &#123; <span class="comment">// 如果是循环语句的block退出，则需要处理break逻辑</span></span><br><span class="line">		breakjump(L, ls, bl); </span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="title">fs</span>-&gt;</span><span class="function"><span class="title">bl</span> = bl-&gt;</span>previous; <span class="comment">// 返回上一层block</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面的注释，对leaveblock的操作，进行了一些说明，由于到目前为止，读者对一些概念还没有认知，因此只要理解block的概念，以及需要关注进入和离开实践即可，我将在后续逐步展开论述。</p>
<h1 id="local-statement编译流程"><a href="#local-statement编译流程" class="headerlink" title="local statement编译流程"></a>local statement编译流程</h1><p>​    现在我们将进入local statement编译流程的讨论之中，在开始正式讨论之前，我们先来看一下local statement的EBNF文法：</p>
<figure class="highlight delphi"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> namelist [<span class="string">'='' explist]</span></span><br><span class="line"><span class="string">namelist = Name &#123;'</span>,<span class="string">''</span> <span class="keyword">Name</span>&#125;</span><br></pre></td></tr></table></figure>
<p>对于explist的论述，Part7已经写了很多了，这里不再赘述，如果读者有回顾的需求，那么请回到Part7进行复习。对于local statement来说，它的EBNF文法非常简单，不过再继续讨论之前，我们还是要先复习一下FuncState的数据结构：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">FuncState</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> firstlocal;     <span class="comment">// 第一个local变量的位置</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">FuncState</span>* <span class="title">prev</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">LexState</span>* <span class="title">ls</span>;</span></span><br><span class="line">    Proto* p;           <span class="comment">// 主要存放虚拟机指令，常量表等</span></span><br><span class="line">    <span class="keyword">int</span> pc;             <span class="comment">// 下一个指令，应当存放在Proto结构中的code列表的位置</span></span><br><span class="line">    <span class="keyword">int</span> jpc;            <span class="comment">// 标记jump指令的位置，这些jump指令将跳转到新生成的指令的位置上</span></span><br><span class="line">    <span class="keyword">int</span> last_target;</span><br><span class="line">    <span class="keyword">int</span> nk;             <span class="comment">// 当前常量的数量</span></span><br><span class="line">    <span class="keyword">int</span> nups;           <span class="comment">// 当前upvalue的数量</span></span><br><span class="line">    <span class="keyword">int</span> nlocvars;       <span class="comment">// 当前local变量的数量</span></span><br><span class="line">    <span class="keyword">int</span> nactvars;       <span class="comment">// 当前活跃变量的数量</span></span><br><span class="line">    <span class="keyword">int</span> np;             <span class="comment">// 被编译的代码，Proto的数量</span></span><br><span class="line">    <span class="keyword">int</span> freereg;        <span class="comment">// 下一个可被使用的，空闲寄存器的位置</span></span><br><span class="line">&#125; FuncState;</span><br></pre></td></tr></table></figure>
<p>另外，我们还需要关注另外一个数据结构Dyndata：</p>
<figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Dyndata</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">		short* arr;     <span class="comment">// 记录local变量的信息，index为local变量所在的寄存器，而值则是Proto结构中</span></span><br><span class="line">						<span class="comment">// locvars列表的索引，用于查找local变量名</span></span><br><span class="line">		<span class="keyword">int</span> n;          <span class="comment">// 下一个local变量要存储的索引</span></span><br><span class="line">		<span class="keyword">int</span> <span class="built_in">size</span>;       <span class="comment">// array列表的大小</span></span><br><span class="line">	&#125; actvar;</span><br><span class="line">	Labellist labellist;</span><br><span class="line">&#125; Dyndata;</span><br></pre></td></tr></table></figure>
<p>Dyndata数据实例，是保存在词法分析器LexState实例的dyd变量中，在对这些结构，重新进行梳理之后，我们可以预想一下，准备开始编译lua脚本的内存形态，现在通过图3来展示这一点：<br><img src="local-statement-1.png" alt="image">图3</p>
<p>​    接下来，我们来看一下local statement的编译流程，我们前面已经展示了local statement的EBNF文法，当我们的语法分析器识别statement的首个token为local时，则需要判断该statement是个local statement还是local function statement（稍后会讨论）。当我们的代码，不符合local function statement的语法结构时，则进入到local statement的编译分支之中。token TK_LOCAL之后，到token ‘=’之前的部分，我们称之为variable（变量），如下所示：</p>
<figure class="highlight maxima"><table><tr><td class="code"><pre><span class="line"><span class="built_in">local</span> <span class="built_in">var1</span>, var2;</span><br><span class="line"><span class="built_in">local</span> var3, var4 = exp1, exp2</span><br></pre></td></tr></table></figure>
<p>上述伪代码中的var1、var2、var3和var4都是variable，那么我们的编译器会怎么处理这些variable呢？首先，他们会被按顺序存入Proto结构的locvars表中，同时，也会被存到Dyndata这个数据结构中，上面的例子，我们可以通过图4来展示：<br><img src="local-statement-2.png" alt="image">图4<br>图4中的locvars列表和arr列表，均填上了对应的信息。我们可以观察到var1~var4被存到了Proto的locvars列表中，而dyd-&gt;actvar-&gt;arr则存了locvars列表的索引（图中的红色数字为数组索引）。Proto结构中，locvars列表的下标，就指明了该local变量，位于哪个寄存器中。比如var1位于索引值为0的位置，那么它在lua stack中的位置就位于R(0)，var2位于R(1)，var3位于R(2)，var4位于R(3)。我们也可以观察到，指示下一个被使用的，空闲寄存器的变量freereg指向了R(4)，这么做的目的是，local变量是暂存在寄存器中，只要block没退出，这些local变量会一直占用这些寄存器（lua通过栈的空间，来表示寄存器），调整freereg的目的是为了避免原来的local变量被覆盖。<br>    local变量的赋值，和全局变量的赋值逻辑是很不一样的，就拿上面的例子来看，假设exp1和exp2的值为e1和e2，那么，我们将得到如图5的结果：<br><img src="local-statement-3.png" alt="image">图5<br>我们可以看到，值是直接被赋值到栈上的，并没有像全局变量一样，有个表，存储local变量的key，并将其值作为value进行存储。lua栈上的那些值，也只是示意用的，在编译阶段，其实并不会执行指令进行操作。那么现在一个新的问题来了，我们查找一个全局变量，可以到_ENV中查找，那么我们查找一个local变量，又该如何查找呢？现在，我们还是用回上面的例子，并以查找var1为例：</p>
<ul>
<li>首先，编译器会找到Dyndata结构中的arr数组，找到最右边的有效值，如图6所示：<img src="local-statement-4.png" alt="image">图6 图中右侧的蓝色箭头，就是arr表中的最后一个有效local变量，这里需要注意的是Dyndata是所有function共享的，因此每个function进行编译的时候，都要记录其首个local变量，在dyd-&gt;actvar-&gt;arr中的位置。</li>
<li>右侧蓝色箭头所指向的值为3，那么意味着这个local变量位于当前编译的function，的locvar列表中的第4个位置，也就是fs-&gt;p-&gt;locvars[3]这个变量，此时需要将它的变量名，和我们想要查找的var1变量名进行对比，因为变量名var4和var1不相等，因此这个local变量不是我们想要找的变量，此时右侧的蓝色箭头需要向左移一位，去查找下一个local变量，用相同的方式查找，看看它是否是我们要查找的变量。<img src="local-statement-5.png" alt="image">图7</li>
<li>根据上述的流程，fs-&gt;localvars[2]的变量名也不是var1，因此需要继续向左移动。</li>
<li>当右侧蓝色箭头，移动到fs-&gt;firstlocal的位置时，fs-&gt;localvars[0]上的local变量就是我们要查找的var1，此时获取locvars表的索引值0，这个0值，指示了我们的var1位于寄存器R(0)值，也就是说R(0)的值就是我们的local变量var1。<img src="local-statement-6.png" alt="image">图8</li>
</ul>
<p>为什么要以从右往左的顺序查找呢？按照我们的编译流程，dyd-&gt;actvar-&gt;arr列表中，越是在右边的local变量，说明其所处的block层次越深，而我们的local变量是允许重名的，比如下面的例子：</p>
<figure class="highlight maxima"><table><tr><td class="code"><pre><span class="line"><span class="built_in">local</span> <span class="built_in">var1</span> = <span class="number">10</span> </span><br><span class="line"></span><br><span class="line"><span class="keyword">do</span> </span><br><span class="line">	<span class="built_in">local</span> <span class="built_in">var1</span> = <span class="number">100</span></span><br><span class="line">	<span class="built_in">print</span>(<span class="built_in">var1</span>)</span><br><span class="line">end</span><br></pre></td></tr></table></figure>
<p>do和end之间，是一个层次更深的block，而do end之间的var1，此时位于dyd-&gt;actvar-&gt;arr的最右侧，而我们的print函数，显然是要找到离自己最近的var1，因此从右往左找，能够找到相同block的local变量，从而进行正确的处理。<br>    大家可以回顾一下Part5关于变量名查找的流程，当我们没办法在fs-&gt;p-&gt;locvars中找到想要查找的变量名时，则会去upvalue中查找，如果还找不到，则会到外层函数的local列表中查找，而后是外层函数的upvalue列表，然后如果再找不到，则需要再往外一层的函数查找。最后会到_ENV中，也就是去全局表中查找。当我们的local变量很多的时候，查找一个全局变量名是非常耗时的，这也是为什么，将全局变量先赋值到一个local变量能够提升运行效率的原因，如下所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> <span class="built_in">math</span> = <span class="built_in">math</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">math</span>.xxx()</span><br><span class="line">....</span><br></pre></td></tr></table></figure>
<p>我们的local变量，也不是能够被无限定义的，它也有一个上限值，默认是200个，超过200个local变量的定义，解释器将会在编译阶段报错。<br>    我已经对存储local变量的数据结构，识别和存放方式，赋值方法，和查找方法进行了介绍，接下来，我将通过一个新的例子，来展示local-statement的编译流程，来结束local-statement编译流程的讨论。接下来我们来看一下下面一个例子，看看其编译流程是怎样的：</p>
<figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line"><span class="built_in">local</span> <span class="keyword">a</span>, b, c = <span class="number">1</span>, <span class="number">2</span></span><br></pre></td></tr></table></figure>
<p>首先，我们的statement的首个token是TK_LOCAL，并且紧随其后的token是个TK_NAME类型的，因此此时进入到了local statement的编译分支之中。首先编译器会对local之后，‘=’之前的variable部分进行处理，即将他们存入到fs-&gt;p-&gt;locvars列表，和dyd-&gt;actvar-&gt;arr列表之中，于是得到图8的结果：<br><img src="local-statement-7.png" alt="image">图8<br>我们可以观察到，变量a、b和c都被存到了fs-&gt;p-&gt;locvars列表中了，而此时，常量表k不会写入任何东西，因为不需要，此时，fs-&gt;freereg也仍然指向R(0)，因为尚未生成任何指令，目前阶段也只是对variable进行处理。这里还需要关注的是ls-&gt;dyd-&gt;actvar-&gt;arr和fs-&gt;p-&gt;locvars的对应关系。<br>    在处理完variable之后，接下来，我们遇到了等号，此时编译器就会按照explist的处理流程，处理等号右边的exp列表了，其处理流程也非常简单，就是将1、2的值，先写入到常量表k中，然后生成两个OP_LOADK指令。由于左边的variable的数量，多于右边exp的数量，因此，多出来的variable需要赋值为nil。如果是variable的数量少于exp的数量时，多出来的exp则会被丢弃。编译后的结果，如图9所示：<br><img src="local-statement-8.png" alt="image">图9<br>注意此时的fs-&gt;freereg的指向，前面的寄存器已经被三个local变量占据了，所以fs-&gt;freereg的值为3。此外，这里需要注意的是，在编译上面的赋值语句的时候，没有查找变量的需要，只有后续有对local变量进行访问的需要时，才需要去查找local变量。到目前为止，我们就完成了local statement的编译流程了。</p>
<h1 id="do-end-statement编译流程"><a href="#do-end-statement编译流程" class="headerlink" title="do-end statement编译流程"></a>do-end statement编译流程</h1><p>​    对于do-end statement，我们可以先看看它的EBNF文法：</p>
<figure class="highlight fortran"><table><tr><td class="code"><pre><span class="line"><span class="keyword">do</span> </span><br><span class="line">	<span class="keyword">block</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>我们已经讨论过local-statement的编译流程了，现在可以进一步观察一下，enterblock和leaveblock对local变量的处理，承接上一节的例子，我们现在来看一个新的例子：</p>
<figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line"><span class="built_in">local</span> <span class="keyword">a</span>, b, c = <span class="number">1</span>, <span class="number">2</span></span><br><span class="line"><span class="built_in">do</span> </span><br><span class="line">	<span class="built_in">local</span> <span class="keyword">a</span>, b = <span class="number">3</span>, <span class="number">4</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>在完成第一行编译的时候，我们得到了图9的结果，接下来，我们则进入到do-end的编译逻辑之中了，此时要执行enterblock的逻辑流程，这时候，我们需要一个新的BlockCnt变量，用它来存储当前的local变量数量，于是得到图10的结果：<br><img src="local-statement-9.png" alt="image">图10<br>我们可以看到，图10左下角有两个BlockCnt的结构实例，左边那个是原来fs-&gt;bl指向的那个，进入到do-end block以后，则创建了一个新的BlockCnt实例，此时fs-&gt;bl要指向新的BlockCnt实例，而新BlockCnt实例的previous指针，则指向原来的那个BlockCnt实例。新的BlockCnt实例，要记录目前为止，do-end之外的所有local变量的总数，而此时的local变量一共有3个，因此nactvar的值为3。接下来，编译器将编译do-end之内的代码，首先将a和b加入到fs-&gt;p-&gt;locvars列表中，同时拓展ls-&gt;dyd-&gt;actvar-&gt;arr列表，于是得到图11的结果：<br><img src="local-statement-10.png" alt="image">图11<br>我们应该留意到，此时的fs-&gt;nactvars的值已经变成了5，因为此时的local变量已经达到了5个，而bl-&gt;nactvars则保持不变，因为它代表进入当前block前，local变量的总数，此时相应的信息也填入了fs-&gt;p-&gt;locvars和ls-&gt;dyd-&gt;actvar-&gt;arr中。接下来，就是编译等号右侧的explist。3和4是常量，因此，首先会将这两个数值，写入到常量表k中，也就是fs-&gt;p-&gt;k列表中，于是我们得到了图12的结果：<br><img src="local-statement-11.png" alt="image">图12<br>到这一步为止，do-end这个block内的代码就编译完了，接下来就是退出block的流程，首先要做的是，将fs-&gt;nlocalvars、fs-&gt;nactvars和ls-&gt;dyd-&gt;actvar.n的值回退回来，那么现在的问题来了，首先是回退多少？回退的含义是什么？针对第一个问题，回退多少呢？我们的目标是将当前block的local变量清空，那么回退的数量，自然是当前block的local变量的数量，于是，我们只需要将fs-&gt;nactvars - fs-&gt;bl-&gt;nactvars就能得到，因为我们的fs-&gt;nactvar总是能和fs-&gt;nlocalvars变量同步。第二个问题，回退的含义是什么？意味着原来被占用的空间，可以给新的block的local变量暂存。在完成leaveblock的操作之后，我们得到了图13的结果：<img src="local-statement-12.png" alt="image">图13<br>这里还需要注意的是，我们的fs-&gt;freereg也被重置了，直接被置放到local变量c的上方，这样清退了前一个block的local变量，至于之前的局部变量的一些信息，gc将会在合适的时间将其清理。到目前为止，我们就完成了do-end block的编译流程了。</p>
<h1 id="if-statement编译流程"><a href="#if-statement编译流程" class="headerlink" title="if statement编译流程"></a>if statement编译流程</h1><p>​    我们现在将进入到if statement的编译流程的讨论之中，按照惯例，首先要描述if statement的EBNF文法，其EBNF文法如下所示：</p>
<figure class="highlight maxima"><table><tr><td class="code"><pre><span class="line">ifstat ::= <span class="keyword">if</span> cond-<span class="built_in">part</span> <span class="keyword">then</span> <span class="built_in">block</span> &#123; <span class="keyword">elseif</span> cond-<span class="built_in">part</span> <span class="keyword">then</span> <span class="built_in">block</span> &#125; [ <span class="keyword">else</span> <span class="built_in">block</span> ] end</span><br><span class="line">cond-<span class="built_in">part</span> ::= expr</span><br></pre></td></tr></table></figure>
<p>从EBNF文法来看，当statement的首个token是TK_IF的时候，进入ifstat的编译分支，花括号内的部分，代表可以出现0次或者多次，方括号内的部分，代表可以出现0次或者1次。我们的expr的编译流程，已经在Part7进行了详细的说明了，这里不再赘述。<br>    尽管EBNF文法，能够很好地描述if statement的语法结构，但是有些细节它是无法表达的，比如cond-part，不仅仅只是要计算一个expr的值，并把值放入某个寄存器就可以了，它还需要生成对该寄存器进行测试的指令，还需要进行条件跳转处理、逃逸跳转处理等。到目前为止，我们还有一个任务没有做完，就是对if statement的结构，进行进一步说明，从结构上看，if statement由几个部分来组成：</p>
<ul>
<li>表示随后跟着的是cond-part的系统保留字：if、elseif</li>
<li>cond-part：条件判断的部分</li>
<li>then-part：系统保留字then和其后的block</li>
<li>else-part：系统保留字else和其后的block</li>
<li>表示结尾的部分：系统保留字end</li>
</ul>
<p>接下来，我首先会通过一张概念图来展示if statement的跳转流程，最后通过一个例子，展现if statement的编译流程。<br><img src="if-statement-1.png" alt="image">图14<br>我们可以看到，上图中，黑色箭头代表测试失败时的跳转，当一个cond-part测试失败，要么跳转到下一个cond-part的测试中，要么跳转到else-part中，亦或是直接跳过if statement，进入下一个statement。而红色箭头表示，当一个then-part结束时，那么它需要跳出来，执行图14中的next instruction，因为if statement每次只能执行一个分支。接下来，我们来看一个例子，并且通过编译这个例子来看看if statement的整体编译流程：</p>
<figure class="highlight gauss"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> a, b, c = <span class="number">10</span>, <span class="number">30</span>, <span class="number">20</span></span><br><span class="line"><span class="keyword">if</span> a &gt;= b then</span><br><span class="line">	<span class="keyword">print</span>(<span class="string">"a&gt;=b"</span>)</span><br><span class="line"><span class="keyword">elseif</span> a &lt;= c then</span><br><span class="line">	<span class="keyword">print</span>(<span class="string">"a&lt;=c"</span>)</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">	<span class="keyword">print</span>(<span class="string">"else-part"</span>)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>由于在Part7中，我花了巨量的篇幅论述了expr的编译流程，因此这里会略过很多细节，直接写入生成结果。首先第一步，我们要对local statement进行编译，得到了图15的结果：<br><img src="if-statement-2.png" alt="image">图15<br>接下来就进入到了if statement的编译流程了，首先我们要先跳过TK_IF这个token，然后开始编译第一个cond-part：a&gt;=b，于是得到图16的结果：<br><img src="if-statement-3.png" alt="image">图16<br>上图code列表中，被染成红色的两个指令，就是我们的cond-part编译后的结果，这里我们回顾一下OP_LE的指令：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">OP_LE</span> A <span class="keyword">B </span>C</span><br><span class="line"><span class="symbol">if</span> ((RK(<span class="keyword">B) </span>&lt;= RK(C)) ~= A) then <span class="built_in">pc</span>++]</span><br></pre></td></tr></table></figure>
<p>在本例中的含义则是，R(1)&lt;=R(0)的结果不等于0（也就是R(1)&lt;=R(0)即b&lt;=a成立），那么PC寄存器自增，也就是跳过OP_JMP指令，直接执行第一个then-part的逻辑，否则执行JUMP指令，这个JUMP指令会跳到下一个cond-part中进行新的判断。这里需要注意的是，cond-part里生成的OP_JMP指令，是iAsBx模式，也就是高18位来表示跳转的相对地址值，为了既能够表示正值，又能够表示负值，编译器用了131071表示0值，-1则表示为131070，这里为了方便读者理解，直接填写-1，而不是131070。接下来，编译器会对第一个then-part进行编译，得到图17的结果：<br><img src="if-statement-4.png" alt="image">图17<br>这个then-part编译好的指令，我通过蓝色标记出来，主要用于区分。完成了第一个then-part的编译，接下来到了elseif之后的那个cond-part的编译了，得到图18的结果。<br><img src="if-statement-5.png" alt="image">图18<br>我们可以看到，第一个OP_JMP指令，指向了第二个cond-part的第一个指令，因为PC寄存器自身就会执行自增的操作，因此这里的-1被改成了4。接下来，就会对第二个then-part进行编译，于是得到图19的结果：<br><img src="if-statement-6.png" alt="image">图19<br>第二个then-part的编译流程很简单，就是将”a&lt;=c”这个字符串打印。接下来则是进行else-part的编译，得到图20的结果：<br><img src="if-statement-7.png" alt="image">图20<br>图中的紫色部分代码，就是else-part的编译结果，它先将print函数入栈，然后将“else-part”这个字符串入栈，最后调用这个函数。else-part完成编译之后，我们就进入到了最后的收尾阶段，这里需要处理逃逸跳转，也就是then-part执行完之后，跳到下一个statement生成的第一个指令之中，于是得到了图21的结果。<br><img src="if-statement-8.png" alt="image">图21<br>到目前为止，我就完成了if statement编译流程的论述了。</p>
<h1 id="while-statement编译流程"><a href="#while-statement编译流程" class="headerlink" title="while statement编译流程"></a>while statement编译流程</h1><p>​    现在我们进入到了，循环语句的编译流程之中了，现在先从while statement开始。首先我们来看一下，while statement的EBNF文法：</p>
<figure class="highlight elixir"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> <span class="keyword">cond</span>-part <span class="keyword">do</span> block <span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>严格来说，while statement主要由以下几个部分组成：</p>
<ul>
<li>表示进入while statement编译流程的首部token–while</li>
<li>cond-part：条件判断部分，判断是否要进入循环</li>
<li>do-end：while循环语句的body部分，执行其他的statement list</li>
<li>跳转回cond-part的JMP指令：这个指令紧随block的最后一个指令，跳转回cond-part判断是否需要继续循环</li>
</ul>
<p>我先通过一个逻辑结构图，来展示while语句的语法结构，然后通过一个例子来对while语句的编译流程进行阐述：<br><img src="while-statement-1.png" alt="image">图22<br>我们现在来看一个新的例子，并借助这个例子来观察一下，while语句的编译流程：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">local <span class="built_in">i</span> = <span class="number">0</span></span><br><span class="line"><span class="keyword">while</span> <span class="built_in">i</span> &lt; <span class="number">10</span> do </span><br><span class="line">	<span class="built_in">i</span> = <span class="built_in">i</span> + <span class="number">1</span></span><br><span class="line">	print(<span class="built_in">i</span>)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>首先，编译器会先编译第一行的statement，然后得到图23的结果：<br><img src="while-statement-2.png" alt="image">图23<br>我们可以看到，在图23中，将local变量i，写入了fs-&gt;p-&gt;locvars列表中，将0写入常量表fs-&gt;p-&gt;k中，此外ls-&gt;dyd-&gt;arr也有所调整。接下来，则进入到while语句的编译之中，在识别了while statement的首个token–TK_WHILE以后，编译器进入到了cond-part（i&lt;10）的编译之中，得到如图24的结果：<br><img src="while-statement-3.png" alt="image">图24<br>和if statement类似，这里的cond-part，除了将i&lt;10这个exp编译成指令，还在这个exp指令之后，添加了一个OP_JUMP指令，这个jump指令，将在我们测试失败时，跳转到下一个statement的第一个指令中。接下来，我们就进入到了while语句的body部分的编译了，这里需要进入到新的block，首先我们会进行i=i+1的编译工作，于是得到图25的结果：<br><img src="while-statement-4.png" alt="image">图25<br>新的指令，将local变量i增加了1，并且将结果存回到local变量i的寄存器位置上。接下来要执行block的另一个语句，就是print(i)，于是得到图26的结果：<br><img src="while-statement-5.png" alt="image">图26<br>上图的结果，首先将print函数放到栈中，然后将参数i的值，move到函数print的上方，最后通过调用函数的指令来执行这个流程。到目前为止，while body内的block编译就结束了，但是这是一个循环，因此block执行完之后，还需要跳转到cond-part进行新的判断，于是，我们需要新增一个jump指令，得到图27的结果：<br><img src="while-statement-6.png" alt="image">图27<br>jump指令跳转到了条件判断的流程之中，同时OP_LT指令测试失败时，我们的指令跳转到下一个statement的第一个指令中。到目前为止，我对while语句的编译流程的的论述就结束了。</p>
<h1 id="repeat-statement编译流程"><a href="#repeat-statement编译流程" class="headerlink" title="repeat statement编译流程"></a>repeat statement编译流程</h1><p>​    现在，我们将进入到repeat statement的编译流程的论述之中了，其EBNF文法的结构如下所示：</p>
<figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line"><span class="keyword">repeat</span> <span class="keyword">block</span> <span class="keyword">until</span> cond-part</span><br></pre></td></tr></table></figure>
<p>现在我来对repeat statement的组成结构进行详细的说明：</p>
<ul>
<li>statement的第一个token：这个token为repeat，它标志着编译流程进入到repeat statement的编译流程分支之中</li>
<li>body部分：repeat之后，until之前的statement list集合，就是repeat statement的body部分</li>
<li>表示cond-part的引导token，这个token即是until，在其之后即是条件判断部分，cond-part</li>
<li>cond-part：条件判断，判断是否能够继续循环</li>
</ul>
<p>我通过一张逻辑图来展示，repeat的循环跳转逻辑：<br><img src="repeat-statement-1.png" alt="image">图28<br>接下来，我通过一个例子，来展示repeat statement的编译流程：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">local <span class="built_in">i</span> = <span class="number">0</span></span><br><span class="line">repeat</span><br><span class="line">	<span class="built_in">i</span> = <span class="built_in">i</span> + <span class="number">1</span></span><br><span class="line">until <span class="built_in">i</span> &gt; <span class="number">100</span></span><br></pre></td></tr></table></figure>
<p>首先，编译器会执行local statement的编译，得到图29的结果，于是有：<br><img src="repeat-statement-2.png" alt="image">图29<br>接下来，则进入到repeat statement的body部分，现在开始编译i=i+1，于是得到图30的结果：<br><img src="repeat-statement-3.png" alt="image">图30<br>这里将local变量i和常量1相加，并且存回到local变量i的寄存器中。到这一步为止，repeat statement的body部分就执行完了，后面则跳过until，对cond-part进行编译，得到图31的结果：<br><img src="repeat-statement-4.png" alt="image">图31<br>上图的指令，如果测试通过，则跳过jump指令，进入下一个指令，如果测试失败，则回到红色箭头所指的指令。到目前为止，我们就完成了repeat statement的编译流程探索了，对于其他的情形，读者可以结合Part7的内容，进行推导。</p>
<h1 id="for-statement编译流程"><a href="#for-statement编译流程" class="headerlink" title="for statement编译流程"></a>for statement编译流程</h1><p>​    接下来，我们要探索的是最后一种循环语句–for循环。for循环语句，从类别上分，主要有两大类，一类是fornum，一类是forlist，他们的语法结构也是不同的。fornum的EBNF文法如下所示：</p>
<figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> Name <span class="string">'='</span> <span class="built_in">exp</span> <span class="string">','</span> <span class="built_in">exp</span> [<span class="string">','</span> <span class="built_in">exp</span>] <span class="keyword">do</span> block <span class="built_in">end</span></span><br></pre></td></tr></table></figure>
<p>fornum的语法结构，我们可以梳理成如下的形式：</p>
<ul>
<li>开头标识的token–for：表示应当进入到for statement编译流程</li>
<li>index-part：索引值定义部分，在fornum类型中，这里一般是给一个局部变量赋初值，Name是TK_NAME类型的token，是个局部变量，=后的exp的值应当是数值类型</li>
<li>limit-part: 限制部分，就是初始化阶段，定义的局部变量的值，超过limit之后，将不再进行循环</li>
<li>step-part：每次执行完fornum的body之后，初始化部分的局部变量，每次要加的值，可以省略，默认值是1</li>
<li>body-part：do-block-end的部分，是循的本体</li>
</ul>
<p>我们现在通过一张图来感受一下，fornum的语法构成：<br><img src="for-statement-1.png" alt="image">图32<br>接下来，我通过一个例子来说明fornum的编译流程：</p>
<figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> i = <span class="number">1</span>, <span class="number">10</span> <span class="keyword">do</span> </span><br><span class="line">	<span class="built_in">print</span>(i)</span><br><span class="line"><span class="built_in">end</span></span><br></pre></td></tr></table></figure>
<p>语句很简单，编译器首先识别statement的第一个token是TK_FOR，然后识别到了exp “i = 1”，表明现在要进入fornum的编译流程之中。此时编译器分别对fornum语句的index-part、limit-part和step-part进行编译，得到图33的结果：<br><img src="for-statement-2.png" alt="image">图33<br>上图包含的信息很多，已经涵盖了fornum语句的初始化部分了，我们可以把这个部分的操作作如下归纳：</p>
<ul>
<li>生成4个local变量，分别是(for index)、(for limit)、(for step)和i。前面三个分别对应的是，for语句中的局部index变量i，循环限制10，每次循环递增量1(默认值)，以及表示局部的index变量i。第四个变量，其实是(for index)的值赋值过来的，至于为什么要做这个操作，和upvalue有关，相关的问题，我们需要在后续的upvalue相关的章节里深入探讨。在lua虚拟机运行阶段，完成fornum初始化的堆栈信息，如下所示：<img src="for-statement-3.png" alt="image">图34</li>
<li>fornum语句，初始化阶段，分别通过OP_LOADK指令，将对应的值，放置到对应的寄存器中。这里需要留意的是OP_FORPREP指令，这个指令的作用是，对目标寄存器，也就是我们的(for index)，减去(for step)的值，然后跳转到循环判断指令OP_FORLOOP指令处，进行是否执行循环逻辑的判断，用公式表达它做的行为，则是：R(A)-=R(A+2); pc+=sBx。由于，现在OP_FORLOOP指令尚未生成，因此OP_FORPREP指令的第二个参数，目前赋值为NO_JUMP也就是-1</li>
</ul>
<p>在完成了fornum的初始化流程之后，现在进入到body的编译流程之中，首先，我们会进入到新的block，然后对print(i)进行编译，最后生成一个判断是否执行循环体的指令OP_FORLOOP，于是我们得到如图35的结果：<br><img src="for-statement-4.png" alt="image">图35<br>    现在我们完成了完整的fornum语句编译了，这里需要注意几个点，第一个是当我们执行到OP_FORPREP指令之后，会对(for index)进行初始化操作，也就是将(for index)的值，减去(for step)的值，然后跳转到OP_FORLOOP指令处。OP_FORLOOP指令，会根据(for step)的情况，来对(for index)和(for limit)进行比较判断，当(for step)大于等于0的时候，如果(for index)的值小于或者等于(for limit)的值时，那么PC寄存器会从OP_FORLOOP指令，顺着图中的红色箭头，跳转到目标指令处，否则执行OP_FORLOOP的下一个指令。当(for step)的值小于0的时候，如果(for step)小于(for limit)的值的时候，OP_FORLOOP跳转到红色箭头所指的指令中，否则执行OP_FORLOOP的下一个指令。此外，OP_FORLOOP指令会令(for index)的值加上(for step)的值，并将结果覆盖到(for index)上，当OP_FORLOOP满足跳转条件时，会令index_value部分赋值为(for index)的值，用伪代码表示则是：</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line"><span class="built_in">R</span>(A)+=<span class="built_in">R</span>(A+2); <span class="keyword">if</span> <span class="built_in">R</span>(A) &lt; ? = <span class="built_in">R</span>(A+1) then &#123; pc+=sBx; <span class="built_in">R</span>(A+3)=<span class="built_in">R</span>(A) &#125;</span><br></pre></td></tr></table></figure>
<p>​    现在我们将进入到forlist语句的编译流程探索之中，依照惯例，我们首先看一下forlist的EBNF文法：</p>
<figure class="highlight fortran"><table><tr><td class="code"><pre><span class="line">for <span class="keyword">namelist</span> <span class="keyword">in</span> explist <span class="keyword">do</span> <span class="keyword">block</span> <span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>按照上面的EBNF文法，理论上，forlist语句中，用于循环的局部变量可以是无限多的，系统保留字in之后的exp列表理论上也应当支持无限多，dummylua目前只支持最常规的写法，因此EBNF文法将会调整为如下所示：</p>
<figure class="highlight fortran"><table><tr><td class="code"><pre><span class="line">for kname, vname <span class="keyword">in</span> funccall(<span class="built_in">exp</span>) <span class="keyword">do</span> <span class="keyword">block</span> <span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>这里默认funccall是我们初始化迭代器的函数，而exp则是它的参数，即是那个要被遍历的表达式，exp在这里要求是一个table，上面的kname和vname则是遍历时的k和v的值。接下来，我们通过两个例子来探讨一下forlist的编译流程，第一个是我们的ipairs调用，第二个是pairs调用。先来看一下第一个例子：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> k, v <span class="keyword">in</span> <span class="built_in">ipairs</span>(t) <span class="keyword">do</span> </span><br><span class="line">	<span class="built_in">print</span>(k, v)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>进入编译前，我们需要先对forlist语句，进行初始化操作，for之后do之前的部分，均参与到了编译初始化的流程之中，于是得到图36的结果：<br><img src="for-statement-5.png" alt="image">图36<br>图36的初始化阶段，做了几个操作，首先设置了一些局部变量(for generator)、(for state)、(for control)、kvar和vvar，其中kvar和vvar分别对应上述的k、v值，而前面的几个部分，后面会讨论。接着编译器将ipairs函数压入栈中，然后把ipairs的参数也压入栈中，接着通过OP_CALL指令，调用了ipairs函数，这个ipairs函数，在base库中是luaB_ipairs，其定义如下所示：</p>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luabase.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">luaB_ipairs</span>(<span class="params"><span class="keyword">struct</span> lua_State* L</span>)</span> &#123;</span><br><span class="line">	lua_pushvalue(L, <span class="number">-1</span>); </span><br><span class="line">	setfvalue(L-&gt;top - <span class="number">2</span>, ipairs);</span><br><span class="line">	lua_pushinteger(L, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个函数的作用，则是将内部的luabase.c内的c函数–ipairs（注意不是脚本中的ipairs函数）设置到(for generator)域，将要遍历的参数t放置到(for state)域，将0值设置到(for control)域，并预留kvar和vvar的位置，在虚拟机运行阶段，在完成forlist的初始化操作之后，虚拟机的内存结构则是图37的结果：<br><img src="for-statement-6.png" alt="image">图37<br>图中ipairs的函数逻辑，如下所示：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luabase.c</span></span><br><span class="line">static int ipairs(struct lua_State* L) &#123;</span><br><span class="line">	S<span class="function"><span class="title">tkId</span> t = L-&gt;</span>top - <span class="number">2</span>;</span><br><span class="line">	<span class="keyword">if</span> (!ttistable(t)) &#123;</span><br><span class="line">		printf(<span class="string">"ipairs:target is not a table"</span>);</span><br><span class="line">		luaD_throw(L, LUA_ERRRUN);</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	S<span class="function"><span class="title">tkId</span> key = L-&gt;</span>top - <span class="number">1</span>;</span><br><span class="line">	TV<span class="function"><span class="title">alue</span>* v = (TValue*)luaH_getint(L, gco2tbl(gcvalue(t)), key-&gt;</span>value_.i + <span class="number">1</span>);</span><br><span class="line">	<span class="keyword">if</span> (ttisnil(v)) &#123;</span><br><span class="line">		lua_pushnil(L);</span><br><span class="line">		lua_pushnil(L);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> &#123;</span><br><span class="line">		<span class="function"><span class="title">lua_pushinteger</span>(L, key-&gt;</span>value_.i + <span class="number">1</span>);</span><br><span class="line">		<span class="function"><span class="title">setobj</span>(L-&gt;</span>top, v);</span><br><span class="line">		increase_top(L);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	return <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个函数的作用，就是遍历table t中的array域，并返回刚刚遍历到的index和value的值。下次调用ipairs函数是，上次遍历得到的key结果+1，作为新的索引，去取新的值，然后将新的索引值，赋值到kvar域中。这里只是简单阐述一下，ipairs要做的事情，实际上，这个函数是在OP_TFORCALL指令里调用，初始化阶段，并不会调用它，在虚拟机执行阶段，forlist语句的初始化阶段完成之后，内存结果就是图37所示的那样。<br>    接下来，则是对do-end部分的forbody进行编译，得到图38的结果，于是有：<br><img src="for-statement-7.png" alt="image">图38<br>OP_JUMP之后的指令，则是forbody内部编译的指令，其本质就是打印k,v的值。在forbody退出之前，编译器还需要添加两个指令，一个是OP_TFORCALL，另一个则是OP_TFORLOOP。<br><img src="for-statement-8.png" alt="image">图39<br>注意图中的OP_JUMP指令和OP_TFORLOOP指令的跳转。到这个阶段，其实forlist的编译工作就完成了，不过这里还是要对OP_TFORCALL和OP_TFORLOOP指令，结合上面的实例进行说明：</p>
<ul>
<li>OP_TFORCALL：它是iABC模式，A域指定(for generator)的位置，B域不使用，C则指明了从(for generator)中接收多少个返回值。执行它的时候，如果我们的(for generator)位于R(A)，那么此时，它会将迭代函数、迭代的table和当前迭代的key设置到R(A+3)~R(A+5)的位置上，于是在运行期lua栈得到图40的结果：<img src="for-statement-9.png" alt="image">图40 接着，我们的OP_TFORCALL指令，会调用位于R(A+3)的ipairs函数，然后从ipairs函数接收C个返回值，并且将其从R(A+3)的位置开始赋值，得到图41的结果：<img src="for-statement-10.png" alt="image">图41</li>
<li>OP_TFORLOOP：它是iAsBx模式，这个指令是和OP_TFORCALL指令相辅相成的，它首先判断图41的R(A+3)值是否是nil值（当完成table array的最后一个元素的遍历时，ipairs函数会返回nil值），如果不为nil值，则将R(A+3)赋值给R(A+2)，然后执行图39的跳转指令，得到图42的结果，否则执行下一个指令，跳出循环：<img src="for-statement-11.png" alt="image">图42 我们可以看到，对于ipairs函数而言，我们输入一个key，它会返回该key的下一个key和对应的value值，放置在R(A+3)和R(A+4)的位置上（这两个值对应token in前面的k和v，可以通过搜索local变量k和v，找到他们的值），由于table的array最小下标是1，因此luaB_ipairs在初始化阶段，将(for index)设置为0，因此在迭代器中，默认0的下一个key是1，并返回1和value1，下次调用(for generator)时，则会将1传入，获得2和value2值，以此类推，直至(for generator)返回nil为止，遍历结束。</li>
</ul>
<p>​    我们的另一个forlist的例子，则如下所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> k, v <span class="keyword">in</span> <span class="built_in">pairs</span>(t) <span class="keyword">do</span> </span><br><span class="line">	<span class="built_in">print</span>(k, v)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>它的编译指令和前面的ipairs是一样的，只是我们的(for generator)则替换成了pairs，这个函数不仅会遍历table的array部分，还会遍历hash表部分，终止的条件都是一样的，这部分留给读者结合源码去推导，这里不再赘述。到目前为止，我就完成了for statement的编译流程论述了。</p>
<h1 id="break-statement编译流程"><a href="#break-statement编译流程" class="headerlink" title="break statement编译流程"></a>break statement编译流程</h1><p>​    在完成了循环语句的讨论以后，现在要讨论一个问题了，如何跳出循环？除了循环语句的cond-part以外，我们还可以通过break语句来跳出循环，那么我们现在就来讨论一下它的设计与实现原理。要彻底理解break语句的逻辑，首先还是要从数据结构层面入手。我们前面已经探讨了LexState结构和Dyndata结构，现在我们需要给Dyndata添加新的成员，如下所示：</p>
<figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Labeldesc</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">TString</span>* <span class="title">varname</span>;</span> <span class="comment">// 标签名称</span></span><br><span class="line">	<span class="keyword">int</span> pc;					 <span class="comment">// 标签生成的jump指令的相对地址</span></span><br><span class="line">	<span class="keyword">int</span> <span class="built_in">line</span>;                <span class="comment">// 标签出现在代码中的哪一行</span></span><br><span class="line">&#125; Labeldesc;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Labellist</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">Labeldesc</span>* <span class="title">arr</span>;</span>   <span class="comment">// 标签列表</span></span><br><span class="line">	<span class="keyword">int</span> n; 					 <span class="comment">// 下一个标签在标签列表中的位置索引</span></span><br><span class="line">	<span class="keyword">int</span> <span class="built_in">size</span>; 				 <span class="comment">// 标签列表的大小</span></span><br><span class="line">&#125; Labellist;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Dyndata</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">		short* arr; </span><br><span class="line">		<span class="keyword">int</span> n;</span><br><span class="line">		<span class="keyword">int</span> <span class="built_in">size</span>;</span><br><span class="line">	&#125; actvar;</span><br><span class="line">	Labellist labellist;    <span class="comment">// 标签列表</span></span><br><span class="line">&#125; Dyndata;</span><br></pre></td></tr></table></figure>
<p>上面的代码，展示了新的数据结构，标签和标签列表，在dummylua中，由于我没有实现goto语句，因此这里的标签列表其实质就是break标签的列表。我们在block一节里介绍过BlockCnt这个数据结构，当我们enterblock的时候，会记录该block如果有break语句，那么这个break语句会位于Labellist里的哪个位置，只有当Labellist的n值&gt;BlockCnt的firstlabel的值时，才代表这个block内部有break语句。<br>    那么我们的break语句是怎么编译的呢？首先我们要搞清楚block和Labellist的关系，我们现在来看一个例子：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">local <span class="built_in">i</span> = <span class="number">0</span></span><br><span class="line"><span class="keyword">while</span> <span class="built_in">true</span> do </span><br><span class="line">	<span class="built_in">i</span> = <span class="built_in">i</span> + <span class="number">1</span></span><br><span class="line">	<span class="keyword">if</span> <span class="built_in">i</span> &gt;= <span class="number">10</span> then </span><br><span class="line">		<span class="keyword">break</span></span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>首先我们通过<a href="https://github.com/Manistein/dummylua-tutorial/tree/master/tools/protodump" rel="external nofollow noopener noreferrer" target="_blank">protodump</a>工具来看一下，它的编译结果是怎样的：</p>
<figure class="highlight gherkin"><table><tr><td class="code"><pre><span class="line">file_name = ../test.lua.out</span><br><span class="line">+type_name [LClosure]</span><br><span class="line">+proto+type_name [Proto]</span><br><span class="line">|<span class="string">     +source [@../test.lua]</span></span><br><span class="line">|<span class="string">     +maxstacksize [2]</span></span><br><span class="line">|<span class="string">     +k+1 [int:0]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +2 [int:1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +3 [int:10]</span></span><br><span class="line">|<span class="string">     +numparams [0]</span></span><br><span class="line">|<span class="string">     +lineinfo+1 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +2 [3]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +3 [4]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +4 [4]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +5 [6]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +6 [7]</span></span><br><span class="line">|<span class="string">     +is_vararg [1]</span></span><br><span class="line">|<span class="string">     +upvalues+1+idx [0]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">          +instack [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">          +name [_ENV]</span></span><br><span class="line">|<span class="string">     +p</span></span><br><span class="line">|<span class="string">     +locvars+1+startpc [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">         +endpc [6]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">         +varname [i]</span></span><br><span class="line">|<span class="string">     +lastlinedefine [0]</span></span><br><span class="line">|<span class="string">     +code+1 [iABx:OP_LOADK     A:0   Bx :0         ; R(A) := Kst(Bx)]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +2 [iABC:OP_ADD       A:0   B  :0   C:257 ; R(A) := RK(B) + RK(C)]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +3 [iABC:OP_LE        A:1   B  :258 C:0   ; if ((RK(B) &lt;= RK(C)) ~= A) then pc++]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +4 [iAsBx:OP_JMP      A:0   sBx:131072    ; pc+=sBx; if (A) close all upvalues &gt;= R(A - 1)]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +5 [iAsBx:OP_JMP      A:0   sBx:131067    ; pc+=sBx; if (A) close all upvalues &gt;= R(A - 1)]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +6 [iABC:OP_RETURN    A:0   B  :1         ; return R(A), ... ,R(A+B-2)  (see note)]</span></span><br><span class="line">|<span class="string">     +linedefine [0]</span></span><br><span class="line"><span class="string">+upvals+1+name [_ENV]</span></span><br></pre></td></tr></table></figure>
<p>上面的code就是我们的指令列表，其中，code指令列表中的第4个指令，就是根据我们的break语句生成出来的jump，当i&gt;=10的条件满足时，它就跳出循环。接下来我们再通过一张图来观察一下，这段代码有几个block？这些block和Labellist有什么关系？break语句怎么处理？<br><img src="break-statement-1.png" alt="image">图43<br>从图中，我们可以观察到，最下部将我们的范例代码，通过色块的方式结构化了，每一种颜色，代表一个block。我们的break语句处于最内存的block之中，而且block的previous指针，指向了上一层的block，每个BlockCnt结构的firstlabel都指向了全局的label列表的第0个位置，且is_loop变量我也标红了。可以观察上面已经完成编译的虚拟机指令集合，当我们编译到最内层block的break语句时，会生成一个OP_JMP指令，并且将break写入标签列表ls-&gt;dyd-&gt;labellist-&gt;n则向右移动了一位(从0移到1的位置，如图43蓝色箭头所示的部分)，此时并没有立即处理OP_JMP指令的跳转位置，而是暂存起来。当我们退出break所在的那个block时，因为那个block不是循环语句的block，因此leave这个block并不会进行任何的break语句处理。当我们退出中间的那个block时，因为它是循环语句的block，因此我们需要在ls-&gt;dyd-&gt;labellist-&gt;arr从右往左找break语句，将每一个break语句找出，找到他们所对应的jump指令地址，将跳转的位置设置到要退出的block外的第一个指令，直至n移动到block的firstlabel所指的位置为止。完成之后，ls-&gt;dyd-&gt;labellist-&gt;n则向左移动一位（从1移动到0的位置），此时n和蓝色块block的firstlabel相等，因此代表这个block没有break语句，不需要处理。<br>    到现在为止，我们就完成了break statement的编译流程探讨了，接下来将进行function statement和return statement的编译流程探讨。</p>
<h1 id="function-statement的编译流程"><a href="#function-statement的编译流程" class="headerlink" title="function statement的编译流程"></a>function statement的编译流程</h1><p>​    现在我们来探讨一下function statement的编译流程，首先我们来观察一下，function statement的EBNF文法：</p>
<figure class="highlight xquery"><table><tr><td class="code"><pre><span class="line">[local] <span class="keyword">function</span><span class="built_in"> name</span> &#123;<span class="string">'.'</span><span class="built_in"> name</span>&#125; [<span class="string">':'</span><span class="built_in"> name</span>] <span class="string">'('</span> explist <span class="string">')'</span></span><br><span class="line">	block</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>方括号内代表的是，该部分出现0次或1次，而花括号内出现的部分，表示出现0次或多次。上面的EBNF文法，展示了function statement的语法结构，如果还要细分，可以按照如下几个点来：</p>
<ul>
<li>name-part:token function之后，token ‘(‘之前的部分，被我称之为name-part。因为它定义了函数的名称，提供了查找函数的依据</li>
<li>func-part:token ‘(‘之后，token end之前的部分，是函数的本体</li>
</ul>
<p>现在我们通过两个例子来探讨一下，首先来看一下第一个例子：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> <span class="function"><span class="keyword">function</span> <span class="title">test</span><span class="params">(a, b, c)</span></span></span><br><span class="line">	<span class="built_in">print</span>(a, b, c)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>这个例子，定义了一个local函数，因为有了local保留字，因此这个函数的name-part就是在栈上的一个局部变量，我们现在先来看看编译器对name-part的编译结果，如图44所示：<br><img src="function-statement-1.png" alt="image">图44<br>从图44来看，name-part的编译处理，和我们的local statement对变量名的处理并无二致，实际上也是如此，我们的TK_FUNCTION只是确保我们的编译流程，能够走到function statement的编译分支之中。到这里，上面例子中的name-part就完成编译了，紧随其后的则是对func-part进行编译。func-part的编译主要分为两个部分，第一个部分是对参数列表的处理，第二个部分则是对函数体的处理。在进入func-part的编译之前，编译器首先会构建一个新的FuncState实例，用它来存储我们对test函数的编译结果，在开始编译函数体之前，我们得到图45的表示：<br><img src="function-statement-2.png" alt="image">图45<br>上图中，蓝色部分，就是新增的FuncState结构，同时也新增了一个Proto结构，这是进入函数体编译前的准备工作，接下来，我们的编译器首先会对’(“)‘内的参数进行编译，其实它的做法很简单，就是将参数变量，作为local变量写入到新FuncState结构的Proto实例的locvars列表中，作为这个函数的局部变量存在，同时调整freereg变量的值，于是我们得到了图46的结果：<br><img src="function-statement-3.png" alt="image">图46<br>在完成了参数处理之后，编译器会开始处理函数体部分，上面例子很简单，就是先将print函数入栈，然后将a、b和c三个参数入栈，最后再打印，于是得到图47的结果：<br><img src="function-statement-4.png" alt="image">图47<br>函数体的编译结果，基本上存储在了新的Proto结构（蓝色）之中，注意其左边的虚拟栈，在编译器是不存在的，这里画出来的目的，主要是想告诉读者，每个函数在运行阶段，都有自己逻辑上独立的虚拟栈，这个虚拟栈是在调用函数之前确定的。在函数完成编译之后，新的FuncState结构是需要被回收的，但是Proto结构将被保留，并且存储在上一级FuncState结构所对应的Proto实例的p列表之中，同时ls-&gt;dyd-&gt;actvar-&gt;arr中的a、b和c这三个参数变量，也会被清除掉，于是我们得到了图48的结果：<br><img src="function-statement-5.png" alt="image">图48<br>这里需要注意的是，编译top-level function对应的FuncState结构的Proto实例，将新编译的Proto放到了自己的p列表中的第一个位置，表示其内部的第一个被定义的函数，同时生成了两个指令，一个是OP_CLOSURE，它是iABx模式，它会创建一个LClosure类型的实例，并且放置到R(A)的位置，在我们的例子中，就是R(0)的位置，也就是local变量test所在的寄存器上，Bx指定了，它的函数体是哪个，这里是0，表示刚刚编译好的蓝色体Proto实例，是这个LClosure实例的编译信息载体。如果你忘记了虚拟机如何运行函数的流程，那么可以回到Part5进行复习，这里只论述编译器的部分，不再进行赘述。<br>    我们的name-part还可能是table里的某个域，也可能是一个全局变量，但不管怎样，最终，编译器都会为编译好对的Proto结构，创建一个LClosure实例，并且将其赋值到指定的变量中，我把第二个例子留给读者自己去推导，就当是一个随堂作业：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span><span class="params">(a, b, c)</span></span></span><br><span class="line">	<span class="built_in">print</span>(a, b, c)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>​    除了上面例子所展示的方式外，lua还给我们提供了一种语法糖，如下所示：</p>
<figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">t</span>:<span class="title">foo</span>(<span class="title">a</span>, <span class="title">b</span>, <span class="title">c</span>)</span></span><br><span class="line">	self.v = <span class="keyword">a</span> + b + c</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>函数名前，通过’:‘来修饰，并且函数体内出现了self的关键字。通过’:‘来修饰的函数，其实就是一种语法糖，上面的代码等价于：</p>
<figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">t</span>.<span class="title">foo</span>(<span class="title">t</span>, <span class="title">a</span>, <span class="title">b</span>, <span class="title">c</span>)</span></span><br><span class="line">	t.v = <span class="keyword">a</span> + b + c</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>这种方式，让我们省去了将参数t传入，而且t是作为参数表的第一个参数，self就是指代这个函数本身，我们现在来看一下，这个函数的编译流程。首先编译器会对name-part进行编译，得到图49的结果：<br><img src="function-statement-6.png" alt="image">图49<br>这个阶段，我们往常量表k中，写入了”t”和”foo”，并且生成了一个OP_GETTABUP指令，这个指令将全局变量t，读取到寄存器R(0)中。接下来则进入到函数体的编译流程之中了，于是我们得到了图50的结果：<br><img src="function-statement-7.png" alt="image">图50<br>我们可以看到，新的Proto实例（蓝色）的locvars列表，第一个local变量为self，也就是说，函数体内所有的self调用，都会找到R(0)这个位置的变量，其后才是变量，a、b和c。在完成参数表的编译之后，接下来开始编译函数体，得到图51的结果：<br><img src="function-statement-8.png" alt="image">图51<br>上图一次性将函数体内的编译流程执行完，将编译后的结果，存储到了蓝色的Proto实例中，它首先将a和b相加，然后再和c相加，将最后的结果，放置到R(0)这个table的”v”域中。完成编译以后，蓝色的FuncState结构将被清除，并且将蓝色的Proto实例，设置给白色Proto实例的p列表中，得到图52的结果：<br><img src="function-statement-9.png" alt="image">图52<br>我们可以看到，白色的Proto实例，新增了几个指令，分别是OP_CLOSURE、OP_SETTABLE和OP_RETURN，OP_CLOSURE和前面讨论的一样，这里往R(1)的位置，创建一个LClosure实例，并且其执行体为fs-&gt;p-&gt;p[0]，也就是我们的蓝色Proto实例，然后将这个LClosure实例，设置到位于R(0)的table t的”foo”域上，到这里为止，编译流程就完成了。<br>    虽然，我们已经完成了，关于self函数的编译流程，但是这个self变量，是怎样设置到函数栈的第一个local位置呢？这里，我们需要结合调用函数的逻辑来看，首先我们来看一个例子：</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">t:<span class="comment">(a, b, c)</span></span><br></pre></td></tr></table></figure>
<p>我们通过protodump工具，将上面的函数编译好以后，得到如下的结果：</p>
<figure class="highlight gherkin"><table><tr><td class="code"><pre><span class="line">file_name = ../test.lua.out</span><br><span class="line">+type_name [LClosure]</span><br><span class="line">+proto+locvars</span><br><span class="line">|<span class="string">     +lineinfo+1 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +2 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +3 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +4 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +5 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +6 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +7 [1]</span></span><br><span class="line">|<span class="string">     +lastlinedefine [0]</span></span><br><span class="line">|<span class="string">     +p</span></span><br><span class="line">|<span class="string">     +upvalues+1+idx [0]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">          +name [_ENV]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">          +instack [1]</span></span><br><span class="line">|<span class="string">     +is_vararg [1]</span></span><br><span class="line">|<span class="string">     +type_name [Proto]</span></span><br><span class="line">|<span class="string">     +code+1 [iABC:OP_GETTABUP      A:0   B  :0   C:256 ; R(A) := UpValue[B][RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +2 [iABC:OP_SELF          A:0   B  :0   C:257 ; R(A+1) := R(B); R(A) := R(B)[RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +3 [iABC:OP_GETTABUP      A:2   B  :0   C:258 ; R(A) := UpValue[B][RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +4 [iABC:OP_GETTABUP      A:3   B  :0   C:259 ; R(A) := UpValue[B][RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +5 [iABC:OP_GETTABUP      A:4   B  :0   C:260 ; R(A) := UpValue[B][RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +6 [iABC:OP_CALL          A:0   B  :5   C:1   ; R(A), ... ,R(A+C-2) := R(A)(R(A+1), ... ,R(A+B-1))]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +7 [iABC:OP_RETURN        A:0   B  :1         ; return R(A), ... ,R(A+B-2)  (see note)]</span></span><br><span class="line">|<span class="string">     +maxstacksize [5]</span></span><br><span class="line">|<span class="string">     +k+1 [t]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +2 [foo]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +3 [a]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +4 [b]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +5 [c]</span></span><br><span class="line">|<span class="string">     +source [@../test.lua]</span></span><br><span class="line">|<span class="string">     +linedefine [0]</span></span><br><span class="line">|<span class="string">     +numparams [0]</span></span><br><span class="line"><span class="string">+upvals+1+name [_ENV]</span></span><br></pre></td></tr></table></figure>
<p>从上面的结果，我们得到如下的流程：</p>
<ul>
<li>执行code[1]的指令OP_GETTABUP 0 0 256，将table t加载到R(0)的位置，得到图53：<img src="function-statement-10.png" alt="image">图53</li>
<li>执行code[2]的指令OP_SELF 0 0 257，将位于R(0)的table t加载到R(1)的位置上，并将函数t[“foo”]加载到R(0)的位置，得到图54：<img src="function-statement-11.png" alt="image">图54</li>
<li>执行code[3]~code[5]，将a、b和c三个参数入栈，得到图55：<img src="function-statement-12.png" alt="image">图55</li>
<li>调用t[“foo”]这个函数，并结束程序</li>
</ul>
<p>通过上面这个例子，和前面的编译结果对比，就能够理解self函数的编译和执行流程，一切也就一目了然了，到这里为止，我们就完成了function statement编译流程的论述了。</p>
<h1 id="return-statement编译流程"><a href="#return-statement编译流程" class="headerlink" title="return statement编译流程"></a>return statement编译流程</h1><p>​    return statement的编译流程很简单，就是将要返回的参数压栈，并且生成OP_RETURN指令，OP_RETURN指令是iABC模式，A指示了第一个要返回的参数在栈中的位置，B指示了又多少个返回值，当B&gt;0时，表示返回B-1个参数，当B==0时，表示从第一个返回值，到栈顶都是返回值。关于OP_RETURN的执行流程，Part5里有详细的讨论，这里不再赘述。读者可以使用protodump工具，来查看return语句的编译结果。</p>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>​    截止到本篇，我就完成了lua内置编译器的全部论述了，后续内容将与编译技术无关。本篇我首先对block的含义，处理流程进行详细的说明，接着对各种不同的statement的编译进行了详细的讨论。全文通过图文的方式来论述流程，没有贴过多的代码和数学公式来进行编译技术的讲解。我相信这种方式能够更好地表述编译流程。尽管本篇没有前面几篇的篇幅长，但是也同样花费了我大量的时间和精力，希望能够给读者带来良好的阅读体验。截止到目前，最难的编译器和虚拟机构建部分已然完成，后续将一马平川，我相信整个lua系列即将完结。</p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>lua的演进</title>
    <url>/lua/lua%E7%9A%84%E6%BC%94%E8%BF%9B/</url>
    <content><![CDATA[<p>lua的优点：</p>
<p>可移植性<br>容易嵌入<br>体积小<br>高效率<br>这些优点都来自于lua的设计目标：简洁。从Scheme获得了很多灵感，包括匿名函数，合理的语义域概念</p>
<h1 id="3-lua前身"><a href="#3-lua前身" class="headerlink" title="3 lua前身"></a>3 lua前身</h1><hr>
<p>巴西被商贸限制，引入计算机软件和硬件受限，巴西人有强烈的民族情绪去创造自己的软件。三名作者都是同一个实验室Tecgraf的，这个实验室与很多工业实体有合作关系。成立的头十年，重点是创造交互性的图形软件，帮助合作伙伴进行设计。巴西石油公司是其中一个重要伙伴。有大量的遗留数据需要处理。于是诞生了DEL，一个领域专用语言，主要用来描述数据流图的数据的。后来人们对DEL需求越来越多，不止是一门简单的数据描述语言可以解决的了。</p>
<p>lua为解决实际问题而生，受到三位作者学过的语言影响。自己创造的是..（两个句号）连接字符串。自豪的是，在13年的演进里，lua的类型系统只修改了两次。lua诞生的时候，基本类型包括nil，number，string，table，C function，Lua function，userdata。97年的时候，Lua3.0将C function和Lua function合并了。03年的时候，提出了boolean值类型，增加了thread协程类型。</p>
<p>1993年，第一版Lua由Waldemar在Roberto指导下完成。词法分析用了Unix上经典的yacc和lex。解释器将lua代码翻译成针对一个基于栈的虚拟机的指令。C API很容易扩展，因此最早只有5个函数（print,tonumber,type,next,nextvar）和3个库（input/output, string, math)。</p>
<h1 id="5-Lua的历史"><a href="#5-Lua的历史" class="headerlink" title="5 Lua的历史"></a>5 Lua的历史</h1><hr>
<p>lua的发布模式和其他社区不一样。alpha版本已经相当稳定，beta版本几乎可以作为final版，除非是用来修复bug。这个发布模式对于lua的稳定性有很大帮助，但不利于尝试新特性。因此，从5.0版本开始，添加了新的work版本。work版本是lua当前开发版的snapshot，有助于lua社区迈向开源社区的哲学：早发布、多发布。</p>
<p>lua的标准库被刻意保持在一个很小的范围，因为大部分需要的功能都会由宿主或第三方库提供。4.0对C API重新设计了，C API有很大改动，之后就向着完美一点点前进了。结果是不再有任何内置函数，所有标准库都是基于C API实现，没有通过特别的后门去访问Lua内部结构。</p>
<p>Lua的vm在4.0版本以前一直是基于栈的。在3.1版本，我们对多个指令添加了变量来提高性能。后来发现这个实现复杂度很高，性能提升不明显，于是在Lua 3.2版本去掉了。从Lua 5.0开始，vm改为基于寄存器。代码生成器因此有更多机会去优化和减少常见Lua程序的指令数了。</p>
<h2 id="5-1-Lua-1"><a href="#5-1-Lua-1" class="headerlink" title="5.1 Lua 1"></a><strong>5.1 Lua 1</strong></h2><p>Lua的成功使得Lua被大规模用在数据描述上，而预编译Lua代码为VM字节码的特性，迫使Lua编译器必须非常快，即使是对于大型项目。通过将lex产生的scanner替换为一个手写的版本，我们几乎让Lua编译器的速度翻了一倍。同时，我们修改了Lua VM的table构造函数，构造一个大型的表不再需要一条条指令传参数进去了，可以一次调用完成。从那以后，优化重点就变成了预编译时间。</p>
<p>1994年，我们发布了带有这些优化的lua版本：Lua1.1。这次发布和第一篇描述Lua的设计和实现的文章是重合的。之前从未公开发布过的版本，就被称为Lua 1.0（Lua1.0的一个1993年7月的snapshot在2003年10月发布，以此庆祝Lua十周年）</p>
<p>Lua 1.1最早以源码方式在ftp提供下载。这远在开源运动兴盛蓬勃之前。Lua1.1有限制性的用户协议，对于学术用途是免费的，但是商业用途则需要协商。那部分协议没有凑效：尽管我们有一些初始联系人，从没有商业使用是经过协商的。其它脚本语言（比如Tcl）的免费促使我们意识到，限制商用甚至会反过来影响学术用途的发展，因为一些学术项目是打算最终走向市场的。因此，当发布Lua2.1的时候，我们将Lua作为无限制免费软件发布了。我们天真的以一种学院气的口吻对之前的协议重新措辞，并觉得新协议写的很直观了。稍后，随着开源许可证的散播，我们的协议文本变成了干扰着一些用户的噪音：我们的协议没有清晰的说明是否和GPL协议相容。在2002年5月，在邮件列表里进行了冗长的讨论后，我们决定将来发布的Lua版本（从Lua 5.0开始）都使用非常清晰直观的MIT协议。2002年7月，自由软件基金会（FSF）确认了我们之前的协议是GPL兼容的。但我们已经决定采纳MIT协议了。对我们协议的疑问从此都消失了。</p>
<h2 id="5-2-Lua-2"><a href="#5-2-Lua-2" class="headerlink" title="5.2 Lua 2"></a><strong>5.2 Lua 2</strong></h2><p>1990年的时候，面向对象迈向巅峰，对于Lua没有面向对象的支持，我们受到了很大的压力。我们不想将Lua变成面向对象，因为我们不想“修复”一种编程范式（fix a programming paradigm）。特别是，我们不觉得Lua需要将对象和类作为基础语言概念，我们觉得可以透过table来实现（table可以保存方法和数据，因为函数是第一类对象）。直到今天，Lua也没有强加任何对象和类模型给用户，我们初心不变。很多用户建议和实现了面向对象模型；面向对象也是邮件列表里经常讨论的问题，我们觉得这是健康的。</p>
<p>另一方面，我们希望允许Lua可以面向对象编程。我们决定提供一套灵活的机制，让用户可以选择对应用来说合适的模型，而不是修复模型。1995年2月发布的Lua2.1，标志着这些灵活语义的问世，极大的增加了Lua的表达能力，从此，灵活语义就变成了Lua的标志。</p>
<p>灵活语义的一个目标是允许table作为对象和类的基础。为了实现这个目标，我们需要实现table的继承。另一个目标是将userdata变成应用数据的天然代理，可以作为函数参数而不只是一个句柄。我们希望能够索引userdata，就好像他们只是一个table，可供调用他们身上的方法。这让Lua可以更为自然的实现自己的主要设计目标：通过提供脚本访问到应用服务和数据，从而扩展应用。我们决定实现一套fallback机制，让Lua把未定义行为交给程序员处理，而不是直接在语言本身实现这些特性。</p>
<p>在Lua2.1的时候，我们提供了fallback机制，支持以下行为：table索引，算术操作符，字符串拼接，顺序比较，函数调用。当这些操作应用到“错误”的类型上，对应的fallback就会被调用到，允许程序员决定Lua如何处理。table索引fallback允许userdata和其它值类型表现的跟表一样。我们也定义了当Key不在table时的fallback，从而实现多种形式的继承（通过委托）。为了完善面向对象编程，我们添加了两个语法糖：function a:foo(…)就好比function a.foo(self,…)一样，以及a:foo(…)作为a.foo(a, …)的语法糖。在6.8节我们会讨论fallback的细节。</p>
<p>从Lua1.0开始，我们就提供了值类型的内省函数（introspective functions）：type，可以用来获取Lua值的类型；next，可以用来遍历一个table；以及nextvar，可以遍历全局环境。（正如第四章所述，这是为了实现类似SOL的类型检查）为了应付用户对完整调试设施的强烈需求，1995年12月发布的Lua2.2引入了一个debug API来获取运行中的函数信息。这个API为用户提供了以C语言编写自己的内省函数工具链的手段，比如编写自己的调试器和性能分析工具。debug API刚开始的时候相当简洁：debug库允许访问Lua的调用栈，访问当前执行的代码行行数，以及一个可以查找指定值的变量名的函数。根据M.Sc.的Tomas Gorham的工作，debug API在1996年5月发布的Lua2.4版本里得到了完善，提供了函数访问局部变量，提供了钩子在行数变化和函数调用时触发。</p>
<p>因为Lua在Tecgraf的广泛使用，很多大型的图形源文件都是用Lua写的，作为图形编辑器的输出格式。加载这些源文件会随着文件大小变得越来越大和越来越复杂而变的越来越长。从第一版开始，Lua就预编译所有程序到字节码，再执行字节码。大型代码文件的加载时间可以通过保存bytecode来缩减。这和处理图形源文件特别有关系。所以在Lua 2.4版本，我们引入了一个外部编译器Luac，可以编译一个Lua文件为字节码并保存为二进制文件。这个二进制文件的格式经过精心选择，可以轻松加载同时体积小巧。通过luac，程序员可以在运行期避免词法分析和代码生成，这些操作早期还是比较耗时的。除了更快的加载，luac还允许离线的语法检查，以及随意的用户改动。很多产品（比如模拟人生和Adobe的Lightroom）都是透过预编译格式发布Lua脚本的。</p>
<p>在luac的实现过程里，我们开始将Lua的核心重构成清晰的分离模块。于是，我们现在可以轻易移除词法解析的模块（词法解析器，语法解析器和代码生成器），这些部分占了大约35%的核心代码，剩下的部分可以加载预编译的Lua脚本。这种代码剪裁，对于在移动设备、机器人和感应器这些小设备里嵌入Lua，是有显著意义的。</p>
<p>从第一版开始，Lua就自带有一个库来进行字符串处理。这个库在Lua2.4之前功能有限。但是，随着Lua的成熟，Lua需要进行更重量级的字符串处理。我们认为，沿革Snobol，Icon，Awk和Perl的传统，为Lua添加模式匹配是自然而然的。但是，我们不想将第三方的模式匹配引擎打包到Lua里面去，因为这些引擎通常都很大，我们也希望避开因为引入第三方库带来的代码版权问题。</p>
<p>1995年的第二学期，作为Roberto指导下的学生项目，Milton Jonathan，Pedro Miller Rabinovitch，Pedro Willemsens和Vinicius Almendra为Lua写出了一个模式匹配库。那个设计的经验引导我们写出了我们自己的模式匹配引擎。1996年的12月，我们在Lua2.5中添加了两个函数：strfind（最早职能查找纯文本）和新的gsub函数（名字来源于Awk）。gsub函数可以用来全局替换符合指定模式的子串。它接受一个新的字符串或者一个函数作为参数，函数参数会在每次遇到匹配时调用，并预期该函数返回新子串以供替换。为了缩小实现的规模，我们没有支持完整的正则表达式。我们支持的模式包括字符类，重复，以及捕获（但是没有可选或组匹配）除了简洁性，这种模式匹配还十分强大，是Lua的一个强有力的补充。</p>
<p>那一年是Lua历史上的转折点，因为Lua获得了全球的曝光。在1996年6月，我们在《Software：Practice &amp; Experience》杂志上发布了一篇Lua的文章，为Lua带来了外部的关注，至少是学术圈子的关注。在1996年的12月，Lua 2.5刚刚发布后，杂志Dr.Dobb’s Journal也发表了Lua的文章。Dr.Dobb’s Journal是一本面向程序员的流行刊物，那篇文章为Lua带去了软件工业界的关注。在那次发布之后，我们收到了很多消息，其中一条是1997年1月收到的，来自Bret Mogilefsky，LucasArts出品的冒险游戏——Grim Fandango的首席程序员。Bret告诉我们，他从Dr.Dobb’s上读到了Lua，他们打算用Lua代替自己写的脚本语言。1998年10月，Grim Fandango发布，1999年5月Bret告诉我们“大量的游戏都是用Lua写的”。那个时候，Bret参加了GDC（Game Developers’ Conference, 游戏开发者会议）的有关游戏脚本的圆桌会议。他谈到了Grim Fandango应用Lua的成功经验。很多我们认识的开发者，都是在那次事件里认识到Lua的。在那以后，Lua在游戏程序员之间口耳相传，变成了游戏工业里可资销售的技能。</p>
<p>因为Lua的国际化曝光，向我们提问Lua的信息越来越多。1997年2月，我们建立了邮件列表，好更有效率的处理这些问题，让其他人也能帮忙回答问题，并开始建设Lua社区。这个列表至今发布了超过38000条消息。很多热门游戏对Lua的使用，吸引了很多人到邮件列表里。现在已经有超过1200个订阅者了。Lua列表十分友善，同时又富有技术性，我们对此深感幸运。邮件列表逐渐变成了Lua社区的焦点所在，也是Lua演进的动力源泉。所有的重大事件，都是首先在邮件列表里出现的：发布重大通知，请求新特性，bug报告等等。</p>
<p>在Usenet新闻组里建立comp.lang.lua讨论组曾经在邮件列表里讨论过两次，分别是1998年的4月和1999年的7月。两次的结论都是邮件列表的流量不能保证创建一个新闻组。而且，更多人倾向于邮件列表。随着多个阅读和搜索完整邮件存档的web界面问世，创建新闻组变得无关紧要了。</p>
<h2 id="5-3-Lua-5-3"><a href="#5-3-Lua-5-3" class="headerlink" title="5.3 Lua 5.3"></a><strong>5.3 Lua 5.3</strong></h2><p>Lua2.1里引入的fallback机制，可以很好的支持灵活扩展的语义，但这个机制是全局的：每个事件只有一个钩子。这让共享或重用代码变的很艰难，因为同一事件的fallback在模块里只能定义一次，不能共存。1996年12月，听取了Stephan Herrmann的建议后，我们在1997年7月发布的Lua 3.0中，我们解决了fallback冲突问题。我们将fallback替换为tag方法：钩子是以(event, tag)的形式挂在字典里的。Tags是在Lua2.1引入的整数标签，可以附在userdata上。最初的动机是希望同类的C对象，在Lua里都有相同的tag(不过，Lua没有强迫要对Tag提供解释)。Lua3.0里，我们对所有值类型提供了tag支持，以支持对应的tag方法。6.8节里还会继续讨论fallback的演进。</p>
<p>1998年7月Lua 3.1引入了函数式编程，Lua拥有了匿名函数和upvalue支持的函数闭包。引入闭包是被高阶函数的存在所启发的，例如gsub，可以使用函数作为参数。在Lua 3.1点工作中，邮件列表里讨论了多线程和协作式多任务，这些讨论主要源于Bret Mogilefsky在Grim Fandango中，对Lua2.5和Lua 3.1 alpha版所作的改动。虽然没有最终定论，但这个话题一直很热门。协作式多任务在2003年4月发布的Lua5.0里提供了，详见6.7节。</p>
<p>从Lua 1.0到Lua 3.2，C API总体上没有变化，都是针对一个隐式Lua虚拟机进行操作的。但是，新的应用程序比如web，需要多状态支持。为了解决这一问题，Lua 3.1引入了多个独立Lua状态的设计，可以在运行时自由切换。而完全可重入的API则需要等到Lua4.0。同时，两个非官方的Lua3.2修改版出现了，它们都带有显式的Lua状态，一个是1998年，Roberto Ierusalimschy和Anna Hester为CGILua写的、基于Lua 3.2 alpha的版本。另一个是1999年，Erik Hougaard基于Lua 3.2 final写的版本。Erik的版本是公开发布的，并应用在Crazy Ivan robot里面。CGILua是作为CGILua发行版的一部分发布，从未以独立包的形式出现。</p>
<p>1999年7月，Lua 3.2主要是一个维护性的版本。没有新奇的特性，除了一个可以用Lua编写调试器相关代码的debug库。但是，从那时起，Lua就处在一个相对稳定的版本，因此Lua 3.2有很长的生命期。因为下一个版本Lua 4.0是用的全新的不兼容API，很多用户就只停留在Lua 3.2，没有再迁移到4.0版本了。比如Tecgraf就从来没有迁移到4.0版本，打算直接上Lua 5.0.很多Tecgraf的产品还是用的Lua 3.2</p>
<h2 id="5-4-Lua-4"><a href="#5-4-Lua-4" class="headerlink" title="5.4 Lua 4"></a><strong>5.4 Lua 4</strong></h2><p>2000年12月，Lua 4.0正式发布。正如上文所述，4.0的主要改变是完全可重入的API，为那些需要多份Lua state的应用而设计。因为改造API为完全可重入已经是主要改动，我们借此机会重新设计了API，依赖清晰的堆栈实现与C层的值交换。这是Reuben Thomas在2000年7月提出的。</p>
<p>Lua 4同时引入了for语句，这是邮件列表中的日常话题和大部分Lua用户最想要的特性。我们早期没有引入for语句，是因为while循环更为一般化。但是，用户总是投诉忘记在while循环的尾部更新控制变量，从而引起死循环。而且，我们在好语法这一点上没有达成一致。我们觉得Modula语言的for语句限制性太大了，因为它既不能迭代table里的元素，也不能迭代文件里的行。C传统上的for循环也不适合Lua。基于Lua 3.1引入的闭包和匿名函数，我们决定使用高阶函数来实现迭代。所以，Lua 3.1提供了一个高阶函数来迭代table，可以对table的每一对键值对回调用户自定义函数。比如，要打印table t里面的每一个键值对，只需要写foreach(t,print)。</p>
<p>在Lua 4.0我们终于设计了一个for循环，它有两种方式：一个数字式的循环以及一个表遍历的循环（1997年10月由Michael Spalinski提出）。这两种方式覆盖了大部分常用应用环境；对于更一般化的循环，依然有while循环可供使用。要打印table t里的所有键值对，可以这样写：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> k, v <span class="keyword">in</span> t <span class="keyword">do</span></span><br><span class="line">  <span class="built_in">print</span>(k, v)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p>
<p>添加for循环只是一个很简单的一个改动，但它的确改变了Lua程序的外观。Roberto不得不重写了Programming in Lua草稿里的许多例子。Roberto在1998年开始写书了，但是他从来都没写完，因为Lua是一个不断变动的目标。随着Lua 4.0的发布，书里大部分内容需要改写，几乎所有的代码块都要重写了。</p>
<p>Lua 4.0发布后，我们开始为Lua 4.1工作。在Lua 4.1版本，我们面临的主要挑战，大概是要不要支持、如何支持多线程吧，这在当时是一个大问题。随着Java的用户增长以及Pthreads的出现，很多程序员开始考虑多线程，将其作为编程语言的关键特性进行考量。但是，对于我们来说，Lua支持多线程需要考虑几个严肃的问题。首先，在C层面支持多线程就会用到非ANSI C的原语——尽管Pthread很流行，但仍然有很多平台（当时和现在）都缺乏这个库的支持。第二，更重要的是，我们不相信标准多线程模型：那个共享内存的抢占式并发模型。我们仍然认为，对于a=a+1都没有确定结果的语言，没人能写出正确的程序。</p>
<p>对于Lua 4.1，我们开始用Lua的经典方式解决这些难题：我们只实现了一个简单的多栈共存的机制，我们称为threads（线程）。外部的库可以使用这些Lua线程来实现多线程，比如基于Pthreads来实现。同样的机制也能用来实现协程，一个协作式的、非抢占的多线程模型。2001年7月，Lua 4.1 alpha发布，带有额外的多线程和协程库支持；同时也引入了弱表，以及标志性的基于寄存器的虚拟机。我们一直很想用基于寄存器的虚拟机进行实验。</p>
<p>Lua 4.1 alpha发布后的第二天，John D.Ramsdell在邮件列表里开始了关于语法域的大讨论。经过数十封邮件的讨论，我们清晰的发现，Lua需要完全词法作用域的支持，而不是Lua 3.1开始的upvalue机制。在2001年10月，我们想到了实现高效的完全词法作用域的支持，并在同年的12月发布了一个可以工作的版本。那个版本还引入了新的table混合模型，可以在合适的时候将table作为数组实现。因为那个版本实现了新的基础算法，我们决定作为可工作版本来发布，尽管我们已经为Lua 4.1发布了一个alpha版本。</p>
<p>2002年2月，我们为Lua 4.1发布了一个新的工作版本，带有三个相对新颖的特性：一个基于迭代函数的通用的for loop，取代tags和fallbacks的元表和元方法，以及协程。在那次发布后，我们意识到Lua 4.1带来了太多的巨变——可能Lua 5.0 比较适合作为下个版本的版本号。</p>
<h2 id="5-5-Lua-5"><a href="#5-5-Lua-5" class="headerlink" title="5.5 Lua 5"></a><strong>5.5 Lua 5</strong></h2><p>对于Lua 4.1这个版本号来说，最后的改动来自于Christian Lindig和Norman Ramsey在哈佛大学召开的Lua库设计会议。这次会议的一个主要结论是，Lua需要某种模块系统。我们一直认为模块可以透过table来完成，但是连标准库都没有按这种方式实现。于是我们决定下个版本迈出这一步。</p>
<p>将库函数放入table里面是一个巨大的冲击，因为这会影响到所有使用了至少一个库函数的程序。比如，老版本的strfind函数现在叫string.find(在string库里的find域，存储在叫string的table里)；openfile变成了io.open；sin变成了math.sin，诸如此类。为了让转换变的简单点，我们提供了一个兼容脚本，在里面定义了老函数去应用新函数：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">strfind = <span class="built_in">string</span>.<span class="built_in">find</span></span><br><span class="line">openfile = <span class="built_in">io</span>.<span class="built_in">open</span></span><br><span class="line"><span class="built_in">sin</span> = <span class="built_in">math</span>.<span class="built_in">sin</span></span><br></pre></td></tr></table></figure></p>
<p>但是，将库打包到table里面去始终是一个大改动。2002年6月，当我们放出了带有这一改动的可工作版本时，我们放弃了”Lua 4.1”这个名字，并将其命名为”Lua 5.0 work0”。最终版本的进展从那时开始就相当稳定了，2003年4月，我们发布了Lua 5.0。这次发布让Lua的特性得以稳定下来，让Roberto可以完成他的新书了，新书在2003年12月发布。</p>
<p>Lua 5.0发布后不久，我们立即开始Lua 5.1的工作。最初的动机是实现增量式的垃圾回收系统，以满足游戏程序员的需要。Lua使用的是传统的标记-清除垃圾回收算法，直到Lua 5.0为止，垃圾回收都是原子执行的。副作用是对于某些应用，可能会在垃圾回收时有很长的暂停。在那个时间点，我们的主要考虑是，添加写屏障需要实现增量式垃圾回收，对于Lua的性能会有负面影响。为了补偿这一劣势，我们也常侍了分代回收。我们也希望保留老一套的自适应行为，可以根据总内存占用调节垃圾收集的频率。而且我们希望回收器保持简洁的特点，就像Lua的其他部分一样。</p>
<p>我们在增量式分代GC上花了超过一年的时间。但因我们没有接触过对内存有强烈需求的应用（比如游戏），所以很难在真实场景中测试收集器的表现。从2004年的3月到12月，我们放出了数个可工作版本，试图得到收集器在应用中表现的具体反馈。我们最终收到了奇怪的内存分配行为的报告，并成功重现了但没有解释。2005年1月，一名Lua社区的活跃成员Mike Pall，通过内存分配图解释了问题的根源：在某些特定场景，增量行为、分代行为和自适应行为之间会有微妙的交互，导致收集器“自适应”到越来越低频率的收集上去。因为太复杂和不可预测，我们放弃了分代方式，并在Lua 5.1实现了更简单的增量回收。</p>
<p>在这段时间里，程序员尝试了Lua 5.0的模块系统。新包开始涌现，老包也开始转移到新系统去。包作者们希望指导构建模块的最佳实践。2005年7月，在Lua 5.1的开发期间，一场由Mark Hamburg组织的国际性Lua会议，在San Jose的Adobe召开。其中一场演讲是关于Lua 5.1的新特性，引发了关于模块和包的冗长讨论。结果是我们对模块系统作出了一些细微但收效甚大的改变。尽管我们对Lua有“机制而非策略”的指南，我们还是定义了一系列的策略来编写模块和加载包，并做了些小改动让这些策略跑的更好。2006年2月，Lua 5.1发布。尽管Lua 5.1的初衷是增量垃圾收集，模块系统的改进可能是最容易留意到的。另一方面，增量垃圾收集没有被注意到，说明了它成功的避免了长时间的停顿。</p>
<h1 id="6-特性演进"><a href="#6-特性演进" class="headerlink" title="6 特性演进"></a>6 特性演进</h1><hr>
<p>在这一节，我们会详细讨论Lua的一些特性的演进。</p>
<h2 id="6-1-类型"><a href="#6-1-类型" class="headerlink" title="6.1 类型"></a>6.1 类型</h2><p>Lua的类型系统是相对稳定的。很长时间里，Lua只有6个基本类型：nil，number，string，table，function和userdata（实际上，直到Lua 3.0为止，之前的C函数和Lua函数有不同的内部类型，但是这个差异对于调用者是透明的）。唯一真正的改变来自于Lua 5.0，这个版本引入了两个新类型：thread和boolean。</p>
<p>引入的类型thread用于表示协程。像其他Lua值类型一样，thread是第一类的值。为了避免创造新的语法，所有针对thread的基础操作都是通过library来提供的。</p>
<p>很长时间里，我们都很抗拒引入布尔值类型：nil就是false，其他所有一切都是true。这种情况对于我们的目的是简洁够用的。但是，nil也作为table里元素缺失的值，以及未定义变量的默认值。在某些应用里，需要一个table的域被置为false但仍然可见表示存在，一个显式的false可以表示这种情况。Lua 5.0里，我们最终引入了布尔值类型true和false。nil还是作为false。回顾这个设计，可能在布尔表达式里引用nil报错会是更好的解决办法，就跟其他表达式里一样。这样，nil扮演未定义值类型的代理，就会更加具有一致性。但是，这个改动很可能会导致很多现存的程序报错。LISP有类似的问题，空列表代表了nil和false。Scheme显式使用false，并将空列表作为true。但一些Scheme实现仍然将空列表作为false。</p>
<h2 id="6-2-Table"><a href="#6-2-Table" class="headerlink" title="6.2 Table"></a>6.2 Table</h2><p>Lua 1.1有三种语法来创建一个table：@()，@[]和@{}。最简单的形式是@()，用于创建一个空表。可以在创建时提供一个可选的长度参数，作为优化性能的提示信息。@[]这种形式是用来构建数组的，比如@[2,4,9,16,25]。在这种table里，key是隐式的从1开始的自然数。@{}这种形式是用来构造记录的，比如@{name=”John”,age=35}。这种table是键值对的集合，键是显式的字符串。一个table可以用这三种的其中一种方式构造，构造后可以动态修改，不因构造方式受限。此外，还能在构造列表和记录的时候提供用户函数调用，比如@foo[]或者@foo{}。这个语法是从SOL里继承的，是过程数据表述的表达式，Lua的一个主要特性(参见第二节)。语法是构建好table后，调用对应的函数，将构造好的table作为单参数传入函数。函数允许随意检查和修改table，但是函数的返回值会被忽略：table就是表达式的最终值。</p>
<p>Lua 2.1里，table创建的语法是统一的，并得到简化：开头的@被移除了，唯一的构造方式是{…}。Lua 2.1也允许混合的构造方式，比如</p>
<p>grades{8.5, 6.0, 9.2; name=”John”, major=”math”}<br>数组的部分和记录的部分被分号分割开。最后，foo{…}变成了foo({…})的语法糖。也就是说，函数加上表构造器，变成了普通的函数调用。所以，函数需要显式返回table（或其他值）。从构造器里拿掉@是一个平凡的改动，但它的确改变了语言的感觉，不仅仅是它的外观。改变语言观感的平凡改动不应该被忽视。</p>
<p>然而，这个语法的简化和table构造器的语义改变带来了副作用。Lua 1.1里，=用来判断相等。Lua 2.1里table构造器统一之后，表达式{a=3}变成有歧义了，因为它可以表示<code>(&quot;a&quot;, 3)个键值对，或者(1, b)，其中b是表达式a=3的值。为了解决二义性，Lua 2.1用==取代了</code>=<code>。改动后，</code>{a=3}<code>表示一个含有键值对</code>(“a”, 3)<code>的table，而</code>{a==3}<code>表示</code>(1, b)`。</p>
<p>这些改变让Lua 2.1变得不兼容Lua 1.1了（所以大版本号也变了）。但是，因为那时候实际上所有的Lua用户都是Tecgraf的，这并不是一个致命的改变：现存的代码可以经过我们编写的特定工具进行转换。table构造器的语法从那时起几乎不变了，唯一的例外是Lua 3.1引入的：记录的key可以用上任意表达式了，比如{[10*x+f(y)]=47}。尤其是这一改动允许了key为任意的字符串了，包括保留字和带有空格的字符串。因此，{function=1}是无效的，因为function是一个保留字，但是{[“function”]=1}是有效的。从Lua 5.0开始，也可以自由的混合数组部分和记录部分了，因此没有必要再在table构造器里使用分号了。</p>
<p>虽然table的语法演进了，但是table的语义得到完整保留：table依然是关联数组，可以存放任意键值对。但是，实践中经常使用的table要么是单纯的数组（连续的整型key）或者记录（字符串作为key）。因为table是Lua里唯一的数据结构机制，我们投入了大量的努力去实现table，让table在Lua的核心代码里高效运行。直到Lua 4.0为止，table都是作为纯哈希表实现的，所有的键值对都是显式存储的。在Lua 5.0版本我们引入了table的混合表示：每个table包含了一个哈希部分和一个数组部分，两个部分都可以是空的。Lua检测一个table是不是作为一个数组来使用，并自动将数字索引的值移动到数组部分，而非原本的存储在哈希部分。这种分裂只在底层实现层次进行；访问table域是透明的，即使是对虚拟机来说。table会自动根据内容使用两个部分。</p>
<p>这个混合机制有两个优点。第一，访问整型key的操作会变得更快了，因为不再需要哈希。第二，更重要的是，数组部分只占原来哈希部分的一半大小，因为哈希部分需要同时存储key和value，而数组部分的key已经隐含在下标了。结果是，如果一个table是作为数组使用的，它的表现就像数组一样，只要它的整型key是密集分布的。而且，哈希部分没有内存或者时间的代价，因为作为数组使用时，哈希部分不存在。反过来说，如果table是作为记录使用而非数组，那么数组部分就是空的。这些节省下来的内存是重要的，因为对于Lua程序来说，创建大量小table是很常见的（比如用table来表示object）。Lua的table也能优雅的处理稀疏数组：语句a={[1000000000]=1}在哈希部分创建了一个键值对，而非一个10亿元素的数组。</p>
<p>另一个打造高效table实现的原因，是我们可以使用table实现各种各样的任务。比如，在Lua 5.0版本里的标准库函数，从Lua 1.1开始就作为全局变量存在，现在移动到table中去了。最近，Lua 5.1带来了完整的包和模块系统，这些系统都是基于table实现的。</p>
<p>table扮演了Lua核心的一个突出角色。有两个场景我们直接用table代替了核心代码的特殊数据结构：Lua 4.0版本，用table来表示全局环境（用于保存全局变量），Lua 5.0实现了可扩展语义（参见6.8）。从Lua 4.0开始，全局变量就存储在普通的Lua table里，称为全局table，这是John Belmonte在2000年4月提出的简化建议。在Lua 5.0我们用元表和元方法取代了tag和tag方法（Lua 3.0引入的）。元表是普通的Lua table，元方法是作为元表的域存储的。Lua 5.0也引入了环境table，可以附加到Lua函数上；它们就是Lua函数索引的全局环境。Lua 5.1将环境变量table扩展到C函数、userdata和协程，取代了全局的环境变量。这些改动简化了Lua的实现、Lua和C程序员所用的API，因为全局变量和元方法可以在Lua里操控，不再需要特殊函数了。</p>
<h2 id="6-3-字符串"><a href="#6-3-字符串" class="headerlink" title="6.3 字符串"></a>6.3 字符串</h2><p>字符串在一门脚本语言里扮演了主要的角色。因此，创建和处理字符串的设施是脚本语言易用性的重要部分。</p>
<p>文本字符串的语法有一个有趣的演进过程。从Lua 1.1开始，一个文本串可以用单引号或者双引号来定义，也可以包含像C一样的转义序列。同时使用单引号和双引号来定义字符串，并且具有相同语义，在当时有点不寻常。（比如，在脚本语言的传统里，Perl是会展开双引号字符串里的变量的，而单引号字符串则保持不变）这两种引号表示方式允许字符串包含其中一种引号，而不需要使用转义符。但是任意文本的字符串还是需要转义符序列的。</p>
<p>Lua 2.2引入了长字符串，传统编程语言所没有，却普遍存在于脚本语言的一项特性。长字符串可以有许多行，不需要解析其中的转义符序列；它们提供了一个灵活的方式，将任意文本作为字符串，不再需要担心其中的内容（是否需要转义符）。但是，要为长字符串定义一个好的语法，没有想象中的简单，尤其是它们会被用于包含任意的程序文本（其中可能包含其他的长字符串）。这让我们思考两个问题：长字符串应该如何结束，它们是否可以嵌套。直到Lua 5.0为止，长字符串都是被包含在一对匹配的[[…]]中，并且可以包含嵌套的长字符串。可惜，结束分隔符]]可以是有效Lua程序的一部分，以一种不平衡的方式出现：a[b[i]]，或者在其他上下文环境里，比如XML的&lt;[!CDATA[…]]&gt;。</p>
<p>Lua 5.1引入了长字符串的新形式：用[===[…]===]包围文本，=的数量可以是任意的（包括0）。这种新的长字符串不嵌套：一个长字符串遇到匹配数量的=就会结束。不管怎么说，现在包裹任意文本变得更简单了，即使文本包含其他长字符串或者不匹配的]=…=]序列：只要使用适当数量的=字符。</p>
<h2 id="6-4-块注释"><a href="#6-4-块注释" class="headerlink" title="6.4 块注释"></a>6.4 块注释</h2><p>Lua的注释是—开始，到行尾结束。这是最简单的注释，非常有效。很多语言使用单行注释，不过它们用的符号不一样。使用—表示注释的语言包括Ada和Haskell。</p>
<p>我们从未觉得需要多行注释，或者块注释，除非是作为关闭一块代码的快捷方式。使用什么语法永远是一个问题：使用C语言的熟悉的/<em>…</em>/语法，或者其他语言的，都和Lua的单行注释格式不搭配。同样，块注释是否能嵌套也是一个问题，困扰着用户，并影响到词法分析器的复杂度。嵌套的块注释的场景，一般是程序员需要注释掉某些代码块。自然，它们希望代码块里的注释可以正确处理，这只有在块注释可以嵌套的情况下才能发生。</p>
<p>ANSI C支持块注释，但是不允许嵌套。C程序员通常使用C预处理器的惯用法来关闭代码块：#if 0 …#endif。这种方式有清晰的优势，它可以优雅的处理好被关闭代码里的注释。带着同样的动机和启发，我们强调了关闭Lua代码块的需要——不是块注释的——通过在Lua 3.0引入类似C语言的pragma进行条件编译。尽管块注释可以用上条件编译，我们不觉得这是它的正确用法。在Lua 4.0的改造工作中，我们认为条件编译带来的词法器复杂度太大，不值得继续支持，同时对于用户来说，它的语义对于用户有一定复杂性，对于完整宏支持的设施也没有达成共识（参见第7章）。所以，Lua 4.0里我们去掉了条件编译的支持，Lua仍然没有支持块注释。</p>
<p>块注释是在Lua 5.0才被最终引入的，形式是–[[…]]。因为他们故意模仿了长字符串（参见6.3）的语法，所以很容易修改词法器进行支持。这种相似性也帮助用户掌握概念和语法。块注释也可以被用于关闭代码：惯用法是在需要关闭代码的前后加上两行，分别包含—[[和—]]。要打开代码，只需要在第一行添加一个-：这两行便都变成单行注释。</p>
<p>像长字符串一样，块注释也可以嵌套，同样有长字符串所遇到的问题。特别是包含不平衡的]]有效的Lua代码，比如a[b[i]]，不能在Lua 5.0被可靠的注释掉。Lua 5.1长字符串的新语法也能用于块注释，形式是—[===[…]===]，这个问题因此得到简洁稳定的解决方案。</p>
<h2 id="6-5-函数"><a href="#6-5-函数" class="headerlink" title="6.5 函数"></a>6.5 函数</h2><p>Lua里的函数一直是第一类对象。一个函数可以在运行时创建，通过编译和执行包含其定义的字符串实现。随着Lua 3.1引入的匿名函数和upvalue，程序员可以在运行时创建函数，不再需要从文本里编译了。</p>
<p>Lua的函数无论是C或者Lua的，都没有声明。被调用时，他们会接受不定数量的参数：多余的参数会被丢掉，缺失的参数会被赋予nil值。（这吻合了多重赋值的语义）。C函数一直都能处理不同数量的参数。Lua 2.5介绍了可变参数类型Lua函数，标志是参数列表以…结尾（只在Lua 3.0出现的实验特性）。当一个变参函数被调用，对应…的参数将会收集到一个叫arg的table里。这种方式虽然很简单便捷，但是要把这些参数传给另一个函数，就需要解包这个table。因为程序员经常将参数传递给另一个函数，Lua 5.1允许…用于参数列表和赋值表达式的右值。这避免了没必要的创建arg table。</p>
<p>Lua执行的基本单位是代码块（chunk）；它只是一个语句序列。Lua的一个代码块就像其他语言的main程序：它可以包含函数定义和可执行代码。（实际上，一个函数定义就是可执行代码：一个赋值）同时，一个代码块和一个普通的Lua函数非常相似。比如，代码块和普通Lua函数一直有着同一类字节码。但是，在Lua 5.0之前，代码块需要一些内部的“魔法”来开始执行。Lua 2.2开始，代码块开始类似普通函数，那时候，作为未记录在文档的特性，函数外可以定义局部变量了（只在Lua 3.1变成了官方发布）。Lua 2.5允许代码块返回值。Lua 3.0代码块变成了内部的函数，只是他们在被编译后立即执行；他们不以函数形式暴露给用户层。最终，在Lua 5.0迈出了这一步，将代码块的加载和执行变成了两步，来为宿主程序员提供更好的控制性和错误报告。结果，Lua 5.0里代码块变成了日常无参数的匿名函数。Lua 5.1里代码块变成了匿名变参函数，因为可以在运行时进行值传递。这些值都可以透过新的…机制进行传递。</p>
<p>从别的角度看，代码块就像其他语言的模块一样：他们一般用来提供全局环境的函数和变量。最初，我们没想过Lua会用于大规模编程，所以我们不觉得需要显式的模块。而且，我们觉得table就足够应付模块的需要了。在Lua 5.0里，我们将所有标准库打包进table里了，以此说明table足够应付模块。这鼓励了其他人将库打包进table里，让共享库变得更为简单。我们现在觉得Lua可以用来大规模编程了，特别是在Lua 5.1带来了基于table的包系统和模块系统之后。</p>
<h2 id="6-6-词法范围"><a href="#6-6-词法范围" class="headerlink" title="6.6 词法范围"></a>6.6 词法范围</h2><p>在Lua开发的早期，我们开始考虑全词法域的第一类函数。这是一个优雅的构造，完美演绎了Lua的少而精哲学，提供少量但强大的构造方式。同时也让Lua适合进行函数式编程。但是，我们找不到全词法的合理实现方式。一开始，Lua就使用了一个简单的基于数组的栈来记录活跃记录（所有临时变量和局部变量存储的地方）。这个实现简单高效，我们找不到理由推翻它。当我们允许嵌套函数和全词法域之后，一个内部函数使用的变量，可能有着比创建它的函数更长的生命期，所以我们不能用栈的方式去处理这些变量。</p>
<p>简单的Scheme实现在堆上分配栈帧。1987年，Dybvig[20]描述了如何使用栈去分配栈帧，前提是栈帧不包含嵌套函数使用的变量。他的方法需要编译器提前知道，一个变量是不是嵌套函数的自由变量。这不适合Lua编译器，因为Lua编译器解析完表达式后就立即生成操作变量的代码，它无法得知任一变量是否会在稍后的运行中用作嵌套函数的自由变量。我们希望实现Lua时保持这个设计，因此没有使用Dybvig的方法。基于相同的原因，我们也不能使用先进的编译器技巧，比如数据流分析。</p>
<p>现在，有很多优化策略去避免使用堆来存储栈帧（e.g.,[21]），但它们全都需要编译器有中间表达方式，Lua编译器却没有。McDermott的基于栈的栈帧分配建议[36]，特别强调是针对解释器的，是我们所知唯一的不需要中间表示方式的代码生成。就像我们的实现一样【31】，他的提案将变量放在栈上，如果它们被嵌套的闭包使用又超出了词法域，就将它们移动到堆上。但是，他的提案假设环境是以关联表的方式组织的。所以，将一个环境移动到堆上，解释器只需要修正列表头部，所有的局部变量自动到堆上了。Lua使用真实的记录作为活跃记录，局部变量的访问转译成直接访问栈顶对应偏移地址，所以不能使用McDermott的方法。</p>
<p>在很长一段时间里，这些困难一直困扰着我们，无法在Lua里引入嵌套的第一类函数，并实现全词法域支持。最终，在Lua 3.1里，我们接受了一个称为upvalue的妥协实现。在这个方案里，内部函数不能访问和修改运行时的外部变量，但可以访问函数创建时的变量。这些变量称为upvalue。upvalue的主要优势，在于可以简单的实现：所有局部变量在栈上；创建一个函数时，函数被打包在一个闭包之中，闭包包含了函数使用的外部变量的副本。换句话说，upvalue是外部变量的冻结值。为了避免误解，我们创建了新的语法来访问upvalue：%varname。这个语法清晰表明代码是在访问变量的冻结值，而不是变量本身。upvalue虽然不能修改，但依然非常有用。有需要的时候，我们可以使用table作为upvalue来模拟可变外部变量：尽管我们不能改变变量指向的table，我们仍可以改变table的域本身。这个特性在以下场景特别有用：匿名函数作为高阶函数的参数，用于table遍历和模式匹配。</p>
<p>2000年12月，Roberto在他的书【27】的第一版手稿里写到”Lua通过upvalue提供了一种合适的词法作用域形式”。在2001年7月，John D. Ramsdell在邮件列表中争论到，”一门语言要么是词法作用域的，要么不是；在词法作用域前添加’合适的’这个形容词毫无意义”。那条消息促使我们寻找更好的解决方案，以及支持全词法域的方式。2001年10月，我们做出了了全词法域支持的初步实现，并发布到邮件列表中。构想是每个upvalue都是间接访问的，当变量在词法域的时候指向栈；在词法域结束的时候，会将变量值移到堆区，并将指针指向堆区。开放闭包（带有指向栈的upvalue）被保存在一个列表里，允许重定向和重用开放的upvalue。重用对于获得正确语义有其必要性。如果两个闭包，共享一个外部变量，各自有自身的upvalue，那么在词法域结束的时候，每个闭包都有自己的变量拷贝了，但正确的语义要求它们共享变量。为了保证重用，创建闭包的算法是这样做的：对于闭包使用的每个外部变量，首先搜索开放闭包的列表，如果发现一个upvalue指向同样的外部变量，就重用那个upvalue；否则，创建新的upvalue。</p>
<p>Edgar Toering，Lua社区的活跃成员，误解了我们对词法域的描述。结果是他理解的比我们的最初的构想要更好：只保留开放upvalue的列表，而不是开放闭包的列表。因为闭包使用的局部变量的数量，一般远小于使用它们的闭包的数量（局部变量的数量被代码文本静态限制了），他的实现比我们的更加高效。而且也更容易适配到协程中（基本上同一时间实现），因为我们可以为每个栈保留一个独立的upvalue列表。我们在Lua 5.0添加了全词法域支持，用的正是这个算法，因为它符合了我们的所有期望：它可以用一遍编译器实现；没有让只访问内部局部变量的函数增加运行代价，因为它们依然在栈上处理所有局部变量；而且访问外部局部变量的代价仅仅是一次额外的中转【31】。</p>
<h2 id="6-7-协程"><a href="#6-7-协程" class="headerlink" title="6.7 协程"></a>6.7 协程</h2><p>我们一直在为Lua寻找某种形式的第一类延续性。这种寻找是受Scheme的第一类延续的启发诞生的（Scheme真是我们的灵感源泉），也是游戏程序员对于”软”多线程的需要（一般描述为”以某种方式挂起一个角色，并且稍候继续”）。</p>
<p>2000年的时候，Maria Julia de Lima在Lua 4.0 alpha版实现了完整的第一类延续性，作为她在读PhD的部分工作成果【35】。她使用了一个简单的实现，因为就像词法域问题一样，以更高明的技巧实现协程，对于Lua总体上的简洁而言，实在太复杂了。对于她的实验来说，结果是令人满意的，但是作为最终产品，就显得太慢了。无论如何，她的实现发现了Lua的一个奇特问题。因为Lua是一门可扩展语言，有可能（而且很普遍）会从C调用Lua，或者从Lua调用C。因此，在一个Lua程序执行的任意点，当前延续性通常都是有部分是Lua，部分是C。尽管可以操作Lua的延续性（特别是通过操控Lua调用栈），却不能操作C的延续性，尤其是在ANSI C标准下。那时候，我们对这个问题没有很深的了解。特别是，我们找不到C调用的准确限制。Lima的实现只是简单的禁止了所有C调用。再次强调，这对于她的实验而言是可用的，但是对于Lua的官方发行版来说，完全无法接受。因为Lua和C代码可以轻易融合正是Lua的一个标记。</p>
<p>2001年12月，Thatcher Ulrich在没有理解这一困难的前提下，在邮件列表里宣布： 我已经为Lua 4.0创建了一个补丁，让Lua到Lua的调用变成非递归的（比如”无堆栈”(stackless)）。这带来了sleep()调用的实现， 允许从宿主程序退出【省略】，并让Lua state可以通过新的API函数lua_resume回到刚刚的执行环境。</p>
<p>换句话说，他提议了一个非对称的协程机制，基于两个原语：yield（他称为sleep）和resume。他的补丁遵循了邮件列表中，Bret Mogilefsky提到的，Grim Fandango公司针对Lua 2.5和3.1添加协作式多任务，这一实现的高阶描述（Bret不能提供细节，因为是公司私有的）。</p>
<p>在这次公告之后，2002年2月在哈佛举行了Lua库设计大会，有了一些关于Lua的第一类延续性的讨论。有人认为，如果第一类延续性太复杂，我们可以实现一次性延续性。其他人认为实现对称的协程更好。但我们找不到这些机制的合适实现，可以解决C调用的问题。</p>
<p>我们花了不少时间研究，才意识到为什么在Lua实现对称协程那么困难，为什么Ulrich的基于非对称协程的实现，避开了我们遇到的难点。一次性延续性和对称协程都涉及到操作全延续性。所以，只要这些延续性包含了任何C的部分，就不可能捕捉到他们（除非用ANSI C以外的工具）。相反，一个基于yield和resume的非对称的协程机制，只操作偏序延续性：yield捕获了当前调用点到对应resume之间的延续性【19】。在非对称协程上，当前延续性可以包含C的部分，只要他们在被捕获的延续性之外。换句话说，唯一的限制是我们不能跨C调用的yield。</p>
<p>意识到这一点之后，基于Ulrich的概念验证实现，我们在Lua 5.0实现了非对称协程。主要的改动是为虚拟机执行指令的解释器循环，不再能递归调用了。在之前的版本里，当解释器循环执行一个CALL指令，他会递归调用自己来执行被调用的函数。Lua 5.0里，解释器更像是一个真实的CPU：当他执行一个CALL指令的时候，他将一些上下文信息推到栈上，然后处理被调用函数，被调用函数返回的时候，再恢复上下文。改成这样以后，实现协程变得很直接了。</p>
<p>不像大多数非对称协程的实现，Lua里协程是stackfull【19】的。通过协程，我们可以实现对称协程，甚至是Scheme的call／1cc操作符（调用当前一次性延续）。但是，使用C函数在这些实现里还是受到严重限制。</p>
<p>我们希望Lua 5.0引入的协程成为一个标志，标志着协程作为强有力的控制结构的复兴。</p>
<h2 id="6-8-可扩展语义"><a href="#6-8-可扩展语义" class="headerlink" title="6.8 可扩展语义"></a>6.8 可扩展语义</h2><p>在5.2节，我们介绍了可扩展语义的一个例子，Lua 2.1里允许程序员使用fallback作为通用机制，处理Lua未定义的场景。fallback因此提供了一种可恢复异常处理的受限形式。尤其是，通过使用fallback，我们可以让一个值对不是为其设计的操作进行响应，或者让一个值表现得像另一个值一样。例如，我们可以让userdata和table响应算术运算符，userdata可以表现得像table一样，字符串表现得像函数一样，诸如此类。此外，我们可以让一个table响应不存在的key，这是实现继承的基石。通过table索引的fallback机制，以及一点点定义和调用方法的语法糖，面向对象编程及继承在Lua里成为了现实。</p>
<p>尽管对象、类和继承不是Lua的核心概念，他们可以直接在Lua里实现，并根据应用程序的需要采取多种多样的方式实现。换句话说，Lua提供了机制，而非策略——我们由始至终都紧密追随的宗旨。</p>
<p>最简单的继承是通过委托继承，Self率先引入这机制并被其他基于原型的语言所采用，比如NewtonScript和JavaScript。下面的代码展示了Lua 2.1里通过委托实现继承的一个实现。<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Index</span><span class="params">(a, i)</span></span></span><br><span class="line">  <span class="keyword">if</span> i == <span class="string">"parent"</span> <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">  <span class="keyword">end</span></span><br><span class="line">  <span class="keyword">local</span> p = a.parent</span><br><span class="line">  <span class="keyword">if</span> <span class="built_in">type</span>(p) == <span class="string">"table"</span> <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">return</span> p[i]</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">  <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line">setfallback(<span class="string">"index"</span>, Index)</span><br></pre></td></tr></table></figure></p>
<p>当访问一个table的缺失域（比如属性或者方法），就会触发索引的fallback。实现继承就是将索引的fallback指向一条向上的祖系链，并有可能再次触发fallback，直到遇到一个拥有指定域的table，或者这条链完结。</p>
<p>在设置了索引fallback以后，以下代码会打印red，虽然b没有color域：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">a=Window&#123;x=<span class="number">100</span>,y=<span class="number">200</span>,color=<span class="string">"red"</span>&#125;</span><br><span class="line">b=Window&#123;x=<span class="number">300</span>,y=<span class="number">400</span>,parent=a&#125;</span><br><span class="line"><span class="built_in">print</span>(b.color)</span><br></pre></td></tr></table></figure></p>
<p>通过parent域进行委托，这过程中没有黑魔法或者硬编码。程序员拥有完整的自由：他们可以使用parent或者其他名字，可以通过尝试一系列的父系函数实现多继承，诸如此类。我们没有硬编码任一行为的决定，引出了Lua的主要设计概念：元机制。我们提供了方法让用户可以编码他们需要的特性，以他们想要的方式实现，并只实现他们需要的特性，而非将语言本身塞满特性。</p>
<p>Fallback机制极大扩展了Lua的表达能力。但是，fallback是全局的句柄：对于每个事件，只有一个函数可以被执行。结果就是，很难在一个程序里混合不同的继承机制，因为只有一个钩子来实现继承（只有索引fallback）。对于一个单一的开发组，基于自己的对象系统进行开发，这可能不是什么严重的问题，但是一个组尝试使用另一个组的代码，就会变成问题，因为他们的对象系统并不一定一致。不同机制的钩子可以串在一起，但是链式调用很慢，很复杂，充满错误，并且不礼貌。fallback链不鼓励代码共享和重用；实际上几乎没有人使用。这让使用第三方库变得很难。</p>
<p>Lua 2.1允许标记userdata。Lua 3.0我们将标记扩展到所有值，并用标记方法（tag methods）取代了fallback机制。标记方法是只在带有指定标记的值上执行的fallback。这让实现独立的继承机制成为可能。这种情况下不需要链式调用，因为一个标记的方法不会影响另一个标记的方法。</p>
<p>标记方法机制工作的很好，一直存续到Lua 5.0为止，我们在Lua 5.0实现了元表和元方法来取代标记和标记方法。元表只是普通的Lua table，所以可以用Lua直接操作，不需要特殊函数。就像标记一样，元表可以用来表示userdata和table的用户定义类型：所有“同类”对象应该共享同一个元表。不像标记，元表和他们的内容会在所有引用消失后自动被回收掉。（相反，标记和标记方法会等到程序结束才会被回收。）元表的引入同时简化了实现：标记方法需要在Lua核心代码里添加特殊的私有表示方法，元表主要就是标准的table机制。</p>
<p>下面的代码展示了Lua 5.0里，继承是如何实现的。index元方法取代了index标记，元表里则是用<strong>index域来表示。代码通过将b的元表里的</strong>index域指向a，实现了b继承a。（一般情况下，index元方法都是函数，但我们允许它设为table，以直接支持简单的委托继承。）<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">a=Window&#123;x=<span class="number">100</span>, y=<span class="number">200</span>, color=<span class="string">"red"</span>&#125;</span><br><span class="line">b=Window&#123;x=<span class="number">300</span>, y=<span class="number">400</span>&#125;</span><br><span class="line"><span class="built_in">setmetatable</span>(b, &#123;<span class="built_in">__index</span> = a&#125;)</span><br><span class="line"><span class="built_in">print</span>(b.color) —&gt;red</span><br></pre></td></tr></table></figure></p>
<p>6.9 C API<br>Lua提供了C函数库和宏，允许宿主和Lua通信。这层Lua和C之间的API是Lua的一个主要组成部分；它让Lua变成了一门嵌入式语言。</p>
<p>就像Lua的其他部分一样，API在Lua的演进里发生了很多变化。可惜，和语言本身相比，API设计很少受到外来影响，主要是因为这方面很少研究活动。</p>
<p>API一直是双向的，因为从Lua 1.0开始，我们就考虑了从C调用Lua，以及从Lua调用C，并认为两者同样重要。能从C调用Lua让Lua变成了扩展语言，即一门用于通过配置、宏和其他终端用户定制功能，来扩充应用程序特性的语言。能从Lua调用C让Lua变成了可扩展语言，因为我们可以用C函数扩展Lua，为其提供新设施。(这就是为什么我们说Lua是一门可扩展的扩展语言【30】)这两个视角的共同点是，API必须处理好C和Lua之间的失配：C是静态类型的，Lua是动态类型的，C是人工管理内存，Lua是自动进行垃圾回收的。</p>
<p>目前，C API通过引入一个虚拟栈来交换Lua和C的数据，解决了这两个失配问题。每个Lua的C函数调用都会使用一个新的栈帧，包含了函数调用的参数。如果C函数需要给Lua返回值，它就在返回前把这些值压到栈上。</p>
<p>每个栈槽可以保存任一Lua值类型。每个Lua类型都有C的唯一表示（比如字符串和数字），目前有两个API 函数：一个是注入函数，用于压栈一个Lua值，对应给出的C值；一个是投影函数，可以返回一个C值，对应指定栈位置的Lua值。Lua值类型里没有对应C表示的（比如table和函数），可以通过API及他们的栈位置来操作。</p>
<p>实际上，所有的API函数都从栈上获得他们的操作数，并将结果放到栈上。因为栈可以存放任意Lua值类型，这些API函数都可以操作任意Lua类型，因此解决了类型失配问题。为了避免C使用中的Lua值被回收掉，栈上的所有值都不会被回收。当一个C返回，它的Lua栈帧消失了，所有的C函数使用的Lua值都会被释放。如果没有其他引用，这些值最终都会被回收。这就解决了内存管理的失配问题。</p>
<p>我们花了好多时间才实现成现在这套API的样子。为了讨论API的演进，我们以等价于如下Lua函数的C代码进行说明：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span><span class="params">(t)</span></span></span><br><span class="line">    <span class="keyword">return</span> t.x</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p>
<p>换句话说，这个函数接受一个单参数，参数类型应该是table，并返回table里储存在x域的值。尽管很简单，这个例子足够说明API里的三个重要问题：如何获取参数，如何索引table，如何返回值。</p>
<p>在Lua 1.0里，foo函数的C代码如下：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">void foo_l(void) &#123;</span><br><span class="line">    lua_Object t = lua_getparam(<span class="number">1</span>);</span><br><span class="line">    lua_Object r = lua_getfield(t, <span class="string">"x"</span>);</span><br><span class="line">    lua_pushobject(r);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>注意，我们所索引的值是存储在字符串索引x，因为t.x是t[“x”]的语法糖。而且所有的API都是以lua_(或者LUA_)开头的，以此避免和其他C库的命名冲突。</p>
<p>为了暴露这个C函数给Lua，并使用foo作为函数名，我们会这样做</p>
<p>lua_register(“foo”, foo_l);<br>之后，foo就能像普通Lua函数一样从C调用了：</p>
<p>t = {x = 200}<br>print(foo(t))         —&gt; 200<br>API的一个关键部件是lua_Object类型，定义如下：</p>
<p>typedef struct Object *lua_Object;<br>简而言之，lua_Object是一个抽象类型，用于在C里表示Lua值。传递给C函数的参数，通过调用lua_getparam获得，这个函数会返回lua_Object。在上述例子里，我们调用一次lua_getparam来获得table，这个table应当是foo函数的第一个参数。（其余的参数会自动略过。）当table在C里变成可用的（作为lua_Object），我们就可以通过调用lua_getfield函数获取x域的值。这个值在C里也是表示为lua_Object，最后会通过lua_pushobject压栈，送回到Lua中去。</p>
<p>栈也是API的另一个关键部件。它用来从C里传值给Lua。每个Lua类型都有对应的C版本push函数：number类型有lua_pushnumber，string类型有lua_pushstring，特殊值nil可以用lua_pushnil。也有允许C回传任意Lua值到Lua的lua_pushobject函数。当一个C函数返回，所有栈上的值都会返回给Lua，作为C函数的结果（Lua的函数可以返回多个值）。</p>
<p>概念上，lua_Object是一个union类型，因为它可以指代任意Lua值。很多脚本语言，比如Perl，Python和Ruby，依然使用union类型来在C层表示它们的值。这种表示方法的主要弊端，在于很难为语言设计垃圾回收。没有额外信息的情况下，垃圾回收器不可能知道，一个值是否被C代码里的一个union值任用。没有这个信息，垃圾回收器有可能回收值，导致union变成了悬空指针。即使这个union是C函数的局部变量，这个C函数还是能再次调用Lua，并触发垃圾回收流程。</p>
<p>Ruby通过检查C栈来解决这个问题，这种方法难以移植。Perl和Python则是以另一种方式实现，通过为union类型提供显式引用计数函数来解决这个问题。一旦你增加了一个值的引用计数，垃圾回收器就不会回收那个值了，直到你将计数减为0。但是，让程序员保证引用计数的正确性并不容易。很容易会产生错误，但后期很难查出来（对内存泄漏和悬空指针排查过的人都可以作证）。此外，引用计数解决不了循环引用的问题。</p>
<p>Lua从来没有提供过这类引用计数函数。Lua 2.1之前，为了保证一个未被引用的lua_Object不被回收，你可以做的事情不多，顶多就是自己持有这个对象的引用，并避免调用Lua。（只要你可以保证union指向的值同样储存在一个Lua变量里，你就是安全的。）Lua 2.1带来了一个重要的变化：它跟踪了所有传给C的lua_Object，保证它们在C函数活跃的时候不会被回收掉。当C函数返回到Lua之后，（只有在这个时候）所有这些lua_Object的值引用都会被释放，所以它们可以被回收。【JNI使用一个类似的方法来处理局部引用】</p>
<p>更具体地说，Lua 2.1里，一个lua_Object不再是一个指向Lua内部数据结构的指针，而是一个内部数组的索引，这个内部数组存储了所有传给C的值：</p>
<p>typedef unsigned int lua_Object;<br>这个改动让lua_Object的使用变得可靠了：当一个值在数组里，Lua就不会回收它。当C函数返回了，整个数组就被释放，并回收掉没有其他引用的函数所使用的值。（这个改动也给实现垃圾回收带来了更多的灵活性，因为它可以按需移动对象了；但是，我们没有这么做）</p>
<p>对于简单的使用，Lua 2.1的行为是非常实际的：这种方案安全，C程序员也不用担心引用计数。每个lua_Object就像C里的一个局部变量：对应的Lua值会保证处理它的C函数的生命期内都存活。对于复杂的使用场景：这个简单的方法有两个缺陷，需要额外的机制保证：有时候，lua_Object的值需要锁定一段比C函数生命期更长的时间；有时候，需要更短的锁定时间。</p>
<p>第一种情况有一个简单的解决办法：Lua 2.1引入了引用系统。函数lua_lock从栈上取一个Lua值，并返回一个引用。这个引入是一个数字，可以以后任意时间使用来获得Lua值，使用lua_getlocked函数即可。（同时还有lua_unlock函数，用于销毁一个引用）通过这种引用方法，要保存非本地的C变量就变得简单了。</p>
<p>第二种情况更为微妙。存在内部数组的对象只有函数返回的时候才会被释放。如果函数使用了太多的值，就会发生数组越界，或者内存不足错误。比如，考虑以下的高阶迭代函数，会迭代调用函数并打印结果，直到调用返回nil：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">void l_loop(void) &#123;</span><br><span class="line">  lua_Object f = lua_getparam(<span class="number">1</span>);</span><br><span class="line">  <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    lua_Object res;</span><br><span class="line">    lua_callfunction(f);</span><br><span class="line">    res = lua_getresult(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (lua_isnil(res)) <span class="keyword">break</span>;</span><br><span class="line">    printf(<span class="string">"%s\n"</span>, lua_getstring(res));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这段代码的问题是，每个调用返回的字符串都不能回收，直到循环的底部（整个函数结束为止），因此有可能发生数组越界或者内存耗尽。这种错误很难追踪，因此Lua 2.1的实现设置了内部数组保存lua_Object的上限。Lua会报错：“C函数里太多对象”而非泛泛的“内存耗尽”错误，让错误变得容易跟踪，但没有完全避免这一问题。</p>
<p>为了解决这个问题，Lua 2.1的API提供了两个函数，lua_beginblock和lua_endblock，为lua_Object的值创建动态范围（blocks）；所有在lua_beginblock之后创建的值，都会在对应的lua_endblock调用后，从内部数组里被移除。但是，因为块原则（block discipline）不能强制让C程序员遵守，很容易就会忘记使用这些区块。而且，这些显式作用域控制用起来有点棘手。比如，一个欠周到的修复前述例子的方法，是用块包裹住for循环的循环体，但这样做存在问题：我们需要在break语句所在位置，也调用lua_endblock。这个Lua对象扩大生命周期的困难，经历了数个版本，最后在Lua 4.0才解决，我们重构了整个API。无论如何，就像我们之前提到的那样，对于普通应用，API是非常易用的，而且大部分程序员永远不会遇到这里描述的情景。更重要的是，API是安全的。错误用法会产生定义好的错误，但不会产生悬空指针或者内存泄露。</p>
<p>Lua 2.1还为API带来了其他改变。其中之一是引入了lua_getsubscript，允许使用任何值来索引table。这个函数没有显式参数：它从栈上取出table和key。老的lua_getfield被重定义为宏，以实现兼容：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> lua_getfield(o,f) \</span></span><br><span class="line">    (lua_pushobject(o), lua_pushstring(f), \</span><br><span class="line">     lua_getsubscript())</span><br></pre></td></tr></table></figure></p>
<p>（C API的向后兼容性通常用宏来实现，只要代价可以接受。）</p>
<p>除了上述修改，从语法上讲，API从Lua 1到Lua 2的变化是很少的。比如说，我们的说明函数foo可以直接用Lua 1.0的版本，在Lua 2.0依然可以运行。lua_Object的含义变了，lua_getfield用新的原语实现了，但对于普通用户来说，就好像没有变化一样。因此，API一直很稳定，持续到Lua 4.0版本。</p>
<p>Lua 2.4扩展了引用机制以支撑弱引用。Lua程序的一个常见设计，是使用一个Lua对象（通常是一个table）作为一个C对象的代理。常见的场景是，C对象必须知道它的代理是什么，并保留对代理的引用。但是，那个引用会阻止对代理对象的回收，即使对象从Lua里不能再访问了。在Lua 2.4版本，就可以创建一个弱引用指向proxy；那个引用不会阻止代理对象的回收。获取一个被回收的引用将返回一个特殊值LUA_NOOBJECT。</p>
<p>Lua 4.0为C API带来了两个新颖的设计：支持多个Lua虚拟机和用于C和Lua交换值的虚拟栈。要实现多个独立的Lua虚拟机，就需要去掉所有全局的虚拟机。直到Lua 3.0为止，都只有一个Lua虚拟机存在，而且使用了很多静态变量，分散在代码里。Lua 3.1 引入了多个独立Lua虚拟机；所有静态变量都归集到一个单一的C结构体里去。添加了一个新的API来切换虚拟机，但任意时刻只有一个Lua虚拟机是活跃的。所有其他API函数都是针对活跃虚拟机操作的，而活跃虚拟机并没有出现在调用里。Lua 4.0在API引入了显式的Lua虚拟机。这引起了和前述版本的不兼容。【17】所有和Lua通讯的C代码（尤其是向Lua注册的C函数）需要在C API中添加一个显式的虚拟机变量。既然所有C函数都必须重写，我们借此机会在Lua 4.0里大改了C-Lua交流方式：我们替换了lua_Object的概念，代之以显式的虚拟栈，用来在Lua和C之间双向通信。这个栈也能用来存储临时值。</p>
<p>在Lua 4.0里，我们的foo函数可以改写如下：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">int foo_l (lua_State *L) &#123;</span><br><span class="line">   lua_pushstring(L, <span class="string">"x"</span>);</span><br><span class="line">   lua_gettable(L, <span class="number">1</span>);</span><br><span class="line">   <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>第一个区别是函数签名：foo_l现在接受一个Lua虚拟机，用于接收操作数和返回函数返回值。在之前版本里，函数调用完毕后，所有残留在栈上的值都会返回Lua。现在，因为所有操作都使用栈，它可以包含除返回值外的中间值，所以函数需要告诉Lua，栈上到底有几个元素是返回值。另一个区别是，不再需要lua_getparam了，因为函数参数在函数开始时就在栈上，并可以透过他们的index直接访问，就好像其他栈上的值一样。</p>
<p>最后的区别是lua_gettable的使用，这个函数用于替代lua_getsubscript，用于访问table里的域。lua_gettable接受一个栈索引（而不是一个Lua对象），索引指向所操作的table，从栈顶弹出key，并将结果压栈。该函数不改变table在栈中的位置，因为table经常重复索引。在函数foo_l里，lua_gettable使用的table在栈位置1，因为他是函数的第一个参数，key是字符串”x”，需要在调用lua_gettable之前压栈。这个函数调用将栈中的key，换成了对应的table里的值。所以，在lua_gettable之后，栈上有两个值：栈位置1的table，和栈位置2上key索引到的结果。这个C函数返回1，告诉Lua使用栈顶的值作为函数的唯一返回值。</p>
<p>为了更清楚的说明新的API，这是我们循环例子的Lua 4.0实现：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">l_loop</span> <span class="params">(lua_State *L)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    lua_pushvalue(L, <span class="number">1</span>);</span><br><span class="line">    lua_call(L, <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (lua_isnil(L, <span class="number">-1</span>)) <span class="keyword">break</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%s\n"</span>, lua_tostring(L, <span class="number">-1</span>));</span><br><span class="line">    lua_pop(L, <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>为了调用一个Lua函数，我们将它压到栈上，并压入其参数（上面的例子没有）。然后我们调用lua_call，告诉lua要从栈上获取多少个参数（因此隐式的表达了函数在栈上的位置），要在调用里获取多少个结果。在例子里，我们没有使用参数，期待一个结果。lua_call函数将函数和参数从栈上移除，并压入指定数目的结果。调用lua_pop会从栈上去掉一个返回值，让栈在循环开始时处在相同的位置。为方便起见，我们可以用正数索引栈，表示从底部开始的位置，或者负数来表示从栈顶开始的位置。在示例中，我们使用-1作为索引，让lua_isnil和lua_tostring从栈顶开始索引元素，该位置包含了函数调用的结果。</p>
<p>后见之明，在API里使用一个单独的栈是很明显的简化措施，但是Lua 4.0发布的时候，很多用户都抱怨新api的复杂性。尽管Lua 4.0的API有更简洁的概念模型，直接操作栈还是需要一些思考来保证正确性。很多用户都宁愿用前一版的API，即使它没有任何清晰的概念模型来表达其原理。简单的任务不需要概念模型，之前的API因此工作的很好。更复杂的任务经常会打破用户自定义的自有模式，因为大部分的用户从未用C来编码复杂项目。所以，新API第一眼看上去太复杂了。但是，这些怀疑论最后都消失了，因为用户理解并肯定了新模型，这个模型证明了自身的简洁、不易出错。</p>
<p>Lua 4.0多个虚拟机共存的可能性，导致了有关引用机制的意料之外的问题。之前，一个C库如果需要保留固定对象，可以创建这个对象的引用，并存储在全局C变量里。在Lua 4.0，如果一个C库是和多个虚拟机协作的，它就需要为每个虚拟机保留单独的引用，不能用全局C变量来表示了。为了解决这个难题，Lua 4.0引入了注册表（registry），注册表就是一个普通的Lua表，只在C层使用。有了注册表，C库想要保留Lua对象，就能选择唯一的key索引，并在注册表里用这个key来关联对象。因为每个独立的Lua虚拟机都有自己的注册表，C库可以在所有虚拟机里都使用同样的key来操作对应的对象。</p>
<p>我们可以轻易的在注册表上实现之前的引用机制，只需要使用整数key来表示索引即可。要创建一个新的索引，我们只需要找到没使用的整数key，并在那个key存储值。获取一个引用就是一个简单的table访问。但是，我们不能使用注册表实现弱引用。所以，Lua 4.0保持了之前的引用机制。Lua 5.0里，由于语言本身引入了弱表，我们终于可以将核心里的引用机制干掉，并移到库里。</p>
<p>C API在向着完整性缓慢演进。从Lua 4.0开始，所有标准库函数都可以只使用C API来编写。在Lua 4.0之前，Lua有数目可观的内置函数（Lua 1.1有7个，Lua 3.2有35个），大部分可以使用C API编写，但因为追求速度，我们并没有这样做。有少量内置函数是不能用C API来编写的，因为C API并不完整。比如，Lua 3.2之前是不可能用C API来遍历table的内容的，尽管在Lua里可以使用内置函数next去实现。目前，C API也尚不完整，不是所有Lua能干的都可以用C API完成；比如，C API还缺少对Lua值进行算术运算的函数。我们计划下个版本解决这个问题。</p>
<h2 id="6-10-userdata"><a href="#6-10-userdata" class="headerlink" title="6.10 userdata"></a>6.10 userdata</h2><p>从第一版开始，Lua的一个重要特性就是可以直接操作C数据，这个特性是通过提供userdata这种特殊Lua数据类型来实现的。这种能力是Lua可扩展性的基础。</p>
<p>对于Lua程序来说，userdata类型在Lua的演进里从未改变：尽管是第一类值类型，userdata还是透明的类型，在Lua里唯一有效的操作就是相等性判断。其他针对userdata的操作（比如创建，探测，修改）都需要C函数提供。</p>
<p>对于C函数而言，userdata类型在Lua的演进里经历很多变更。在Lua 1.0里，一个userdata值只是一个简单的void*指针。这种简化方法等主要问题是，C库没有办法检查一个userdata是不是有效的。尽管Lua代码不能创建userdata值，但可以将一个库创建的userdata传给另一个库，而另一个库期待的其实是指向不同结构的指针。因为C函数没有机制检查这种失配，指针适配往往对应用程序造成致命后果。我们一直认为，Lua程序导致宿主程序崩溃是不可接受的。Lua应该是一门安全的语言。</p>
<p>为了克服指针失配的问题，Lua 2.1引入了标记的概念（也是Lua 3.0里标记方法的概念来源）。一个标记只是一个任意的整数值，关联到一个userdata上。一个userdata的标记只能在创建时设置一次，假设每个C库都使用自己的独特的标记，C代码可以检查userdata的标记，确保userdata就是所期待的类型。（一个库作者如何选择不会和别人重复的tag还是一个问题。这个问题只有在Lua 3.0才得到解决，在这个版本Lua提供了lua_newtag作标记管理）</p>
<p>Lua 2.1更大的问题是C资源的管理。userdata往往指向一块在C里动态分配的内存，当对应userdata在Lua里被回收，这块内存也需要释放掉。但是，userdata是值，不是对象。因此，他们不会被回收（就像number不会被回收一样）。为了克服这个限制，一个典型的设计是利用table作为C结构在Lua中的代理，将实际的userdata存储在预定义的域里。当table被回收，它的析构函数就释放对应的C结构。</p>
<p>这种简单的解决方案会产生潜在问题。因为userdata是存在代理table的一个普通域里面，一个恶意的用户有可能在Lua里污染它。特别是，一个用户可以创建userdata的副本，并在table被回收后继续使用这个副本。这个时候，对应的C结构已经被销毁了，userdata就变成悬空指针了，这会带来灾难性后果。为了改善对userdata生命周期的控制，Lua 3.0将userdata从值类型改为对象类型，以便垃圾回收。用户可以使用userdata析构函数（垃圾回收的标记方法）来释放对应的C结构。Lua的垃圾回收器的正确性，保证了userdata不能在回收后再被使用。</p>
<p>但是，userdata作为对象会带来一致性问题。给出一个userdata，要获得对应的指针是很简单的，但是我们经常需要做相反的事：给出一个C指针，获得对应的userdata。在Lua 2中，两个具有相同指针和相同标记的userdata就是相同的；相等性是基于它们的值。所以，给出指针和标记，我们就能获得userdata。在Lua 3，因为userdata变成了对象，所以相等性变成了一致性：两个userdata只有在两者同一的情况下才会相同（也就是说，这两个userdata是同一个）。每个userdata的创建都不一样。因此，一个指针和标记不足以获得对应的userdata。</p>
<p>为了解决这一困难，并解决与Lua 2的不兼容问题，Lua 3采用了下面的语意来将一个userdata压到栈上：如果Lua已经有一个userdata，拥有给出的指针和标记，那么那个userdata就会被推到栈上；否则就创建一个新的userdata并压入栈。所以，C代码要翻译一个C指针倒对应的userdata就变简单了。（实际上，C代码可以沿用Lua 2的。）</p>
<p>但是，Lua 3的行为有一个主要缺陷：它将两个基本操作合成了一个原语（lua_pushuserdata）：搜索userdata和创建userdata。比如，没有办法在不创建userdata的情况下，检查一个给定的C指针有没有对应的userdata。而且，也不能不管C指针，直接创建一个新的userdata。如果Lua已经有对应值的userdata，就不能再创建新的userdata了。</p>
<p>Lua 4引入了新函数lua_newuserdata以缓解这个问题。不像lua_pushuserdata，这个函数总是会创建一个新的userdata。而且，更重要的是，这些userdata可以存储任意C数据，而不仅仅是指针。用户可以告诉lua_newuserdata需要分配多少内存，lua_newuserdata会返回一个指针，指向所分配的内存。通过让Lua给用户分配内存，很多常见的与userdata相关的任务都简化了。比如，C代码不需要处理内存分配错误，因为Lua已经接手了。更重要的是，C代码不需要处理内存释放：这种userdata使用的内存会在userdata被回收的时候，被Lua自动释放。</p>
<p>但是，Lua 4依然没有提供一个漂亮的解决方案来应对搜索问题（即通过给定C指针寻找一个userdata）。所以，它保留了lua_pushuserdata的行为，结果产生了一个混合的系统。只有在Lua 5的时候，我们才删除了lua_pushuserdata，并去掉userdata创建和搜索的关联。实际上，Lua 5将整个搜索设施都移除了。Lua 5也引入了轻userdata（light userdata），只用来存放C指针，跟Lua 1的普通userdata完全一致。应用程序可以使用弱表来关联C指针（light userdata所包含的）和对应的“重”userdata。</p>
<p>正如其他Lua演进一样，Lua 5的userdata比Lua 4的更加灵活；也更容易解释和实现。对于简单的用例，比如只需要存储一个C结构，Lua 5的userdata就很易用。对于更为复杂的需求，比如需要将一个C指针映射到一个Lua的userdata，Lua 5提供了机制（light userdata和弱表）给用户来实现适合宿主应用的策略。</p>
<h2 id="6-11-反射性"><a href="#6-11-反射性" class="headerlink" title="6.11 反射性"></a>6.11 反射性</h2><p>Lua从刚开始的版本就支持一些反射设施。提供支持的主要原因是，Lua的建议用法是作为替换SOL的配置语言。正如第四章所述，我们的设想是，程序员若有需要，可以使用语言本身，写日常的类型检查。</p>
<p>比如，如果一个用户写了下面的代码：</p>
<p>T = @track{ y=9, x=10, id=”1992-34” }<br>我们希望可以检查到，track的确有一个域y，这个域就是一个数字。我们也希望可以校验出track没有外来的域（比如检查出打字输入错误）。为了完成这两个任务，我们需要访问到Lua值的类型，以及遍历一个table的机制，并访问所有键值对。</p>
<p>Lua 1.0用两个函数完成了所需的功能，这两个函数延续到今天：type和next。type函数返回一个描述给定值的类型的字符串（比如number, nil, table之类）。next函数接受一个table和一个键，并返回一个table里的“下一个”键（以任意顺序）。以next(t, nil)调用会返回“第一个”键。通过next函数，我们可以遍历一个table并处理所有的键值对。比如，下面的代码可以打印table t的所有键值对：</p>
<p>k = next(t, nil)<br>while k do<br>    print(k, t[k])<br>    k = next(t,k)<br>end<br>所有这些函数都有一个简单的实现：type检查给定值的内部标记，并返回对应的字符串；next根据内部table的表示，在table里寻找给定的key并访问下一个key。</p>
<p>在Java和Smalltalk这些语言里，反射需要具体的概念比如类、方法、实例变量。而且，这种具像化还需要新的概念比如元类（具体类的类）。Lua不需要这些东西。Lua里，大部分类似Java反射包提供的设施都是免费的：类和模块都是table，方法就是函数。所以，Lua不需要任何特别的机制来具像化；它们就是朴素的程序值。类似的，Lua不需要特殊机制来在运行时建立方法调用（因为函数是第一类值，而且Lua的参数传递机制天然支持可变数量参数传递），也不需要特殊的机制来访问一个全局变量或者根据一个给定的名字访问一个实例变量（因为它们都是普通的table域）。</p>
<h1 id="7-回顾"><a href="#7-回顾" class="headerlink" title="7 回顾"></a>7 回顾</h1><p>这一节我们会给出一个Lua演进的简短评论，讨论其中做得好的，我们后悔的，以及我们不后悔，但是觉得可以以不同方式实现的。</p>
<p>最高明的决策，莫过于Lua 1.0的时候决定使用table作为Lua唯一的数据结构机制。table被证明是强大而高效的。table在语言里的核心地位和它的实现，是Lua的主要特性。我们顶住了用户要求添加其他数据结构的压力，主要是添加“真正的”数组和元祖，对此我们表现得十分顽固，但我们也提供了高效实现的table和灵活的设计。比如，我们可以实现一个set（集合），只需要将它的元素作为table的key即可。全赖Lua的table接受任意值作为key，这个特性才得以实现。</p>
<p>另一个则是我们对可移植性的坚持，最初是因为Tecgraf客户处在大量不同的平台上。这允许Lua运行在我们从未梦想得到支持的平台上。特别是，可移植性是Lua被游戏开发广泛采用的原因之一。受限的环境，比如游戏主机，一般不支持完整语意的全套标准C库。最终，通过减少Lua核心对标准C库的依赖，我们将Lua核心打造成只依赖独立的ANSI C实现标准。这一举措主要是为了嵌入的灵活性，但同样增加了可移植性。比如，从Lua 3.1开始，只要摆弄代码里少数几个宏，就可以让Lua使用一个应用程序自定义的内存分配器，而不是直接使用malloc等内存分配函数。从Lua 5.1开始，内存分配器可以在创建Lua虚拟机的时候动态提供。</p>
<p>事后来看，我们认为在一个小型委员会的抚养下成长，对于Lua的演进是十分积极的。由一个庞大委员会设计的语言，倾向于变得十分复杂，从未完全实现过它们支持者的期望。大部分成功的语言都是被抚养长大，而非设计出来的。它们遵循着一个缓慢的自下而上的过程，以一门小语言开始，带着谦虚的目标。语言的成长是真实用户实际反馈的成果，这个过程里设计渐渐浮出水面，实际有效的新特性得到认同。这个描述正是对Lua演进的真实写照。我们倾听用户的反馈和建议，但我们只在三人同意的前提下才添加一个新的特性；否则，就留待未来决定。稍后再添加特性比移除特性要容易得多。这个开发过程对于保持语言的简洁性至关重要，而简洁性是我们最重要的资本。绝大部分Lua的其他特性——速度、体积小、可移植性——都是从简洁性而来。</p>
<p>从第一个版本开始，Lua就有真实用户了，也即是除了我们自己以外的用户，他们根本不关心语言本身，只关心如何高效率的使用这门语言。用户总是为语言做出重要贡献，通过建议，抱怨，使用报告，和问题等形式。我们的小小委员会在管理这类反馈的时候再一次扮演了重要角色：它的结构给我们足够的惯性，去贴近用户、倾听用户，但不完全遵循它们的建议。</p>
<p>Lua的最佳描述是：一个封闭开发的开源项目。这意味着，尽管源码对于审查和采用是免费可得的，Lua还是没有以合作的方式开发。我们会采纳用户建议，但从未逐字逐句采纳他们的代码。我们总是尝试实现我们自己的设计。</p>
<p>Lua演进的另一个不寻常的方面，是我们处理不兼容修改的方式。很长一段时间里，我们都认为简洁和优雅比兼容前一个版本更为重要。当一个老的特性被新特性取代，我们就删掉老的特性。我们经常（但非总是）提供一些兼容性工具，比如一个兼容的库，一个转换脚本，或者编译时的选项来保留老特性。在这些情形下，用户需要在转移到新版本的时候采取一定措施。</p>
<p>我们并未真正后悔这种演进方式。但是，我们最终变得更为保守了。不仅因为我们的用户和代码基变得比以前更大，还因为我们觉得作为一门语言，Lua变得更成熟了。</p>
<p>我们应该从一开始就引入布尔类型，但我们希望从最为简洁的语言开始。没有一开始就引入布尔类型，带来了一些不幸的副作用。其中一个是我们现在有两个false值了：nil和false。另一个是，Lua函数用于警告调用者错误发生的常见用法，是返回nil值接着一段错误信息。如果用false而非nil，就更适合这个场景了，nil可保留作为缺少值的含义。</p>
<p>在算术运算时，自动将字符串转化为数字，是我们从Awk里参考的，其实可以忽略。（在字符串操作中自动将数字转换为字符串则很方便，也更少问题。）</p>
<p>除了我们的“机制，而非策略”的规则外——我们觉得在Lua演进里这个规则很有价值——我们应该为模块和包提供一个准确的指导方针。缺乏通用的构建模块和安装包的策略，限制了不同工作组共享代码，不利于发展整个社区的代码基。Lua 5.1提供了模块和包的指导方针，我们希望能够补救现存的局面。</p>
<p>正如6.4节提到的，Lua 3.0引入了条件编译的支持，主要是为了提供一种方式来关闭代码。我们收到很多请求，要求加强Lua的条件编译，这些请求甚至来自于不使用条件编译的用户！目前最大的需求是一个完整的宏处理器，就像C的预处理器一样。提供这样一个宏处理器，跟我们提供扩展机制的哲学是一致的。但是，我们希望宏也能用Lua编写，而不是其他特殊语言。我们不希望直接在词法器上添加宏设施，以免其变得臃肿不堪，拖慢编译过程。更重要的是，那个时候Lua语法分析器还不是完全可重入的，所以没有办法在词法器里调用Lua。（这个限制在Lua 5.1去掉了）所以邮件列表里和Lua开发者里有无穷无尽的讨论。我们依然希望能够为Lua提供一个宏系统：它会给Lua提供更灵活的语法，支持更灵活的语义。</p>
<h1 id="8-结论"><a href="#8-结论" class="headerlink" title="8 结论"></a>8 结论</h1><p>Lua在很多大型公司里大获成功，比如Adobe，Bombardier，Disney，Electronic Arts，Intel，LucasArts，Microsoft，Nasa，Olivetti和Philips。这些公司很多产品都直接在商用产品里嵌入Lua，并向最终用户暴露Lua脚本。</p>
<p>Lua在游戏领域特别成功。最近流行一个说法：“Lua正迅速变成游戏脚本的事实标准”【37】。两个在gamedev.net进行的非正式的投票【5，6】，分别在2003年9月和2006年6月结束，结果显示Lua正是最炙手可热的游戏开发脚本语言。GDC专门针对游戏开发中的Lua的圆桌会议举行了两次，分别是2004年和2006年。很多著名游戏都使用Lua：Baldur’s Gate, Escape from Monkey Island, FarCry, Grim Fandango, Homeworld 2, Illarion, Impossible Creatures, Psychonauts, The Sims, World of Warcraft。现在有两本使用Lua进行游戏开发的书【42，25】，而且很多其他游戏开发的书都会开辟专门的章节来讲述Lua【23，44，41，24】。</p>
<p>Lua在游戏界的广泛使用是我们的惊喜收获。我们并没有为Lua考虑过游戏开发这个目标。（Tecgraf主要考虑的是科学软件）事后看来，这种成功是可以理解的，因为所有让Lua变得与众不同的特质，对游戏开发都十分重要：</p>
<p>可移植性：很多游戏跑在非传统的平台上，比如游戏机上，这些平台需要特殊的开发工具。构建Lua只需要一个ANSI C编译器。</p>
<p>容易嵌入：游戏是性能敏感型应用。他们既需要性能，来应付图形和模拟，还需要灵活性，以应对创意的部分。很多游戏都用至少两种语言开发，这并非偶然，一种语言用于脚本编写，另一种用于构建引擎。在这个框架内，轻松集成Lua到其他语言（在游戏界主要是C++）是一个巨大的优势。</p>
<p>简洁：很多游戏设计师，脚本编写者，关卡作者都不是专业的程序员。对他们来说，一门语法简洁，语义清晰的语言尤其重要。</p>
<p>高效率，小体积：游戏是性能敏感应用程序；分配给脚本运行的事件通常都很小。Lua是最快的脚本语言之一【1】。游戏机是受限环境。脚本解释器应该节约资源。Lua核心的体积只有100K。</p>
<p>对代码的控制：不像其他大部分的软件企业，游戏产品很少演化。很多时候，一个游戏发布之后，就没有更新或者新版本了，只有新游戏。所以，冒险在游戏里使用一门新的脚本语言更容易。一门脚本语言是否会演进，如何演进，对于游戏开发者来说不是一个很关键的点。他们所需要的就只是在游戏里使用的版本。因为他们对Lua源码有完全访问的权限，他们可以选择永远保持在同一个Lua版本。</p>
<p>自由的协议：很多商业游戏是不开源的。很多游戏公司甚至拒绝使用任何开源代码。因为竞争非常激烈，所以游戏公司倾向于保守技术上的秘密。对他们来说，像Lua一样的自由协议是非常方便的。</p>
<p>协程：如果脚本语言支持多任务，编写脚本游戏会变的更简单，因为一个角色或者活动可以被暂停，并稍后恢复。Lua通过协程支持协作式多任务【14】。</p>
<p>过程式数据文件：Lua的原始设计目标是提供威力强大的数据描述设施，这允许游戏使用Lua组织数据文件，替换特殊格式的文本数据文件，带来诸多益处，尤其是同源性和表达力。</p>
<p>致谢<br>Lua是在大家的帮助下成长起来的。Tecgraf的所有人都以不同形式为Lua贡献——使用这门语言，讨论它，向Tecgraf以外传播它。特别鸣谢Marcelo Gattass，作为Tecgraf的主管，总是鼓励我们并给予我们完全的自由，去演进Lua这门语言和它的实现。Lua不再是Tecgraf的一个产品，但依然在PUC-Rio内开发，开发组属于2004年5月建成的LabLua实验室。</p>
<p>没有用户的话，Lua就只是另外一门语言，注定被遗忘。用户和他们的使用是一门语言的试金石。特别鸣谢我们邮件列表的成员，感谢他们的建议、抱怨和耐心。邮件列表相对较小，但非常友善，并有一些Lua开发团队以外的技术水平很高的人士参与，他们慷慨的向整个社区分享自己的专业知识，让人获益匪浅。</p>
<p>我们感谢Norman Ramsey的牵头，Norman Ramsey建议我们在HOPL III上发表一篇有关Lua的论文，并帮助我们联系到会议管理团队。我们感谢Julia Lawall，帮助我们通读数遍这篇文章的草稿，并代表HOPL III委员会仔细处理本文。我们感谢Norman Ramsey，Julia Lawall，Brent Hailpern， Barbara Ryder，以及未具名的编辑，感谢他们详细的评论和富有建设性的建议。</p>
<p>我们也向Andre ́ Carregal, Anna Hester, Bret Mogilefsky, Bret Victor, Daniel Collins, David Burgess, Diego Nehab, Eric Raible, Erik Hougaard, Gavin Wraith, John Belmonte, Mark Hamburg, Peter Sommerfeld, Reuben Thomas, Stephan Herrmann, Steve Dekorte, Taj Khattra和Thatcher Ulrich致谢，感谢他们对Lua发展历程的补充，并对相关文本进行润色。Katrina Avery做了很好的拷贝-编辑工作。</p>
<p>最后，我们感谢PUC-Rio，IMPA和CNPq对我们Lua工作一如既往的支持，以及FINEP和微软研究院对数个Lua相关项目的支持。</p>
<h1 id="相关文献："><a href="#相关文献：" class="headerlink" title="相关文献："></a>相关文献：</h1><ol>
<li>The computer language shootout benchmarks. <a href="http://shootout.alioth.debian.org/" rel="external nofollow noopener noreferrer" target="_blank">http://shootout.alioth.debian.org/</a>.</li>
<li>Lua projects. <a href="http://www.lua.org/uses.html" rel="external nofollow noopener noreferrer" target="_blank">http://www.lua.org/uses.html</a>.</li>
<li>The MIT license. <a href="http://www.opensource.org/licenses/mit-license.html" rel="external nofollow noopener noreferrer" target="_blank">http://www.opensource.org/licenses/mit-license.html</a>.</li>
<li>Timeline of programming languages. <a href="http://en.wikipedia.org/wiki/Timeline" rel="external nofollow noopener noreferrer" target="_blank">http://en.wikipedia.org/wiki/Timeline</a> of programming languages.</li>
<li>Which language do you use for scripting in your gameengine? <a href="http://www.gamedev.net/gdpolls/viewpoll.asp?ID=163" rel="external nofollow noopener noreferrer" target="_blank">http://www.gamedev.net/gdpolls/viewpoll.asp?ID=163</a>, Sept. 2003.</li>
<li>Which is your favorite embeddable scripting language?<a href="http://www.gamedev.net/gdpolls/viewpoll.asp?ID=788" rel="external nofollow noopener noreferrer" target="_blank">http://www.gamedev.net/gdpolls/viewpoll.asp?ID=788</a>, June 2006.</li>
<li>K. Beck. Extreme Programming Explained: EmbraceChange. Addison-Wesley, 2000.</li>
<li>G. Bell, R. Carey, and C. Marrin. The Virtual RealityModeling Language Specification—Version 2.0.<a href="http://www.vrml.org/VRML2.0/FINAL/" rel="external nofollow noopener noreferrer" target="_blank">http://www.vrml.org/VRML2.0/FINAL/</a>, Aug. 1996.(ISO/IEC CD 14772).</li>
<li>J. Bentley. Programming pearls: associative arrays. Communicationsof the ACM, 28(6):570–576, 1985.</li>
<li>J. Bentley. Programming pearls: little languages. Communicationsof the ACM, 29(8):711–721, 1986.</li>
<li>C. Bruggeman, O. Waddell, and R. K. Dybvig. Representingcontrol in the presence of one-shot continuations. InSIGPLAN Conference on Programming Language Designand Implementation, pages 99–107, 1996.</li>
<li>W. Celes, L. H. de Figueiredo, and M. Gattass. EDG: umaferramenta para criac¸ ˜ao de interfaces gr´aficas interativas.In Proceedings of SIBGRAPI ’95 (Brazilian Symposium onComputer Graphics and Image Processing), pages 241–248,1995.</li>
<li>B. Davis, A. Beatty, K. Casey, D. Gregg, and J.Waldron. Thecase for virtual register machines. In Proceedings of the 2003Workshop on Interpreters, Virtual Machines and Emulators,pages 41–49. ACM Press, 2003.</li>
<li>L. H. de Figueiredo, W. Celes, and R. Ierusalimschy.Programming advanced control mechanisms with Luacoroutines. In Game Programming Gems 6, pages 357–369.Charles River Media, 2006.</li>
<li>L. H. de Figueiredo, R. Ierusalimschy, and W. Celes. Thedesign and implementation of a language for extendingapplications. In Proceedings of XXI SEMISH (BrazilianSeminar on Software and Hardware), pages 273–284, 1994.1. </li>
<li>L. H. de Figueiredo, R. Ierusalimschy,and W. Celes. Lua:an extensible embedded language. Dr. Dobb’s Journal,21(12):26–33, Dec. 1996.</li>
<li>L. H. de Figueiredo, C. S. Souza, M. Gattass, and L. C. G.Coelho. Gerac¸ ˜ao de interfaces para captura de dados sobredesenhos. InProceedings of SIBGRAPI ’92 (BrazilianSymposium on Computer Graphics and Image Processing),pages 169–175, 1992.</li>
<li>A. de Moura, N. Rodriguez, and R. Ierusalimschy. Coroutinesin Lua. Journal of Universal Computer Science, 10(7):910–925, 2004.</li>
<li>A. L. de Moura and R. Ierusalimschy. Revisiting coroutines.MCC 15/04, PUC-Rio, 2004.</li>
<li>R. K. Dybvig. Three Implementation Models for Scheme.PhD thesis, Department of Computer Science, Universityof North Carolina at Chapel Hill, 1987. Technical Report#87-011.</li>
<li>M. Feeley and G. Lapalme. Closure generation based onviewing LAMBDA as EPSILON plus COMPILE. Journal ofComputer Languages, 17(4):251–267, 1992.</li>
<li>T. G. Gorham and R. Ierusalimschy. Um sistema dedepurac¸ ˜ao reflexivo para uma linguagem de extens˜ao.In Anais do I Simp´osio Brasileiro de Linguagens deProgramac¸ ˜ao, pages 103–114, 1996.</li>
<li>T. Gutschmidt. Game Programming with Python, Lua, andRuby. Premier Press, 2003.</li>
<li>M. Harmon. Building Lua into games. In Game Programming<br>Gems 5, pages 115–128. Charles River Media, 2005.</li>
<li>J. Heiss. Lua Scripting f¨ur Spieleprogrammierer. Hit theGround with Lua. Stefan Zerbst, Dec. 2005.</li>
<li>A. Hester, R. Borges, and R. Ierusalimschy. Building flexibleand extensible web applications with Lua. Journal ofUniversal Computer Science, 4(9):748–762, 1998.</li>
<li>R. Ierusalimschy. Programming in Lua. Lua.org, 2003.</li>
<li>R. Ierusalimschy. Programming in Lua. Lua.org, 2nd edition,2006.</li>
<li>R. Ierusalimschy, W. Celes, L. H. de Figueiredo, andR. de Souza. Lua: uma linguagem para customizac¸ ˜ao deaplicac¸ ˜oes. In VII Simp´osio Brasileiro de Engenharia deSoftware — Caderno de Ferramentas, page 55, 1993.</li>
<li>R. Ierusalimschy, L. H. de Figueiredo, and W. Celes. Lua:an extensible extension language. Software: Practice &amp;Experience, 26(6):635–652, 1996.</li>
<li>R. Ierusalimschy, L. H. de Figueiredo, and W. Celes. Theimplementation of Lua 5.0. Journal of Universal ComputerScience, 11(7):1159–1176, 2005.</li>
<li>R. Ierusalimschy, L. H. de Figueiredo, and W. Celes. Lua 5.1Reference Manual. Lua.org, 2006.1. [33] K. Jung and A. Brown. Beginning Lua Programming. Wrox,2007.</li>
<li>L. Lamport. LATEX: A Document Preparation System.Addison-Wesley, 1986.</li>
<li>M. J. Lima and R. Ierusalimschy. Continuac¸ ˜oes em Lua.In VI Simp´osio Brasileiro de Linguagens de Programac¸ ˜ao,pages 218–232,June 2002.</li>
<li>D. McDermott. An efficient environment allocation schemein an interpreter for a lexically-scoped LISP. In ACMconference on LISP and functional programming, pages 154–162, 1980.</li>
<li>I. Millington. Artificial Intelligence for Games. MorganKaufmann, 2006.</li>
<li>B. Mogilefsky. Lua in Grim Fandango. <a href="http://www.grimfandango.net/?page=articles&amp;pagenumber=2,May" rel="external nofollow noopener noreferrer" target="_blank">http://www.grimfandango.net/?page=articles&amp;pagenumber=2,May</a> 1999.</li>
<li>Open Software Foundation. OSF/Motif Programmer’s Guide.Prentice-Hall, Inc., 1991.</li>
<li>J. Ousterhout. Tcl: an embeddable command language. InProc. of the Winter 1990 USENIX Technical Conference.USENIX Association, 1990.</li>
<li>D. Sanchez-Crespo. Core Techniques and Algorithms inGame Programming. New Riders Games, 2003.</li>
<li>P. Schuytema and M. Manyen. Game Development with Lua.Delmar Thomson Learning, 2005.</li>
<li>A. van Deursen, P. Klint, and J. Visser. Domain-specificlanguages: an annotated bibliography. SIGPLAN Notices,35(6):26–36, 2000.</li>
<li>A. Varanese. Game Scripting Mastery. Premier Press, 2002.</li>
</ol>
]]></content>
      <categories>
        <category>lua</category>
        <category>lua知识点</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>lua知识点</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part7：构建完整的语法分析器(上)</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part7/</url>
    <content><![CDATA[<p><a href="http://manistein.club/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="构建Lua解释器Part7：构建完整的语法分析器-上"><a href="#构建Lua解释器Part7：构建完整的语法分析器-上" class="headerlink" title="构建Lua解释器Part7：构建完整的语法分析器(上)"></a>构建Lua解释器Part7：构建完整的语法分析器(上)</h1><h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>​    在上一章里，我完成了词法分析器的设计与实现的论述，接下来我要继续论述语法分析器的设计与实现。限于篇幅，我将会把语法分析器分为两个部分来论述，本章为上部，下一章为下部。本章将会重新复习编译器的构造，并且论述编译流程，以及lua的parser所涉及的编译相关的内容，最后通过阐述虚拟机相关指令的实现，作为结尾。本章所涉及的代码，全部在<a href="https://github.com/Manistein/dummylua-tutorial" rel="external nofollow noopener noreferrer" target="_blank">dummylua</a>工程里，欢迎star。另外，如果你喜欢我写的文章，喜欢讨论技术，欢迎加入我创建的群：QQ185017593</p>
<h1 id="编译器结构"><a href="#编译器结构" class="headerlink" title="编译器结构"></a>编译器结构</h1><p>​    早在Part5的时候，我就论述过编译器的构造了，现在重新回顾一下，按照编译器标准的划分方式，编译器可以分为前端、优化器和后端。编译器前端主要的工作，就是将源码经过语法分析，语义分析后，生成抽象语法树AST(Abstract Syntax Tree)，然后将AST本身，或者转化为另一种形式的中间表示(Intermediate Representation简称IR)，交给优化器进行优化，然后再交给编译器后端去生成机器码。<br><img src="three_phase_compiler.png" alt="image">图1<br>​    编译器的本质，就是将一种语言，翻译成另一种语言，在Part5中，我也较为详细地论述了编译器和解释器的区别，这里简单回顾一下。编译器是将源码编译成对应平台的机器码，生成可执行文件或者动态库，尔后存储在磁盘里。当我们要执行这些代码的时候，需要创建一个进程，然后加载这些目标代码才能执行。而解释器则是可以直接加载源码，然后在解释器内部编译并且立刻执行。<br>​    Lua是标准的解释器，Lua代码被加载后，可以直接被编译，且立即执行。本章要讨论的内容则是Lua解释器内部的编译流程。Lua的编译工作，主要是由Parser模块来完成。</p>
<h1 id="编译流程"><a href="#编译流程" class="headerlink" title="编译流程"></a>编译流程</h1><p>​    按照《Compilers Principles Techniques and Tools》的定义，编译流程大概要经历以下几个步骤：<br><img src="phases_of_compiler.png" alt="image">图2</p>
<ul>
<li>首先，我们输入的代码字符流，会被加载到词法分析器–lexer中，进行词法分析，从而获得token流</li>
<li>语法分析器，会从词法分析器中，不断获得token进行语法分析，判断输入代码的语法结构是否合法，如果不合法则抛错，如果合法则会生成一个抽象语法树–AST[1]</li>
<li>语义分析器，会对上一阶段的AST，进行语义分析，分析语义内容是否合法，比如code is water在语法分析阶段，语法上是合法的，但是语义上是无意义的，如果不合法则抛出错误中断编译流程，否则对AST进行改造</li>
<li>在经过语义分析阶段之后，中间代码生成器，会将AST转化为另一种中间表示(IR)，一般是三址代码(three-address code)，这种代码与平台无关，但是利于被翻译成与机器平台相关的代码</li>
<li>随后，优化器会对IR进行指令优化，提升最终的性能</li>
<li>最后会通过代码生成器Code Generator转化为目标机器码</li>
<li>目标机器码还可以经过Machine-Dependent Code Optimizer进行进一步优化</li>
</ul>
<p>实际上，图2中的词法分析器(Lexical Analyzer)、语法分析器(Syntax Analyzer)、语义分析器(Semantic Analyzer)和中间代码生成器(Intermediate Code Generator)属于编译器前端，而代码生成器(Code Generator)和平台依赖代码优化器(Machine-Dependent Code Optimizer)则属于编译器后端。<br>    实际上，编译过程中，出现错误，也不一定是要中断编译流程，有些编译器会尝试修复错误[2]，在lua中，遇到编译问题，则是终止编译流程。此外，IR也并非一定是三址代码，也可以是AST[3]。</p>
<h1 id="Lua-Parser的主要工作"><a href="#Lua-Parser的主要工作" class="headerlink" title="Lua Parser的主要工作"></a>Lua Parser的主要工作</h1><p>​    如图2所示的编译流程中，词法分析器我已经在上一章详细论述过了。由于Lua实现了完整的虚拟机，有自己的虚拟机指令，因此它的编译流程只需要将Lua代码编译成虚拟机指令即可，Lua解释器没有内置JIT，因此不会将Lua代码编译成目标机器码，所有的虚拟机指令，均是由c语言编写，本质上来说，Lua的内置编译器只是一个前端编译器，而Lua虚拟机的虚拟机指令，则可以视为是一种IR，这种IR将在虚拟机里被识别并运行。<br>​    Lua的Parser模块，则是执行了语法分析和语义分析，最后将Lua代码编译成虚拟机指令，这些虚拟机指令最后会交给虚拟机执行。</p>
<p><img src="parser.png" alt="image">图3<br>如果要将Lua的编译器和虚拟机通过一张图串联起来，那么图4则展示了这一点。<br><img src="phases_of_parser.png" alt="image">图4<br>Lua的Parser的主要工作，就是进行语法分析和语义分析，最后直接生成opcodes，opcodes将作为IR交由vm去执行。</p>
<h1 id="本章Parser要实现的部分"><a href="#本章Parser要实现的部分" class="headerlink" title="本章Parser要实现的部分"></a>本章Parser要实现的部分</h1><p>​    在继续讨论之前，我们首先要理清楚两个概念，即是statement和expression。首先我们来看看，在一门语言中什么是expression？expression是一个表达式，表达式最终能够演变成一个值，并且这个值可以被赋值到一个变量中，符合这个条件的就是expression否则就是statement[4]。而statement则是一系列操作的集合，它其他包含一系列的expression或者statement。statement是一门语言中单独的一个单元，一门语言中，有不同的statement类别，并且这些有限类别的statement组成的序列，构成一门语言的逻辑代码。<br>​    我们现在来看一个例子，如下所示：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">a</span> = <span class="number">1</span></span><br><span class="line"><span class="attr">b</span> = <span class="number">1</span>+<span class="number">2</span>*(<span class="number">3</span>-<span class="number">4</span>)</span><br><span class="line"><span class="attr">c</span> = <span class="literal">true</span></span><br><span class="line"><span class="attr">d</span> = nil</span><br></pre></td></tr></table></figure>
<p>左边所示的a、b、c、d是变量，右边的1、1+2*(3-4)、true和nil，最终能够演变成一个值，并且可以赋值给左边的变量，因此他们是表达式，而每一行的赋值语句，则是单独的statement。<br>    此外，我们还有以下几种statement，并且这些statement都不能演变成一个值，并赋值到一个变量中：</p>
<figure class="highlight fortran"><table><tr><td class="code"><pre><span class="line">// <span class="keyword">do</span> <span class="keyword">end</span> statement</span><br><span class="line"><span class="keyword">do</span> <span class="keyword">block</span> <span class="keyword">end</span> </span><br><span class="line"></span><br><span class="line">// <span class="keyword">while</span> statement</span><br><span class="line"><span class="keyword">while</span> <span class="built_in">exp</span> <span class="keyword">do</span> <span class="keyword">block</span> <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">// <span class="built_in">repeat</span> statement</span><br><span class="line"><span class="built_in">repeat</span> <span class="keyword">block</span> until <span class="built_in">exp</span></span><br><span class="line"></span><br><span class="line">// <span class="keyword">if</span> statement</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">exp</span> <span class="keyword">then</span> <span class="keyword">block</span> &#123;elseif <span class="built_in">exp</span> <span class="keyword">then</span> <span class="keyword">block</span>&#125; [<span class="keyword">else</span> <span class="keyword">block</span>] <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">// for statement</span><br><span class="line">for <span class="keyword">Name</span> `=´ <span class="built_in">exp</span> `,´ <span class="built_in">exp</span> [`,´ <span class="built_in">exp</span>] <span class="keyword">do</span> <span class="keyword">block</span> <span class="keyword">end</span></span><br><span class="line">for <span class="keyword">namelist</span> <span class="keyword">in</span> explist1 <span class="keyword">do</span> <span class="keyword">block</span> <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">// <span class="function"><span class="keyword">function</span></span> statement</span><br><span class="line"><span class="function"><span class="keyword">function</span></span> funcname funcbody</span><br><span class="line"></span><br><span class="line">// local statement</span><br><span class="line">local <span class="function"><span class="keyword">function</span></span> <span class="keyword">Name</span> funcbody</span><br><span class="line">local <span class="keyword">namelist</span> [`=´ explist1]</span><br><span class="line"></span><br><span class="line">// expr statement</span><br><span class="line">varlist1 `=´ explist1</span><br><span class="line">functioncall</span><br></pre></td></tr></table></figure>
<p>这里需要注意的是，在lua中，function是个单独的类型，因此function statement是可以直接被赋值到某个变量的，因此从某种意义上来说，它也是expression，但是传统定义上，比如我们的c语言，这些都是作为statement存在的，我们现在需要关注这个特例。此外，functioncall是在expr statement分支里调用的，但是它的实现是包含在suffixedexp的函数里的，在lua中其本质上也被归类为expression。<br>    我们现在来复习一下，构成lua语言的EBNF范式表达，从最顶层的层面来看，我们可以看到如下的结果：</p>
<figure class="highlight inform7"><table><tr><td class="code"><pre><span class="line">chunk ::= &#123;stat <span class="comment">[`;´]</span>&#125; <span class="comment">[laststat<span class="comment">[`;´]</span>]</span></span><br></pre></td></tr></table></figure>
<p>其中stat表示statement，laststat表示last statement。一个lua脚本表示为一个chunk，一段被输入解释器的代码字符串也是一个chunk，上述的范式很好地表达了，statement是构成lua代码的最基础的单元。虽然Part5中有对EBNF范式进行说明，这里我们再来复习一下它的用法：</p>
<ul>
<li>{}表示内部的元素将会重复0次或者多次</li>
<li>[]表示内部的元素会出现1次或者0次</li>
<li>()表示这是一个集合</li>
<li>|表示左右两边的元素选择其中的一个</li>
<li>::=表示左边的terminal由右边的terminal或、和nonterminal组成</li>
</ul>
<p>如果你忘记terminal和nonterminal的概念，可以回到Part5的EBNF章节复习一下。<br>    现在我们再来看一下，这个stat和laststat的EBNF表达:</p>
<figure class="highlight coq"><table><tr><td class="code"><pre><span class="line">stat ::= ifstat | <span class="type">whilestat</span> | <span class="type">dostat</span> | <span class="type">forstat</span> | <span class="type">repeatstat</span> | <span class="type">functionstat</span> | <span class="type"></span></span><br><span class="line"><span class="type">         localstat</span> | <span class="type">labelstat</span> | <span class="type">returnstat</span> | <span class="type">gotostat</span> | <span class="type">exprstat</span></span><br><span class="line">laststat ::= <span class="keyword">return</span> [explist]  |  <span class="type">break</span></span><br></pre></td></tr></table></figure>
<p>​    现在我们通过一张图来展示它的statement类别：</p>
<p><img src="statments.png" alt="image">图5<br>    我们已知，lua代码是由一系列的statement组合而成，因此在在编译过程中，就是要将脚本代码中的statement识别出来，并且进入到对应的编译分支，将其编译为虚拟机指令，由于篇幅所限，我无法在本章展示所有的编译分支逻辑，因此本章集中力量，对expr statement的设计和实现进行详细的分析。这也是本章Parser要实现的部分。<br>    现在我将对lua的exprstat的EBNF范式进行说明，为了能够和我们后面的实现逻辑相契合，本章展示的EBNF与Part5展示的略有出入，一切以本章的为主，其EBNF范式如下所示：</p>
<figure class="highlight bnf"><table><tr><td class="code"><pre><span class="line">exprstat ::= func | assignment</span><br><span class="line"></span><br><span class="line">assignment ::= suffixedexp &#123;, suffixedexp&#125; [<span class="string">'='</span> explist]</span><br><span class="line">func ::= suffixedexp</span><br><span class="line"></span><br><span class="line">suffixedexp ::= primaryexp &#123;<span class="string">'.'</span> NAME | <span class="string">'['</span> expr <span class="string">']'</span> | <span class="string">':'</span> NAME funcargs | funcargs&#125;</span><br><span class="line">primaryexp ::= NAME | <span class="string">'('</span> expr <span class="string">')'</span></span><br><span class="line"></span><br><span class="line">explist ::= expr &#123;<span class="string">','</span> expr&#125;</span><br><span class="line">expr ::= (simpleexp | unop expr) &#123;binop expr&#125;</span><br><span class="line">simpleexp ::= FLT|INT|STRING|NIL|TRUE|FALSE|...|constructor|FUNCTION body|suffixedexp</span><br><span class="line">FUNCTION body ::= <span class="string">'function'</span> <span class="string">'('</span>parlist<span class="string">')'</span> statlist <span class="string">'end'</span></span><br><span class="line">parlist ::= &#123;param [<span class="string">','</span> param]&#125;</span><br><span class="line">param ::= NAME | ...</span><br><span class="line"></span><br><span class="line">constructor ::= <span class="string">'&#123;'</span> [field &#123; sep field &#125; [sep]] <span class="string">'&#125;'</span></span><br><span class="line">field ::= listfield | recfield</span><br><span class="line">listfield ::= expr</span><br><span class="line">recfield ::= (NAME | <span class="string">'['</span> expr <span class="string">']'</span>) <span class="string">'='</span> expr</span><br><span class="line">sep ::= <span class="string">','</span>|<span class="string">';'</span></span><br><span class="line"></span><br><span class="line">funcargs ::= <span class="string">'('</span>[explist]<span class="string">')'</span> | constructor | STRING</span><br><span class="line"></span><br><span class="line">unop ::= <span class="string">'-'</span>|not|<span class="string">'#'</span>|bnot</span><br><span class="line">binop ::= <span class="string">'+'</span> | <span class="string">'-'</span> | <span class="string">'*'</span> | <span class="string">'/'</span> | <span class="string">'%'</span> | <span class="string">'^'</span> | <span class="string">'&amp;'</span> | <span class="string">'|'</span> | <span class="string">'~'</span> | <span class="string">'&lt;&lt;'</span> | <span class="string">'&gt;&gt;'</span> | <span class="string">'..'</span> | </span><br><span class="line">		  '&gt;' | '<span class="attribute">&lt;' | '=' | '&gt;</span>=' | '<span class="attribute">&lt;=' | '==' | '~=' | and | or</span></span><br></pre></td></tr></table></figure>
<p>我们已经通过EBNF范式，来描述了我们即将实现的statement的语法结构和逻辑层次，可以看到，EBNF范式的表述比较抽象且复杂，为此，我绘制了一张图，用来展现它的逻辑层次：</p>
<p><a href="EBNF.png"><img src="EBNF.png" alt="image-1"></a></p>
<p>图6<br>图文的内容展示的很小，读者可以点击图片，显示大图，如果觉得图片还是不够大，可以右键点击在新的页签查看。该图是对上述EBNF范式的归纳，之所以要画这张图，是为了向读者自顶向下地展示完整的exprstat的语法结构，展示完整的解析层次，展示各个部件的构成和联系，它也是我们展示Parse Tree生成的绝佳利器。我们后续需要根据这张图来实现exprstat的编译逻辑。本章实现的Parser部分，只会对符合exprstat语法的代码进行编译，否则将抛出错误。</p>
<h1 id="Parser所涉及的基本数据结构"><a href="#Parser所涉及的基本数据结构" class="headerlink" title="Parser所涉及的基本数据结构"></a>Parser所涉及的基本数据结构</h1><p>​    本节，我将来介绍Parser的一些基本的数据结构，但是并不会一次性将所有的数据结构都在这里展示出来。有些数据结构在具体应用到的章节再进行展示，有利于帮助读者消化和理解。在lua的语法分析器中，最终要的数据结构分别是FuncState和Proto这两个结构了，我们先来看看FuncState结构，它的定义如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">FuncState</span> &#123;</span></span><br><span class="line">	<span class="keyword">int</span> firstlocal;		<span class="comment">// 第一个local变量的位置</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">FuncState</span>* <span class="title">prev</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">LexState</span>* <span class="title">ls</span>;</span></span><br><span class="line">	Proto* p;			<span class="comment">// 主要存放虚拟机指令，常量表等</span></span><br><span class="line">	<span class="keyword">int</span> pc;				<span class="comment">// 下一个指令，应当存放在Proto结构中的code列表的位置</span></span><br><span class="line">	<span class="keyword">int</span> jpc;			<span class="comment">// 当一个statement中同时存在，and和or的逻辑语句时，</span></span><br><span class="line">						<span class="comment">// 用来标记jump指令位置的</span></span><br><span class="line">	<span class="keyword">int</span> last_target;</span><br><span class="line">	<span class="keyword">int</span> nk;				<span class="comment">// 当前常量的数量</span></span><br><span class="line">	<span class="keyword">int</span> nups;			<span class="comment">// 当前upvalue的数量</span></span><br><span class="line">	<span class="keyword">int</span> nlocvars;		<span class="comment">// 当前local变量的数量</span></span><br><span class="line">	<span class="keyword">int</span> nactvars;		<span class="comment">// 当前活跃变量的数量</span></span><br><span class="line">	<span class="keyword">int</span> np;				<span class="comment">// 被编译的代码，Proto的数量</span></span><br><span class="line">	<span class="keyword">int</span> freereg;		<span class="comment">// 下一个可被使用的，空闲寄存器的位置</span></span><br><span class="line">&#125; FuncState;</span><br></pre></td></tr></table></figure>
<p>上面对FuncState结构的一些主要的变量进行了说明，关于upvalue，后续我会单独抽出一章节来详细讨论，因此这里不会深入研究这个部分，local变量，则会在下一章进行讲解。现在我们只要FuncState结构的变量，有一个初步的概念即可，要彻底理解它们，需要在编译流程的论述之后才能做到，我们接下来看看Proto的数据结构定义：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaobject.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Proto</span> &#123;</span></span><br><span class="line">    CommonHeader;			<span class="comment">// gc部分</span></span><br><span class="line">    <span class="keyword">int</span> is_vararg;			<span class="comment">// 是否可变参数的函数</span></span><br><span class="line">    <span class="keyword">int</span> nparam;				<span class="comment">// 参数个数</span></span><br><span class="line">    Instruction* code;  	<span class="comment">// 指令列表，Instruction本质是int型</span></span><br><span class="line">    <span class="keyword">int</span> sizecode;			<span class="comment">// code列表的大小</span></span><br><span class="line">    TValue* k;				<span class="comment">// 常量表</span></span><br><span class="line">    <span class="keyword">int</span> sizek;				<span class="comment">// 常量表的大小</span></span><br><span class="line">    LocVar* locvars;		<span class="comment">// local变量列表</span></span><br><span class="line">    <span class="keyword">int</span> sizelocvar;			<span class="comment">// local变量列表的大小</span></span><br><span class="line">    Upvaldesc* upvalues;	<span class="comment">// upvalue列表</span></span><br><span class="line">    <span class="keyword">int</span> sizeupvalues;		<span class="comment">// upvalue列表的大小</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Proto</span>** <span class="title">p</span>;</span>		<span class="comment">// Proto结构列表</span></span><br><span class="line">    <span class="keyword">int</span> sizep;				<span class="comment">// Proto结构列表的大小</span></span><br><span class="line">    TString* source;		<span class="comment">// 源代码文件</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gclist</span>;</span><span class="comment">// gc列表</span></span><br><span class="line">	<span class="keyword">int</span> maxstacksize;		<span class="comment">// 经过编译后，需要栈的最大大小</span></span><br><span class="line">&#125; Proto;</span><br></pre></td></tr></table></figure>
<p>早在Part5的时候，我就有讲解过，一个lua脚本内部的代码本质上就是一个chunk，在chunk在编译后，本质上也是一个function对象，一般一个function对象对应一个Proto实例，在编译过程中，一个function则对应一个FuncState结构。FuncState结构实例，只在编译期存在。我将在本章后续的内容里，阐述编译流程，让读者能够真正理解。</p>
<h1 id="本章所涉及的虚拟机指令"><a href="#本章所涉及的虚拟机指令" class="headerlink" title="本章所涉及的虚拟机指令"></a>本章所涉及的虚拟机指令</h1><p>​    本节，将对本章涉及到的虚拟机指令进行说明，在开始阅读指令说明之前，如果你忘记了lua虚拟机指令的相关概念，那么我强烈建议读者复习一下Part5的【Lua的指令集编码方式】和【Lua5.3中的指令集】两个小节，本章涉及的指令如下所示：</p>
<ul>
<li>OP_MOVE R(A) := R(B)<br>将寄存器B中的内容，移动到寄存器A中</li>
<li>OP_GETUPVAL R(A) := UpVal[B]<br>将upvalue列表中索引为B的值，拷贝到寄存器A中</li>
<li>OP_GETTABUP R(A) := UpVal[B][RK( C )]<br>先在UpVal列表中，找到索引值为B的upvalue，它必须是一个table，然后从常量表K(当C值&gt;=256)，中取出以K-256为索引的值，或者从寄存器C中的值，存放到寄存器A中</li>
<li>OP_GETTABLE R(A) = R[B][RK( C )]<br>寄存器B中的值，必须是一个table，然后从常量表K(当C值&gt;=256)，中取出以K-256为索引的值，或者从寄存器C中的值，存放到寄存器A中</li>
<li>OP_SELF R(A+1) = R(B); R(A) = R(B)[RK( C )]<br>寄存器B的变量必须是个table，然后从常量表K(当C值&gt;=256)，中取出以K-256为索引的值，或者从寄存器C中的值，存放到寄存器A中。最后将寄存器B中的table变量赋值给R(A+1)</li>
<li>OP_TEST if not (R(A) &lt;=&gt; C) then pc++<br>如果寄存器A的值，与C值相等，那么PC寄存器自增1(跳过下一个指令)，通常TEST指令后面必定要跟一个JUMP指令</li>
<li>OP_TESTSET if (R(B) &lt;=&gt; C) then R(A) := R(B) else pc++<br>如果寄存器B的值，与C值相等，那么将寄存器B的值，赋值给寄存器A，否则PC寄存器自增1(跳过下一个指令)，通常TESTSET指令后面必定要跟一个JUMP指令</li>
<li>OP_JUMP pc+=sBx; if (A) close all upvalues &gt;= R(A - 1)<br>pc寄存器加上常量sBx的值</li>
<li>OP_UNM R(A) := -R(B)</li>
<li>OP_LEN R(A) := length of R(B)</li>
<li>OP_BNOT R(A) := ~R(B)</li>
<li>OP_NOT R(A) := not R(B)</li>
<li>OP_ADD R(A) := RK(B) + RK( C )</li>
<li>OP_SUB R(A) := RK(B) - RK( C )</li>
<li>OP_MUL R(A) := RK(B) * RK( C )</li>
<li>OP_DIV R(A) := RK(B) / RK( C )</li>
<li>OP_IDIV R(A) := RK(B) // RK( C )</li>
<li>OP_MOD R(A) := RK(B) % RK( C )</li>
<li>OP_POW R(A) := RK(B) ^ RK( C )</li>
<li>OP_BAND R(A) := RK(B) &amp; RK( C )</li>
<li>OP_BOR R(A) := RK(B) | RK( C )</li>
<li>OP_BXOR R(A) := RK(B) ~ RK( C )</li>
<li>OP_SHL R(A) := RK(B) &lt;&lt; RK( C )</li>
<li>OP_SHR R(A) := RK(B) &gt;&gt; RK( C )</li>
<li>OP_CONCAT R(A) := R(B).. … ..R( C )</li>
<li>OP_EQ if ((RK(B) == RK( C )) ~= A) then pc++</li>
<li>OP_LT if ((RK(B) &lt; RK( C )) ~= A) then pc++</li>
<li>OP_LE if ((RK(B) &lt;= RK( C )) ~= A) then pc++</li>
<li>OP_LOADBOOL R(A) := (Bool)B; if( C ) PC++<br>将常量值B，加载到寄存器A中，如果C值不为0，那么PC寄存器自增1(跳过下一条指令)</li>
<li>OP_LOADNIL R(A) := … := R(B) := nil<br>从R(A)到R(A+B)的值，全部赋值为NIL</li>
<li>OP_SETUPVAL UpValue[B] := R(A)<br>将寄存器A的值，赋值给UpValue列表中，的UpValue[B]值</li>
<li>OP_SETTABUP UpValue[A][RK(B)] := RK( C )<br>UpValue[A]的值必须是table，将RK( C )的值赋值给该table的RK(B)域</li>
<li>OP_NEWTABLE R(A) := {} (size = B,C)<br>创建一个新的table，并且赋值给寄存器A中</li>
<li>OP_SETLIST R(A)[(C-1)<em>FPF+i] := R(A+i), 1 &lt;= i &lt;= B<br>寄存器A的值是个table，i的值表示从1自增到B，将R(A + i)的值，赋值到R[A][(C-1)</em>FPF+i]中，其中C值由编译器指定(C&gt;=1)，FPF默认为50</li>
<li>OP_SETTABLE R(A)[RK(B)] = RK( C )</li>
</ul>
<p>上面展示了本章涉及的虚拟机指令，有些指令光看注释就能明白它的作用，因此不作更多的文字赘述。</p>
<h1 id="编译逻辑与EBNF的关联"><a href="#编译逻辑与EBNF的关联" class="headerlink" title="编译逻辑与EBNF的关联"></a>编译逻辑与EBNF的关联</h1><p>​    现在我将介绍如何通过EBNF进行派生演变(Derivation)，然后说明Derivation的问题，接着为了解决这些问题会引入Parse Tree，后面还会简要介绍一下Abstract Syntax Tree。再往后，就会根据EBNF范式来实现exprstat的逻辑，并结合一些实例进行讲解。<br>​    我们先以foo函数的调用为例子，看看派生演变的操作是怎样的，首先，很显然，在对foo函数这段代码进行编译时，我们很自然地进入到了exprstat的分支之中，那么它用EBNF范式的完整的演变流程如下所示：</p>
<figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">1 </span>   exprstat</span><br><span class="line"><span class="symbol">2 </span>   func</span><br><span class="line"><span class="symbol">3 </span>   suffixedexp</span><br><span class="line"><span class="symbol">4 </span>   primaryexp <span class="comment">'(' ')'</span></span><br><span class="line"><span class="symbol">5 </span>   TK_NAME <span class="comment">'(' ')'</span></span><br><span class="line"><span class="symbol">6 </span>   foo<span class="comment">'('')'</span></span><br></pre></td></tr></table></figure>
<p>非常简单的编译流程，这种方式，我们称之为Derivation，但是Derivation有个问题，就是演变的顺序是随意的，比如下面一段代码，我们可以有两种演变方式：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">a</span> = <span class="number">1</span></span><br></pre></td></tr></table></figure>
<p>演变方式1：</p>
<figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">1 </span>   exprstat</span><br><span class="line"><span class="symbol">2 </span>   suffixedexp=explist</span><br><span class="line"><span class="symbol">3 </span>   primaryexp=explist</span><br><span class="line"><span class="symbol">4 </span>   TK_NAME=explist</span><br><span class="line"><span class="symbol">5 </span>   a=explist</span><br><span class="line"><span class="symbol">6 </span>   a=expr</span><br><span class="line"><span class="symbol">7 </span>   a=simpleexp</span><br><span class="line"><span class="symbol">8 </span>   a=TK_INT</span><br><span class="line"><span class="symbol">9 </span>   a=<span class="number">1</span></span><br></pre></td></tr></table></figure>
<p>演变方式2：</p>
<figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">1 </span>   exprstat</span><br><span class="line"><span class="symbol">2 </span>   suffixedexp=explist</span><br><span class="line"><span class="symbol">3 </span>   suffixedexp=expr</span><br><span class="line"><span class="symbol">4 </span>   suffixedexp=simpleexp</span><br><span class="line"><span class="symbol">5 </span>   suffixedexp=TK_INT</span><br><span class="line"><span class="symbol">6 </span>   suffixedexp=<span class="number">1</span></span><br><span class="line"><span class="symbol">7 </span>   primaryexp=<span class="number">1</span></span><br><span class="line"><span class="symbol">8 </span>   TK_NAME=<span class="number">1</span></span><br><span class="line"><span class="symbol">9 </span>   a=<span class="number">1</span></span><br></pre></td></tr></table></figure>
<p>我们已经展示了两种演变方式，但实际上，演变的方式还有很多种，这里不一一展示，这里也说明了，EBNF范式可以描述一门语言的语法，但是无法精确描述编译解析的先后顺序，为了解决这个问题，我们将引入Parse Tree的概念。什么是Parse Tree呢？其实，就是将我们的lua代码，代入到图6所示的EBNF范式中，然后生成一颗树。有了Parse Tree，我们只要指定它的遍历顺序，那么一切就都清晰了，lua编译器使用的是递归下降的方式来编译lua脚本，因此这里使用先序遍历更加合适（如果你忘记了树的几种遍历方式，可以查看Tree Traversals这篇文章[5]）,我们现在来看一下上面所示的例子的Parse Tree是怎样的，图7展示了这一点，红色数值表示遍历的顺序。<br><img src="parser_example4.png" alt="image">图7<br>通过Parse Tree，我们既能解构代码的语法结构，也能确定其编译过程中的执行顺序，我们前面也介绍过，一般的多趟编译器(multi pass compiler)，编译器前端要生成中间表示（IR），然后经过优化器优化，再交给编译器后端去生成对应平台的机器码。一般意义来说，Parse Tree虽然也可以作为IR使用，但是它包含太多不必要的信息，也不利于我们去生成三址代码形式的IR(three-address code，该种形式相比于树形结构更容易转换成机器指令)，因此另一种形式的树，更易于我们去生成three-address code，它就是抽象语法树–Abstract Syntax Tree，简称AST[6]。AST在维基百科中的解释是：</p>
<blockquote>
<p>In computer science, an abstract syntax tree (AST), or just syntax tree, is a tree representation of the abstract syntactic structure of source code written in a programming language. Each node of the tree denotes a construct occurring in the source code.</p>
</blockquote>
<p>简单的来说AST是源码的抽象树形结构，它的每个节点都是源码中的某个构造。与Parse Tree不同，Parse Tree会事无巨细地将源码的语法结构，完整地展露出来，一个标点符号都不会放过，而AST则不同，它会省略掉一些无关紧要的信息，比如一些符号’(‘、’)‘、’,‘、’;‘、’{‘、’}‘等等，它只关注执行什么指令，以及什么指令先执行，什么指令后执行。比如上面例子的AST则如图8所示：</p>
<p><img src="ast.png" alt="image">图8<br>上图的含义是执行赋值操作，将1赋值给a，如果我们的AST执行后序遍历的方式，那么它的逻辑执行顺序就非常清晰了，比如a=1+2*3，得到如下的结果：</p>
<p><img src="ast_postorder.png" alt="image">图9</p>
<p>​    lua的编译器是典型的单趟编译(one pass compile)，没有显示地生成任何形式的IR，包括Parse Tree或者是AST，它所有的编译逻辑都是在Parser里执行，直接生成虚拟机指令，所以本系列，不会对AST着色过的的笔墨，后续将会结合一些实例，适当结合Parse Tree进行讲解。</p>
<h1 id="dummylua的项目目录结构"><a href="#dummylua的项目目录结构" class="headerlink" title="dummylua的项目目录结构"></a>dummylua的项目目录结构</h1><p>​    本节我们来回顾一下<a href="https://github.com/Manistein/dummylua-tutorial" rel="external nofollow noopener noreferrer" target="_blank">dummylua</a>的目录结构，并对不同的模块进行说明：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">+ <span class="number">3</span>rd/              #引用的第三方库均放置在这里</span><br><span class="line">+ bin/              #编译生成的二进制文件放置在这里</span><br><span class="line">~ clib/             #外部要在c层使用Lua的C API，那么只能调用clib里提供的接口，而不能调用其他内部接口</span><br><span class="line">  luaaux<span class="selector-class">.h</span>              #供外部使用的辅助库</span><br><span class="line">  luaaux.c</span><br><span class="line">~ common/           #vm和compiler共同使用的结构、接口均放置在这里</span><br><span class="line">  lua<span class="selector-class">.h</span>                 #提供lua基本类型的定义，错误码定义，全项目都可能用到的宏均会放置在这里</span><br><span class="line">  luamem<span class="selector-class">.h</span>              #lua内存分配器</span><br><span class="line">  luamem.c</span><br><span class="line">  luaobject<span class="selector-class">.h</span>           #lua基本类型</span><br><span class="line">  luaobject.c</span><br><span class="line">  luastate<span class="selector-class">.h</span>            #虚拟机状态结构，以及对其相关操作的接口均放置于此</span><br><span class="line">  luastate.c</span><br><span class="line">  luabase<span class="selector-class">.h</span>             #lua标准库，提供最基础的函数</span><br><span class="line">  luabase.c</span><br><span class="line">  luainit<span class="selector-class">.c</span>         	  #第三方库加载机制的函数所在地</span><br><span class="line">  luastring<span class="selector-class">.h</span> 			    #lua字符串</span><br><span class="line">  luastring.c</span><br><span class="line">  luatable<span class="selector-class">.h</span> 			      <span class="selector-id">#lua</span> table</span><br><span class="line">  luatable.c</span><br><span class="line">~ compiler/         #编译器相关的部分放置在这里</span><br><span class="line">  luacode<span class="selector-class">.h</span> 			      #编译成虚拟机指令的库</span><br><span class="line">  luacode.c</span><br><span class="line">  lualexer<span class="selector-class">.h</span>            #词法分析器</span><br><span class="line">  lualexer.c</span><br><span class="line">  luaparser<span class="selector-class">.h</span> 			    #语法分析器</span><br><span class="line">  luaparser.c</span><br><span class="line">  luazio<span class="selector-class">.h</span>    			    #协助词法分析器，分析文本的库</span><br><span class="line">  luazio.c</span><br><span class="line">+ scripts/          #lua脚本测试用例</span><br><span class="line">+ test/             #测试用例全部放置在这里</span><br><span class="line">~ vm/               #虚拟机相关的部分放置在这里</span><br><span class="line">  luado<span class="selector-class">.h</span>               #函数调用相关的接口均放置于此</span><br><span class="line">  luado.c</span><br><span class="line">  luafunc<span class="selector-class">.h</span>             #lua虚拟机函数体</span><br><span class="line">  luafunc.c</span><br><span class="line">  luagc<span class="selector-class">.h</span>               #gc机制</span><br><span class="line">  luagc.c</span><br><span class="line">  luaopcodes<span class="selector-class">.h</span>          #OPCODE定义</span><br><span class="line">  luaopcodes.c</span><br><span class="line">main.c</span><br><span class="line">makefile</span><br></pre></td></tr></table></figure>
<p>我们编译器相关的部分，都放在compiler目录之下，其中lualexer.h|c是词法分析器相关，luaparser.h|c是语法分析器相关（编译流程处理），luacode.h|c则是生成虚拟机指令的库，而luaopcodes.h|c则是opcode定义的地方。</p>
<h1 id="exprstat编译流程"><a href="#exprstat编译流程" class="headerlink" title="exprstat编译流程"></a>exprstat编译流程</h1><p>​    现在，我们进入到exprstat编译流程的探讨之中了，实际上，这部分并不好写，我写了几版，都觉得不够清晰或者细节太多而删除重写，经过反复思索，确定了通过EBNF范式描述语法，然后代码只展示结构和主要流程，一些操作细节通过图的方式展现，这样有助于我们快速理解知识点和需要注意的事项，最后还是强烈建议读者结合代码一起阅读，这样才能真正做到理解。</p>
<h2 id="exprstat的构造"><a href="#exprstat的构造" class="headerlink" title="exprstat的构造"></a>exprstat的构造</h2><p>​    我们前面也介绍过，statement是构成代码逻辑，最基础的单独单元，lua中同样有多种statement，比如ifstat、whilestat、forstat等等，每个statement执行不同的逻辑，并且在编译器中有独立的处理逻辑分支，而每个statement中，头一个token决定了我们应该进入哪个处理分支之中。我们现在以官方lua源码的statement逻辑来举例：</p>
<figure class="highlight mel"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lua-5.3.5 lparser.c</span></span><br><span class="line">static void statement (LexState *<span class="keyword">ls</span>) &#123;</span><br><span class="line">  <span class="keyword">int</span> line = <span class="keyword">ls</span>-&gt;linenumber;  <span class="comment">/* may be needed for error messages */</span></span><br><span class="line">  enterlevel(<span class="keyword">ls</span>);</span><br><span class="line">  <span class="keyword">switch</span> (<span class="keyword">ls</span>-&gt;t.token) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">';'</span>: &#123;  <span class="comment">/* stat -&gt; ';' (empty statement) */</span></span><br><span class="line">      luaX_next(<span class="keyword">ls</span>);  <span class="comment">/* skip ';' */</span></span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_IF: &#123;  <span class="comment">/* stat -&gt; ifstat */</span></span><br><span class="line">      ifstat(<span class="keyword">ls</span>, line);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_WHILE: &#123;  <span class="comment">/* stat -&gt; whilestat */</span></span><br><span class="line">      whilestat(<span class="keyword">ls</span>, line);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_DO: &#123;  <span class="comment">/* stat -&gt; DO block END */</span></span><br><span class="line">      luaX_next(<span class="keyword">ls</span>);  <span class="comment">/* skip DO */</span></span><br><span class="line">      block(<span class="keyword">ls</span>);</span><br><span class="line">      check_match(<span class="keyword">ls</span>, TK_END, TK_DO, line);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_FOR: &#123;  <span class="comment">/* stat -&gt; forstat */</span></span><br><span class="line">      forstat(<span class="keyword">ls</span>, line);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_REPEAT: &#123;  <span class="comment">/* stat -&gt; repeatstat */</span></span><br><span class="line">      repeatstat(<span class="keyword">ls</span>, line);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_FUNCTION: &#123;  <span class="comment">/* stat -&gt; funcstat */</span></span><br><span class="line">      funcstat(<span class="keyword">ls</span>, line);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_LOCAL: &#123;  <span class="comment">/* stat -&gt; localstat */</span></span><br><span class="line">      luaX_next(<span class="keyword">ls</span>);  <span class="comment">/* skip LOCAL */</span></span><br><span class="line">      <span class="keyword">if</span> (testnext(<span class="keyword">ls</span>, TK_FUNCTION))  <span class="comment">/* local function? */</span></span><br><span class="line">        localfunc(<span class="keyword">ls</span>);</span><br><span class="line">      <span class="keyword">else</span></span><br><span class="line">        localstat(<span class="keyword">ls</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_DBCOLON: &#123;  <span class="comment">/* stat -&gt; label */</span></span><br><span class="line">      luaX_next(<span class="keyword">ls</span>);  <span class="comment">/* skip double colon */</span></span><br><span class="line">      labelstat(<span class="keyword">ls</span>, str_checkname(<span class="keyword">ls</span>), line);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_RETURN: &#123;  <span class="comment">/* stat -&gt; retstat */</span></span><br><span class="line">      luaX_next(<span class="keyword">ls</span>);  <span class="comment">/* skip RETURN */</span></span><br><span class="line">      retstat(<span class="keyword">ls</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_BREAK:   <span class="comment">/* stat -&gt; breakstat */</span></span><br><span class="line">    <span class="keyword">case</span> TK_GOTO: &#123;  <span class="comment">/* stat -&gt; 'goto' NAME */</span></span><br><span class="line">      gotostat(<span class="keyword">ls</span>, luaK_jump(<span class="keyword">ls</span>-&gt;fs));</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">default</span>: &#123;  <span class="comment">/* stat -&gt; func | assignment */</span></span><br><span class="line">      exprstat(<span class="keyword">ls</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  lua_assert(<span class="keyword">ls</span>-&gt;fs-&gt;f-&gt;maxstacksize &gt;= <span class="keyword">ls</span>-&gt;fs-&gt;freereg &amp;&amp;</span><br><span class="line">             <span class="keyword">ls</span>-&gt;fs-&gt;freereg &gt;= <span class="keyword">ls</span>-&gt;fs-&gt;nactvar);</span><br><span class="line">  <span class="keyword">ls</span>-&gt;fs-&gt;freereg = <span class="keyword">ls</span>-&gt;fs-&gt;nactvar;  <span class="comment">/* free registers */</span></span><br><span class="line">  leavelevel(<span class="keyword">ls</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代码片段1<br>在代码片段1中，ls-&gt;t.token就是我们获得的，在一个statement语句中的首个token，我们可以很容易地看到，首个token决定了我们要进入哪个case中，执行哪个编译流程。在就<a href="https://github.com/Manistein/dummylua-tutorial" rel="external nofollow noopener noreferrer" target="_blank">dummylua</a>目前的完成情况来说，当前只实现了exprstat部分的逻辑，下面节取自dummylua的代码：</p>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">statement</span>(<span class="params"><span class="keyword">struct</span> lua_State* L, LexState* ls, FuncState* fs</span>)</span> &#123;</span><br><span class="line">	<span class="keyword">switch</span> (ls-&gt;t.token) &#123;</span><br><span class="line">	<span class="keyword">case</span> TK_NAME: &#123;</span><br><span class="line">		exprstat(L, ls, fs);</span><br><span class="line">	&#125; <span class="keyword">break</span>;</span><br><span class="line">	<span class="keyword">default</span>:</span><br><span class="line">		luaX_syntaxerror(L, ls, <span class="string">"unsupport syntax"</span>);</span><br><span class="line">		<span class="keyword">break</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代码片段2<br>可以看到，如果输入的chunk(一个lua文件里的所有代码，或者在交互模式下的一行代码称之为chunk[7])，不符合exprstat的语法结构，那么将抛出语法错误。我们现在通过一张图来看一下什么样的chunk，可以进入到exprstat之中：<br><img src="exprstat-example1.png" alt="image">图10<br>可以看到，上面两个chunk对应了两个statement(我们前面提到过funccall由于在suffixedexp内定义，因此是个exp，而在有些说法里，expr也是statement，在这里的情况，chunk1本质就是一个statement[8])，而这些statement中，的首个token是TK_NAME类型的，因此直接进入到exprstat的编译流程之中，图10也展示了exprstat的两种形态，一种是funccall，还有一种则是赋值。<br>    说到这里，就要引入exprstat的EBNF描述了，它描述了exprstat的语法结构，并且EBNF范式与递归下降的逻辑实现，能够很好地对应起来。</p>
<figure class="highlight bnf"><table><tr><td class="code"><pre><span class="line">exprstat ::= funccall | assignment</span><br><span class="line"></span><br><span class="line">assignment ::= suffixedexp &#123;, suffixedexp&#125; [<span class="string">'='</span> explist]</span><br><span class="line">funccall ::= suffixedexp</span><br></pre></td></tr></table></figure>
<p>这里清晰地展示了，exprstat主要的两种形态，我们exprstat的代码，则如下所示：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line"> <span class="number">574</span> static void exprstat(struct lua_State* L, LexState* ls, FuncState* fs) &#123;</span><br><span class="line"> <span class="number">575</span>     LH_assign lh;</span><br><span class="line"> <span class="number">576</span>     suffixedexp(L, ls, fs, &amp;lh.v);</span><br><span class="line"> <span class="number">577</span> </span><br><span class="line"> <span class="number">578</span>     if (ls-&gt;t.token == '=' || ls-&gt;t.token == ',') &#123;</span><br><span class="line"> <span class="number">579</span>         assignment(fs, &amp;lh, <span class="number">1</span>);</span><br><span class="line"> <span class="number">580</span>     &#125;</span><br><span class="line"> <span class="number">581</span>     else &#123;</span><br><span class="line"> <span class="number">582</span>         check_condition(ls, lh.v.k == VCALL, <span class="string">"exp type error"</span>);</span><br><span class="line"> <span class="number">583</span>     &#125;</span><br><span class="line"> <span class="number">584</span> &#125;</span><br></pre></td></tr></table></figure>
<p>代码片段3<br>上面的代码，与前面exprstat的EBNF范式基本吻合，这里希望读者仔细观察一下。但是上面的方式有点让人疑惑，就是赋值语句中，等号左边的必然是变量，而不能是函数调用，上面的EBNF范式清晰地暗示了，suffixedexp既能是一个变量，也能是一个函数调用。这似乎并不符合我们的直观直觉，实际上下面的定义，似乎更直观：</p>
<figure class="highlight bnf"><table><tr><td class="code"><pre><span class="line">exprstat ::= funccall | assignment</span><br><span class="line">nameexp  ::= primaryexp &#123; <span class="string">'.'</span> TK_NAME | <span class="string">'['</span> expr <span class="string">']'</span> &#125;</span><br><span class="line">funccall ::= nameexp ( <span class="string">':'</span> TK_NAME funcargs | funcargs )</span><br><span class="line">assignment ::= nameexp &#123; <span class="string">','</span> nameexp &#125; <span class="string">'='</span> explist</span><br></pre></td></tr></table></figure>
<p>相对应的，它的伪代码如下所示，更符合我们的直觉：</p>
<figure class="highlight vbscript"><table><tr><td class="code"><pre><span class="line">void exprstat() &#123;</span><br><span class="line">	<span class="keyword">if</span> <span class="built_in">exp</span> <span class="keyword">is</span> funccall <span class="keyword">then</span> </span><br><span class="line">		funccall</span><br><span class="line">	<span class="keyword">else</span> <span class="keyword">if</span> <span class="built_in">exp</span> <span class="keyword">is</span> assignment <span class="keyword">then</span></span><br><span class="line">		assignment</span><br><span class="line">	<span class="keyword">else</span> </span><br><span class="line">		syntax <span class="keyword">error</span></span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代码片段4<br>多么nice的逻辑。但是，很快我们就要遇到一个现实的问题，那就是，我怎么知道当前的这个statement是funccall还是assignment？如图10所示，我们进入exprstat逻辑分支的时候，当前的token是一个TK_NAME类型的，同时，也正如图10所示，不管是funccall还是assignment，他们的首个token都是TK_NAME类型，而刚进入exprstat这段逻辑时，我们只有statement中首个token的信息，单靠这个信息我们无法判定这个是funccall还是assignment。因此lua采取的方式就是，不管怎样，先把第一个exp识别出来，如果它是个变量，那么就必须当做赋值语句处理，如果是个funccall，那么处理直接结束。回到代码片段3的逻辑，exprstat的逻辑是，不管三七二十一，先把expr识别出来再说，而做这件事情的则如576行代码所示，通过suffixedexp函数来识别。但是在开始讨论suffixedexp的编译流程之前，我们首先要看看expr的处理。</p>
<h4 id="expr的构造与编译"><a href="#expr的构造与编译" class="headerlink" title="expr的构造与编译"></a>expr的构造与编译</h4><p>1、simpleexp的识别与处理<br>    expr是expression的缩写，意为表达式，在lua的语法分析器中，它的作用就是将表达式识别出来，并将对应的信息存储到一个中间的结构之中。为什么要先从它开始讨论呢？因为它是组成statement的基础，我们先来看一下它的EBNF范式：</p>
<figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line">expr ::= (simpleexp | unop expr) <span class="comment">&#123;binop expr&#125;</span></span><br><span class="line">simpleexp ::= FLT|INT|STRING|<span class="keyword">NIL</span>|<span class="keyword">TRUE</span>|<span class="keyword">FALSE</span>|...|<span class="function"><span class="keyword">constructor</span>|<span class="title">FUNCTION</span> <span class="title">body</span>|<span class="title">suffixedexp</span></span></span><br></pre></td></tr></table></figure>
<p>前面我们也介绍过，expr最终的归宿就是计算成一个确切的值，这个确切的值是什么意思呢？就是expr最后可以演变成simpleexp中，除了…和suffixedexp以外的任意一种类型的值，这些值实际上就是lua的基本类型的值。从上面的EBNF范式来看，它以simpleexp或者一个单目运算的exp为开头，后面可能跟一堆双目运算操作，但不论如何，最终的结果都是演变为一个lua基本类型的值。expr的逻辑如下所示：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line"> <span class="number">349</span> static const struct &#123;</span><br><span class="line"> <span class="number">350</span>     lu_byte left;   <span class="comment">// left priority for each binary operator</span></span><br><span class="line"> <span class="number">351</span>     lu_byte right;  <span class="comment">// right priority</span></span><br><span class="line"> <span class="number">352</span> &#125; priority[] = &#123;</span><br><span class="line"> <span class="number">353</span>     &#123;<span class="number">10</span>,<span class="number">10</span>&#125;, &#123;<span class="number">10</span>,<span class="number">10</span>&#125;,          <span class="comment">// '+' and '-'</span></span><br><span class="line"> <span class="number">354</span>     &#123;<span class="number">11</span>,<span class="number">11</span>&#125;, &#123;<span class="number">11</span>,<span class="number">11</span>&#125;, &#123;<span class="number">11</span>,<span class="number">11</span>&#125;, &#123;<span class="number">11</span>, <span class="number">11</span>&#125;, <span class="comment">// '*', '/', '//' and '%'</span></span><br><span class="line"> <span class="number">355</span>     &#123;<span class="number">14</span>,<span class="number">13</span>&#125;,                   <span class="comment">// '^' right associative</span></span><br><span class="line"> <span class="number">356</span>     &#123;<span class="number">6</span>,<span class="number">6</span>&#125;, &#123;<span class="number">4</span>,<span class="number">4</span>&#125;, &#123;<span class="number">5</span>,<span class="number">5</span>&#125;,       <span class="comment">// '&amp;', '|' and '~'</span></span><br><span class="line"> <span class="number">357</span>     &#123;<span class="number">7</span>,<span class="number">7</span>&#125;, &#123;<span class="number">7</span>,<span class="number">7</span>&#125;,              <span class="comment">// '&lt;&lt;' and '&gt;&gt;'</span></span><br><span class="line"> <span class="number">358</span>     &#123;<span class="number">9</span>,<span class="number">8</span>&#125;,                     <span class="comment">// '..' right associative</span></span><br><span class="line"> <span class="number">359</span>     &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, <span class="comment">// '&gt;', '&lt;', '&gt;=', '&lt;=', '==', '~=',</span></span><br><span class="line"> <span class="number">360</span>     &#123;<span class="number">2</span>,<span class="number">2</span>&#125;, &#123;<span class="number">1</span>,<span class="number">1</span>&#125;,              <span class="comment">// 'and' and 'or'</span></span><br><span class="line"> <span class="number">361</span> &#125;;</span><br><span class="line"> <span class="number">362</span> </span><br><span class="line"> <span class="number">363</span> static int subexpr(FuncState* fs, expdesc* e, int limit) &#123;</span><br><span class="line"> <span class="number">364</span>     LexState* ls = fs-&gt;ls;</span><br><span class="line"> <span class="number">365</span>     int unopr = getunopr(ls);</span><br><span class="line"> <span class="number">366</span> </span><br><span class="line"> <span class="number">367</span>     if (unopr != NOUNOPR) &#123;</span><br><span class="line"> <span class="number">368</span>         luaX_next(fs-&gt;ls-&gt;L, fs-&gt;ls);</span><br><span class="line"> <span class="number">369</span>         subexpr(fs, e, UNOPR_PRIORITY);</span><br><span class="line"> <span class="number">370</span>         luaK_prefix(fs, unopr, e);</span><br><span class="line"> <span class="number">371</span>     &#125;</span><br><span class="line"> <span class="number">372</span>     else simpleexp(fs, e);</span><br><span class="line"> <span class="number">373</span> </span><br><span class="line"> <span class="number">374</span>     int binopr = getbinopr(ls);</span><br><span class="line"> <span class="number">375</span>     while (binopr != NOBINOPR &amp;&amp; priority[binopr].left &gt; limit) &#123;</span><br><span class="line"> <span class="number">376</span>         expdesc e2;</span><br><span class="line"> <span class="number">377</span>         init_exp(&amp;e2, VVOID, <span class="number">0</span>);</span><br><span class="line"> <span class="number">378</span> </span><br><span class="line"> <span class="number">379</span>         luaX_next(ls-&gt;L, ls);</span><br><span class="line"> <span class="number">380</span>         luaK_infix(fs, binopr, e);</span><br><span class="line"> <span class="number">381</span>         int nextop = subexpr(fs, &amp;e2, priority[binopr].right);</span><br><span class="line"> <span class="number">382</span>         luaK_posfix(fs, binopr, e, &amp;e2);</span><br><span class="line"> <span class="number">383</span> </span><br><span class="line"> <span class="number">384</span>         binopr = nextop;</span><br><span class="line"> <span class="number">385</span>     &#125;</span><br><span class="line"> <span class="number">386</span> </span><br><span class="line"> <span class="number">387</span>     return binopr;</span><br><span class="line"> <span class="number">388</span> &#125;</span><br><span class="line"> <span class="number">389</span> </span><br><span class="line"> <span class="number">390</span> static void expr(FuncState* fs, expdesc* e) &#123;</span><br><span class="line"> <span class="number">391</span>     subexpr(fs, e, <span class="number">0</span>);</span><br><span class="line"> <span class="number">392</span> &#125;</span><br></pre></td></tr></table></figure>
<p>代码片段5<br>    我们先从最简单的情况开始讨论，先从一张图来大致看一下expr到底做了什么事情：<br><img src="expr-example1.png" alt="image">图11<br>如图11所示，expr函数，会将我们输入的token，转化为一个被称之为expdesc结构变量的信息之中，在完成转化之后，我们获取下一个token。实际上expr可能会处理多个token，一般在单目运算或者是双目运算的expr里，目前我们只来看最简单的情况–只对单个token进行处理的情况。<br>    实际上，并非所有的token都能够被expr函数转化为expdesc的信息的，只有那些是exp的token才行，这些token都被定义在了EBNF范式中的simpleexp里，他们是TK_FLT、TK_INT、TK_STRING、TK_NIL、TK_TRUE、TK_FALSE、TK_FUNCTION、’{}‘、TK_NAME等，除了TK_NAME，其他都能在lua中找到对应的基本类型（因为TK_NAME表示变量，可以存储任意一种基本类型的值）。这些是输入信息的部分，现在我们来看一下经过expr转化，得到的数据结构是怎样的，以下是它的定义：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.h</span></span><br><span class="line"><span class="comment">// 表达式(exp)的类型</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">enum</span> expkind &#123;</span><br><span class="line">    VVOID,          <span class="comment">// 表达式是空的，也就是void</span></span><br><span class="line">    VNIL,           <span class="comment">// 表达式是nil类型</span></span><br><span class="line">    VFLT,           <span class="comment">// 表达式是浮点类型</span></span><br><span class="line">    VINT,           <span class="comment">// 表达式是整型类型</span></span><br><span class="line">    VTRUE,          <span class="comment">// 表达式是TRUE</span></span><br><span class="line">    VFALSE,         <span class="comment">// 表达式是FALSE</span></span><br><span class="line">    VINDEXED,       <span class="comment">// 表示索引类型，当exp是这种类型时，expdesc的ind域被使用</span></span><br><span class="line">    VCALL,          <span class="comment">// 表达式是函数调用，expdesc中的info字段，表示的是instruction pc</span></span><br><span class="line">                    <span class="comment">// 也就是它指向Proto code列表的哪个指令</span></span><br><span class="line">    VLOCAL,         <span class="comment">// 表达式是local变量，expdesc的info字段，表示该local变量，在栈中的位置</span></span><br><span class="line">    VUPVAL,         <span class="comment">// 表达式是Upvalue，expdesc的info字段，表示Upvalue数组的索引</span></span><br><span class="line">    VK,             <span class="comment">// 表达式是常量类型，expdesc的info字段表示，这个常量是常量表k中的哪个值</span></span><br><span class="line">    VRELOCATE,      <span class="comment">// 表达式可以把结果放到任意的寄存器上，expdesc的info表示的是instruction pc</span></span><br><span class="line">    VNONRELOC,      <span class="comment">// 表达式已经在某个寄存器上了，expdesc的info字段，表示该寄存器的位置</span></span><br><span class="line">&#125; expkind;</span><br><span class="line"></span><br><span class="line"><span class="comment">// exp临时存储结构</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">expdesc</span> &#123;</span></span><br><span class="line">    expkind k;              <span class="comment">// expkind</span></span><br><span class="line">    <span class="keyword">union</span> &#123;</span><br><span class="line">        <span class="keyword">int</span> info;           </span><br><span class="line">        lua_Integer i;      <span class="comment">// for VINT</span></span><br><span class="line">        lua_Number r;       <span class="comment">// for VFLT</span></span><br><span class="line"></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">            <span class="keyword">int</span> t;      <span class="comment">// 表示table或者是UpVal的索引</span></span><br><span class="line">            <span class="keyword">int</span> vt;     <span class="comment">// 标识上一个字段't'是upvalue(VUPVAL) 还是 table(VLOCAL)</span></span><br><span class="line">            <span class="keyword">int</span> idx;    <span class="comment">// 常量表k或者是寄存器的索引，这个索引指向的值就是被取出值得key</span></span><br><span class="line">                        <span class="comment">// 不论t是Upvalue还是table的索引，它取出的值一般是一个table</span></span><br><span class="line">        &#125; ind;</span><br><span class="line">    &#125; u;</span><br><span class="line">&#125; expdesc;</span><br></pre></td></tr></table></figure>
<p>我们现在通过一个表格，来看一下这些exp是如何转化成expdesc结构变量的：</p>
<table>
<thead>
<tr>
<th style="text-align:left">simpleexp</th>
<th style="text-align:left">expdesc</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">TK_FLT</td>
<td style="text-align:left">e-&gt;k=VFLT e-&gt;u.r = float_value</td>
</tr>
<tr>
<td style="text-align:left">TK_INT</td>
<td style="text-align:left">e-&gt;k=VINT e-&gt;u.i = int_value</td>
</tr>
<tr>
<td style="text-align:left">TK_STRING</td>
<td style="text-align:left">e-&gt;k=VK e-&gt;u.info = index_in_k（常量表k的索引值）</td>
</tr>
<tr>
<td style="text-align:left">TK_NIL</td>
<td style="text-align:left">e-&gt;k=VNIL</td>
</tr>
<tr>
<td style="text-align:left">TK_TRUE</td>
<td style="text-align:left">e-&gt;k=VTRUE</td>
</tr>
<tr>
<td style="text-align:left">TK_FALSE</td>
<td style="text-align:left">e-&gt;k=VFALSE</td>
</tr>
</tbody>
</table>
<p>这里需要注意的是，常量表k中不止可以存字符串，还可以存数值类型的值，要通过expdesc表示数值类型，同样可以用类似字符串的方式去表示。那在什么情况下，数值类型要通过VK的方式展现呢？答案是生成指令的时候，比如生成OP_LOADK指令，这个指令要从常量表k中，load一个值到栈顶，我们首先要将数值存入常量表，然后再去索引它，用expdesc e来表示，就是e-&gt;k = VK, e-&gt;u.info = index_of_number_in_k，后面会详细讨论。对于TK_FUNCTION的情况，我将在下一章进行说明，而对于constructor的情况(也就是’{}‘)，expr会怎么处理呢？我们结合图12来进行说明：<br><img src="expr-example2.png" alt="image">图12<br>当我们的expr函数，检测到当前的token为’{‘时，会生成一个指令&lt; OP_NEWTABLE 0 0 0 &gt;，这里的含义是，在寄存器R(0)中创建一个table，同时它的array_size和hash_size都为0。此时，expdesc的值如下所示：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">e</span>-&gt;</span>k = VNONRELOC</span><br><span class="line"><span class="function"><span class="title">e</span>-&gt;</span>u.info = <span class="number">0</span></span><br></pre></td></tr></table></figure>
<p>类型VNONRELOC表示，它的值在lua stack中，e-&gt;u.info则说明了它的值在栈中所在的位置。在图12的例子中，它在R(0)。我们知道，OP_NEWTABLE指令是iABC模式（指令的模式可以到luaopcodes.c里查找），在生成指令后，e-&gt;u.info的值，就是A域的值，因为指令操作的目标在R(A)，所以e所代表的值，就是刚刚被处理过的R(A)值，在本例中(它就是指明刚生成的table在栈中的位置)，不止OP_NEWTABLE是这样，其他指令也是这样。由于花括号内没有其他定义，因此expr会直接跳过’}‘，调用luaX_next获取下一个token。<br>    接下来，要分析的则是expr函数如何处理类型为TK_NAME的token，TK_NAME其实是由primaryexp函数来处理的，而primaryexp又是suffixedexp的组成部分，但是suffixedexp又是simpleexp的组成部分，所以TK_NAME我们可以视为在expr内进行处理。现在我们来看一下这里的处理流程是怎样的。首先在开始讨论，expr如何处理TK_NAME之前，我们需要明确几个概念，首先是我们的代码中，函数与upvalue、FuncState结构的关系，现在我们通过图13的例子来进行分析：<br><img src="expr-example3.png" alt="image">图13<br>如上图所示，我们现在有一个test.lua的脚本，里面定义了两个local变量和一个函数，那么这个test.lua本身里面所有的代码就是一个chunk，这个chunk实际上也是一个function类型，我们称之为top-level function。test.lua脚本里，定义了一个foo函数，我们在编译的过程中，chunk因为也是一个function，因此会有一个FuncState结构和它对应，这个结构主要用来存储编译结果的，另外foo函数，因为也是一个函数，因此它也有一个FuncState结构和它对应，并且它的prev指针，指向了chunk的FuncState结构。每个函数都有一个upvalue列表，其中第一个upvalue必定是_ENV，它默认指向全局表，如果我们没有对_ENV进行任何赋值操作，那么它就是全局表的象征。每个函数在运行时，都有独立的虚拟栈，local变量会被放入栈中。这里的图，只是我用来方便讲解的图，实际上的布局并非真的完全如此。<br>    图13帮助我们梳理了很多概念，我们现在可以根据图13来做一些逻辑分析了，同样是expr函数，当我们输入不同的token的时候，它会生成什么样的expdesc变量呢？现在来罗列一下这些在编译过程中的情况：</p>
<ul>
<li>先到栈上找local变量，看看是否在local列表中，如果是则返回，此时e-&gt;k=VLOCAL，e-&gt;u.info为栈上的索引值</li>
<li>如果上一步找不到，则到自己函数的upvalue列表里去查找，如果找到，然后e-&gt;k=VUPVAL，e-&gt;u.info为upvalue列表里的索引值</li>
<li>如果上一步找不到，则到外层函数里查找它的local列表，如果找到，则将其添加到自己的upvalue列表中，如果找不到，则到外层函数的upvalue列表里查找，如果在那里找到，先添加到自己的upvalue列表中，再将e-&gt;k=VUPVAL，e-&gt;u.info为upvalue列表里的索引值。如果都没找到，则需要再到更外一层函数里查找，执行相似的逻辑，找不到就一直到外层函数找，直至到top-level function为止</li>
<li>如果还是找不到，就到_ENV表里查找，也就是upvalue[0]，也就是全局表_G里查找，此时e的值为e-&gt;k=VINDEXED，e-&gt;u.ind.vt=VUPVAL，e-&gt;u.ind.t=0，e-&gt;u.ind.idx=idx_of_name</li>
</ul>
<p>结合图13，我们通过几个例子来看一下这个流程。<br>    首先我们来看一下，编译foo函数时的情况。如果当前token为&lt; TK_NAME, c &gt;的情况，此时执行expr的逻辑，它首先会查找foo函数的local列表，它在第0个位置找到了变量c，因此生成expdesc结构，e-&gt;k=VLOCAL，e-&gt;u.info=0<br>    然后我们来看一下，当前token为&lt; TK_NAME, b &gt;的情况，此时执行expr逻辑，因为在foo的local列表中找不到变量b，所以到外层函数中查找，并且在外层函数的local列表中，找到变量b，此时foo函数将其添加到自己的upvalue列表中，并且生成expdesc结构，此时e-&gt;k=VUPVAL，e-&gt;u.info=1<br>    最后我们来看一下，当前token为&lt; TK_NAME, print &gt;时，expr的编译执行情况，首先到foo函数体的local列表中查找print，因为没有找到，所以到自己的upvalue中查找，发现也没找到。此时到外层函数的local列表中查找，因为没有找到，所以去外层函数的upvalue列表里查找，也没找到，最后只能去_ENV里查找。编译器会先将print这个字符串，加入到foo函数对应的Proto结构的常量表k中，然后将e-&gt;k=VINDEXED，e-&gt;u.ind.vt=VUPVAL，e-&gt;u.ind.t=0，e-&gt;u.ind.idx=258(假设print在常量表第3个位置)。这个expdesc的含义是，因为e-&gt;k为VINDEXED，所以这是一个从一个table取值的处理，而且e-&gt;u.ind.vt为VUPVAL，所以这个被操作的表，要去upvalue表去查找，又因为e-&gt;u.ind.t为0，所以操作目标是UpValue[0]，最后idx为258，则是去常量表里找到访问table的key值。<br>    我们在获得了expdesc结构以后，最终是要将这些信息转化为指令的，那么这个转化的过程是怎样的呢？我们现在来看一下：</p>
<ul>
<li>当expdesc类型变量e-&gt;k为VNIL时，生成&lt; OP_LOADNIL A B &gt;指令，该指令会被写入code指令列表中，其中A为lua栈中，这个nil值要被写入的位置，B的值表示从A指向的位置开始，要将多少个栈上的变量变为NIL值，同时将e-&gt;k设置为VNONRELOC，将e-&gt;u.info设置为A值</li>
<li>当expdesc类型变量e-&gt;k为VFLT、VINT时，先将e包含的lua_Number或lua_Integer值存入常量表k，然后生成&lt; OP_LOADK A Bx &gt;，该指令会被写入code列表，其中A为lua栈中，这个常量要被写入的寄存器位置，Bx是刚被写入常量表k的数值的k表索引</li>
<li>当expdesc类型变量e-&gt;k为VTRUE、VFALSE时，直接生成&lt; OP_LOADBOOL A, 1, 0 &gt;或&lt; OP_LOADBOOL, A, 0, 0 &gt;表示分别将1或0的值，读入lua栈A值所指的位置，第三个参数为0表示pc寄存器不自增，这个值将在比较运算指令生成时用到。此时e-&gt;k设置为VNONRELOC，并且e-&gt;u.info设置为A的值</li>
<li>当expdesc类型变量e-&gt;k为VINDEXED时，如果e-&gt;u.ind.vt为VLOCAL则生成&lt; OP_GETTABLE A, B, C &gt;指令，意思是将R(B)[RK( C )]的值，赋值到R(A)上，RK我们之前也讨论过，当C&gt;=256时，RK( C )=Kst(C-256)，即从常量表k中取C-256这个值。当C&lt;256时，取R( C )的值，即从寄存器中取值；如果e-&gt;u.ind.vt为VUPVAL时，则生成&lt; OP_GETTABUP A, B, C &gt;指令，意思是将UpValue[B][RK( C )]的值，赋值到R(A)上。此时e-&gt;k设置为VNONRELOC，并且e-&gt;u.info设置为A的值</li>
<li>当expdesc类型变量e-&gt;k为VCALL时，直接生成&lt; OP_CALL A, B, C &gt;指令，其中A表示要被调用的函数的寄存器位置。当B&gt;0时，参数个数为B-1个，当B=0时，表示从R(A+1)到栈顶都是参数。当C&gt;0时，C-1表示返回值的个数，当C=0时，表示返回值是从R(A)到栈顶</li>
<li>当expdesc类型变量e-&gt;k为VLOCAL时，表示目标已经在栈中，因此直接将e-&gt;k改为VNONRELOC</li>
<li>当expdesc类型变量e-&gt;k为VUPVAL时，生成指令&lt; OP_GETUPVAL A, B &gt;，这个表示R(A)=UpValue[B]，此时e-&gt;k设置为VNONRELOC，并且e-&gt;u.info设置为A的值</li>
<li>当expdesc类型变量e-&gt;k为VK时，生成指令&lt; OP_LOADK A, Bx &gt;表示R(A)=Kst(Bx)，此时e-&gt;k设置为VNONRELOC，并且e-&gt;u.info设置为A的值</li>
<li>当expdesc类型变量e-&gt;k为VRELOCATE时，这个表示指令已经生成，只是寄存器A未指定，此时需要将一个空闲的寄存器位置赋值给A域即可，一般由freereg指定</li>
<li>当expdesc类型变量e-&gt;k为VNONRELOC时，生成指令&lt; OP_MOVE A, B &gt;即是，R(A)=R(B)，要生成这个指令，A不能等于B</li>
</ul>
<p>等下我会用一些例子来展示这个转化的流程，这个转换的功能一般是使用luaK_exp2nextreg函数来实现。</p>
<p>2、单目运算操作<br>    回顾一下代码片段5，我们的expr函数，最终是调用了subexpr函数，其最后一个参数是指定当前运算符的优先级的，在subexpr函数内部，处理完一个exp以后，如果紧随其后的操作符优先级高于exp前面的，那么后面的操作要优先处理。接下来，我们来讨论一下单目运算的情况。什么是单目运算呢？就是exp前面有-、#、~和not操作的时候。我们现在分情况来讨论。<br>    第一种情况就是expr前面有’-‘的情况。我们接下来看一下几个例子：</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"><span class="deletion">-100</span></span><br><span class="line"><span class="deletion">-0.5</span></span><br><span class="line"><span class="deletion">-value</span></span><br></pre></td></tr></table></figure>
<p>’-‘操作符之后，应该跟数值类型，否则这就是一个无效的exp，应当抛出Syntax Error。不过这里需要注意的是，token ’-‘后面，如果跟TK_NAME类型的token，那么在编译阶段，是不会检查该token是否是数值类型，它只会到虚拟机运行阶段的时候去检查。我们先来看一下，token ‘-‘后面紧跟的是一个数值token的情况，我们可以结合图14的情况来看，以下逻辑流程均发生在expr函数内:<br><img src="expr-example4.png" alt="image">图14<br>现在假设有如图14的表达式要经过expr函数处理，首先expr函数，会先检查当前的token，是否是’-‘，如果是，将操作符’-‘存到一个变量中，并且进入到单目运算处理流程，然后调用luaX_next函数，获取下一个token，此时得到图15的情况：<br><img src="expr-example4-2.png" alt="image">图15<br>图15的结果，就是代码片段5中，第365行代码执行的结果，在完成操作符识别以后，要识别和处理紧随操作符的token &lt; TK_INT, 100 &gt;，此时subexpr函数递归调用了自己，目的是识别和处理这个token，并将其转化为一个expdesc结构。得到图16的结果：<br><img src="expr-example4-3.png" alt="image">图16<br>我们可以观察到，token指针又前进了一步，每个simpleexp处理完以后，就会将当前token的值，设置为下一个token的。再次回顾代码片段5，此时第370行要对unopr和expdesc变量，进行整合，于是得到图17的效果：<br><img src="expr-example4-4.png" alt="image">图17<br>我们可以观察到，实际上，这里是对常量进行处理，面对数值常量，’-‘运算符操作，是将结果直接算好，并存入expdesc结构的，读者可以将任何其他数值类型的值带入，也是这样的操作流程。不过当token ‘-‘后面紧跟的token是TK_NAME类型的话，会怎么处理呢？首先，和刚刚讨论过的例子一样，expr函数首先要识别的是unopr是否存在，如果存在，那么识别单目操作符后面的那个exp，我们前面在讨论simpleexp的时候也讨论过，对于expr函数处理TK_NAME类型的token，最后生成的expdesc结构有几种形态，我们将其整理成表格如下所示：</p>
<table>
<thead>
<tr>
<th style="text-align:left">e-&gt;k</th>
<th style="text-align:left">使用的字段</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">VLOCAL</td>
<td style="text-align:left">e-&gt;u.info</td>
<td style="text-align:left">表示值在栈中，R(e-&gt;u.info)</td>
</tr>
<tr>
<td style="text-align:left">VUPVAL</td>
<td style="text-align:left">e-&gt;u.info</td>
<td style="text-align:left">表示值在upvalues中，UpValue[e-&gt;u.info]</td>
</tr>
<tr>
<td style="text-align:left">VINDEXED</td>
<td style="text-align:left">e-&gt;u.ind.vt = VUPVAL e-&gt;u.ind.t e-&gt;u.ind.idx</td>
<td style="text-align:left">表示值在UpValue[e-&gt;u.ind.t][RK(e-&gt;u.ind.idx)]</td>
</tr>
</tbody>
</table>
<p>上面这个表格中，使用字段那一列，没有赋值运算的，代表值是可以变动的，有赋值运算的则代表值一定是固定的。我们现在来看一下-a的例子，假设现在已经完成了操作符和操作符后面的exp识别，得到图18的情况：<br><img src="expr-example5.png" alt="image">图18<br>此时要将unopr和expdesc结构整合，那么就要生成OP_UNM指令了，在生成这个指令之前，首先要将如图18里，expdesc结构的，转化为指令，转换的流程，前面也已经讨论过，这里就是直接生成OP_GETTABUP指令，生成结果如图19所示：<br><img src="expr-example6.png" alt="image">图19<br>我们可以看到，生成OP_GETTABUP的指令过程中，将指令的A域赋值为freereg原来指向的值，尔后freereg自增1，然后expdesc改变对应的值，读者可以仔细查看图19。现在我们要生成一个新的指令OP_UNM，这个指令的意思是，将目标对象的值取负。我们此时要生成的指令为，其中A的值为上一条指令OP_GETTABUP的A域，而B的值则由图19中的expdesc来指定，于是有：<br><img src="expr-example7.png" alt="image">图20<br>我们可以看到，当expdesc的类型为VNONRELOC时，e-&gt;u.info指向刚刚生成那条指令的操作目标。现在我们完成了UNM操作的论述了，后面我们将继续讨论其他单目运算操作。<br>    我们现在要论述的第二种情况，则是expr前面是’#‘的情况，一般来说，这个指令是用来计算字符串或者table的长度的，这些对象，首先要先被加载到栈中，也就是说，首先要生成创建指定对象、或者将指定对象加载到栈中的指令，此时我们也会得到一个类型为VNONRELOC的expdesc结构，然后根据这个expdesc结构，生成指令，其中A的值由freereg指定，而B的值则是e-&gt;u.info（也就是上一条指令的操作目标）。其实整个流程和前面讨论过的流程类似，读者可以自行将这个流程分析一次。<br>    接下来要论述的第三种情况，则是expr前面是’~‘的情况，这种操作符，其实和’-‘几乎一样，只是操作的对象应当是int型。读者可以结合前面的例子再次分析一次，只是这里要生成的指令是OP_BNOT，而不是OP_UNM。<br>    我们要讨论的第四种情况是，expr前面是’not’的情况。在lua中，除了nil和false是表示false以外，其他的都表示为true。第四种情况的表现形式如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">not</span> <span class="built_in">exp</span></span><br></pre></td></tr></table></figure>
<p>我在前面已经详细讨论过了，exp如何通过expr函数转化为expdesc的流程，现在要看的，则是如何将not操作和这个expdesc整合起来。我们则通过图21来展示这个流程：<br><img src="expr-example8.png" alt="image">图21<br>上图展示了expdesc为常量时，与not操作符整合的结果，其实就是修正原来的expdesc结构为VFALSE或者为VTRUE。对于not后面的exp为变量的情况，处理流程和’-‘一样，只是最终生成的指令从OP_UNM到OP_NOT。<br>    在我们的虚拟机指令之中（主要是iABC模式的指令），B域和C域的值要两种情况来看待，一种是小于256的情况，还有一种则是大于等于256的情况。为什么要这样处理呢？实际上这是lua对指令的一种优化，我们先复习一下lua虚拟机的指令编码格式：</p>
<figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line"><span class="code">+--------+</span>---------<span class="code">+---------+</span>--------<span class="code">+--------+</span></span><br><span class="line">|  iABC  |    B:9  |   C:9   |   A:8  |Opcode:6|</span><br><span class="line"><span class="code">+--------+</span>---------<span class="code">+---------+</span>--------<span class="code">+--------+</span></span><br><span class="line">|  iABx  |   Bx:18(unsigned) |   A:8  |Opcode:6|</span><br><span class="line"><span class="code">+--------+</span>---------<span class="code">+---------+</span>--------<span class="code">+--------+</span></span><br><span class="line">|  iAsBx |   sBx:18(signed)  |   A:8  |Opcode:6|</span><br><span class="line"><span class="code">+--------+</span>---------<span class="code">+---------+</span>--------<span class="code">+--------+</span></span><br></pre></td></tr></table></figure>
<p>当B域或者C域的值大于等于256的时候，说明他要去常量表k中取参数，即Kst(B-256)或者Kst(C-256)。如果小于256，则需要去lua栈上取参数。在一个函数调用的过程中，一个函数的lua stack的最大尺寸是255，它是由指令的A域决定的，因为它只有8个bit位。我们一个指令要从栈中访问数据，也是受这个条件限制，因为要取的数据，必然是前面执行的指令对栈进行操作的结果。如果没有大于或等于256，就去常量表找参数的规则，那么我们首先就得用OP_LOADK指令，将其先读入栈中，这样本来一条指令就能完成的事情，就可能需要两条甚至是三条。接下来还有一个问题，就是，是不是所有在常量表K中的常量，都能够被非OP_LOADK的指令直接获取呢？答案是不是，因为B域和C域都是9个bit为，最大值为512，因此，它能够直接从常量表k中找参数的范围就是0~255(一共512-256=256个)。</p>
<p>3、双目运算操作<br>    现在我们进入到双目运算的讨论之中了，这里，我会把它分为几个部分：算术运算、逻辑运算和比较运算，最后会在此基础上讨论一下递归的情况。<br>    我们先来看一下算术运算的部分，这里主要分为三种情况看待，分别是：两个常量操作、一个常量与一个变量操作和两个变量的操作。现在先来看一下两个常量相加的例子：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>+<span class="number">1</span></span><br></pre></td></tr></table></figure>
<p>我们进入到expr函数以后，就会开始处理上面这个exp了，先来回顾一下expr函数的定义：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">expr</span><span class="params">(FuncState* fs, expdesc* e)</span></span>;</span><br></pre></td></tr></table></figure>
<p>首先最左边的1，会通过simpleexp函数来识别，获得图22的结果，此时调用luaX_next函数，获得下一个token：<br><img src="expr-example9.png" alt="image">图22<br>此时，当前token为操作符’+‘，所有的双目运算操作，在这个阶段，都要进入“中序”处理，在expr函数中，是通过调用luaK_infix函数来实现的，在算术运算中，首先判断操作符左边的exp，是否是数值类型，如果是则什么都不做，如果不是，则生成将exp入栈的指令。在本例中，由于1是数值类型，因此不作任何处理。这里需要注意的是，目前的expdesc结构变量e，就上上面expr函数定义里的参数e。此时，我们需要调用luaX_next函数，获得下一个token，而这个新的token，需要新的expdesc结构来存储，我们姑且称之为e2，经过处理得到如图23所示的结果：<br><img src="expr-example9-2.png" alt="image">图23<br>在完成了两个exp的装载之后，就要执行运算处理了，在expr函数中，这个处理我们称之为后序处理，就是要将e和e2进行运算处理，这里相当于将e和e2两个参数先入栈，再压入运算符。总之这个处理是在luaK_posfix函数内实现的。在本例中，因为e和e2都是常量，因此直接计算两个常量的结果，并将结果存入e中，这个e其实就是expr函数处理的结果，一直遵循我们前面讨论过的规则：</p>
<figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line">tokens =&gt; expr <span class="keyword">function</span> =&gt; expdesc</span><br></pre></td></tr></table></figure>
<p>此时，我们得到图24的结果：<br><img src="expr-example9-3.png" alt="image">图24<br>我们现在接下来看一下第二种情况，一个常量与一个变量相加的情况：</p>
<figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>+<span class="keyword">a</span></span><br></pre></td></tr></table></figure>
<p>我们先将操作符左边的exp转化成expdesc，得到图25的结果：<br><img src="expr-example9-4.png" alt="image">图25<br>此时我们获得操作符，token ‘+‘，然后此时进入到“中序处理”流程之中，因为1这个exp是个数值类型，因此不需要做任何处理，此时获取下一个token &lt; TK_NAME, a &gt;，根据前面已经讨论过的内容，我们的变量a直接转化为expdesc结构，最终的结果如图26所示：<br><img src="expr-example9-5.png" alt="image">图26<br>接着我们要用luaK_posfix函数来处理e和e2了，因为他们不全是常量，因此两个参数都要先生成入栈指令，如图27所示：<br><img src="expr-example9-6.png" alt="image">图27<br>最后，需要将e和e2进行双目运算操作，生成相关的指令，如图28所示，这里要注意e和freereg的变化：<br><img src="expr-example9-7.png" alt="image">图28<br>OP_ADD操作指令的操作结果，会存入寄存器R(0)，所有的参数入栈，最终的结果都是为了生成一个值，freereg则被复位到R(1)的位置，这里需要注意的是，freereg永远指向刚刚生成的那条指令，A域所对的R(A)寄存器的下一个寄存器。<br>    现在我们来看一下，算术运算的最后一种讨论情况，两个变量操作的情况，我们现在来看一下下面一个例子：</p>
<figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line"><span class="keyword">a</span> + b</span><br></pre></td></tr></table></figure>
<p>首先，我们进入到expr函数以后，首先会通过simpleexp函数，来处理变量a，得到图29的效果：<br><img src="expr-example9-8.png" alt="image">图29<br>此时，我们遇到操作符，执行luaK_infix操作，因为e不是数值类型，因此要生成入栈指令，得到图30的结果：<br><img src="expr-example9-9.png" alt="image">图30<br>此时，进入到操作符右边的token的识别流程之中了，识别之后，直接装载到新的expdesc结构中，得到图31的结果：<br><img src="expr-example9-10.png" alt="image">图31<br>两个参数都被识别和装载之后，就进入到了luaK_posfix的执行流程了，此时会生成将第二个参数都入栈的指令，图32展示了这个结果：<br><img src="expr-example9-11.png" alt="image">图32<br>最后，生成对两个参数进行运算的指令，这里是OP_ADD指令，得到图33的结果：<br><img src="expr-example9-12.png" alt="image">图33<br>我们可以用任何一个双目算术运算符，代替上面几个例子中的加法运算符，操作流程都是一致的。而拼接字符串的CONCAT操作，可以结合第三种情况去分析，这个留给读者自己去推导流程。</p>
<p>4、逻辑运算<br>    我们的逻辑运算，本质是and和or运算符执行逻辑运算，我们接下来通过几个例子来看。首先是一个and和or只有变量参与的例子，然后我们再参入常量，最后让and和or结合。我们首先来看第一个例子：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line">a <span class="keyword">and </span><span class="keyword">b </span><span class="keyword">and </span>c</span><br></pre></td></tr></table></figure>
<p>进入到expr函数以后，我们首先对token &lt; TK_NAME, a &gt;进行处理，处理它的是simpleexp函数，对应代码片段5的第372行。执行完以后，我们将该token的信息，装载到expdesc中，得到图34的结果：<br><img src="expr-example10.png" alt="image">图34<br>在完成了第一个exp的识别之后，调用luaX_next函数获得下一个token，我们获得操作符，token TK_AND，此时要进入“中序处理流程”，也就是代码片段5中的第380行代码luaK_infix函数的执行。luaK_infix函数在这种情况下会做什么呢？其运作流程如下所示：</p>
<ul>
<li>将当前expdesc结构内的信息，转化为指令，一般是生成参数入栈的指令</li>
<li>生成OP_TESTSET指令</li>
<li>生成OP_JUMP指令</li>
</ul>
<p>结合图34的情况，现在将expdesc结构转化为指令，就得到了图35的情况：<br><img src="expr-example10-2.png" alt="image">图35<br>完成入栈指令生成之后，现在要生成的OP_TESTSET指令和OP_JUMP指令，得到图36的效果：<br><img src="expr-example10-3.png" alt="image">图36<br>图36的信息量很大，首先我们生成了OP_TESTSET指令，目前的A域并不是最终值，在最后一个指令被处理完以后，它会被重置。这个指令的作用是判断R(B)和C是否相等，如果相等，则令R(A)=R(B)，否则PC++。PC++的含义是，跳过下一个指令，在图36的例子中，如果R(B)的值（也就是a的值）不与C值0相等，那么将R(A)赋值为a，此时下一条指令为OP_JMP。如果R(A)的值和C不相等，那么下一条指令，则是OP_JMP的下一条指令，也就是跳过了OP_JMP指令。含义就是如果a的值为false，后面的判断都不用做了，因为一定是false，我们直接把a所代表的false值记录下来就行了。第二个要注意的点是，expdesc结构的f值，指向了我们刚生成的OP_JMP指令，记录了这个位置，并且OP_JMP的参数为-1，这个值后续还要进行处理。<br>    在完成了第一个and的处理之后，编译器通过luaX_next函数，获取下一个token &lt; TK_NAME, b &gt;，此时，将变量b装载到新的expdesc结构e2中，于是得到图37的结果：<br><img src="expr-example10-4.png" alt="image">图37<br>此时进入到了第二个and的处理流程，和前面的处理流程一样，首先e2的信息要转化为指令，然后生成OP_TESTSET和OP_JMP指令，得到图38的结果：<br><img src="expr-example10-5.png" alt="image">图38<br>对b的处理，和对a是基本相似的，但是有一点需要注意的是，第一个OP_JMP指令，的-1值变为了2，我们前面已经知道了，e-&gt;f指向了第一个OP_JMP指令，这个f其实是一个false列表的意思，意思就是and逻辑运算中，有一个参数为false，那么就进入这个jump流程之中。在图38中，我们可以看到第一个JMP指令，的sBx的值为2，那么它的下一个JMP指令则是sBx+1。在完成这个处理之后，需要将e和e2合并，其实就是将e2中，除了f以外的值赋值给e。在完成了第二个and的处理流程之后，我们调用luaX_next函数，获得下一个token &lt; TK_NAME, c &gt;，得到图39的结果：<br><img src="expr-example10-6.png" alt="image">图39<br>此时经过luaK_posfix函数，将e和e2合并，得到图40的结果：<br><img src="expr-example10-7.png" alt="image">图40<br>其实到目前为止，我们的expr函数的处理就结束了，就是输入一些token，经过expr函数处理，输出一个expdesc结构。但是为了本次讨论的完整性，我需要最后将expdesc结构转化为指令，实际上这一步可能是在assignment的赋值操作开始之前，或者在ifstat、forstat、whilestat等statement中的condition部分结束分析时进行处理的。那么，接下来，我们首先要将e中的信息，转化为指令得到图41的结果：<br><img src="expr-example10-8.png" alt="image">图41<br>在完成这些处理之后，我们要根据e-&gt;f取得false列表，从第一个OP_JMP指令开始，修改它的参数，使其跳转到指令列表的第8个位置的那个指令。同时将OP_TESTSET指令的A域，设置为freereg-1的值，得到图42的结果：<br><img src="expr-example10-9.png" alt="image">图42<br>由于我们的sBx是18个bit位，而OP_JMP指令是iAsBx模式，jump指令可以往前跳转，因此sBx是允许负数存在的，又因为sBx在低位，无法通过最高位表示负数。sBx的最大值是262143，它向右移一位是131071，因此这个值则表示0值，小于它的都是负数，比如131700表示-1，这里的131705表示pc+4，又因为pc寄存器本身会自增，于是就相当于跳转到箭头所指的位置了。这里还需要注意的是，官方lua是会在这个阶段对OP_TESTSET指令进行优化的，将其优化为OP_TEST指令，由于dummylua只注重实现正确逻辑，性能还不是最优先考虑的因素，为了方便讲解和实现，dummylua并未对该指令进行任何优化。<br>    我们现在拿上面的例子，来模拟执行一下，感受一下这一系列指令的运作流程，我将从三个例子切入，他们分别如下所示：</p>
<figure class="highlight nix"><table><tr><td class="code"><pre><span class="line">a <span class="literal">and</span> b <span class="literal">and</span> c</span><br><span class="line"></span><br><span class="line">例<span class="number">1</span>: <span class="attr">a</span> = <span class="literal">false</span>, <span class="attr">b</span> = <span class="literal">true</span>, <span class="attr">c</span> = <span class="literal">true</span></span><br><span class="line">例<span class="number">2</span>: <span class="attr">a</span> = <span class="literal">true</span>, <span class="attr">b</span> = <span class="literal">false</span>, <span class="attr">c</span> = <span class="literal">true</span></span><br><span class="line">例<span class="number">3</span>: <span class="attr">a</span> = <span class="literal">true</span>, <span class="attr">b</span> = <span class="literal">true</span>, <span class="attr">c</span> = <span class="literal">false</span></span><br></pre></td></tr></table></figure>
<p>我们先看例1的情况，它的执行流程是怎样的呢？首先第一步，需要将变量a通过OP_GETTABUP指令，将变量a的值读入栈中，如图43所示：<br><img src="expr-example10-10.png" alt="image">图43<br>在将a值入栈之后，我们现在则需要对它进行测试，测试它的指令就是OP_TESTSET指令，此时，会将a的值和指令C域进行比较，比较代码是c语言，因此0代表false，非0代表true，这里因为a的值为false，因此重新将false的值设置到寄存器R(0)上，因为是a的值是false，所以这里会直接执行下一条指令，OP_JMP，最后跳过所有的TEST，如图44所示：<br><img src="expr-example10-11.png" alt="image">图44<br>图中的曲线，就是测试通过后，直接跳过所有的测试指令，因为and逻辑中，只要有一个false，立马终止测试，跳过所有的测试指令。我们接下来看一下例2，它的运作流程如图45所示：<br><img src="expr-example10-12.png" alt="image">图45<br>图45一口气把例2的逻辑执行了一遍，其流程总结如下所示：</p>
<ul>
<li>先将a的值入栈，然后测试a的值是否为true，因为在例2中，a的值为true，因此PC寄存器会加1，所以跳到了下下个指令中</li>
<li>这个指令获取了b的值，并且放入寄存器R(0)中，然后使用OP_TESTSET指令，因为此时b的值为false，因此下一个指令为OP_JMP指令</li>
<li>最后一个OP_JMP指令直接跳过了最后一个OP_GETTABUP指令，此时我们and运算的最终的结果，就是b的值</li>
</ul>
<p>接下来，我们来看一下例3的情况，得到图46的结果：<br><img src="expr-example10-13.png" alt="image">图46<br>图46展示了例3的运作流程，与前面的没有很大的差别，这里需要注意的是，如果a和b的值都为true，那么决定这个and逻辑运算结果的是最后一个表达式的值。<br>    我已经展示了and逻辑运算的流程，至于or的，只需要把OP_TESTSET指令的C域改成1，就是or的逻辑运算了，读者可以自己去推导。接下来，我们来看一下逻辑运算中，包含常量的情况。这里要分情况来讨论，当我们在纯粹的and逻辑处理流程过程中时，如果and操作符前面的exp，经过expr函数输出的expdesc结构是VK、VFLT、VINT或VTRUE类型时，它是被直接忽略掉的，也就是说不参与到指令生成。如图47所示的结果：<br><img src="expr-example10-14.png" alt="image">图47<br>我们可以看到，除了最后一个exp，其他exp均在and操作符前面，他们在and逻辑运算中，如果处于and操作符之前，就可以直接被忽略，因为他们的值一定是true，所以直接跳到下一个TEST指令之中，这属于编译器的一种优化。如果and逻辑执行到最后一个exp，说明前面所有的exp都测试通过，因此最后一个exp的值决定了最后的结果，所以不能被忽略。<br>    接下来，看一下or逻辑的情况，如果or操作符前面的exp，经过expr函数处理后，得到的expdesc结构是VNIL或VFALSE的类型时，它可以被忽略，如图48的情况：<br><img src="expr-example10-15.png" alt="image">图48<br>我们可以看到，除了最后一个exp是false或者nil，其他在or前面的exp，凡是这两个类型，都可以被忽略，因为在or逻辑处理中，只要exp的值是false就会进入下一个or操作的测试之中。去除了这些常量，我们就可以将余下的内容，套用到前面讨论过的流程之中了。<br>    关于逻辑运算的处理，我们还有最后一种情况没有讨论，就是and和or结合的例子，我们现在来看一组例子，如下所示：</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><span class="line">a <span class="keyword">and </span><span class="keyword">b </span><span class="keyword">and </span>c <span class="keyword">or </span>d</span><br></pre></td></tr></table></figure>
<p>面对这样的情况，我们可以接着图40的情况，往下讨论，假设现在我们已经处理了a and b and c的流程，现在我们将c变量的信息，装填到expdesc结构中，得到图49的结果：<br><img src="expr-example10-16.png" alt="image">图49<br>此时我们获得了操作符or，进入中序处理流程(调用luaK_infix函数)，这里首先将e中的信息，转化为虚拟机指令，于是得到图50的结果：<br><img src="expr-example10-17.png" alt="image">图50<br>尔后，我们需要生成的是OP_TESTSET指令和OP_JMP指令，得到图51的结果：<br><img src="expr-example10-18.png" alt="image">图51<br>我们可以看到，e中原来代表false列表的字段就不使用了，取而代之的是e-&gt;t，这个表示为true时的列表，也就是说当变量值为true的时候，直接跳过所有测试。原来的false列表，并未被立即消费，而是转移到FuncState结构的jpc变量来暂存。此时，luaK_infix函数的处理就结束了，接下来使用luaK_next函数，来获得下一个token &lt; TK_NAME, d &gt;，此时需要把d的内容，装填到新的expdesc结构中，得到图52的结果：<br><img src="expr-example10-19.png" alt="image">图52<br>接下来就进入到luaK_posfix函数的处理之中了，在这种情况下，就是将e和e2的信息整合在一起，其实就是将t的值赋值给e2-&gt;t，最后用e2的值覆盖e的值，得到图53的结果：<br><img src="expr-example10-20.png" alt="image">图53<br>到了这一步，我们的expr函数的处理就成就算结束了。但是为了完整性，我们需要对expdesc中的信息，转化为指令，在赋值语句中，或者在ifstat、whilestat、forstat等的condition部分，我们需要将e转化为指令，于是得到图54的结果：<br><img src="expr-example10-21.png" alt="image">图54<br>从图54中的内容，我们可以看到，jpc所指向的jump指令列表的跳转索引，被改变了，直接指向了获取变量d到栈中的指令&lt; OP_GETTABUP 0 0 259 &gt;，这表示，当a、b的值为false时，直接跳转到d这里，以d的值作为表达式的结果。而e-&gt;t指向的JUMP指令，则指向了下下个指令，因为如果c为true，则不用理会d的具体值了。<br>    这里展示了and和or的逻辑运算，至于or和and的结合，就留给读者自己去推导了，到目前为止，逻辑运算相关的论述就结束了，接下来我们将进入到比较运算的讨论之中。</p>
<p>5、比较运算<br>    现在我们将进入比较运算的探索之中了，我们的比较运算包括了&gt;、&lt;、&gt;=、&lt;=、==、!=6种，但是我们用于比较运算的指令只有三个，分别是OP_EQ、OP_LT和OP_LE。我们通过==和!=两个实例，来看一下它的运作流程，其他的运算符编译流程，和这个差不多，因此不会再赘述，留给读者自己去推导，现在我们来看一下以下两个例子：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">a</span> == b</span><br><span class="line"><span class="selector-tag">a</span> != b</span><br></pre></td></tr></table></figure>
<p>我们先来看一下a==b的例子，如图55所示：<br><img src="expr-example10-22.png" alt="image">图55<br>lua编译器，先通过simpleexp函数，去识别变量a，并且将变量a的结果装填到了expdesc的结构之中去了，此时调用luaX_next函数，获得下一个token，也就是操作符==，此时要对e和==号进行中序流程处理，而处理它们的函数则是luaK_infix函数，这个函数，在比较运算过程中，对操作符前的exp的处理是，将保存操作符前的那个exp信息的expdesc结构，转化为指令，因此得到图56的结果：<br><img src="expr-example10-23.png" alt="image">图56<br>接下来，expr函数会调用luaK_next函数，获取下一个token，得到&lt; TK_NAME, b &gt;，此时我们需要一个新的expdesc结构，e2，并将b的信息装填到里面，于是得到图57的结果：<br><img src="expr-example10-24.png" alt="image">图57<br>此时我们获得了e和e2，接下来，就是要进行真正的比较指令生成了，这个操作是交给luaK_posfix函数来执行。首先要将e2的信息，转化为指令，于是得到图58的效果：<br><img src="expr-example10-25.png" alt="image">图58<br>然后整合e和e2，并且生成OP_EQ指令，OP_JMP指令和OP_LOADBOOL指令，如图59所示：<br><img src="expr-example10-26.png" alt="image">图59<br>我们需要关注几个地方，第一个是freereg被复位了，第二个是OP_EQ指令，如果测试失败，则跳过OP_JMP指令，进入第一个OP_LOADBOOL指令，然后将0值赋值到R(A)中，然后PC寄存器跳到蓝色箭头所指向的位置。如果测试成功，则下一个指令执行OP_JMP指令，然后PC寄存器跳转到黑色箭头指向的位置。我们现在先来看一下a==b成立的情况，它如图60所示：<br><img src="expr-example10-27.png" alt="image">图60<br>图中的黑色箭头，就是指令执行的顺序，现在我们来看另一种情况，当a==b不成立的情况，它如图61所示：<br><img src="expr-example10-28.png" alt="image">图61<br>黑色箭头是指令的执行顺序。对于不等于的情况，只需要将OP_EQ指令的A域由1变为0即可，其他的比较运算，读者可以根据前面的虚拟机指令说明，将其套入我们刚刚讨论过的指令生成流程之中，这个工作就交给读者自己去推演，这里不再赘述。</p>
<p>6、逻辑运算中包含比较运算的情况<br>    我们前面已经讨论了逻辑运算和比较运算的情况，现在来讨论一下逻辑运算和比较运算结合的情况。这点非常重要，为什么说这点很重要呢？因为比较运算生成的虚拟机指令，和逻辑运算结合，会改变原有的一些参数，我将结合如下的例子，来论述这个流程：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line">a == <span class="keyword">b </span><span class="keyword">and </span>c <span class="keyword">and </span>d</span><br></pre></td></tr></table></figure>
<p>和前面的流程一样，首先我们获得的第一个token是&lt; TK_NAME, a &gt;，然后此时要进入到expr函数的执行流程之中，expr函数首先要识别第一个token，这是一个simpleexp，因此，此时需要将变量a装填到expdesc结构之中，得到图62所示的情况：<br><img src="expr-example10-29.png" alt="image">图62<br>在完成a变量的expdesc装填之后，此时parser会调用luaX_next函数，获取下一个token TK_EQ，然后需要对刚刚装填好的expdesc结构变量，和这个操作符做一次“中序处理”，也就是通过luaK_infix函数来处理它们之间的关系。在这种情况下，编译器会将刚刚的expdesc信息，转化为指令，得到图63的结果：<br><img src="expr-example10-30.png" alt="image">图63<br>完成luaK_infix函数的调用之后，我们获得了下一个token &lt; TK_NAME, b &gt;，此时需要将b装填到新的expdesc结构之中，得到图64的结果：<br><img src="expr-example10-31.png" alt="image">图64<br>在获得了e和e2两个结构之后，我们需要对它们进行后缀表达式来计算，操作符是==，此时需要将e2也转化成虚拟机指令，于是得到图65的结果：<br><img src="expr-example10-32.png" alt="image">图65<br>现在的情况是，两个操作数已经入栈，然后需要根据操作符进行后缀表达式运算，我们当前的操作符是==，此时生成OP_EQ和OP_JMP指令，如图66所示：<br><img src="expr-example10-33.png" alt="image">图66<br>我们现在获得了新的token，TK_AND，前面经过比较运算得到的结果，存储到了expdesc结构之中，现在要对这个结构中的信息，和TK_AND操作符进行整合处理，处理它的函数是luaK_infix函数，此时，我们需要将e-&gt;info的信息，赋值给e-&gt;f，并且修改OP_EQ指令的A域。得到图67的结果：<br><img src="expr-example10-34.png" alt="image">图67<br>接下来，lua parser会调用luaX_next函数，获得下一个token &lt; TK_NAME, c &gt;，并且将token装填到新的expdesc结构之，得到图68的结果：<br><img src="expr-example10-35.png" alt="image">图68<br>此时我们获得了e和e2，现在要对他们进行后缀表达式处理，处理函数是luaK_posifx，而操作符是第一个and，于是有图69的结果：<br><img src="expr-example10-36.png" alt="image">图69<br>接下来，要对e进行和第二个and操作符的整合处理，这个处理由luaK_infix函数实现，此时，会将e中的信息，转化为指令，得到图70的结果：<br><img src="expr-example10-37.png" alt="image">图70<br>完成luaK_infix处理之后，parser会调用luaX_next函数，获取下一个token &lt; TK_NAME, d &gt;，然后将其装载到新的expdesc结构e2中，得到图71的结果：<br><img src="expr-example10-38.png" alt="image">图71<br>接下来又到了，整合e和e2的流程了，完成整合之后，我们得到图72的结果：<br><img src="expr-example10-39.png" alt="image">图72<br>到这一步，我们的expr函数的处理就完成了，但是为了讨论的完整性，这里要将expdesc结构内的信息，转化为指令，于是得到如图73的结果：<br><img src="expr-example10-40.png" alt="image">图73<br>到这里为止，逻辑运算包含比较运算的情况，我就完成了论述了，当然还有or的情况，还有其他更复杂的组合，不过相信读者通过读懂上面这个例子，能够举一反三，自行推导。最后我通过展示a==b为true或false，c和d都为true的情况，来展示一下指令的执行流程，其他的情况由读者自己推导。<br><img src="expr-example10-41.png" alt="image">图74<br>a==b为true的情况。<br><img src="expr-example10-42.png" alt="image">图75<br>a==b为false的情况。</p>
<p>7、constructor处理<br>    这一部分，实际上就是对一个table进行编译，实际上，这个操作也并不复杂，现在通过一个例子来说明：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">&#123; <span class="selector-tag">a</span>, <span class="selector-tag">b</span>, c = <span class="number">10</span>, d &#125;</span><br></pre></td></tr></table></figure>
<p>我们从图75-2开始，直至完整展现编译流程。<br><img src="expr-example10-43.png" alt="image">图75-2<br>当lua编译器，遇到{的时候，直接生成OP_NEWTABLE指令，图75-2中的A域是0，表示生成的table放置在R(A)也就是R(0)上。B域和C域均为0，表示它的arraysize和hashsize都是0，现在是个空表。然后我们进入下一个操作之中。<br><img src="expr-example10-44.png" alt="image">图75-3<br>回顾一下图6，在constructor内部的处理流程之中，只有单个simpleexp的，则是listfield，这种exp需要直接生成入栈指令，稍后一并处理。接下来，我们获得的token则是’,‘，这里直接跳过，直接获取下一个token &lt; TK_NAME, b &gt;，此时生成新的指令，如图75-4所示：<br><img src="expr-example10-45.png" alt="image">图75-4<br>此时，直接跳过’,‘，处理下一个field，得到图75-5的结果：<br><img src="expr-example10-46.png" alt="image">图75-5<br>这里对c=10生成了OP_SETTABLE指令，这里并没有生成入栈指令，而是直接赋值。然后我们进入到最后一个filed的处理之中，也就是将d生成入栈指令，得到图75-6的结果：<br><img src="expr-example10-47.png" alt="image">图75-6<br>最后，我们需要将已经入栈的参数，设置到table里，这些在栈中的变量，会被设置到table的array域中，于是得到图75-7的结果：<br><img src="expr-example10-48.png" alt="image">图75-7<br>最后这里生成了一个OP_SETLIST指令，它的A、B和C域的值分别是0、3和1，A域指明了table在栈中的位置，B域说明了从R(A+1)开始，一共有多少个栈上的变量，要被设置到table的array中。我们现在来回顾一下，OP_SETLIST的含义：</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">OP_SETLIST A B C R<span class="comment">(A)</span>[<span class="comment">(C-1)</span>*FPF+i] := R<span class="comment">(A+i)</span>, <span class="number">1</span> &lt;= i &lt;= B</span><br></pre></td></tr></table></figure>
<p>A域指向了操作目标，B域指明了从R(A+1)开始，一共有多少个栈上的变量，要被设置到R(A)的array列表中，C域指明了它的array区间，FPF指明了，一次通过OP_SETLIST指令，设置到table array列表中的最大批次，默认是50。举个例子，比如我们现在有一个table，现在有100个变量，要被设置到array中，那么这里需要生成两个OP_SETLIST指令，假设目标table在R(0)，那么第一个OP_SETLIST指令则是&lt; OP_SETLIST 0 50 1 &gt;表示设置到1~50的位置上，第二个OP_SETLIST指令则是&lt; OP_SETLIST 0 50 2 &gt;表示设置到51~100的位置上。<br>    这里需要注意的是，在constructor内，有赋值操作的域，是直接生成赋值指令，将对应的值设置到目标表里，其他的均是列表域，因此都需要通过压入栈，再使用OP_SETLIST指令，来将这些变量设置到table上。</p>
<p>8、优先级处理<br>    接下来我们要讨论的是，优先级处理的情况。优先级的处理，主要有运算符优先级，还有就是表达式优先级。我们先看一下运算符优先级的情况，实际上lua语言对运算符的优先级，有进行分级，其分级如下所示：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line">static const struct &#123;</span><br><span class="line">	lu_byte left;   <span class="comment">// left priority for each binary operator</span></span><br><span class="line">	lu_byte right;  <span class="comment">// right priority</span></span><br><span class="line">&#125; priority[] = &#123;</span><br><span class="line">	&#123;<span class="number">10</span>,<span class="number">10</span>&#125;, &#123;<span class="number">10</span>,<span class="number">10</span>&#125;,		   <span class="comment">// '+' and '-'</span></span><br><span class="line">	&#123;<span class="number">11</span>,<span class="number">11</span>&#125;, &#123;<span class="number">11</span>,<span class="number">11</span>&#125;, &#123;<span class="number">11</span>,<span class="number">11</span>&#125;, &#123;<span class="number">11</span>, <span class="number">11</span>&#125;, <span class="comment">// '*', '/', '//' and '%'</span></span><br><span class="line">	&#123;<span class="number">14</span>,<span class="number">13</span>&#125;,				   <span class="comment">// '^' right associative</span></span><br><span class="line">	&#123;<span class="number">6</span>,<span class="number">6</span>&#125;, &#123;<span class="number">4</span>,<span class="number">4</span>&#125;, &#123;<span class="number">5</span>,<span class="number">5</span>&#125;,	   <span class="comment">// '&amp;', '|' and '~'</span></span><br><span class="line">	&#123;<span class="number">7</span>,<span class="number">7</span>&#125;, &#123;<span class="number">7</span>,<span class="number">7</span>&#125;,			   <span class="comment">// '&lt;&lt;' and '&gt;&gt;'</span></span><br><span class="line">	&#123;<span class="number">9</span>,<span class="number">8</span>&#125;,					   <span class="comment">// '..' right associative</span></span><br><span class="line">	&#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, &#123;<span class="number">3</span>,<span class="number">3</span>&#125;, <span class="comment">// '&gt;', '&lt;', '&gt;=', '&lt;=', '==', '~=',</span></span><br><span class="line">	&#123;<span class="number">2</span>,<span class="number">2</span>&#125;, &#123;<span class="number">1</span>,<span class="number">1</span>&#125;,			   <span class="comment">// 'and' and 'or'</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>代码片段6<br>我们这里的运算符优先级，是双目运算符的优先级，一个双目运算符有两个操作数，分别是左操作数和右操作数，上面花括号内的数字，分别代表左操作数和右操作数的权重，比如下面一个例子：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">a  +  b</span><br><span class="line"><span class="number">10</span>    <span class="number">10</span></span><br></pre></td></tr></table></figure>
<p>到代码片段6中，查找加法运算符的权重，得到的值，附属到了变量a和b的下方。如果出现一连串的算数运算，比如下面的例子：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span>    <span class="number">10</span></span><br><span class="line">a  +  b  *  c</span><br><span class="line">      <span class="number">11</span>    <span class="number">11</span></span><br></pre></td></tr></table></figure>
<p>代码上方的是加法运算的操作数权重，代码下方的是乘法运算的操作数权重，根据这个权重，我们就可以判定，权重高的运算符要先运算。我们的编译器，怎么处理这样的流程的呢？通过图76来说明：<br><img src="expr-example11.png" alt="image">图76<br>图76展示了一个表达式，从例子中，我们可以看到，^运算符的优先级是最高的，然后是*运算符，最后是+运算符。上面这个表达式，最终会经过expr函数，计算得到一个expdesc结构的结果，但是在这个expr函数内部，还是会将表达式分解成若干个表达式，并且通过递归的方式，调用expr函数来编译他们，整个分解过程，就是我们看到的图76的结果。通过图76，我们可以得到几个结论，在一个包含运算符的表达式中，运算符的优先级越高，其层次越深，计算的次序越靠前。我们可以通过先序遍历的方式，去遍历这个Parse Tree，当操作符右边的节点，要退出的时候，操作运算才生效。读者可以结合代码片段5和图76去体会一下，lua编译器对于运算符优先级的处理逻辑。<br>    我们现在来看另一个例子，这个例子是表达式在括号内的情况，如图77所示：<br><img src="expr-example11-2.png" alt="image">图77<br>根据图77，我们可以看到，expr处理带有双目操作符的时候，操作符左边的exp一定是一个simpleexp，而simpleexp本身就要处理括号内的表达式的情况，并且simpleexp一定要优先处理。这样做的好处是，不管操作符的优先级有多高，括号内的表达式要优先完成运算。读者同样可以结合图77和代码片段5来感受这一点。</p>
<h4 id="suffixedexp构造与编译"><a href="#suffixedexp构造与编译" class="headerlink" title="suffixedexp构造与编译"></a>suffixedexp构造与编译</h4><p>​    接下来，我们来看一下suffixedexp的构造，我们前面详细论述了expr的构造，之所以要先论述它，是因为它是一切的基础。我们首先来回顾一下suffixedexp的EBNF范式：</p>
<figure class="highlight bnf"><table><tr><td class="code"><pre><span class="line">suffixedexp ::= primaryexp &#123;<span class="string">'.'</span> NAME | <span class="string">'['</span> expr <span class="string">']'</span> | <span class="string">':'</span> NAME funcargs | funcargs&#125;</span><br><span class="line">primaryexp ::= NAME | <span class="string">'('</span> expr <span class="string">')'</span></span><br><span class="line">funcargs ::= <span class="string">'('</span>[explist]<span class="string">')'</span> | constructor | STRING</span><br></pre></td></tr></table></figure>
<p>我们可以简单地归纳，suffixedexp的主要构成部分有三块，分别是primaryexp、table访问和函数调用三块。前两者可以构成赋值语句中，=左边的variable，函数调用可以作为独立的exp存在，回顾一下代码片段3，这也是exprstat函数，首先要调用suffixedexp函数的原因，因为我们前面也说过，exprstat中，不论是赋值语句还是函数调用，他们首先都要识别出第一个exp，然后根据后续的token，判定输入的statement是个赋值语句(遇到’=‘的时候)，或是函数调用(遇到’(‘的时候)，还是不合法。这也是suffixedexp函数这么去设计的原因。在分别开始讨论三个构造之前，我们先来看一下suffixedexp函数的代码：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line"> <span class="number">493</span> <span class="keyword">static</span> <span class="function"><span class="keyword">void</span> <span class="title">suffixedexp</span><span class="params">(struct lua_State* L, LexState* ls, FuncState* fs, expdesc* e)</span> </span>&#123;</span><br><span class="line"> <span class="function">494     <span class="title">primaryexp</span><span class="params">(L, ls, fs, e)</span></span>;</span><br><span class="line"> <span class="function">495     <span class="title">luaX_next</span><span class="params">(L, ls)</span></span>;</span><br><span class="line"> <span class="number">496</span> </span><br><span class="line"> <span class="function">497     <span class="title">for</span> <span class="params">(;;)</span> </span>&#123;</span><br><span class="line"> <span class="function">498         <span class="title">switch</span> <span class="params">(ls-&gt;t.token)</span> </span>&#123;</span><br><span class="line"> <span class="number">499</span>         <span class="keyword">case</span> <span class="string">'.'</span>: &#123;</span><br><span class="line"> <span class="function">500             <span class="title">fieldsel</span><span class="params">(L, ls, fs, e)</span></span>;</span><br><span class="line"> <span class="number">501</span>         &#125; <span class="keyword">break</span>;</span><br><span class="line"> <span class="number">502</span>         <span class="keyword">case</span> <span class="string">':'</span>: &#123;</span><br><span class="line"> <span class="number">503</span>             expdesc key;</span><br><span class="line"> <span class="function">504             <span class="title">luaX_next</span><span class="params">(L, ls)</span></span>;</span><br><span class="line"> <span class="function">505             <span class="title">checkname</span><span class="params">(L, ls, &amp;key)</span></span>;</span><br><span class="line"> <span class="function">506             <span class="title">luaK_self</span><span class="params">(fs, e, &amp;key)</span></span>;</span><br><span class="line"> <span class="function">507             <span class="title">funcargs</span><span class="params">(fs, e)</span></span>;</span><br><span class="line"> <span class="number">508</span>         &#125; <span class="keyword">break</span>;</span><br><span class="line"> <span class="number">509</span>         <span class="keyword">case</span> <span class="string">'['</span>: &#123;</span><br><span class="line"> <span class="function">510             <span class="title">luaK_exp2anyregup</span><span class="params">(fs, e)</span></span>;</span><br><span class="line"> <span class="number">511</span>             expdesc key;</span><br><span class="line"> <span class="function">512             <span class="title">yindex</span><span class="params">(L, fs, &amp;key)</span></span>;</span><br><span class="line"> <span class="function">513             <span class="title">luaK_indexed</span><span class="params">(fs, e, &amp;key)</span></span>;</span><br><span class="line"> <span class="number">514</span>         &#125; <span class="keyword">break</span>;</span><br><span class="line"> <span class="number">515</span>         <span class="keyword">case</span> <span class="string">'('</span>: &#123;</span><br><span class="line"> <span class="function">516             <span class="title">luaK_exp2nextreg</span><span class="params">(fs, e)</span></span>;</span><br><span class="line"> <span class="function">517             <span class="title">funcargs</span><span class="params">(fs, e)</span></span>;</span><br><span class="line"> <span class="number">518</span>         &#125; <span class="keyword">break</span>;</span><br><span class="line"> <span class="number">519</span>         <span class="keyword">default</span>: <span class="keyword">return</span>;</span><br><span class="line"> <span class="number">520</span>         &#125;</span><br><span class="line"> <span class="number">521</span>     &#125;</span><br><span class="line"> <span class="number">522</span> &#125;</span><br></pre></td></tr></table></figure>
<p>代码片段7</p>
<p>1、primaryexp构造<br>    代码片段7，很符合我们的EBNF范式，同时我们也可以看到，suffixedexp函数，首先要识别的就是primaryexp，它的EBNF范式在前面也已经展示过，这里展示一下它的实现逻辑：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line"> <span class="number">147</span> <span class="keyword">static</span> <span class="function"><span class="keyword">void</span> <span class="title">primaryexp</span><span class="params">(struct lua_State* L, LexState* ls, FuncState* fs, expdesc* e)</span> </span>&#123;</span><br><span class="line"> <span class="function">148     <span class="title">switch</span> <span class="params">(ls-&gt;t.token)</span> </span>&#123;</span><br><span class="line"> <span class="number">149</span>     <span class="keyword">case</span> TK_NAME: &#123;</span><br><span class="line"> <span class="function">150         <span class="title">singlevar</span><span class="params">(fs, e, ls-&gt;t.seminfo.s)</span></span>;</span><br><span class="line"> <span class="number">151</span>     &#125; <span class="keyword">break</span>;</span><br><span class="line"> <span class="number">152</span>     <span class="keyword">case</span> <span class="string">'('</span>: &#123;</span><br><span class="line"> <span class="function">153         <span class="title">luaX_next</span><span class="params">(L, ls)</span></span>;</span><br><span class="line"> <span class="function">154         <span class="title">expr</span><span class="params">(fs, e)</span></span>;</span><br><span class="line"> <span class="function">155         <span class="title">luaK_exp2nextreg</span><span class="params">(fs, e)</span></span>;</span><br><span class="line"> <span class="number">156</span>         check(L, ls, <span class="string">')'</span>);</span><br><span class="line"> <span class="number">157</span>     &#125; <span class="keyword">break</span>;</span><br><span class="line"> <span class="number">158</span>     <span class="keyword">default</span>: &#123;</span><br><span class="line"> <span class="function">159         <span class="title">luaX_syntaxerror</span><span class="params">(L, ls, <span class="string">"unsupport syntax"</span>)</span></span>;</span><br><span class="line"> <span class="number">160</span>     &#125; <span class="keyword">break</span>;</span><br><span class="line"> <span class="number">161</span>     &#125;</span><br><span class="line"> <span class="number">162</span> &#125;</span><br></pre></td></tr></table></figure>
<p>代码片段8<br>primaryexp的两个主要分支，一个是变量识别，还有一个是括号内的expr处理，这两个内容，我们在expr构造一节中，已经花了大量的篇幅去介绍，这里就不再赘述，其本质也和expr函数一样。我们可以通过图6去感知一下，expr和suffixedexp以及primaryexp的关系，他们其实是通过递归来实现嵌套逻辑的。我们的primaryexp，依然是输入一些token，输出一个expdesc结构：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">a =&gt; <span class="function"><span class="params">primaryexp</span> =&gt;</span> expdesc</span><br><span class="line">(a + b * c) =&gt; <span class="function"><span class="params">primaryexp</span> =&gt;</span> expdesc</span><br><span class="line">(<span class="function">(<span class="params">(a</span>))) =&gt;</span> <span class="function"><span class="params">primaryexp</span> =&gt;</span> expdesc</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<p>虽然EBNF范式，已经展示了primaryexp和suffixedexp的关系，但是我们也可以通过一张图来展现：<br><img src="suffixedexp.png" alt="image">图78</p>
<p>2、table访问<br>    我们要讨论的第二部分内容，就是table的访问。不论如何，首先我们都要先处理primaryexp，这是table的本体(即table自身)，而访问处理也是针对它来进行的。从上面的EBNF范式来看，table的组织，可以抽象为以下的形式：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="class">primaryexp </span>&#123; .NAME | <span class="string">'['</span> expr <span class="string">']'</span> &#125;</span><br></pre></td></tr></table></figure>
<p>直接通过’.‘来访问，和通过方括号来访问，现在我们来通过一个例子，来梳理一下table访问的流程，同时我也希望读者通过这个例子，自己去推导其他复杂的情况：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">foo.bar[<span class="string">"call"</span>][<span class="symbol">a</span>]</span><br></pre></td></tr></table></figure>
<p>首先，上面这个表达式，会交给suffixedexp函数来处理，而处理第一个token的，则是primaryexp，此时，parser会将foo直接装载到expdesc结构之中，得到图79的结果：<br><img src="suffixedexp-2.png" alt="image">图79<br>此时，我们当前的token是’.‘，因此进入了通过点来访问table域的逻辑流程之中，对应代码片段7中的第500行代码，此时需要做两件事情，第一件是将e中的信息转化为指令，第二件是，获取’.‘之后的那个simpleexp，并装载到新的expdesc结构之中，于是得到图80的结果<br><img src="suffixedexp-3.png" alt="image">图80<br>接下来，我们需要整合e和k，并且获取下一个token，得到图81所示的结果：<br><img src="suffixedexp-4.png" alt="image">图81<br>现在，我们当前的token是’[‘，此时进入到方括号的table域访问流程之中，到这里，先要把e中的信息转化为虚拟机指令，于是得到图82的结果：<br><img src="suffixedexp-5.png" alt="image">图82<br>此时我们获得了一个OP_GETTABLE指令，B为0表示操作对象位于R(0)，C为257，表示key值为Kst(257-256)=Kst(1)，也就是bar字符串，A值为1，表示访问结果存储到R(1)中，这里需要注意一下freereg的变化。关于expdesc转化为指令的流程，本篇前面的章节已经详细论述过，读者如果还有疑惑，可以翻回去查阅，这里不再赘述。接下来，lua parser会调用luaX_next函数，获取下一个token，并且将字符串”call”的信息，装载到新的expdesc结构之中，于是得到图83的结果：<br><img src="suffixedexp-6.png" alt="image">图83<br>接下来，lua parser会跳过下一个token ‘]‘，直接获得其后面的token ‘[‘，并且整合e和k，得到图84的结果：<br><img src="suffixedexp-7.png" alt="image">图84<br>现在当前的token是’[‘，还是进入方括号访问table域的流程之中，此时，先将e中的信息，转化为虚拟机指令，然后通过luaX_next获取下一个token a，并且将a装载到新的expdesc结构之中，于是得到图85的结果：<br><img src="suffixedexp-8.png" alt="image">图85<br>接下来，就是调用luaX_next函数，获得下一个token ‘]‘，并且将key中的信息转化为虚拟机指令，于是得到图86的结果：<br><img src="suffixedexp-9.png" alt="image">图86<br>接下来就是要整合e和key了，得到图87的结果：<br><img src="suffixedexp-10.png" alt="image">图87<br>到目前为止，suffixedexp对上面例子的处理就结束了，至于怎么将当前的exp转化为虚拟机指令，读者可以参阅【expr构造和编译】那一节的相关内容，这里不再赘述，由读者自行去推导。</p>
<p>3、函数调用<br>    suffixedexp是处理函数调用编译流程的，这里有两种模式，一种是通过’.‘访问，还有一种是通过’:‘访问，我们前面已经花了巨量的篇幅论述expr相关的编译流程，这里不打算继续通过这种方式来，而是通过<a href="https://github.com/Manistein/dummylua-tutorial/tree/master/tools/protodump" rel="external nofollow noopener noreferrer" target="_blank">protodump</a>工具来生成我们的虚拟机指令，由读者自己推导编译流程，关于protodump的使用，读者可以阅读Part5的【Bytecode分析工具】一节。现在我们来看两个例子，第一个是：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">foo</span><span class="params">(a, b, c)</span></span></span><br></pre></td></tr></table></figure>
<p>其生成的虚拟机指令如下所示：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">1 lua.dump</span><br><span class="line">file_name = ../test.lua.out</span><br><span class="line">+upvals+1+name [_ENV]</span><br><span class="line">+proto+code+1 [<span class="string">iABC:OP_GETTABUP      A:0   B  :0   C:256 ; R(A) := UpValue[B</span>][<span class="symbol">RK(C)</span>]]</span><br><span class="line">|     |    +2 [<span class="string">iABC:OP_GETTABUP      A:1   B  :0   C:257 ; R(A) := UpValue[B</span>][<span class="symbol">RK(C)</span>]]</span><br><span class="line">|     |    +3 [<span class="string">iABC:OP_GETTABUP      A:2   B  :0   C:258 ; R(A) := UpValue[B</span>][<span class="symbol">RK(C)</span>]]</span><br><span class="line">|     |    +4 [<span class="string">iABC:OP_GETTABUP      A:3   B  :0   C:259 ; R(A) := UpValue[B</span>][<span class="symbol">RK(C)</span>]]</span><br><span class="line">|     |    +5 [iABC:OP_CALL          A:0   B  :4   C:1   ; R(A), ... ,R(A+C-2) := R(A)(R(A+1), ... ,R(A+B-1))]</span><br><span class="line">|     |    +6 [iABC:OP_RETURN        A:0   B  :1         ; return R(A), ... ,R(A+B-2)  (see note)]</span><br><span class="line">|     +maxstacksize [4]</span><br><span class="line">|     +numparams [0]</span><br><span class="line">|     +k+1 [foo]</span><br><span class="line">|     | +2 [a]</span><br><span class="line">|     | +3 [b]</span><br><span class="line">|     | +4 [c]</span><br><span class="line">|     +source [@../test.lua]</span><br><span class="line">|     +linedefine [0]</span><br><span class="line">|     +type_name [Proto]</span><br><span class="line">|     +lineinfo+1 [1]</span><br><span class="line">|     |        +2 [1]</span><br><span class="line">|     |        +3 [1]</span><br><span class="line">|     |        +4 [1]</span><br><span class="line">|     |        +5 [1]</span><br><span class="line">|     |        +6 [1]</span><br><span class="line">|     +p</span><br><span class="line">|     +lastlinedefine [0]</span><br><span class="line">|     +locvars</span><br><span class="line">|     +is_vararg [1]</span><br><span class="line">|     +upvalues+1+instack [1]</span><br><span class="line">|                +idx [0]</span><br><span class="line">|                +name [_ENV]</span><br><span class="line">+type_name [LClosure]</span><br></pre></td></tr></table></figure>
<p>第二个例子是：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">foo</span>:<span class="keyword">bar(a, </span><span class="keyword">b, </span>c)</span><br></pre></td></tr></table></figure>
<p>其生成的虚拟机指令如下所示：</p>
<figure class="highlight gherkin"><table><tr><td class="code"><pre><span class="line">1 lua.dump</span><br><span class="line">file_name = ../test.lua.out</span><br><span class="line">+proto+p</span><br><span class="line">|<span class="string">     +linedefine [0]</span></span><br><span class="line">|<span class="string">     +maxstacksize [5]</span></span><br><span class="line">|<span class="string">     +locvars</span></span><br><span class="line">|<span class="string">     +k+1 [foo]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +2 [bar]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +3 [a]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +4 [b]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string"> +5 [c]</span></span><br><span class="line">|<span class="string">     +code+1 [iABC:OP_GETTABUP      A:0   B  :0   C:256 ; R(A) := UpValue[B][RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +2 [iABC:OP_SELF          A:0   B  :0   C:257 ; R(A+1) := R(B); R(A) := R(B)[RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +3 [iABC:OP_GETTABUP      A:2   B  :0   C:258 ; R(A) := UpValue[B][RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +4 [iABC:OP_GETTABUP      A:3   B  :0   C:259 ; R(A) := UpValue[B][RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +5 [iABC:OP_GETTABUP      A:4   B  :0   C:260 ; R(A) := UpValue[B][RK(C)]]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +6 [iABC:OP_CALL          A:0   B  :5   C:1   ; R(A), ... ,R(A+C-2) := R(A)(R(A+1), ... ,R(A+B-1))]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">    +7 [iABC:OP_RETURN        A:0   B  :1         ; return R(A), ... ,R(A+B-2)  (see note)]</span></span><br><span class="line">|<span class="string">     +type_name [Proto]</span></span><br><span class="line">|<span class="string">     +lineinfo+1 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +2 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +3 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +4 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +5 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +6 [1]</span></span><br><span class="line">|<span class="string">     </span>|<span class="string">        +7 [1]</span></span><br><span class="line">|<span class="string">     +is_vararg [1]</span></span><br><span class="line">|<span class="string">     +lastlinedefine [0]</span></span><br><span class="line">|<span class="string">     +numparams [0]</span></span><br><span class="line">|<span class="string">     +source [@../test.lua]</span></span><br><span class="line">|<span class="string">     +upvalues+1+instack [1]</span></span><br><span class="line">|<span class="string">                +idx [0]</span></span><br><span class="line">|<span class="string">                +name [_ENV]</span></span><br><span class="line"><span class="string">+upvals+1+name [_ENV]</span></span><br><span class="line"><span class="string">+type_name [LClosure]</span></span><br></pre></td></tr></table></figure>
<h4 id="assignment构造和编译"><a href="#assignment构造和编译" class="headerlink" title="assignment构造和编译"></a>assignment构造和编译</h4><p>​    我们现在回顾一下，assignment的EBNF范式，如下所示：</p>
<figure class="highlight bnf"><table><tr><td class="code"><pre><span class="line">assignment ::= suffixedexp &#123;, suffixedexp&#125; [<span class="string">'='</span> explist]</span><br></pre></td></tr></table></figure>
<p>这样的statement，一定要有一个等于号，位于等于号左边的是suffixedexp，但是不能是funccall类型的，而右边的则是exp列表，这个statement的工作，就是将右边的exp赋值到左边的variable，其函数实现如下所示：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line"> <span class="number">546</span> static void assignment(FuncState* fs, LH_assign* v, int nvars) &#123;</span><br><span class="line"> <span class="number">547</span>     <span class="function"><span class="title">expdesc</span>* var = &amp;v-&gt;</span>v;</span><br><span class="line"> <span class="number">548</span>     <span class="function"><span class="title">check_condition</span>(fs-&gt;</span>ls, vkisvar(var), <span class="string">"not var"</span>);</span><br><span class="line"> <span class="number">549</span> </span><br><span class="line"> <span class="number">550</span>     LH_assign lh;</span><br><span class="line"> <span class="number">551</span>     lh.prev = v;</span><br><span class="line"> <span class="number">552</span> </span><br><span class="line"> <span class="number">553</span>     expdesc e;</span><br><span class="line"> <span class="number">554</span>     init_exp(&amp;e, VVOID, <span class="number">0</span>);</span><br><span class="line"> <span class="number">555</span>     <span class="function"><span class="title">if</span> (fs-&gt;</span><span class="function"><span class="title">ls</span>-&gt;</span>t.token == <span class="string">','</span>) &#123;</span><br><span class="line"> <span class="number">556</span>         <span class="function"><span class="title">luaX_next</span>(fs-&gt;</span><span class="function"><span class="title">ls</span>-&gt;</span>L, <span class="function"><span class="title">fs</span>-&gt;</span>ls);</span><br><span class="line"> <span class="number">557</span>         <span class="function"><span class="title">suffixedexp</span>(fs-&gt;</span><span class="function"><span class="title">ls</span>-&gt;</span>L, <span class="function"><span class="title">fs</span>-&gt;</span>ls, fs, &amp;lh.v);</span><br><span class="line"> <span class="number">558</span> </span><br><span class="line"> <span class="number">559</span>         assignment(fs, &amp;lh, nvars + <span class="number">1</span>);</span><br><span class="line"> <span class="number">560</span>     &#125;</span><br><span class="line"> <span class="number">561</span>     <span class="function"><span class="title">else</span> <span class="keyword">if</span> (fs-&gt;</span><span class="function"><span class="title">ls</span>-&gt;</span>t.token == <span class="string">'='</span>) &#123;</span><br><span class="line"> <span class="number">562</span>         <span class="function"><span class="title">luaX_next</span>(fs-&gt;</span><span class="function"><span class="title">ls</span>-&gt;</span>L, <span class="function"><span class="title">fs</span>-&gt;</span>ls);</span><br><span class="line"> <span class="number">563</span>         int nexps = explist(fs, &amp;e);</span><br><span class="line"> <span class="number">564</span>         adjust_assign(fs, nvars, nexps, &amp;e);</span><br><span class="line"> <span class="number">565</span>     &#125;</span><br><span class="line"> <span class="number">566</span>     <span class="keyword">else</span> &#123;</span><br><span class="line"> <span class="number">567</span>         <span class="function"><span class="title">luaX_syntaxerror</span>(fs-&gt;</span><span class="function"><span class="title">ls</span>-&gt;</span>L, <span class="function"><span class="title">fs</span>-&gt;</span>ls, <span class="string">"syntax error"</span>);</span><br><span class="line"> <span class="number">568</span>     &#125;</span><br><span class="line"> <span class="number">569</span> </span><br><span class="line"> <span class="number">570</span>     <span class="function"><span class="title">init_exp</span>(&amp;e, VNONRELOC, fs-&gt;</span>freereg - <span class="number">1</span>);</span><br><span class="line"> <span class="number">571</span>     <span class="function"><span class="title">luaK_storevar</span>(fs, &amp;v-&gt;</span>v, &amp;e);</span><br><span class="line"> <span class="number">572</span> &#125;</span><br></pre></td></tr></table></figure>
<p>代码片段9<br>我们可以看到assignment是递归调用的，我现在将通过一个例子，来阐述赋值语句的编译流程：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">a</span>, <span class="selector-tag">b</span> = c, d</span><br></pre></td></tr></table></figure>
<p>我们通过图88来说明这个流程：<br><img src="assignment.png" alt="image">图88<br>从图88，我现在梳理出如下的信息：</p>
<ul>
<li>在赋值语句中，等号左边的variable是直接被存储到expdesc结构中，并不会立即释放，生成虚拟机指令。等号右边的explist，会先生成虚拟机指令。</li>
<li>在explist编译完成之后，就会逐一从离等号最近的variable开始赋值</li>
</ul>
<p>完成整个编译以后，得到图89的结果：<br><img src="assignment-2.png" alt="image">图89<br>赋值顺序，是根据variable列表，从右到左的顺序，赋值的内容，则是从freereg-1开始，朝R(0)方向的顺序。到现在为止，我就完成了assignment编译流程的论述了。</p>
<h1 id="虚拟机部分"><a href="#虚拟机部分" class="headerlink" title="虚拟机部分"></a>虚拟机部分</h1><p>​    我们前面已经阐述过，虚拟机指令的操作流程，这里不再赘述，虚拟机指令实现全部在luavm.h|c文件内，读者可以到这些文件中去查阅相关的操作，这里不再进行说明，因为通过前面的指令说明，再结合源码，相信读者可以很容易地就消化理解。</p>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>​    到现在为止，我就完成了【构建Lua解释器：构建完整的语法分析器(上)】的论述。本篇我花费了巨量的精力在exprsatat的编译流程的论述之上。我们首先探讨了编译器的概念，编译器的组成，然后探讨了lua编译器的工作，以及本篇要实现编译器的哪些部分，后来我们又探索了exprstat的编译流程，包括expr、suffixedexp和assignment的构造和编译流程。本文并未展示过多的代码，而是通过EBNF范式描述lua的语法，然后仅仅只是展现了主要流程的代码，最后将一些关键函数，通过一系列构图的方式，展现其编译流程。抽象这些操作能够让我们更好地理解lua parser的运作流程，下篇，我将完整实现lua的编译器。</p>
<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><p>[1] <a href="https://en.wikipedia.org/wiki/Abstract_syntax_tree" rel="external nofollow noopener noreferrer" target="_blank">Abstract Syntax Tree</a><br>[2] 《Compiler Construction Principles And Practice》 3.1 THE PARSING PROCESS<br>[3] <a href="https://web.stanford.edu/class/archive/cs/cs143/cs143.1128/handouts/230 Intermediate Rep.pdf" rel="external nofollow noopener noreferrer" target="_blank">Intermediate Representation</a> More likely, a tree representation used as an IR is not quite the literal parse tree (intermediate nodes may be collapsed, groupings units can be dispensed with, etc.), but it is winnowed down to the structure sufficient to drive the semantic processing and code generation. Such a tree is usually referred to as an abstract syntax tree. (The opposite,a tree that provides a 1:1 mapping without collapsed nodes and with all terminals at the leaves is called a concrete syntax tree.) In the programming projects so far, you have already been immersed in creating and manipulating such an abstract syntax tree. Each node represents a piece of the program structure and the node will have references to its children subtrees (or none if the node is a leaf) and possibly also have a reference to its parent.<br>[4] <a href="https://www.quora.com/Whats-the-difference-between-a-statement-and-an-expression-in-Python-Why-is-print-‘hi’-a-statement-while-other-functions-are-expressions" rel="external nofollow noopener noreferrer" target="_blank">What’s the difference between a statement and an expression in Python? Why is print(‘hi’) a statement while other functions are expressions?</a><br>[5] <a href="https://www.geeksforgeeks.org/tree-traversals-inorder-preorder-and-postorder/" rel="external nofollow noopener noreferrer" target="_blank">Tree Traversals</a><br>[6] <a href="https://en.wikipedia.org/wiki/Abstract_syntax_tree" rel="external nofollow noopener noreferrer" target="_blank">Abstract Syntax Tree</a><br>[7] <a href="https://www.lua.org/pil/1.1.html" rel="external nofollow noopener noreferrer" target="_blank">Chunks</a><br>[8] <a href="https://stackoverflow.com/questions/4728073/what-is-the-difference-between-an-expression-and-a-statement-in-python" rel="external nofollow noopener noreferrer" target="_blank">What is the difference between an expression and a statement in Python?</a></p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>构建Lua解释器Part5：脚本运行基础架构的设计与实现</title>
    <url>/lua/%E6%9E%84%E5%BB%BALua%E8%A7%A3%E9%87%8A%E5%99%A8Part5/</url>
    <content><![CDATA[<p><a href="http://manistein.club/post/program/let-us-build-a-lua-interpreter/%E6%9E%84%E5%BB%BAlua%E8%A7%A3%E9%87%8A%E5%99%A8part5/" rel="external nofollow noopener noreferrer" target="_blank">本文转载自Manistein’s Blog</a></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>   在经过漫长的准备，我终于完成了《构建Lua解释器》系列的第五部分的代码编写，并且通过了预定的测试。本篇我将对<a href="https://github.com/Manistein/dummylua-tutorial" rel="external nofollow noopener noreferrer" target="_blank">dummylua</a>项目中的脚本运行基础架构的设计与实现进行介绍和解析，它包括了lua编译器基础架构以及虚拟机基础架构。过去4个部分的开发，为本部分提供了坚实且必要的基础，本篇将为编译与运行lua脚本搭建最基础的架构，后续内容将在此架构上继续填充与丰富，本部分，我们的目标是编译并运行如下所示的一段脚本代码：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-- lua script</span><br><span class="line">-- <span class="built_in">test</span> file is <span class="keyword">in</span> scripts/part05_test.lua</span><br><span class="line"><span class="built_in">print</span>(<span class="string">"hello world"</span>)</span><br></pre></td></tr></table></figure></p>
<p>图 1<br>尽管这是一段简单的脚本代码，但是我们要达到目标，需要设计与实现虚拟机基础架构，编译器基础架构以及标准库加载机制等，本身所涉及的工作内容并不轻松，我们需要在原有C语言的工程里实现这些内容，而最终我们的测试用例的C语言代码将像如下所示那样：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> 	<span class="comment">// test case is in test/p5_test.c</span></span><br><span class="line"><span class="number">2</span> 	<span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"p5_test.h"</span></span></span><br><span class="line"><span class="number">3</span> 	<span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"../common/lua.h"</span></span></span><br><span class="line"><span class="number">4</span> 	<span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"../clib/luaaux.h"</span></span></span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">6</span> 	<span class="function"><span class="keyword">void</span> <span class="title">p5_test_main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="number">7</span>		<span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">L</span> = <span class="title">luaL_newstate</span>();</span></span><br><span class="line"><span class="number">8</span> 		luaL_openlibs(L);</span><br><span class="line"><span class="number">9</span></span><br><span class="line"><span class="number">10</span> 		<span class="keyword">int</span> ok = luaL_loadfile(L, <span class="string">"../dummylua/scripts/part05_test.lua"</span>);</span><br><span class="line"><span class="number">11</span> 		<span class="keyword">if</span> (ok == LUA_OK) &#123;</span><br><span class="line"><span class="number">12</span> 			luaL_pcall(L, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line"><span class="number">13</span> 		&#125;</span><br><span class="line"><span class="number">14</span></span><br><span class="line"><span class="number">15</span> 		luaL_close(L);</span><br><span class="line"><span class="number">16</span> 	&#125;</span><br></pre></td></tr></table></figure></p>
<p>图 2<br>如上面的代码所示，第7行代码，创建了一个lua_State实例，这个实例保存了lua虚拟机里所有要用到的变量。第8行代码，则为该lua_State加载了标准库。第10行的代码，则对part05_test.lua里的lua代码进行编译，生成虚拟机指令保存在lua_State数据实例中。最后，第12行则是运行已经编译好的虚拟机指令。第15行则是释放lua_State相关联的数据实例，实际上是销毁该lua_State实例。事实上，为了编译与运行lua脚本，我们的编译器和虚拟机都是用C语言来实现的，我们主要的工作就是通过C来实现这些功能。从上面所示的一段C代码我们可以了解到，我们的标准库加载逻辑是在luaL_openlibs函数中实现，编译器逻辑是在luaL_loadfile函数里实现，而虚拟机运作逻辑则是在luaL_pcall函数里实现。本篇博文，我将逐步丰富和拓展这几个函数。<br>  dummylua基本是参照lua5.3的标准来设计与实现，由于整个工程是我凭借理解重新编写，因此不会在所有的细节上和lua5.3保持一致，但是尽可能在运行结果上与其保持一致，但是我相信，通过阅读整个系列，能够帮助大家理解和梳理lua的基本的设计思路和一些实现细节，最后回归官方源码的时候能够达到事半功倍的效果。</p>
<h1 id="Lua解释器的整体运作流程"><a href="#Lua解释器的整体运作流程" class="headerlink" title="Lua解释器的整体运作流程"></a>Lua解释器的整体运作流程</h1><p>  在开始深入探讨我们的设计与实现之前，我们先来回顾一下Lua解释器是怎么运作的。图3所示的是Lua解释器的运行示意图，该图援引《The Lua Architecture》[1]一文中的一张图<br><img src="initializing Lua and loading a script file.png" alt><br>上图展示了官方Lua解释器的运作流程，主要可以分为3个大部分，首先我们要使用lua的应用程序，通过lua提供的API，创建lua解释器实例，图3中的“Create Lua Interpreter”流程则展示了这一点。然后将会为该解释器实例注册标准库，图3中的“Register Std Libs”则说明了这一点。在完成标准库注册以后，我们需要加载lua脚本，并且编译它生成虚拟机指令，图3中的“Load File”阶段则做了这件事情。这个图没有展示的非常重要的一步，就是运行是需要调用另外的接口去运行这些编译好的虚拟机指令。<br>   结合图2所示的代码，图3中的“Create Lua Interpreter”流程，对应了图2中第7行代码，创建一个解释器实例，这个解释器实例，即包含了虚拟机要用到的变量和结构，也包含了lua编译器要用到的变量和结构，我们将其称之为虚拟机实例也是可以的。图2中的第8行代码，调用了luaL_openlibs接口，对应了图3中“Register Std Libs”的操作，即注册了标准库。而接下来的第10行代码，调用了luaL_loadfile接口，它对应了图3中的“Load File”所对应的步骤，这个函数主要做了几件事情，首先是将lua文件里的代码读出，然后通过lexer和parser进行编译，编译所得的虚拟机指令保存在lua解释器实例中。最后，通过图2中的第12行代码，通过调用luaL_pcall函数来执行编译后的结果，于是我们的整个运行流程就完成了。<br>   dummylua作为研究学习官方lua的项目，它的组织方式有所不同，首先所有供应用程序(Application)调用的api，一律加上luaL前缀，L代表Library。截止到本篇为止，dummylua工程的目录组织如图4所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dummylua</span><br><span class="line">	- clib</span><br><span class="line">		luaaux.c 			// 提供给应用程序使用的api，一般以luaL作为开头</span><br><span class="line">		luaaux.h</span><br><span class="line">	- common</span><br><span class="line">		lua.h   			// 基本定义头文件，包括最基本的宏</span><br><span class="line">		luabase.c 			// 提供最基本的标准库</span><br><span class="line">		luabase.h</span><br><span class="line">		luainit.c 			// 包含加载标准库的入口函数</span><br><span class="line">		luamem.c 			// 分配释放内存相关</span><br><span class="line">		luamem.h</span><br><span class="line">		luaobject.c 		// lua数据类型的数据结构定义以及相关的操作</span><br><span class="line">		luaobject.h</span><br><span class="line">		luastate.c  		// lua_State数据实例相关的初始化，销毁和操作</span><br><span class="line">		luastate.h</span><br><span class="line">		luastring.c 		// lua字符串相关</span><br><span class="line">		luastring.h</span><br><span class="line">		luatable.c 			// lua table相关</span><br><span class="line">		luatable.h</span><br><span class="line">	- compiler</span><br><span class="line">		luacode.c 			// 生成虚拟机指令(bytecode instructions)的函数集合</span><br><span class="line">		luacode.h</span><br><span class="line">		lualexer.c  		// 词法分析器</span><br><span class="line">		lualexer.h</span><br><span class="line">		luaparser.c 		// 语法分析器</span><br><span class="line">		luaparser.h</span><br><span class="line">		luazio.c 			// 供词法分析器使用，每次从文件流中读取指定数量的字符，避免编译阶段就加载整个文件，</span><br><span class="line">							// 提升排错效率</span><br><span class="line">		luazio.h</span><br><span class="line">	+ scripts 				// lua脚本，包括测试用例</span><br><span class="line">	+ <span class="built_in">test</span> 					// 基于c语言的测试用例</span><br><span class="line">	- vm</span><br><span class="line">		luado.c  			// 函数调用相关</span><br><span class="line">		luado.h</span><br><span class="line">		luafunc.c           // 创建函数相关的数据结构实例</span><br><span class="line">		luafunc.h</span><br><span class="line">		luagc.c             // gc处理模块</span><br><span class="line">		luagc.h</span><br><span class="line">		luaopcodes.c        // bytecode的一些定义</span><br><span class="line">		luaopcodes.h</span><br><span class="line">		luavm.c   			// 虚拟机运作逻辑</span><br><span class="line">		luavm.h</span><br><span class="line">	main.c</span><br><span class="line">	makefile</span><br></pre></td></tr></table></figure></p>
<p>图4<br>图4展示了截止到目前为止，dummylua的模块组织以及其用途，图5则展示了dummylua的整体运作流程：<br><img src="run_brief_sequence.png" alt><br>上图所示的4个函数，分别与图2中的代码调用一一对应，我们的目标则是从这几个接口出发，逐步深入开发其内部涉及到的步骤和模块，以最终达到我们在上一小节中定下的目标。后面的小节我们将逐步实现相关的内容。</p>
<h1 id="Bytecode分析工具"><a href="#Bytecode分析工具" class="headerlink" title="Bytecode分析工具"></a>Bytecode分析工具</h1><p>   分析和研究lua解释器，个人认为最有效的方式则是从虚拟机的运作方式开始研究，然后再深入编译器部分，实际上网络上很多资料都是从研究lua虚拟机开始的，而要很好的研究lua虚拟机的运作方式，我们首先要知道lua源码经过编译后，它将以什么样的方式呈现。lua源码，在经过lua编译器编译以后，会生成所谓的bytecode，而这个bytecode会在lua虚拟机里运行，把它们的组织方式展现出来，并加以研究，能够极大促进我们对虚拟机的理解。到目前为止，我还没介绍到底什么是bytecode，按照Wikipedia Bytecode词条[2]的解释，它是经过编译器生成的一种供解释器运行的编码，其本质就是一种中间表示(Intermediate Representation)</p>
<blockquote>
<p>Bytecode, also termed portable code or p-code, is a form of instruction set designed for efficient execution by a software interpreter. Unlike human-readable source code, bytecodes are compact numeric codes, constants, and references (normally numeric addresses) that encode the result of compiler parsing and performing semantic analysis of things like type, scope, and nesting depths of program objects.</p>
</blockquote>
<p>Wikipedia的词条，对bytecode的起源，更是加以清晰的解释，这使我们对bytecode的理解则更加清晰</p>
<blockquote>
<p>The name bytecode stems from instruction sets that have one-byte opcodes followed by optional parameters. Intermediate representations such as bytecode may be output by programming language implementations to ease interpretation, or it may be used to reduce hardware and operating system dependence by allowing the same code to run cross-platform, on different devices. Bytecode may often be either directly executed on a virtual machine (a p-code machine i.e., interpreter), or it may be further compiled into machine code for better performance.</p>
</blockquote>
<p>词条上表示，bytecode起源于指令集(Instruction Sets)的编码方式，即一个表示指令操作的opcode，和一些可被选择的操作数，编码到一个定长的字节序中。它出现的目的是为了减少被编译的程序，对机器的依赖，从而实现跨平台运行。由此，我们可以理解为，所谓的bytecode就是能够被虚拟机的解释器识别并运行的指令。<br>   事实上，官方有提供一个独立的lua编译器，它就是luac，这个编译器能够将lua源码编译后，将bytecode保存为一个二进制文件，这个文件可以在我们需要的时候，直接提供给解释器加载运行。这种方式能够节约我们编译的时间，但是不能够提升我们运行时的效率，因为lua在直接加载脚本并运行的模式中，也是先将lua脚本编译成bytecode，再交给虚拟机去运行[3]。luac也提供了对二进制文件反编译的功能，它能够还原lua脚本在内存中的结构，例如我们有一个test脚本，位于luac的上级目录，那么要对它进行编译，然后再反编译的指令则如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./luac -l -l -v ../test.lua</span><br></pre></td></tr></table></figure></p>
<p>test.lua的代码是：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">"hello world"</span>)</span><br></pre></td></tr></table></figure></p>
<p>那么最终输出的结果则是：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Lua 5.3.4  Copyright (C) 1994-2017 Lua.org, PUC-Rio</span><br><span class="line"></span><br><span class="line">main &lt;../test.lua:0,0&gt; (4 instructions at 0xe35e30)</span><br><span class="line">0+ params, 2 slots, 1 upvalue, 0 locals, 2 constants, 0 <span class="built_in">functions</span></span><br><span class="line">        1       [1]     GETTABUP        0 0 -1  ; _ENV <span class="string">"print"</span></span><br><span class="line">        2       [1]     LOADK           1 -2    ; <span class="string">"hello world"</span></span><br><span class="line">        3       [1]     CALL            0 2 1</span><br><span class="line">        4       [1]     RETURN          0 1</span><br><span class="line">constants (2) <span class="keyword">for</span> 0xe35e30:</span><br><span class="line">        1       <span class="string">"print"</span></span><br><span class="line">        2       <span class="string">"hello world"</span></span><br><span class="line">locals (0) <span class="keyword">for</span> 0xe35e30:</span><br><span class="line">upvalues (1) <span class="keyword">for</span> 0xe35e30:</span><br><span class="line">        0       _ENV    1       0</span><br></pre></td></tr></table></figure></p>
<p>  虽然官方已经提供了反编译的工具，但是排版比较乱，同时我们也希望能够让输出的指令自动加上注释，方便我们阅读和分析，尤其是应对比较复杂的脚本的情况下。为此，我专门写了一个工具，这个工具能够将官方lua的代码，直接转换成易于我们查看的方式，展示对应lua脚本代码经过编译以后的虚拟机指令，也就是对luac编译好的二进制流进行反编译。读者可以从这个地址里下载到该工具。这个工具是模仿ChunkSpy的反编译工具，不过与ChunkSpy不同的是，它支持的版本是lua5.1，而我写的protodump支持的版本是lua5.3。目前只有linux上的版本，暂时不考虑出windows的版本。<br>  使用它的方式也非常简单，在下载工具以后，进入工具的目录，你可以输入如下命令，先去编译lua源码，然后再去生成反编译的结果：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-- 如果你是首次使用这个工具，那么需要先编译lua源码，生成lua和luac这两个可执行文件</span><br><span class="line">-- 如果在protodump目录下已经有这两个文件，那么不需要每次使用都重新编译</span><br><span class="line">./build.sh</span><br><span class="line"></span><br><span class="line">-- 将lua源码dump成易于分析的信息</span><br><span class="line">./dump.sh path_to_script</span><br></pre></td></tr></table></figure></p>
<p>其中path_to_script则是你要进行分析的脚本路径，现在假设，我们有一个test.lua脚本，在protodump工具的上层目录，其代码如图7所示：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- test.lua</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">"hello world"</span>)</span><br></pre></td></tr></table></figure></p>
<p>图7<br>那么我们输入./dump.sh ../test.lua命令，会在protodump目录里生成一个lua.dump文件，打开它，我们可以看到脚本对应的虚拟指令信息：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1 lua.dump                                                                                                                                                                                                                            </span><br><span class="line">file_name = ../test.lua.out</span><br><span class="line">+proto+maxstacksize [2]</span><br><span class="line">|     +k+1 [<span class="built_in">print</span>]</span><br><span class="line">|     | +2 [hello world]</span><br><span class="line">|     +locvars</span><br><span class="line">|     +lineinfo+1 [1]</span><br><span class="line">|     |        +2 [1]</span><br><span class="line">|     |        +3 [1]</span><br><span class="line">|     |        +4 [1]</span><br><span class="line">|     +p</span><br><span class="line">|     +numparams [0]</span><br><span class="line">|     +upvalues+1+idx [0]</span><br><span class="line">|     |          +instack [1]</span><br><span class="line">|     |          +name [_ENV]</span><br><span class="line">|     +<span class="built_in">source</span> [@../test.lua]</span><br><span class="line">|     +is_vararg [1]</span><br><span class="line">|     +linedefine [0]</span><br><span class="line">|     +code+1 [iABC:OP_GETTABUP      A:0   B  :0   C:256 ; R(A) := UpValue[B][RK(C)]]</span><br><span class="line">|     |    +2 [iABx:OP_LOADK         A:1   Bx :1         ; R(A) := Kst(Bx)]</span><br><span class="line">|     |    +3 [iABC:OP_CALL          A:0   B  :2   C:1   ; R(A), ... ,R(A+C-2) := R(A)(R(A+1), ... ,R(A+B-1))]</span><br><span class="line">|     |    +4 [iABC:OP_RETURN        A:0   B  :1         ; <span class="built_in">return</span> R(A), ... ,R(A+B-2)  (see note)]</span><br><span class="line">|     +type_name [Proto]</span><br><span class="line">|     +lastlinedefine [0]</span><br><span class="line">+upvals+1+name [_ENV]</span><br><span class="line">+type_name [LClosure]</span><br></pre></td></tr></table></figure></p>
<p>图8展示了该脚本编译以后的信息，code列表展示的则是它将在虚拟机里运行的指令，’;‘后是自动生成的与指令相关的注释，这也算是对ChunkSpy的一种改进吧:)。接下来，我将对上面的结构作简要说的说明，以方便大家更好的使用这个工具。<br>        图8展示的是一种被我们称之为LClosure的结构，他实际上是一个function类型，是lua脚本在编译时，解释器为其创建的一个用于容纳编译结果的一个数据结构，它包含一个Proto类型的实例，和一个upvalue列表，现在将对其内容进行解释和说明：</p>
<ul>
<li>proto：我们的proto结构，对应的是一个function，每个function都包含一个proto结构的实例，proto结构包含了函数要用到的常量表，local变量表，指令信息，调试信息等。对于一个lua脚本而言，脚本本身就是一个top level function，因此一个脚本被编译以后，也会有一个对应的proto结构实例。<ul>
<li>maxstacksize：proto所对应的函数的栈的大小。</li>
<li>k：存放脚本里的常量，所有的常量都会被放入这个脚本，它虽然是一个列表，更是一个常量集合，多个值相同的常量，只会在列表里出现一次。</li>
<li>localvars：函数的local变量表，包含的信息包括local变量的名称和位置。</li>
<li>lineinfo：列表下标表示的是code列表里的那个指令，中括号里的数字表示该指令所对应的源码的line number。</li>
<li>p：在一个函数内部定义的函数的结构列表，其本质就是一个proto结构列表</li>
<li>numparams：函数的参数个数</li>
<li>upvalues：这个列表里存放的不是upvalue实例，而是记录upvalue的信息，也用于调试时使用，instack表示其是在lua栈上，还是在function的upvalue实例列表里。</li>
<li>source：只有top level function里的source才有具体的值，也就是lua脚本的路径和名称。</li>
<li>is_vararg：表示是否是可变参函数，1表示是，0表示不是。</li>
<li>code：bytecode列表，包含虚拟机可以运行的指令列表。</li>
<li>type_name：类型名称</li>
<li>upvals：包含的是真实的upval实例的列表。</li>
<li>type_name：表示这个结构是一个LClosure类型的实例。<br>上面对protodump工具生成的结果，进行了解释和说明，它有助于我们分析官方lua源码的编译结果，对于研究lua的编译器部分，有极大的帮助。</li>
</ul>
</li>
</ul>
<h1 id="虚拟机基础设计与实现"><a href="#虚拟机基础设计与实现" class="headerlink" title="虚拟机基础设计与实现"></a>虚拟机基础设计与实现</h1><p>  接下来，我们就要开始构建dummylua的虚拟机基础架构了，事实上，我在第一篇<a href="https://manistein.github.io/blog/post/program/let-us-build-a-lua-interpreter/%E6%9E%84%E5%BB%BAlua%E8%A7%A3%E9%87%8A%E5%99%A8part1/" rel="external nofollow noopener noreferrer" target="_blank">【构建Lua解释器Part1：虚拟机的基础–Lua基本数据结构、栈和基于栈的C函数调用的设计与实现】</a>就已经设计和定义了虚拟机要用到的基本数据结构，以及其基本运作机制了，但是这篇涉及的主要是light c function在lua栈中的调用流程，本篇我们将把lua脚本的执行，也纳入到原有的机制上，如果你对那篇文章已经生疏，或者完全没有看过它，我建议先回顾一下这篇文章，这样有利于读者理解接下来的内容。<br>  要理解lua虚拟机的运作流程，首先要理解它的指令编码方式和指令集，然后深入研究支撑其运作的基本数据结构，最后通过阐述其运作机制，最终将整个运作机制清晰地串通起来。我们还会在后面展示实现这套机制的关键代码，并加以说明。</p>
<h1 id="Lua的指令编码方式"><a href="#Lua的指令编码方式" class="headerlink" title="Lua的指令编码方式"></a>Lua的指令编码方式</h1><p> lua所有的指令都会编码到一个32位的字节序中，也就是说lua所有的虚拟机指令都是32位的，其编码方式一共有3种方式，iABC、iABx、iAsBx三种，它们的格式定义如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">+--------+---------+---------+--------+--------+</span><br><span class="line">|  iABC  |    B:9  |   C:9   |   A:8  |Opcode:6|</span><br><span class="line">+--------+---------+---------+--------+--------+</span><br><span class="line">|  iABx  |   Bx:18(unsigned) |   A:8  |Opcode:6|</span><br><span class="line">+--------+---------+---------+--------+--------+</span><br><span class="line">|  iAsBx |   sBx:18(signed)  |   A:8  |Opcode:6|</span><br><span class="line">+--------+---------+---------+--------+--------+</span><br></pre></td></tr></table></figure></p>
<p>上面展示了三种模式的编码，在lua中，以iABC模式进行编码的指令，占据了绝大多数。这里对不同的部分进行解释和说明，首先Opcode表示操作指令，它占据6个bit位，剩下的则表示参数信息；A表示函数栈中，哪个位置将作为RA寄存器，它占据8个bit位，这个RA寄存器一般是操作的目标所在，任何模式的指令都需要对RA寄存器进行操作，而在lua虚拟机中，所有的寄存器都是以栈上的某个域来充当的，并不像我们的计算机硬件那样，有专门的寄存器独立于栈之外。<br>  除了sBx，其他所有的域都是无符号整数表示，而且sBx域，也不是采用二进制补码的方式进行编码，它采用了一个basis值作为0值，这个basis值是Bx的最大值除以2得到，即262143 &gt;&gt; 1，也就是131071。如果sBx表示-1时，那么它的值则是-1 + 131071 = 131070 = 0x1FFFE[4]，也就是说0表示sBx的最小值-131071，0x3FFFF表示sBx的最大值131072。<br>  正如前面所说的那样，我们绝大多数虚拟机指令是iABC模式，采用iABx模式的指令主要有OP_LOADK和OP_CLOSURE，OP_LOADK之所以要采用iABx指令，是因为其参数常量列表的下标，如果它采用iABC模式，那么有9个bit会被浪费掉，而仅仅依靠9个bit，最多只能支持到512个常量，这是远远不够的。而OP_CLOSURE的参数是从Proto列表里，找到与之关联的proto实例，我们同样不可能只限制我们的脚本里只能定义最多不超过512个函数，因此它也是iABx模式。采用iAsBx模式的指令，大多数是需要跳转的指令，比如jump、判断和循环相关的指令。如果读者对目前涉及到的一些内容感到困惑，并不要紧，本篇后续内容以及后续章节，我会慢慢丰富这些内容。</p>
<h1 id="Lua5-3中的指令集"><a href="#Lua5-3中的指令集" class="headerlink" title="Lua5.3中的指令集"></a>Lua5.3中的指令集</h1><p>   接下来我将展示lua5.3的指令集合，dummylua的目标，是最终实现这些指令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-------------------------------------------------------------------------</span><br><span class="line">-- name                         mode    description</span><br><span class="line">------------------------------------------------------------------------</span><br><span class="line">OP_MOVE                         A B     R(A) := R(B)                    </span><br><span class="line">OP_LOADK                        A Bx    R(A) := Kst(Bx)                 </span><br><span class="line">OP_LOADKX                       A       R(A) := Kst(extra arg)              </span><br><span class="line">OP_LOADBOOL                     A B C   R(A) := (Bool)B; <span class="keyword">if</span> (C) pc++            </span><br><span class="line">OP_LOADNIL                      A B     R(A), R(A+1), ..., R(A+B) := nil        </span><br><span class="line">OP_GETUPVAL                     A B     R(A) := UpValue[B]              </span><br><span class="line"></span><br><span class="line">OP_GETTABUP                     A B C   R(A) := UpValue[B][RK(C)]           </span><br><span class="line">OP_GETTABLE                     A B C   R(A) := R(B)[RK(C)]             </span><br><span class="line"></span><br><span class="line">OP_SETTABUP                     A B C   UpValue[A][RK(B)] := RK(C)          </span><br><span class="line">OP_SETUPVAL                     A B     UpValue[B] := R(A)              </span><br><span class="line">OP_SETTABLE                     A B C   R(A)[RK(B)] := RK(C)                </span><br><span class="line"></span><br><span class="line">OP_NEWTABLE                     A B C   R(A) := &#123;&#125; (size = B,C)             </span><br><span class="line"></span><br><span class="line">OP_SELF                         A B C   R(A+1) := R(B); R(A) := R(B)[RK(C)]     </span><br><span class="line"></span><br><span class="line">OP_ADD                          A B C   R(A) := RK(B) + RK(C)               </span><br><span class="line">OP_SUB                          A B C   R(A) := RK(B) - RK(C)               </span><br><span class="line">OP_MUL                          A B C   R(A) := RK(B) * RK(C)               </span><br><span class="line">OP_MOD                          A B C   R(A) := RK(B) % RK(C)               </span><br><span class="line">OP_POW                          A B C   R(A) := RK(B) ^ RK(C)               </span><br><span class="line">OP_DIV                          A B C   R(A) := RK(B) / RK(C)               </span><br><span class="line">OP_IDIV                         A B C   R(A) := RK(B) // RK(C)              </span><br><span class="line">OP_BAND                         A B C   R(A) := RK(B) &amp; RK(C)               </span><br><span class="line">OP_BOR                          A B C   R(A) := RK(B) | RK(C)               </span><br><span class="line">OP_BXOR                         A B C   R(A) := RK(B) ~ RK(C)               </span><br><span class="line">OP_SHL                          A B C   R(A) := RK(B) &lt;&lt; RK(C)              </span><br><span class="line">OP_SHR                          A B C   R(A) := RK(B) &gt;&gt; RK(C)              </span><br><span class="line">OP_UNM                          A B     R(A) := -R(B)                   </span><br><span class="line">OP_BNOT                         A B     R(A) := ~R(B)                   </span><br><span class="line">OP_NOT                          A B     R(A) := not R(B)                </span><br><span class="line">OP_LEN                          A B     R(A) := length of R(B)              </span><br><span class="line"></span><br><span class="line">OP_CONCAT                       A B C   R(A) := R(B).. ... ..R(C)           </span><br><span class="line"></span><br><span class="line">OP_JMP                          A sBx   pc+=sBx; <span class="keyword">if</span> (A) close all upvalues &gt;= R(A - 1)  </span><br><span class="line">OP_EQ                           A B C   <span class="keyword">if</span> ((RK(B) == RK(C)) ~= A) <span class="keyword">then</span> pc++        </span><br><span class="line">OP_LT                           A B C   <span class="keyword">if</span> ((RK(B) &lt;  RK(C)) ~= A) <span class="keyword">then</span> pc++        </span><br><span class="line">OP_LE                           A B C   <span class="keyword">if</span> ((RK(B) &lt;= RK(C)) ~= A) <span class="keyword">then</span> pc++        </span><br><span class="line"></span><br><span class="line">OP_TEST                         A C     <span class="keyword">if</span> not (R(A) &lt;=&gt; C) <span class="keyword">then</span> pc++           </span><br><span class="line">OP_TESTSET                      A B C   <span class="keyword">if</span> (R(B) &lt;=&gt; C) <span class="keyword">then</span> R(A) := R(B) <span class="keyword">else</span> pc++ </span><br><span class="line"></span><br><span class="line">OP_CALL                         A B C   R(A), ... ,R(A+C-2) := R(A)(R(A+1), ... ,R(A+B-1)) </span><br><span class="line">OP_TAILCALL                     A B C   <span class="built_in">return</span> R(A)(R(A+1), ... ,R(A+B-1))      </span><br><span class="line">OP_RETURN                       A B     <span class="built_in">return</span> R(A), ... ,R(A+B-2)  (see note)  </span><br><span class="line"></span><br><span class="line">OP_FORLOOP                      A sBx   R(A)+=R(A+2);</span><br><span class="line">                                        <span class="keyword">if</span> R(A) &lt;= R(A+1) <span class="keyword">then</span> &#123; pc+=sBx; R(A+3)=R(A) &#125;</span><br><span class="line">OP_FORPREP                      A sBx   R(A)-=R(A+2); pc+=sBx               </span><br><span class="line"></span><br><span class="line">OP_TFORCALL                     A C     R(A+3), ... ,R(A+2+C) := R(A)(R(A+1), R(A+2));  </span><br><span class="line">OP_TFORLOOP                     A sBx   <span class="keyword">if</span> R(A+1) ~= nil <span class="keyword">then</span> &#123; R(A)=R(A+1); pc += sBx &#125;</span><br><span class="line"></span><br><span class="line">OP_SETLIST                      A B C   R(A)[(C-1)*FPF+i] := R(A+i), 1 &lt;= i &lt;= B    </span><br><span class="line"></span><br><span class="line">OP_CLOSURE                      A Bx    R(A) := closure(KPROTO[Bx])         </span><br><span class="line"></span><br><span class="line">OP_VARARG                       A B     R(A), R(A+1), ..., R(A+B-2) = vararg        </span><br><span class="line"></span><br><span class="line">OP_EXTRAARG                     Ax      extra (larger) argument <span class="keyword">for</span> previous opcode</span><br></pre></td></tr></table></figure></p>
<p>上述列表，展示了lua-5.3.5的指令集，name这一列展示了OPCODE的名称，中间的mode这一列则展示了它的模式，而右边的description则对该指令的操作进行了解释和说明。这里将对里面的一些特殊的符号和参数进行说明，首先需要说明的是，上面的A、B、C、Bx以及sBx都是指令里指定域的值，下面对特殊的符号和函数进行说明：</p>
<ul>
<li>R( A ):寄存器A，一般是操作目标，由指令中的A域的值，指定该寄存器在栈中的位置</li>
<li>R( B ):寄存器B，一般是操作数，由指令(iABC模式)中的B域的值，指定该寄存器在栈中的位置</li>
<li>R( C ):寄存器C，一般是操作数，由指令(iABC模式)中的C域的值，指定该寄存器在栈中的位置</li>
<li>PC:PC寄存器，指向当前执行指令的地址</li>
<li>Kst(n):从常量表中取出第n个值</li>
<li>Upvalues[n]:从upvalue实例列表中，取出第n个upvalue实例</li>
<li>Gbl[sym]:sym表示一个符号(数值、地址或字符串等)，从全局表中，取出key为该符号的值</li>
<li>RK( B ):当B的值&gt;=256时，从常量表中取出Kst(B-256)的值，当B&lt;256时，RK(B):=R(B)</li>
<li>RK( C ):同RK(B)，只是将B值替换为C值</li>
<li>sBx:有符号值，它表示所有jump指令的相对位置<br>到目前位置，我介绍了lua虚拟机的整个虚拟指令集，以及对描述其功能的符号和函数进行了简要的解释和说明，从本章节开始，我会陆续详细介绍章节内容涉及到的指令。</li>
</ul>
<h1 id="本章涉及到的指令"><a href="#本章涉及到的指令" class="headerlink" title="本章涉及到的指令"></a>本章涉及到的指令</h1><p>   回顾一下本文开篇的目标，我们的目标是编译并且运行一个只包含”print(‘hello world’)“代码语句的lua脚本，而在前面的章节中，我已经将这段代码的内存表示dump出来了，它即是图8展示的信息。我们可以关注到图8中的code列表，它所包含的指令，正是本章节需要深入研究的指令，接下来我将一一对他们进行说明。</p>
<ul>
<li>OP_GETTABUP R(A) := UpValue[B][RK( C )]<br>  B表示UpValue列表中的索引，从UpValue列表中获取UpValue[B]的值，这个值必须是一个table类型的变量，并且从这个table中，找到key值为RK( C )的value，并赋值到R(A)中。</li>
<li>OP_LOADK R(A) := Kst(Bx)<br>  Bx表示一个无符号整数值，从常量表中，获取index为Bx的常量，并赋值到R(A)中。</li>
<li>OP_CALL R(A), … ,R(A+C-2) := R(A)(R(A+1), … ,R(A+B-1))<br>  这个是调用函数的指令，R(A)表示要被调用的函数，B表示该函数的参数个数，当B&gt;=1的时候，表示该函数R(A)有B-1个参数(R(A+1)~R(A+B-1))，当B为0时，表示从R(A+1)到栈顶都是该函数的参数。C代表该函数R(A)调用后的返回值个数，当C&gt;=1时，则表示该函数有C-1个返回值(R(A)~R(A+C-2))，当C为0时，表示函数调用完以后，从R(A)到栈顶都是其返回值。</li>
<li>OP_RETURN return R(A), … ,R(A+B-2)<br>  每个函数的最后一个指令，就是return指令，该指令主要处理被调用函数的返回值，并且将其安排到栈上合适的位置，该处理与light c function的返回值处理方式类似，R(A)是第一个返回值存放的位置，B的值需要加以特殊说明，当B&gt;=1时，表示函数的返回值是B-1个(R(A)~R(A+B-2))，当B的值为0时，表示其返回值是不定的，从R(A)到栈顶。读到这里，可能读者会有疑惑，既然OP_CALL指令已经指明了返回值，那么OP_RETURN为什么还要去重新指明一次呢？这是因为OP_RETURN是针对被调用函数(callee)做的处理，而OP_CALL指令，则是对调用者(caller)做的处理。<h1 id="运行脚本所需的基本数据结构"><a href="#运行脚本所需的基本数据结构" class="headerlink" title="运行脚本所需的基本数据结构"></a>运行脚本所需的基本数据结构</h1> 在开始介绍虚拟机运行脚本最基础的架构之前，这里需要先介绍几个非常重要的概念和流程。首先我要介绍的是chunk的概念，实际上，官方对chunk的解释是，它是一段能够被lua解释器编译运行的代码，它可以是一个lua脚本文件，或者是在交互模式中，输入的包含一段代码的字符串[5]，我们在图7展示了一段，包含在test.lua里的代码，而这个test.lua里的全部内容，就可以被视作是一个chunk。lua解释器，首先要将文件中的代码读出来，然后将它编译，然后再进行运行。我们的lua脚本源码就是包含在这些chunk中。对chunk进行处理，就是lua解释器运行脚本的起点。不过，在对chunk进行编译之前，首先lua解释器要做的是创建一个LClosure类型的变量，并压入栈顶，如图9所示：<br><img src="vm_basic_01.png" alt><br>LClosure对象，按中文的翻译来说，它就是闭包对象，实质上也是一个函数对象，我们在执行图2的代码时，调用luaL_loadfile函数，首先要做的事情就是创建这个LClosure类型的数据实例，并压入栈顶。有了这个数据实例，解释器接下来就将脚本的文件流读出，也就是我们所说的chunk，然后将其编译，并将编译结果存入LClosure结构实例中的Proto类型的变量中。前面我们也提过，这个Proto结构，就是用来保存编译结果的，后续的虚拟机运行，就是通过读取存在Proto结构里的虚拟机指令来运作。<br><img src="vm_basic_02.png" alt><br>在完成加载和编译以后，就可以调用如图2中的luaL_pcall函数来运行刚刚编译好的虚拟机指令了。<br> 通过上文的论述，我们可以感知到LCLosure结构是需要我们重点研究的数据结构，下面我展示了该结构的代码：<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luaobject.h</span></span><br><span class="line"><span class="comment">// Closure</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ClosureHeader CommonHeader; int nupvalues; struct GCObject* gclist</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LClosure</span> &#123;</span></span><br><span class="line">    ClosureHeader;</span><br><span class="line">    Proto* p;</span><br><span class="line">    UpVal* upvals[<span class="number">1</span>];</span><br><span class="line">&#125; LClosure;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>从上面的代码，我们可以得知，它是一种受gc管控的类型，除去和gc有关的部分(有关gc的内容，可以回顾一下Part2)，LClosure主要包含一个Proto类型的结构，以及一个Upvalue列表，目前我并不打算深入研究Upvalue的机制，现在只需要知道的是，每个Closure的第一个upvalue是一个名为_ENV的变量，它默认指向我们的全局表_G，因此访问_ENV实质上就是访问_G，当然lua也支持我们自定义赋什么值给_ENV。<br>        接下来，让我们看一下Proto这个数据结构：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// common/luaobject.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Proto</span> &#123;</span></span><br><span class="line">    CommonHeader;</span><br><span class="line">    <span class="keyword">int</span> is_vararg;		<span class="comment">/* 表示该LClosure对象的参数，是动态传入的，还是一个指定参数列表的函数，当它的值为1</span></span><br><span class="line"><span class="comment">    					   时表示参数是动态传入，为0时表示不是*/</span></span><br><span class="line">    <span class="keyword">int</span> nparam;			<span class="comment">/* 当is_vararg为0时生效，它表示该函数参数的数量*/</span></span><br><span class="line">    Instruction* code;  <span class="comment">/* Instruction结构，其实是一个typedef的数据类型，它本质是一个int型变量的列表，</span></span><br><span class="line"><span class="comment">    					   Instruction表示的就是虚拟机的指令，这是函数的指令列表*/</span></span><br><span class="line">    <span class="keyword">int</span> sizecode;		<span class="comment">/* 指明code列表的大小*/</span></span><br><span class="line">    TValue* k;			<span class="comment">/* 常量列表*/</span></span><br><span class="line">    <span class="keyword">int</span> sizek;			<span class="comment">/* 常量列表的大小*/</span></span><br><span class="line">    LocVar* locvars;	<span class="comment">/* local变量列表，主要包含的信息包括，local变量的名称，以及它是第几个local变量</span></span><br><span class="line"><span class="comment">    					   local变量是存在栈中的*/</span></span><br><span class="line">    <span class="keyword">int</span> sizelocvar;		<span class="comment">/* local变量列表的大小*/</span></span><br><span class="line">    Upvaldesc* upvalues;<span class="comment">/* upvalue信息列表，主要记录upvalue的名称，以及其所在的位置，并不是upvalue实际</span></span><br><span class="line"><span class="comment">    					   值的列表*/</span></span><br><span class="line">    <span class="keyword">int</span> sizeupvalues; 	<span class="comment">/* upvalue列表大小*/</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Proto</span>** <span class="title">p</span>;</span>	<span class="comment">/* 在内部定义的函数，编译后会生成一个个proto实例，存放在这个列表中*/</span></span><br><span class="line">    <span class="keyword">int</span> sizep;			<span class="comment">/* proto列表的长度*/</span></span><br><span class="line">    TString* source;	<span class="comment">/* 记录的是脚本的路径*/</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">GCObject</span>* <span class="title">gclist</span>;</span></span><br><span class="line">	<span class="keyword">int</span> maxstacksize; 	<span class="comment">/* 栈的最大大小*/</span></span><br><span class="line">&#125; Proto;</span><br></pre></td></tr></table></figure></p>
<p>除了gc相关的变量，其他所有的变量我都在上面的注释里说明了，这里对一些特别的点进行一下说明：<br>1) Instruction类型，其本质就是int类型，是经过typedef修饰的类型，int是4个字节的，32bit位，它实际代表的就是虚拟机的指令。<br>2) k列表是存放脚本常量的列表，什么是常量，即boolean型变量、数值型变量和字符串等。<br>3) proto列表p，它所容纳的是一个函数内部定义的函数的编译结果，比如test.lua脚本的代码如下所示<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- test.lua</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span><span class="params">()</span></span></span><br><span class="line">	<span class="comment">-- Do Something</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span><span class="params">()</span></span></span><br><span class="line">	<span class="comment">-- Do Something</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p>
<p>那么我们第一个创建出来的LClosure实例，的Proto变量，存储的就是test.lua内部编译的结果，而foo和bar函数，在完成编译后，就存放在了列表p里，此时并不会立即为他们创建LClosure实例，而是在调用到这些函数时创建，并与对应的Proto实例关联。<img src="vm_basic_03.png" alt><br>图11说明整个test.lua脚本的编译结果都存储在LClosure实例的Proto结构里，test.lua本身就是被当作一个函数来对待，它是一个被称之为top level function的函数。图中的两个函数，foo和bar，以及其内部定义的逻辑，都被编译到了该Proto实例里的proto列表里，而test.lua的非函数定义的其他逻辑，other code则经过编译变成了一个虚拟机指令集序列，被存放在Proto结构的code列表中，同样的，foo和bar函数内部的指令，则存放在它们所指向的proto结构的code列表里。<br>4) 关于upvalue，proto中的upvalue并不是存放真实的UpVal实例，而是存放的与之关联的信息。<br>        到目前为止，我已经完成了LClosure结构的介绍和说明，实际上，lua中，函数的类型共有3种，第一种就是在part1详细论述的light c function，第二种就是刚刚论述完的LClosure，还有一种就是CClosure。Closure是闭包的意思，LClosure就是lua闭包，而CClosure则是C的闭包，下面我们来看一下它的数据结构：</p>
<p>// luaobject.h<br>typedef struct CClosure {<br>    ClosureHeader;<br>    lua_CFunction f;<br>    UpVal* upvals[1];<br>} CClosure;<br>我们可以看到，它也是一个受gc管理的类型，和LClosure不同的是，除去gc相关的部分，它只包含一个函数指针类型的变量lua_CFunction和一个upvalue列表。这种类型主要在我们注册标准库的时候用到，也就是在调用图2的luaL_openlibs函数的时候，被我们注册的标准库函数的类型，对他的调用方式与light c function并没有什么特别的地方，特别的地方主要在于它受gc管理，并且含有upvalue。</p>
<h1 id="虚拟机运行的基本流程"><a href="#虚拟机运行的基本流程" class="headerlink" title="虚拟机运行的基本流程"></a>虚拟机运行的基本流程</h1><p>   我们已经知道了，在执行图2所示的代码中的luaL_loafile函数以后，我们先后创建了一个LClosure实例并放入栈顶，然后将test.lua脚本的代码编译好以后，并将其存入LClosure结构的Proto变量中以后，我们就可以调用luaL_pcall函数去执行这个编译好的结果了，现在我将正式进入到阐述lua虚拟机运转机制的阶段。首先，我们得展示一下，在执行完luaL_loadfile函数以后，我们虚拟机中的内存布局是怎样的：<br><img src="after_loadfile.png" alt><br>通过上图，我们可以看到，脚本已经被编译好，并存入LClosure实例的Proto结构里了，当我们继续调用图2代码里的luaL_pcall的时候，我们会创建一个新的CallInfo实例，这个实例会为我们调用栈顶的LClosure实例设定好栈的范围，并指定其调用者是谁等信息，在调用luaL_pcall函数以后，我们就得到了图13的结构：<br><img src="pcall.png" alt><br>对于上面展示出来的信息，我们有几点需要注意，首先要注意的一点则是我们的CallInfo结构新增了一个结构体l，这个结构体专门用于处理lua函数调用的情况，目前dummylua中，它一共有两个变量，一个是base指针，这个base指针指向了函数体LClosure实例的下一个栈位置，他的作用是用于指定被调用函数的栈底在哪里。另外savedpc指针。则指向了Proto结构的code列表，此外我们的top指针则根据maxstackszie的大小调整了它的位置，因为我们的指令集指令中，几乎很少直接调用需要对栈push和pop的操作，取而代之的，更多的是set操作，因此把top指针一开始就设置在maxstacksize之外是个不错的选择。<br>   以上是调用LClosure函数，也就是一个lua函数前的准备工作，那么在开始执行这个函数时，lua虚拟机会从最新的CallInfo实例中(也就是指向LClosure实例的CallInfo实例)的l.savepc中取出最新要执行的指令，然后l.savedpc指针会自增，接下来就执行这个指令，其伪代码如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">Instruction i = L-&gt;ci-&gt;l.savedpc;</span><br><span class="line">L-&gt;ci-&gt;l.savedpc++;</span><br><span class="line"></span><br><span class="line"><span class="keyword">switch</span>(GET_OPCODE(i)) &#123;</span><br><span class="line">	<span class="keyword">case</span> OP_LOADK: &#123;</span><br><span class="line">		.....</span><br><span class="line">	&#125;<span class="keyword">break</span>;</span><br><span class="line">	<span class="keyword">case</span> OP_CALL: &#123;</span><br><span class="line">		.....</span><br><span class="line">	&#125;<span class="keyword">break</span>;</span><br><span class="line">	....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们可以看到，其实虚拟机运行指令的机制其实非常简单，其实就是一个switch语句，判定当前指令是哪个指令，最后执行这个指令。当然我们这里省去了一些前置操作，实际上来说，我们将Proto结构里的code列表里的指令，从头到尾执行一次的过程，被称作是一个frame，每次调用一个新的lua函数，就是进入到了一个新的frame。<br>   现在我们结合图1和图2的代码，也就是本章节的示例程序，来对虚拟机的运作流程进行解释和说明。图8是我们的print(“hello world”)脚本经过编译以后的内存组织形式，图14展示了在调用luaL_loadfile函数以后的内存布局：<br><img src="after_loadfile_02.png" alt><br>上图将我们要使用到的结构均以图像的形式展现出来了，关于编译的流程，我会在后面的章节里展示，不过现在主要是集中精力来探讨lua虚拟机的运作流程。在完成load file的操作以后，接下来就是要进行函数调用了，也就是执行我们的luaL_pcall函数。当我们被调用的函数是一个LClosure类型的函数时，luaL_pcall会执行两个步骤，第一个步骤则是创建一个CallInfo实例，并且做好运行虚拟指令的准备工作，这时并没有立刻执行指令，如图15所示：<br><img src="pcall_precall.png" alt><br>我们可以看到，这个准备工作，主要做了几件事情，首先是将lua_State中，指向当前CallInfo的指针ci，指向了新创建的CallInfo实例，这个指针指向谁，就代表当前调用执行的是哪个函数，然后是设置了ci-&gt;l.base的位置，这个位置代表当前被调用函数的栈底，然后设置ci-&gt;l.savedpc指针到Proto内部的指令列表首部，同时还设置了ci-&gt;top指针和L-&gt;top指针，他们被设置到相同的位置上，设置他们的是由Proto结构中，maxstacksize的大小决定。同时我们也可以观察到，图1脚本代码的print和hello world都被当做是常量，存储到了常量表k中，这些常量将会在后续执行过程中被用到。<br>   完成了准备工作以后，就进入到了执行虚拟机指令的运行阶段了，我们可以看到Proto结构的code列表中，已经列好了要被执行的指令，以及它们附带的参数，指令是以OP_作为前缀，后面的数字代表参数。第一个参数表示的是R(A)寄存器，也就是我们的操作目标。进入到运行流程以后，我们将从第一个指令开始执行。第一个指令是OP_GETTAPUP，它是iABC模式，而它的参数是0、0和256，也就是说R(A)的位置在ci-&gt;l.base + 0的位置上，后面的两个值分别是B值和C值。OP_GETTABUP的操作是，从LCLosure的UpVal列表中，取出UpVal[B]的值，然后以RK( C )的值作为key，从UpVal[B]中取出value，放到R(A)中。我们可以看到在这个脚本里，LClosure实例的UpVal列表只有一个值–_ENV，而这个_ENV则指向了全局表_G，也就是说UpVal[B]指向了全局表_G，因为C的值&gt;=256，因此RK( C )将会去Proto的常量表中找对应的key，也就是说我们的R(A):=UpVal[B][RK( C )]实际上是R(A):=UpVal[B][Kst(C-256)]，最后推导得到:<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">R(A):=UpVal[0][Kst(0)] </span><br><span class="line">&lt;===&gt;</span><br><span class="line">R(A):=_G[<span class="string">"print"</span>]</span><br><span class="line">``` </span><br><span class="line">“<span class="built_in">print</span>”字符串，位于Proto常量表k中的第0个位置，因此Kst(0)就是指向“<span class="built_in">print</span>”这个字符串，也就是说我们最终从全局表中，取出key为“<span class="built_in">print</span>”的value，也就是一个CClosure函数对象到R(A)上，也就是ci-&gt;l.base指向的位置，于是我们得到图16的结果。</span><br><span class="line">![](vm_execute.png)</span><br><span class="line">实际上，我们做的第一件事情，就是将全局表中的<span class="built_in">print</span>函数放到寄存器R(A)上，以供我们等下调用，这里我们要注意到ci-&gt;l.savedpc指向的位置，向后偏移了一个位置，它指向的是下一个要被执行的命令。</span><br><span class="line">  接下来，虚拟机就会执行第二个指令，这个指令是OP_LOADK，它是iABx模式，因此它只有两个参数，第一个参数是A值，第二个参数是Bx，也就是说A=1且Bx=1。OP_LOADK的作用就是从常量表中，取出Kst(Bx)的值，赋值到R(A)上，即R(A):=Kst(Bx)，编译器已经帮我们选好了R(A)的位置了，因此这里我们只需要执行就好了。因为A的值是1，因此R(A)的位置是在ci-&gt;l.base + 1的位置之上，也就是<span class="built_in">print</span>函数的上面那个位置，又因为Bx的值是1，因此Kst(Bx)就是Kst(1)，它对应常量表中的“hello world”字符串，于是我们得到下面的等式：</span><br><span class="line"></span><br><span class="line">R(A):=Kst(Bx)</span><br><span class="line">&lt;===&gt;</span><br><span class="line">R(A):=Kst(1)</span><br><span class="line">于是我们得到了图17的结果。</span><br><span class="line">![](vm_execute02.png)</span><br><span class="line">  执行完第二个指令，我们发现ci-&gt;l.savedpc的指针又向右移动了一位，接下来则开始执行第三个指令OP_CALL。OP_CALL指令时iABC模式，因此A、B和C的值分别是0、2和1。OP_CALL指令的运作流程是，调用R(A)这个位置的函数，并且当B&gt;=1时，将其上的B-1个栈变量作为参数传入，且传参顺序时由下往上；当B的值为0时，那么从R(A+1)到栈顶(L-&gt;top - 1)的变量都是其参数。同样，当C&gt;=1时，则被调用函数(callee)返回C-1个返回值，当C为0时，意味着从R(A)到栈顶(L-&gt;top - 1)都是被调用函数的返回值。此时A为0，因此OP_CALL调用的是ci-&gt;l.base所指的函数，也就是我们的<span class="built_in">print</span>函数，而B的值为2，表示其只有一个参数，就是<span class="built_in">print</span>函数上方的”hello world”字符串，C的值为1，表示这个函数没有返回值。于是我们开始新的函数调用，同样的，此时会创建一个新的CallInfo实例，L-&gt;ci会指向这个新CallInfo实例，以表示它为当前调用的函数，于是我们得到了图18：</span><br><span class="line">![](vm_execute03.png)</span><br><span class="line">前面我介绍过CClosure结构的参数，调用它实际上是调用它身上存放的函数指针，而这个函数指针指向的是一个c函数，因此这里走进了c函数的调用流程，这里就不深入探讨该C函数的实现细节了，读者可以在common/luabase.c里找到它的实现逻辑，总而言之，他的作用就是在控制台打印一行“hello world”，然后完成<span class="built_in">print</span>函数调用，在完成函数调用后，因为该函数没有返回值，因此<span class="built_in">print</span>和hello world都会出栈，于是我们得到了图19：</span><br><span class="line">![](vm_execute04.png)</span><br><span class="line">似乎和图17没有多大的差别，但是细心的你可能发现了ci-&gt;l.savedpc指针已经指向了最后一个指令了。</span><br><span class="line">  现在已经到了执行最后一个指令了，那便是OP_RETURN指令，这个指令几乎是所有lua函数的最后一个指令，每个lua函数在执行了这个指令之后，就需要对返回值进行处理，并且清除自己的栈，并且将控制权返回给<span class="built_in">caller</span>。OP_RETURN函数是iABC模式，虽然用了这个模式，实际上C没有被用到，B表示它有几个返回值，当B&gt;=1时，那么该函数有B-1个返回值，当B为0时，从函数所在的位置开始(ci-&gt;l-&gt;base - 1)，到栈顶都是返回值。实际上lua函数的返回值处理，和light c <span class="keyword">function</span>基本上是差不多的。R(A)表示其第一个返回值所在的位置，由于B为1，因此它没有返回值，最终我们会把栈清空，将控制权返回给调用者，于是得到图20：</span><br><span class="line">![](vm_execute05.png)</span><br><span class="line">到了这里，实际上控制权已经返回给了图2代码中的main函数了，最后的结果时，我们得到如图21所示的输出。</span><br><span class="line">![](vm_result.png)</span><br><span class="line">   本小结，我花费了较大篇幅用于描述构建lua虚拟机所需要的基本数据结构，以及lua虚拟机的基本云状机制，相信读者对这个部分有了一个大致的理解，至于更多细节的东西，需要读者自己深入源码进行研究，本章节所涉及的脚本位于script/part05_test.lua脚本中，涉及的c代码入口在<span class="built_in">test</span>/p5_test.c中，读者可以从c函数的入口开始，从luaL_pcall函数开始跟踪源码，相信阅读了本章，你对虚拟机的运作机制会更加清晰。</span><br><span class="line"></span><br><span class="line"><span class="comment"># 标准库加载机制 #</span></span><br><span class="line">  上一节，我们完成了Lua虚拟机的运作流程，它展示了从全局表中，调出<span class="built_in">print</span>函数的流程，之前的部分默认这些全局的标准库函数，已经注册好了，并且之前的示意图也略有简化。实际上，在开始加载和运行脚本之前，我们需要先注册标准库，这个标准库注册流程就是我们现在需要讨论的内容。回顾一下图2中的代码，也就是贯穿本章节的示例代码，我们可以知道，标准库的注册逻辑就是在luaL_openlib函数内实现，现在我们来看一下这个函数的基本流程：</span><br><span class="line">```c</span><br><span class="line">// luainit.c</span><br><span class="line"><span class="comment">#include "../clib/luaaux.h"</span></span><br><span class="line"><span class="comment">#include "luabase.h"</span></span><br><span class="line"></span><br><span class="line">const lua_Reg reg[] = &#123;</span><br><span class="line">	&#123; <span class="string">"_G"</span>, luaB_openbase &#125;,</span><br><span class="line">	&#123; NULL, NULL &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">void luaL_openlibs(struct lua_State* L) &#123;</span><br><span class="line">	<span class="keyword">for</span> (int i = 0; i &lt; sizeof(reg) / sizeof(reg[0]); i++) &#123;</span><br><span class="line">		lua_Reg r = reg[i];</span><br><span class="line">		<span class="keyword">if</span> (r.name != NULL &amp;&amp; r.func != NULL) &#123;</span><br><span class="line">			luaL_requiref(L, r.name, r.func, 1);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上述的代码中，reg列表中罗列的项就是我们要注册的模块，以第一项为例，”_G”就是模块的key，而luaB_openbase就是模块初始化的函数，一般我们的模块的初始化函数，最终会注册好一个table，并压入栈中，最后注册到一个被称之为_LOADED的表中。每个模块会自己初始化好自己的table，一般而言就是注册一些函数和变量，一个模块一般情况下是对应一张表。<br>   以luaB_openbase为例，这个函数将会在luaL_requiref内部表调用，在完成这个函数的调用以后，它会从l_registry中获取_G表，并且朝里面注册一些CClosure函数实例，也就是：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="built_in">_G</span>[<span class="string">"print"</span>] = cclosure // cclosure-&gt;func = lprint</span><br><span class="line"><span class="built_in">_G</span>[<span class="string">"tostring"</span>] = cclosure // cclosure-&gt;func = ltostring</span><br><span class="line">``` </span><br><span class="line">也就是这个<span class="built_in">_G</span>注册了两个cclosure实例。</span><br><span class="line">        最后将初始化好的模块(也就是那个代表模块的<span class="built_in">table</span>实例)，注册到_LOADED <span class="built_in">table</span>的函数也是luaL_requiref函数。以上面的例子作为演示，我们的luaB_openbase在完成初始化以后，luaL_requiref函数会将其注册到_LOADED表中，其结果如下所示：</span><br><span class="line">```lua</span><br><span class="line">_LOADED[<span class="string">"_G"</span>] = <span class="built_in">_G</span></span><br></pre></td></tr></table></figure></p>
<p>实际上，luaL_requiref函数的第三个参数，会指定被传入的模块是否需要被加载到_G中，1表示需要，0表示不需要，这里我们传入了1，也就是说所有通过luaL_openlibs来注册的模块，全部会被加载到_G中，也就是说我们的_G也会被注册到_G里，于是有：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="built_in">_G</span>[<span class="string">"_G"</span>] = <span class="built_in">_G</span></span><br><span class="line">_LOADED[<span class="string">"_G"</span>] = <span class="built_in">_G</span></span><br></pre></td></tr></table></figure></p>
<p>也就是说_G._G的访问是合法的。在执行完上面的代码以后，我们可以得到如图21所示的结果：<br><img src="stdlib.png" alt><br>我们可以看到_LOADED表也是在l_registry中的，也就是我们的全局注册表中。<br>   以后我们需要注册新的模块到标准库中的时候，那么我们需要定义一个初始化函数，这个初始化函数，需要创建或获取一个table，并且往该table里注册CClosure函数或者变量，并且为该模块附上一个名称，这个table最终需要压到栈顶，以便luaL_requiref函数能将这个栈顶的table注册到_LOADED中和_G中。本小节讨论的luaL_openlibs函数在common/luaaux.c中，读者可以找他它，阅读它的源码，了解更多的细节。</p>
<h1 id="编译器基础设计与实现"><a href="#编译器基础设计与实现" class="headerlink" title="编译器基础设计与实现"></a>编译器基础设计与实现</h1><p>   前面我们花费了大量的篇幅，介绍了lua虚拟机的运作流程，我们也知道了，lua解释器要执行一个脚本，首先需要对该脚本进行编译，然后将编译的结果存入一个叫做Proto的结构中，最后交给我们的虚拟机去运行。现在我就来阐述一下编译器相关的内容，由于我并非编译器方面的专家，因此本篇只论述lua涉及到的方面，而不会太深入编译器设计中，常用的设计思想和算法，相信这些知识，读者可以通过《Compilers Principles Techniques and Tools》、《Engineering a Compiler - 2nd Edition - K. Cooper, L. Torczon (Morgan Kaufman, 2012)》等优秀的编译原理相关的书籍中了解到。</p>
<h2 id="编译器简介"><a href="#编译器简介" class="headerlink" title="编译器简介"></a>编译器简介</h2><p> 现在我们进入到编译器相关的论述中，什么是编译器？《Engineer A Compiler》是这么定义的[6]:</p>
<blockquote>
<p>Programming languages are, in general, designed to allow humans to express computations as sequences of operations. Computer processors, hereafter referred to as processors, microprocessors, or machines, are designed to execute sequences of operations. The operations that a processor implements are, for the most part, at a much lower level of abstraction than those specified in a programming language. For example, a programming language typically includes a concise way to print some number to a file. That single programming language statement must be translated into literally hundreds of machine operations before it can execute.<br>The tool that performs such translations is called a compiler. The compiler takes as input a program written in some language and produces as its output an equivalent program. In the classic notion of a compiler, the output program is expressed in the operations available on some specific processor, often called the target machine</p>
</blockquote>
<p>总的来说，我们操作系统中的进程，其实是对处理器也就是CPU的抽象，进程在执行，就是我们的处理器在执行，只是因为要并发处理任务，因此操作系统有了分时系统，因此引入进程的概念更方便于操作系统管理。我们的编程语言，是为了方便人们使用而设计的，人们可以通过高级的编程语言去描述自己想要做的处理。但高级语言是不能被计算机所识别的，为了能够翻译成机器所能识别的指令，需要经过一个软件的处理，这个软件就是编译器。<br> 上面所论述的是我们传统认知的编译器，实际上编译器的概念并不局限于，将高级语言源码编译成目标机器的机器码，只要能将一种语言，翻译成另一种语言形式的工具，我们都可以称之为编译器。实际上有一些编译器，会先将其支持的语言的代码编译成c的代码，然后再通过c的编译器，将其编译成目标机器的机器码，之所以要用这种方式，是因为c是一种被广泛使用和支持的语言，通过这种方式可以极大降低语言的移植成本[7]。图22(图片引自Engineer A Compiler一书)将编译器视作是一个黑盒，并将其工作流程展示出来了<br>image图22</p>
<h2 id="编译器组成"><a href="#编译器组成" class="headerlink" title="编译器组成"></a>编译器组成</h2><p>  我们在了解了什么是编译器以后，就需要了解编译器由哪些部分组成，至少在通用的概念上，我们需要做一些区分。从整体上来说，现代编译器并非整体的一个模块，它至少可以分为两个部分，而通常来说，一个能将源码编译成目标机器码的编译器，会包含3个部分，它们分别是编译器前端(front end)，优化器(optimizer)和编译器后端(back end)。<br>  编译器前端，通常包含一个词法分析器(lexer或scanner)和一个语法分析器(parser)，它主要的工作就是，首先把源码中的字符，识别出一个又一个的token，这里的token指的是编译过程中的基本单位，这些token包含一个类别指示和一个值的标识，有些token只有类型标识而没有值得标识，比如我们的print(“hello world”)代码，词法分析器会将它们的token识别出来：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span> 			&lt;TK_NAME, <span class="string">"print"</span>&gt;</span><br><span class="line">(     			&lt;<span class="string">'('</span>, NONE&gt;</span><br><span class="line"><span class="string">"hello world"</span>	&lt;TK_STRING, <span class="string">"hello world"</span>&gt;</span><br><span class="line">)				&lt;<span class="string">')'</span>, NONE&gt;</span><br></pre></td></tr></table></figure></p>
<p>其实我们可以看到，其实词法分析器的作用主要是将源码中，语句的基本单位抽离出来，打上标签，以方便后面要经过的语法分析器，进行语法分析和语义分析，最后将源码编译成一种作为中间表示的数据组织–IR。计算机语言和自然语言最大的区别就是，计算机语言不允许有二义性存在，而自然语言允许二义性，允许模糊的空间，比如我们汉语中就有很多有二义性含义的语句存在，事实上这种二义性也满足了我们的日常交际需要，但计算机语言不允许二义性，它必须是明确的，简洁的。计算机语言中，有一些有限的规则，这些有限的规则使得我们的语言能够表达各种操作，这些规则的集合就叫做grammar，什么是grammar？举个例子，我们编程语言中有许许多多的范式，比如：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1. 条件语句</span><br><span class="line"><span class="keyword">if</span> expr <span class="keyword">then</span></span><br><span class="line">	statments</span><br><span class="line">elseif expr <span class="keyword">then</span></span><br><span class="line">	statments</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">	statments</span><br><span class="line">end</span><br><span class="line"></span><br><span class="line">2. 循环语句</span><br><span class="line"><span class="keyword">while</span> expr <span class="keyword">then</span></span><br><span class="line">	statments</span><br><span class="line">end</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> expr <span class="keyword">in</span> pairs(expr) <span class="keyword">do</span></span><br><span class="line">	statments</span><br><span class="line">end</span><br><span class="line"></span><br><span class="line">3. 赋值语句</span><br><span class="line">expr1 = expr2</span><br><span class="line"></span><br><span class="line">4. 函数调用</span><br><span class="line">expr()</span><br><span class="line">...</span><br></pre></td></tr></table></figure></p>
<p>以上的范式，实际上是个有限集合，而这个有限集合的统称就叫做grammar，我们的语法分析器，也就是parser做的主要工作，就是将词法分析器中，按顺序读出一个又一个token，并判断当前token符合哪个范式，然后根据这个范式将对应的指令生成另一种数据结构–IR。<br>   编译器的优化器，它的主要作用，就是对编译器前端生成的IR，进行进一步的优化，比如指令合并。优化的目的，是使得编译的结果，在优化后，比优化前运行速度更快，或者占据磁盘的空间更小。<br>   编译器后端，主要的作用，就是将编译器前端编译好的IR，直接转换成目标机器码，只有经过编译器后端，将IR转成机器码，cpu才能识别源码中的操作请求，才能运行起来。<br>   现在我来介绍一下IR的概念，所谓的IR就是Intermediate Represent，在《Engineer A Compiler》中的解释是：</p>
<blockquote>
<p>A compiler uses some set of data structures to represent the code that it processes. That form is called an intermediate representation, or IR.</p>
</blockquote>
<p>简单的来说，它是源码经过编译器处理以后，用于存放编译结果的数据结构，它本质是一种中间表示。IR的作用很大，实际上它可以充当编译器前端和编译器后端解耦的桥梁。如果我们保持统一的IR，那么我们不同的语言，只要编译器前端能够生成相同的IR，而针对某目标平台的编译器后端是将这种IR转换成目标机器的机器码的话，这个编译器后端就能被多种语言的编译器前端所复用，那么我们开发新的语言就不需要自己再去实现该目标平台的编译器后端了。同样的，由于IR是统一的，那么我们同一门语言，编译生成的IR也能够被多种平台的编译器后端，直接编译成机器码，那么，只要这门语言的编译器前端能够生成这种IR，它同样可以直接使用这些编译器的后端。<br>  经过前面几个段落的论述，我们大致知道了编译器的组成，其运作流程如下图所示(图片引自Engineer A Compiler一书)：<br><img src="three_phase_compiler.png" alt></p>
<h2 id="编译器和解释器的区别"><a href="#编译器和解释器的区别" class="headerlink" title="编译器和解释器的区别"></a>编译器和解释器的区别</h2><p>  上一小节我已经大致介绍了编译器的概念，和编译器的大致组成，现在我们必须还有一个概念需要捋清楚，那便是编译器和解释器的区别。首先我们知道了，编译器的功能，就是将一种代码转化成另一种代码，但是转换的过程并不执行逻辑，而是编译好以后，存在磁盘里，将来要用到时再加载到内存进行执行。<br>        我们的解释器则有所不同，解释器的概念就是，加载我们要转化的源码后，直接编译，然后立刻执行，而不会将编译的结果存在磁盘里，它会输出源码里的操作结果。其运作流程如图24所示(图片引自Engineer A Compiler一书)：<br><img src="interpreter.png" alt><br>  通过上面的论述，我们不难发现，lua就是个典型的解释器，因为它在加载脚本的同时，能够立刻输出结果。但是我们也应该看到，lua内部的lexer和parser，实际上是将脚本源码，先编译成一个proto结构，然后再放到虚拟机里执行，根据前文对IR的论述，实际上，这个proto结构就是我们所说的IR。实际上《Engineer A Compiler》一书中，还有这么一句话：</p>
<blockquote>
<p>Finally, compilers are being invoked at runtime to generate customized code that capitalizes on facts that cannot be known any earlier. If the compilation time can be kept small and the benefits are large, this strategy can produce noticeable improvements. the</p>
</blockquote>
<p>实际上编译器是可以在运行时被激活使用的，而我们的lua中的lexer和parser实际上又是编译器前端的重要组成部分，因此dummylua将lexer和parser归结为compiler模块是合理的。后面在本文中所说的编译器，实际上就是指代lua的lexer和parser两个模块，而后续我要论述的正是这两个模块。optimizer和back end的设计与实现，完全超出了本系列的范畴，因此这里不会去过度延伸，读者如果有兴趣，可以找对应的书籍去了解。<br>  前面我们对解释器进行了解释和说明，这里补充一个知识点，就是virtual machine的实质定义，事实上这里的virtual machine，实际上指的对处理器的抽象，它接受的是编译器编好的IR，然后转成处理器能够识别的指令。在《Engineer A Compiler》一书中，其对vm的解释如下所示：</p>
<blockquote>
<p>Virtual machine<br>A virtual machine is a simulator for some processor. It is an interpreter for that machine’s instruction set.</p>
</blockquote>
<p>虚拟机是一些处理器(CPU)的模拟器，它是机器指令集的解释器。按这种标准来划分，实际上将lua分的更细一些，其虚拟机部分就是解释器，而lexer和parser是编译器，按这种方式来划分其实也是说的通的。<br>   本章后续部分，将会集中探讨lua的编译器部分，lexer和parser。</p>
<h1 id="EBNF范式"><a href="#EBNF范式" class="headerlink" title="EBNF范式"></a>EBNF范式</h1><p>   任何一种编程语言，都需要通过一组规则作为有限集合，组成它的语法(grammar)，而我们需要通过某种方式来组成这种语法，EBNF范式就是其中一种。EBNF是通过数学的方式来描述我们的编程语言语法的，但它的描述范围不局限于编程领域。一般来说，EBNF范式由以下部分组成[8]：</p>
<ul>
<li>terminal符号的集合</li>
<li>nonterminal符号的集合</li>
<li>production的集合<br>接下来我将对以上三组概念进行解释和说明。terminal是指语句中，不能被继续拆解的基本单位，一般来说，我们的terminal就是指token。nonterminal是由一组terminal按一定规则组合而成的。而我们的production又是由一组nonterminal和/或terminal按照一定的规则组合而成。接下来，我们通过一个示例来感受一下，EBNF范式的规则。<br>现在假设我们要设计一种语言，这种语言就是将数值(整数和小数)，通过EBNF范式表达出来，也就是说我们通过EBNF来描述数值的语法，于是我们得到以下的内容：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">S ::= <span class="string">'-'</span> FN | FN</span><br><span class="line">FN ::= DL | DL <span class="string">'.'</span> DL</span><br><span class="line">DL ::= &#123;D&#125;</span><br><span class="line">D ::= <span class="string">'0'</span> | <span class="string">'1'</span> | <span class="string">'2'</span> | <span class="string">'3'</span> | <span class="string">'4'</span> | <span class="string">'5'</span> | <span class="string">'6'</span> | <span class="string">'7'</span> | <span class="string">'8'</span> | <span class="string">'9'</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>上面所示的代码中，::=表示定义，左边的符号可以被右边的符号所替换。|符号表示或的意思，也就是说左边的符号，可以通过|符号左边或者右边的符号来代替。被”包起来的字符，就是我们实际会显示的字符。被{}包起来的，表示它可以重复0次或者多次。上面的代码，’0’~‘9’已经是最基本的单位了，它不能够被进一步分解，所以他们是terminal，而::=左边的符号，均是nonterminal，每一行定义，就是我们所说的production。通过上述的方式，我们可以用其表示任意的负数、0和正数。现在我们来将3.14代入这个范式，则有：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">S ::= FN  			没有<span class="string">'-'</span>，因此选择FN分支</span><br><span class="line">S ::= DL . DL      	因为带有<span class="string">'.'</span>，所以选择DL <span class="string">'.'</span> DL分支</span><br><span class="line">S ::= D . DL 		因为整数位只有一个字符，因此选择D分支</span><br><span class="line">S ::= 3.DL 			替换整数位的值</span><br><span class="line">S ::= 3.D D   		因为小数位是两个字符，因此小数位转换为D D</span><br><span class="line">S ::= 3.1 D        	替换小数位十分位</span><br><span class="line">S ::= 3.14        	替换小数位百分位，全部替换完毕，且每个被分解的最小单位都合法，因此解析成功</span><br></pre></td></tr></table></figure></p>
<p>如果一个语句，如果能够通过范式，完整替换解析的话，那么我们就称这个语句是语法合法的(well form)，在上面的例子中，能够被完整解析的语句，就是一个货真价实的数值，否则就应当抛出编译错误，比如我们的U.14，因为整数位不是定义terminal中的任何一个，因此此时需要抛出syntax error的错误。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">S ::= FN  			没有<span class="string">'-'</span>，因此选择FN分支</span><br><span class="line">S ::= DL . DL      	因为带有<span class="string">'.'</span>，所以选择DL <span class="string">'.'</span> DL分支</span><br><span class="line">S ::= D . DL 		因为整数位只有一个字符，因此选择D分支</span><br><span class="line">S ::= U.DL 			替换整数位的值</span><br><span class="line">S ::= U.D D   		因为小数位是两个字符，因此小数位转换为D D</span><br><span class="line">S ::= U.1 D        	替换小数位十分位</span><br><span class="line">S ::= U.14         	替换小数位百分位，全部替换完毕，因为整数位不是合法的terminal，因此解析失败，语句不合法</span><br></pre></td></tr></table></figure></p>
<p>因为无法通过解析，因此U.14就不属于我们定义的数值语言。</p>
<h1 id="Lua的EBNF范式"><a href="#Lua的EBNF范式" class="headerlink" title="Lua的EBNF范式"></a>Lua的EBNF范式</h1><pre><code>在介绍完EBNF范式以后，我们现在来看一下Lua语言，完整的范式规则[9]：
</code></pre><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chunk ::= &#123;<span class="built_in">stat</span> [`;´]&#125; [laststat[`;´]]</span><br><span class="line"></span><br><span class="line">block ::= chunk</span><br><span class="line"></span><br><span class="line"><span class="built_in">stat</span> ::=  varlist1 `=´ explist1  |</span><br><span class="line">         functioncall  |</span><br><span class="line">         <span class="keyword">do</span> block end  |</span><br><span class="line">         <span class="keyword">while</span> exp <span class="keyword">do</span> block end  |</span><br><span class="line">         repeat block until exp  |</span><br><span class="line">         <span class="keyword">if</span> exp <span class="keyword">then</span> block &#123;elseif exp <span class="keyword">then</span> block&#125; [<span class="keyword">else</span> block] end  |</span><br><span class="line">         <span class="keyword">for</span> Name `=´ exp `,´ exp [`,´ exp] <span class="keyword">do</span> block end  |</span><br><span class="line">         <span class="keyword">for</span> namelist <span class="keyword">in</span> explist1 <span class="keyword">do</span> block end  |</span><br><span class="line">         <span class="keyword">function</span> funcname funcbody  |</span><br><span class="line">         <span class="built_in">local</span> <span class="keyword">function</span> Name funcbody  |</span><br><span class="line">         <span class="built_in">local</span> namelist [`=´ explist1]</span><br><span class="line"></span><br><span class="line">laststat ::= <span class="built_in">return</span> [explist1]  |  <span class="built_in">break</span></span><br><span class="line"></span><br><span class="line">funcname ::= Name &#123;`.´ Name&#125; [`:´ Name]</span><br><span class="line"></span><br><span class="line">varlist1 ::= var &#123;`,´ var&#125;</span><br><span class="line"></span><br><span class="line">var ::=  Name  |  prefixexp `[´ exp `]´  |  prefixexp `.´ Name</span><br><span class="line"></span><br><span class="line">namelist ::= Name &#123;`,´ Name&#125;</span><br><span class="line"></span><br><span class="line">explist1 ::= &#123;exp `,´&#125; exp</span><br><span class="line"></span><br><span class="line">exp ::=  nil  |  <span class="literal">false</span>  |  <span class="literal">true</span>  |  Number  |  String  |  `...´  |</span><br><span class="line">         <span class="keyword">function</span>  |  prefixexp  |  tableconstructor  |  exp binop exp  |  unop exp</span><br><span class="line"></span><br><span class="line">prefixexp ::= var  |  functioncall  |  `(´ exp `)´</span><br><span class="line"></span><br><span class="line">functioncall ::=  prefixexp args  |  prefixexp `:´ Name args</span><br><span class="line"></span><br><span class="line">args ::=  `(´ [explist1] `)´  |  tableconstructor  |  String</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> ::= <span class="keyword">function</span> funcbody</span><br><span class="line"></span><br><span class="line">funcbody ::= `(´ [parlist1] `)´ block end</span><br><span class="line"></span><br><span class="line">parlist1 ::= namelist [`,´ `...´]  |  `...´</span><br><span class="line"></span><br><span class="line">tableconstructor ::= `&#123;´ [fieldlist] `&#125;´</span><br><span class="line"></span><br><span class="line">fieldlist ::= field &#123;fieldsep field&#125; [fieldsep]</span><br><span class="line"></span><br><span class="line">field ::= `[´ exp `]´ `=´ exp  |  Name `=´ exp  |  exp</span><br><span class="line"></span><br><span class="line">fieldsep ::= `,´  |  `;´</span><br><span class="line"></span><br><span class="line">binop ::= `+´  |  `-´  |  `*´  |  `/´  |  `^´  |  `%´  |  `..´  |</span><br><span class="line">         `&lt;´  |  `&lt;=´  |  `&gt;´  |  `&gt;=´  |  `==´  |  `~=´  |</span><br><span class="line">         and  |  or</span><br><span class="line"></span><br><span class="line">unop ::= `-´  |  not  |  `<span class="comment">#´</span></span><br></pre></td></tr></table></figure>
<p>上面所涉及的多数符号，前面已经介绍过，这里对[]符号进行补充，包含在[]内的内容，表示可以被省略或者只出现一次。虽然dummylua模仿的对象是lua53，但是从语法层面的发展历史来看，51和53的语法并没有太多的变化，因此我将遵循上面的范式，逐步丰富dummylua。我最终的目标，也是将上述描述的规则完全实现。</p>
<h2 id="本章要实现的范式"><a href="#本章要实现的范式" class="headerlink" title="本章要实现的范式"></a>本章要实现的范式</h2><p>  虽然上一节贴出了lua完整的语法，但是本章节要设计与实现的部分，也只是函数调用那一部分，并且不支持任何形式的赋值语句。我们前面也介绍过，我们的一个要被编译和解析的脚本既是一个chunk，而图1中的代码，正是该chunk中，需要被编译的那一部分，也就是我们现在只需要支持类似print(“hello world”)这种形式的函数调用即可，于是我们的EBNF范式可以如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chunk 		 ::= &#123; <span class="built_in">stat</span> &#125;</span><br><span class="line"><span class="built_in">stat</span> 		 ::= functioncall</span><br><span class="line">functioncall ::= prefixexp args</span><br><span class="line"></span><br><span class="line">prefixexp 	 ::= var</span><br><span class="line">var 		 ::= Name 	       </span><br><span class="line">args 		 ::= <span class="string">'('</span> [explist] <span class="string">')'</span></span><br><span class="line">explist      ::= &#123;exp <span class="string">','</span>&#125; exp</span><br><span class="line">exp          ::= nil  |  <span class="literal">false</span>  |  <span class="literal">true</span>  |  Number  |  String</span><br></pre></td></tr></table></figure></p>
<p>上述范式集合，表达了我们这次要支持的句式和语法。这里需要提醒的是，{}里的stat，是可以没有或者有多个的。而::=符号右边的Name和nil、false、true、Number、String则是terminal。现在我们将图1中的代码print(“hello world”)带入该范式，看看它是怎么运作的：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chunk 		 ::= <span class="built_in">stat</span> 		    由于chunk中只有一个<span class="built_in">stat</span>，所以右边替换为<span class="built_in">stat</span></span><br><span class="line">chunk        ::= functioncall   由于<span class="built_in">stat</span>只有一个nonterminal可以替换，因此将其替换为functioncall</span><br><span class="line">chunk        ::= prefixexp args 由于当前的functioncall中只有一个选项，且输入语句符合这个选项，</span><br><span class="line">								因此选择了prefixexp args来替换</span><br><span class="line">chunk        ::= var args       将prefixexp替换为var</span><br><span class="line">chunk        ::= Name args</span><br><span class="line">chunk        ::= <span class="built_in">print</span> args     <span class="built_in">print</span>属于Name这个terminal这个类别，将<span class="built_in">print</span>替换Name</span><br><span class="line">chunk        ::= <span class="built_in">print</span> <span class="string">'('</span> [explist] <span class="string">')'</span></span><br><span class="line">chunk        ::= <span class="built_in">print</span> <span class="string">'('</span> explist <span class="string">')'</span>  因为参数表有参数，因此选择explist</span><br><span class="line">chunk        ::= <span class="built_in">print</span> <span class="string">'('</span> exp <span class="string">')'</span></span><br><span class="line">chunk        ::= <span class="built_in">print</span> <span class="string">'('</span> String <span class="string">')'</span>   所有项均替换为terminal，并且均符合定义，所以解析通过</span><br><span class="line">chunk        ::= <span class="built_in">print</span> <span class="string">'('</span> <span class="string">"hello world"</span> <span class="string">')'</span> 将<span class="string">"helloworld"</span>字符串替换String</span><br></pre></td></tr></table></figure></p>
<p>上图展示了图1的脚本，完整的语法解析流程，因为脚本里的代码，能够通过我们当前EBNF范式的解析，因此它是语法合法的，如果一个语句通过语法解析，但是被调用的函数并不存在，那么在lua中，它只能在运行时抛出错误。但如果一个语句不符合范式规定的规范，那么我们就会抛出编译时报错，提醒脚本编写者。<br> 刚才展示的流程和范式，就是本章节我最终会实现的部分，我在后续的章节中，会以此为蓝本，逐步丰富这个范式，直至和上一节中贴出的范式重合为止。</p>
<h2 id="词法分析器设计与实现"><a href="#词法分析器设计与实现" class="headerlink" title="词法分析器设计与实现"></a>词法分析器设计与实现</h2><p>  通过上面的EBNF范式的解析流程，我们可以知道，要使用Parser对源码里的语句，进行语法解析，我们就必须要首先识别源码中的terminal，上文也有对terminal进行简要的说明，不过我们现在还没真正搞清楚，lua语言中的terminal到底是什么？其实terminal本质就是token，这个问题其实相当于在问token到底是什么。前面我也说过，token的本质就是一门语言中，最基本的单位，例如前面的S::=FN的例子中，它的token集合就是’0’~‘9’。不同的语言，对token的定义也略有不同，但是却有相通的属性。现在，我通过一张表来列举lua的最基本的元素，也就是我们的token集合：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">token id	token name	token value	description</span><br><span class="line">-1	EOS	-	表示源代码已经读完了，代表应该停止继续解析该脚本</span><br><span class="line">40	’(’	-	一般作为函数参数表的左限定符</span><br><span class="line">41	’)’	-	一般作为函数参数表的右限定符</span><br><span class="line">42	‘*’	-	表示运算符乘，id为其ASCII值</span><br><span class="line">43	’+’	-	表示算数运算符+，id为其ASCII值</span><br><span class="line">45	’-’	-	表示算数运算-，也表示符号，id为其ASCII值</span><br><span class="line">60	‘&lt;’	-	表示小于</span><br><span class="line">61	’=’	-	表示赋值运算</span><br><span class="line">62	’&gt;’	-	表示大于</span><br><span class="line">91	‘[’	-	一般用于表示访问table时，key的左限定符</span><br><span class="line">92	’]’	-	一般用于表示访问table时，key的右限定符</span><br><span class="line">123	’&#123;’	-	作为table body的开始标志</span><br><span class="line">124	’&#125;’	-	作为table body的结束标志</span><br><span class="line">126	‘~’	-	表示运算符非，或者是按位取反操作，id为其ASCII值</span><br><span class="line">257	TK_LOCAL	“<span class="built_in">local</span>”	表示系统保留字”<span class="built_in">local</span>”</span><br><span class="line">258	TK_NIL	“nil”	表示系统保留字”nil”</span><br><span class="line">259	TK_TRUE	“<span class="literal">true</span>”	表示系统保留字”<span class="literal">true</span>”</span><br><span class="line">260	TK_FALSE	“<span class="literal">false</span>”	表示系统保留字”<span class="literal">false</span>”</span><br><span class="line">261	TK_END	“end”	表示系统保留字”end”</span><br><span class="line">262	TK_THEN	“<span class="keyword">then</span>”	表示系统保留字”<span class="keyword">then</span>”</span><br><span class="line">263	TK_IF	“<span class="keyword">if</span>”	表示系统保留字”<span class="keyword">if</span>”</span><br><span class="line">264	TK_ELSEIF	“elseif”	表示系统保留字”elseif”</span><br><span class="line">265	TK_FUNCTION	“<span class="keyword">function</span>”	表示系统保留字”<span class="keyword">function</span>”</span><br><span class="line">266	TK_STRING	字符串的值，比如”xxxx”	通过”“或”包起来的内容，则是字符串</span><br><span class="line">267	TK_NAME	表示变量名的字符串	表示变量名</span><br><span class="line">268	TK_FLOAT	浮点数值	表示浮点数</span><br><span class="line">269	TK_INT	整数值	表示整数值</span><br><span class="line">270	TK_NOTEQUAL	-	表示~=比较运算，id为其枚举值</span><br><span class="line">271	TK_EQUAL	-	表示==运算</span><br><span class="line">272	TK_GREATEREQUAL	-	表示&gt;=运算</span><br><span class="line">273	TK_LESSEQUAL	-	表示&lt;=运算</span><br><span class="line">274	TK_SHL	-	表示&lt;&lt;运算，这是左移运算</span><br><span class="line">275	TK_SHR	-	表示&gt;&gt;运算，这是右移运算</span><br><span class="line">276	TK_MOD	-	表示’%‘运算，即取模运算</span><br><span class="line">277	TK_DOT	-	表示’.‘运算符，id为其枚举值</span><br><span class="line">278	TK_VARARG	-	表示’…‘运算符，这个运算符一般用在参数列表中，表示参数可以是任意个</span><br><span class="line">279	TK_CONCAT	-	表示’..‘运算符，该运算符表示连接运算符，一般是用于连接两个字符串用的</span><br></pre></td></tr></table></figure></p>
<p>   上面所示的TOKEN集合，是dummylua目前已经定义好的集合，并非官方lua的token集合，而且该集合也只是一个临时版本，后续持续的拓展可能会改变上面的一些内容，比如token的id值可能会有所调整，这里展示出来的目的，是为了让大家对token集合有一个清晰的认识。同时我们也会注意到，上述的token集合中，有些有值，有些没有值，这是由于有些token表达的内容比较单一，比如它就是一个操作符，因此不需要填内容，而有些token表达的内容比较丰富，比如TK_STRING这种，我们既要知道它是一个string，也需要知道它的内容，因此我们需要一个变量来存放这个token的值。<br>   接下来我们来看看token的数据结构。<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> Seminfo &#123;</span><br><span class="line">    lua_Number r;</span><br><span class="line">    lua_Integer i;</span><br><span class="line">    TString* s; </span><br><span class="line">&#125; Seminfo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Token</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> token;          <span class="comment">// token enum value</span></span><br><span class="line">    Seminfo seminfo;    <span class="comment">// token info</span></span><br><span class="line">&#125; Token;</span><br></pre></td></tr></table></figure></p>
<p>我们看到token的结构其实很简单，首先它有一个标记其id的token变量，后面的Seminfo结构，则是存储含有值的token的值。这个Seminfo结构，是一个union结构，也就是说token的值是整数、浮点数或者字符串中的一种，从前面我们列举的表格，我们也可以看到，token的值的类型，也大致是这三个类型。接下来的问题则是，我们如何在源码里识别并获取一个又一个的token呢？对于这个问题，dummylua中获取token的api如下所示：</p>
<p>// lualexer.h<br>int luaX_next(struct lua_State<em> L, LexState</em> ls);<br>这个api能够将，我们源码中的下一个token识别出来，并且将其存放在LexState类型的结构中，对于LexState这个数据结构，其定义如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LexState</span> &#123;</span></span><br><span class="line">	Zio* zio;				<span class="comment">// zio实例，会从源码文件中，读取一定数量的字符，以供lexer使用</span></span><br><span class="line">    <span class="keyword">int</span> current;			<span class="comment">// 当前读出来的那个字符的ASCII码</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">MBuffer</span>* <span class="title">buff</span>;</span>	<span class="comment">// 当当前的token被识别为TK_FLOAT、TK_INT、TK_NAME或者TK_STRING类型时，</span></span><br><span class="line">							<span class="comment">// 这个buff会临时存放被读出来的字符，等token识别完毕后，在赋值到Token</span></span><br><span class="line">							<span class="comment">// 结构的seminfo变量中</span></span><br><span class="line">	Token t;				<span class="comment">// 通过luaX_next读出来的token</span></span><br><span class="line">	<span class="keyword">int</span> linenumber; 		<span class="comment">// 当前处理到哪一行</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">Dyndata</span>* <span class="title">dyd</span>;</span>    <span class="comment">// 语法分析器里要用到的结构</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">FuncState</span>* <span class="title">fs</span>;</span>   <span class="comment">// 语法分析器使用的重要数据结构</span></span><br><span class="line">	lua_State* L;</span><br><span class="line">	TString* source;		<span class="comment">// 被解析的源文件名称和路径</span></span><br><span class="line">	TString* env; 			<span class="comment">// 就是"_ENV"的TSting表示</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">Table</span>* <span class="title">h</span>;</span>        <span class="comment">// 在编译源文件时，常量会临时存放在这里，提升检索效率，也就是提升编译效率</span></span><br><span class="line">&#125; LexState;</span><br></pre></td></tr></table></figure></p>
<p>我们可以看到，通过luaX_next接口读出来的token，会被存放在LexState结构的t变量里，以供语法分析器Parser晚些时候使用。词法分析器在进行解析时，一次只会取出一个字符，然后再进行处理，而取字符则需要从文件里读取，这部分读取的操作，正是通过Zio模块来执行，Zio的数据结构如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luazio.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">char</span>* (*lua_Reader)(struct lua_State* L, <span class="keyword">void</span>* data, <span class="keyword">size_t</span>* <span class="built_in">size</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LoadF</span> &#123;</span></span><br><span class="line">    FILE* f;				<span class="comment">// 被打开的源码文件句柄</span></span><br><span class="line">    <span class="keyword">char</span> buff[BUFSIZE]; 	<span class="comment">// Zio每次会从文件中读取BUFSIZE个字符，并存在buff数组中</span></span><br><span class="line">	<span class="keyword">int</span> n;		       		<span class="comment">// n表示被读入buff数组中的字符有多少个</span></span><br><span class="line">&#125; LoadF;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Zio</span> &#123;</span></span><br><span class="line">	lua_Reader reader;		<span class="comment">// 执行从文件中读取字符，并村妇LoadF的buff中的执行函数，由外部指定</span></span><br><span class="line">	<span class="keyword">int</span> n;					<span class="comment">// 还有多少个未被处理的字符，初始值是LoadF的n值</span></span><br><span class="line">	<span class="keyword">char</span>* p;				<span class="comment">// 指向LoadF结构的buff数组的指针，每处理一个字符，它会自增</span></span><br><span class="line">	<span class="keyword">void</span>* data;				<span class="comment">// LoadF结构实例的指针</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">lua_State</span>* <span class="title">L</span>;</span></span><br><span class="line">&#125; Zio;</span><br></pre></td></tr></table></figure></p>
<p>现在我们来看一下首次调用luaX_next的执行流程：</p>
<ul>
<li>Zio模块，通过外部指定的read函数，从源文件中读取BUFSIZE个字符，并存入LoadF结构的buff中，它的n值记录了读取了多少个字符</li>
<li>Zio的p指针，指向LoadF的buff数组，并且将LoadF中的n值赋值给zio-&gt;n</li>
<li>将*p赋值给lexstate-&gt;current，然后zio-&gt;p++，然后zio-&gt;n–，当zio-&gt;n小于等于0时，在下一次调用luaX_next函数时，Zio模块会重新从文件中读取新的BUFSIZE个字符，重置LoadF结构的buff数组，重置n，zio-&gt;p和zio-&gt;n</li>
<li>接下来luaX_next会判断lexstate-&gt;current的值，与最上面的token集合表中的哪个token匹配，如果是没有token value的token，那么直接将lexstate-&gt;current的值赋值给lexstate-&gt;t.token，或者从token的枚举值中，找到一个与之匹配的token id赋值给lexstate-&gt;t.token；如果被匹配到的token是有token value值的token，那么需要判定它的类型，是TK_FLOAT、TK_INT、TK_NAME还是TK_STRING的一种，除了要给lexstate-&gt;t.token赋值正确的token id外，还需要将对应的值读取，然后赋值到lexstate-&gt;t.seminfo中。<br>当我们的词法分析器，读取一个字符后，判定其类型时TK_FLOAT、TK_INT、TK_NAME或TK_STRING的一种时，它会从Zio模块中，连续读取字符，并暂存在LexState结构的Buffer数组中，直到下一个字符与其类型规定的规则不符合为止，于是我们就可以将暂存起来的字符数据，转换成对应类型的变量。不同的类型，也会有不同的匹配方式，现在我们通过一张表来看看他们的匹配规则，我们使用正则表达式[10]来表达这一点。<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">token name	regular expression	description</span><br><span class="line">TK_FLOAT	[0-9]*.[0-9]+	整数位0个或者多个在[0~9]范围内的值，小数位1个或多个[0~9]范围内的值</span><br><span class="line">TK_INT	[0-9]+	数位值是1个或者多个，在[0~9]范围内的值</span><br><span class="line">TK_NAME	( _*[Aa-Zz]+)(( _ * [Aa-Zz]*)( _ * [0-9]*)) * _*	必须以_或者字母开头，该类型token的值，只能</span><br><span class="line">包含字母或者数字以及下划线</span><br><span class="line">TK_STRING	\”[\s\S]\”	[\s\S]表示任意字符，\s表示空白字符，\S表示非空白字符，组合在一起就表示任意字符</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>符合上面规则的连续字符，会被合并在一起，成为该token的值，它开始于第一个匹配的字符，结束于第一个不匹配的字符。现在我们来看一下luaX_next函数的实现：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaX_next</span><span class="params">(struct lua_State* L, LexState* ls)</span> </span>&#123;</span><br><span class="line">	ls-&gt;t.token = llex(ls, &amp;ls-&gt;t.seminfo);</span><br><span class="line">	<span class="keyword">return</span> ls-&gt;t.token;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">llex</span><span class="params">(LexState* ls, Seminfo* seminfo)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">for</span> (;;) &#123;</span><br><span class="line">		luaZ_resetbuffer(ls); 		<span class="comment">// 清空ls-&gt;buffer的缓存</span></span><br><span class="line">		<span class="keyword">switch</span> (ls-&gt;current)</span><br><span class="line">		&#123;</span><br><span class="line">		<span class="comment">// new line</span></span><br><span class="line">		<span class="keyword">case</span> <span class="string">'\n'</span>: <span class="keyword">case</span> <span class="string">'\r'</span>: &#123;</span><br><span class="line">			inclinenumber(ls);</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="comment">// skip spaces</span></span><br><span class="line">		<span class="keyword">case</span> <span class="string">' '</span>: <span class="keyword">case</span> <span class="string">'\t'</span>: <span class="keyword">case</span> <span class="string">'\v'</span>: &#123;</span><br><span class="line">			next(ls); 				<span class="comment">// 将下一个字符，存入ls-&gt;current</span></span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'-'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="comment">// this line is comment</span></span><br><span class="line">			<span class="keyword">if</span> (ls-&gt;current == <span class="string">'-'</span>) &#123;</span><br><span class="line">				<span class="keyword">while</span> (!currIsNewLine(ls) &amp;&amp; ls-&gt;current != EOF)</span><br><span class="line">					next(ls);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'-'</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125; <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> EOF:&#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> TK_EOS;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'+'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'+'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'*'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'*'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'/'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'/'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'~'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="comment">// not equal</span></span><br><span class="line">			<span class="keyword">if</span> (ls-&gt;current == <span class="string">'='</span>) &#123;</span><br><span class="line">				next(ls);</span><br><span class="line">				<span class="keyword">return</span> TK_NOTEQUAL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'~'</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'%'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> TK_MOD;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'.'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">if</span> (<span class="built_in">isdigit</span>(ls-&gt;current)) &#123;</span><br><span class="line">				<span class="keyword">return</span> str2number(ls, <span class="literal">true</span>);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (ls-&gt;current == <span class="string">'.'</span>) &#123;</span><br><span class="line">				next(ls);</span><br><span class="line">				<span class="comment">// the '...' means vararg </span></span><br><span class="line">				<span class="keyword">if</span> (ls-&gt;current == <span class="string">'.'</span>) &#123;</span><br><span class="line">					next(ls);</span><br><span class="line">					<span class="keyword">return</span> TK_VARARG;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="comment">// the '..' means concat</span></span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="keyword">return</span> TK_CONCAT;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> TK_DOT;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'"'</span>: <span class="keyword">case</span> <span class="string">'\''</span>: &#123; <span class="comment">// process string</span></span><br><span class="line">			<span class="comment">// 将读出的字符串，存入ls-&gt;t.seminfo中</span></span><br><span class="line">			<span class="keyword">return</span> read_string(ls, ls-&gt;current, &amp;ls-&gt;t.seminfo);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'('</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'('</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">')'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">')'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'['</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'['</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">']'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">']'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'&#123;'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'&#123;'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'&#125;'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">'&#125;'</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'&gt;'</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">if</span> (ls-&gt;current == <span class="string">'='</span>) &#123;</span><br><span class="line">				next(ls);</span><br><span class="line">				<span class="keyword">return</span> TK_GREATEREQUAL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (ls-&gt;current == <span class="string">'&gt;'</span>) &#123;</span><br><span class="line">				next(ls);</span><br><span class="line">				<span class="keyword">return</span> TK_SHR;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'&gt;'</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'&lt;'</span>:&#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">if</span> (ls-&gt;current == <span class="string">'='</span>) &#123;</span><br><span class="line">				next(ls);</span><br><span class="line">				<span class="keyword">return</span> TK_LESSEQUAL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (ls-&gt;current == <span class="string">'&lt;'</span>)</span><br><span class="line">			&#123;</span><br><span class="line">				next(ls);</span><br><span class="line">				<span class="keyword">return</span> TK_SHL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'&lt;'</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'='</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">if</span> (ls-&gt;current == <span class="string">'='</span>) &#123;</span><br><span class="line">				next(ls);</span><br><span class="line">				<span class="keyword">return</span> TK_EQUAL;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="string">'='</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">','</span>: &#123;</span><br><span class="line">			next(ls);</span><br><span class="line">			<span class="keyword">return</span> <span class="string">','</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'0'</span>: <span class="keyword">case</span> <span class="string">'1'</span>: <span class="keyword">case</span> <span class="string">'2'</span>: <span class="keyword">case</span> <span class="string">'3'</span>: <span class="keyword">case</span> <span class="string">'4'</span>:</span><br><span class="line">		<span class="keyword">case</span> <span class="string">'5'</span>: <span class="keyword">case</span> <span class="string">'6'</span>: <span class="keyword">case</span> <span class="string">'7'</span>: <span class="keyword">case</span> <span class="string">'8'</span>: <span class="keyword">case</span> <span class="string">'9'</span>: &#123;</span><br><span class="line">			<span class="keyword">return</span> str2number(ls, <span class="literal">false</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">default</span>: &#123;</span><br><span class="line">			<span class="comment">// TK_NAME or reserved name</span></span><br><span class="line">			<span class="keyword">if</span> (<span class="built_in">isalpha</span>(ls-&gt;current) || ls-&gt;current == <span class="string">'_'</span>) &#123;</span><br><span class="line">				<span class="keyword">while</span> (<span class="built_in">isalpha</span>(ls-&gt;current) || ls-&gt;current == <span class="string">'_'</span>) &#123;</span><br><span class="line">					<span class="comment">//将当前值存入ls-&gt;buffer中，并将下一个字符存入ls-&gt;current</span></span><br><span class="line">					save_and_next(ls-&gt;L, ls, ls-&gt;current);  </span><br><span class="line">				&#125;</span><br><span class="line">				save(ls-&gt;L, ls, <span class="string">'\0'</span>);</span><br><span class="line">				</span><br><span class="line">				TString* s = luaS_newlstr(ls-&gt;L, ls-&gt;buff-&gt;<span class="built_in">buffer</span>, <span class="built_in">strlen</span>(ls-&gt;buff-&gt;<span class="built_in">buffer</span>));</span><br><span class="line">				<span class="keyword">if</span> (s-&gt;extra &gt; <span class="number">0</span>) &#123;</span><br><span class="line">					<span class="keyword">return</span> s-&gt;extra;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					ls-&gt;t.seminfo.s = s;</span><br><span class="line">					<span class="keyword">return</span> TK_NAME;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123; <span class="comment">// single char</span></span><br><span class="line">				<span class="keyword">int</span> c = ls-&gt;current;</span><br><span class="line">				next(ls);</span><br><span class="line">				<span class="keyword">return</span> c;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> TK_EOS;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上面代码中，关键的部位，已经标上注释了。<br>   这里还需要注意的一点是，上面代码的default分支，我们在识别一个字符串以后，还需要判断一下它是否系统保留字，TString字符串中的extra字段&gt;0的就是系统保留字，这个extra的值就是该系统保留字的token id。所有的系统保留字都是短字符串，我们在Part3提到过，所有的短字符串都会被内部化，并且暂存在一个strt表中，lexer模块在初始化的时候，就会注册这些系统保留字了，其逻辑如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lualexer.c</span></span><br><span class="line"><span class="comment">// the sequence must be the same as enum RESERVED</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span>* luaX_tokens[] = &#123;</span><br><span class="line">	<span class="string">"local"</span>, <span class="string">"nil"</span>, <span class="string">"true"</span>, <span class="string">"false"</span>, <span class="string">"end"</span>, <span class="string">"then"</span>, <span class="string">"if"</span>, <span class="string">"elseif"</span>, <span class="string">"function"</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">luaX_init</span><span class="params">(struct lua_State* L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	TString* env = luaS_newliteral(L, LUA_ENV);</span><br><span class="line">	luaC_fix(L, obj2gco(env));</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; NUM_RESERVED; i++) &#123;</span><br><span class="line">		TString* reserved = luaS_newliteral(L, luaX_tokens[i]);</span><br><span class="line">		luaC_fix(L, obj2gco(reserved));</span><br><span class="line">		reserved-&gt;extra = i + FIRST_REVERSED;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>  接下来，我们以图1中的脚本代码为例子，来说明一下luaX_next的运作流程，其流程，我们可以简化为下面所示的伪代码：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">// 第一次调用luaX_next，第一个字符匹配到，它是一个TK_NAME类型的token</span><br><span class="line">// 并且连续从Zio模块读取字符，直至第一个字符<span class="string">'('</span>，不能匹配TK_NAME的规则</span><br><span class="line">// 此时将已经暂存的<span class="built_in">print</span>生成字符串，赋值给ls-&gt;t.seminfo.s上</span><br><span class="line">&lt;TK_NAME, <span class="string">"print"</span>&gt; = luaX_next()</span><br><span class="line"></span><br><span class="line">// 第二次调用luaX_next</span><br><span class="line">&lt;<span class="string">'('</span>, None&gt; = luaX_next()</span><br><span class="line"></span><br><span class="line">// 第三次调用luaX_next，因为ls-&gt;current是<span class="string">"字符，因此判定该token是TK_STRING，</span></span><br><span class="line"><span class="string">// 将"</span>后的字符全部读出，并暂存在ls-&gt;buffer中，当ls-&gt;current匹配到另一个<span class="string">"时，</span></span><br><span class="line"><span class="string">// 表示字符串已经完整读完，因此此时可以为其创建一个新的token，并将值赋给</span></span><br><span class="line"><span class="string">// ls-&gt;t.seminfo.s上</span></span><br><span class="line"><span class="string">&lt;TK_STRING, "</span>hello world<span class="string">"&gt; = luaX_next()</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">// 第四次调用luax_next</span></span><br><span class="line"><span class="string">&lt;')', None&gt; = luaX_next()</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">// 第五次调用luaX_next</span></span><br><span class="line"><span class="string">&lt;TK_EOS, None&gt; = luaX_next()</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">// 调用结束</span></span><br></pre></td></tr></table></figure></p>
<p>   现在将对Zio模块进行特别说明，我们前面已经说过LexState结构实例ls里，ls-&gt;current的值，是从Zio模块中获取的，而Zio模块又是将源码中的字符，读取BUFSIZE个到它的buff数组中，然后一个一个给ls-&gt;current，直到给完，再从文件中重新加载，之所以这样做的原因是，如果源文件很大，那么加载的过程将会很长，而我们常常又不可能，一次性就将代码写对，因此总会遇到一些词法上甚至是语法上的错误，因此如果每次加载编译，就直接读取完整的源文件，那么将会极大降低我们的调试效率，而每次又直接从文件里读取一个字符的速度又太慢，因此加多一个Zio缓冲模块，能够极大缓解这个问题。<br>   到这里为止，对于词法分析器的设计论述就完成了，关于代码实现，读者可以到compiler/luazio.h、compiler/luazio.c、compiler/lualexer.h和compiler/lualexer.c中查看源码。我相信只看这篇文章的论述是不够的，因此我希望读者能够深入源码去理解具体的实现方式，文章是勾勒出轮廓，而细节需要读者自己去探索，毕竟篇幅有限，不可能展示所有的细节。而且我已经将不同的部分拆分开来了，把很多无关的逻辑剖离，一步一步实现我们最终想要的解释器，相信读者跟着进度，也能够更加容易得进入研究lua解释器的状态中。</p>
<h1 id="语法分析器基础设计与实现"><a href="#语法分析器基础设计与实现" class="headerlink" title="语法分析器基础设计与实现"></a>语法分析器基础设计与实现</h1><p>   在完成了词法分析器的论述以后，现在我们开始进入到语法分析器基础的设计和实现上。本章涉及的语法分析器，并不是包含完整的功能，而是对于前面范式所定义的内容进行技术层面的实现。当然，本章的意义也非常重大，工作量并不意味着比后续的内容小，因为这是要搭建语法分析器最基础的框架，因此在构思上花费的时间甚至可能会更多。本节首先会从EBNF范式出发，讨论dummylua语法分析器的整体逻辑架构，然后再逐步深入各个细节。我们最终的目的也很清晰，就是能够编译并运行图1所示的脚本代码print(“hello world”)。<br>   回顾一下图2所示的代码，我们的脚本加载和编译的流程，都是在luaL_loadfile函数里进行的，因此我们Parser的运作流程，也是在这里面执行的，实际上，编译流程的入口函数，是一个叫做luaY_parser的函数，其函数声明如下所示：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.h</span></span><br><span class="line"><span class="function">LClosure* <span class="title">luaY_parser</span><span class="params">(struct lua_State* L, Zio* zio, MBuffer* <span class="built_in">buffer</span>, Dyndata* dyd, <span class="keyword">const</span> <span class="keyword">char</span>* name)</span></span></span><br></pre></td></tr></table></figure></p>
<p>我们先不深入里面的代码实现细节，只是为了告诉大家，我们编译在代码层面是从什么地方开始的。前面也提到过，我们在调用luaL_loadfile函数的时候，首先会创建一个LClosure结构的函数实例，并压入栈中，然后加载脚本并编译，最后将编译的结果放入LClosure结构的Proto结构里。我们本节的目标就是探讨这个编译流程。<br>   我们要被编译加载的脚本，可以被视作是一个chunk，因此它将被作为我们的研究起点，回顾一下官方lua的EBNF范式，对chunk的有关定义：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chunk ::= &#123;<span class="built_in">stat</span> [`;´]&#125; [laststat[`;´]]</span><br></pre></td></tr></table></figure></p>
<p>上面的production中，chunk由右边的nonterminal来定义。{}内的内容，表示会出现0次或者多次，而[]内的内容表示会出现0次或者1次。也就是说chunk是由0个、1个或者多个stat语句组成，可以有laststat和’;‘，也可以没有这两个。laststat的定义如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">laststat ::= <span class="built_in">return</span> [explist1]  |  <span class="built_in">break</span></span><br></pre></td></tr></table></figure></p>
<p>前面我们也已经定义过本章节要实现的EBNF范式规则，laststat并没有被纳入，因此本章不会讨论laststat相关的内容，为了简化问题，我也将’;‘忽略，也就是说我们现在要关注的起点是：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chunk ::= &#123;<span class="built_in">stat</span>&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们运行脚本是由stat组合而成，stat是statement的简写，它是我们构建命令的最基本的单位，官方lua对stat的定义如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">stat</span> ::=  varlist1 `=´ explist1  |</span><br><span class="line">         functioncall  |</span><br><span class="line">         <span class="keyword">do</span> block end  |</span><br><span class="line">         <span class="keyword">while</span> exp <span class="keyword">do</span> block end  |</span><br><span class="line">         repeat block until exp  |</span><br><span class="line">         <span class="keyword">if</span> exp <span class="keyword">then</span> block &#123;elseif exp <span class="keyword">then</span> block&#125; [<span class="keyword">else</span> block] end  |</span><br><span class="line">         <span class="keyword">for</span> Name `=´ exp `,´ exp [`,´ exp] <span class="keyword">do</span> block end  |</span><br><span class="line">         <span class="keyword">for</span> namelist <span class="keyword">in</span> explist1 <span class="keyword">do</span> block end  |</span><br><span class="line">         <span class="keyword">function</span> funcname funcbody  |</span><br><span class="line">         <span class="built_in">local</span> <span class="keyword">function</span> Name funcbody  |</span><br><span class="line">         <span class="built_in">local</span> namelist [`=´ explist1]</span><br></pre></td></tr></table></figure></p>
<p>内容比较丰富，本章并不会对上面所有的分支进行论述，这里只是为了给读者提供一个最基本的概念。实际上官方lua的statement分支，全在lparser.c的statement函数里：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lua-5.3.5 lparser.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">statement</span> <span class="params">(LexState *ls)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> <span class="built_in">line</span> = ls-&gt;linenumber;  <span class="comment">/* may be needed for error messages */</span></span><br><span class="line">  enterlevel(ls);</span><br><span class="line">  <span class="keyword">switch</span> (ls-&gt;t.token) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">';'</span>: &#123;  <span class="comment">/* stat -&gt; ';' (empty statement) */</span></span><br><span class="line">      luaX_next(ls);  <span class="comment">/* skip ';' */</span></span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_IF: &#123;  <span class="comment">/* stat -&gt; ifstat */</span></span><br><span class="line">      ifstat(ls, <span class="built_in">line</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_WHILE: &#123;  <span class="comment">/* stat -&gt; whilestat */</span></span><br><span class="line">      whilestat(ls, <span class="built_in">line</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_DO: &#123;  <span class="comment">/* stat -&gt; DO block END */</span></span><br><span class="line">      luaX_next(ls);  <span class="comment">/* skip DO */</span></span><br><span class="line">      block(ls);</span><br><span class="line">      check_match(ls, TK_END, TK_DO, <span class="built_in">line</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_FOR: &#123;  <span class="comment">/* stat -&gt; forstat */</span></span><br><span class="line">      forstat(ls, <span class="built_in">line</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_REPEAT: &#123;  <span class="comment">/* stat -&gt; repeatstat */</span></span><br><span class="line">      repeatstat(ls, <span class="built_in">line</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_FUNCTION: &#123;  <span class="comment">/* stat -&gt; funcstat */</span></span><br><span class="line">      funcstat(ls, <span class="built_in">line</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_LOCAL: &#123;  <span class="comment">/* stat -&gt; localstat */</span></span><br><span class="line">      luaX_next(ls);  <span class="comment">/* skip LOCAL */</span></span><br><span class="line">      <span class="keyword">if</span> (testnext(ls, TK_FUNCTION))  <span class="comment">/* local function? */</span></span><br><span class="line">        localfunc(ls);</span><br><span class="line">      <span class="keyword">else</span></span><br><span class="line">        localstat(ls);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_DBCOLON: &#123;  <span class="comment">/* stat -&gt; label */</span></span><br><span class="line">      luaX_next(ls);  <span class="comment">/* skip double colon */</span></span><br><span class="line">      labelstat(ls, str_checkname(ls), <span class="built_in">line</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_RETURN: &#123;  <span class="comment">/* stat -&gt; retstat */</span></span><br><span class="line">      luaX_next(ls);  <span class="comment">/* skip RETURN */</span></span><br><span class="line">      retstat(ls);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> TK_BREAK:   <span class="comment">/* stat -&gt; breakstat */</span></span><br><span class="line">    <span class="keyword">case</span> TK_GOTO: &#123;  <span class="comment">/* stat -&gt; 'goto' NAME */</span></span><br><span class="line">      gotostat(ls, luaK_jump(ls-&gt;fs));</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">default</span>: &#123;  <span class="comment">/* stat -&gt; func | assignment */</span></span><br><span class="line">      exprstat(ls);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  lua_assert(ls-&gt;fs-&gt;f-&gt;maxstacksize &gt;= ls-&gt;fs-&gt;freereg &amp;&amp;</span><br><span class="line">             ls-&gt;fs-&gt;freereg &gt;= ls-&gt;fs-&gt;nactvar);</span><br><span class="line">  ls-&gt;fs-&gt;freereg = ls-&gt;fs-&gt;nactvar;  <span class="comment">/* free registers */</span></span><br><span class="line">  leavelevel(ls);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们需要关注的是它的switch语句，里面的case，基本上对应了一个独立的stat函数，这些stat函数分别实现了不同的stat分支，我们可以用图25来表示<br><img src="statments.png" alt><br>回顾一下上面EBNF有关stat的定义，如果要将里面的nonterminal和图25的stat做对应关系的话，那么将如下表所示：<br><img src="20210408142904.png" alt><br>由于我们本章的内容，是为了实现对图1脚本代码的编译和执行，因此就是要实现functioncall的范式描述，我们再次回顾一下上文中，有关我们本文要实现的范式那里，我们的functioncall定义则如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">functioncall ::= prefixexp args</span><br><span class="line"></span><br><span class="line">prefixexp 	 ::= var</span><br><span class="line">var 		 ::= Name 	       </span><br><span class="line">args 		 ::= <span class="string">'('</span> [explist] <span class="string">')'</span></span><br><span class="line">explist      ::= &#123;exp <span class="string">','</span>&#125; exp</span><br><span class="line">exp          ::= nil  |  <span class="literal">false</span>  |  <span class="literal">true</span>  |  Number  |  String</span><br></pre></td></tr></table></figure></p>
<p>结合上表，其实我们只需要聚焦exprstat这个分支就可以了，与此同时，我们也忽略了赋值语句部分。<br>        在进一步深入探讨functioncall范式的编译流程实现之前，首先我要向大家介绍一下Parser的几个重要的数据结构，这个数据结构是Parser能够运作的重要基础：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.h</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">FuncState</span> &#123;</span></span><br><span class="line">	<span class="keyword">int</span> firstlocal;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">FuncState</span>* <span class="title">prev</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">LexState</span>* <span class="title">ls</span>;</span></span><br><span class="line">	Proto* p;</span><br><span class="line">	<span class="keyword">int</span> pc;				<span class="comment">// Proto结构code数组中，下一个可被写入的位置的下标</span></span><br><span class="line">	<span class="keyword">int</span> nk;				<span class="comment">// Proto结构常量数组k中，下一个可被写入的位置的下标</span></span><br><span class="line">	<span class="keyword">int</span> nups;			<span class="comment">// Proto结构upvalue数组中，下一个可被写入的位置的下标</span></span><br><span class="line">	<span class="keyword">int</span> nlocvars;		<span class="comment">// 标识已经有多少个local变量了</span></span><br><span class="line">	<span class="keyword">int</span> nactvars;		<span class="comment">// 活跃变量的数量，一般指local变量</span></span><br><span class="line">	<span class="keyword">int</span> np;				<span class="comment">// Proto结构实例中，proto列表p中proto实例的个数</span></span><br><span class="line">	<span class="keyword">int</span> freereg;		<span class="comment">// 能够被使用的寄存器位置</span></span><br><span class="line">&#125; FuncState;</span><br></pre></td></tr></table></figure></p>
<p>由于我们目前没有涉及local变量、activate变量、upvalue相关的内容，因此这些部分可以暂时被忽略。这个数据结构为什么重要？因为它记录了我们新的常量要被存储在Proto结构中的什么位置，记录了下一个可被使用的寄存器位置，记录了新生成的指令要存放在哪里等。用于记录这些信息的变量，会随着编译流程的持续执行而动态变化。<br>  现在我们来看一下另外一个重要的数据结构，这个数据结构主要是用来临时存放当前表达式信息的。其用途本质就是临时存放一个表达式的类型和信息，Parser在遇到下一个exp的时候，会将存放在这个结构里的上一个表达式，转换成虚拟机指令：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.h</span></span><br><span class="line"><span class="comment">// 表达式(exp)的类型</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">enum</span> expkind &#123;</span><br><span class="line">	VVOID,			<span class="comment">// 表达式是空的，也就是void</span></span><br><span class="line">	VNIL,			<span class="comment">// 表达式是nil类型</span></span><br><span class="line">	VFLT,			<span class="comment">// 表达式是浮点类型</span></span><br><span class="line">	VINT,			<span class="comment">// 表达式是整型类型</span></span><br><span class="line">	VTRUE,			<span class="comment">// 表达式是TRUE</span></span><br><span class="line">	VFALSE,			<span class="comment">// 表达式是FALSE</span></span><br><span class="line">	VINDEXED,		<span class="comment">// 表示索引类型，当exp是这种类型时，expdesc的ind域被使用</span></span><br><span class="line">	VCALL,			<span class="comment">// 表达式是函数调用，expdesc中的info字段，表示的是instruction pc</span></span><br><span class="line">					<span class="comment">// 也就是它指向Proto code列表的哪个指令</span></span><br><span class="line">	VLOCAL,			<span class="comment">// 表达式是local变量，expdesc的info字段，表示该local变量，在栈中的位置</span></span><br><span class="line">	VUPVAL,			<span class="comment">// 表达式是Upvalue，expdesc的info字段，表示Upvalue数组的索引</span></span><br><span class="line">	VK,				<span class="comment">// 表达式是常量类型，expdesc的info字段表示，这个常量是常量表k中的哪个值</span></span><br><span class="line">	VRELOCATE,		<span class="comment">// 表达式可以把结果放到任意的寄存器上，expdesc的info表示的是instruction pc</span></span><br><span class="line">	VNONRELOC,		<span class="comment">// 表达式已经在某个寄存器上了，expdesc的info字段，表示该寄存器的位置</span></span><br><span class="line">&#125; expkind;</span><br><span class="line"></span><br><span class="line"><span class="comment">// exp临时存储结构</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">expdesc</span> &#123;</span></span><br><span class="line">	expkind k;				<span class="comment">// expkind</span></span><br><span class="line">	<span class="keyword">union</span> &#123;</span><br><span class="line">		<span class="keyword">int</span> info;			</span><br><span class="line">		lua_Integer i;		<span class="comment">// for VINT</span></span><br><span class="line">		lua_Number r;		<span class="comment">// for VFLT</span></span><br><span class="line"></span><br><span class="line">		<span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">			<span class="keyword">int</span> t;		<span class="comment">// 表示table或者是UpVal的索引</span></span><br><span class="line">			<span class="keyword">int</span> vt;		<span class="comment">// 标识上一个字段't'是upvalue(VUPVAL) 还是 table(VLOCAL)</span></span><br><span class="line">			<span class="keyword">int</span> idx;	<span class="comment">// 常量表k或者是寄存器的索引，这个索引指向的值就是被取出值得key</span></span><br><span class="line">						<span class="comment">// 不论t是Upvalue还是table的索引，它取出的值一般是一个table</span></span><br><span class="line">		&#125; ind;</span><br><span class="line">	&#125; u;</span><br><span class="line">&#125; expdesc;</span><br></pre></td></tr></table></figure></p>
<p>现在我们来举几个简单的例子来看看，这个expdesc的实际用途，下面的伪代码展示了几个使用的例子：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> lv <span class="comment">-- lua</span></span><br><span class="line"></span><br><span class="line">expdesc.expkind = VLOCAL</span><br><span class="line">expdesc.info = <span class="number">0</span></span><br></pre></td></tr></table></figure></p>
<p>上面所示的伪代码中，因为lv是第一个local变量，因此它会被安排在栈中的第0个位置，所以根据定义我们的expdesc.info要指向这个位置，并且将lv这个名称存在Proto的localvar列表中，当我们通过lv去查找local变量在哪里时，首先会去Proto的localvar列表中，通过名称去匹配，当找到匹配项时，该匹配项指向的位置，就是该local value所在的位置。现在再来看一个例子：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="number">3.14</span> <span class="comment">-- lua</span></span><br><span class="line"></span><br><span class="line">expdesc.expkind = VFLT</span><br><span class="line">expdesc.u.r = <span class="number">3.14</span></span><br><span class="line"></span><br><span class="line"><span class="number">10</span> <span class="comment">-- lua</span></span><br><span class="line"></span><br><span class="line">expdesc.expkind = VINT</span><br><span class="line">expdesc.u.i = <span class="number">10</span></span><br></pre></td></tr></table></figure></p>
<p>上面两个例子分别对应浮点数和整数的情况，由于比较简单这里不作额外的解释。我们现在来看一个索引的例子：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">second_upvalue[<span class="string">"key"</span>]  -- lua</span><br><span class="line"></span><br><span class="line">expdesc.expkind = VINDEXED</span><br><span class="line">expdesc.u.idx.t = 1  -- second_upvalue是当前被调用函数的第二个upvalue</span><br><span class="line">expdesc.u.vt = VUPVAL</span><br><span class="line">expdesc.u.idx = 0  -- 假设<span class="string">"key"</span>在常量表的第0个位置</span><br></pre></td></tr></table></figure></p>
<p>现在来看一个引用全局表的例子：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">global_table[<span class="string">"key"</span>] -- lua</span><br><span class="line"></span><br><span class="line">expdesc.expkind = VINDEXED</span><br><span class="line">-- global_table是在_G中，_ENV是每个LClosure的第1个Upvule(索引是0)，而_ENV默认指向_G</span><br><span class="line">-- 所以t是0，指向_ENV</span><br><span class="line">expdesc.u.ind.t = 0 </span><br><span class="line">expdesc.u.ind.vt = VUPVAL</span><br><span class="line">expdesc.u.ind.idx = 0 -- 假设<span class="string">"key"</span>在常量表的第0个位置</span><br></pre></td></tr></table></figure></p>
<p>我们最后来看一个local table引用的例子：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">local_table[<span class="string">"key"</span>] -- lua</span><br><span class="line"></span><br><span class="line">expdesc.expkind = VINDEXED</span><br><span class="line">-- 假设local_table是chunk内第一个<span class="built_in">local</span> value，因此它在栈中，且索引值为0</span><br><span class="line">expdesc.u.ind.t = 0 </span><br><span class="line">expdesc.u.ind.vt = VLOCAL</span><br><span class="line">expdesc.u.ind.idx = 0 -- 假设<span class="string">"key"</span>在常量表第0个位置</span><br></pre></td></tr></table></figure></p>
<p>上面三个索引的例子，就是VINDEXED类别的全部情况，我们可以看到ind.t指向的，要么是个upvalue，要么是栈的位置，而不管它指向哪里，从那里取出来的值一定是个table类型，所以我们可以通过ind.idx所指向的常量表的值，作为key，并以该key对应的value作为目标值。<br>   介绍完基本的数据结构以后，我们就可以来看一下，我们到底如何处理functioncall的编译流程。图26为我们展示了functioncall的解析逻辑树，我们会陆续从源码中获取token，而这些按顺序获取的token类型，必须和节点里规定的token匹配，否则就要抛出syntax error。<br><img src="functioncall.png" alt><br>   上图的解释比较抽象，现在假设我们的part05_test.lua的逻辑做出如下修改，并且用图2所示的C语言代码对其进行编译和执行，那么则有：<br><figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- part05_test.lua</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">"Good bye "</span>, <span class="number">2019</span>, <span class="string">"Hello "</span>, <span class="number">2020</span>)</span><br></pre></td></tr></table></figure></p>
<p>当我们开始调用luaL_loadfile函数，这个函数首先会创建一个LClosure结构的实例，并压入栈中，并打开part05_test.lua文件，此时内存布局如图27所示：<br><img src="functioncall2.png" alt><br>在完成准备工作之后，我们就进入到luaY_parser的函数逻辑里了，此时lexer读取第一个token，”print”，判断得到token的类别为TK_NAME，符合图26中，第一步对token的要求，事实上该步骤是进入到了一个叫做suffixexp的逻辑流程中，所谓的suffixexp是指TK_NAME[TK_NAME]或TK_NAME(explist)这种形式，前者是访问table的某个域，后者就是我们的functioncall调用了，而上面所示的两个表达式中，第一个TK_NAME就是primaryexp，我们现在来看一个实例：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&lt;-------------------suffixexp------------------&gt;</span><br><span class="line">|<span class="built_in">print</span>      (<span class="string">"Good bye "</span>, 2019, <span class="string">"Hello "</span>, 2020)|</span><br><span class="line">&lt;primaryexp&gt;</span><br></pre></td></tr></table></figure></p>
<p>这个例子中，我们的值为”print”的TK_NAME型token，就是作为primaryexp存在的，因此需要将其填入到expdesc的结构中，目前我们对primaryexp的处理逻辑，是查找print所在的位置，并将对应信息写到expdesc结构上。primaryexp查找TK_NAME这种类型的token，本质就是查找一个变量名，查找变量名的代码如下所示(入口函数是singlevar函数)：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// luaparser.c</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">init_exp</span><span class="params">(expdesc* e, expkind k, <span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">	e-&gt;k = k;</span><br><span class="line">	e-&gt;u.info = i;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> LocVar* <span class="title">getlocvar</span><span class="params">(FuncState* fs, <span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">	LexState* ls = fs-&gt;ls;</span><br><span class="line">	<span class="keyword">int</span> idx = ls-&gt;dyd-&gt;actvar.arr[fs-&gt;firstlocal + n];</span><br><span class="line">	lua_assert(idx &lt; fs-&gt;nlocvars);</span><br><span class="line">	<span class="keyword">return</span> &amp;fs-&gt;p-&gt;locvars[idx];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// search local var for current function</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">searchvar</span><span class="params">(FuncState* fs, TString* name)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = fs-&gt;nactvars - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">		<span class="function"><span class="keyword">if</span> <span class="title">eqstr</span><span class="params">(getlocvar(fs, i)-&gt;varname, name)</span> </span>&#123;</span><br><span class="line">			<span class="keyword">return</span> i;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">searchupvalues</span><span class="params">(FuncState* fs, expdesc* e, TString* n)</span> </span>&#123;</span><br><span class="line">	Proto* p = fs-&gt;p;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; fs-&gt;nups; i++) &#123;</span><br><span class="line">		<span class="keyword">if</span> (eqstr(p-&gt;upvalues[i].name, n)) &#123;</span><br><span class="line">			init_exp(e, VUPVAL, i);</span><br><span class="line">			<span class="keyword">return</span> i;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">singlevaraux</span><span class="params">(FuncState* fs, expdesc* e, TString* n)</span> </span>&#123;</span><br><span class="line">	<span class="comment">// can not find n in local variables, try to find it in global</span></span><br><span class="line">	<span class="keyword">if</span> (fs == <span class="literal">NULL</span>) &#123;</span><br><span class="line">		init_exp(e, VVOID, <span class="number">0</span>);</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">int</span> reg = searchvar(fs, n);</span><br><span class="line">	<span class="keyword">if</span> (reg &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">		init_exp(e, VLOCAL, reg);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> &#123; </span><br><span class="line">		<span class="comment">// can not find it in local variables, then try upvalues</span></span><br><span class="line">		reg = searchupvalues(fs, e, n);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// try to find in parent function</span></span><br><span class="line">		<span class="keyword">if</span> (reg &lt; <span class="number">0</span>) &#123;</span><br><span class="line">			singlevaraux(fs-&gt;prev, e, n);</span><br><span class="line">			<span class="keyword">if</span> (e-&gt;k != VVOID) &#123;</span><br><span class="line">				reg = newupvalues(fs, e, n);</span><br><span class="line">				init_exp(e, VUPVAL, reg);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> reg;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// First, search local variable, if it is not exist, then it will try to search it's upvalues</span></span><br><span class="line"><span class="comment">// if it also not exist, then try to search it in _ENV</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">singlevar</span><span class="params">(FuncState* fs, expdesc* e, TString* n)</span> </span>&#123;</span><br><span class="line">	singlevaraux(fs, e, n);</span><br><span class="line">	<span class="keyword">if</span> (e-&gt;k == VVOID) &#123; <span class="comment">// variable is in global</span></span><br><span class="line">		expdesc k;</span><br><span class="line">		singlevaraux(fs, e, fs-&gt;ls-&gt;env);</span><br><span class="line">		lua_assert(e-&gt;k == VUPVAL);</span><br><span class="line">		codestring(fs, n, &amp;k);</span><br><span class="line">		luaK_indexed(fs, e, &amp;k);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上面所示的代码，基本遵循一个流程：<br>1) 查找一个变量，先查找自己的locvar列表，如果找到则生成VLOCAL类型的expdesc，没找到就进入下一步<br>2) 查找自己的upvalue列表，如果找到，就生成VUPVAL类型的expdesc，没找到就进入下一步<br>3) 查找父函数的local列表，如果找到同名的变量名，则将该变量注册为自己新的upvalue后，执行第2步，如果没查找到，则进入下一步<br>4) 查找父函数的upvalue列表，如果找到同名的变量名，则将该变量注册为自己的upvalue后，执行第2步，如果没找到，自己是top level function的话，则执行第5步，否则返回第3步<br>5) 查找_ENV，如果找到同名的变量名，则将该变量注册为自己的upvalue后，生成VINDEXED类型的expdesc<br>   我们前面讨论到，调用luaL_loadfile函数时，它首先会做一些准备工作，并生成了图27那样的内存布局，然后就要开始调用luaY_parser函数进行编译了，本章编译的整体流程如图26所示，编译的脚本，我们也做出了调整，现在开始执行图26中的第1步，我们从脚本part05_test.lua中，读取第一个token &lt; TK_NAME, “print” &gt;，由于token id符合第1步骤的要求，因此判定其语法合法(否则抛出syntax error)，此时的&lt; TK_NAME, “print” &gt;，是一个primaryexp，我们要去查找”print”在什么位置，并生成合适的expdesc。由于脚本中没有local变量，除了_ENV又没有其他upvalue，而_ENV默认指向_G，因此我们会在全局表_G中查找”print”，于是我们生成了图28的结构了：<br><img src="functioncall4.png" alt><br>标红的部分，就是这次更新的部分，我们可以看到，“print”被注册到了Proto的常量表k中了，我们所有的常量(包括TK_NAME、TK_STRING、TK_FLOAT、TK_INT等)都会被注册到常量表中，然后我们可以看到，expdesc的kind被设置为了VINDEXED类型，ind.vt被设置为VUPVAL，表示ind.t指向一个UpVal，由于它的值是0，因此它指向了_ENV，然后_ENV默认指向_G，因此这两个组合实质就是表示我们的值要从_G里取得。最后我们观察到ind.idx的值是256，前面我们也已经说过，ind.idx指向一个寄存器(栈上的位置)或者是常量表的某个位置，当ind.idx&lt; 256时，它就是R(ind.idx)，当ind.idx &gt;= 256时，它就是Kst(ind.idx - 256)，在这里它的值是256，因此它就是指Kst(0)，也就是”print”，综合起来看，这个expdesc表达的含义如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">// 因为expkind为</span><br><span class="line">UpVal[expdesc.u.ind.t][Kst(expdesc.u.ind.idx - 256)]</span><br><span class="line">==&gt;</span><br><span class="line">UpVal[0][Kst(0)]</span><br><span class="line">==&gt;</span><br><span class="line">_G[Kst(0)]</span><br><span class="line">==&gt;</span><br><span class="line">_G[<span class="string">"print"</span>]</span><br></pre></td></tr></table></figure></p>
<p>expdesc是个非常重要的数据结构，它是临时存储我们表达式的重要变量，而我们在编译的过程中，往往又需要复用这个结构，以节约内存和提升效率。<br>   在完成图26的第一步以后，我们进入到第二个步骤，此时我们读取下一个token &lt; ‘(’, None &gt;, 这个token符合我们第二个步骤的要求，因此它可以继续执行(否则就要报syntax error)，识别了这个token，意味着后续可能会有一个explist，它们同样可能会将自己的信息写入到expdesc结构中，而我们的expdesc结构又是共享的，而且它内部又包含上一个表达式的值了，因此我们此时需要将它消费掉，此时我们需要将expdesc转换成instruction，因此我们得到图29<br><img src="functioncall5.png" alt><br>图中橙色的部分，就是本次发生变更的部分，我们已经知道expdesc存放的实质是一个_G[“print”]的表达式，它表达的是把key为”print”的value取出来，那么它将存放到哪里去呢？在FuncState结构的freereg变量更新之前，它是0，这个变量表示的是当前能够被写入的寄存器是哪个，也就是如果有一个表达式要对栈进行写入，那么这个freereg就指明了它可以写入的位置。由于当前expdesc所表达的含义已经非常清晰了，因此将其转换成虚拟机指令，最匹配的则是OP_GETTABUP指令，它表示从upvalue中，取出一个值为table的变量，再从它取出一个key为R( C )的value，于是生成了&lt; OP_GETTABUP 0 0 256 &gt;这个指令(真实的指令是一个经过组合的整数值，这里只是逻辑表示)，其中其A值由FuncState的freereg来指定，其B值由expdesc结构里的ind.t来指定，它的OPCODE由ind.vt(VUPVAL)来决定，它的C值由ind.idx来决定，此时我们就可以清晰地了解到，expdesc、FuncState与生成指令的部分关联了。在完成指令生成以后，FuncState的freereg的值自增1。<br>        完成第二步的逻辑以后，我们接下来进行到图26所示的解析树里的第三步，这里我们将对explist进行解析，所谓explist，就是’(‘和’)‘包起来的通过’,‘隔开的表达式exp的集合。本章前面定义的范式，已经非常清楚了，只要我们的explist里的exp符合以下terminal集合中的其中一个，那么这个exp我们可以认为是合法的：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">exp          ::= nil  |  <span class="literal">false</span>  |  <span class="literal">true</span>  |  Number  |  String</span><br></pre></td></tr></table></figure></p>
<p>接下来，我们通过词法分析器的luaX_next函数，来获取下一个token，&lt; TK_STRING, “Good Bye ” &gt;，显而易见，我们的token符合上面的范式，这是一个合法的token，现在我们要填信息到expdesc中了，此时我们需要expdesc去描述这个exp，因为它的值是”Good Bye “，一个字符串类型，因此它需要被注册到常量表k中，同时我们需要指明它是在k中的哪个位置，因此expkind为VK，而expdesc的info则为1：<br><img src="functioncall6.png" alt><br>上图的蓝色部分，则表明了更新的信息。随着我们的语法分析器继续运作，我们获得了下一个token，&lt; ‘,’ , None &gt;，在explist中，遇到这个token，意味着前一个被缓存在expdesc中的exp，应当立马被转换为虚拟机指令，与expdesc的VK类型对应的，则是OP_LOADK指令。前面我已经提到过，OP_LOADK的作用是将常量表中的某个值，读到目标寄存器上，这里生成了&lt; OP_LOADK 1 1 &gt;，意思是将常量表中，k[1]的值，放入寄存器中，也就是：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">R(A) = Kst(n)</span><br><span class="line">==&gt;</span><br><span class="line">R(1) = Kst(1)</span><br><span class="line">==&gt;</span><br><span class="line">R(1) = <span class="string">"Good Bye "</span></span><br></pre></td></tr></table></figure></p>
<p>在虚拟机指令&lt; OP_LOADK 1 1 &gt;中，他的A值是由FuncState的freereg变量指定，而它的B值是由expdesc中的info字段指定，在虚拟机指令生成以后，FuncState的freereg就自增1变成2了(表示前面两个位置已经被占用了)。前面我也说过，真实的虚拟机指令，其实是一个int型变量，我们前面的标记，只是为了方便理解而写的，这也是为什么，虚拟机指令只记录OPCODE以及一些索引参数的原因了。在完成虚拟机指令生成以后，就变成了图31的样子：<br><img src="functioncall7.png" alt><br>图中淡紫色部分就是更新的部分。同理，接下来的exp操作，与上面的流程类似，我们直接输出图32，这是执行过后的结果。<br><img src="functioncall8.png" alt><br>淡紫色的就是发生变更的部分。写到这里，我们图26中的第三个编译步骤就完成了。<br>   现在进入到了第4个编译步骤，我们继续通过词法分析器的luaX_next函数获取最后一个token，&lt; ‘)’ , None &gt;，该token符合第4步对token的要求，因此是合法的(否则抛出syntax error)，遇到这个token，意味着我们的explist已经结束，存在expdesc里的表示获取2020这个值的描述结构，也需要将其转换成虚拟机指令&lt; OP_LOADK 4 4 &gt;，最后functioncall要表达的是调用一个函数，现在函数和参数入栈的指令也已经生成，因此需要生成调用函数的指令OP_CALL, 这个指令需要我们指定，它要调用的函数位于栈的哪个位置，有多少个参数，几个返回值。OP_CALL是iABC模式，其中A是要被调用的函数，在栈中的位置，B是参数的个数+1，C是返回值个数+1，我们可以回顾一下已经生成的指令里，”print”函数是被读到R(0)的位置，也就是栈底的位置，因此A的值就是0，因为我们有4个参数，所以B的值是5，而这个print函数没有返回值，所以C是1，最终得到&lt; OP_CALL 0 5 1 &gt;，我们最终得到图33所示的结果：<br><img src="functioncall9.png" alt><br>到这里为止，我关于编译本章涉及到的编译器相关的内容就全部介绍完了。在了解流程之后，我还是希望读者能够自己去看看part5的源码，去了解更多的实现细节，文章只是阐述了它的基本数据结构和主要流程，更多的细节无法在有限的篇幅内一一展现，因此只有读者自己深入源码，才能够做到彻底理解和掌握，这里再次贴上dummylua的连接地址：dummylua项目。</p>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>  本文开篇介绍了，我们最终要实现的目标，以一个简单的脚本代码作为例子，贯穿了整篇博文。我先介绍了lua解释器的整体运作流程，介绍了lua代码的反编译工具，介绍了lua的虚拟指令集，以及虚拟机的整体运作框架。接着又论述了编译器的概念，EBNF范式，词法分析器和语法分析器。内容比较多，但是希望对大家有所帮助。<br>   经过十几天的浴血奋战，我完成了这篇blog的编写，耗费了大量的时间和精力，我总共投入了不少于100小时到这篇文章中，本篇的重大意义在于，我的dummylua正式进入到编译器和虚拟机相关的部分，真正的挑战开始了，后续我也将继续投入大量的时间，完成剩下的部分。最后，原创不易，希望大家转载的时候注明出处。</p>
<p>Reference<br>[1] <a href="https://pgl.yoyo.org/lua/docs/luaarchitecture.doc" rel="external nofollow noopener noreferrer" target="_blank">The Lua Architecture</a> 图片位于Lua: An Embedded Script Language这个章节中：Figure 1: process of initializing Lua and loading a script file<br>[2] <a href="https://en.wikipedia.org/wiki/Bytecode" rel="external nofollow noopener noreferrer" target="_blank">Wikipedia Bytecode</a><br>[3] <a href="https://www.lua.org/manual/4.0/luac.html" rel="external nofollow noopener noreferrer" target="_blank">LUAC man page</a><br>[4] <a href="http://luaforge.net/docman/83/98/ANoFrillsIntroToLua51VMInstructions.pdf" rel="external nofollow noopener noreferrer" target="_blank">ANoFrillsIntroToLua51VMInstructions 2 Lua Instruction Basics</a><br>[5] <a href="https://www.lua.org/pil/1.1.html" rel="external nofollow noopener noreferrer" target="_blank">Programing in Lua:1.1 Chunks</a><br>[6] [Engineer A Compiler 1.1 Introduction Overview]<br>[7] [Engineer A Compiler 1.1 Introduction Overview] Many research compilers produce C programs as their output. Because compilers for C are available on most computers, this makes the target program executable on all those systems, at the cost of an extra compilation for the final target. Compilers that target programming languages rather than the instruction set of a computer are often called source-to-source translators.<br>[8] <a href="http://www.cs.umsl.edu/~janikow/cs4280/EBNF.pdf" rel="external nofollow noopener noreferrer" target="_blank">Microsoft Word EBNF_m</a><br>[9] <a href="http://parrot.github.io/parrot-docs0/0.4.10/html/languages/lua/doc/lua51.EBNF.html" rel="external nofollow noopener noreferrer" target="_blank">lua51.EBNF</a><br>[10] <a href="https://www.runoob.com/regexp/regexp-syntax.html" rel="external nofollow noopener noreferrer" target="_blank">正则表达式语法</a></p>
]]></content>
      <categories>
        <category>lua</category>
        <category>构建Lua解释器</category>
      </categories>
      <tags>
        <tag>lua</tag>
        <tag>构建Lua解释器</tag>
      </tags>
  </entry>
</search>
